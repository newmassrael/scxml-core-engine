# SCE Mesh compile verification — shared memory transport binding
version: "1.0"
topology:
  ecu1:
    machines:
      brake:
        source: brake.scxml
        bindings:
          "#motor":
            transport: shm
      motor:
        source: motor.scxml
