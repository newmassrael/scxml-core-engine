name: W3C SCXML Compliance Tests

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      log-level:
        description: 'Log level for test execution'
        required: false
        default: 'error'
        type: choice
        options:
          - trace
          - debug
          - info
          - warn
          - error
          - off
      specific-tests:
        description: 'Specific test numbers (comma-separated, e.g., 531,518,519) or empty for all'
        required: false
        default: ''

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ============================================================
  # C++ (Interpreter + AOT) — 404 tests
  # ============================================================
  test-cpp:
    name: C++ W3C Tests
    runs-on: ubuntu-latest
    permissions:
      checks: write
      pull-requests: write
      contents: write

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Install Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          build-essential \
          cmake \
          g++ \
          meson \
          ninja-build \
          lld \
          ccache \
          pkg-config \
          libxml2-dev \
          libspdlog-dev \
          libfmt-dev \
          libboost-dev

    - name: Setup Problem Matchers
      run: |
        echo "::add-matcher::.github/problem-matchers/gcc.json"
        echo "::add-matcher::.github/problem-matchers/cmake.json"

    - name: Install Python Dependencies
      run: |
        python3 -m pip install --upgrade pip
        pip install -r tools/codegen/requirements.txt

    - name: Setup Emscripten (emsdk)
      run: |
        git clone https://github.com/emscripten-core/emsdk.git
        cd emsdk
        ./emsdk install latest
        ./emsdk activate latest
        cd ..

    - name: Cache CMake Build
      uses: actions/cache@v4
      with:
        path: |
          build/CMakeCache.txt
          build/CMakeFiles
          build/_deps
        key: ${{ runner.os }}-cmake-${{ github.repository }}-${{ hashFiles('**/CMakeLists.txt') }}
        restore-keys: |
          ${{ runner.os }}-cmake-${{ github.repository }}-

    - name: Setup ccache
      uses: actions/cache@v4
      with:
        path: ~/.ccache
        key: ${{ runner.os }}-ccache-${{ github.sha }}
        restore-keys: |
          ${{ runner.os }}-ccache-

    - name: Verify lld Installation
      run: |
        echo "=== Checking lld installation ==="
        which ld.lld || echo "Warning: ld.lld not found in PATH"
        ld.lld --version || echo "Warning: ld.lld version check failed"

    - name: Configure CMake
      run: |
        cmake -B build \
          -DCMAKE_BUILD_TYPE=RelWithDebInfo \
          -DCMAKE_CXX_COMPILER=g++ \
          -G Ninja

    - name: Build
      run: |
        echo "=== Starting build with timing ==="
        time cmake --build build --target w3c_test_cli --parallel $(nproc)

    - name: Verify Resources
      run: |
        if [ ! -d "resources" ]; then
          echo "Error: resources directory not found"
          exit 1
        fi
        txml_count=$(find resources -name '*.txml' | wc -l)
        test_dirs=$(find resources -mindepth 1 -maxdepth 1 -type d | wc -l)
        echo "Resources directory found:"
        echo "  - Test directories: $test_dirs"
        echo "  - TXML files: $txml_count"

    - name: Debug Environment
      run: |
        echo "=== Current Directory ==="
        pwd
        echo ""
        echo "=== Resources Directory Check ==="
        if [ -d "resources" ]; then
          echo "Resources found at: $(pwd)/resources"
          ls -la resources | head -n 10
        else
          echo "ERROR: resources directory not found!"
          echo "Directory contents:"
          ls -la
        fi
        echo ""
        echo "=== Build Directory ==="
        ls -lh build/tests/w3c_test_cli
        echo ""
        echo "=== Executable Check ==="
        file build/tests/w3c_test_cli
        echo ""
        echo "=== Library Dependencies ==="
        ldd build/tests/w3c_test_cli || echo "ldd failed or static binary"
        echo ""
        echo "=== Test Executable Help ==="
        build/tests/w3c_test_cli --help || echo "Failed to run --help"

    - name: Run W3C SCXML Test Suite
      run: |
        cd build/tests
        ulimit -c unlimited

        echo "=== Pre-execution Environment ==="
        echo "Current directory: $(pwd)"
        echo "Resources search from: $(pwd)"
        echo "Project root should be: $(pwd)/../.."
        ls -la ../../resources | head -n 5 || echo "../../resources not found"
        echo ""

        # Determine log level (workflow_dispatch input or default 'debug')
        LOG_LEVEL="${{ inputs.log-level || 'debug' }}"

        # Determine test arguments
        if [ -n "${{ inputs.specific-tests }}" ]; then
          # Convert comma-separated list to space-separated
          TEST_ARGS=$(echo "${{ inputs.specific-tests }}" | tr ',' ' ')
          echo "Running specific tests: $TEST_ARGS"
          echo "Command: env SPDLOG_LEVEL=$LOG_LEVEL ./w3c_test_cli $TEST_ARGS --output w3c_test_results.xml"
          echo ""
          env SPDLOG_LEVEL=$LOG_LEVEL ./w3c_test_cli $TEST_ARGS --output w3c_test_results.xml 2>&1 | tee w3c_test_output.log
          EXIT_CODE=${PIPESTATUS[0]}
        else
          echo "Running all tests"
          echo "Command: env SPDLOG_LEVEL=$LOG_LEVEL ./w3c_test_cli --output w3c_test_results.xml"
          echo ""
          env SPDLOG_LEVEL=$LOG_LEVEL ./w3c_test_cli --output w3c_test_results.xml 2>&1 | tee w3c_test_output.log
          EXIT_CODE=${PIPESTATUS[0]}
        fi

        echo ""
        echo "=== Test Execution Result ==="
        echo "Exit code: $EXIT_CODE"
        echo "Output log size: $(wc -l < w3c_test_output.log) lines"

        # Check for XML file in both locations (current dir and project root)
        if [ -f w3c_test_results.xml ]; then
          echo "SUCCESS: w3c_test_results.xml created in current directory"
          ls -lh w3c_test_results.xml
        elif [ -f ../../w3c_test_results.xml ]; then
          echo "SUCCESS: w3c_test_results.xml created in project root"
          ls -lh ../../w3c_test_results.xml
          # Move to build/tests for easier access by other steps
          cp ../../w3c_test_results.xml .
          echo "Copied to build/tests/ for artifact upload"
        else
          echo "ERROR: w3c_test_results.xml was not created!"
          echo ""
          echo "=== Last 50 lines of output ==="
          tail -n 50 w3c_test_output.log
        fi

        exit $EXIT_CODE
      continue-on-error: true

    - name: Analyze Crash (if occurred)
      if: always()
      run: |
        cd build/tests
        echo "=== Crash Analysis ==="
        if [ -f core ]; then
          echo "Core dump found!"
          gdb -batch -ex "bt" -ex "quit" ./w3c_test_cli core 2>&1 || echo "GDB not available"
        else
          echo "No core dump found"
        fi
        echo ""
        echo "=== Test Output Summary ==="
        if [ -f w3c_test_output.log ]; then
          echo "Log file size: $(wc -l < w3c_test_output.log) lines"
          echo "First 20 lines:"
          head -n 20 w3c_test_output.log
          echo ""
          echo "Last 30 lines:"
          tail -n 30 w3c_test_output.log
        else
          echo "No output log found"
        fi
        echo ""
        echo "=== XML Results Check ==="
        if [ -f w3c_test_results.xml ]; then
          echo "XML file found in current directory"
          head -n 20 w3c_test_results.xml
        elif [ -f ../../w3c_test_results.xml ]; then
          echo "XML file found in project root (expected location)"
          head -n 20 ../../w3c_test_results.xml
        else
          echo "XML file NOT found in either location - test execution likely failed early"
        fi

    - name: Upload Test Output Log
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: w3c-test-output-log
        path: build/tests/w3c_test_output.log
        retention-days: 30
        if-no-files-found: warn

    - name: Publish Test Report
      uses: mikepenz/action-junit-report@v4
      if: always()
      with:
        report_paths: 'build/tests/w3c_test_results.xml'
        detailed_summary: true
        include_passed: true
        fail_on_failure: false
        check_name: 'W3C SCXML Test Results (C++)'
        summary: '[View Detailed HTML Report](https://newmassrael.github.io/scxml-core-engine/test-results/test-results.html)'
        annotate_only: false

    - name: Upload Test Results Artifact
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: w3c-test-results-cpp
        path: build/tests/w3c_test_results.xml
        retention-days: 30

    - name: Upload Build Artifacts
      uses: actions/upload-artifact@v4
      if: failure()
      with:
        name: build-logs
        path: |
          build/CMakeFiles/*.log
          build/Testing/Temporary/
        retention-days: 7

    - name: Generate Test Summary
      if: always()
      run: |
        cd build/tests
        echo "## W3C SCXML Test Results (C++)" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "[View Detailed HTML Report](https://newmassrael.github.io/scxml-core-engine/test-results/test-results.html)" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY

        if [ -f w3c_test_results.xml ]; then
          total=$(grep -oP 'tests="\K[0-9]+' w3c_test_results.xml | head -1)
          failures=$(grep -oP 'failures="\K[0-9]+' w3c_test_results.xml | head -1)
          errors=$(grep -oP 'errors="\K[0-9]+' w3c_test_results.xml | head -1)
          passed=$((total - failures - errors))
          pass_rate=$(awk "BEGIN {printf \"%.1f\", ($passed / $total) * 100}")

          # Count tests by engine type (using classname)
          interpreter_total=$(grep -oP 'classname="W3C_Interpreter"' w3c_test_results.xml | wc -l)
          aot_total=$(grep -oP 'classname="W3C_AOT"' w3c_test_results.xml | wc -l)

          # Count passed tests by engine type (no failure tags)
          interpreter_passed=$(grep 'classname="W3C_Interpreter"' w3c_test_results.xml | grep -v '<failure' | wc -l)
          aot_passed=$(grep 'classname="W3C_AOT"' w3c_test_results.xml | grep -v '<failure' | wc -l)

          interpreter_failed=$((interpreter_total - interpreter_passed))
          aot_failed=$((aot_total - aot_passed))

          # Display engine-specific statistics
          echo "| Engine | Total | Passed | Failed |" >> $GITHUB_STEP_SUMMARY
          echo "|--------|-------|--------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Interpreter | $interpreter_total | $interpreter_passed | $interpreter_failed |" >> $GITHUB_STEP_SUMMARY
          if [ $aot_total -gt 0 ]; then
            echo "| AOT | $aot_total | $aot_passed | $aot_failed |" >> $GITHUB_STEP_SUMMARY
          fi
          echo "| **Total** | **$total** | **$passed** | **$((failures + errors))** |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Pass Rate:** ${pass_rate}%" >> $GITHUB_STEP_SUMMARY
        else
          echo "Test results file not found" >> $GITHUB_STEP_SUMMARY
        fi

  # ============================================================
  # Rust AOT — 202 tests
  # ============================================================
  test-rust:
    name: Rust W3C Tests
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Install Rust Toolchain
      uses: dtolnay/rust-toolchain@stable
      with:
        toolchain: stable

    - name: Cache Cargo
      uses: actions/cache@v4
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          target
        key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          ${{ runner.os }}-cargo-

    - name: Run W3C Tests
      run: |
        echo "=== Rust W3C SCXML Conformance Tests ==="
        cargo test --release -p sce-rust-tests 2>&1 | tee rust_test_output.log
        echo "Exit code: $?"

    - name: Upload Test Log
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: w3c-test-output-rust
        path: rust_test_output.log
        retention-days: 30

    - name: Generate Test Summary
      if: always()
      run: |
        echo "## W3C SCXML Test Results (Rust)" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY

        if [ -f rust_test_output.log ]; then
          # Cargo runs each test binary separately, so sum all "test result:" lines
          passed=$(grep "^test result:" rust_test_output.log | grep -oP '\d+ passed' | grep -oP '\d+' | paste -sd+ | bc)
          failed=$(grep "^test result:" rust_test_output.log | grep -oP '\d+ failed' | grep -oP '\d+' | paste -sd+ | bc)
          total=$((passed + failed))
          if [ "$total" -gt 0 ] 2>/dev/null; then
            echo "| Metric | Count |" >> $GITHUB_STEP_SUMMARY
            echo "|--------|-------|" >> $GITHUB_STEP_SUMMARY
            echo "| Passed | $passed |" >> $GITHUB_STEP_SUMMARY
            echo "| Failed | ${failed:-0} |" >> $GITHUB_STEP_SUMMARY
            echo "| Total | $total |" >> $GITHUB_STEP_SUMMARY
            echo "{\"passed\":$passed,\"failed\":${failed:-0},\"total\":$total}" > rust_results.json
          else
            echo "Could not parse test results" >> $GITHUB_STEP_SUMMARY
          fi
        fi

    - name: Upload Results JSON
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: backend-results-rust
        path: rust_results.json
        retention-days: 7
        if-no-files-found: ignore

  # ============================================================
  # Kotlin/JVM (Rhino) — 202 tests
  # ============================================================
  test-kotlin:
    name: Kotlin W3C Tests
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Setup JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: Cache Gradle
      uses: actions/cache@v4
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle.kts', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-

    - name: Run W3C Tests (Rhino Engine)
      run: |
        echo "=== Kotlin W3C SCXML Conformance Tests (Rhino) ==="
        ./gradlew :sce-kotlin-tests:test 2>&1 | tee kotlin_test_output.log

    - name: Upload Test Results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: w3c-test-results-kotlin
        path: sce-kotlin-tests/build/reports/tests/test/
        retention-days: 30

    - name: Publish Test Report
      uses: mikepenz/action-junit-report@v4
      if: always()
      with:
        report_paths: 'sce-kotlin-tests/build/test-results/test/*.xml'
        detailed_summary: true
        include_passed: true
        fail_on_failure: false
        check_name: 'W3C SCXML Test Results (Kotlin)'

    - name: Generate Test Summary
      if: always()
      run: |
        echo "## W3C SCXML Test Results (Kotlin/JVM - Rhino)" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY

        REPORT_DIR="sce-kotlin-tests/build/test-results/test"
        if [ -d "$REPORT_DIR" ]; then
          total=$(grep -roh 'tests="[0-9]*"' "$REPORT_DIR"/*.xml | grep -oP '\d+' | paste -sd+ | bc)
          failures=$(grep -roh 'failures="[0-9]*"' "$REPORT_DIR"/*.xml | grep -oP '\d+' | paste -sd+ | bc)
          errors=$(grep -roh 'errors="[0-9]*"' "$REPORT_DIR"/*.xml | grep -oP '\d+' | paste -sd+ | bc)
          passed=$((total - failures - errors))
          echo "| Metric | Count |" >> $GITHUB_STEP_SUMMARY
          echo "|--------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| Passed | $passed |" >> $GITHUB_STEP_SUMMARY
          echo "| Failed | $((failures + errors)) |" >> $GITHUB_STEP_SUMMARY
          echo "| Total | $total |" >> $GITHUB_STEP_SUMMARY
          echo "{\"passed\":$passed,\"failed\":$((failures + errors)),\"total\":$total}" > kotlin_results.json
        else
          echo "Test report directory not found" >> $GITHUB_STEP_SUMMARY
        fi

    - name: Upload Results JSON
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: backend-results-kotlin
        path: kotlin_results.json
        retention-days: 7
        if-no-files-found: ignore

  # ============================================================
  # Python Bindings (pybind11) — 202 tests
  # ============================================================
  test-python:
    name: Python W3C Tests
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Install Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          build-essential \
          cmake \
          g++ \
          ninja-build \
          pkg-config \
          libxml2-dev \
          libspdlog-dev \
          libfmt-dev \
          libboost-dev \
          python3-dev

    - name: Install Python Dependencies
      run: |
        python3 -m pip install --upgrade pip
        pip install -r tools/codegen/requirements.txt

    - name: Cache CMake Build
      uses: actions/cache@v4
      with:
        path: |
          build_python/CMakeCache.txt
          build_python/CMakeFiles
          build_python/_deps
        key: ${{ runner.os }}-cmake-python-${{ hashFiles('**/CMakeLists.txt', 'sce-python/**') }}
        restore-keys: |
          ${{ runner.os }}-cmake-python-

    - name: Build Python Bindings
      run: |
        cmake -B build_python \
          -DBUILD_PYTHON_BINDINGS=ON \
          -DCMAKE_BUILD_TYPE=Release \
          -DBUILD_TESTS=OFF \
          -DBUILD_EXAMPLES=OFF \
          -G Ninja
        cmake --build build_python --target _sce --parallel $(nproc)

    - name: Run W3C Tests
      run: |
        echo "=== Python W3C SCXML Conformance Tests ==="
        SPDLOG_LEVEL=off \
        PYTHONPATH=build_python/sce-python:sce-python/python \
          python3 sce-python/tests/test_w3c.py 2>&1 | tee python_test_output.log

    - name: Upload Test Log
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: w3c-test-output-python
        path: python_test_output.log
        retention-days: 30

    - name: Generate Test Summary
      if: always()
      run: |
        echo "## W3C SCXML Test Results (Python)" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY

        if [ -f python_test_output.log ]; then
          # Parse Python test runner output
          pass_line=$(grep "PASS:" python_test_output.log | tail -1)
          fail_line=$(grep "FAIL:" python_test_output.log | tail -1)
          if [ -n "$pass_line" ]; then
            passed=$(echo "$pass_line" | grep -oP '\d+(?=/)')
            total=$(echo "$pass_line" | grep -oP '(?<=/)\d+')
            failed=$(grep "FAIL:" python_test_output.log | tail -1 | grep -oP '\d+(?=/)' || echo "0")
            echo "| Metric | Count |" >> $GITHUB_STEP_SUMMARY
            echo "|--------|-------|" >> $GITHUB_STEP_SUMMARY
            echo "| Passed | $passed |" >> $GITHUB_STEP_SUMMARY
            echo "| Failed | ${failed:-0} |" >> $GITHUB_STEP_SUMMARY
            echo "| Total | $total |" >> $GITHUB_STEP_SUMMARY
            echo "{\"passed\":$passed,\"failed\":${failed:-0},\"total\":$total}" > python_results.json
          else
            echo "Could not parse test results" >> $GITHUB_STEP_SUMMARY
          fi
        fi

    - name: Upload Results JSON
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: backend-results-python
        path: python_results.json
        retention-days: 7
        if-no-files-found: ignore

  # ============================================================
  # Unified HTML Report + GitHub Pages Deploy
  # ============================================================
  report:
    name: Unified Report
    runs-on: ubuntu-latest
    needs: [test-cpp, test-rust, test-kotlin, test-python]
    if: always()
    permissions:
      contents: write

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Download All Artifacts
      uses: actions/download-artifact@v4
      with:
        path: artifacts/

    - name: Merge Backend Results
      run: |
        # Combine Rust/Kotlin/Python JSON results into one file
        echo '{' > backend_results.json
        first=true
        for backend in rust kotlin python; do
          json_file="artifacts/backend-results-${backend}/${backend}_results.json"
          if [ -f "$json_file" ]; then
            if [ "$first" = true ]; then first=false; else echo ',' >> backend_results.json; fi
            echo "\"${backend}\": $(cat "$json_file")" >> backend_results.json
            echo "Found $backend results: $(cat "$json_file")"
          else
            echo "No results for $backend"
          fi
        done
        echo '}' >> backend_results.json
        echo "=== Merged backend results ==="
        cat backend_results.json

    - name: Generate Unified HTML Report
      run: |
        CPP_XML="artifacts/w3c-test-results-cpp/w3c_test_results.xml"
        if [ -f "$CPP_XML" ]; then
          python3 tests/w3c/scripts/xml_to_html.py \
            "$CPP_XML" \
            test-results.html \
            --backend-results backend_results.json
          echo "Unified HTML report generated"
        else
          echo "C++ XML results not found - cannot generate report"
        fi

    - name: Prepare GitHub Pages
      run: |
        mkdir -p gh-pages
        if [ -f test-results.html ]; then
          cp test-results.html gh-pages/test-results.html
        fi
        CPP_XML="artifacts/w3c-test-results-cpp/w3c_test_results.xml"
        if [ -f "$CPP_XML" ]; then
          cp "$CPP_XML" gh-pages/w3c_test_results.xml
        fi

    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      if: github.ref == 'refs/heads/main'
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./gh-pages
        destination_dir: test-results
        keep_files: true
