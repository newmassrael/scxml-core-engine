{# W3C SCXML 4.6: <foreach> — iteration #}
{# Phase 1: Static-only iteration (array must be a known variable) #}
            {{ action.array }}.forEach { {{ action.item }} ->
{% for body_action in action.actions %}
{% include 'actions/' + body_action.type + '.kt.jinja2' with context %}
{% endfor %}
            }
