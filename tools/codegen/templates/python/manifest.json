{
  "language": "python",
  "version": "0.4.0",
  "description": "Atomic γ-2 — adds <history> shallow + deep with pre-exit snapshot + replay; default-transition actions fire on first entry when no snapshot exists. <invoke>, remaining executable content, and binding=late remain reject-walled.",
  "entry_template": "state_machine.py.jinja2",
  "runtime_dependency": "sce-python-runtime",
  "output_extension": ".py"
}
