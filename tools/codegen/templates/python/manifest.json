{
  "language": "python",
  "version": "0.5.0",
  "description": "Atomic γ-3a — adds <log>, <if>/<elseif>/<else>, and <foreach> (index + value forms). <send>, <cancel>, and the delayed-event scheduler land in γ-3b; <invoke> and binding=late remain reject-walled.",
  "entry_template": "state_machine.py.jinja2",
  "runtime_dependency": "sce-python-runtime",
  "output_extension": ".py"
}
