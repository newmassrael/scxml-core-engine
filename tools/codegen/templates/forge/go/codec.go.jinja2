{# SPDX-License-Identifier: LGPL-2.1-or-later OR LicenseRef-SCE-Commercial #}
{# SPDX-FileCopyrightText: Copyright (c) 2025 newmassrael #}

// SCE Forge: Auto-generated from Extended SCXML (sce:kind="codec")
// Runtime: {{ runtime_dep }}
// Do not edit — regenerate from the source SCXML file.

package {{ package }}
{% if all_imports %}

import (
{% for imp in all_imports %}
{{ imp.include_stmt }}
{% endfor %}
)
{% endif %}

// {{ struct_name }} represents the codec frame layout.
type {{ struct_name }} struct {
{% for field in fields %}
	{{ field.id }} {{ field.go_type }}
{% endfor %}
}

// Decode{{ struct_name }} decodes raw bytes into a {{ struct_name }}.
// Returns nil if the input is too short.
func Decode{{ struct_name }}(raw []byte) *{{ struct_name }} {
	if len(raw) < {{ min_bytes }} {
		return nil
	}
	return &{{ struct_name }}{
{% for field in fields %}
		{{ field.id }}: {{ field.decode_expr }},
{% endfor %}
	}
}

// Encode serializes the {{ struct_name }} into raw bytes.
func (s *{{ struct_name }}) Encode() []byte {
	return []byte{
{% for expr in encode_exprs %}
		{{ expr }},
{% endfor %}
	}
}
