// SCE Forge: Auto-generated from Extended SCXML (sce:kind="procedure")
// Do not edit — regenerate from the source SCXML file.

#pragma once
#ifndef {{ guard }}
#define {{ guard }}

#include <cstdint>
#include <string>
#include "core/ProcedureStateMachine.h"

namespace SCE::Generated::{{ namespace }} {

struct {{ struct_name }} : public SCE::Core::ProcedureStateMachine<{{ struct_name }}> {
    enum State : int {
{% for s in state_enum %}
        {{ s.name }} = {{ s.index }},
{% endfor %}
    };

    static constexpr int INITIAL_STATE = {{ initial_index }};
    static constexpr int STATE_COUNT = {{ state_count }};

    static constexpr bool isFinal(int s) {
        return {{ final_check }};
    }

    static const char* stateName(int s) {
        static const char* names[] = { {% for n in state_names %}"{{ n }}"{% if not loop.last %}, {% endif %}{% endfor %} };
        return (s >= 0 && s < STATE_COUNT) ? names[s] : "";
    }

    int evaluateTransitions(int current, {{ params }}) const {
        switch (current) {
{% for s in non_final_states %}
            case {{ s.index }}:
{% for tr in s.transitions %}
{% if tr.has_cond %}
                if ({{ tr.cond }}) return {{ tr.target_index }};
{% else %}
                return {{ tr.target_index }};
{% endif %}
{% endfor %}
{% endfor %}
        }
        return -1;
    }
};

}  // namespace SCE::Generated::{{ namespace }}

#endif  // {{ guard }}
