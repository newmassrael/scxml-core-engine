// SCE Forge: Auto-generated from Extended SCXML (sce:kind="timer")
// Do not edit — regenerate from the source SCXML file.

#pragma once
#ifndef {{ guard }}
#define {{ guard }}

#include <cstdint>
#include <sce/forge/timer.h>
{% if all_imports %}
{% for imp in all_imports %}
{{ imp.include_stmt }}
{% endfor %}
{% endif %}

namespace SCE::Generated::{{ namespace }} {

class {{ struct_name }} {
public:
    {{ struct_name }}({% for timer in timers %}sce::forge::ITimer& {{ timer.id }}Timer{% if not loop.last %}, {% endif %}{% endfor %})
        : {% for timer in timers %}{{ timer.id }}Timer_({{ timer.id }}Timer){% if not loop.last %}, {% endif %}{% endfor %} {}
{% for timer in timers %}

    void start{{ timer.id_pascal }}() {
{% if timer.is_periodic %}
        {{ timer.id }}Timer_.startPeriodic({{ timer.time_ms }}, &on{{ timer.callback_pascal }}, this);
{% else %}
        {{ timer.id }}Timer_.startOneShot({{ timer.time_ms }}, &on{{ timer.callback_pascal }}, this);
{% endif %}
    }

    void cancel{{ timer.id_pascal }}() {
        {{ timer.id }}Timer_.cancel();
    }
{% endfor %}

private:
{% for timer in timers %}
    sce::forge::ITimer& {{ timer.id }}Timer_;
{% endfor %}

{% for timer in timers %}
    static void on{{ timer.callback_pascal }}(void* ctx) {
        static_cast<{{ struct_name }}*>(ctx)->fire{{ timer.callback_pascal }}();
    }
{% endfor %}

{% for timer in timers %}
    void fire{{ timer.callback_pascal }}();
{% endfor %}
};

}  // namespace SCE::Generated::{{ namespace }}

#endif  // {{ guard }}
