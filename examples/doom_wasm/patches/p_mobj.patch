--- a/doomgeneric/p_mobj.c
+++ b/doomgeneric/p_mobj.c
@@ -32,6 +32,9 @@

 #include "doomstat.h"

+// SCE State Machine Integration
+#include "sce_doom_hooks.h"
+

 void G_PlayerReborn (int player);
 void P_SpawnMapThing (mapthing_t*	mthing);
@@ -555,6 +558,9 @@ P_SpawnMobj

     P_AddThinker (&mobj->thinker);

+    // SCE: Track monster spawn
+    SCE_EnemySpawned(mobj);
+
     return mobj;
 }

@@ -585,6 +591,9 @@ void P_RemoveMobj (mobj_t* mobj)
     // unlink from sector and block lists
     P_UnsetThingPosition (mobj);

+    // SCE: Track monster removal
+    SCE_EnemyRemoved(mobj);
+
     // stop any playing sound
     S_StopSound (mobj);

