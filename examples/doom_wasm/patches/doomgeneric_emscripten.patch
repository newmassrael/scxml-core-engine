--- a/doomgeneric/doomgeneric_emscripten.c
+++ b/doomgeneric/doomgeneric_emscripten.c
@@ -12,6 +12,9 @@

 #include <emscripten.h>

+// SCE State Machine Integration
+#include "sce_doom_hooks.h"
+
 SDL_Window* window = NULL;
 SDL_Renderer* renderer = NULL;
 SDL_Texture* texture;
@@ -50,6 +53,14 @@ static unsigned char convertToDoomKey(unsigned int key)
     case SDLK_DOWN:
       key = KEY_DOWNARROW;
       break;
+    case SDLK_w:
+      key = KEY_UPARROW;
+      break;
+    case SDLK_s:
+      key = KEY_DOWNARROW;
+      break;
+    // NOTE: A and D are NOT converted here - they pass through as lowercase
+    // letters so that cheat codes (like IDDQD) work correctly.
+    // Strafe is handled by m_controls.c key bindings (key_strafeleft = 'a', key_straferight = 'd')
     case SDLK_LCTRL:
     case SDLK_RCTRL:
       key = KEY_FIRE;
@@ -156,6 +167,9 @@ void DG_Init()
                                      0, SDL_PIXELFORMAT_RGB888);

   memset(DG_ScreenBuffer, 0, DOOMGENERIC_RESX * DOOMGENERIC_RESY * 4);
+
+  // Initialize SCE state machines
+  SCE_Init();
 }

 void DG_DrawFrame()
