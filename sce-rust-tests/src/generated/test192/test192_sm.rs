// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: 2025 [Author of input SCXML file]
//
// Generated by SCE Code Generator (Rust backend)
// From: resources/192/test192.scxml
//
// This generated code is MIT licensed and may be freely used in any project.
// Runtime engine dependency (sce-rust-runtime): LGPL-2.1-or-later OR LicenseRef-SCE-Commercial
// Full license: https://github.com/newmassrael/scxml-core-engine/blob/main/LICENSE
//! Generated Rust state machine for `test192`.
//!
//! Generated by SCE Code Generator (Rust backend, Phase 2).
//! Implements `sce_rust_runtime::StatePolicy` with full transition logic,
//! entry/exit actions, and event processing.

#![allow(dead_code)]
#![allow(unused_variables)]
#![allow(unused_imports)]
#![allow(unused_mut)]
#![allow(unused_labels)]
#![allow(clippy::all)]
#![allow(unreachable_patterns)]
#![allow(unreachable_code)]

use std::time::Duration;
use sce_rust_runtime::{Engine, StatePolicy};


// ======================================================================
// State enum (W3C SCXML 3.3)
// ======================================================================

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum Test192State {
    Fail,
    Pass,
    S0,
    S01,
    S02,
}

// ======================================================================
// Event enum (W3C SCXML 3.12)
// ======================================================================

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum Test192Event {
    CancelInvoke,
    ChildToParent,
    DoneInvoke,
    ErrorExecution,
    EventReceived,
    ParentToChild,
    Timeout,
    /// W3C SCXML 3.13: Sentinel for eventless transition dispatch
    Null,
}

// ======================================================================
// Policy struct
// ======================================================================

pub struct Test192Policy {
    // W3C SCXML 3.13: Last transition metadata
    last_transition_is_internal: bool,
    last_transition_is_targetless: bool,
    last_transition_source_state: Test192State,
    // W3C SCXML 3.13: Transition action tracking
    last_transition_index: usize,
    has_transition_actions: bool,
    // W3C SCXML 5.10: Session ID (script engine + invoke tracking)
    pub session_id: Option<String>,
    // W3C SCXML 6.4: Pending invoke queue (deferred until macrostep end)
    pending_invokes: Vec<sce_rust_runtime::invoke::PendingInvoke<Test192State>>,
    // W3C SCXML 6.4: Active child sessions (invoke_id -> ChildSession)
    active_invokes: std::collections::HashMap<String, sce_rust_runtime::invoke::ChildSession>,
    // W3C SCXML 6.4: Static invoke child 'invokedChild' (test192_child0)
    child_invokedChild: Option<Box<sce_rust_runtime::Engine<super::test192_child0_sm::Test192Child0Policy>>>,
    pending_done_invoke_invokedChild: bool,
    // W3C SCXML 6.4: Parent engine external queue for #_parent send routing
    // Always generated — any SM can be invoked as a child
    pub parent_external_queue: Option<std::sync::Arc<std::sync::Mutex<Vec<(String, String)>>>>,
    // W3C SCXML 6.4.1: This child's invoke ID (for _event.invokeid in parent)
    pub invoke_id: String,
    // W3C SCXML 6.5: Child session ID for finalize origin matching
    pub child_session_id: String,
}

impl Test192Policy {
    pub fn new() -> Self {
        Self {
            last_transition_is_internal: false,
            last_transition_is_targetless: false,
            last_transition_source_state: Test192State::S01,
            last_transition_index: 0,
            has_transition_actions: false,
            session_id: None,
            pending_invokes: Vec::new(),
            active_invokes: std::collections::HashMap::new(),
            child_invokedChild: None,
            pending_done_invoke_invokedChild: false,
            parent_external_queue: None,
            invoke_id: String::new(),
            child_session_id: String::new(),
        }
    }

    // W3C SCXML 6.4: Execute pending invokes at macrostep end
    // 1:1 port of C++ executePendingInvokes() in entry_exit_actions.jinja2
    fn do_execute_pending_invokes(&mut self, engine: &mut sce_rust_runtime::Engine<Self>) {
        if self.pending_invokes.is_empty() {
            return;
        }

        // W3C SCXML 6.4: Copy-and-clear to prevent iterator invalidation
        let invokes_to_execute: Vec<_> = self.pending_invokes.drain(..).collect();

        for pending in &invokes_to_execute {
            if pending.invoke_id.contains(".invokedChild") {

                // W3C SCXML 6.5: Generate child session ID for finalize origin matching
                let child_session_id = format!("{}.{}",
                    self.session_id.as_deref().unwrap_or(""),
                    &pending.invoke_id);

                // W3C SCXML 6.4: Create child state machine
                let mut child_policy = super::test192_child0_sm::Test192Child0Policy::new();
                // W3C SCXML 6.4: Pass parent session info to child for #_parent routing
                child_policy.parent_external_queue = Some(engine.get_external_queue_handle());
                child_policy.invoke_id = pending.invoke_id.clone();
                child_policy.child_session_id = child_session_id.clone();

                // W3C SCXML 6.4: Ensure child session ID for invoke tracking
                if child_policy.session_id.is_none() {
                    static CHILD_SESSION_COUNTER: std::sync::atomic::AtomicU64 = std::sync::atomic::AtomicU64::new(0);
                    let cid = CHILD_SESSION_COUNTER.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
                    child_policy.session_id = Some(format!("child_session_{}", cid));
                }



                // W3C SCXML 6.4.6: Track active invoke session BEFORE initialize
                self.active_invokes.insert("invokedChild".to_string(),
                    sce_rust_runtime::invoke::ChildSession {
                        session_id: child_session_id,
                        // W3C SCXML 6.4: Element invoke_id for _event.invokeid (test 228)
                        invoke_id: "invokedChild".to_string(),
                        parent_session_id: self.session_id.clone().unwrap_or_default(),
                        autoforward: false,
                        finalize_script: "".to_string(),
                    },
                );

                let mut child_engine = sce_rust_runtime::Engine::new(child_policy);
                // W3C SCXML 6.4: Set completion callback so child runs final state onexit (test 236)
                child_engine.set_completion_callback(|| {});
                child_engine.initialize();

                // W3C SCXML 6.4: Store child BEFORE draining events so autoforward can find it
                self.child_invokedChild = Some(Box::new(child_engine));

                // W3C SCXML 6.4: Drain child-to-parent events raised during initialize
                {
                    let child_ref = self.child_invokedChild.as_ref().unwrap();
                    let parent_events: Vec<(String, String)> = {
                        if let Some(ref queue) = child_ref.policy().parent_external_queue {
                            let mut q = queue.lock().unwrap();
                            q.drain(..).collect()
                        } else { Vec::new() }
                    };
                    for (ev_name, ev_data) in parent_events {
                        if let Some(event) = Self::get_event_from_name(&ev_name) {
                            let mut meta = sce_rust_runtime::EventWithMetadata::new(event);
                            meta.metadata.data = ev_data;
                            meta.metadata.invoke_id = self.active_invokes
                                .get("invokedChild")
                                .map_or_else(String::new, |cs| cs.invoke_id.clone());
                            meta.metadata.origin = self.active_invokes
                                .get("invokedChild")
                                .map_or_else(String::new, |cs| cs.session_id.clone());
                            meta.metadata.origin_type = sce_rust_runtime::helpers::scxml_constants::SCXML_EVENT_PROCESSOR_TYPE.to_string();
                            engine.raise_external_with_meta(meta);
                        }
                    }
                }

                // W3C SCXML 6.4: Check if child completed during initialize (test 236)
                if self.child_invokedChild.as_ref().map_or(false, |c| c.is_in_final_state()) {
                    self.pending_done_invoke_invokedChild = true;
                    let runtime_invoke_id = "invokedChild".to_string();
                    {
                        let mut done_meta = sce_rust_runtime::EventWithMetadata::new(
                            Test192Event::DoneInvoke);
                        done_meta.metadata.invoke_id = runtime_invoke_id;
                        engine.raise_external_with_meta(done_meta);
                    }
                }
                continue;
            }
        }
    }

    // W3C SCXML 6.4: Tick child state machines (propagate scheduler ticks)
    fn do_tick_children(&mut self, engine: &mut sce_rust_runtime::Engine<Self>) {
        if !self.pending_done_invoke_invokedChild {
            // W3C SCXML 6.4: Take child out temporarily to avoid split-borrow
            if let Some(mut child) = self.child_invokedChild.take() {
                // W3C SCXML 6.4: Drain parent events sent by child via #_parent BEFORE tick
                {
                    let parent_events: Vec<(String, String)> = {
                        if let Some(ref queue) = child.policy().parent_external_queue {
                            let mut q = queue.lock().unwrap();
                            q.drain(..).collect()
                        } else { Vec::new() }
                    };
                    for (ev_name, ev_data) in parent_events {
                        if let Some(event) = Self::get_event_from_name(&ev_name) {
                            let mut meta = sce_rust_runtime::EventWithMetadata::new(event);
                            meta.metadata.data = ev_data;
                            meta.metadata.invoke_id = self.active_invokes
                                .get("invokedChild")
                                .map_or_else(String::new, |cs| cs.invoke_id.clone());
                            meta.metadata.origin = self.active_invokes
                                .get("invokedChild")
                                .map_or_else(String::new, |cs| cs.session_id.clone());
                            meta.metadata.origin_type = sce_rust_runtime::helpers::scxml_constants::SCXML_EVENT_PROCESSOR_TYPE.to_string();
                            engine.raise_external_with_meta(meta);
                        }
                    }
                }

                child.tick();

                // W3C SCXML 6.4: Drain parent events sent by child AFTER tick
                {
                    let parent_events: Vec<(String, String)> = {
                        if let Some(ref queue) = child.policy().parent_external_queue {
                            let mut q = queue.lock().unwrap();
                            q.drain(..).collect()
                        } else { Vec::new() }
                    };
                    for (ev_name, ev_data) in parent_events {
                        if let Some(event) = Self::get_event_from_name(&ev_name) {
                            let mut meta = sce_rust_runtime::EventWithMetadata::new(event);
                            meta.metadata.data = ev_data;
                            meta.metadata.invoke_id = self.active_invokes
                                .get("invokedChild")
                                .map_or_else(String::new, |cs| cs.invoke_id.clone());
                            meta.metadata.origin = self.active_invokes
                                .get("invokedChild")
                                .map_or_else(String::new, |cs| cs.session_id.clone());
                            meta.metadata.origin_type = sce_rust_runtime::helpers::scxml_constants::SCXML_EVENT_PROCESSOR_TYPE.to_string();
                            engine.raise_external_with_meta(meta);
                        }
                    }
                }

                // W3C SCXML 6.4: Check if child reached final state after tick
                if child.is_in_final_state() && !self.pending_done_invoke_invokedChild {
                    self.pending_done_invoke_invokedChild = true;
                    let runtime_invoke_id = "invokedChild".to_string();
                    {
                        let mut done_meta = sce_rust_runtime::EventWithMetadata::new(
                            Test192Event::DoneInvoke);
                        done_meta.metadata.invoke_id = runtime_invoke_id;
                        engine.raise_external_with_meta(done_meta);
                    }
                }

                // Put child back
                self.child_invokedChild = Some(child);
            }
        }
    }



}

impl Default for Test192Policy {
    fn default() -> Self {
        Self::new()
    }
}

// ======================================================================
// StatePolicy trait implementation
// ======================================================================

impl StatePolicy for Test192Policy {
    type State = Test192State;
    type Event = Test192Event;

    // W3C SCXML feature flags
    const HAS_PARALLEL_STATES: bool = false;
    const NEEDS_SCRIPT_ENGINE: bool = false;
    const NEEDS_DATA_MODEL_INIT: bool = false;
    const HAS_INVOKE_SUPPORT: bool = true;
    const HAS_CHILD_TICK: bool = true;

    // ======================================================================
    // Static metadata methods (W3C SCXML document structure)
    // ======================================================================

    fn initial_state() -> Self::State {
        Test192State::S01
    }

    fn is_final_state(state: Self::State) -> bool {
        match state {
            Test192State::Fail => true,
            Test192State::Pass => true,
            _ => false,
        }
    }

    fn get_parent(state: Self::State) -> Option<Self::State> {
        match state {
            Test192State::S01 => Some(Test192State::S0),
            Test192State::S02 => Some(Test192State::S0),
            _ => None,
        }
    }

    fn is_compound_state(state: Self::State) -> bool {
        match state {
            Test192State::S0 => true,
            _ => false,
        }
    }


    fn is_descendant_of(desc: Self::State, anc: Self::State) -> bool {
        let mut current = desc;
        loop {
            match Self::get_parent(current) {
                None => return false,
                Some(parent) => {
                    if parent == anc {
                        return true;
                    }
                    current = parent;
                }
            }
        }
    }

    fn get_document_order(state: Self::State) -> u32 {
        match state {
            Test192State::Fail => 4,
            Test192State::Pass => 3,
            Test192State::S0 => 0,
            Test192State::S01 => 1,
            Test192State::S02 => 2,
        }
    }

    fn get_event_name(event: Self::Event) -> &'static str {
        match event {
            Test192Event::CancelInvoke => "cancel.invoke",
            Test192Event::ChildToParent => "childToParent",
            Test192Event::DoneInvoke => "done.invoke",
            Test192Event::ErrorExecution => "error.execution",
            Test192Event::EventReceived => "eventReceived",
            Test192Event::ParentToChild => "parentToChild",
            Test192Event::Timeout => "timeout",
            Test192Event::Null => "",
        }
    }

    fn get_event_from_name(name: &str) -> Option<Self::Event> {
        match name {
            "cancel.invoke" => Some(Test192Event::CancelInvoke),
            "childToParent" => Some(Test192Event::ChildToParent),
            "done.invoke" => Some(Test192Event::DoneInvoke),
            "error.execution" => Some(Test192Event::ErrorExecution),
            "eventReceived" => Some(Test192Event::EventReceived),
            "parentToChild" => Some(Test192Event::ParentToChild),
            "timeout" => Some(Test192Event::Timeout),
            _ => None,
        }
    }


    fn null_event() -> Self::Event {
        Test192Event::Null
    }

    // W3C SCXML 3.6: Get initial children of a compound state
    fn get_initial_children(state: Self::State) -> Vec<Self::State> {
        match state {
            Test192State::S0 => vec![
                Test192State::S01,
            ],
            _ => Vec::new(),
        }
    }

    // W3C SCXML 3.11: Get initial or history-restored child
    fn get_initial_or_history_child(&self, state: Self::State) -> Self::State {
        match state {
            Test192State::S0 => {
                Test192State::S01
            }
            _ => state,
        }
    }

    // ======================================================================
    // Mutable field accessors
    // ======================================================================

    fn last_transition_is_internal(&self) -> bool {
        self.last_transition_is_internal
    }

    fn set_last_transition_is_internal(&mut self, value: bool) {
        self.last_transition_is_internal = value;
    }

    fn last_transition_is_targetless(&self) -> bool {
        self.last_transition_is_targetless
    }

    fn set_last_transition_is_targetless(&mut self, value: bool) {
        self.last_transition_is_targetless = value;
    }

    fn last_transition_source_state(&self) -> Self::State {
        self.last_transition_source_state
    }

    fn set_last_transition_source_state(&mut self, state: Self::State) {
        self.last_transition_source_state = state;
    }




    // ======================================================================
    // Instance methods - generated executable content
    // ======================================================================


    // W3C SCXML 3.7: Execute <onentry> actions for a state
    fn execute_entry_actions(&mut self, state: Self::State, engine: &mut sce_rust_runtime::Engine<Self>) {
        match state {
            Test192State::S0 => {
                // W3C SCXML 3.8: onentry block 1/1
                // Labeled block allows actions to break out on error (W3C 3.8: error stops block)
                'action_block: {
{
    let send_id = "__send_0".to_string();


    let event_data: &str = "";



    // W3C SCXML 6.2: Delayed send (5000ms)
    engine.schedule_event(
        Test192Event::Timeout,
        std::time::Duration::from_millis(5000),
        &send_id,
        event_data,
    );


    let _ = send_id;  // suppress unused warning when no send operation
    let _ = event_data;  // suppress unused warning in branches that skip dispatch
}
                }
                // W3C SCXML 6.4: Defer static invoke execution until macrostep end
                {
                    let generated_invoke_id = format!("{}.{}.invokedChild",
                        "s0", self as *const _ as usize);
                    sce_rust_runtime::invoke::defer_invoke(
                        &mut self.pending_invokes,
                        sce_rust_runtime::invoke::PendingInvoke {
                            invoke_id: generated_invoke_id,
                            state: Test192State::S0,
                        },
                    );
                }
            }
            _ => {}
        }

    }

    // W3C SCXML 3.8: Execute <onexit> actions for a state
    fn execute_exit_actions(
        &mut self,
        state: Self::State,
        engine: &mut sce_rust_runtime::Engine<Self>,
        pre_transition_active: &[Self::State],
    ) {
        // W3C SCXML 6.4: Cancel pending invokes and cleanup active children on state exit
        match state {
            Test192State::S0 => {
                // W3C SCXML 6.4: Cancel pending invokes for exited state
                sce_rust_runtime::invoke::cancel_invokes_for_state(
                    &mut self.pending_invokes,
                    Test192State::S0,
                );
                // W3C SCXML 6.4: Cleanup running child 'invokedChild'
                if self.child_invokedChild.is_some() {
                    // W3C SCXML 6.4: Only raise cancel.invoke if child hasn't completed (test 236)
                    if !self.pending_done_invoke_invokedChild {
                        engine.raise(sce_rust_runtime::EventWithMetadata::new(
                            Test192Event::CancelInvoke));
                    }
                    self.child_invokedChild = None;
                }
                self.active_invokes.remove("invokedChild");
                self.pending_done_invoke_invokedChild = false;
            }
            _ => {}
        }
        match state {
            _ => {}
        }
    }


    // W3C SCXML 3.13: Evaluate guards and take a matching transition
    fn process_transition(
        &mut self,
        current_state: &mut Self::State,
        event: Self::Event,
        engine: &mut sce_rust_runtime::Engine<Self>,
    ) -> bool {
        let mut transition_taken = false;


        // W3C SCXML 3.12: Hierarchical event processing (innermost to outermost)
        let mut check_state = *current_state;

        loop {
            let found = self.try_transition_in_state(check_state, event, current_state, &mut transition_taken, engine);

            if found {
                break;
            }

            // W3C SCXML 3.13: Eventless transitions do NOT bubble to parent states
            if event == Self::null_event() {
                break;
            }

            // W3C SCXML 3.12: Move to parent state for hierarchical event bubbling
            match Self::get_parent(check_state) {
                Some(parent) => check_state = parent,
                None => break,
            }
        }

        transition_taken
    }

    // W3C SCXML 3.13: Execute transition actions (called between exit and entry)
    fn execute_transition_actions(&mut self, engine: &mut sce_rust_runtime::Engine<Self>) {
        if !self.has_transition_actions {
            return;
        }

        // Switch on source state, then transition index
        match self.last_transition_source_state {
            Test192State::S01 => {
                match self.last_transition_index {
                    0 => {
                        // W3C SCXML 3.13: Transition 0 actions
{
    let send_id = "__send_1".to_string();


    let event_data: &str = "";



    // W3C SCXML 6.4: Send to child invoke 'invokedChild' via #_invokedChild
    if let Some(ref mut child) = self.child_invokedChild {
        child.raise_external_by_name("parentToChild", &event_data);
    }


    let _ = send_id;  // suppress unused warning when no send operation
    let _ = event_data;  // suppress unused warning in branches that skip dispatch
}
                    }
                    _ => {}
                }
            }
            _ => {}
        }

        // Reset flags after execution
        self.has_transition_actions = false;
    }

    // W3C SCXML 6.4: Execute pending invokes at macrostep end
    fn execute_pending_invokes(&mut self, engine: &mut Engine<Self>) {
        self.do_execute_pending_invokes(engine);
    }

    // W3C SCXML 6.4: Tick child state machines
    fn tick_children(&mut self, engine: &mut Engine<Self>) {
        self.do_tick_children(engine);
    }
}

// ======================================================================
// Helper impl block (try_transition_in_state, conflict resolution, etc.)
// ======================================================================

impl Test192Policy {
    // W3C SCXML 3.12: Helper method for hierarchical transition checking
    fn try_transition_in_state(
        &mut self,
        check_state: Test192State,
        event: Test192Event,
        current_state: &mut Test192State,
        transition_taken: &mut bool,
        engine: &mut sce_rust_runtime::Engine<Self>,
    ) -> bool {
        match check_state {
            Test192State::Fail => false,
            Test192State::Pass => false,
            Test192State::S0 => {
                // W3C SCXML 3.12: Event-triggered transitions (document order)
                // W3C SCXML 5.9.3: Direct enum comparison
                if event == Test192Event::Timeout {
                        // W3C SCXML 3.4: Track transition metadata
                        self.last_transition_source_state = check_state;
                        self.last_transition_index = 0;
                        self.has_transition_actions = false;
                        self.last_transition_is_internal = false;
                        self.last_transition_is_targetless = false;

                            *current_state = Test192State::Fail;
                            *transition_taken = true;
                        return true;
                }
                // W3C SCXML 5.9.3: Direct enum comparison
                if event == Test192Event::DoneInvoke {
                        // W3C SCXML 3.4: Track transition metadata
                        self.last_transition_source_state = check_state;
                        self.last_transition_index = 1;
                        self.has_transition_actions = false;
                        self.last_transition_is_internal = false;
                        self.last_transition_is_targetless = false;

                            *current_state = Test192State::Fail;
                            *transition_taken = true;
                        return true;
                }
                false
            }
            Test192State::S01 => {
                // W3C SCXML 3.12: Event-triggered transitions (document order)
                // W3C SCXML 5.9.3: Direct enum comparison
                if event == Test192Event::ChildToParent {
                        // W3C SCXML 3.4: Track transition metadata
                        self.last_transition_source_state = check_state;
                        self.last_transition_index = 0;
                        self.has_transition_actions = true;
                        self.last_transition_is_internal = false;
                        self.last_transition_is_targetless = false;

                            *current_state = Test192State::S02;
                            *transition_taken = true;
                        return true;
                }
                false
            }
            Test192State::S02 => {
                // W3C SCXML 3.12: Event-triggered transitions (document order)
                // W3C SCXML 5.9.3: Direct enum comparison
                if event == Test192Event::EventReceived {
                        // W3C SCXML 3.4: Track transition metadata
                        self.last_transition_source_state = check_state;
                        self.last_transition_index = 0;
                        self.has_transition_actions = false;
                        self.last_transition_is_internal = false;
                        self.last_transition_is_targetless = false;

                            *current_state = Test192State::Pass;
                            *transition_taken = true;
                        return true;
                }
                false
            }
            _ => false,
        }
    }



}