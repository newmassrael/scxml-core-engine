# SPDX-License-Identifier: LGPL-2.1-or-later WITH LicenseRef-SCE-Linking-Exception OR LicenseRef-SCE-Commercial
# SPDX-FileCopyrightText: Copyright (c) 2026 newmassrael
"""SCE Python AOT integration-fixture test root.

Counterpart to Rust's ``sce-rust-tests/src/integration/``, Kotlin's
``com.sce.integration`` package, and Go's ``sce-go-tests/integration/``.
Each ``<stem>/`` subdirectory holds:

  * ``<stem>_sm.py`` + synth-invoke ``__sce_synth_invoke__<id>_sm.py``
    (gitignored, regenerated by
    ``scripts/regen_donedata_local_invoke_python.sh`` and
    ``sce-codegen generate-integration -l python``).
  * ``test_<stem>_aot.py`` — hand-authored pytest wrapper driving the
    AOT-generated engine against the fixture's W3C contract.
  * ``__init__.py`` — package marker.

The pybind11 channel test at ``sce-python/tests/test_donedata_local_invoke.py``
covers the same SCXML contract via the C++ Interpreter; both channels
exist in production so both are verified independently.

See ``docs/SCE_INTEGRATION_FIXTURE_LAYOUT.md`` for the per-backend
layout convention and architectural-axis rationale.
"""
