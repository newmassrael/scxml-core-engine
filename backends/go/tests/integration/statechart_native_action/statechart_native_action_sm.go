// SCE-GENERATED — DO NOT EDIT
// source-hash: d8762291f8adee4223c5af3de347a3acefddf00a392ed861e70d8d9802dc3abb
// template-hash: 85660c1341dd8abf7326f61f4efe828117f6cbaf56814ccb03d3fd81b42e6ed0
// generated-at: 0


// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) 2025-2026 [Author of input SCXML file]
//
// Generated by SCE Code Generator (Go backend)
// From: sce-build/tests/fixtures/event_schema/statechart_native_action.scxml
//
// This generated code is MIT licensed and may be freely used in any project.
// Runtime engine dependency (sce-go-runtime): LGPL-2.1-or-later WITH LicenseRef-SCE-Linking-Exception OR LicenseRef-SCE-Commercial
// Full license: https://github.com/newmassrael/scxml-core-engine/blob/main/LICENSE
// Generated Go state machine for `statechart_native_action`.
//
// Generated by SCE Code Generator (Go backend).
// Implements sce.StatePolicy with full transition logic,
// entry/exit actions, and event processing.


// SCE-MAP: statechart_native_action.scxml:31 :: _machine

package statechart_native_action

import (
	"fmt"
	"path/filepath"
	"strings"
	"time"

	sce "github.com/newmassrael/sce-go-runtime"
)

// Ensure imports are used
var (
	_ = fmt.Sprintf
	_ = filepath.Join
	_ = strings.HasPrefix
	_ = time.Duration(0)
)

// ======================================================================
// State type (W3C SCXML 3.3)
// ======================================================================

type StatechartNativeActionState int

const (
	StatechartNativeActionStateAssembling StatechartNativeActionState = 0
	StatechartNativeActionStateFaulted StatechartNativeActionState = 1
	StatechartNativeActionStateIdle StatechartNativeActionState = 2
)

func (s StatechartNativeActionState) String() string {
	switch s {
	case StatechartNativeActionStateAssembling:
		return "assembling"
	case StatechartNativeActionStateFaulted:
		return "faulted"
	case StatechartNativeActionStateIdle:
		return "idle"
	}
	return "unknown"
}

// StatechartNativeActionStateFromName is the read half of the pair above — a state
// id back into the state it names (W3C SCXML 3.3).
//
// A host that persists where a machine was has to write it down as TEXT: the
// constants above are a build artefact of one binary, and the process that
// resumes is a different one. String publishes the name and this reads it back,
// which is what lets a journal survive its own record and reach
// sce.Engine.EnterAt.
//
// The second return is false for a name this document does not declare. A name
// guessed at rather than refused is how a restore reaches a configuration
// nobody recorded — and the refusal is what makes a typo in a journal a
// reported failure instead of a machine quietly somewhere else.
//
// Emitted from the same loop over the document's states as String, so the two
// age together.
func StatechartNativeActionStateFromName(name string) (StatechartNativeActionState, bool) {
	switch name {
	case "assembling":
		return StatechartNativeActionStateAssembling, true
	case "faulted":
		return StatechartNativeActionStateFaulted, true
	case "idle":
		return StatechartNativeActionStateIdle, true
	}
	var zero StatechartNativeActionState
	return zero, false
}

// StatechartNativeActionAllStates is every state this document declares, in the
// order the constants above are issued.
//
// Emitted from the same loop, so a walk over it is a walk over the document
// rather than over a list somebody maintained beside it — a test that spells
// its own list goes on passing when the document grows a state.
var StatechartNativeActionAllStates = []StatechartNativeActionState{
	StatechartNativeActionStateAssembling,
	StatechartNativeActionStateFaulted,
	StatechartNativeActionStateIdle,
}

// ======================================================================
// Event type (W3C SCXML 3.12)
// ======================================================================

type StatechartNativeActionEvent int

const (
	StatechartNativeActionEventErrorExecution StatechartNativeActionEvent = 0
	StatechartNativeActionEventFragmentReceived StatechartNativeActionEvent = 1
	StatechartNativeActionEventReset StatechartNativeActionEvent = 2
	StatechartNativeActionEventSelftest StatechartNativeActionEvent = 3
	// W3C SCXML 3.13: Sentinel for eventless transition dispatch
	StatechartNativeActionEventNull StatechartNativeActionEvent = 4
)

func (e StatechartNativeActionEvent) String() string {
	switch e {
	case StatechartNativeActionEventErrorExecution:
		return "error.execution"
	case StatechartNativeActionEventFragmentReceived:
		return "fragment.received"
	case StatechartNativeActionEventReset:
		return "reset"
	case StatechartNativeActionEventSelftest:
		return "selftest"
	case StatechartNativeActionEventNull:
		return ""
	}
	return "unknown"
}

// ── NL→IR Item C1 Path A: typed `_event.data` payload channel ─────────
// NL→IR Item C1 Path A (EventSchema MCU native lowering): typed
// `_event.data` payload channel for the EventSchema-imported events whose
// transition guards lowered to a native Go comparison (no script engine).
// The Go twin of the Rust `StatechartNativeActionPayload` enum / C11 tagged union: a tag
// plus one `pending<Event>Payload` field per guarded event on the policy.
type StatechartNativeActionPayloadTag int

const (
	StatechartNativeActionPayloadTagNone StatechartNativeActionPayloadTag = iota
	StatechartNativeActionPayloadTagFragmentReceived
)

// StatechartNativeActionFragmentReceivedPayload is the NL→IR Item C1 Path A typed `_event.data`
// payload for `fragment.received`. Internal to the package; consumers inject it via
// the `RaiseFragmentReceived` seam below (fields are unexported by design).
type StatechartNativeActionFragmentReceivedPayload struct {
	payload []byte
	offset uint32
}

// RaiseFragmentReceived is the NL→IR Item C1 Path A per-event typed `_event.data`
// inject seam for `fragment.received` — binds the event name and the payload field
// values in one call so the name↔type pairing cannot be constructed
// inconsistently. Twin of the Rust `RaiseFragmentReceived` extension-trait method
// and the C11 `raise_<event>_typed` entry.
func RaiseFragmentReceived(e *sce.Engine[StatechartNativeActionState, StatechartNativeActionEvent], payload []byte, offset uint32) {
	e.RaiseExternalWithMeta(sce.EventWithMetadata[StatechartNativeActionEvent]{
		Event:    StatechartNativeActionEventFragmentReceived,
		Metadata: sce.EventMetadata{EventType: sce.EventTypeExternal, TypedPayload: StatechartNativeActionFragmentReceivedPayload{payload: payload, offset: offset, }},
	})
}


// ── W3C SCXML G.7: `<sce:action>` host dispatch ───────────────────────
// W3C SCXML G.7: host operations dispatched by `<sce:action>`.
// The host supplies the side effects while the statechart keeps each
// operation symbolic. No runtime script engine is involved.
type StatechartNativeActionActions interface {
	AppendFragmentPayload(payload []byte, offset uint32)
	OnAssemblingExit()
	OnIdleEntry()
	ResetSlot()
}

// ======================================================================
// Policy struct
// ======================================================================

type StatechartNativeActionPolicy struct {
	// W3C SCXML G.7: the host implementation every `<sce:action>` in the
	// document calls directly (`p.actions.<Op>(…)`) instead of a runtime
	// script engine. Taken at construction, because the initial state's
	// `<onentry>` may already perform an act — a host installed afterwards
	// would arrive one act too late.
	actions StatechartNativeActionActions
	// NL→IR Item C1 Path A: the current event's typed `_event.data` payload,
	// lifted from the dequeued event by PopulateEventMetadata and read by the
	// native transition guards (`p.pending<Event>Payload.<field>`). Mirrors
	// the Rust policy's `pending_payload` / the C11 `sm->pending_payload`.
	pendingPayloadTag StatechartNativeActionPayloadTag
	pendingFragmentReceivedPayload StatechartNativeActionFragmentReceivedPayload

	// W3C SCXML 3.13: Last transition metadata
	lastTransitionIsInternal  bool
	lastTransitionIsTargetless bool
	lastTransitionSourceState StatechartNativeActionState
	// W3C SCXML 3.13: Transition action tracking
	lastTransitionIndex   int
	hasTransitionActions   bool
	// W3C SCXML 5.10: Session ID
	SessionID string
	// W3C SCXML 6.4: Parent communication
	ParentExternalQueue chan sce.ParentEvent
	InvokeID           string
	ChildSessionID     string
	// §scxml-6.4.3: invoke param VALUES staged by the parent, applied after
	// this machine's datamodel init so they override its `<data>` defaults.
	pendingParams      map[string]interface{}
}

// NewStatechartNativeActionPolicy creates a new policy with default values.
//
// W3C SCXML G.7: `actions` is the host implementation of every `<sce:action>`
// this document declares. It is a parameter rather than a setter because the
// initial state's `<onentry>` can already perform an act — this is the one
// call every host has to make, so it is where the requirement belongs.
func NewStatechartNativeActionPolicy(actions StatechartNativeActionActions) StatechartNativeActionPolicy {
	return StatechartNativeActionPolicy{
		actions: actions,
		lastTransitionSourceState: StatechartNativeActionStateIdle,
	}
}



// No script engine needed for this state machine.
func (p *StatechartNativeActionPolicy) InitializeDataModel(eng *sce.Engine[StatechartNativeActionState, StatechartNativeActionEvent]) {}

// SetParamValueInScriptEngine is a no-op for state machines without script engine.
func (p *StatechartNativeActionPolicy) SetParamValueInScriptEngine(name string, value interface{}) {}


// ExecutePendingInvokes is a no-op (no invokes in this SM).
func (p *StatechartNativeActionPolicy) ExecutePendingInvokes(engine *sce.Engine[StatechartNativeActionState, StatechartNativeActionEvent]) {}

// TickChildren is a no-op (no invokes in this SM).
func (p *StatechartNativeActionPolicy) TickChildren(engine *sce.Engine[StatechartNativeActionState, StatechartNativeActionEvent]) {}

// ======================================================================
// StatePolicy interface implementation
// ======================================================================

// InitialState returns the initial state (W3C SCXML 3.3).
func (p *StatechartNativeActionPolicy) InitialState() StatechartNativeActionState {
	return StatechartNativeActionStateIdle
}

// IsFinalState returns true if state is a <final> state (W3C SCXML 3.7).
func (p *StatechartNativeActionPolicy) IsFinalState(state StatechartNativeActionState) bool {
	switch state {
	}
	return false
}

// GetParent returns the parent of state, or (zero, false) if root (W3C SCXML 3.3).
func (p *StatechartNativeActionPolicy) GetParent(state StatechartNativeActionState) (StatechartNativeActionState, bool) {
	switch state {
	}
	return 0, false
}

// IsCompoundState returns true if state has children (W3C SCXML 3.3).
func (p *StatechartNativeActionPolicy) IsCompoundState(state StatechartNativeActionState) bool {
	switch state {
	}
	return false
}

func (p *StatechartNativeActionPolicy) IsParallelState(_ StatechartNativeActionState) bool { return false }
func (p *StatechartNativeActionPolicy) GetParallelRegions(_ StatechartNativeActionState) []StatechartNativeActionState { return nil }

// IsDescendantOf returns true if desc is a descendant of anc (W3C SCXML 3.12).
func (p *StatechartNativeActionPolicy) IsDescendantOf(desc, anc StatechartNativeActionState) bool {
	current := desc
	for {
		parent, ok := p.GetParent(current)
		if !ok {
			return false
		}
		if parent == anc {
			return true
		}
		current = parent
	}
}

// GetDocumentOrder returns the document order index (W3C SCXML Appendix D).
func (p *StatechartNativeActionPolicy) GetDocumentOrder(state StatechartNativeActionState) int {
	switch state {
	case StatechartNativeActionStateAssembling:
		return 1
	case StatechartNativeActionStateFaulted:
		return 2
	case StatechartNativeActionStateIdle:
		return 0
	}
	return -1
}

// GetEventName returns the string name of an event (W3C SCXML 3.12).
func (p *StatechartNativeActionPolicy) GetEventName(event StatechartNativeActionEvent) string {
	return event.String()
}

// GetEventFromName looks up an event by name (W3C SCXML 3.12).
func (p *StatechartNativeActionPolicy) GetEventFromName(name string) (StatechartNativeActionEvent, bool) {
	switch name {
	case "error.execution":
		return StatechartNativeActionEventErrorExecution, true
	case "fragment.received":
		return StatechartNativeActionEventFragmentReceived, true
	case "reset":
		return StatechartNativeActionEventReset, true
	case "selftest":
		return StatechartNativeActionEventSelftest, true
	}
	return StatechartNativeActionEventNull, false
}

// GetStateName returns the string name of a state (W3C SCXML 3.3).
func (p *StatechartNativeActionPolicy) GetStateName(state StatechartNativeActionState) string {
	return state.String()
}

// GetStateFromName reads a state id back into the state it names (W3C SCXML 3.3).
// The reverse of GetStateName, and what turns a host's recorded configuration
// back into the argument sce.Engine.EnterAt takes.
func (p *StatechartNativeActionPolicy) GetStateFromName(name string) (StatechartNativeActionState, bool) {
	return StatechartNativeActionStateFromName(name)
}

// NullEvent returns the sentinel for eventless transition dispatch (W3C SCXML 3.13).
func (p *StatechartNativeActionPolicy) NullEvent() StatechartNativeActionEvent {
	return StatechartNativeActionEventNull
}

// GetInitialChildren returns initial children of a compound state (W3C SCXML 3.6).
func (p *StatechartNativeActionPolicy) GetInitialChildren(state StatechartNativeActionState) []StatechartNativeActionState {
	switch state {
	}
	return nil
}

// LastTransitionIsInternal returns the internal transition flag (W3C SCXML 3.13).
func (p *StatechartNativeActionPolicy) LastTransitionIsInternal() bool {
	return p.lastTransitionIsInternal
}

// SetLastTransitionIsInternal sets the internal transition flag.
func (p *StatechartNativeActionPolicy) SetLastTransitionIsInternal(value bool) {
	p.lastTransitionIsInternal = value
}

// LastTransitionIsTargetless returns the targetless transition flag (W3C SCXML 3.13).
func (p *StatechartNativeActionPolicy) LastTransitionIsTargetless() bool {
	return p.lastTransitionIsTargetless
}

// SetLastTransitionIsTargetless sets the targetless transition flag.
func (p *StatechartNativeActionPolicy) SetLastTransitionIsTargetless(value bool) {
	p.lastTransitionIsTargetless = value
}

// LastTransitionSourceState returns the source state of the last transition.
func (p *StatechartNativeActionPolicy) LastTransitionSourceState() StatechartNativeActionState {
	return p.lastTransitionSourceState
}

// SetLastTransitionSourceState sets the source state of the last transition.
func (p *StatechartNativeActionPolicy) SetLastTransitionSourceState(state StatechartNativeActionState) {
	p.lastTransitionSourceState = state
}



// HasParallelStates returns whether the SM has parallel states.
func (p *StatechartNativeActionPolicy) HasParallelStates() bool {
	return false
}

// NeedsScriptEngine returns whether the SM needs a script engine.
func (p *StatechartNativeActionPolicy) NeedsScriptEngine() bool {
	return false
}

// NeedsEventScheduler reports whether a host must drive this machine with
// Tick rather than Step alone.
//
// W3C SCXML 6.2: Step runs a macrostep and never drains the delayed-send
// scheduler nor ticks an invoked child, so a host that only ever calls it
// waits forever with nothing said. Same verdict the generate manifest
// publishes as needs_event_scheduler.
func (p *StatechartNativeActionPolicy) NeedsEventScheduler() bool {
	return false
}

// NeedsDataModelInit returns whether the SM needs datamodel init.
func (p *StatechartNativeActionPolicy) NeedsDataModelInit() bool {
	return false
}

func (p *StatechartNativeActionPolicy) HasInvokeSupport() bool { return false }
func (p *StatechartNativeActionPolicy) HasChildTick() bool { return false }
func (p *StatechartNativeActionPolicy) HasFinalize() bool { return false }
func (p *StatechartNativeActionPolicy) HasAutoforward() bool { return false }
func (p *StatechartNativeActionPolicy) HasActiveStates() bool { return false }
func (p *StatechartNativeActionPolicy) GetActiveStates() []StatechartNativeActionState { return nil }
// W3C SCXML 3.4: this machine keeps no active set — its configuration is the
// parent walk from the current state — so there is nothing for a restore to
// hand back here. sce.Engine.EnterAt reaches this only through HasActiveStates,
// which is false above; the method exists because the interface is one contract.
func (p *StatechartNativeActionPolicy) SetActiveStates(_ []StatechartNativeActionState) {}
func (p *StatechartNativeActionPolicy) HasExternalEventFlag() bool { return false }
func (p *StatechartNativeActionPolicy) SetNextEventIsExternal(_ bool) {}
// GetInitialOrHistoryChild returns the initial child considering history (W3C SCXML 3.11).
func (p *StatechartNativeActionPolicy) GetInitialOrHistoryChild(state StatechartNativeActionState) StatechartNativeActionState {
	children := p.GetInitialChildren(state)
	if len(children) > 0 {
		return children[0]
	}
	return state
}
// ExecuteFinalizeForChildEvent is a no-op (no finalize invokes).
func (p *StatechartNativeActionPolicy) ExecuteFinalizeForChildEvent(_ *sce.EventWithMetadata[StatechartNativeActionEvent], _ *sce.Engine[StatechartNativeActionState, StatechartNativeActionEvent]) {}
// ForwardToAutoforwardChildren is a no-op (no autoforward invokes).
func (p *StatechartNativeActionPolicy) ForwardToAutoforwardChildren(_ string, _ sce.EventMetadata, _ *sce.Engine[StatechartNativeActionState, StatechartNativeActionEvent]) {}

// PopulateEventMetadata stores pending event metadata (W3C SCXML 5.10).
// Note: event name is set separately via setCurrentEvent(), not from metadata.
func (p *StatechartNativeActionPolicy) PopulateEventMetadata(meta *sce.EventMetadata) {
	// NL→IR Item C1 Path A: lift the dequeued event's typed payload from the
	// type-erased carrier into the typed policy field the native guards read
	// (twin of the C11 pop loop's `sm->pending_payload = evt.payload`).
	p.pendingPayloadTag = StatechartNativeActionPayloadTagNone
	switch v := meta.TypedPayload.(type) {
	case StatechartNativeActionFragmentReceivedPayload:
		p.pendingPayloadTag = StatechartNativeActionPayloadTagFragmentReceived
		p.pendingFragmentReceivedPayload = v
	}

}

// ClearEventMetadata resets pending event metadata (W3C SCXML 5.10).
func (p *StatechartNativeActionPolicy) ClearEventMetadata() {
	// NL→IR Item C1 Path A: reset the typed-payload tag (PopulateEventMetadata
	// always re-establishes it for the next event; this keeps a stale payload
	// from being read between macrosteps).
	p.pendingPayloadTag = StatechartNativeActionPayloadTagNone

}




// ExecuteEntryActions executes onentry actions for a state (W3C SCXML 3.8).
//line statechart_native_action.scxml:31
func (p *StatechartNativeActionPolicy) ExecuteEntryActions(state StatechartNativeActionState, engine *sce.Engine[StatechartNativeActionState, StatechartNativeActionEvent], pathChild *StatechartNativeActionState) {
	// Only a `<parallel>` machine descends into defaults here, so a machine
	// without one has nothing to tell an ancestor entry from a target entry.
	_ = pathChild
	switch state {
	case StatechartNativeActionStateIdle:
		//line statechart_native_action.scxml:38
		// W3C SCXML 3.8: onentry block 0 (break on error stops subsequent actions)
		for actionBlock0 := 0; actionBlock0 < 1; actionBlock0++ {
			_ = actionBlock0

		// W3C SCXML G.7: <sce:action name="on_idle_entry">
		p.actions.OnIdleEntry()
		}
	default:
		// No entry actions
	}
}

// ExecuteExitActions executes onexit actions for a state (W3C SCXML 3.9).
//line statechart_native_action.scxml:31
func (p *StatechartNativeActionPolicy) ExecuteExitActions(state StatechartNativeActionState, engine *sce.Engine[StatechartNativeActionState, StatechartNativeActionEvent], preTransitionActive []StatechartNativeActionState) {
	switch state {
	case StatechartNativeActionStateAssembling:
		//line statechart_native_action.scxml:52
		// W3C SCXML 3.9: onexit block 0
		for exitBlock0 := 0; exitBlock0 < 1; exitBlock0++ {
			_ = exitBlock0

		// W3C SCXML G.7: <sce:action name="on_assembling_exit">
		p.actions.OnAssemblingExit()
		}
	default:
		// No exit actions
	}
}

// ProcessTransition evaluates guards and takes a matching transition (W3C SCXML 3.13).
// Returns true if a transition was taken.
//line statechart_native_action.scxml:31
func (p *StatechartNativeActionPolicy) ProcessTransition(currentState *StatechartNativeActionState, event StatechartNativeActionEvent, engine *sce.Engine[StatechartNativeActionState, StatechartNativeActionEvent]) bool {

	// W3C SCXML 3.12: Try transitions in current state first
	if p.tryTransitionInState(*currentState, event, currentState, engine) {
		return true
	}


	return false
}


// tryTransitionInState checks transitions for a single state.
//line statechart_native_action.scxml:31
func (p *StatechartNativeActionPolicy) tryTransitionInState(checkState StatechartNativeActionState, event StatechartNativeActionEvent, currentState *StatechartNativeActionState, engine *sce.Engine[StatechartNativeActionState, StatechartNativeActionEvent]) bool {
	switch checkState {
	case StatechartNativeActionStateAssembling:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == StatechartNativeActionEventReset {
			*currentState = StatechartNativeActionStateIdle
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = StatechartNativeActionStateAssembling
			p.lastTransitionIndex = 0
			p.hasTransitionActions = true
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == StatechartNativeActionEventErrorExecution {
			*currentState = StatechartNativeActionStateFaulted
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = StatechartNativeActionStateAssembling
			p.lastTransitionIndex = 1
			p.hasTransitionActions = false
			return true
		}
	case StatechartNativeActionStateFaulted:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == StatechartNativeActionEventReset {
			*currentState = StatechartNativeActionStateIdle
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = StatechartNativeActionStateFaulted
			p.lastTransitionIndex = 0
			p.hasTransitionActions = false
			return true
		}
	case StatechartNativeActionStateIdle:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == StatechartNativeActionEventFragmentReceived {
			*currentState = StatechartNativeActionStateAssembling
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = StatechartNativeActionStateIdle
			p.lastTransitionIndex = 0
			p.hasTransitionActions = true
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == StatechartNativeActionEventSelftest {
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = true
			p.lastTransitionSourceState = StatechartNativeActionStateIdle
			p.lastTransitionIndex = 1
			p.hasTransitionActions = true
			return true
		}
	}
	return false
}

// ExecuteTransitionActions executes actions for the last taken transition (W3C SCXML 3.13).
//line statechart_native_action.scxml:31
func (p *StatechartNativeActionPolicy) ExecuteTransitionActions(engine *sce.Engine[StatechartNativeActionState, StatechartNativeActionEvent]) {
	if !p.hasTransitionActions {
		return
	}
	source := p.lastTransitionSourceState
	idx := p.lastTransitionIndex
	if source == StatechartNativeActionStateAssembling && idx == 0 {
		//line statechart_native_action.scxml:56

		// W3C SCXML G.7: <sce:action name="reset_slot">
		p.actions.ResetSlot()
		return
	}
	if source == StatechartNativeActionStateIdle && idx == 0 {
		//line statechart_native_action.scxml:42

		// W3C SCXML G.7: <sce:action name="append_fragment_payload">
		if p.pendingPayloadTag == StatechartNativeActionPayloadTagFragmentReceived {
		p.actions.AppendFragmentPayload(p.pendingFragmentReceivedPayload.payload, p.pendingFragmentReceivedPayload.offset)
	} else {
		engine.Raise(sce.NewPlatformError(StatechartNativeActionEventErrorExecution, "<sce:action name='append_fragment_payload'> needs the typed payload of 'fragment.received', which this delivery did not carry"))
	}
		return
	}
	if source == StatechartNativeActionStateIdle && idx == 1 {
		//line statechart_native_action.scxml:48

	engine.Raise(sce.NewEventWithMetadata(StatechartNativeActionEventFragmentReceived))

		return
	}
}
