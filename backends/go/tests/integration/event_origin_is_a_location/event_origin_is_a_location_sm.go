// SCE-GENERATED — DO NOT EDIT
// source-hash: c56e8b2e82b26aafed117bfaa06905c41b2c8e5d207725d3f84b7293eb1eb4ee
// template-hash: 0afb1f0f0230f40c373aa80a890f61f2cc90b35724e7d86493a9e44e197b2d1b
// generated-at: 0


// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) 2025-2026 [Author of input SCXML file]
//
// Generated by SCE Code Generator (Go backend)
// From: integration_resources/event_origin_is_a_location/event_origin_is_a_location.scxml
//
// This generated code is MIT licensed and may be freely used in any project.
// Runtime engine dependency (sce-go-runtime): LGPL-2.1-or-later WITH LicenseRef-SCE-Linking-Exception OR LicenseRef-SCE-Commercial
// Full license: https://github.com/newmassrael/scxml-core-engine/blob/main/LICENSE
// Generated Go state machine for `event_origin_is_a_location`.
//
// Generated by SCE Code Generator (Go backend).
// Implements sce.StatePolicy with full transition logic,
// entry/exit actions, and event processing.


// SCE-MAP: event_origin_is_a_location.scxml:40 :: _machine

package event_origin_is_a_location

import (
	"fmt"
	"path/filepath"
	"strings"
	"time"

	sce "github.com/newmassrael/sce-go-runtime"
)

// Ensure imports are used
var (
	_ = fmt.Sprintf
	_ = filepath.Join
	_ = strings.HasPrefix
	_ = time.Duration(0)
)

// ======================================================================
// State type (W3C SCXML 3.3)
// ======================================================================

type EventOriginIsALocationState int

const (
	EventOriginIsALocationStateAwaitReply EventOriginIsALocationState = 0
	EventOriginIsALocationStateFail EventOriginIsALocationState = 1
	EventOriginIsALocationStatePass EventOriginIsALocationState = 2
	EventOriginIsALocationStatePhase EventOriginIsALocationState = 3
	EventOriginIsALocationStateWaiting EventOriginIsALocationState = 4
)

func (s EventOriginIsALocationState) String() string {
	switch s {
	case EventOriginIsALocationStateAwaitReply:
		return "await_reply"
	case EventOriginIsALocationStateFail:
		return "fail"
	case EventOriginIsALocationStatePass:
		return "pass"
	case EventOriginIsALocationStatePhase:
		return "phase"
	case EventOriginIsALocationStateWaiting:
		return "waiting"
	}
	return "unknown"
}

// EventOriginIsALocationStateFromName is the read half of the pair above — a state
// id back into the state it names (W3C SCXML 3.3).
//
// A host that persists where a machine was has to write it down as TEXT: the
// constants above are a build artefact of one binary, and the process that
// resumes is a different one. String publishes the name and this reads it back,
// which is what lets a journal survive its own record and reach
// sce.Engine.EnterAt.
//
// The second return is false for a name this document does not declare. A name
// guessed at rather than refused is how a restore reaches a configuration
// nobody recorded — and the refusal is what makes a typo in a journal a
// reported failure instead of a machine quietly somewhere else.
//
// Emitted from the same loop over the document's states as String, so the two
// age together.
func EventOriginIsALocationStateFromName(name string) (EventOriginIsALocationState, bool) {
	switch name {
	case "await_reply":
		return EventOriginIsALocationStateAwaitReply, true
	case "fail":
		return EventOriginIsALocationStateFail, true
	case "pass":
		return EventOriginIsALocationStatePass, true
	case "phase":
		return EventOriginIsALocationStatePhase, true
	case "waiting":
		return EventOriginIsALocationStateWaiting, true
	}
	var zero EventOriginIsALocationState
	return zero, false
}

// EventOriginIsALocationAllStates is every state this document declares, in the
// order the constants above are issued.
//
// Emitted from the same loop, so a walk over it is a walk over the document
// rather than over a list somebody maintained beside it — a test that spells
// its own list goes on passing when the document grows a state.
var EventOriginIsALocationAllStates = []EventOriginIsALocationState{
	EventOriginIsALocationStateAwaitReply,
	EventOriginIsALocationStateFail,
	EventOriginIsALocationStatePass,
	EventOriginIsALocationStatePhase,
	EventOriginIsALocationStateWaiting,
}

// ======================================================================
// Event type (W3C SCXML 3.12)
// ======================================================================

type EventOriginIsALocationEvent int

const (
	EventOriginIsALocationEventCancelInvoke EventOriginIsALocationEvent = 0
	EventOriginIsALocationEventDoneInvoke EventOriginIsALocationEvent = 1
	EventOriginIsALocationEventErrorCommunication EventOriginIsALocationEvent = 2
	EventOriginIsALocationEventErrorExecution EventOriginIsALocationEvent = 3
	EventOriginIsALocationEventFromChild EventOriginIsALocationEvent = 4
	EventOriginIsALocationEventReply EventOriginIsALocationEvent = 5
	EventOriginIsALocationEventReplyArrived EventOriginIsALocationEvent = 6
	// W3C SCXML 3.13: Sentinel for eventless transition dispatch
	EventOriginIsALocationEventNull EventOriginIsALocationEvent = 7
)

func (e EventOriginIsALocationEvent) String() string {
	switch e {
	case EventOriginIsALocationEventCancelInvoke:
		return "cancel.invoke"
	case EventOriginIsALocationEventDoneInvoke:
		return "done.invoke"
	case EventOriginIsALocationEventErrorCommunication:
		return "error.communication"
	case EventOriginIsALocationEventErrorExecution:
		return "error.execution"
	case EventOriginIsALocationEventFromChild:
		return "fromChild"
	case EventOriginIsALocationEventReply:
		return "reply"
	case EventOriginIsALocationEventReplyArrived:
		return "replyArrived"
	case EventOriginIsALocationEventNull:
		return ""
	}
	return "unknown"
}

// ======================================================================
// Policy struct
// ======================================================================

type EventOriginIsALocationPolicy struct {
	// W3C SCXML 3.13: Last transition metadata
	lastTransitionIsInternal  bool
	lastTransitionIsTargetless bool
	lastTransitionSourceState EventOriginIsALocationState
	// W3C SCXML 3.13: Transition action tracking
	lastTransitionIndex   int
	hasTransitionActions   bool
	// W3C SCXML 5.10.1: External event flag
	nextEventIsExternal bool
	pendingEventName string
	pendingEventData string
	pendingEventType string
	pendingEventSendid string
	pendingEventOrigin string
	pendingEventOrigintype string
	pendingEventInvokeid string
	// W3C SCXML 5.10: Session ID
	SessionID string
	// W3C SCXML B.1: Script engine handle (per-instance, Path B+ Q1=(d) Go=interface ref)
	ScriptEngine sce.IScriptEngine
	scriptEngineInitialized bool
	// W3C SCXML C.2.3: inbound BasicHTTP endpoint serving this machine,
	// declared by the deployment before Initialize(). The address belongs to
	// whoever runs the listener, so the machine takes it from here rather than
	// guessing one. Empty means no such endpoint is deployed, and no BasicHTTP
	// entry is published in _ioprocessors.
	BasicHTTPAccessURI string
	pendingInvokes []sce.PendingInvoke[EventOriginIsALocationState]
	activeInvokes  map[string]*sce.ChildSession
	childInvPeer sce.ChildEngine
	pendingDoneInvokeInvPeer bool
	// W3C SCXML 6.4: Parent communication
	ParentExternalQueue chan sce.ParentEvent
	InvokeID           string
	ChildSessionID     string
	// §scxml-6.4.3: invoke param VALUES staged by the parent, applied after
	// this machine's datamodel init so they override its `<data>` defaults.
	pendingParams      map[string]interface{}
}

// NewEventOriginIsALocationPolicy creates a new policy with default values.
func NewEventOriginIsALocationPolicy() EventOriginIsALocationPolicy {
	return EventOriginIsALocationPolicy{
		lastTransitionSourceState: EventOriginIsALocationStateWaiting,
		pendingInvokes: make([]sce.PendingInvoke[EventOriginIsALocationState], 0),
		activeInvokes:  make(map[string]*sce.ChildSession),
	}
}




// ensureScriptEngine lazily initializes the script engine session.
//
// Engine DI Parity RFC: panics if ScriptEngine was not set on the policy
// before Initialize() — mirrors Kotlin's mandatory constructor parameter
// contract. Silent no-op would otherwise turn missed injection into a
// hidden runtime regression (every <assign>/<log>/guard becomes a no-op
// and the SM walks to its default-state final without diagnostic).
func (p *EventOriginIsALocationPolicy) ensureScriptEngine() {
	if p.scriptEngineInitialized {
		return
	}
	engine := p.ScriptEngine
	if engine == nil {
		panic("EventOriginIsALocationPolicy: ScriptEngine field must be set before Initialize()")
	}
	sessionID := p.SessionID
	if sessionID == "" {
		sessionID = sce.GenerateSessionID()
		p.SessionID = sessionID
	}
	if !engine.HasSession(sessionID) {
		_ = engine.CreateSession(sessionID)
	}
	// W3C SCXML C.1.1 / C.2.3: the _ioprocessors entries come from the same
	// helper every other backend uses, so a machine reads the same entry names
	// and the same addresses whichever one runs it.
	_ = engine.SetupSystemVariables(sessionID, "event_origin_is_a_location",
		sce.BuildIoProcessors(sessionID, p.BasicHTTPAccessURI))
	p.scriptEngineInitialized = true
}

// InitializeDataModel sets up datamodel via script engine (W3C SCXML 5.3).
func (p *EventOriginIsALocationPolicy) InitializeDataModel(eng *sce.Engine[EventOriginIsALocationState, EventOriginIsALocationEvent]) {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	sessionID := p.SessionID

	// W3C SCXML 5.3: Initialize _sessionid
	_ = engine.SetVariable(sessionID, "_sessionid", sessionID)

	// W3C SCXML 5.3: Initialize _name
	_ = engine.SetVariable(sessionID, "_name", "event_origin_is_a_location")

	// W3C SCXML 5.2.2: Initialize global datamodel variables



	// §scxml-6.4.3: apply the invoke params the parent staged — overriding the
	// `<data>` default this block just seeded, and only for a name the child
	// declares ("if the names do not match, the Processor MUST NOT add the
	// value ... to the invoked session's data model"). `HasVariable` is that
	// filter, and it is this channel's only copy of the rule.
	if len(p.pendingParams) > 0 {
		for paramName, paramValue := range p.pendingParams {
			if engine.HasVariable(sessionID, paramName) {
				_ = engine.SetVariable(sessionID, paramName, paramValue)
			}
		}
		p.pendingParams = nil
	}
}

// evaluateGuard evaluates a guard condition via script engine.
// W3C SCXML 4.3.2: If a condition expression cannot be evaluated, the processor
// MUST treat it as false and MUST raise error.execution.
func (p *EventOriginIsALocationPolicy) evaluateGuard(guard string, eng *sce.Engine[EventOriginIsALocationState, EventOriginIsALocationEvent]) bool {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	result, err := engine.EvaluateExpression(p.SessionID, guard)
	if err != nil {
		eng.Raise(sce.NewPlatformError(EventOriginIsALocationEventErrorExecution, "a <transition> cond failed to evaluate"))
		return false
	}
	return sce.ScriptToBool(result)
}

// executeScript executes a script block via script engine.
func (p *EventOriginIsALocationPolicy) executeScript(script string) error {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	return engine.ExecuteScript(p.SessionID, script)
}

// setCurrentEvent binds _event system variable (W3C SCXML 5.10) and answers
// which rung of §scxml-B-2-8-1 the payload got, because the binding is the
// only place that knows.
//
// This used to be `_ = engine.SetCurrentEvent(...)`: the ladder decided
// between a DOM, a value and a space-normalized string, and the decision was
// dropped one line later. A payload that announced structure and would not
// parse therefore reached the document as raw characters, every
// _event.data.<field> read empty, and nothing anywhere could say so — see
// sce.PayloadReading.
func (p *EventOriginIsALocationPolicy) setCurrentEvent(name string) sce.PayloadReading {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	data := p.pendingEventData
	eventType := p.pendingEventType
	sendID := p.pendingEventSendid
	// W3C SCXML C.1: `_event.origin` is the sender's published
	// `_ioprocessors` location, not its bare session id — and this is the one
	// place that publishes `_event` to the document, so this is where the id
	// becomes a location. The engine keeps the bare id in
	// `EventMetadata.Origin` because its session-keyed lookups (<finalize>
	// dispatch, cancelled-invoke filtering) match on it; converting at the
	// raise would make one value serve two consumers that need different
	// spellings. The conversion itself lives in `sce.PublishedOrigin`, the
	// port of the `IOProcessorHelper::publishedOrigin` the C++ engines share:
	// a second spelling of the rule is how the backends would stop agreeing.
	origin := sce.PublishedOrigin(p.pendingEventOrigin)
	originType := p.pendingEventOrigintype
	invokeID := p.pendingEventInvokeid
	reading, err := engine.SetCurrentEvent(p.SessionID, sce.SetCurrentEventArgs{
		Name:       name,
		Data:       data,
		Type:       eventType,
		SendID:     sendID,
		Origin:     origin,
		OriginType: originType,
		InvokeID:   invokeID,
	})
	if err != nil {
		// An engine that could not bind _event at all has a larger problem
		// than an unreadable payload, and reporting PayloadUndecodable for it
		// would point a host at the wrong thing.
		return sce.PayloadAbsent
	}
	return reading
}

// assignVariable assigns a value to a datamodel variable via script engine.
func (p *EventOriginIsALocationPolicy) assignVariable(location, expr string) error {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	script := location + " = " + expr
	return engine.ExecuteScript(p.SessionID, script)
}

// getVariable reads a datamodel variable from the script engine.
func (p *EventOriginIsALocationPolicy) getVariable(name string) interface{} {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	result, err := engine.GetVariable(p.SessionID, name)
	if err != nil {
		return nil
	}
	return result
}

// SetParamValueInScriptEngine stores an invoke param's VALUE for deferred
// application after datamodel init (§scxml-6.4.3).
//
// Params are applied at the end of InitializeDataModel, overriding the
// `<data>` default only for names the child declares. The value crosses as a
// value: this took the parent's value rendered as Lua source and
// re-evaluated it here until 2026-08-21, which survived every value a test
// had tried and lost the ones Lua cannot spell — `ToScriptLiteral` renders a
// non-finite number as `+Inf`, which is not a Lua expression, so an
// `<invoke>` `<param expr="1/0"/>` arrived as nothing at all.
func (p *EventOriginIsALocationPolicy) SetParamValueInScriptEngine(name string, value interface{}) {
	if p.pendingParams == nil {
		p.pendingParams = make(map[string]interface{})
	}
	p.pendingParams[name] = value
}


// ExecutePendingInvokes starts deferred child state machines (W3C SCXML 6.4).
// Port of Rust do_execute_pending_invokes().
func (p *EventOriginIsALocationPolicy) ExecutePendingInvokes(engine *sce.Engine[EventOriginIsALocationState, EventOriginIsALocationEvent]) {
	if len(p.pendingInvokes) == 0 {
		return
	}

	// W3C SCXML 6.4: Copy-and-clear to prevent iterator invalidation
	invokesToExecute := make([]sce.PendingInvoke[EventOriginIsALocationState], len(p.pendingInvokes))
	copy(invokesToExecute, p.pendingInvokes)
	p.pendingInvokes = p.pendingInvokes[:0]

	for _, pending := range invokesToExecute {
		if strings.Contains(pending.InvokeID, ".inv_peer") {

			// W3C SCXML 6.5: Generate child session ID for finalize origin matching
			childSessionID := fmt.Sprintf("%s.%s", p.SessionID, pending.InvokeID)

			// W3C SCXML 6.4: Create child state machine
			childPolicy := NewEventOriginIsALocationSceSynthInvokeInvPeerPolicy()
			childPolicy.ScriptEngine = p.ScriptEngine
			parentQueue := make(chan sce.ParentEvent, 100)
			childPolicy.ParentExternalQueue = parentQueue
			childPolicy.InvokeID = pending.InvokeID
			childPolicy.ChildSessionID = childSessionID
			// W3C SCXML C.1: the child adopts the id its parent recorded for
			// it. The parent mints `parentSession.invokeId` and keys
			// `activeInvokes` on it, so an event from this child reaches the
			// parent carrying that id as its origin; if the child also minted
			// an id of its own, the location it publishes in `_ioprocessors`
			// and the origin the parent sees would be two names for one
			// session that can never be compared — which is exactly what C.1
			// requires of them.
			childPolicy.SessionID = childSessionID



			// W3C SCXML 6.4.1: Track active invoke session BEFORE initialize
			p.activeInvokes["inv_peer"] = &sce.ChildSession{
				SessionID:       childSessionID,
				InvokeID:        "inv_peer",
				ParentSessionID: p.SessionID,
				Autoforward:     false,
				FinalizeScript:  "",
			}

			childEngine := sce.NewEngine[EventOriginIsALocationSceSynthInvokeInvPeerState, EventOriginIsALocationSceSynthInvokeInvPeerEvent](&childPolicy)
			// §scxml-6.4: the child's delayed sends are measured against the
			// same clock as ours. A child reading its own would start its
			// origin at construction time, so <send delay="100ms"> on either
			// side of the boundary would mean two different absolute instants —
			// and on a host-owned clock the child would not move at all,
			// because the host advances the engine it holds, not the ones that
			// engine invoked.
			childEngine.SetClock(engine.Clock())
			childEngine.SetCompletionCallback(func() {})
			childEngine.Initialize()

			// W3C SCXML 6.4: Store child and create wrapper
			wrapper := &childEngineWrapperInvPeer{engine: childEngine, policy: &childPolicy}
			p.childInvPeer = wrapper

			// W3C SCXML 6.4: Drain child-to-parent events raised during initialize
			sce.DrainAndRaiseChildEvents(wrapper, p.activeInvokes, "inv_peer", engine)

			// W3C SCXML 6.4: Check if child completed during initialize
			if wrapper.IsInFinalState() {
				p.pendingDoneInvokeInvPeer = true
				// W3C SCXML 5.5 + 6.3.1: Lift the child's stashed donedata onto
				// done.invoke.<id>._event.data. Mirrors the C++ AOT contract
				// and the Rust raise_done_invoke(donedata) 3-arg signature.
				sce.RaiseDoneInvoke("inv_peer", wrapper.DonedataAtFinal(), engine)
			}
			continue
		}
	}
}

// TickChildren ticks child state machines (W3C SCXML 6.4).
// Port of Rust do_tick_children().
func (p *EventOriginIsALocationPolicy) TickChildren(engine *sce.Engine[EventOriginIsALocationState, EventOriginIsALocationEvent]) {
	if !p.pendingDoneInvokeInvPeer && p.childInvPeer != nil {
		child := p.childInvPeer
		sce.DrainAndRaiseChildEvents(child, p.activeInvokes, "inv_peer", engine)
		child.Tick()
		sce.DrainAndRaiseChildEvents(child, p.activeInvokes, "inv_peer", engine)
		if child.IsInFinalState() && !p.pendingDoneInvokeInvPeer {
			p.pendingDoneInvokeInvPeer = true
			// W3C SCXML 5.5 + 6.3.1: Lift the child's stashed donedata onto
			// done.invoke.<id>._event.data.
			sce.RaiseDoneInvoke("inv_peer", child.DonedataAtFinal(), engine)
		}
	}
}


// DeliverToChildSession delivers an event addressed to a child's published
// location (W3C SCXML C.1).
//
// The parent mints `parentSession.invokeId` for each child and that id is what
// the child's `_ioprocessors` entry names, so a <send> whose target decodes to
// one of them is addressed to that child rather than to this machine. A false
// return means the address names no live child of ours and the event takes the
// normal external path — the routing half of C.1 is what makes the published
// location a usable target rather than a string that merely compares equal.
func (p *EventOriginIsALocationPolicy) DeliverToChildSession(childSessionID, eventName, eventData string) bool {
	if childSessionID == "" {
		return false
	}
	if cs, ok := p.activeInvokes["inv_peer"]; ok && cs.SessionID == childSessionID {
		if p.childInvPeer != nil {
			p.childInvPeer.RaiseExternalByName(eventName, eventData)
			return true
		}
	}
	return false
}


// ── Child engine wrappers (implement sce.ChildEngine interface) ──

type childEngineWrapperInvPeer struct {
	engine *sce.Engine[EventOriginIsALocationSceSynthInvokeInvPeerState, EventOriginIsALocationSceSynthInvokeInvPeerEvent]
	policy *EventOriginIsALocationSceSynthInvokeInvPeerPolicy
}
func (w *childEngineWrapperInvPeer) Initialize() { w.engine.Initialize() }
func (w *childEngineWrapperInvPeer) Tick() { w.engine.Tick() }
func (w *childEngineWrapperInvPeer) IsInFinalState() bool { return w.engine.IsInFinalState() }
func (w *childEngineWrapperInvPeer) RaiseExternalByName(name, data string) { w.engine.RaiseExternalByName(name, data) }
func (w *childEngineWrapperInvPeer) RaiseExternalByNameWithMeta(name string, metadata sce.EventMetadata) { w.engine.RaiseExternalByNameWithMeta(name, metadata) }
func (w *childEngineWrapperInvPeer) SetCompletionCallback(cb func()) { w.engine.SetCompletionCallback(cb) }
func (w *childEngineWrapperInvPeer) GetParentEventQueue() chan sce.ParentEvent { return w.policy.ParentExternalQueue }
func (w *childEngineWrapperInvPeer) DonedataAtFinal() string { return w.engine.DonedataAtFinal() }


// ======================================================================
// StatePolicy interface implementation
// ======================================================================

// InitialState returns the initial state (W3C SCXML 3.3).
func (p *EventOriginIsALocationPolicy) InitialState() EventOriginIsALocationState {
	return EventOriginIsALocationStateWaiting
}

// IsFinalState returns true if state is a <final> state (W3C SCXML 3.7).
func (p *EventOriginIsALocationPolicy) IsFinalState(state EventOriginIsALocationState) bool {
	switch state {
	case EventOriginIsALocationStateFail:
		return true
	case EventOriginIsALocationStatePass:
		return true
	}
	return false
}

// GetParent returns the parent of state, or (zero, false) if root (W3C SCXML 3.3).
func (p *EventOriginIsALocationPolicy) GetParent(state EventOriginIsALocationState) (EventOriginIsALocationState, bool) {
	switch state {
	case EventOriginIsALocationStateAwaitReply:
		return EventOriginIsALocationStatePhase, true
	case EventOriginIsALocationStateWaiting:
		return EventOriginIsALocationStatePhase, true
	}
	return 0, false
}

// IsCompoundState returns true if state has children (W3C SCXML 3.3).
func (p *EventOriginIsALocationPolicy) IsCompoundState(state EventOriginIsALocationState) bool {
	switch state {
	case EventOriginIsALocationStatePhase:
		return true
	}
	return false
}

func (p *EventOriginIsALocationPolicy) IsParallelState(_ EventOriginIsALocationState) bool { return false }
func (p *EventOriginIsALocationPolicy) GetParallelRegions(_ EventOriginIsALocationState) []EventOriginIsALocationState { return nil }

// IsDescendantOf returns true if desc is a descendant of anc (W3C SCXML 3.12).
func (p *EventOriginIsALocationPolicy) IsDescendantOf(desc, anc EventOriginIsALocationState) bool {
	current := desc
	for {
		parent, ok := p.GetParent(current)
		if !ok {
			return false
		}
		if parent == anc {
			return true
		}
		current = parent
	}
}

// GetDocumentOrder returns the document order index (W3C SCXML Appendix D).
func (p *EventOriginIsALocationPolicy) GetDocumentOrder(state EventOriginIsALocationState) int {
	switch state {
	case EventOriginIsALocationStateAwaitReply:
		return 2
	case EventOriginIsALocationStateFail:
		return 4
	case EventOriginIsALocationStatePass:
		return 3
	case EventOriginIsALocationStatePhase:
		return 0
	case EventOriginIsALocationStateWaiting:
		return 1
	}
	return -1
}

// GetEventName returns the string name of an event (W3C SCXML 3.12).
func (p *EventOriginIsALocationPolicy) GetEventName(event EventOriginIsALocationEvent) string {
	return event.String()
}

// GetEventFromName looks up an event by name (W3C SCXML 3.12).
func (p *EventOriginIsALocationPolicy) GetEventFromName(name string) (EventOriginIsALocationEvent, bool) {
	switch name {
	case "cancel.invoke":
		return EventOriginIsALocationEventCancelInvoke, true
	case "done.invoke":
		return EventOriginIsALocationEventDoneInvoke, true
	case "error.communication":
		return EventOriginIsALocationEventErrorCommunication, true
	case "error.execution":
		return EventOriginIsALocationEventErrorExecution, true
	case "fromChild":
		return EventOriginIsALocationEventFromChild, true
	case "reply":
		return EventOriginIsALocationEventReply, true
	case "replyArrived":
		return EventOriginIsALocationEventReplyArrived, true
	}
	return EventOriginIsALocationEventNull, false
}

// GetStateName returns the string name of a state (W3C SCXML 3.3).
func (p *EventOriginIsALocationPolicy) GetStateName(state EventOriginIsALocationState) string {
	return state.String()
}

// GetStateFromName reads a state id back into the state it names (W3C SCXML 3.3).
// The reverse of GetStateName, and what turns a host's recorded configuration
// back into the argument sce.Engine.EnterAt takes.
func (p *EventOriginIsALocationPolicy) GetStateFromName(name string) (EventOriginIsALocationState, bool) {
	return EventOriginIsALocationStateFromName(name)
}

// NullEvent returns the sentinel for eventless transition dispatch (W3C SCXML 3.13).
func (p *EventOriginIsALocationPolicy) NullEvent() EventOriginIsALocationEvent {
	return EventOriginIsALocationEventNull
}

// GetInitialChildren returns initial children of a compound state (W3C SCXML 3.6).
func (p *EventOriginIsALocationPolicy) GetInitialChildren(state EventOriginIsALocationState) []EventOriginIsALocationState {
	switch state {
	case EventOriginIsALocationStatePhase:
		return []EventOriginIsALocationState{
			EventOriginIsALocationStateWaiting,
		}
	}
	return nil
}

// LastTransitionIsInternal returns the internal transition flag (W3C SCXML 3.13).
func (p *EventOriginIsALocationPolicy) LastTransitionIsInternal() bool {
	return p.lastTransitionIsInternal
}

// SetLastTransitionIsInternal sets the internal transition flag.
func (p *EventOriginIsALocationPolicy) SetLastTransitionIsInternal(value bool) {
	p.lastTransitionIsInternal = value
}

// LastTransitionIsTargetless returns the targetless transition flag (W3C SCXML 3.13).
func (p *EventOriginIsALocationPolicy) LastTransitionIsTargetless() bool {
	return p.lastTransitionIsTargetless
}

// SetLastTransitionIsTargetless sets the targetless transition flag.
func (p *EventOriginIsALocationPolicy) SetLastTransitionIsTargetless(value bool) {
	p.lastTransitionIsTargetless = value
}

// LastTransitionSourceState returns the source state of the last transition.
func (p *EventOriginIsALocationPolicy) LastTransitionSourceState() EventOriginIsALocationState {
	return p.lastTransitionSourceState
}

// SetLastTransitionSourceState sets the source state of the last transition.
func (p *EventOriginIsALocationPolicy) SetLastTransitionSourceState(state EventOriginIsALocationState) {
	p.lastTransitionSourceState = state
}


// SetNextEventIsExternal sets the external event flag (W3C SCXML 5.10.1).
func (p *EventOriginIsALocationPolicy) SetNextEventIsExternal(value bool) {
	p.nextEventIsExternal = value
}

// HasParallelStates returns whether the SM has parallel states.
func (p *EventOriginIsALocationPolicy) HasParallelStates() bool {
	return false
}

// NeedsScriptEngine returns whether the SM needs a script engine.
func (p *EventOriginIsALocationPolicy) NeedsScriptEngine() bool {
	return true
}

// NeedsEventScheduler reports whether a host must drive this machine with
// Tick rather than Step alone.
//
// W3C SCXML 6.2: Step runs a macrostep and never drains the delayed-send
// scheduler nor ticks an invoked child, so a host that only ever calls it
// waits forever with nothing said. Same verdict the generate manifest
// publishes as needs_event_scheduler.
func (p *EventOriginIsALocationPolicy) NeedsEventScheduler() bool {
	return true
}

// NeedsDataModelInit returns whether the SM needs datamodel init.
func (p *EventOriginIsALocationPolicy) NeedsDataModelInit() bool {
	return true
}

// HasInvokeSupport returns true for invoke-capable SMs.
func (p *EventOriginIsALocationPolicy) HasInvokeSupport() bool { return true }
// HasChildTick returns true for child-ticking SMs.
func (p *EventOriginIsALocationPolicy) HasChildTick() bool { return true }
func (p *EventOriginIsALocationPolicy) HasFinalize() bool { return false }
func (p *EventOriginIsALocationPolicy) HasAutoforward() bool { return false }
func (p *EventOriginIsALocationPolicy) HasActiveStates() bool { return false }
func (p *EventOriginIsALocationPolicy) GetActiveStates() []EventOriginIsALocationState { return nil }
// W3C SCXML 3.4: this machine keeps no active set — its configuration is the
// parent walk from the current state — so there is nothing for a restore to
// hand back here. sce.Engine.EnterAt reaches this only through HasActiveStates,
// which is false above; the method exists because the interface is one contract.
func (p *EventOriginIsALocationPolicy) SetActiveStates(_ []EventOriginIsALocationState) {}
func (p *EventOriginIsALocationPolicy) HasExternalEventFlag() bool { return true }
// GetInitialOrHistoryChild returns the initial child considering history (W3C SCXML 3.11).
func (p *EventOriginIsALocationPolicy) GetInitialOrHistoryChild(state EventOriginIsALocationState) EventOriginIsALocationState {
	children := p.GetInitialChildren(state)
	if len(children) > 0 {
		return children[0]
	}
	return state
}
// ExecuteFinalizeForChildEvent is a no-op (no finalize invokes).
func (p *EventOriginIsALocationPolicy) ExecuteFinalizeForChildEvent(_ *sce.EventWithMetadata[EventOriginIsALocationEvent], _ *sce.Engine[EventOriginIsALocationState, EventOriginIsALocationEvent]) {}
// ForwardToAutoforwardChildren is a no-op (no autoforward invokes).
func (p *EventOriginIsALocationPolicy) ForwardToAutoforwardChildren(_ string, _ sce.EventMetadata, _ *sce.Engine[EventOriginIsALocationState, EventOriginIsALocationEvent]) {}

// PopulateEventMetadata stores pending event metadata (W3C SCXML 5.10).
// Note: event name is set separately via setCurrentEvent(), not from metadata.
func (p *EventOriginIsALocationPolicy) PopulateEventMetadata(meta *sce.EventMetadata) {
	p.pendingEventData = meta.Data
	p.pendingEventType = meta.EventType.String()
	p.pendingEventSendid = meta.SendID
	p.pendingEventOrigin = meta.Origin
	p.pendingEventOrigintype = meta.OriginType
	p.pendingEventInvokeid = meta.InvokeID
}

// ClearEventMetadata resets pending event metadata (W3C SCXML 5.10).
func (p *EventOriginIsALocationPolicy) ClearEventMetadata() {
	p.pendingEventName = ""
	p.pendingEventData = ""
	p.pendingEventType = ""
	p.pendingEventSendid = ""
	p.pendingEventOrigin = ""
	p.pendingEventOrigintype = ""
	p.pendingEventInvokeid = ""
}




// ExecuteEntryActions executes onentry actions for a state (W3C SCXML 3.8).
//line event_origin_is_a_location.scxml:40
func (p *EventOriginIsALocationPolicy) ExecuteEntryActions(state EventOriginIsALocationState, engine *sce.Engine[EventOriginIsALocationState, EventOriginIsALocationEvent], pathChild *EventOriginIsALocationState) {
	// Only a `<parallel>` machine descends into defaults here, so a machine
	// without one has nothing to tell an ancestor entry from a target entry.
	_ = pathChild
	p.ensureScriptEngine()
	switch state {
	case EventOriginIsALocationStatePhase:
		//line event_origin_is_a_location.scxml:49
		// W3C SCXML 6.4: Defer invoke execution until macrostep end
		{
			generatedInvokeID := fmt.Sprintf("%s.%d.inv_peer", "phase", sce.NextInvokeCounter())
			sce.DeferInvoke(&p.pendingInvokes, sce.PendingInvoke[EventOriginIsALocationState]{
				InvokeID: generatedInvokeID,
				State:    EventOriginIsALocationStatePhase,
			})
		}
	default:
		// No entry actions
	}
}

// ExecuteExitActions executes onexit actions for a state (W3C SCXML 3.9).
//line event_origin_is_a_location.scxml:40
func (p *EventOriginIsALocationPolicy) ExecuteExitActions(state EventOriginIsALocationState, engine *sce.Engine[EventOriginIsALocationState, EventOriginIsALocationEvent], preTransitionActive []EventOriginIsALocationState) {
	p.ensureScriptEngine()
	// W3C SCXML 6.4: Cancel pending invokes and cleanup active children on state exit
	switch state {
	case EventOriginIsALocationStatePhase:
		sce.CancelInvokesForState(&p.pendingInvokes, EventOriginIsALocationStatePhase)
		if p.childInvPeer != nil {
			p.childInvPeer = nil
		}
		delete(p.activeInvokes, "inv_peer")
		p.pendingDoneInvokeInvPeer = false
	default:
	}
	switch state {
	default:
		// No exit actions
	}
}

// ProcessTransition evaluates guards and takes a matching transition (W3C SCXML 3.13).
// Returns true if a transition was taken.
//line event_origin_is_a_location.scxml:40
func (p *EventOriginIsALocationPolicy) ProcessTransition(currentState *EventOriginIsALocationState, event EventOriginIsALocationEvent, engine *sce.Engine[EventOriginIsALocationState, EventOriginIsALocationEvent]) bool {
	// W3C SCXML 5.10: Bind _event system variable for guard evaluation
	if event != EventOriginIsALocationEventNull {
		// §scxml-B-2-8-1: the rung the payload got, handed to the engine
		// rather than dropped. This is the only frame that has both the
		// reading and the event it belongs to.
		engine.NotePayloadReading(event, p.setCurrentEvent(p.GetEventName(event)))
	}

	// W3C SCXML 3.12: Try transitions in current state first
	if p.tryTransitionInState(*currentState, event, currentState, engine) {
		return true
	}

	// W3C SCXML 3.13: Eventless transitions do NOT bubble to parent states
	if event == EventOriginIsALocationEventNull {
		return false
	}

	// W3C SCXML 3.12: Hierarchical event bubbling — walk up to parent
	checkState := *currentState
	for {
		parent, hasParent := p.GetParent(checkState)
		if !hasParent {
			break
		}
		if p.tryTransitionInState(parent, event, currentState, engine) {
			return true
		}
		checkState = parent
	}

	return false
}


// tryTransitionInState checks transitions for a single state.
//line event_origin_is_a_location.scxml:40
func (p *EventOriginIsALocationPolicy) tryTransitionInState(checkState EventOriginIsALocationState, event EventOriginIsALocationEvent, currentState *EventOriginIsALocationState, engine *sce.Engine[EventOriginIsALocationState, EventOriginIsALocationEvent]) bool {
	switch checkState {
	case EventOriginIsALocationStateAwaitReply:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == EventOriginIsALocationEventReplyArrived {
			*currentState = EventOriginIsALocationStatePass
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = EventOriginIsALocationStateAwaitReply
			p.lastTransitionIndex = 0
			p.hasTransitionActions = false
			return true
		}
	case EventOriginIsALocationStateWaiting:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == EventOriginIsALocationEventFromChild {
			if p.evaluateGuard(`_scxml_eq(_event.origin, _event.data.myLocation)`, engine) {
			*currentState = EventOriginIsALocationStateAwaitReply
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = EventOriginIsALocationStateWaiting
			p.lastTransitionIndex = 0
			p.hasTransitionActions = true
			return true
			}
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == EventOriginIsALocationEventFromChild {
			*currentState = EventOriginIsALocationStateFail
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = EventOriginIsALocationStateWaiting
			p.lastTransitionIndex = 1
			p.hasTransitionActions = false
			return true
		}
	}
	return false
}

// ExecuteTransitionActions executes actions for the last taken transition (W3C SCXML 3.13).
//line event_origin_is_a_location.scxml:40
func (p *EventOriginIsALocationPolicy) ExecuteTransitionActions(engine *sce.Engine[EventOriginIsALocationState, EventOriginIsALocationEvent]) {
	p.ensureScriptEngine()
	if !p.hasTransitionActions {
		return
	}
	source := p.lastTransitionSourceState
	idx := p.lastTransitionIndex
	if source == EventOriginIsALocationStateWaiting && idx == 0 {
		//line event_origin_is_a_location.scxml:75

	// W3C SCXML 6.2: send id="__send_0"
	{
		eventDataStr := ""
		_ = eventDataStr
	// W3C SCXML 6.2: External send
	{
		// W3C SCXML 6.2: Evaluate targetexpr at runtime
		sendTargetVal, sendTargetErr := p.ScriptEngine.EvaluateExpression(p.SessionID, `_event.origin`)
		if sendTargetErr != nil || sendTargetVal == nil || fmt.Sprintf("%v", sendTargetVal) == "<nil>" {
			// W3C SCXML 6.2.4: If target is not valid, raise error.communication
			{
				errEvt := sce.NewPlatformError(EventOriginIsALocationEventErrorCommunication, "<send> targetexpr evaluated to nothing, so there is no target to reach")
				errEvt.Metadata.SendID = "__send_0"
				engine.Raise(errEvt)
			}
		} else {
			sendEvtName := "reply"
			// W3C SCXML C.1: a target that decodes to one of our children's
			// published locations is addressed to that child. Without this the
			// address a peer was told to answer at routes back into the
			// sender's own queue, so the location compares equal and still
			// reaches nobody.
			addressedChild := sce.SessionIDFromScxmlLocation(fmt.Sprintf("%v", sendTargetVal))
			if !p.DeliverToChildSession(addressedChild, sendEvtName, eventDataStr) {
			if sendEvt, sendOk := p.GetEventFromName(sendEvtName); sendOk {
				meta := sce.NewEventWithMetadata(sendEvt)
				meta.Metadata = sce.ExternalMetadata("__send_0", "")
				meta.Metadata.Data = eventDataStr
				engine.RaiseExternalWithMeta(meta)
			}
			}
		}
	}
	}

		return
	}
}
