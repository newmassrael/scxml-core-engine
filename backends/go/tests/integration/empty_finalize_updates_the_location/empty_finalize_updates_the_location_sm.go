// SCE-GENERATED — DO NOT EDIT
// source-hash: c97edcb094613d8138825758fc943d853d23ad4854f2fa7dcf6ff6f58539b674
// template-hash: 672592645a46a971e7d9a638044244b01d838f9ebaf5e6860dc88538368c4548
// generated-at: 0


// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) 2025-2026 [Author of input SCXML file]
//
// Generated by SCE Code Generator (Go backend)
// From: integration_resources/empty_finalize_updates_the_location/empty_finalize_updates_the_location.scxml
//
// This generated code is MIT licensed and may be freely used in any project.
// Runtime engine dependency (sce-go-runtime): LGPL-2.1-or-later WITH LicenseRef-SCE-Linking-Exception OR LicenseRef-SCE-Commercial
// Full license: https://github.com/newmassrael/scxml-core-engine/blob/main/LICENSE
// Generated Go state machine for `empty_finalize_updates_the_location`.
//
// Generated by SCE Code Generator (Go backend).
// Implements sce.StatePolicy with full transition logic,
// entry/exit actions, and event processing.


// SCE-MAP: empty_finalize_updates_the_location.scxml:52 :: _machine

package empty_finalize_updates_the_location

import (
	"fmt"
	"path/filepath"
	"strings"
	"time"

	sce "github.com/newmassrael/sce-go-runtime"
)

// Ensure imports are used
var (
	_ = fmt.Sprintf
	_ = filepath.Join
	_ = strings.HasPrefix
	_ = time.Duration(0)
)

// ======================================================================
// State type (W3C SCXML 3.3)
// ======================================================================

type EmptyFinalizeUpdatesTheLocationState int

const (
	EmptyFinalizeUpdatesTheLocationStateAbsentPhase EmptyFinalizeUpdatesTheLocationState = 0
	EmptyFinalizeUpdatesTheLocationStateEmptyPhase EmptyFinalizeUpdatesTheLocationState = 1
	EmptyFinalizeUpdatesTheLocationStateFailAbsentChildSilent EmptyFinalizeUpdatesTheLocationState = 2
	EmptyFinalizeUpdatesTheLocationStateFailEmptyChildSilent EmptyFinalizeUpdatesTheLocationState = 3
	EmptyFinalizeUpdatesTheLocationStateFailNotUpdated EmptyFinalizeUpdatesTheLocationState = 4
	EmptyFinalizeUpdatesTheLocationStateFailUnmatchedChildSilent EmptyFinalizeUpdatesTheLocationState = 5
	EmptyFinalizeUpdatesTheLocationStateFailUnmatchedNameWrote EmptyFinalizeUpdatesTheLocationState = 6
	EmptyFinalizeUpdatesTheLocationStateFailUpdatedWithoutFinalize EmptyFinalizeUpdatesTheLocationState = 7
	EmptyFinalizeUpdatesTheLocationStatePass EmptyFinalizeUpdatesTheLocationState = 8
	EmptyFinalizeUpdatesTheLocationStateUnmatchedPhase EmptyFinalizeUpdatesTheLocationState = 9
)

func (s EmptyFinalizeUpdatesTheLocationState) String() string {
	switch s {
	case EmptyFinalizeUpdatesTheLocationStateAbsentPhase:
		return "absentPhase"
	case EmptyFinalizeUpdatesTheLocationStateEmptyPhase:
		return "emptyPhase"
	case EmptyFinalizeUpdatesTheLocationStateFailAbsentChildSilent:
		return "failAbsentChildSilent"
	case EmptyFinalizeUpdatesTheLocationStateFailEmptyChildSilent:
		return "failEmptyChildSilent"
	case EmptyFinalizeUpdatesTheLocationStateFailNotUpdated:
		return "failNotUpdated"
	case EmptyFinalizeUpdatesTheLocationStateFailUnmatchedChildSilent:
		return "failUnmatchedChildSilent"
	case EmptyFinalizeUpdatesTheLocationStateFailUnmatchedNameWrote:
		return "failUnmatchedNameWrote"
	case EmptyFinalizeUpdatesTheLocationStateFailUpdatedWithoutFinalize:
		return "failUpdatedWithoutFinalize"
	case EmptyFinalizeUpdatesTheLocationStatePass:
		return "pass"
	case EmptyFinalizeUpdatesTheLocationStateUnmatchedPhase:
		return "unmatchedPhase"
	}
	return "unknown"
}

// ======================================================================
// Event type (W3C SCXML 3.12)
// ======================================================================

type EmptyFinalizeUpdatesTheLocationEvent int

const (
	EmptyFinalizeUpdatesTheLocationEventCancelInvoke EmptyFinalizeUpdatesTheLocationEvent = 0
	EmptyFinalizeUpdatesTheLocationEventDoneInvoke EmptyFinalizeUpdatesTheLocationEvent = 1
	EmptyFinalizeUpdatesTheLocationEventErrorExecution EmptyFinalizeUpdatesTheLocationEvent = 2
	EmptyFinalizeUpdatesTheLocationEventFromAbsentChild EmptyFinalizeUpdatesTheLocationEvent = 3
	EmptyFinalizeUpdatesTheLocationEventFromEmptyChild EmptyFinalizeUpdatesTheLocationEvent = 4
	EmptyFinalizeUpdatesTheLocationEventFromUnmatchedChild EmptyFinalizeUpdatesTheLocationEvent = 5
	EmptyFinalizeUpdatesTheLocationEventTimeoutAbsent EmptyFinalizeUpdatesTheLocationEvent = 6
	EmptyFinalizeUpdatesTheLocationEventTimeoutEmpty EmptyFinalizeUpdatesTheLocationEvent = 7
	EmptyFinalizeUpdatesTheLocationEventTimeoutUnmatched EmptyFinalizeUpdatesTheLocationEvent = 8
	// W3C SCXML 3.13: Sentinel for eventless transition dispatch
	EmptyFinalizeUpdatesTheLocationEventNull EmptyFinalizeUpdatesTheLocationEvent = 9
)

func (e EmptyFinalizeUpdatesTheLocationEvent) String() string {
	switch e {
	case EmptyFinalizeUpdatesTheLocationEventCancelInvoke:
		return "cancel.invoke"
	case EmptyFinalizeUpdatesTheLocationEventDoneInvoke:
		return "done.invoke"
	case EmptyFinalizeUpdatesTheLocationEventErrorExecution:
		return "error.execution"
	case EmptyFinalizeUpdatesTheLocationEventFromAbsentChild:
		return "fromAbsentChild"
	case EmptyFinalizeUpdatesTheLocationEventFromEmptyChild:
		return "fromEmptyChild"
	case EmptyFinalizeUpdatesTheLocationEventFromUnmatchedChild:
		return "fromUnmatchedChild"
	case EmptyFinalizeUpdatesTheLocationEventTimeoutAbsent:
		return "timeoutAbsent"
	case EmptyFinalizeUpdatesTheLocationEventTimeoutEmpty:
		return "timeoutEmpty"
	case EmptyFinalizeUpdatesTheLocationEventTimeoutUnmatched:
		return "timeoutUnmatched"
	case EmptyFinalizeUpdatesTheLocationEventNull:
		return ""
	}
	return "unknown"
}

// ======================================================================
// Policy struct
// ======================================================================

type EmptyFinalizeUpdatesTheLocationPolicy struct {
	// W3C SCXML 3.13: Last transition metadata
	lastTransitionIsInternal  bool
	lastTransitionIsTargetless bool
	lastTransitionSourceState EmptyFinalizeUpdatesTheLocationState
	// W3C SCXML 5.10.1: External event flag
	nextEventIsExternal bool
	pendingEventName string
	pendingEventData string
	pendingEventType string
	pendingEventSendid string
	pendingEventOrigin string
	pendingEventOrigintype string
	pendingEventInvokeid string
	// W3C SCXML 5.10: Session ID
	SessionID string
	// W3C SCXML B.1: Script engine handle (per-instance, Path B+ Q1=(d) Go=interface ref)
	ScriptEngine sce.IScriptEngine
	scriptEngineInitialized bool
	// W3C SCXML C.2.3: inbound BasicHTTP endpoint serving this machine,
	// declared by the deployment before Initialize(). The address belongs to
	// whoever runs the listener, so the machine takes it from here rather than
	// guessing one. Empty means no such endpoint is deployed, and no BasicHTTP
	// entry is published in _ioprocessors.
	BasicHTTPAccessURI string
	pendingInvokes []sce.PendingInvoke[EmptyFinalizeUpdatesTheLocationState]
	activeInvokes  map[string]*sce.ChildSession
	childInvEmpty sce.ChildEngine
	pendingDoneInvokeInvEmpty bool
	childInvAbsent sce.ChildEngine
	pendingDoneInvokeInvAbsent bool
	childInvUnmatched sce.ChildEngine
	pendingDoneInvokeInvUnmatched bool
	// W3C SCXML 6.4: Parent communication
	ParentExternalQueue chan sce.ParentEvent
	InvokeID           string
	ChildSessionID     string
	// §scxml-6.4.3: invoke param VALUES staged by the parent, applied after
	// this machine's datamodel init so they override its `<data>` defaults.
	pendingParams      map[string]interface{}
}

// NewEmptyFinalizeUpdatesTheLocationPolicy creates a new policy with default values.
func NewEmptyFinalizeUpdatesTheLocationPolicy() EmptyFinalizeUpdatesTheLocationPolicy {
	return EmptyFinalizeUpdatesTheLocationPolicy{
		lastTransitionSourceState: EmptyFinalizeUpdatesTheLocationStateEmptyPhase,
		pendingInvokes: make([]sce.PendingInvoke[EmptyFinalizeUpdatesTheLocationState], 0),
		activeInvokes:  make(map[string]*sce.ChildSession),
	}
}

// Tally reports what the `tally` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `tally` was assigned a value of another type, or the engine refused.
func (p *EmptyFinalizeUpdatesTheLocationPolicy) Tally() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "tally")
}

// Guard reports what the `guard` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `guard` was assigned a value of another type, or the engine refused.
func (p *EmptyFinalizeUpdatesTheLocationPolicy) Guard() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "guard")
}

// Keeper reports what the `keeper` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `keeper` was assigned a value of another type, or the engine refused.
func (p *EmptyFinalizeUpdatesTheLocationPolicy) Keeper() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "keeper")
}




// ensureScriptEngine lazily initializes the script engine session.
//
// Engine DI Parity RFC: panics if ScriptEngine was not set on the policy
// before Initialize() — mirrors Kotlin's mandatory constructor parameter
// contract. Silent no-op would otherwise turn missed injection into a
// hidden runtime regression (every <assign>/<log>/guard becomes a no-op
// and the SM walks to its default-state final without diagnostic).
func (p *EmptyFinalizeUpdatesTheLocationPolicy) ensureScriptEngine() {
	if p.scriptEngineInitialized {
		return
	}
	engine := p.ScriptEngine
	if engine == nil {
		panic("EmptyFinalizeUpdatesTheLocationPolicy: ScriptEngine field must be set before Initialize()")
	}
	sessionID := p.SessionID
	if sessionID == "" {
		sessionID = sce.GenerateSessionID()
		p.SessionID = sessionID
	}
	if !engine.HasSession(sessionID) {
		_ = engine.CreateSession(sessionID)
	}
	// W3C SCXML C.1.1 / C.2.3: the _ioprocessors entries come from the same
	// helper every other backend uses, so a machine reads the same entry names
	// and the same addresses whichever one runs it.
	_ = engine.SetupSystemVariables(sessionID, "empty_finalize_updates_the_location",
		sce.BuildIoProcessors(sessionID, p.BasicHTTPAccessURI))
	p.scriptEngineInitialized = true
}

// InitializeDataModel sets up datamodel via script engine (W3C SCXML 5.3).
func (p *EmptyFinalizeUpdatesTheLocationPolicy) InitializeDataModel(eng *sce.Engine[EmptyFinalizeUpdatesTheLocationState, EmptyFinalizeUpdatesTheLocationEvent]) {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	sessionID := p.SessionID

	// W3C SCXML 5.3: Initialize _sessionid
	_ = engine.SetVariable(sessionID, "_sessionid", sessionID)

	// W3C SCXML 5.3: Initialize _name
	_ = engine.SetVariable(sessionID, "_name", "empty_finalize_updates_the_location")

	// W3C SCXML 5.2.2: Initialize global datamodel variables
	// W3C SCXML 5.2/5.3: Initialize tally from expr="1"
	{
		result, err := engine.EvaluateExpression(sessionID, `1`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "tally", result)
		} else {
			eng.Raise(sce.NewPlatformError(EmptyFinalizeUpdatesTheLocationEventErrorExecution, "<data id='tally'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "tally", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize guard from expr="1"
	{
		result, err := engine.EvaluateExpression(sessionID, `1`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "guard", result)
		} else {
			eng.Raise(sce.NewPlatformError(EmptyFinalizeUpdatesTheLocationEventErrorExecution, "<data id='guard'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "guard", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize keeper from expr="3"
	{
		result, err := engine.EvaluateExpression(sessionID, `3`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "keeper", result)
		} else {
			eng.Raise(sce.NewPlatformError(EmptyFinalizeUpdatesTheLocationEventErrorExecution, "<data id='keeper'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "keeper", nil)
		}
	}



	// §scxml-6.4.3: apply the invoke params the parent staged — overriding the
	// `<data>` default this block just seeded, and only for a name the child
	// declares ("if the names do not match, the Processor MUST NOT add the
	// value ... to the invoked session's data model"). `HasVariable` is that
	// filter, and it is this channel's only copy of the rule.
	if len(p.pendingParams) > 0 {
		for paramName, paramValue := range p.pendingParams {
			if engine.HasVariable(sessionID, paramName) {
				_ = engine.SetVariable(sessionID, paramName, paramValue)
			}
		}
		p.pendingParams = nil
	}
}

// evaluateGuard evaluates a guard condition via script engine.
// W3C SCXML 4.3.2: If a condition expression cannot be evaluated, the processor
// MUST treat it as false and MUST raise error.execution.
func (p *EmptyFinalizeUpdatesTheLocationPolicy) evaluateGuard(guard string, eng *sce.Engine[EmptyFinalizeUpdatesTheLocationState, EmptyFinalizeUpdatesTheLocationEvent]) bool {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	result, err := engine.EvaluateExpression(p.SessionID, guard)
	if err != nil {
		eng.Raise(sce.NewPlatformError(EmptyFinalizeUpdatesTheLocationEventErrorExecution, "a <transition> cond failed to evaluate"))
		return false
	}
	return sce.ScriptToBool(result)
}

// executeScript executes a script block via script engine.
func (p *EmptyFinalizeUpdatesTheLocationPolicy) executeScript(script string) error {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	return engine.ExecuteScript(p.SessionID, script)
}

// setCurrentEvent binds _event system variable (W3C SCXML 5.10) and answers
// which rung of §scxml-B-2-8-1 the payload got, because the binding is the
// only place that knows.
//
// This used to be `_ = engine.SetCurrentEvent(...)`: the ladder decided
// between a DOM, a value and a space-normalized string, and the decision was
// dropped one line later. A payload that announced structure and would not
// parse therefore reached the document as raw characters, every
// _event.data.<field> read empty, and nothing anywhere could say so — see
// sce.PayloadReading.
func (p *EmptyFinalizeUpdatesTheLocationPolicy) setCurrentEvent(name string) sce.PayloadReading {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	data := p.pendingEventData
	eventType := p.pendingEventType
	sendID := p.pendingEventSendid
	// W3C SCXML C.1: `_event.origin` is the sender's published
	// `_ioprocessors` location, not its bare session id — and this is the one
	// place that publishes `_event` to the document, so this is where the id
	// becomes a location. The engine keeps the bare id in
	// `EventMetadata.Origin` because its session-keyed lookups (<finalize>
	// dispatch, cancelled-invoke filtering) match on it; converting at the
	// raise would make one value serve two consumers that need different
	// spellings. The conversion itself lives in `sce.PublishedOrigin`, the
	// port of the `IOProcessorHelper::publishedOrigin` the C++ engines share:
	// a second spelling of the rule is how the backends would stop agreeing.
	origin := sce.PublishedOrigin(p.pendingEventOrigin)
	originType := p.pendingEventOrigintype
	invokeID := p.pendingEventInvokeid
	reading, err := engine.SetCurrentEvent(p.SessionID, sce.SetCurrentEventArgs{
		Name:       name,
		Data:       data,
		Type:       eventType,
		SendID:     sendID,
		Origin:     origin,
		OriginType: originType,
		InvokeID:   invokeID,
	})
	if err != nil {
		// An engine that could not bind _event at all has a larger problem
		// than an unreadable payload, and reporting PayloadUndecodable for it
		// would point a host at the wrong thing.
		return sce.PayloadAbsent
	}
	return reading
}

// assignVariable assigns a value to a datamodel variable via script engine.
func (p *EmptyFinalizeUpdatesTheLocationPolicy) assignVariable(location, expr string) error {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	script := location + " = " + expr
	return engine.ExecuteScript(p.SessionID, script)
}

// getVariable reads a datamodel variable from the script engine.
func (p *EmptyFinalizeUpdatesTheLocationPolicy) getVariable(name string) interface{} {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	result, err := engine.GetVariable(p.SessionID, name)
	if err != nil {
		return nil
	}
	return result
}

// SetParamValueInScriptEngine stores an invoke param's VALUE for deferred
// application after datamodel init (§scxml-6.4.3).
//
// Params are applied at the end of InitializeDataModel, overriding the
// `<data>` default only for names the child declares. The value crosses as a
// value: this took the parent's value rendered as Lua source and
// re-evaluated it here until 2026-08-21, which survived every value a test
// had tried and lost the ones Lua cannot spell — `ToScriptLiteral` renders a
// non-finite number as `+Inf`, which is not a Lua expression, so an
// `<invoke>` `<param expr="1/0"/>` arrived as nothing at all.
func (p *EmptyFinalizeUpdatesTheLocationPolicy) SetParamValueInScriptEngine(name string, value interface{}) {
	if p.pendingParams == nil {
		p.pendingParams = make(map[string]interface{})
	}
	p.pendingParams[name] = value
}


// ExecutePendingInvokes starts deferred child state machines (W3C SCXML 6.4).
// Port of Rust do_execute_pending_invokes().
func (p *EmptyFinalizeUpdatesTheLocationPolicy) ExecutePendingInvokes(engine *sce.Engine[EmptyFinalizeUpdatesTheLocationState, EmptyFinalizeUpdatesTheLocationEvent]) {
	if len(p.pendingInvokes) == 0 {
		return
	}

	// W3C SCXML 6.4: Copy-and-clear to prevent iterator invalidation
	invokesToExecute := make([]sce.PendingInvoke[EmptyFinalizeUpdatesTheLocationState], len(p.pendingInvokes))
	copy(invokesToExecute, p.pendingInvokes)
	p.pendingInvokes = p.pendingInvokes[:0]

	for _, pending := range invokesToExecute {
		if strings.Contains(pending.InvokeID, ".inv_absent") {
			// W3C SCXML 6.4.1: Validate namelist variables in PARENT before creating child
			{
				p.ensureScriptEngine()
				se := p.ScriptEngine
				namelistValid := true
				if !se.HasVariable(p.SessionID, "guard") {
					engine.Raise(sce.NewPlatformError(EmptyFinalizeUpdatesTheLocationEventErrorExecution, "<invoke> namelist names 'guard', which the parent does not declare"))
					namelistValid = false
				}
				if !namelistValid {
					continue
				}
			}

			// W3C SCXML 6.5: Generate child session ID for finalize origin matching
			childSessionID := fmt.Sprintf("%s.%s", p.SessionID, pending.InvokeID)

			// W3C SCXML 6.4: Create child state machine
			childPolicy := NewEmptyFinalizeUpdatesTheLocationSceSynthInvokeInvAbsentPolicy()
			childPolicy.ScriptEngine = p.ScriptEngine
			parentQueue := make(chan sce.ParentEvent, 100)
			childPolicy.ParentExternalQueue = parentQueue
			childPolicy.InvokeID = pending.InvokeID
			childPolicy.ChildSessionID = childSessionID
			// W3C SCXML C.1: the child adopts the id its parent recorded for
			// it. The parent mints `parentSession.invokeId` and keys
			// `activeInvokes` on it, so an event from this child reaches the
			// parent carrying that id as its origin; if the child also minted
			// an id of its own, the location it publishes in `_ioprocessors`
			// and the origin the parent sees would be two names for one
			// session that can never be compared — which is exactly what C.1
			// requires of them.
			childPolicy.SessionID = childSessionID

			// W3C SCXML 6.4.1: Pass validated namelist values to child datamodel
			{
				se := p.ScriptEngine
				if val, err := se.EvaluateExpression(p.SessionID, "guard"); err == nil {
					// §scxml-6.4.1: "the value stored at the location is the
					// value" — so the value crosses, not a rendering of it.
					childPolicy.SetParamValueInScriptEngine("guard", val)
				}
			}


			// W3C SCXML 6.4.1: Track active invoke session BEFORE initialize
			p.activeInvokes["inv_absent"] = &sce.ChildSession{
				SessionID:       childSessionID,
				InvokeID:        "inv_absent",
				ParentSessionID: p.SessionID,
				Autoforward:     false,
				FinalizeScript:  "",
			}

			childEngine := sce.NewEngine[EmptyFinalizeUpdatesTheLocationSceSynthInvokeInvAbsentState, EmptyFinalizeUpdatesTheLocationSceSynthInvokeInvAbsentEvent](&childPolicy)
			// §scxml-6.4: the child's delayed sends are measured against the
			// same clock as ours. A child reading its own would start its
			// origin at construction time, so <send delay="100ms"> on either
			// side of the boundary would mean two different absolute instants —
			// and on a host-owned clock the child would not move at all,
			// because the host advances the engine it holds, not the ones that
			// engine invoked.
			childEngine.SetClock(engine.Clock())
			childEngine.SetCompletionCallback(func() {})
			childEngine.Initialize()

			// W3C SCXML 6.4: Store child and create wrapper
			wrapper := &childEngineWrapperInvAbsent{engine: childEngine, policy: &childPolicy}
			p.childInvAbsent = wrapper

			// W3C SCXML 6.4: Drain child-to-parent events raised during initialize
			sce.DrainAndRaiseChildEvents(wrapper, p.activeInvokes, "inv_absent", engine)

			// W3C SCXML 6.4: Check if child completed during initialize
			if wrapper.IsInFinalState() {
				p.pendingDoneInvokeInvAbsent = true
				// W3C SCXML 5.5 + 6.3.1: Lift the child's stashed donedata onto
				// done.invoke.<id>._event.data. Mirrors the C++ AOT contract
				// and the Rust raise_done_invoke(donedata) 3-arg signature.
				sce.RaiseDoneInvoke("inv_absent", wrapper.DonedataAtFinal(), engine)
			}
			continue
		}
		if strings.Contains(pending.InvokeID, ".inv_empty") {
			// W3C SCXML 6.4.1: Validate namelist variables in PARENT before creating child
			{
				p.ensureScriptEngine()
				se := p.ScriptEngine
				namelistValid := true
				if !se.HasVariable(p.SessionID, "tally") {
					engine.Raise(sce.NewPlatformError(EmptyFinalizeUpdatesTheLocationEventErrorExecution, "<invoke> namelist names 'tally', which the parent does not declare"))
					namelistValid = false
				}
				if !namelistValid {
					continue
				}
			}

			// W3C SCXML 6.5: Generate child session ID for finalize origin matching
			childSessionID := fmt.Sprintf("%s.%s", p.SessionID, pending.InvokeID)

			// W3C SCXML 6.4: Create child state machine
			childPolicy := NewEmptyFinalizeUpdatesTheLocationSceSynthInvokeInvEmptyPolicy()
			childPolicy.ScriptEngine = p.ScriptEngine
			parentQueue := make(chan sce.ParentEvent, 100)
			childPolicy.ParentExternalQueue = parentQueue
			childPolicy.InvokeID = pending.InvokeID
			childPolicy.ChildSessionID = childSessionID
			// W3C SCXML C.1: the child adopts the id its parent recorded for
			// it. The parent mints `parentSession.invokeId` and keys
			// `activeInvokes` on it, so an event from this child reaches the
			// parent carrying that id as its origin; if the child also minted
			// an id of its own, the location it publishes in `_ioprocessors`
			// and the origin the parent sees would be two names for one
			// session that can never be compared — which is exactly what C.1
			// requires of them.
			childPolicy.SessionID = childSessionID

			// W3C SCXML 6.4.1: Pass validated namelist values to child datamodel
			{
				se := p.ScriptEngine
				if val, err := se.EvaluateExpression(p.SessionID, "tally"); err == nil {
					// §scxml-6.4.1: "the value stored at the location is the
					// value" — so the value crosses, not a rendering of it.
					childPolicy.SetParamValueInScriptEngine("tally", val)
				}
			}


			// W3C SCXML 6.4.1: Track active invoke session BEFORE initialize
			p.activeInvokes["inv_empty"] = &sce.ChildSession{
				SessionID:       childSessionID,
				InvokeID:        "inv_empty",
				ParentSessionID: p.SessionID,
				Autoforward:     false,
				FinalizeScript:  "if (_scxml_truthy(_event.data) and (_event.data.tally ~= nil)) then\n  tally = _event.data.tally\nend",
			}

			childEngine := sce.NewEngine[EmptyFinalizeUpdatesTheLocationSceSynthInvokeInvEmptyState, EmptyFinalizeUpdatesTheLocationSceSynthInvokeInvEmptyEvent](&childPolicy)
			// §scxml-6.4: the child's delayed sends are measured against the
			// same clock as ours. A child reading its own would start its
			// origin at construction time, so <send delay="100ms"> on either
			// side of the boundary would mean two different absolute instants —
			// and on a host-owned clock the child would not move at all,
			// because the host advances the engine it holds, not the ones that
			// engine invoked.
			childEngine.SetClock(engine.Clock())
			childEngine.SetCompletionCallback(func() {})
			childEngine.Initialize()

			// W3C SCXML 6.4: Store child and create wrapper
			wrapper := &childEngineWrapperInvEmpty{engine: childEngine, policy: &childPolicy}
			p.childInvEmpty = wrapper

			// W3C SCXML 6.4: Drain child-to-parent events raised during initialize
			sce.DrainAndRaiseChildEvents(wrapper, p.activeInvokes, "inv_empty", engine)

			// W3C SCXML 6.4: Check if child completed during initialize
			if wrapper.IsInFinalState() {
				p.pendingDoneInvokeInvEmpty = true
				// W3C SCXML 5.5 + 6.3.1: Lift the child's stashed donedata onto
				// done.invoke.<id>._event.data. Mirrors the C++ AOT contract
				// and the Rust raise_done_invoke(donedata) 3-arg signature.
				sce.RaiseDoneInvoke("inv_empty", wrapper.DonedataAtFinal(), engine)
			}
			continue
		}
		if strings.Contains(pending.InvokeID, ".inv_unmatched") {
			// W3C SCXML 6.4.1: Validate namelist variables in PARENT before creating child
			{
				p.ensureScriptEngine()
				se := p.ScriptEngine
				namelistValid := true
				if !se.HasVariable(p.SessionID, "keeper") {
					engine.Raise(sce.NewPlatformError(EmptyFinalizeUpdatesTheLocationEventErrorExecution, "<invoke> namelist names 'keeper', which the parent does not declare"))
					namelistValid = false
				}
				if !namelistValid {
					continue
				}
			}

			// W3C SCXML 6.5: Generate child session ID for finalize origin matching
			childSessionID := fmt.Sprintf("%s.%s", p.SessionID, pending.InvokeID)

			// W3C SCXML 6.4: Create child state machine
			childPolicy := NewEmptyFinalizeUpdatesTheLocationSceSynthInvokeInvUnmatchedPolicy()
			childPolicy.ScriptEngine = p.ScriptEngine
			parentQueue := make(chan sce.ParentEvent, 100)
			childPolicy.ParentExternalQueue = parentQueue
			childPolicy.InvokeID = pending.InvokeID
			childPolicy.ChildSessionID = childSessionID
			// W3C SCXML C.1: the child adopts the id its parent recorded for
			// it. The parent mints `parentSession.invokeId` and keys
			// `activeInvokes` on it, so an event from this child reaches the
			// parent carrying that id as its origin; if the child also minted
			// an id of its own, the location it publishes in `_ioprocessors`
			// and the origin the parent sees would be two names for one
			// session that can never be compared — which is exactly what C.1
			// requires of them.
			childPolicy.SessionID = childSessionID

			// W3C SCXML 6.4.1: Pass validated namelist values to child datamodel
			{
				se := p.ScriptEngine
				if val, err := se.EvaluateExpression(p.SessionID, "keeper"); err == nil {
					// §scxml-6.4.1: "the value stored at the location is the
					// value" — so the value crosses, not a rendering of it.
					childPolicy.SetParamValueInScriptEngine("keeper", val)
				}
			}


			// W3C SCXML 6.4.1: Track active invoke session BEFORE initialize
			p.activeInvokes["inv_unmatched"] = &sce.ChildSession{
				SessionID:       childSessionID,
				InvokeID:        "inv_unmatched",
				ParentSessionID: p.SessionID,
				Autoforward:     false,
				FinalizeScript:  "if (_scxml_truthy(_event.data) and (_event.data.keeper ~= nil)) then\n  keeper = _event.data.keeper\nend",
			}

			childEngine := sce.NewEngine[EmptyFinalizeUpdatesTheLocationSceSynthInvokeInvUnmatchedState, EmptyFinalizeUpdatesTheLocationSceSynthInvokeInvUnmatchedEvent](&childPolicy)
			// §scxml-6.4: the child's delayed sends are measured against the
			// same clock as ours. A child reading its own would start its
			// origin at construction time, so <send delay="100ms"> on either
			// side of the boundary would mean two different absolute instants —
			// and on a host-owned clock the child would not move at all,
			// because the host advances the engine it holds, not the ones that
			// engine invoked.
			childEngine.SetClock(engine.Clock())
			childEngine.SetCompletionCallback(func() {})
			childEngine.Initialize()

			// W3C SCXML 6.4: Store child and create wrapper
			wrapper := &childEngineWrapperInvUnmatched{engine: childEngine, policy: &childPolicy}
			p.childInvUnmatched = wrapper

			// W3C SCXML 6.4: Drain child-to-parent events raised during initialize
			sce.DrainAndRaiseChildEvents(wrapper, p.activeInvokes, "inv_unmatched", engine)

			// W3C SCXML 6.4: Check if child completed during initialize
			if wrapper.IsInFinalState() {
				p.pendingDoneInvokeInvUnmatched = true
				// W3C SCXML 5.5 + 6.3.1: Lift the child's stashed donedata onto
				// done.invoke.<id>._event.data. Mirrors the C++ AOT contract
				// and the Rust raise_done_invoke(donedata) 3-arg signature.
				sce.RaiseDoneInvoke("inv_unmatched", wrapper.DonedataAtFinal(), engine)
			}
			continue
		}
	}
}

// TickChildren ticks child state machines (W3C SCXML 6.4).
// Port of Rust do_tick_children().
func (p *EmptyFinalizeUpdatesTheLocationPolicy) TickChildren(engine *sce.Engine[EmptyFinalizeUpdatesTheLocationState, EmptyFinalizeUpdatesTheLocationEvent]) {
	if !p.pendingDoneInvokeInvEmpty && p.childInvEmpty != nil {
		child := p.childInvEmpty
		sce.DrainAndRaiseChildEvents(child, p.activeInvokes, "inv_empty", engine)
		child.Tick()
		sce.DrainAndRaiseChildEvents(child, p.activeInvokes, "inv_empty", engine)
		if child.IsInFinalState() && !p.pendingDoneInvokeInvEmpty {
			p.pendingDoneInvokeInvEmpty = true
			// W3C SCXML 5.5 + 6.3.1: Lift the child's stashed donedata onto
			// done.invoke.<id>._event.data.
			sce.RaiseDoneInvoke("inv_empty", child.DonedataAtFinal(), engine)
		}
	}
	if !p.pendingDoneInvokeInvAbsent && p.childInvAbsent != nil {
		child := p.childInvAbsent
		sce.DrainAndRaiseChildEvents(child, p.activeInvokes, "inv_absent", engine)
		child.Tick()
		sce.DrainAndRaiseChildEvents(child, p.activeInvokes, "inv_absent", engine)
		if child.IsInFinalState() && !p.pendingDoneInvokeInvAbsent {
			p.pendingDoneInvokeInvAbsent = true
			// W3C SCXML 5.5 + 6.3.1: Lift the child's stashed donedata onto
			// done.invoke.<id>._event.data.
			sce.RaiseDoneInvoke("inv_absent", child.DonedataAtFinal(), engine)
		}
	}
	if !p.pendingDoneInvokeInvUnmatched && p.childInvUnmatched != nil {
		child := p.childInvUnmatched
		sce.DrainAndRaiseChildEvents(child, p.activeInvokes, "inv_unmatched", engine)
		child.Tick()
		sce.DrainAndRaiseChildEvents(child, p.activeInvokes, "inv_unmatched", engine)
		if child.IsInFinalState() && !p.pendingDoneInvokeInvUnmatched {
			p.pendingDoneInvokeInvUnmatched = true
			// W3C SCXML 5.5 + 6.3.1: Lift the child's stashed donedata onto
			// done.invoke.<id>._event.data.
			sce.RaiseDoneInvoke("inv_unmatched", child.DonedataAtFinal(), engine)
		}
	}
}


// DeliverToChildSession delivers an event addressed to a child's published
// location (W3C SCXML C.1).
//
// The parent mints `parentSession.invokeId` for each child and that id is what
// the child's `_ioprocessors` entry names, so a <send> whose target decodes to
// one of them is addressed to that child rather than to this machine. A false
// return means the address names no live child of ours and the event takes the
// normal external path — the routing half of C.1 is what makes the published
// location a usable target rather than a string that merely compares equal.
func (p *EmptyFinalizeUpdatesTheLocationPolicy) DeliverToChildSession(childSessionID, eventName, eventData string) bool {
	if childSessionID == "" {
		return false
	}
	if cs, ok := p.activeInvokes["inv_empty"]; ok && cs.SessionID == childSessionID {
		if p.childInvEmpty != nil {
			p.childInvEmpty.RaiseExternalByName(eventName, eventData)
			return true
		}
	}
	if cs, ok := p.activeInvokes["inv_absent"]; ok && cs.SessionID == childSessionID {
		if p.childInvAbsent != nil {
			p.childInvAbsent.RaiseExternalByName(eventName, eventData)
			return true
		}
	}
	if cs, ok := p.activeInvokes["inv_unmatched"]; ok && cs.SessionID == childSessionID {
		if p.childInvUnmatched != nil {
			p.childInvUnmatched.RaiseExternalByName(eventName, eventData)
			return true
		}
	}
	return false
}

// ExecuteFinalizeForChildEvent executes finalize for events from invoked children (W3C SCXML 6.5).
func (p *EmptyFinalizeUpdatesTheLocationPolicy) ExecuteFinalizeForChildEvent(eventWithMeta *sce.EventWithMetadata[EmptyFinalizeUpdatesTheLocationEvent], engine *sce.Engine[EmptyFinalizeUpdatesTheLocationState, EmptyFinalizeUpdatesTheLocationEvent]) {
	if eventWithMeta.Metadata.Origin == "" {
		return
	}

	origin := eventWithMeta.Metadata.Origin
	var finalizeScript string
	for _, childSession := range p.activeInvokes {
		if childSession.SessionID == origin && childSession.FinalizeScript != "" {
			finalizeScript = childSession.FinalizeScript
			break
		}
	}

	if finalizeScript != "" {
		p.ensureScriptEngine()
		se := p.ScriptEngine
		if se != nil {
			eventName := p.GetEventName(eventWithMeta.Event)
			// W3C SCXML 6.4: `<finalize>` binds the child's event so the
			// handler can read it, and the rung that binding chose is not
			// this site's to report. The parent's own dequeue already
			// counted the delivery — see `NotePayloadReading` — and counting
			// it again here would double every payload that reaches a
			// document with an `<invoke>`. Discarded deliberately, and named
			// rather than left to a bare `_`.
			_, _ = se.SetCurrentEvent(
				p.SessionID,
				sce.SetCurrentEventArgs{
					Name:       eventName,
					Data:       eventWithMeta.Metadata.Data,
					Type:       "external",
					SendID:     eventWithMeta.Metadata.SendID,
					Origin:     eventWithMeta.Metadata.Origin,
					OriginType: eventWithMeta.Metadata.OriginType,
					InvokeID:   eventWithMeta.Metadata.InvokeID,
				},
			)
			_ = se.ExecuteScript(p.SessionID, finalizeScript)
		}
	}
}

// ── Child engine wrappers (implement sce.ChildEngine interface) ──

type childEngineWrapperInvEmpty struct {
	engine *sce.Engine[EmptyFinalizeUpdatesTheLocationSceSynthInvokeInvEmptyState, EmptyFinalizeUpdatesTheLocationSceSynthInvokeInvEmptyEvent]
	policy *EmptyFinalizeUpdatesTheLocationSceSynthInvokeInvEmptyPolicy
}
func (w *childEngineWrapperInvEmpty) Initialize() { w.engine.Initialize() }
func (w *childEngineWrapperInvEmpty) Tick() { w.engine.Tick() }
func (w *childEngineWrapperInvEmpty) IsInFinalState() bool { return w.engine.IsInFinalState() }
func (w *childEngineWrapperInvEmpty) RaiseExternalByName(name, data string) { w.engine.RaiseExternalByName(name, data) }
func (w *childEngineWrapperInvEmpty) RaiseExternalByNameWithMeta(name string, metadata sce.EventMetadata) { w.engine.RaiseExternalByNameWithMeta(name, metadata) }
func (w *childEngineWrapperInvEmpty) SetCompletionCallback(cb func()) { w.engine.SetCompletionCallback(cb) }
func (w *childEngineWrapperInvEmpty) GetParentEventQueue() chan sce.ParentEvent { return w.policy.ParentExternalQueue }
func (w *childEngineWrapperInvEmpty) DonedataAtFinal() string { return w.engine.DonedataAtFinal() }
type childEngineWrapperInvAbsent struct {
	engine *sce.Engine[EmptyFinalizeUpdatesTheLocationSceSynthInvokeInvAbsentState, EmptyFinalizeUpdatesTheLocationSceSynthInvokeInvAbsentEvent]
	policy *EmptyFinalizeUpdatesTheLocationSceSynthInvokeInvAbsentPolicy
}
func (w *childEngineWrapperInvAbsent) Initialize() { w.engine.Initialize() }
func (w *childEngineWrapperInvAbsent) Tick() { w.engine.Tick() }
func (w *childEngineWrapperInvAbsent) IsInFinalState() bool { return w.engine.IsInFinalState() }
func (w *childEngineWrapperInvAbsent) RaiseExternalByName(name, data string) { w.engine.RaiseExternalByName(name, data) }
func (w *childEngineWrapperInvAbsent) RaiseExternalByNameWithMeta(name string, metadata sce.EventMetadata) { w.engine.RaiseExternalByNameWithMeta(name, metadata) }
func (w *childEngineWrapperInvAbsent) SetCompletionCallback(cb func()) { w.engine.SetCompletionCallback(cb) }
func (w *childEngineWrapperInvAbsent) GetParentEventQueue() chan sce.ParentEvent { return w.policy.ParentExternalQueue }
func (w *childEngineWrapperInvAbsent) DonedataAtFinal() string { return w.engine.DonedataAtFinal() }
type childEngineWrapperInvUnmatched struct {
	engine *sce.Engine[EmptyFinalizeUpdatesTheLocationSceSynthInvokeInvUnmatchedState, EmptyFinalizeUpdatesTheLocationSceSynthInvokeInvUnmatchedEvent]
	policy *EmptyFinalizeUpdatesTheLocationSceSynthInvokeInvUnmatchedPolicy
}
func (w *childEngineWrapperInvUnmatched) Initialize() { w.engine.Initialize() }
func (w *childEngineWrapperInvUnmatched) Tick() { w.engine.Tick() }
func (w *childEngineWrapperInvUnmatched) IsInFinalState() bool { return w.engine.IsInFinalState() }
func (w *childEngineWrapperInvUnmatched) RaiseExternalByName(name, data string) { w.engine.RaiseExternalByName(name, data) }
func (w *childEngineWrapperInvUnmatched) RaiseExternalByNameWithMeta(name string, metadata sce.EventMetadata) { w.engine.RaiseExternalByNameWithMeta(name, metadata) }
func (w *childEngineWrapperInvUnmatched) SetCompletionCallback(cb func()) { w.engine.SetCompletionCallback(cb) }
func (w *childEngineWrapperInvUnmatched) GetParentEventQueue() chan sce.ParentEvent { return w.policy.ParentExternalQueue }
func (w *childEngineWrapperInvUnmatched) DonedataAtFinal() string { return w.engine.DonedataAtFinal() }


// ======================================================================
// StatePolicy interface implementation
// ======================================================================

// InitialState returns the initial state (W3C SCXML 3.3).
func (p *EmptyFinalizeUpdatesTheLocationPolicy) InitialState() EmptyFinalizeUpdatesTheLocationState {
	return EmptyFinalizeUpdatesTheLocationStateEmptyPhase
}

// IsFinalState returns true if state is a <final> state (W3C SCXML 3.7).
func (p *EmptyFinalizeUpdatesTheLocationPolicy) IsFinalState(state EmptyFinalizeUpdatesTheLocationState) bool {
	switch state {
	case EmptyFinalizeUpdatesTheLocationStateFailAbsentChildSilent:
		return true
	case EmptyFinalizeUpdatesTheLocationStateFailEmptyChildSilent:
		return true
	case EmptyFinalizeUpdatesTheLocationStateFailNotUpdated:
		return true
	case EmptyFinalizeUpdatesTheLocationStateFailUnmatchedChildSilent:
		return true
	case EmptyFinalizeUpdatesTheLocationStateFailUnmatchedNameWrote:
		return true
	case EmptyFinalizeUpdatesTheLocationStateFailUpdatedWithoutFinalize:
		return true
	case EmptyFinalizeUpdatesTheLocationStatePass:
		return true
	}
	return false
}

// GetParent returns the parent of state, or (zero, false) if root (W3C SCXML 3.3).
func (p *EmptyFinalizeUpdatesTheLocationPolicy) GetParent(state EmptyFinalizeUpdatesTheLocationState) (EmptyFinalizeUpdatesTheLocationState, bool) {
	switch state {
	}
	return 0, false
}

// IsCompoundState returns true if state has children (W3C SCXML 3.3).
func (p *EmptyFinalizeUpdatesTheLocationPolicy) IsCompoundState(state EmptyFinalizeUpdatesTheLocationState) bool {
	switch state {
	}
	return false
}

func (p *EmptyFinalizeUpdatesTheLocationPolicy) IsParallelState(_ EmptyFinalizeUpdatesTheLocationState) bool { return false }
func (p *EmptyFinalizeUpdatesTheLocationPolicy) GetParallelRegions(_ EmptyFinalizeUpdatesTheLocationState) []EmptyFinalizeUpdatesTheLocationState { return nil }

// IsDescendantOf returns true if desc is a descendant of anc (W3C SCXML 3.12).
func (p *EmptyFinalizeUpdatesTheLocationPolicy) IsDescendantOf(desc, anc EmptyFinalizeUpdatesTheLocationState) bool {
	current := desc
	for {
		parent, ok := p.GetParent(current)
		if !ok {
			return false
		}
		if parent == anc {
			return true
		}
		current = parent
	}
}

// GetDocumentOrder returns the document order index (W3C SCXML Appendix D).
func (p *EmptyFinalizeUpdatesTheLocationPolicy) GetDocumentOrder(state EmptyFinalizeUpdatesTheLocationState) int {
	switch state {
	case EmptyFinalizeUpdatesTheLocationStateAbsentPhase:
		return 1
	case EmptyFinalizeUpdatesTheLocationStateEmptyPhase:
		return 0
	case EmptyFinalizeUpdatesTheLocationStateFailAbsentChildSilent:
		return 8
	case EmptyFinalizeUpdatesTheLocationStateFailEmptyChildSilent:
		return 7
	case EmptyFinalizeUpdatesTheLocationStateFailNotUpdated:
		return 4
	case EmptyFinalizeUpdatesTheLocationStateFailUnmatchedChildSilent:
		return 9
	case EmptyFinalizeUpdatesTheLocationStateFailUnmatchedNameWrote:
		return 6
	case EmptyFinalizeUpdatesTheLocationStateFailUpdatedWithoutFinalize:
		return 5
	case EmptyFinalizeUpdatesTheLocationStatePass:
		return 3
	case EmptyFinalizeUpdatesTheLocationStateUnmatchedPhase:
		return 2
	}
	return -1
}

// GetEventName returns the string name of an event (W3C SCXML 3.12).
func (p *EmptyFinalizeUpdatesTheLocationPolicy) GetEventName(event EmptyFinalizeUpdatesTheLocationEvent) string {
	return event.String()
}

// GetEventFromName looks up an event by name (W3C SCXML 3.12).
func (p *EmptyFinalizeUpdatesTheLocationPolicy) GetEventFromName(name string) (EmptyFinalizeUpdatesTheLocationEvent, bool) {
	switch name {
	case "cancel.invoke":
		return EmptyFinalizeUpdatesTheLocationEventCancelInvoke, true
	case "done.invoke":
		return EmptyFinalizeUpdatesTheLocationEventDoneInvoke, true
	case "error.execution":
		return EmptyFinalizeUpdatesTheLocationEventErrorExecution, true
	case "fromAbsentChild":
		return EmptyFinalizeUpdatesTheLocationEventFromAbsentChild, true
	case "fromEmptyChild":
		return EmptyFinalizeUpdatesTheLocationEventFromEmptyChild, true
	case "fromUnmatchedChild":
		return EmptyFinalizeUpdatesTheLocationEventFromUnmatchedChild, true
	case "timeoutAbsent":
		return EmptyFinalizeUpdatesTheLocationEventTimeoutAbsent, true
	case "timeoutEmpty":
		return EmptyFinalizeUpdatesTheLocationEventTimeoutEmpty, true
	case "timeoutUnmatched":
		return EmptyFinalizeUpdatesTheLocationEventTimeoutUnmatched, true
	}
	return EmptyFinalizeUpdatesTheLocationEventNull, false
}

// GetStateName returns the string name of a state (W3C SCXML 3.3).
func (p *EmptyFinalizeUpdatesTheLocationPolicy) GetStateName(state EmptyFinalizeUpdatesTheLocationState) string {
	return state.String()
}

// NullEvent returns the sentinel for eventless transition dispatch (W3C SCXML 3.13).
func (p *EmptyFinalizeUpdatesTheLocationPolicy) NullEvent() EmptyFinalizeUpdatesTheLocationEvent {
	return EmptyFinalizeUpdatesTheLocationEventNull
}

// GetInitialChildren returns initial children of a compound state (W3C SCXML 3.6).
func (p *EmptyFinalizeUpdatesTheLocationPolicy) GetInitialChildren(state EmptyFinalizeUpdatesTheLocationState) []EmptyFinalizeUpdatesTheLocationState {
	switch state {
	}
	return nil
}

// LastTransitionIsInternal returns the internal transition flag (W3C SCXML 3.13).
func (p *EmptyFinalizeUpdatesTheLocationPolicy) LastTransitionIsInternal() bool {
	return p.lastTransitionIsInternal
}

// SetLastTransitionIsInternal sets the internal transition flag.
func (p *EmptyFinalizeUpdatesTheLocationPolicy) SetLastTransitionIsInternal(value bool) {
	p.lastTransitionIsInternal = value
}

// LastTransitionIsTargetless returns the targetless transition flag (W3C SCXML 3.13).
func (p *EmptyFinalizeUpdatesTheLocationPolicy) LastTransitionIsTargetless() bool {
	return p.lastTransitionIsTargetless
}

// SetLastTransitionIsTargetless sets the targetless transition flag.
func (p *EmptyFinalizeUpdatesTheLocationPolicy) SetLastTransitionIsTargetless(value bool) {
	p.lastTransitionIsTargetless = value
}

// LastTransitionSourceState returns the source state of the last transition.
func (p *EmptyFinalizeUpdatesTheLocationPolicy) LastTransitionSourceState() EmptyFinalizeUpdatesTheLocationState {
	return p.lastTransitionSourceState
}

// SetLastTransitionSourceState sets the source state of the last transition.
func (p *EmptyFinalizeUpdatesTheLocationPolicy) SetLastTransitionSourceState(state EmptyFinalizeUpdatesTheLocationState) {
	p.lastTransitionSourceState = state
}


// SetNextEventIsExternal sets the external event flag (W3C SCXML 5.10.1).
func (p *EmptyFinalizeUpdatesTheLocationPolicy) SetNextEventIsExternal(value bool) {
	p.nextEventIsExternal = value
}

// HasParallelStates returns whether the SM has parallel states.
func (p *EmptyFinalizeUpdatesTheLocationPolicy) HasParallelStates() bool {
	return false
}

// NeedsScriptEngine returns whether the SM needs a script engine.
func (p *EmptyFinalizeUpdatesTheLocationPolicy) NeedsScriptEngine() bool {
	return true
}

// NeedsEventScheduler reports whether a host must drive this machine with
// Tick rather than Step alone.
//
// W3C SCXML 6.2: Step runs a macrostep and never drains the delayed-send
// scheduler nor ticks an invoked child, so a host that only ever calls it
// waits forever with nothing said. Same verdict the generate manifest
// publishes as needs_event_scheduler.
func (p *EmptyFinalizeUpdatesTheLocationPolicy) NeedsEventScheduler() bool {
	return true
}

// NeedsDataModelInit returns whether the SM needs datamodel init.
func (p *EmptyFinalizeUpdatesTheLocationPolicy) NeedsDataModelInit() bool {
	return true
}

// HasInvokeSupport returns true for invoke-capable SMs.
func (p *EmptyFinalizeUpdatesTheLocationPolicy) HasInvokeSupport() bool { return true }
// HasChildTick returns true for child-ticking SMs.
func (p *EmptyFinalizeUpdatesTheLocationPolicy) HasChildTick() bool { return true }
func (p *EmptyFinalizeUpdatesTheLocationPolicy) HasFinalize() bool { return true }
func (p *EmptyFinalizeUpdatesTheLocationPolicy) HasAutoforward() bool { return false }
func (p *EmptyFinalizeUpdatesTheLocationPolicy) HasActiveStates() bool { return false }
func (p *EmptyFinalizeUpdatesTheLocationPolicy) GetActiveStates() []EmptyFinalizeUpdatesTheLocationState { return nil }
func (p *EmptyFinalizeUpdatesTheLocationPolicy) HasExternalEventFlag() bool { return true }
// GetInitialOrHistoryChild returns the initial child considering history (W3C SCXML 3.11).
func (p *EmptyFinalizeUpdatesTheLocationPolicy) GetInitialOrHistoryChild(state EmptyFinalizeUpdatesTheLocationState) EmptyFinalizeUpdatesTheLocationState {
	children := p.GetInitialChildren(state)
	if len(children) > 0 {
		return children[0]
	}
	return state
}
// ForwardToAutoforwardChildren is a no-op (no autoforward invokes).
func (p *EmptyFinalizeUpdatesTheLocationPolicy) ForwardToAutoforwardChildren(_ string, _ sce.EventMetadata, _ *sce.Engine[EmptyFinalizeUpdatesTheLocationState, EmptyFinalizeUpdatesTheLocationEvent]) {}

// PopulateEventMetadata stores pending event metadata (W3C SCXML 5.10).
// Note: event name is set separately via setCurrentEvent(), not from metadata.
func (p *EmptyFinalizeUpdatesTheLocationPolicy) PopulateEventMetadata(meta *sce.EventMetadata) {
	p.pendingEventData = meta.Data
	p.pendingEventType = meta.EventType.String()
	p.pendingEventSendid = meta.SendID
	p.pendingEventOrigin = meta.Origin
	p.pendingEventOrigintype = meta.OriginType
	p.pendingEventInvokeid = meta.InvokeID
}

// ClearEventMetadata resets pending event metadata (W3C SCXML 5.10).
func (p *EmptyFinalizeUpdatesTheLocationPolicy) ClearEventMetadata() {
	p.pendingEventName = ""
	p.pendingEventData = ""
	p.pendingEventType = ""
	p.pendingEventSendid = ""
	p.pendingEventOrigin = ""
	p.pendingEventOrigintype = ""
	p.pendingEventInvokeid = ""
}




// ExecuteEntryActions executes onentry actions for a state (W3C SCXML 3.8).
//line empty_finalize_updates_the_location.scxml:52
func (p *EmptyFinalizeUpdatesTheLocationPolicy) ExecuteEntryActions(state EmptyFinalizeUpdatesTheLocationState, engine *sce.Engine[EmptyFinalizeUpdatesTheLocationState, EmptyFinalizeUpdatesTheLocationEvent], pathChild *EmptyFinalizeUpdatesTheLocationState) {
	// Only a `<parallel>` machine descends into defaults here, so a machine
	// without one has nothing to tell an ancestor entry from a target entry.
	_ = pathChild
	p.ensureScriptEngine()
	switch state {
	case EmptyFinalizeUpdatesTheLocationStateAbsentPhase:
		//line empty_finalize_updates_the_location.scxml:105
		// W3C SCXML 3.8: onentry block 0 (break on error stops subsequent actions)
		for actionBlock0 := 0; actionBlock0 < 1; actionBlock0++ {
			_ = actionBlock0

	// W3C SCXML 6.2: send id="__send_1"
	{
		eventDataStr := ""
		_ = eventDataStr
	// W3C SCXML 6.2: Delayed send
	{
		delayDur := sce.ParseDelay("3s")
		delayEvtName := "timeoutAbsent"
		if delayEvt, delayOk := p.GetEventFromName(delayEvtName); delayOk {
			engine.ScheduleEvent(delayEvt, delayDur, "__send_1", eventDataStr)
		}
	}
	}

		}
		// W3C SCXML 6.4: Defer invoke execution until macrostep end
		{
			generatedInvokeID := fmt.Sprintf("%s.%d.inv_absent", "absentPhase", sce.NextInvokeCounter())
			sce.DeferInvoke(&p.pendingInvokes, sce.PendingInvoke[EmptyFinalizeUpdatesTheLocationState]{
				InvokeID: generatedInvokeID,
				State:    EmptyFinalizeUpdatesTheLocationStateAbsentPhase,
			})
		}
	case EmptyFinalizeUpdatesTheLocationStateEmptyPhase:
		//line empty_finalize_updates_the_location.scxml:71
		// W3C SCXML 3.8: onentry block 0 (break on error stops subsequent actions)
		for actionBlock0 := 0; actionBlock0 < 1; actionBlock0++ {
			_ = actionBlock0

	// W3C SCXML 6.2: send id="__send_0"
	{
		eventDataStr := ""
		_ = eventDataStr
	// W3C SCXML 6.2: Delayed send
	{
		delayDur := sce.ParseDelay("3s")
		delayEvtName := "timeoutEmpty"
		if delayEvt, delayOk := p.GetEventFromName(delayEvtName); delayOk {
			engine.ScheduleEvent(delayEvt, delayDur, "__send_0", eventDataStr)
		}
	}
	}

		}
		// W3C SCXML 6.4: Defer invoke execution until macrostep end
		{
			generatedInvokeID := fmt.Sprintf("%s.%d.inv_empty", "emptyPhase", sce.NextInvokeCounter())
			sce.DeferInvoke(&p.pendingInvokes, sce.PendingInvoke[EmptyFinalizeUpdatesTheLocationState]{
				InvokeID: generatedInvokeID,
				State:    EmptyFinalizeUpdatesTheLocationStateEmptyPhase,
			})
		}
	case EmptyFinalizeUpdatesTheLocationStateUnmatchedPhase:
		//line empty_finalize_updates_the_location.scxml:144
		// W3C SCXML 3.8: onentry block 0 (break on error stops subsequent actions)
		for actionBlock0 := 0; actionBlock0 < 1; actionBlock0++ {
			_ = actionBlock0

	// W3C SCXML 6.2: send id="__send_2"
	{
		eventDataStr := ""
		_ = eventDataStr
	// W3C SCXML 6.2: Delayed send
	{
		delayDur := sce.ParseDelay("3s")
		delayEvtName := "timeoutUnmatched"
		if delayEvt, delayOk := p.GetEventFromName(delayEvtName); delayOk {
			engine.ScheduleEvent(delayEvt, delayDur, "__send_2", eventDataStr)
		}
	}
	}

		}
		// W3C SCXML 6.4: Defer invoke execution until macrostep end
		{
			generatedInvokeID := fmt.Sprintf("%s.%d.inv_unmatched", "unmatchedPhase", sce.NextInvokeCounter())
			sce.DeferInvoke(&p.pendingInvokes, sce.PendingInvoke[EmptyFinalizeUpdatesTheLocationState]{
				InvokeID: generatedInvokeID,
				State:    EmptyFinalizeUpdatesTheLocationStateUnmatchedPhase,
			})
		}
	default:
		// No entry actions
	}
}

// ExecuteExitActions executes onexit actions for a state (W3C SCXML 3.9).
//line empty_finalize_updates_the_location.scxml:52
func (p *EmptyFinalizeUpdatesTheLocationPolicy) ExecuteExitActions(state EmptyFinalizeUpdatesTheLocationState, engine *sce.Engine[EmptyFinalizeUpdatesTheLocationState, EmptyFinalizeUpdatesTheLocationEvent], preTransitionActive []EmptyFinalizeUpdatesTheLocationState) {
	p.ensureScriptEngine()
	// W3C SCXML 6.4: Cancel pending invokes and cleanup active children on state exit
	switch state {
	case EmptyFinalizeUpdatesTheLocationStateAbsentPhase:
		sce.CancelInvokesForState(&p.pendingInvokes, EmptyFinalizeUpdatesTheLocationStateAbsentPhase)
		if p.childInvAbsent != nil {
			p.childInvAbsent = nil
		}
		delete(p.activeInvokes, "inv_absent")
		p.pendingDoneInvokeInvAbsent = false
	case EmptyFinalizeUpdatesTheLocationStateEmptyPhase:
		sce.CancelInvokesForState(&p.pendingInvokes, EmptyFinalizeUpdatesTheLocationStateEmptyPhase)
		if p.childInvEmpty != nil {
			p.childInvEmpty = nil
		}
		delete(p.activeInvokes, "inv_empty")
		p.pendingDoneInvokeInvEmpty = false
	case EmptyFinalizeUpdatesTheLocationStateUnmatchedPhase:
		sce.CancelInvokesForState(&p.pendingInvokes, EmptyFinalizeUpdatesTheLocationStateUnmatchedPhase)
		if p.childInvUnmatched != nil {
			p.childInvUnmatched = nil
		}
		delete(p.activeInvokes, "inv_unmatched")
		p.pendingDoneInvokeInvUnmatched = false
	default:
	}
	switch state {
	default:
		// No exit actions
	}
}

// ProcessTransition evaluates guards and takes a matching transition (W3C SCXML 3.13).
// Returns true if a transition was taken.
//line empty_finalize_updates_the_location.scxml:52
func (p *EmptyFinalizeUpdatesTheLocationPolicy) ProcessTransition(currentState *EmptyFinalizeUpdatesTheLocationState, event EmptyFinalizeUpdatesTheLocationEvent, engine *sce.Engine[EmptyFinalizeUpdatesTheLocationState, EmptyFinalizeUpdatesTheLocationEvent]) bool {
	// W3C SCXML 5.10: Bind _event system variable for guard evaluation
	if event != EmptyFinalizeUpdatesTheLocationEventNull {
		// §scxml-B-2-8-1: the rung the payload got, handed to the engine
		// rather than dropped. This is the only frame that has both the
		// reading and the event it belongs to.
		engine.NotePayloadReading(event, p.setCurrentEvent(p.GetEventName(event)))
	}

	// W3C SCXML 3.12: Try transitions in current state first
	if p.tryTransitionInState(*currentState, event, currentState, engine) {
		return true
	}


	return false
}


// tryTransitionInState checks transitions for a single state.
//line empty_finalize_updates_the_location.scxml:52
func (p *EmptyFinalizeUpdatesTheLocationPolicy) tryTransitionInState(checkState EmptyFinalizeUpdatesTheLocationState, event EmptyFinalizeUpdatesTheLocationEvent, currentState *EmptyFinalizeUpdatesTheLocationState, engine *sce.Engine[EmptyFinalizeUpdatesTheLocationState, EmptyFinalizeUpdatesTheLocationEvent]) bool {
	switch checkState {
	case EmptyFinalizeUpdatesTheLocationStateAbsentPhase:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == EmptyFinalizeUpdatesTheLocationEventFromAbsentChild {
			if p.evaluateGuard(`(guard ~= 1)`, engine) {
			*currentState = EmptyFinalizeUpdatesTheLocationStateFailUpdatedWithoutFinalize
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = EmptyFinalizeUpdatesTheLocationStateAbsentPhase
			return true
			}
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == EmptyFinalizeUpdatesTheLocationEventFromAbsentChild {
			*currentState = EmptyFinalizeUpdatesTheLocationStateUnmatchedPhase
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = EmptyFinalizeUpdatesTheLocationStateAbsentPhase
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == EmptyFinalizeUpdatesTheLocationEventTimeoutAbsent {
			*currentState = EmptyFinalizeUpdatesTheLocationStateFailAbsentChildSilent
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = EmptyFinalizeUpdatesTheLocationStateAbsentPhase
			return true
		}
	case EmptyFinalizeUpdatesTheLocationStateEmptyPhase:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == EmptyFinalizeUpdatesTheLocationEventFromEmptyChild {
			if p.evaluateGuard(`(tally == 7)`, engine) {
			*currentState = EmptyFinalizeUpdatesTheLocationStateAbsentPhase
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = EmptyFinalizeUpdatesTheLocationStateEmptyPhase
			return true
			}
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == EmptyFinalizeUpdatesTheLocationEventFromEmptyChild {
			*currentState = EmptyFinalizeUpdatesTheLocationStateFailNotUpdated
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = EmptyFinalizeUpdatesTheLocationStateEmptyPhase
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == EmptyFinalizeUpdatesTheLocationEventTimeoutEmpty {
			*currentState = EmptyFinalizeUpdatesTheLocationStateFailEmptyChildSilent
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = EmptyFinalizeUpdatesTheLocationStateEmptyPhase
			return true
		}
	case EmptyFinalizeUpdatesTheLocationStateUnmatchedPhase:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == EmptyFinalizeUpdatesTheLocationEventFromUnmatchedChild {
			if p.evaluateGuard(`(keeper ~= 3)`, engine) {
			*currentState = EmptyFinalizeUpdatesTheLocationStateFailUnmatchedNameWrote
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = EmptyFinalizeUpdatesTheLocationStateUnmatchedPhase
			return true
			}
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == EmptyFinalizeUpdatesTheLocationEventFromUnmatchedChild {
			*currentState = EmptyFinalizeUpdatesTheLocationStatePass
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = EmptyFinalizeUpdatesTheLocationStateUnmatchedPhase
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == EmptyFinalizeUpdatesTheLocationEventTimeoutUnmatched {
			*currentState = EmptyFinalizeUpdatesTheLocationStateFailUnmatchedChildSilent
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = EmptyFinalizeUpdatesTheLocationStateUnmatchedPhase
			return true
		}
	}
	return false
}

// ExecuteTransitionActions executes actions for the last taken transition (W3C SCXML 3.13).
//line empty_finalize_updates_the_location.scxml:52
func (p *EmptyFinalizeUpdatesTheLocationPolicy) ExecuteTransitionActions(engine *sce.Engine[EmptyFinalizeUpdatesTheLocationState, EmptyFinalizeUpdatesTheLocationEvent]) {
}
