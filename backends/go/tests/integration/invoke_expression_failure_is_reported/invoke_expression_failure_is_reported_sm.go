// SCE-GENERATED — DO NOT EDIT
// source-hash: c13fc11d9e6b67c63b67dbeb6e7f8a2510d3fe8eb6d1c2b86bb43bf5477e7518
// template-hash: 938c1287426f8f00d9215d1077aa072366881702629aba3e463cff0d9353836f
// generated-at: 0


// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) 2025-2026 [Author of input SCXML file]
//
// Generated by SCE Code Generator (Go backend)
// From: integration_resources/invoke_expression_failure_is_reported/invoke_expression_failure_is_reported.scxml
//
// This generated code is MIT licensed and may be freely used in any project.
// Runtime engine dependency (sce-go-runtime): LGPL-2.1-or-later WITH LicenseRef-SCE-Linking-Exception OR LicenseRef-SCE-Commercial
// Full license: https://github.com/newmassrael/scxml-core-engine/blob/main/LICENSE
// Generated Go state machine for `invoke_expression_failure_is_reported`.
//
// Generated by SCE Code Generator (Go backend).
// Implements sce.StatePolicy with full transition logic,
// entry/exit actions, and event processing.


// SCE-MAP: invoke_expression_failure_is_reported.scxml:38 :: _machine

package invoke_expression_failure_is_reported

import (
	"fmt"
	"path/filepath"
	"strings"
	"time"

	sce "github.com/newmassrael/sce-go-runtime"
)

// Ensure imports are used
var (
	_ = fmt.Sprintf
	_ = filepath.Join
	_ = strings.HasPrefix
	_ = time.Duration(0)
)

// ======================================================================
// State type (W3C SCXML 3.3)
// ======================================================================

type InvokeExpressionFailureIsReportedState int

const (
	InvokeExpressionFailureIsReportedStateFail InvokeExpressionFailureIsReportedState = 0
	InvokeExpressionFailureIsReportedStatePass InvokeExpressionFailureIsReportedState = 1
	InvokeExpressionFailureIsReportedStateProbe InvokeExpressionFailureIsReportedState = 2
)

func (s InvokeExpressionFailureIsReportedState) String() string {
	switch s {
	case InvokeExpressionFailureIsReportedStateFail:
		return "fail"
	case InvokeExpressionFailureIsReportedStatePass:
		return "pass"
	case InvokeExpressionFailureIsReportedStateProbe:
		return "probe"
	}
	return "unknown"
}

// InvokeExpressionFailureIsReportedStateFromName is the read half of the pair above — a state
// id back into the state it names (W3C SCXML 3.3).
//
// A host that persists where a machine was has to write it down as TEXT: the
// constants above are a build artefact of one binary, and the process that
// resumes is a different one. String publishes the name and this reads it back,
// which is what lets a journal survive its own record and reach
// sce.Engine.EnterAt.
//
// The second return is false for a name this document does not declare. A name
// guessed at rather than refused is how a restore reaches a configuration
// nobody recorded — and the refusal is what makes a typo in a journal a
// reported failure instead of a machine quietly somewhere else.
//
// Emitted from the same loop over the document's states as String, so the two
// age together.
func InvokeExpressionFailureIsReportedStateFromName(name string) (InvokeExpressionFailureIsReportedState, bool) {
	switch name {
	case "fail":
		return InvokeExpressionFailureIsReportedStateFail, true
	case "pass":
		return InvokeExpressionFailureIsReportedStatePass, true
	case "probe":
		return InvokeExpressionFailureIsReportedStateProbe, true
	}
	var zero InvokeExpressionFailureIsReportedState
	return zero, false
}

// InvokeExpressionFailureIsReportedAllStates is every state this document declares, in the
// order the constants above are issued.
//
// Emitted from the same loop, so a walk over it is a walk over the document
// rather than over a list somebody maintained beside it — a test that spells
// its own list goes on passing when the document grows a state.
var InvokeExpressionFailureIsReportedAllStates = []InvokeExpressionFailureIsReportedState{
	InvokeExpressionFailureIsReportedStateFail,
	InvokeExpressionFailureIsReportedStatePass,
	InvokeExpressionFailureIsReportedStateProbe,
}

// ======================================================================
// Event type (W3C SCXML 3.12)
// ======================================================================

type InvokeExpressionFailureIsReportedEvent int

const (
	InvokeExpressionFailureIsReportedEventDoneInvoke InvokeExpressionFailureIsReportedEvent = 0
	InvokeExpressionFailureIsReportedEventErrorExecution InvokeExpressionFailureIsReportedEvent = 1
	// W3C SCXML 3.13: Sentinel for eventless transition dispatch
	InvokeExpressionFailureIsReportedEventNull InvokeExpressionFailureIsReportedEvent = 2
)

func (e InvokeExpressionFailureIsReportedEvent) String() string {
	switch e {
	case InvokeExpressionFailureIsReportedEventDoneInvoke:
		return "done.invoke"
	case InvokeExpressionFailureIsReportedEventErrorExecution:
		return "error.execution"
	case InvokeExpressionFailureIsReportedEventNull:
		return ""
	}
	return "unknown"
}

// ======================================================================
// Policy struct
// ======================================================================

type InvokeExpressionFailureIsReportedPolicy struct {
	// W3C SCXML 3.13: Last transition metadata
	lastTransitionIsInternal  bool
	lastTransitionIsTargetless bool
	lastTransitionSourceState InvokeExpressionFailureIsReportedState
	// W3C SCXML 5.10.1: External event flag
	nextEventIsExternal bool
	pendingEventName string
	pendingEventData string
	pendingEventType string
	pendingEventSendid string
	pendingEventOrigin string
	pendingEventOrigintype string
	pendingEventInvokeid string
	// W3C SCXML 5.10: Session ID
	SessionID string
	// W3C SCXML B.1: Script engine handle (per-instance, Path B+ Q1=(d) Go=interface ref)
	ScriptEngine sce.IScriptEngine
	scriptEngineInitialized bool
	// W3C SCXML C.2.3: inbound BasicHTTP endpoint serving this machine,
	// declared by the deployment before Initialize(). The address belongs to
	// whoever runs the listener, so the machine takes it from here rather than
	// guessing one. Empty means no such endpoint is deployed, and no BasicHTTP
	// entry is published in _ioprocessors.
	BasicHTTPAccessURI string
	pendingInvokes []sce.PendingInvoke[InvokeExpressionFailureIsReportedState]
	activeInvokes  map[string]*sce.ChildSession
	childInvoke0 sce.ChildEngine
	pendingDoneInvokeInvoke0 bool
	// W3C SCXML 6.4: Parent communication
	ParentExternalQueue chan sce.ParentEvent
	InvokeID           string
	ChildSessionID     string
	// §scxml-6.4.3: invoke param VALUES staged by the parent, applied after
	// this machine's datamodel init so they override its `<data>` defaults.
	pendingParams      map[string]interface{}
}

// NewInvokeExpressionFailureIsReportedPolicy creates a new policy with default values.
func NewInvokeExpressionFailureIsReportedPolicy() InvokeExpressionFailureIsReportedPolicy {
	return InvokeExpressionFailureIsReportedPolicy{
		lastTransitionSourceState: InvokeExpressionFailureIsReportedStateProbe,
		pendingInvokes: make([]sce.PendingInvoke[InvokeExpressionFailureIsReportedState], 0),
		activeInvokes:  make(map[string]*sce.ChildSession),
	}
}




// ensureScriptEngine lazily initializes the script engine session.
//
// Engine DI Parity RFC: panics if ScriptEngine was not set on the policy
// before Initialize() — mirrors Kotlin's mandatory constructor parameter
// contract. Silent no-op would otherwise turn missed injection into a
// hidden runtime regression (every <assign>/<log>/guard becomes a no-op
// and the SM walks to its default-state final without diagnostic).
func (p *InvokeExpressionFailureIsReportedPolicy) ensureScriptEngine() {
	if p.scriptEngineInitialized {
		return
	}
	engine := p.ScriptEngine
	if engine == nil {
		panic("InvokeExpressionFailureIsReportedPolicy: ScriptEngine field must be set before Initialize()")
	}
	sessionID := p.SessionID
	if sessionID == "" {
		sessionID = sce.GenerateSessionID()
		p.SessionID = sessionID
	}
	if !engine.HasSession(sessionID) {
		_ = engine.CreateSession(sessionID)
	}
	// W3C SCXML C.1.1 / C.2.3: the _ioprocessors entries come from the same
	// helper every other backend uses, so a machine reads the same entry names
	// and the same addresses whichever one runs it.
	_ = engine.SetupSystemVariables(sessionID, "invoke_expression_failure_is_reported",
		sce.BuildIoProcessors(sessionID, p.BasicHTTPAccessURI))
	p.scriptEngineInitialized = true
}

// InitializeDataModel sets up datamodel via script engine (W3C SCXML 5.3).
func (p *InvokeExpressionFailureIsReportedPolicy) InitializeDataModel(eng *sce.Engine[InvokeExpressionFailureIsReportedState, InvokeExpressionFailureIsReportedEvent]) {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	sessionID := p.SessionID

	// W3C SCXML 5.3: Initialize _sessionid
	_ = engine.SetVariable(sessionID, "_sessionid", sessionID)

	// W3C SCXML 5.3: Initialize _name
	_ = engine.SetVariable(sessionID, "_name", "invoke_expression_failure_is_reported")

	// W3C SCXML 5.2.2: Initialize global datamodel variables
	// W3C SCXML 5.2/5.3: Initialize target from expr="null"
	{
		result, err := engine.EvaluateExpression(sessionID, `nil`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "target", result)
		} else {
			eng.Raise(sce.NewPlatformError(InvokeExpressionFailureIsReportedEventErrorExecution, "<data id='target'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "target", nil)
		}
	}



	// §scxml-6.4.3: apply the invoke params the parent staged — overriding the
	// `<data>` default this block just seeded, and only for a name the child
	// declares ("if the names do not match, the Processor MUST NOT add the
	// value ... to the invoked session's data model"). `HasVariable` is that
	// filter, and it is this channel's only copy of the rule.
	if len(p.pendingParams) > 0 {
		for paramName, paramValue := range p.pendingParams {
			if engine.HasVariable(sessionID, paramName) {
				_ = engine.SetVariable(sessionID, paramName, paramValue)
			}
		}
		p.pendingParams = nil
	}
}

// evaluateGuard evaluates a guard condition via script engine.
// W3C SCXML 4.3.2: If a condition expression cannot be evaluated, the processor
// MUST treat it as false and MUST raise error.execution.
func (p *InvokeExpressionFailureIsReportedPolicy) evaluateGuard(guard string, eng *sce.Engine[InvokeExpressionFailureIsReportedState, InvokeExpressionFailureIsReportedEvent]) bool {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	result, err := engine.EvaluateExpression(p.SessionID, guard)
	if err != nil {
		eng.Raise(sce.NewPlatformError(InvokeExpressionFailureIsReportedEventErrorExecution, "a <transition> cond failed to evaluate"))
		return false
	}
	return sce.ScriptToBool(result)
}

// executeScript executes a script block via script engine.
func (p *InvokeExpressionFailureIsReportedPolicy) executeScript(script string) error {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	return engine.ExecuteScript(p.SessionID, script)
}

// setCurrentEvent binds _event system variable (W3C SCXML 5.10) and answers
// which rung of §scxml-B-2-8-1 the payload got, because the binding is the
// only place that knows.
//
// This used to be `_ = engine.SetCurrentEvent(...)`: the ladder decided
// between a DOM, a value and a space-normalized string, and the decision was
// dropped one line later. A payload that announced structure and would not
// parse therefore reached the document as raw characters, every
// _event.data.<field> read empty, and nothing anywhere could say so — see
// sce.PayloadReading.
func (p *InvokeExpressionFailureIsReportedPolicy) setCurrentEvent(name string) sce.PayloadReading {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	data := p.pendingEventData
	eventType := p.pendingEventType
	sendID := p.pendingEventSendid
	// W3C SCXML C.1: `_event.origin` is the sender's published
	// `_ioprocessors` location, not its bare session id — and this is the one
	// place that publishes `_event` to the document, so this is where the id
	// becomes a location. The engine keeps the bare id in
	// `EventMetadata.Origin` because its session-keyed lookups (<finalize>
	// dispatch, cancelled-invoke filtering) match on it; converting at the
	// raise would make one value serve two consumers that need different
	// spellings. The conversion itself lives in `sce.PublishedOrigin`, the
	// port of the `IOProcessorHelper::publishedOrigin` the C++ engines share:
	// a second spelling of the rule is how the backends would stop agreeing.
	origin := sce.PublishedOrigin(p.pendingEventOrigin)
	originType := p.pendingEventOrigintype
	invokeID := p.pendingEventInvokeid
	reading, err := engine.SetCurrentEvent(p.SessionID, sce.SetCurrentEventArgs{
		Name:       name,
		Data:       data,
		Type:       eventType,
		SendID:     sendID,
		Origin:     origin,
		OriginType: originType,
		InvokeID:   invokeID,
	})
	if err != nil {
		// An engine that could not bind _event at all has a larger problem
		// than an unreadable payload, and reporting PayloadUndecodable for it
		// would point a host at the wrong thing.
		return sce.PayloadAbsent
	}
	return reading
}

// assignVariable assigns a value to a datamodel variable via script engine.
func (p *InvokeExpressionFailureIsReportedPolicy) assignVariable(location, expr string) error {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	script := location + " = " + expr
	return engine.ExecuteScript(p.SessionID, script)
}

// getVariable reads a datamodel variable from the script engine.
func (p *InvokeExpressionFailureIsReportedPolicy) getVariable(name string) interface{} {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	result, err := engine.GetVariable(p.SessionID, name)
	if err != nil {
		return nil
	}
	return result
}

// SetParamValueInScriptEngine stores an invoke param's VALUE for deferred
// application after datamodel init (§scxml-6.4.3).
//
// Params are applied at the end of InitializeDataModel, overriding the
// `<data>` default only for names the child declares. The value crosses as a
// value: this took the parent's value rendered as Lua source and
// re-evaluated it here until 2026-08-21, which survived every value a test
// had tried and lost the ones Lua cannot spell — `ToScriptLiteral` renders a
// non-finite number as `+Inf`, which is not a Lua expression, so an
// `<invoke>` `<param expr="1/0"/>` arrived as nothing at all.
func (p *InvokeExpressionFailureIsReportedPolicy) SetParamValueInScriptEngine(name string, value interface{}) {
	if p.pendingParams == nil {
		p.pendingParams = make(map[string]interface{})
	}
	p.pendingParams[name] = value
}


// ExecutePendingInvokes starts deferred child state machines (W3C SCXML 6.4).
// Port of Rust do_execute_pending_invokes().
func (p *InvokeExpressionFailureIsReportedPolicy) ExecutePendingInvokes(engine *sce.Engine[InvokeExpressionFailureIsReportedState, InvokeExpressionFailureIsReportedEvent]) {
	if len(p.pendingInvokes) == 0 {
		return
	}

	// W3C SCXML 6.4: Copy-and-clear to prevent iterator invalidation
	invokesToExecute := make([]sce.PendingInvoke[InvokeExpressionFailureIsReportedState], len(p.pendingInvokes))
	copy(invokesToExecute, p.pendingInvokes)
	p.pendingInvokes = p.pendingInvokes[:0]

	for _, pending := range invokesToExecute {
		if strings.Contains(pending.InvokeID, "._invoke_0") {
			// W3C SCXML 6.4.3: Evaluate srcexpr at runtime
			p.ensureScriptEngine()
			{
				se := p.ScriptEngine
				if _, err := se.EvaluateExpression(p.SessionID, `target.path`); err != nil {
					engine.Raise(sce.NewPlatformError(InvokeExpressionFailureIsReportedEventErrorExecution, "<invoke srcexpr='target.path'> could not be evaluated"))
					continue
				}
			}

			// W3C SCXML 6.5: Generate child session ID
			childSessionID := fmt.Sprintf("%s.%s", p.SessionID, pending.InvokeID)

			childPolicy := NewInvokeExpressionFailureIsReportedHybrid0Policy()
			parentQueue := make(chan sce.ParentEvent, 100)
			childPolicy.ParentExternalQueue = parentQueue
			childPolicy.InvokeID = pending.InvokeID
			childPolicy.ChildSessionID = childSessionID
			// W3C SCXML C.1: the child adopts the id its parent recorded for
			// it. The parent mints `parentSession.invokeId` and keys
			// `activeInvokes` on it, so an event from this child reaches the
			// parent carrying that id as its origin; if the child also minted
			// an id of its own, the location it publishes in `_ioprocessors`
			// and the origin the parent sees would be two names for one
			// session that can never be compared — which is exactly what C.1
			// requires of them.
			childPolicy.SessionID = childSessionID

			p.activeInvokes["_invoke_0"] = &sce.ChildSession{
				SessionID:       childSessionID,
				InvokeID:        "_invoke_0",
				ParentSessionID: p.SessionID,
				Autoforward:     false,
				FinalizeScript:  "",
			}

			childEngine := sce.NewEngine[InvokeExpressionFailureIsReportedHybrid0State, InvokeExpressionFailureIsReportedHybrid0Event](&childPolicy)
			// §scxml-6.4: the child's delayed sends are measured against the
			// same clock as ours. A child reading its own would start its
			// origin at construction time, so <send delay="100ms"> on either
			// side of the boundary would mean two different absolute instants —
			// and on a host-owned clock the child would not move at all,
			// because the host advances the engine it holds, not the ones that
			// engine invoked.
			childEngine.SetClock(engine.Clock())
			childEngine.SetCompletionCallback(func() {})
			childEngine.Initialize()

			wrapper := &childEngineWrapperInvoke0{engine: childEngine, policy: &childPolicy}
			p.childInvoke0 = wrapper

			sce.DrainAndRaiseChildEvents(wrapper, p.activeInvokes, "_invoke_0", engine)

			if wrapper.IsInFinalState() {
				p.pendingDoneInvokeInvoke0 = true
				// W3C SCXML 5.5 + 6.3.1: Lift the child's stashed donedata onto
				// done.invoke.<id>._event.data.
				sce.RaiseDoneInvoke("_invoke_0", wrapper.DonedataAtFinal(), engine)
			}

			continue
		}
	}
}

// TickChildren ticks child state machines (W3C SCXML 6.4).
// Port of Rust do_tick_children().
func (p *InvokeExpressionFailureIsReportedPolicy) TickChildren(engine *sce.Engine[InvokeExpressionFailureIsReportedState, InvokeExpressionFailureIsReportedEvent]) {
	if !p.pendingDoneInvokeInvoke0 && p.childInvoke0 != nil {
		child := p.childInvoke0
		sce.DrainAndRaiseChildEvents(child, p.activeInvokes, "_invoke_0", engine)
		child.Tick()
		sce.DrainAndRaiseChildEvents(child, p.activeInvokes, "_invoke_0", engine)
		if child.IsInFinalState() && !p.pendingDoneInvokeInvoke0 {
			p.pendingDoneInvokeInvoke0 = true
			// W3C SCXML 5.5 + 6.3.1: Lift the child's stashed donedata onto
			// done.invoke.<id>._event.data.
			sce.RaiseDoneInvoke("_invoke_0", child.DonedataAtFinal(), engine)
		}
	}
}




// ── Child engine wrappers (implement sce.ChildEngine interface) ──

type childEngineWrapperInvoke0 struct {
	engine *sce.Engine[InvokeExpressionFailureIsReportedHybrid0State, InvokeExpressionFailureIsReportedHybrid0Event]
	policy *InvokeExpressionFailureIsReportedHybrid0Policy
}
func (w *childEngineWrapperInvoke0) Initialize() { w.engine.Initialize() }
func (w *childEngineWrapperInvoke0) Tick() { w.engine.Tick() }
func (w *childEngineWrapperInvoke0) IsInFinalState() bool { return w.engine.IsInFinalState() }
func (w *childEngineWrapperInvoke0) RaiseExternalByName(name, data string) { w.engine.RaiseExternalByName(name, data) }
func (w *childEngineWrapperInvoke0) RaiseExternalByNameWithMeta(name string, metadata sce.EventMetadata) { w.engine.RaiseExternalByNameWithMeta(name, metadata) }
func (w *childEngineWrapperInvoke0) SetCompletionCallback(cb func()) { w.engine.SetCompletionCallback(cb) }
func (w *childEngineWrapperInvoke0) GetParentEventQueue() chan sce.ParentEvent { return w.policy.ParentExternalQueue }
func (w *childEngineWrapperInvoke0) DonedataAtFinal() string { return w.engine.DonedataAtFinal() }


// ======================================================================
// StatePolicy interface implementation
// ======================================================================

// InitialState returns the initial state (W3C SCXML 3.3).
func (p *InvokeExpressionFailureIsReportedPolicy) InitialState() InvokeExpressionFailureIsReportedState {
	return InvokeExpressionFailureIsReportedStateProbe
}

// IsFinalState returns true if state is a <final> state (W3C SCXML 3.7).
func (p *InvokeExpressionFailureIsReportedPolicy) IsFinalState(state InvokeExpressionFailureIsReportedState) bool {
	switch state {
	case InvokeExpressionFailureIsReportedStateFail:
		return true
	case InvokeExpressionFailureIsReportedStatePass:
		return true
	}
	return false
}

// GetParent returns the parent of state, or (zero, false) if root (W3C SCXML 3.3).
func (p *InvokeExpressionFailureIsReportedPolicy) GetParent(state InvokeExpressionFailureIsReportedState) (InvokeExpressionFailureIsReportedState, bool) {
	switch state {
	}
	return 0, false
}

// IsCompoundState returns true if state has children (W3C SCXML 3.3).
func (p *InvokeExpressionFailureIsReportedPolicy) IsCompoundState(state InvokeExpressionFailureIsReportedState) bool {
	switch state {
	}
	return false
}

func (p *InvokeExpressionFailureIsReportedPolicy) IsParallelState(_ InvokeExpressionFailureIsReportedState) bool { return false }
func (p *InvokeExpressionFailureIsReportedPolicy) GetParallelRegions(_ InvokeExpressionFailureIsReportedState) []InvokeExpressionFailureIsReportedState { return nil }

// IsDescendantOf returns true if desc is a descendant of anc (W3C SCXML 3.12).
func (p *InvokeExpressionFailureIsReportedPolicy) IsDescendantOf(desc, anc InvokeExpressionFailureIsReportedState) bool {
	current := desc
	for {
		parent, ok := p.GetParent(current)
		if !ok {
			return false
		}
		if parent == anc {
			return true
		}
		current = parent
	}
}

// GetDocumentOrder returns the document order index (W3C SCXML Appendix D).
func (p *InvokeExpressionFailureIsReportedPolicy) GetDocumentOrder(state InvokeExpressionFailureIsReportedState) int {
	switch state {
	case InvokeExpressionFailureIsReportedStateFail:
		return 2
	case InvokeExpressionFailureIsReportedStatePass:
		return 1
	case InvokeExpressionFailureIsReportedStateProbe:
		return 0
	}
	return -1
}

// GetEventName returns the string name of an event (W3C SCXML 3.12).
func (p *InvokeExpressionFailureIsReportedPolicy) GetEventName(event InvokeExpressionFailureIsReportedEvent) string {
	return event.String()
}

// GetEventFromName looks up an event by name (W3C SCXML 3.12).
func (p *InvokeExpressionFailureIsReportedPolicy) GetEventFromName(name string) (InvokeExpressionFailureIsReportedEvent, bool) {
	switch name {
	case "done.invoke":
		return InvokeExpressionFailureIsReportedEventDoneInvoke, true
	case "error.execution":
		return InvokeExpressionFailureIsReportedEventErrorExecution, true
	}
	return InvokeExpressionFailureIsReportedEventNull, false
}

// GetStateName returns the string name of a state (W3C SCXML 3.3).
func (p *InvokeExpressionFailureIsReportedPolicy) GetStateName(state InvokeExpressionFailureIsReportedState) string {
	return state.String()
}

// GetStateFromName reads a state id back into the state it names (W3C SCXML 3.3).
// The reverse of GetStateName, and what turns a host's recorded configuration
// back into the argument sce.Engine.EnterAt takes.
func (p *InvokeExpressionFailureIsReportedPolicy) GetStateFromName(name string) (InvokeExpressionFailureIsReportedState, bool) {
	return InvokeExpressionFailureIsReportedStateFromName(name)
}

// NullEvent returns the sentinel for eventless transition dispatch (W3C SCXML 3.13).
func (p *InvokeExpressionFailureIsReportedPolicy) NullEvent() InvokeExpressionFailureIsReportedEvent {
	return InvokeExpressionFailureIsReportedEventNull
}

// GetInitialChildren returns initial children of a compound state (W3C SCXML 3.6).
func (p *InvokeExpressionFailureIsReportedPolicy) GetInitialChildren(state InvokeExpressionFailureIsReportedState) []InvokeExpressionFailureIsReportedState {
	switch state {
	}
	return nil
}

// LastTransitionIsInternal returns the internal transition flag (W3C SCXML 3.13).
func (p *InvokeExpressionFailureIsReportedPolicy) LastTransitionIsInternal() bool {
	return p.lastTransitionIsInternal
}

// SetLastTransitionIsInternal sets the internal transition flag.
func (p *InvokeExpressionFailureIsReportedPolicy) SetLastTransitionIsInternal(value bool) {
	p.lastTransitionIsInternal = value
}

// LastTransitionIsTargetless returns the targetless transition flag (W3C SCXML 3.13).
func (p *InvokeExpressionFailureIsReportedPolicy) LastTransitionIsTargetless() bool {
	return p.lastTransitionIsTargetless
}

// SetLastTransitionIsTargetless sets the targetless transition flag.
func (p *InvokeExpressionFailureIsReportedPolicy) SetLastTransitionIsTargetless(value bool) {
	p.lastTransitionIsTargetless = value
}

// LastTransitionSourceState returns the source state of the last transition.
func (p *InvokeExpressionFailureIsReportedPolicy) LastTransitionSourceState() InvokeExpressionFailureIsReportedState {
	return p.lastTransitionSourceState
}

// SetLastTransitionSourceState sets the source state of the last transition.
func (p *InvokeExpressionFailureIsReportedPolicy) SetLastTransitionSourceState(state InvokeExpressionFailureIsReportedState) {
	p.lastTransitionSourceState = state
}


// SetNextEventIsExternal sets the external event flag (W3C SCXML 5.10.1).
func (p *InvokeExpressionFailureIsReportedPolicy) SetNextEventIsExternal(value bool) {
	p.nextEventIsExternal = value
}

// HasParallelStates returns whether the SM has parallel states.
func (p *InvokeExpressionFailureIsReportedPolicy) HasParallelStates() bool {
	return false
}

// NeedsScriptEngine returns whether the SM needs a script engine.
func (p *InvokeExpressionFailureIsReportedPolicy) NeedsScriptEngine() bool {
	return true
}

// NeedsEventScheduler reports whether a host must drive this machine with
// Tick rather than Step alone.
//
// W3C SCXML 6.2: Step runs a macrostep and never drains the delayed-send
// scheduler nor ticks an invoked child, so a host that only ever calls it
// waits forever with nothing said. Same verdict the generate manifest
// publishes as needs_event_scheduler.
func (p *InvokeExpressionFailureIsReportedPolicy) NeedsEventScheduler() bool {
	return true
}

// NeedsDataModelInit returns whether the SM needs datamodel init.
func (p *InvokeExpressionFailureIsReportedPolicy) NeedsDataModelInit() bool {
	return true
}

// HasInvokeSupport returns true for invoke-capable SMs.
func (p *InvokeExpressionFailureIsReportedPolicy) HasInvokeSupport() bool { return true }
// HasChildTick returns true for child-ticking SMs.
func (p *InvokeExpressionFailureIsReportedPolicy) HasChildTick() bool { return true }
func (p *InvokeExpressionFailureIsReportedPolicy) HasFinalize() bool { return false }
func (p *InvokeExpressionFailureIsReportedPolicy) HasAutoforward() bool { return false }
func (p *InvokeExpressionFailureIsReportedPolicy) HasActiveStates() bool { return false }
func (p *InvokeExpressionFailureIsReportedPolicy) GetActiveStates() []InvokeExpressionFailureIsReportedState { return nil }
// W3C SCXML 3.4: this machine keeps no active set — its configuration is the
// parent walk from the current state — so there is nothing for a restore to
// hand back here. sce.Engine.EnterAt reaches this only through HasActiveStates,
// which is false above; the method exists because the interface is one contract.
func (p *InvokeExpressionFailureIsReportedPolicy) SetActiveStates(_ []InvokeExpressionFailureIsReportedState) {}
func (p *InvokeExpressionFailureIsReportedPolicy) HasExternalEventFlag() bool { return true }
// GetInitialOrHistoryChild returns the initial child considering history (W3C SCXML 3.11).
func (p *InvokeExpressionFailureIsReportedPolicy) GetInitialOrHistoryChild(state InvokeExpressionFailureIsReportedState) InvokeExpressionFailureIsReportedState {
	children := p.GetInitialChildren(state)
	if len(children) > 0 {
		return children[0]
	}
	return state
}
// ExecuteFinalizeForChildEvent is a no-op (no finalize invokes).
func (p *InvokeExpressionFailureIsReportedPolicy) ExecuteFinalizeForChildEvent(_ *sce.EventWithMetadata[InvokeExpressionFailureIsReportedEvent], _ *sce.Engine[InvokeExpressionFailureIsReportedState, InvokeExpressionFailureIsReportedEvent]) {}
// ForwardToAutoforwardChildren is a no-op (no autoforward invokes).
func (p *InvokeExpressionFailureIsReportedPolicy) ForwardToAutoforwardChildren(_ string, _ sce.EventMetadata, _ *sce.Engine[InvokeExpressionFailureIsReportedState, InvokeExpressionFailureIsReportedEvent]) {}

// PopulateEventMetadata stores pending event metadata (W3C SCXML 5.10).
// Note: event name is set separately via setCurrentEvent(), not from metadata.
func (p *InvokeExpressionFailureIsReportedPolicy) PopulateEventMetadata(meta *sce.EventMetadata) {
	p.pendingEventData = meta.Data
	p.pendingEventType = meta.EventType.String()
	p.pendingEventSendid = meta.SendID
	p.pendingEventOrigin = meta.Origin
	p.pendingEventOrigintype = meta.OriginType
	p.pendingEventInvokeid = meta.InvokeID
}

// ClearEventMetadata resets pending event metadata (W3C SCXML 5.10).
func (p *InvokeExpressionFailureIsReportedPolicy) ClearEventMetadata() {
	p.pendingEventName = ""
	p.pendingEventData = ""
	p.pendingEventType = ""
	p.pendingEventSendid = ""
	p.pendingEventOrigin = ""
	p.pendingEventOrigintype = ""
	p.pendingEventInvokeid = ""
}




// ExecuteEntryActions executes onentry actions for a state (W3C SCXML 3.8).
//line invoke_expression_failure_is_reported.scxml:38
func (p *InvokeExpressionFailureIsReportedPolicy) ExecuteEntryActions(state InvokeExpressionFailureIsReportedState, engine *sce.Engine[InvokeExpressionFailureIsReportedState, InvokeExpressionFailureIsReportedEvent], pathChild *InvokeExpressionFailureIsReportedState) {
	// Only a `<parallel>` machine descends into defaults here, so a machine
	// without one has nothing to tell an ancestor entry from a target entry.
	_ = pathChild
	p.ensureScriptEngine()
	switch state {
	case InvokeExpressionFailureIsReportedStateProbe:
		//line invoke_expression_failure_is_reported.scxml:44
		// W3C SCXML 6.4: Defer invoke execution until macrostep end
		{
			generatedInvokeID := fmt.Sprintf("%s.%d._invoke_0", "probe", sce.NextInvokeCounter())
			sce.DeferInvoke(&p.pendingInvokes, sce.PendingInvoke[InvokeExpressionFailureIsReportedState]{
				InvokeID: generatedInvokeID,
				State:    InvokeExpressionFailureIsReportedStateProbe,
			})
		}
	default:
		// No entry actions
	}
}

// ExecuteExitActions executes onexit actions for a state (W3C SCXML 3.9).
//line invoke_expression_failure_is_reported.scxml:38
func (p *InvokeExpressionFailureIsReportedPolicy) ExecuteExitActions(state InvokeExpressionFailureIsReportedState, engine *sce.Engine[InvokeExpressionFailureIsReportedState, InvokeExpressionFailureIsReportedEvent], preTransitionActive []InvokeExpressionFailureIsReportedState) {
	p.ensureScriptEngine()
	// W3C SCXML 6.4: Cancel pending invokes and cleanup active children on state exit
	switch state {
	case InvokeExpressionFailureIsReportedStateProbe:
		sce.CancelInvokesForState(&p.pendingInvokes, InvokeExpressionFailureIsReportedStateProbe)
		if p.childInvoke0 != nil {
			p.childInvoke0 = nil
		}
		delete(p.activeInvokes, "_invoke_0")
		p.pendingDoneInvokeInvoke0 = false
	default:
	}
	switch state {
	default:
		// No exit actions
	}
}

// ProcessTransition evaluates guards and takes a matching transition (W3C SCXML 3.13).
// Returns true if a transition was taken.
//line invoke_expression_failure_is_reported.scxml:38
func (p *InvokeExpressionFailureIsReportedPolicy) ProcessTransition(currentState *InvokeExpressionFailureIsReportedState, event InvokeExpressionFailureIsReportedEvent, engine *sce.Engine[InvokeExpressionFailureIsReportedState, InvokeExpressionFailureIsReportedEvent]) bool {
	// W3C SCXML 5.10: Bind _event system variable for guard evaluation
	if event != InvokeExpressionFailureIsReportedEventNull {
		// §scxml-B-2-8-1: the rung the payload got, handed to the engine
		// rather than dropped. This is the only frame that has both the
		// reading and the event it belongs to.
		engine.NotePayloadReading(event, p.setCurrentEvent(p.GetEventName(event)))
	}

	// W3C SCXML 3.12: Try transitions in current state first
	if p.tryTransitionInState(*currentState, event, currentState, engine) {
		return true
	}


	return false
}


// tryTransitionInState checks transitions for a single state.
//line invoke_expression_failure_is_reported.scxml:38
func (p *InvokeExpressionFailureIsReportedPolicy) tryTransitionInState(checkState InvokeExpressionFailureIsReportedState, event InvokeExpressionFailureIsReportedEvent, currentState *InvokeExpressionFailureIsReportedState, engine *sce.Engine[InvokeExpressionFailureIsReportedState, InvokeExpressionFailureIsReportedEvent]) bool {
	switch checkState {
	case InvokeExpressionFailureIsReportedStateProbe:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == InvokeExpressionFailureIsReportedEventErrorExecution {
			*currentState = InvokeExpressionFailureIsReportedStatePass
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = InvokeExpressionFailureIsReportedStateProbe
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == InvokeExpressionFailureIsReportedEventDoneInvoke {
			*currentState = InvokeExpressionFailureIsReportedStateFail
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = InvokeExpressionFailureIsReportedStateProbe
			return true
		}
	}
	return false
}

// ExecuteTransitionActions executes actions for the last taken transition (W3C SCXML 3.13).
//line invoke_expression_failure_is_reported.scxml:38
func (p *InvokeExpressionFailureIsReportedPolicy) ExecuteTransitionActions(engine *sce.Engine[InvokeExpressionFailureIsReportedState, InvokeExpressionFailureIsReportedEvent]) {
}
