// SCE-GENERATED — DO NOT EDIT
// source-hash: 9e85e11997494073a40bf661d9f24e2bc472fad7b57e93e3c87932bf8615ff0f
// template-hash: 1f4e4f9fb7a6afbcc11d24c73b03e4acaa51ec03610a45bb92197b670933aad7
// generated-at: 0


// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) 2025-2026 [Author of input SCXML file]
//
// Generated by SCE Code Generator (Go backend)
// From: examples/ai_loop/ai_loop.scxml
//
// This generated code is MIT licensed and may be freely used in any project.
// Runtime engine dependency (sce-go-runtime): LGPL-2.1-or-later WITH LicenseRef-SCE-Linking-Exception OR LicenseRef-SCE-Commercial
// Full license: https://github.com/newmassrael/scxml-core-engine/blob/main/LICENSE
// Generated Go state machine for `ai_loop`.
//
// Generated by SCE Code Generator (Go backend).
// Implements sce.StatePolicy with full transition logic,
// entry/exit actions, and event processing.


// SCE-MAP: ai_loop.scxml:127 :: _machine

package ai_loop

import (
	"fmt"
	"path/filepath"
	"sort"
	"strings"
	"time"

	sce "github.com/newmassrael/sce-go-runtime"
)

// Ensure imports are used
var (
	_ = fmt.Sprintf
	_ = filepath.Join
	_ = sort.Slice
	_ = strings.HasPrefix
	_ = time.Duration(0)
)

// ======================================================================
// State type (W3C SCXML 3.3)
// ======================================================================

type AiLoopState int

const (
	AiLoopStateAbandoned AiLoopState = 0
	AiLoopStateAlive AiLoopState = 1
	AiLoopStateBlocked AiLoopState = 2
	AiLoopStateBudget AiLoopState = 3
	AiLoopStateCancelled AiLoopState = 4
	AiLoopStateClosing AiLoopState = 5
	AiLoopStateConverged AiLoopState = 6
	AiLoopStateDrive AiLoopState = 7
	AiLoopStateExhausted AiLoopState = 8
	AiLoopStateFailed AiLoopState = 9
	AiLoopStateJudging AiLoopState = 10
	AiLoopStatePaused AiLoopState = 11
	AiLoopStatePriming AiLoopState = 12
	AiLoopStateRebuilding AiLoopState = 13
	AiLoopStateReflecting AiLoopState = 14
	AiLoopStateReported AiLoopState = 15
	AiLoopStateRestarting AiLoopState = 16
	AiLoopStateRun AiLoopState = 17
	AiLoopStateRunning AiLoopState = 18
	AiLoopStateScreening AiLoopState = 19
	AiLoopStateSpent AiLoopState = 20
	AiLoopStateStuck AiLoopState = 21
	AiLoopStateWatch AiLoopState = 22
	AiLoopStateWithin AiLoopState = 23
	AiLoopStateWorking AiLoopState = 24
)

func (s AiLoopState) String() string {
	switch s {
	case AiLoopStateAbandoned:
		return "abandoned"
	case AiLoopStateAlive:
		return "alive"
	case AiLoopStateBlocked:
		return "blocked"
	case AiLoopStateBudget:
		return "budget"
	case AiLoopStateCancelled:
		return "cancelled"
	case AiLoopStateClosing:
		return "closing"
	case AiLoopStateConverged:
		return "converged"
	case AiLoopStateDrive:
		return "drive"
	case AiLoopStateExhausted:
		return "exhausted"
	case AiLoopStateFailed:
		return "failed"
	case AiLoopStateJudging:
		return "judging"
	case AiLoopStatePaused:
		return "paused"
	case AiLoopStatePriming:
		return "priming"
	case AiLoopStateRebuilding:
		return "rebuilding"
	case AiLoopStateReflecting:
		return "reflecting"
	case AiLoopStateReported:
		return "reported"
	case AiLoopStateRestarting:
		return "restarting"
	case AiLoopStateRun:
		return "run"
	case AiLoopStateRunning:
		return "running"
	case AiLoopStateScreening:
		return "screening"
	case AiLoopStateSpent:
		return "spent"
	case AiLoopStateStuck:
		return "stuck"
	case AiLoopStateWatch:
		return "watch"
	case AiLoopStateWithin:
		return "within"
	case AiLoopStateWorking:
		return "working"
	}
	return "unknown"
}

// AiLoopStateFromName is the read half of the pair above — a state
// id back into the state it names (W3C SCXML 3.3).
//
// A host that persists where a machine was has to write it down as TEXT: the
// constants above are a build artefact of one binary, and the process that
// resumes is a different one. String publishes the name and this reads it back,
// which is what lets a journal survive its own record and reach
// sce.Engine.EnterAt.
//
// The second return is false for a name this document does not declare. A name
// guessed at rather than refused is how a restore reaches a configuration
// nobody recorded — and the refusal is what makes a typo in a journal a
// reported failure instead of a machine quietly somewhere else.
//
// Emitted from the same loop over the document's states as String, so the two
// age together.
func AiLoopStateFromName(name string) (AiLoopState, bool) {
	switch name {
	case "abandoned":
		return AiLoopStateAbandoned, true
	case "alive":
		return AiLoopStateAlive, true
	case "blocked":
		return AiLoopStateBlocked, true
	case "budget":
		return AiLoopStateBudget, true
	case "cancelled":
		return AiLoopStateCancelled, true
	case "closing":
		return AiLoopStateClosing, true
	case "converged":
		return AiLoopStateConverged, true
	case "drive":
		return AiLoopStateDrive, true
	case "exhausted":
		return AiLoopStateExhausted, true
	case "failed":
		return AiLoopStateFailed, true
	case "judging":
		return AiLoopStateJudging, true
	case "paused":
		return AiLoopStatePaused, true
	case "priming":
		return AiLoopStatePriming, true
	case "rebuilding":
		return AiLoopStateRebuilding, true
	case "reflecting":
		return AiLoopStateReflecting, true
	case "reported":
		return AiLoopStateReported, true
	case "restarting":
		return AiLoopStateRestarting, true
	case "run":
		return AiLoopStateRun, true
	case "running":
		return AiLoopStateRunning, true
	case "screening":
		return AiLoopStateScreening, true
	case "spent":
		return AiLoopStateSpent, true
	case "stuck":
		return AiLoopStateStuck, true
	case "watch":
		return AiLoopStateWatch, true
	case "within":
		return AiLoopStateWithin, true
	case "working":
		return AiLoopStateWorking, true
	}
	var zero AiLoopState
	return zero, false
}

// AiLoopAllStates is every state this document declares, in the
// order the constants above are issued.
//
// Emitted from the same loop, so a walk over it is a walk over the document
// rather than over a list somebody maintained beside it — a test that spells
// its own list goes on passing when the document grows a state.
var AiLoopAllStates = []AiLoopState{
	AiLoopStateAbandoned,
	AiLoopStateAlive,
	AiLoopStateBlocked,
	AiLoopStateBudget,
	AiLoopStateCancelled,
	AiLoopStateClosing,
	AiLoopStateConverged,
	AiLoopStateDrive,
	AiLoopStateExhausted,
	AiLoopStateFailed,
	AiLoopStateJudging,
	AiLoopStatePaused,
	AiLoopStatePriming,
	AiLoopStateRebuilding,
	AiLoopStateReflecting,
	AiLoopStateReported,
	AiLoopStateRestarting,
	AiLoopStateRun,
	AiLoopStateRunning,
	AiLoopStateScreening,
	AiLoopStateSpent,
	AiLoopStateStuck,
	AiLoopStateWatch,
	AiLoopStateWithin,
	AiLoopStateWorking,
}

// ======================================================================
// Event type (W3C SCXML 3.12)
// ======================================================================

type AiLoopEvent int

const (
	AiLoopEventCancel AiLoopEvent = 0
	AiLoopEventDoneStateDrive AiLoopEvent = 1
	AiLoopEventDoneStateRun AiLoopEvent = 2
	AiLoopEventDoneStateRunning AiLoopEvent = 3
	AiLoopEventErrorExecution AiLoopEvent = 4
	AiLoopEventFail AiLoopEvent = 5
	AiLoopEventHold AiLoopEvent = 6
	AiLoopEventJudge AiLoopEvent = 7
	AiLoopEventJudgeBegin AiLoopEvent = 8
	AiLoopEventNotifyHuman AiLoopEvent = 9
	AiLoopEventPromptEnd AiLoopEvent = 10
	AiLoopEventPromptSent AiLoopEvent = 11
	AiLoopEventPromptStart AiLoopEvent = 12
	AiLoopEventPromptTurn AiLoopEvent = 13
	AiLoopEventReflectApplied AiLoopEvent = 14
	AiLoopEventReflectBegin AiLoopEvent = 15
	AiLoopEventReflectNone AiLoopEvent = 16
	AiLoopEventResume AiLoopEvent = 17
	AiLoopEventRunBlocked AiLoopEvent = 18
	AiLoopEventRunConverged AiLoopEvent = 19
	AiLoopEventRunExhausted AiLoopEvent = 20
	AiLoopEventScreenBegin AiLoopEvent = 21
	AiLoopEventScreenMatched AiLoopEvent = 22
	AiLoopEventScreenNone AiLoopEvent = 23
	AiLoopEventSessionLost AiLoopEvent = 24
	AiLoopEventSessionReady AiLoopEvent = 25
	AiLoopEventSessionReplace AiLoopEvent = 26
	AiLoopEventTurnBlocked AiLoopEvent = 27
	AiLoopEventTurnDone AiLoopEvent = 28
	AiLoopEventTurnInterrupted AiLoopEvent = 29
	AiLoopEventUnattended AiLoopEvent = 30
	// W3C SCXML 3.13: Sentinel for eventless transition dispatch
	AiLoopEventNull AiLoopEvent = 31
)

func (e AiLoopEvent) String() string {
	switch e {
	case AiLoopEventCancel:
		return "cancel"
	case AiLoopEventDoneStateDrive:
		return "done.state.drive"
	case AiLoopEventDoneStateRun:
		return "done.state.run"
	case AiLoopEventDoneStateRunning:
		return "done.state.running"
	case AiLoopEventErrorExecution:
		return "error.execution"
	case AiLoopEventFail:
		return "fail"
	case AiLoopEventHold:
		return "hold"
	case AiLoopEventJudge:
		return "judge"
	case AiLoopEventJudgeBegin:
		return "judge.begin"
	case AiLoopEventNotifyHuman:
		return "notify.human"
	case AiLoopEventPromptEnd:
		return "prompt.end"
	case AiLoopEventPromptSent:
		return "prompt.sent"
	case AiLoopEventPromptStart:
		return "prompt.start"
	case AiLoopEventPromptTurn:
		return "prompt.turn"
	case AiLoopEventReflectApplied:
		return "reflect.applied"
	case AiLoopEventReflectBegin:
		return "reflect.begin"
	case AiLoopEventReflectNone:
		return "reflect.none"
	case AiLoopEventResume:
		return "resume"
	case AiLoopEventRunBlocked:
		return "run.blocked"
	case AiLoopEventRunConverged:
		return "run.converged"
	case AiLoopEventRunExhausted:
		return "run.exhausted"
	case AiLoopEventScreenBegin:
		return "screen.begin"
	case AiLoopEventScreenMatched:
		return "screen.matched"
	case AiLoopEventScreenNone:
		return "screen.none"
	case AiLoopEventSessionLost:
		return "session.lost"
	case AiLoopEventSessionReady:
		return "session.ready"
	case AiLoopEventSessionReplace:
		return "session.replace"
	case AiLoopEventTurnBlocked:
		return "turn.blocked"
	case AiLoopEventTurnDone:
		return "turn.done"
	case AiLoopEventTurnInterrupted:
		return "turn.interrupted"
	case AiLoopEventUnattended:
		return "unattended"
	case AiLoopEventNull:
		return ""
	}
	return "unknown"
}

// ======================================================================
// Policy struct
// ======================================================================

type AiLoopPolicy struct {
	// W3C SCXML 3.13: Last transition metadata
	lastTransitionIsInternal  bool
	lastTransitionIsTargetless bool
	lastTransitionSourceState AiLoopState
	// W3C SCXML 3.13: Transition action tracking
	lastTransitionIndex   int
	hasTransitionActions   bool
	// W3C SCXML 3.4: Active state configuration for parallel states / In() predicate
	activeStates []AiLoopState
	// W3C SCXML 5.10.1: External event flag
	nextEventIsExternal bool
	pendingEventName string
	pendingEventData string
	pendingEventType string
	pendingEventSendid string
	pendingEventOrigin string
	pendingEventOrigintype string
	pendingEventInvokeid string
	// W3C SCXML 3.11: History state where (shallow)
	historyWhere []AiLoopState
	// W3C SCXML 5.10: Session ID
	SessionID string
	// W3C SCXML B.1: Script engine handle (per-instance, Path B+ Q1=(d) Go=interface ref)
	ScriptEngine sce.IScriptEngine
	scriptEngineInitialized bool
	// W3C SCXML C.2.3: inbound BasicHTTP endpoint serving this machine,
	// declared by the deployment before Initialize(). The address belongs to
	// whoever runs the listener, so the machine takes it from here rather than
	// guessing one. Empty means no such endpoint is deployed, and no BasicHTTP
	// entry is published in _ioprocessors.
	BasicHTTPAccessURI string
	inPredicateStates []string
	// W3C SCXML 6.4: Parent communication
	ParentExternalQueue chan sce.ParentEvent
	InvokeID           string
	ChildSessionID     string
	// §scxml-6.4.3: invoke param VALUES staged by the parent, applied after
	// this machine's datamodel init so they override its `<data>` defaults.
	pendingParams      map[string]interface{}
}

// NewAiLoopPolicy creates a new policy with default values.
func NewAiLoopPolicy() AiLoopPolicy {
	return AiLoopPolicy{
		lastTransitionSourceState: AiLoopStatePriming,
		activeStates: make([]AiLoopState, 0),
		historyWhere: nil,
	}
}

// NorthStar reports what the `north_star` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `north_star` was assigned a value of another type, or the engine refused.
func (p *AiLoopPolicy) NorthStar() (string, bool) {
	return sce.ReadDatamodelString(p.ScriptEngine, p.SessionID, "north_star")
}

// Milestone reports what the `milestone` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `milestone` was assigned a value of another type, or the engine refused.
func (p *AiLoopPolicy) Milestone() (string, bool) {
	return sce.ReadDatamodelString(p.ScriptEngine, p.SessionID, "milestone")
}

// Reference reports what the `reference` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `reference` was assigned a value of another type, or the engine refused.
func (p *AiLoopPolicy) Reference() (string, bool) {
	return sce.ReadDatamodelString(p.ScriptEngine, p.SessionID, "reference")
}

// StartPrompt reports what the `start_prompt` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `start_prompt` was assigned a value of another type, or the engine refused.
func (p *AiLoopPolicy) StartPrompt() (string, bool) {
	return sce.ReadDatamodelString(p.ScriptEngine, p.SessionID, "start_prompt")
}

// TurnPrompt reports what the `turn_prompt` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `turn_prompt` was assigned a value of another type, or the engine refused.
func (p *AiLoopPolicy) TurnPrompt() (string, bool) {
	return sce.ReadDatamodelString(p.ScriptEngine, p.SessionID, "turn_prompt")
}

// EndPrompt reports what the `end_prompt` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `end_prompt` was assigned a value of another type, or the engine refused.
func (p *AiLoopPolicy) EndPrompt() (string, bool) {
	return sce.ReadDatamodelString(p.ScriptEngine, p.SessionID, "end_prompt")
}

// DoneMarker reports what the `done_marker` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `done_marker` was assigned a value of another type, or the engine refused.
func (p *AiLoopPolicy) DoneMarker() (string, bool) {
	return sce.ReadDatamodelString(p.ScriptEngine, p.SessionID, "done_marker")
}

// ScreenRules reports what the `screen_rules` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `screen_rules` was assigned a value of another type, or the engine refused.
//
// The value as JSON text, serialised by the engine's own JSON.stringify
// (§scxml-B-2) so the key order is the document's.
func (p *AiLoopPolicy) ScreenRules() (string, bool) {
	return sce.ReadDatamodelJSON(p.ScriptEngine, p.SessionID, "screen_rules")
}

// ScreenPermissions reports what the `screen_permissions` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `screen_permissions` was assigned a value of another type, or the engine refused.
func (p *AiLoopPolicy) ScreenPermissions() (bool, bool) {
	return sce.ReadDatamodelBool(p.ScriptEngine, p.SessionID, "screen_permissions")
}

// MaxTurns reports what the `max_turns` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `max_turns` was assigned a value of another type, or the engine refused.
func (p *AiLoopPolicy) MaxTurns() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "max_turns")
}

// ReflectEvery reports what the `reflect_every` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `reflect_every` was assigned a value of another type, or the engine refused.
func (p *AiLoopPolicy) ReflectEvery() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "reflect_every")
}

// MaxRestarts reports what the `max_restarts` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `max_restarts` was assigned a value of another type, or the engine refused.
func (p *AiLoopPolicy) MaxRestarts() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "max_restarts")
}

// Turns reports what the `turns` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `turns` was assigned a value of another type, or the engine refused.
func (p *AiLoopPolicy) Turns() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "turns")
}

// TurnsSinceReflect reports what the `turns_since_reflect` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `turns_since_reflect` was assigned a value of another type, or the engine refused.
func (p *AiLoopPolicy) TurnsSinceReflect() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "turns_since_reflect")
}

// Screened reports what the `screened` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `screened` was assigned a value of another type, or the engine refused.
func (p *AiLoopPolicy) Screened() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "screened")
}

// Restarts reports what the `restarts` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `restarts` was assigned a value of another type, or the engine refused.
func (p *AiLoopPolicy) Restarts() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "restarts")
}



// IsStateActive checks if a state is active by name (W3C SCXML 5.9.2: In() predicate).
func (p *AiLoopPolicy) IsStateActive(stateID string) bool {
	for _, s := range p.activeStates {
		if p.GetStateName(s) == stateID {
			return true
		}
	}
	return false
}


// ensureScriptEngine lazily initializes the script engine session.
//
// Engine DI Parity RFC: panics if ScriptEngine was not set on the policy
// before Initialize() — mirrors Kotlin's mandatory constructor parameter
// contract. Silent no-op would otherwise turn missed injection into a
// hidden runtime regression (every <assign>/<log>/guard becomes a no-op
// and the SM walks to its default-state final without diagnostic).
func (p *AiLoopPolicy) ensureScriptEngine() {
	if p.scriptEngineInitialized {
		return
	}
	engine := p.ScriptEngine
	if engine == nil {
		panic("AiLoopPolicy: ScriptEngine field must be set before Initialize()")
	}
	sessionID := p.SessionID
	if sessionID == "" {
		sessionID = sce.GenerateSessionID()
		p.SessionID = sessionID
	}
	if !engine.HasSession(sessionID) {
		_ = engine.CreateSession(sessionID)
	}
	// W3C SCXML C.1.1 / C.2.3: the _ioprocessors entries come from the same
	// helper every other backend uses, so a machine reads the same entry names
	// and the same addresses whichever one runs it.
	_ = engine.SetupSystemVariables(sessionID, "ai_loop",
		sce.BuildIoProcessors(sessionID, p.BasicHTTPAccessURI))
	// W3C SCXML 5.9.2: Register In() predicate callback
	engine.SetStateQueryCallback(sessionID, func(stateID string) bool {
		return p.IsStateActive(stateID)
	})
	p.scriptEngineInitialized = true
}

// InitializeDataModel sets up datamodel via script engine (W3C SCXML 5.3).
func (p *AiLoopPolicy) InitializeDataModel(eng *sce.Engine[AiLoopState, AiLoopEvent]) {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	sessionID := p.SessionID

	// W3C SCXML 5.3: Initialize _sessionid
	_ = engine.SetVariable(sessionID, "_sessionid", sessionID)

	// W3C SCXML 5.3: Initialize _name
	_ = engine.SetVariable(sessionID, "_name", "ai_loop")

	// W3C SCXML 5.2.2: Initialize global datamodel variables
	// W3C SCXML 5.2/5.3: Initialize north_star from expr="'(edit me) the outcome this loop exists to reach'"
	{
		result, err := engine.EvaluateExpression(sessionID, `"(edit me) the outcome this loop exists to reach"`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "north_star", result)
		} else {
			eng.Raise(sce.NewPlatformError(AiLoopEventErrorExecution, "<data id='north_star'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "north_star", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize milestone from expr="'(edit me) the next checkpoint on the way there'"
	{
		result, err := engine.EvaluateExpression(sessionID, `"(edit me) the next checkpoint on the way there"`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "milestone", result)
		} else {
			eng.Raise(sce.NewPlatformError(AiLoopEventErrorExecution, "<data id='milestone'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "milestone", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize reference from expr="'(edit me) paths, URLs or repos to consult'"
	{
		result, err := engine.EvaluateExpression(sessionID, `"(edit me) paths, URLs or repos to consult"`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "reference", result)
		} else {
			eng.Raise(sce.NewPlatformError(AiLoopEventErrorExecution, "<data id='reference'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "reference", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize start_prompt from expr="'North star: ' + north_star + '\n' +                 'Milestone: ' + milestone + '\n' +                 'Reference: ' + reference + '\n' +                 'Report what you did and what is left.'"
	{
		result, err := engine.EvaluateExpression(sessionID, `(_scxml_tostring((_scxml_tostring(_scxml_add((_scxml_tostring((_scxml_tostring(_scxml_add((_scxml_tostring((_scxml_tostring(("North star: " .. _scxml_tostring(north_star))) .. "\n")) .. "Milestone: "), milestone)) .. "\n")) .. "Reference: "), reference)) .. "\n")) .. "Report what you did and what is left.")`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "start_prompt", result)
		} else {
			eng.Raise(sce.NewPlatformError(AiLoopEventErrorExecution, "<data id='start_prompt'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "start_prompt", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize turn_prompt from expr="'Continue toward: ' + milestone + '\n' +                 'Do the next smallest thing that is verifiable, then report.'"
	{
		result, err := engine.EvaluateExpression(sessionID, `(_scxml_tostring((_scxml_tostring(("Continue toward: " .. _scxml_tostring(milestone))) .. "\n")) .. "Do the next smallest thing that is verifiable, then report.")`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "turn_prompt", result)
		} else {
			eng.Raise(sce.NewPlatformError(AiLoopEventErrorExecution, "<data id='turn_prompt'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "turn_prompt", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize end_prompt from expr="'Summarise what changed, what was verified, and what is left open.'"
	{
		result, err := engine.EvaluateExpression(sessionID, `"Summarise what changed, what was verified, and what is left open."`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "end_prompt", result)
		} else {
			eng.Raise(sce.NewPlatformError(AiLoopEventErrorExecution, "<data id='end_prompt'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "end_prompt", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize done_marker from expr="'MILESTONE REACHED'"
	{
		result, err := engine.EvaluateExpression(sessionID, `"MILESTONE REACHED"`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "done_marker", result)
		} else {
			eng.Raise(sce.NewPlatformError(AiLoopEventErrorExecution, "<data id='done_marker'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "done_marker", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize screen_rules from expr="[             { when: 'design-decision', keys: 'Escape',               text: 'Ignore cost. Rethink for the most durable answer, then proceed.' },             { when: 'design-proposal', keys: 'Escape',               text: 'Ignore cost. Rethink for the most durable answer, then proceed.' },             { when: 'multiple-choice', keys: 'Escape',               text: 'Ignore cost. Rethink for the most durable answer, then proceed.' }           ]"
	{
		result, err := engine.EvaluateExpression(sessionID, `{{["when"] = "design-decision", ["keys"] = "Escape", ["text"] = "Ignore cost. Rethink for the most durable answer, then proceed."}, {["when"] = "design-proposal", ["keys"] = "Escape", ["text"] = "Ignore cost. Rethink for the most durable answer, then proceed."}, {["when"] = "multiple-choice", ["keys"] = "Escape", ["text"] = "Ignore cost. Rethink for the most durable answer, then proceed."}}`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "screen_rules", result)
		} else {
			eng.Raise(sce.NewPlatformError(AiLoopEventErrorExecution, "<data id='screen_rules'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "screen_rules", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize screen_permissions from expr="false"
	{
		result, err := engine.EvaluateExpression(sessionID, `false`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "screen_permissions", result)
		} else {
			eng.Raise(sce.NewPlatformError(AiLoopEventErrorExecution, "<data id='screen_permissions'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "screen_permissions", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize max_turns from expr="40"
	{
		result, err := engine.EvaluateExpression(sessionID, `40`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "max_turns", result)
		} else {
			eng.Raise(sce.NewPlatformError(AiLoopEventErrorExecution, "<data id='max_turns'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "max_turns", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize reflect_every from expr="8"
	{
		result, err := engine.EvaluateExpression(sessionID, `8`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "reflect_every", result)
		} else {
			eng.Raise(sce.NewPlatformError(AiLoopEventErrorExecution, "<data id='reflect_every'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "reflect_every", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize max_restarts from expr="6"
	{
		result, err := engine.EvaluateExpression(sessionID, `6`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "max_restarts", result)
		} else {
			eng.Raise(sce.NewPlatformError(AiLoopEventErrorExecution, "<data id='max_restarts'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "max_restarts", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize turns from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "turns", result)
		} else {
			eng.Raise(sce.NewPlatformError(AiLoopEventErrorExecution, "<data id='turns'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "turns", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize turns_since_reflect from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "turns_since_reflect", result)
		} else {
			eng.Raise(sce.NewPlatformError(AiLoopEventErrorExecution, "<data id='turns_since_reflect'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "turns_since_reflect", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize screened from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "screened", result)
		} else {
			eng.Raise(sce.NewPlatformError(AiLoopEventErrorExecution, "<data id='screened'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "screened", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize restarts from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "restarts", result)
		} else {
			eng.Raise(sce.NewPlatformError(AiLoopEventErrorExecution, "<data id='restarts'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "restarts", nil)
		}
	}



	// §scxml-6.4.3: apply the invoke params the parent staged — overriding the
	// `<data>` default this block just seeded, and only for a name the child
	// declares ("if the names do not match, the Processor MUST NOT add the
	// value ... to the invoked session's data model"). `HasVariable` is that
	// filter, and it is this channel's only copy of the rule.
	if len(p.pendingParams) > 0 {
		for paramName, paramValue := range p.pendingParams {
			if engine.HasVariable(sessionID, paramName) {
				_ = engine.SetVariable(sessionID, paramName, paramValue)
			}
		}
		p.pendingParams = nil
	}
}

// evaluateGuard evaluates a guard condition via script engine.
// W3C SCXML 4.3.2: If a condition expression cannot be evaluated, the processor
// MUST treat it as false and MUST raise error.execution.
func (p *AiLoopPolicy) evaluateGuard(guard string, eng *sce.Engine[AiLoopState, AiLoopEvent]) bool {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	result, err := engine.EvaluateExpression(p.SessionID, guard)
	if err != nil {
		eng.Raise(sce.NewPlatformError(AiLoopEventErrorExecution, "a <transition> cond failed to evaluate"))
		return false
	}
	return sce.ScriptToBool(result)
}

// executeScript executes a script block via script engine.
func (p *AiLoopPolicy) executeScript(script string) error {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	return engine.ExecuteScript(p.SessionID, script)
}

// setCurrentEvent binds _event system variable (W3C SCXML 5.10) and answers
// which rung of §scxml-B-2-8-1 the payload got, because the binding is the
// only place that knows.
//
// This used to be `_ = engine.SetCurrentEvent(...)`: the ladder decided
// between a DOM, a value and a space-normalized string, and the decision was
// dropped one line later. A payload that announced structure and would not
// parse therefore reached the document as raw characters, every
// _event.data.<field> read empty, and nothing anywhere could say so — see
// sce.PayloadReading.
func (p *AiLoopPolicy) setCurrentEvent(name string) sce.PayloadReading {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	data := p.pendingEventData
	eventType := p.pendingEventType
	sendID := p.pendingEventSendid
	// W3C SCXML C.1: `_event.origin` is the sender's published
	// `_ioprocessors` location, not its bare session id — and this is the one
	// place that publishes `_event` to the document, so this is where the id
	// becomes a location. The engine keeps the bare id in
	// `EventMetadata.Origin` because its session-keyed lookups (<finalize>
	// dispatch, cancelled-invoke filtering) match on it; converting at the
	// raise would make one value serve two consumers that need different
	// spellings. The conversion itself lives in `sce.PublishedOrigin`, the
	// port of the `IOProcessorHelper::publishedOrigin` the C++ engines share:
	// a second spelling of the rule is how the backends would stop agreeing.
	origin := sce.PublishedOrigin(p.pendingEventOrigin)
	originType := p.pendingEventOrigintype
	invokeID := p.pendingEventInvokeid
	reading, err := engine.SetCurrentEvent(p.SessionID, sce.SetCurrentEventArgs{
		Name:       name,
		Data:       data,
		Type:       eventType,
		SendID:     sendID,
		Origin:     origin,
		OriginType: originType,
		InvokeID:   invokeID,
	})
	if err != nil {
		// An engine that could not bind _event at all has a larger problem
		// than an unreadable payload, and reporting PayloadUndecodable for it
		// would point a host at the wrong thing.
		return sce.PayloadAbsent
	}
	return reading
}

// assignVariable assigns a value to a datamodel variable via script engine.
func (p *AiLoopPolicy) assignVariable(location, expr string) error {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	script := location + " = " + expr
	return engine.ExecuteScript(p.SessionID, script)
}

// getVariable reads a datamodel variable from the script engine.
func (p *AiLoopPolicy) getVariable(name string) interface{} {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	result, err := engine.GetVariable(p.SessionID, name)
	if err != nil {
		return nil
	}
	return result
}

// SetParamValueInScriptEngine stores an invoke param's VALUE for deferred
// application after datamodel init (§scxml-6.4.3).
//
// Params are applied at the end of InitializeDataModel, overriding the
// `<data>` default only for names the child declares. The value crosses as a
// value: this took the parent's value rendered as Lua source and
// re-evaluated it here until 2026-08-21, which survived every value a test
// had tried and lost the ones Lua cannot spell — `ToScriptLiteral` renders a
// non-finite number as `+Inf`, which is not a Lua expression, so an
// `<invoke>` `<param expr="1/0"/>` arrived as nothing at all.
func (p *AiLoopPolicy) SetParamValueInScriptEngine(name string, value interface{}) {
	if p.pendingParams == nil {
		p.pendingParams = make(map[string]interface{})
	}
	p.pendingParams[name] = value
}


// ExecutePendingInvokes is a no-op (no invokes in this SM).
func (p *AiLoopPolicy) ExecutePendingInvokes(engine *sce.Engine[AiLoopState, AiLoopEvent]) {}

// TickChildren is a no-op (no invokes in this SM).
func (p *AiLoopPolicy) TickChildren(engine *sce.Engine[AiLoopState, AiLoopEvent]) {}

// ======================================================================
// StatePolicy interface implementation
// ======================================================================

// InitialState returns the initial state (W3C SCXML 3.3).
func (p *AiLoopPolicy) InitialState() AiLoopState {
	return AiLoopStateRun
}

// IsFinalState returns true if state is a <final> state (W3C SCXML 3.7).
func (p *AiLoopPolicy) IsFinalState(state AiLoopState) bool {
	switch state {
	case AiLoopStateAbandoned:
		return true
	case AiLoopStateBlocked:
		return true
	case AiLoopStateCancelled:
		return true
	case AiLoopStateConverged:
		return true
	case AiLoopStateExhausted:
		return true
	case AiLoopStateFailed:
		return true
	case AiLoopStateReported:
		return true
	case AiLoopStateStuck:
		return true
	}
	return false
}

// GetParent returns the parent of state, or (zero, false) if root (W3C SCXML 3.3).
func (p *AiLoopPolicy) GetParent(state AiLoopState) (AiLoopState, bool) {
	switch state {
	case AiLoopStateAbandoned:
		return AiLoopStateDrive, true
	case AiLoopStateAlive:
		return AiLoopStateWatch, true
	case AiLoopStateBudget:
		return AiLoopStateRun, true
	case AiLoopStateClosing:
		return AiLoopStateRunning, true
	case AiLoopStateDrive:
		return AiLoopStateRun, true
	case AiLoopStateJudging:
		return AiLoopStateRunning, true
	case AiLoopStatePaused:
		return AiLoopStateDrive, true
	case AiLoopStatePriming:
		return AiLoopStateRunning, true
	case AiLoopStateRebuilding:
		return AiLoopStateWatch, true
	case AiLoopStateReflecting:
		return AiLoopStateRunning, true
	case AiLoopStateReported:
		return AiLoopStateRunning, true
	case AiLoopStateRestarting:
		return AiLoopStateRunning, true
	case AiLoopStateRunning:
		return AiLoopStateDrive, true
	case AiLoopStateScreening:
		return AiLoopStateRunning, true
	case AiLoopStateSpent:
		return AiLoopStateBudget, true
	case AiLoopStateStuck:
		return AiLoopStateRunning, true
	case AiLoopStateWatch:
		return AiLoopStateRun, true
	case AiLoopStateWithin:
		return AiLoopStateBudget, true
	case AiLoopStateWorking:
		return AiLoopStateRunning, true
	}
	return 0, false
}

// IsCompoundState returns true if state has children (W3C SCXML 3.3).
func (p *AiLoopPolicy) IsCompoundState(state AiLoopState) bool {
	switch state {
	case AiLoopStateBudget:
		return true
	case AiLoopStateDrive:
		return true
	case AiLoopStateRunning:
		return true
	case AiLoopStateWatch:
		return true
	}
	return false
}

// IsParallelState returns true if state is a <parallel> state (W3C SCXML 3.4).
func (p *AiLoopPolicy) IsParallelState(state AiLoopState) bool {
	switch state {
	case AiLoopStateRun:
		return true
	}
	return false
}

// GetParallelRegions returns child regions of a parallel state (W3C SCXML 3.4).
func (p *AiLoopPolicy) GetParallelRegions(state AiLoopState) []AiLoopState {
	switch state {
	case AiLoopStateRun:
		return []AiLoopState{
			AiLoopStateBudget,
			AiLoopStateDrive,
			AiLoopStateWatch,
		}
	}
	return nil
}

// IsDescendantOf returns true if desc is a descendant of anc (W3C SCXML 3.12).
func (p *AiLoopPolicy) IsDescendantOf(desc, anc AiLoopState) bool {
	current := desc
	for {
		parent, ok := p.GetParent(current)
		if !ok {
			return false
		}
		if parent == anc {
			return true
		}
		current = parent
	}
}

// GetDocumentOrder returns the document order index (W3C SCXML Appendix D).
func (p *AiLoopPolicy) GetDocumentOrder(state AiLoopState) int {
	switch state {
	case AiLoopStateAbandoned:
		return 13
	case AiLoopStateAlive:
		return 15
	case AiLoopStateBlocked:
		return 24
	case AiLoopStateBudget:
		return 17
	case AiLoopStateCancelled:
		return 23
	case AiLoopStateClosing:
		return 9
	case AiLoopStateConverged:
		return 20
	case AiLoopStateDrive:
		return 1
	case AiLoopStateExhausted:
		return 21
	case AiLoopStateFailed:
		return 22
	case AiLoopStateJudging:
		return 6
	case AiLoopStatePaused:
		return 12
	case AiLoopStatePriming:
		return 3
	case AiLoopStateRebuilding:
		return 16
	case AiLoopStateReflecting:
		return 7
	case AiLoopStateReported:
		return 10
	case AiLoopStateRestarting:
		return 8
	case AiLoopStateRun:
		return 0
	case AiLoopStateRunning:
		return 2
	case AiLoopStateScreening:
		return 5
	case AiLoopStateSpent:
		return 19
	case AiLoopStateStuck:
		return 11
	case AiLoopStateWatch:
		return 14
	case AiLoopStateWithin:
		return 18
	case AiLoopStateWorking:
		return 4
	}
	return -1
}

// GetEventName returns the string name of an event (W3C SCXML 3.12).
func (p *AiLoopPolicy) GetEventName(event AiLoopEvent) string {
	return event.String()
}

// GetEventFromName looks up an event by name (W3C SCXML 3.12).
func (p *AiLoopPolicy) GetEventFromName(name string) (AiLoopEvent, bool) {
	switch name {
	case "cancel":
		return AiLoopEventCancel, true
	case "done.state.drive":
		return AiLoopEventDoneStateDrive, true
	case "done.state.run":
		return AiLoopEventDoneStateRun, true
	case "done.state.running":
		return AiLoopEventDoneStateRunning, true
	case "error.execution":
		return AiLoopEventErrorExecution, true
	case "fail":
		return AiLoopEventFail, true
	case "hold":
		return AiLoopEventHold, true
	case "judge":
		return AiLoopEventJudge, true
	case "judge.begin":
		return AiLoopEventJudgeBegin, true
	case "notify.human":
		return AiLoopEventNotifyHuman, true
	case "prompt.end":
		return AiLoopEventPromptEnd, true
	case "prompt.sent":
		return AiLoopEventPromptSent, true
	case "prompt.start":
		return AiLoopEventPromptStart, true
	case "prompt.turn":
		return AiLoopEventPromptTurn, true
	case "reflect.applied":
		return AiLoopEventReflectApplied, true
	case "reflect.begin":
		return AiLoopEventReflectBegin, true
	case "reflect.none":
		return AiLoopEventReflectNone, true
	case "resume":
		return AiLoopEventResume, true
	case "run.blocked":
		return AiLoopEventRunBlocked, true
	case "run.converged":
		return AiLoopEventRunConverged, true
	case "run.exhausted":
		return AiLoopEventRunExhausted, true
	case "screen.begin":
		return AiLoopEventScreenBegin, true
	case "screen.matched":
		return AiLoopEventScreenMatched, true
	case "screen.none":
		return AiLoopEventScreenNone, true
	case "session.lost":
		return AiLoopEventSessionLost, true
	case "session.ready":
		return AiLoopEventSessionReady, true
	case "session.replace":
		return AiLoopEventSessionReplace, true
	case "turn.blocked":
		return AiLoopEventTurnBlocked, true
	case "turn.done":
		return AiLoopEventTurnDone, true
	case "turn.interrupted":
		return AiLoopEventTurnInterrupted, true
	case "unattended":
		return AiLoopEventUnattended, true
	}
	return AiLoopEventNull, false
}

// GetStateName returns the string name of a state (W3C SCXML 3.3).
func (p *AiLoopPolicy) GetStateName(state AiLoopState) string {
	return state.String()
}

// GetStateFromName reads a state id back into the state it names (W3C SCXML 3.3).
// The reverse of GetStateName, and what turns a host's recorded configuration
// back into the argument sce.Engine.EnterAt takes.
func (p *AiLoopPolicy) GetStateFromName(name string) (AiLoopState, bool) {
	return AiLoopStateFromName(name)
}

// NullEvent returns the sentinel for eventless transition dispatch (W3C SCXML 3.13).
func (p *AiLoopPolicy) NullEvent() AiLoopEvent {
	return AiLoopEventNull
}

// GetInitialChildren returns initial children of a compound state (W3C SCXML 3.6).
func (p *AiLoopPolicy) GetInitialChildren(state AiLoopState) []AiLoopState {
	switch state {
	case AiLoopStateBudget:
		return []AiLoopState{
			AiLoopStateWithin,
		}
	case AiLoopStateDrive:
		return []AiLoopState{
			AiLoopStateRunning,
		}
	case AiLoopStateRunning:
		return []AiLoopState{
			AiLoopStatePriming,
		}
	case AiLoopStateWatch:
		return []AiLoopState{
			AiLoopStateAlive,
		}
	}
	return nil
}

// LastTransitionIsInternal returns the internal transition flag (W3C SCXML 3.13).
func (p *AiLoopPolicy) LastTransitionIsInternal() bool {
	return p.lastTransitionIsInternal
}

// SetLastTransitionIsInternal sets the internal transition flag.
func (p *AiLoopPolicy) SetLastTransitionIsInternal(value bool) {
	p.lastTransitionIsInternal = value
}

// LastTransitionIsTargetless returns the targetless transition flag (W3C SCXML 3.13).
func (p *AiLoopPolicy) LastTransitionIsTargetless() bool {
	return p.lastTransitionIsTargetless
}

// SetLastTransitionIsTargetless sets the targetless transition flag.
func (p *AiLoopPolicy) SetLastTransitionIsTargetless(value bool) {
	p.lastTransitionIsTargetless = value
}

// LastTransitionSourceState returns the source state of the last transition.
func (p *AiLoopPolicy) LastTransitionSourceState() AiLoopState {
	return p.lastTransitionSourceState
}

// SetLastTransitionSourceState sets the source state of the last transition.
func (p *AiLoopPolicy) SetLastTransitionSourceState(state AiLoopState) {
	p.lastTransitionSourceState = state
}

// GetActiveStates returns the active state configuration (W3C SCXML 3.4).
func (p *AiLoopPolicy) GetActiveStates() []AiLoopState {
	return p.activeStates
}

// SetActiveStates hands this machine an active set whole (W3C SCXML 3.4).
// The write half of GetActiveStates: entry and exit grow the set one state at a
// time as they walk, and sce.Engine.EnterAt hands back a configuration an
// earlier run already settled on. Copied rather than aliased so a caller's slice
// cannot go on changing this machine's configuration behind it.
func (p *AiLoopPolicy) SetActiveStates(states []AiLoopState) {
	p.activeStates = append(p.activeStates[:0:0], states...)
}

// SetNextEventIsExternal sets the external event flag (W3C SCXML 5.10.1).
func (p *AiLoopPolicy) SetNextEventIsExternal(value bool) {
	p.nextEventIsExternal = value
}

// HasParallelStates returns whether the SM has parallel states.
func (p *AiLoopPolicy) HasParallelStates() bool {
	return true
}

// NeedsScriptEngine returns whether the SM needs a script engine.
func (p *AiLoopPolicy) NeedsScriptEngine() bool {
	return true
}

// NeedsEventScheduler reports whether a host must drive this machine with
// Tick rather than Step alone.
//
// W3C SCXML 6.2: Step runs a macrostep and never drains the delayed-send
// scheduler nor ticks an invoked child, so a host that only ever calls it
// waits forever with nothing said. Same verdict the generate manifest
// publishes as needs_event_scheduler.
func (p *AiLoopPolicy) NeedsEventScheduler() bool {
	return false
}

// NeedsDataModelInit returns whether the SM needs datamodel init.
func (p *AiLoopPolicy) NeedsDataModelInit() bool {
	return true
}

func (p *AiLoopPolicy) HasInvokeSupport() bool { return false }
func (p *AiLoopPolicy) HasChildTick() bool { return false }
func (p *AiLoopPolicy) HasFinalize() bool { return false }
func (p *AiLoopPolicy) HasAutoforward() bool { return false }
func (p *AiLoopPolicy) HasActiveStates() bool { return true }
func (p *AiLoopPolicy) HasExternalEventFlag() bool { return true }
// GetInitialOrHistoryChild returns the initial child considering history (W3C SCXML 3.11).
func (p *AiLoopPolicy) GetInitialOrHistoryChild(state AiLoopState) AiLoopState {
	if state == AiLoopStateRunning {
		if p.historyWhere != nil && len(p.historyWhere) > 0 {
			return p.historyWhere[0]
		}
	}
	children := p.GetInitialChildren(state)
	if len(children) > 0 {
		return children[0]
	}
	return state
}
// ExecuteFinalizeForChildEvent is a no-op (no finalize invokes).
func (p *AiLoopPolicy) ExecuteFinalizeForChildEvent(_ *sce.EventWithMetadata[AiLoopEvent], _ *sce.Engine[AiLoopState, AiLoopEvent]) {}
// ForwardToAutoforwardChildren is a no-op (no autoforward invokes).
func (p *AiLoopPolicy) ForwardToAutoforwardChildren(_ string, _ sce.EventMetadata, _ *sce.Engine[AiLoopState, AiLoopEvent]) {}

// PopulateEventMetadata stores pending event metadata (W3C SCXML 5.10).
// Note: event name is set separately via setCurrentEvent(), not from metadata.
func (p *AiLoopPolicy) PopulateEventMetadata(meta *sce.EventMetadata) {
	p.pendingEventData = meta.Data
	p.pendingEventType = meta.EventType.String()
	p.pendingEventSendid = meta.SendID
	p.pendingEventOrigin = meta.Origin
	p.pendingEventOrigintype = meta.OriginType
	p.pendingEventInvokeid = meta.InvokeID
}

// ClearEventMetadata resets pending event metadata (W3C SCXML 5.10).
func (p *AiLoopPolicy) ClearEventMetadata() {
	p.pendingEventName = ""
	p.pendingEventData = ""
	p.pendingEventType = ""
	p.pendingEventSendid = ""
	p.pendingEventOrigin = ""
	p.pendingEventOrigintype = ""
	p.pendingEventInvokeid = ""
}

// resolveHistoryTargetWhere resolves history target for where (W3C SCXML 3.11).
func (p *AiLoopPolicy) resolveHistoryTargetWhere() AiLoopState {
	if p.historyWhere != nil && len(p.historyWhere) > 0 {
		return p.historyWhere[0]
	}
	// No history recorded — use default transition target
	return AiLoopStateWorking
}



// ExecuteEntryActions executes onentry actions for a state (W3C SCXML 3.8).
//line ai_loop.scxml:127
func (p *AiLoopPolicy) ExecuteEntryActions(state AiLoopState, engine *sce.Engine[AiLoopState, AiLoopEvent], pathChild *AiLoopState) {
	p.ensureScriptEngine()
	// W3C SCXML 3.4/3.12.1: Add state to active configuration for parallel states and In() predicate
	for _, s := range p.activeStates {
		if s == state {
			return // W3C SCXML 3.8: Skip onentry actions for duplicate state entry
		}
	}
	p.activeStates = append(p.activeStates, state)
	switch state {
	case AiLoopStateAbandoned:
		//line ai_loop.scxml:465
		// W3C SCXML 3.8: onentry block 0 (break on error stops subsequent actions)
		for actionBlock0 := 0; actionBlock0 < 1; actionBlock0++ {
			_ = actionBlock0

	engine.Raise(sce.NewEventWithMetadata(AiLoopEventRunBlocked))

		}
		// W3C SCXML 3.7: Final state reached — raise done.state.drive
		engine.Raise(sce.NewEventWithMetadata(AiLoopEventDoneStateDrive))
		// W3C SCXML 3.4: Check if parallel grandparent run has all regions complete
		{
			regions := p.GetParallelRegions(AiLoopStateRun)
			allComplete := true
			for _, region := range regions {
				regionDone := false
				for _, s := range p.activeStates {
					if p.IsFinalState(s) && p.IsDescendantOf(s, region) {
						regionDone = true
						break
					}
				}
				if !regionDone {
					allComplete = false
					break
				}
			}
			if allComplete {
				engine.Raise(sce.NewEventWithMetadata(AiLoopEventDoneStateRun))
			}
		}
	case AiLoopStateClosing:
		//line ai_loop.scxml:377
		// W3C SCXML 3.8: onentry block 0 (break on error stops subsequent actions)
		for actionBlock0 := 0; actionBlock0 < 1; actionBlock0++ {
			_ = actionBlock0

	// W3C SCXML 6.2: send id="__send_7"
	// W3C SCXML 6.2: Evaluate <param>/namelist expressions at send time
	{
		p.ensureScriptEngine()
		se := p.ScriptEngine
		// W3C SCXML 6.2 / test178: a name may repeat and every value must be
		// delivered, so this is an ordered list rather than a map. The typed
		// value is kept rather than its text — a receiver reading
		// `_event.data.value === 42` finds the string "42" unequal.
		parts := make([]sce.EventDataParam, 0)
		if paramVal, paramErr := se.EvaluateExpression(p.SessionID, `end_prompt`); paramErr == nil {
			parts = append(parts, sce.EventDataParam{Name: "text", Value: paramVal})
		} else {
			engine.Raise(sce.NewPlatformError(AiLoopEventErrorExecution, "<send> <param name='text'> expr failed to evaluate"))
		}
		eventDataStr := sce.BuildJSONFromTypedParams(parts)
		_ = eventDataStr
	// §scxml-6.2.5: "x-sce-host" is served by the host, which
	// declared it to this build. Dispatch rather than refuse — and take the
	// whole send, because a processor the host serves owns delivery; falling
	// through would also enqueue the event locally and the document would see
	// the act twice.
	{
		hostParams := sce.WireParamsFromTypedParams(parts)
		hostRequest := sce.HostSendRequest{
			ProcessorType: "x-sce-host",
			EventName:     "prompt.end",
			Target:        "",
			Content:       "",
			Params:        hostParams,
			SendID:        "__send_7",
		}
		_, hostServed := engine.PerformHostSend(hostRequest)
		// W3C SCXML 6.2: a declared type with no handler registered is, from
		// the document's side, a processor the platform does not support — the
		// act it asked for was performed by nobody. Same event as an
		// undeclared type, so a wiring mistake cannot read as success.
		if !hostServed && !engine.HasEventProcessor("x-sce-host") {
			errEvt := sce.NewPlatformError(AiLoopEventErrorExecution, "<send type='x-sce-host'> names a processor the host declared but never registered")
			errEvt.Metadata.SendID = "__send_7"
			engine.Raise(errEvt)
		}
	}
	}

		}
	case AiLoopStateJudging:
		//line ai_loop.scxml:316
		// W3C SCXML 3.8: onentry block 0 (break on error stops subsequent actions)
		for actionBlock0 := 0; actionBlock0 < 1; actionBlock0++ {
			_ = actionBlock0

	// W3C SCXML 6.2: send id="__send_3"
	// W3C SCXML 6.2: Evaluate <param>/namelist expressions at send time
	{
		p.ensureScriptEngine()
		se := p.ScriptEngine
		// W3C SCXML 6.2 / test178: a name may repeat and every value must be
		// delivered, so this is an ordered list rather than a map. The typed
		// value is kept rather than its text — a receiver reading
		// `_event.data.value === 42` finds the string "42" unequal.
		parts := make([]sce.EventDataParam, 0)
		if paramVal, paramErr := se.EvaluateExpression(p.SessionID, `done_marker`); paramErr == nil {
			parts = append(parts, sce.EventDataParam{Name: "marker", Value: paramVal})
		} else {
			engine.Raise(sce.NewPlatformError(AiLoopEventErrorExecution, "<send> <param name='marker'> expr failed to evaluate"))
		}
		eventDataStr := sce.BuildJSONFromTypedParams(parts)
		_ = eventDataStr
	// §scxml-6.2.5: "x-sce-host" is served by the host, which
	// declared it to this build. Dispatch rather than refuse — and take the
	// whole send, because a processor the host serves owns delivery; falling
	// through would also enqueue the event locally and the document would see
	// the act twice.
	{
		hostParams := sce.WireParamsFromTypedParams(parts)
		hostRequest := sce.HostSendRequest{
			ProcessorType: "x-sce-host",
			EventName:     "judge.begin",
			Target:        "",
			Content:       "",
			Params:        hostParams,
			SendID:        "__send_3",
		}
		_, hostServed := engine.PerformHostSend(hostRequest)
		// W3C SCXML 6.2: a declared type with no handler registered is, from
		// the document's side, a processor the platform does not support — the
		// act it asked for was performed by nobody. Same event as an
		// undeclared type, so a wiring mistake cannot read as success.
		if !hostServed && !engine.HasEventProcessor("x-sce-host") {
			errEvt := sce.NewPlatformError(AiLoopEventErrorExecution, "<send type='x-sce-host'> names a processor the host declared but never registered")
			errEvt.Metadata.SendID = "__send_3"
			engine.Raise(errEvt)
		}
	}
	}

		}
	case AiLoopStatePaused:
		//line ai_loop.scxml:423
		// W3C SCXML 3.8: onentry block 0 (break on error stops subsequent actions)
		for actionBlock0 := 0; actionBlock0 < 1; actionBlock0++ {
			_ = actionBlock0

	// W3C SCXML 6.2: send id="__send_8"
	{
		eventDataStr := ""
		_ = eventDataStr
	// §scxml-6.2.5: "x-sce-host" is served by the host, which
	// declared it to this build. Dispatch rather than refuse — and take the
	// whole send, because a processor the host serves owns delivery; falling
	// through would also enqueue the event locally and the document would see
	// the act twice.
	{
		hostParams := map[string][]string{}
		hostRequest := sce.HostSendRequest{
			ProcessorType: "x-sce-host",
			EventName:     "notify.human",
			Target:        "",
			Content:       "",
			Params:        hostParams,
			SendID:        "__send_8",
		}
		_, hostServed := engine.PerformHostSend(hostRequest)
		// W3C SCXML 6.2: a declared type with no handler registered is, from
		// the document's side, a processor the platform does not support — the
		// act it asked for was performed by nobody. Same event as an
		// undeclared type, so a wiring mistake cannot read as success.
		if !hostServed && !engine.HasEventProcessor("x-sce-host") {
			errEvt := sce.NewPlatformError(AiLoopEventErrorExecution, "<send type='x-sce-host'> names a processor the host declared but never registered")
			errEvt.Metadata.SendID = "__send_8"
			engine.Raise(errEvt)
		}
	}
	}

		}
	case AiLoopStatePriming:
		//line ai_loop.scxml:263
		// W3C SCXML 3.8: onentry block 0 (break on error stops subsequent actions)
		for actionBlock0 := 0; actionBlock0 < 1; actionBlock0++ {
			_ = actionBlock0

	// W3C SCXML 6.2: send id="__send_0"
	// W3C SCXML 6.2: Evaluate <param>/namelist expressions at send time
	{
		p.ensureScriptEngine()
		se := p.ScriptEngine
		// W3C SCXML 6.2 / test178: a name may repeat and every value must be
		// delivered, so this is an ordered list rather than a map. The typed
		// value is kept rather than its text — a receiver reading
		// `_event.data.value === 42` finds the string "42" unequal.
		parts := make([]sce.EventDataParam, 0)
		if paramVal, paramErr := se.EvaluateExpression(p.SessionID, `start_prompt`); paramErr == nil {
			parts = append(parts, sce.EventDataParam{Name: "text", Value: paramVal})
		} else {
			engine.Raise(sce.NewPlatformError(AiLoopEventErrorExecution, "<send> <param name='text'> expr failed to evaluate"))
		}
		eventDataStr := sce.BuildJSONFromTypedParams(parts)
		_ = eventDataStr
	// §scxml-6.2.5: "x-sce-host" is served by the host, which
	// declared it to this build. Dispatch rather than refuse — and take the
	// whole send, because a processor the host serves owns delivery; falling
	// through would also enqueue the event locally and the document would see
	// the act twice.
	{
		hostParams := sce.WireParamsFromTypedParams(parts)
		hostRequest := sce.HostSendRequest{
			ProcessorType: "x-sce-host",
			EventName:     "prompt.start",
			Target:        "",
			Content:       "",
			Params:        hostParams,
			SendID:        "__send_0",
		}
		_, hostServed := engine.PerformHostSend(hostRequest)
		// W3C SCXML 6.2: a declared type with no handler registered is, from
		// the document's side, a processor the platform does not support — the
		// act it asked for was performed by nobody. Same event as an
		// undeclared type, so a wiring mistake cannot read as success.
		if !hostServed && !engine.HasEventProcessor("x-sce-host") {
			errEvt := sce.NewPlatformError(AiLoopEventErrorExecution, "<send type='x-sce-host'> names a processor the host declared but never registered")
			errEvt.Metadata.SendID = "__send_0"
			engine.Raise(errEvt)
		}
	}
	}

		}
	case AiLoopStateReflecting:
		//line ai_loop.scxml:346
		// W3C SCXML 3.8: onentry block 0 (break on error stops subsequent actions)
		for actionBlock0 := 0; actionBlock0 < 1; actionBlock0++ {
			_ = actionBlock0

	// W3C SCXML 5.3: <assign location="turns_since_reflect" expr="0">
	if err := p.assignVariable(`turns_since_reflect`, `0`); err != nil {
		engine.Raise(sce.NewPlatformError(AiLoopEventErrorExecution, "<assign> to 'turns_since_reflect' failed"))
		break
	}


	// W3C SCXML 6.2: send id="__send_5"
	{
		eventDataStr := ""
		_ = eventDataStr
	// §scxml-6.2.5: "x-sce-host" is served by the host, which
	// declared it to this build. Dispatch rather than refuse — and take the
	// whole send, because a processor the host serves owns delivery; falling
	// through would also enqueue the event locally and the document would see
	// the act twice.
	{
		hostParams := map[string][]string{}
		hostRequest := sce.HostSendRequest{
			ProcessorType: "x-sce-host",
			EventName:     "reflect.begin",
			Target:        "",
			Content:       "",
			Params:        hostParams,
			SendID:        "__send_5",
		}
		_, hostServed := engine.PerformHostSend(hostRequest)
		// W3C SCXML 6.2: a declared type with no handler registered is, from
		// the document's side, a processor the platform does not support — the
		// act it asked for was performed by nobody. Same event as an
		// undeclared type, so a wiring mistake cannot read as success.
		if !hostServed && !engine.HasEventProcessor("x-sce-host") {
			errEvt := sce.NewPlatformError(AiLoopEventErrorExecution, "<send type='x-sce-host'> names a processor the host declared but never registered")
			errEvt.Metadata.SendID = "__send_5"
			engine.Raise(errEvt)
		}
	}
	}

		}
	case AiLoopStateReported:
		//line ai_loop.scxml:398
		// W3C SCXML 3.8: onentry block 0 (break on error stops subsequent actions)
		for actionBlock0 := 0; actionBlock0 < 1; actionBlock0++ {
			_ = actionBlock0

	engine.Raise(sce.NewEventWithMetadata(AiLoopEventRunConverged))

		}
		// W3C SCXML 3.7: Final state reached — raise done.state.running
		engine.Raise(sce.NewEventWithMetadata(AiLoopEventDoneStateRunning))
	case AiLoopStateRestarting:
		//line ai_loop.scxml:366
		// W3C SCXML 3.8: onentry block 0 (break on error stops subsequent actions)
		for actionBlock0 := 0; actionBlock0 < 1; actionBlock0++ {
			_ = actionBlock0

	// W3C SCXML 5.3: <assign location="restarts" expr="restarts + 1">
	if err := p.assignVariable(`restarts`, `_scxml_add(restarts, 1)`); err != nil {
		engine.Raise(sce.NewPlatformError(AiLoopEventErrorExecution, "<assign> to 'restarts' failed"))
		break
	}


	// W3C SCXML 6.2: send id="__send_6"
	{
		eventDataStr := ""
		_ = eventDataStr
	// §scxml-6.2.5: "x-sce-host" is served by the host, which
	// declared it to this build. Dispatch rather than refuse — and take the
	// whole send, because a processor the host serves owns delivery; falling
	// through would also enqueue the event locally and the document would see
	// the act twice.
	{
		hostParams := map[string][]string{}
		hostRequest := sce.HostSendRequest{
			ProcessorType: "x-sce-host",
			EventName:     "session.replace",
			Target:        "",
			Content:       "",
			Params:        hostParams,
			SendID:        "__send_6",
		}
		_, hostServed := engine.PerformHostSend(hostRequest)
		// W3C SCXML 6.2: a declared type with no handler registered is, from
		// the document's side, a processor the platform does not support — the
		// act it asked for was performed by nobody. Same event as an
		// undeclared type, so a wiring mistake cannot read as success.
		if !hostServed && !engine.HasEventProcessor("x-sce-host") {
			errEvt := sce.NewPlatformError(AiLoopEventErrorExecution, "<send type='x-sce-host'> names a processor the host declared but never registered")
			errEvt.Metadata.SendID = "__send_6"
			engine.Raise(errEvt)
		}
	}
	}

		}
	case AiLoopStateScreening:
		//line ai_loop.scxml:299
		// W3C SCXML 3.8: onentry block 0 (break on error stops subsequent actions)
		for actionBlock0 := 0; actionBlock0 < 1; actionBlock0++ {
			_ = actionBlock0

	// W3C SCXML 5.3: <assign location="screened" expr="screened + 1">
	if err := p.assignVariable(`screened`, `_scxml_add(screened, 1)`); err != nil {
		engine.Raise(sce.NewPlatformError(AiLoopEventErrorExecution, "<assign> to 'screened' failed"))
		break
	}


	// W3C SCXML 6.2: send id="__send_1"
	{
		eventDataStr := ""
		_ = eventDataStr
	// §scxml-6.2.5: "x-sce-host" is served by the host, which
	// declared it to this build. Dispatch rather than refuse — and take the
	// whole send, because a processor the host serves owns delivery; falling
	// through would also enqueue the event locally and the document would see
	// the act twice.
	{
		hostParams := map[string][]string{}
		hostRequest := sce.HostSendRequest{
			ProcessorType: "x-sce-host",
			EventName:     "screen.begin",
			Target:        "",
			Content:       "",
			Params:        hostParams,
			SendID:        "__send_1",
		}
		_, hostServed := engine.PerformHostSend(hostRequest)
		// W3C SCXML 6.2: a declared type with no handler registered is, from
		// the document's side, a processor the platform does not support — the
		// act it asked for was performed by nobody. Same event as an
		// undeclared type, so a wiring mistake cannot read as success.
		if !hostServed && !engine.HasEventProcessor("x-sce-host") {
			errEvt := sce.NewPlatformError(AiLoopEventErrorExecution, "<send type='x-sce-host'> names a processor the host declared but never registered")
			errEvt.Metadata.SendID = "__send_1"
			engine.Raise(errEvt)
		}
	}
	}

		}
	case AiLoopStateSpent:
		//line ai_loop.scxml:501
		// W3C SCXML 3.8: onentry block 0 (break on error stops subsequent actions)
		for actionBlock0 := 0; actionBlock0 < 1; actionBlock0++ {
			_ = actionBlock0

	engine.Raise(sce.NewEventWithMetadata(AiLoopEventRunExhausted))

		}
	case AiLoopStateStuck:
		//line ai_loop.scxml:406
		// W3C SCXML 3.8: onentry block 0 (break on error stops subsequent actions)
		for actionBlock0 := 0; actionBlock0 < 1; actionBlock0++ {
			_ = actionBlock0

	engine.Raise(sce.NewEventWithMetadata(AiLoopEventRunExhausted))

		}
		// W3C SCXML 3.7: Final state reached — raise done.state.running
		engine.Raise(sce.NewEventWithMetadata(AiLoopEventDoneStateRunning))
	default:
		// No entry actions
	}

	// W3C SCXML 3.4: If entering parallel state, enter all child regions
	//
	// §scxml-D-addDescendantStatesToEnter: a <parallel> hands out defaults even
	// when it is only an ancestor — Appendix D's one exception to the ancestor
	// rule. The exception has its own exception: not the region the entry set is
	// already descending into. pathChild names it, and giving that one its
	// default too is what leaves two children of it active at once.
	if p.IsParallelState(state) {
		regions := p.GetParallelRegions(state)
		for _, region := range regions {
			if pathChild != nil && *pathChild == region {
				continue
			}
			p.ExecuteEntryActions(region, engine, nil)

			// W3C SCXML 3.3: If region is compound, enter initial child
			if p.IsCompoundState(region) {
				initialChild := p.GetInitialOrHistoryChild(region)
				if initialChild != region {
					p.ExecuteEntryActions(initialChild, engine, nil)
				}
			}
		}
	}

	// W3C SCXML 3.3: If entering compound state (non-parallel), enter initial child
	//
	// §scxml-D-addAncestorStatesToEnter: not when `state` is merely on the way
	// to a deeper target. The entry set already holds pathChild, and a compound
	// state holds one child at a time.
	if p.IsCompoundState(state) && !p.IsParallelState(state) && pathChild == nil {
		initialChildren := p.GetInitialChildren(state)
		if len(initialChildren) > 0 {
			// W3C SCXML 3.6: Enter through hierarchy to reach initial target(s)
			for _, target := range initialChildren {
				var chain []AiLoopState
				current := target
				for current != state {
					chain = append([]AiLoopState{current}, chain...)
					parent, hasParent := p.GetParent(current)
					if !hasParent {
						break
					}
					current = parent
				}
				// Each link but the last is an ancestor of the initial target,
				// so it takes no default of its own — the chain already names
				// the child that is entering.
				for i := range chain {
					var next *AiLoopState
					if i+1 < len(chain) {
						next = &chain[i+1]
					}
					p.ExecuteEntryActions(chain[i], engine, next)
				}
			}
		} else {
			initialChild := p.GetInitialOrHistoryChild(state)
			if initialChild != state {
				p.ExecuteEntryActions(initialChild, engine, nil)
			}
		}
	}
}

// ExecuteExitActions executes onexit actions for a state (W3C SCXML 3.9).
//line ai_loop.scxml:127
func (p *AiLoopPolicy) ExecuteExitActions(state AiLoopState, engine *sce.Engine[AiLoopState, AiLoopEvent], preTransitionActive []AiLoopState) {
	p.ensureScriptEngine()
	// W3C SCXML 3.4 + 3.13: Parallel state exit order — exit all active descendants first
	if p.IsParallelState(state) {
		var descendantsToExit []AiLoopState
		for _, s := range p.activeStates {
			if s != state && p.IsDescendantOf(s, state) {
				descendantsToExit = append(descendantsToExit, s)
			}
		}
		// W3C SCXML 3.13: Sort descendants by reverse document order (deepest first)
		sort.Slice(descendantsToExit, func(i, j int) bool {
			return p.GetDocumentOrder(descendantsToExit[i]) > p.GetDocumentOrder(descendantsToExit[j])
		})
		for _, descendant := range descendantsToExit {
			p.ExecuteExitActions(descendant, engine, preTransitionActive)
		}
	}
	// W3C SCXML 3.4/3.12.1: Remove state from active configuration
	p.activeStates = func() []AiLoopState {
		var result []AiLoopState
		for _, s := range p.activeStates {
			if s != state {
				result = append(result, s)
			}
		}
		return result
	}()
	switch state {
	default:
		// No exit actions
	}
	// W3C SCXML 3.11: Record history before exiting compound states
	if state == AiLoopStateRunning {
		// W3C SCXML 3.11: Shallow history — record direct children only
		p.historyWhere = sce.FilterShallowHistory(preTransitionActive, AiLoopStateRunning, func(s AiLoopState) (AiLoopState, bool) { return p.GetParent(s) })
	}
}

// ProcessTransition evaluates guards and takes a matching transition (W3C SCXML 3.13).
// Returns true if a transition was taken.
//line ai_loop.scxml:127
func (p *AiLoopPolicy) ProcessTransition(currentState *AiLoopState, event AiLoopEvent, engine *sce.Engine[AiLoopState, AiLoopEvent]) bool {
	// W3C SCXML 5.10: Bind _event system variable for guard evaluation
	if event != AiLoopEventNull {
		// §scxml-B-2-8-1: the rung the payload got, handed to the engine
		// rather than dropped. This is the only frame that has both the
		// reading and the event it belongs to.
		engine.NotePayloadReading(event, p.setCurrentEvent(p.GetEventName(event)))
	}

	// W3C SCXML 3.4 + 3.12 + Appendix D: Parallel state transition handling
	if event == AiLoopEventNull {
		// W3C SCXML Appendix D: Eventless transitions - collect from all active states
		var enabledTransitions []transitionInfo
		statesToCheck := make([]AiLoopState, len(p.activeStates))
		copy(statesToCheck, p.activeStates)

		// Sort by document order for consistent processing
		sort.Slice(statesToCheck, func(i, j int) bool {
			return p.GetDocumentOrder(statesToCheck[i]) < p.GetDocumentOrder(statesToCheck[j])
		})

		for _, activeState := range statesToCheck {
			// W3C SCXML 3.13: Eventless transitions do NOT bubble to parent states
			if trans := p.tryCollectTransition(activeState, event, engine); trans != nil {
				enabledTransitions = append(enabledTransitions, *trans)
			}
		}

		// Appendix D removeConflictingTransitions: Remove conflicting transitions
		if len(enabledTransitions) > 0 {
			enabledTransitions = p.selectOptimalTransitions(enabledTransitions)
		}

		// W3C SCXML Appendix D Steps 2-5: Execute as atomic microstep
		if len(enabledTransitions) > 0 {
			p.executeMicrostep(enabledTransitions, currentState, engine)
			return true
		}
	} else {
		// W3C SCXML Appendix D: External events - collect from active leaf states
		var enabledTransitions []transitionInfo

		for _, activeState := range p.activeStates {
			isNonAtomic := p.IsCompoundState(activeState) || p.IsParallelState(activeState)

			// W3C SCXML 3.13: Check if this is a done.state event
			eventName := p.GetEventName(event)
			isDoneStateEvent := event != AiLoopEventNull && strings.HasPrefix(eventName, "done.state.")

			// Skip non-atomic states UNLESS processing done.state event
			if isNonAtomic && !isDoneStateEvent {
				continue
			}

			// W3C SCXML 3.12: Hierarchical event bubbling
			checkState := activeState
			for {
				if trans := p.tryCollectTransition(checkState, event, engine); trans != nil {
					enabledTransitions = append(enabledTransitions, *trans)
					break
				}
				parent, hasParent := p.GetParent(checkState)
				if !hasParent {
					break
				}
				checkState = parent
			}
		}

		// W3C SCXML 3.13: Deduplicate transitions from multiple descendants
		seen := make(map[[2]int]bool)
		var deduped []transitionInfo
		for _, t := range enabledTransitions {
			key := [2]int{int(t.source), t.transitionIndex}
			if !seen[key] {
				seen[key] = true
				deduped = append(deduped, t)
			}
		}
		enabledTransitions = deduped

		// Appendix D removeConflictingTransitions: Remove conflicting transitions
		if len(enabledTransitions) > 0 {
			enabledTransitions = p.selectOptimalTransitions(enabledTransitions)
		}

		// W3C SCXML Appendix D Steps 2-5: Execute as atomic microstep
		if len(enabledTransitions) > 0 {
			p.executeMicrostep(enabledTransitions, currentState, engine)
			return true
		}
	}

	return false
}

// tryCollectTransition checks if a state has a matching transition and returns it as transitionInfo.
//line ai_loop.scxml:127
func (p *AiLoopPolicy) tryCollectTransition(checkState AiLoopState, event AiLoopEvent, engine *sce.Engine[AiLoopState, AiLoopEvent]) *transitionInfo {
	switch checkState {
	case AiLoopStateAlive:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventSessionLost {
			return &transitionInfo{
				source:          AiLoopStateAlive,
				target:          AiLoopStateRebuilding,
				transitionIndex: 0,
				hasActions:      false,
				isInternal:      false,
				isTargetless:    false,
			}
		}
	case AiLoopStateClosing:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventTurnDone {
			return &transitionInfo{
				source:          AiLoopStateClosing,
				target:          AiLoopStateReported,
				transitionIndex: 0,
				hasActions:      false,
				isInternal:      false,
				isTargetless:    false,
			}
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventTurnBlocked {
			return &transitionInfo{
				source:          AiLoopStateClosing,
				target:          AiLoopStateScreening,
				transitionIndex: 1,
				hasActions:      false,
				isInternal:      false,
				isTargetless:    false,
			}
		}
	case AiLoopStateDrive:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventHold {
			return &transitionInfo{
				source:          AiLoopStateDrive,
				target:          AiLoopStatePaused,
				transitionIndex: 0,
				hasActions:      false,
				isInternal:      true,
				isTargetless:    false,
			}
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventTurnInterrupted {
			return &transitionInfo{
				source:          AiLoopStateDrive,
				target:          AiLoopStatePaused,
				transitionIndex: 1,
				hasActions:      false,
				isInternal:      true,
				isTargetless:    false,
			}
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventSessionLost {
			return &transitionInfo{
				source:          AiLoopStateDrive,
				target:          AiLoopStateRestarting,
				transitionIndex: 2,
				hasActions:      false,
				isInternal:      true,
				isTargetless:    false,
			}
		}
	case AiLoopStateJudging:
		// W3C SCXML 5.9.3: Precomputed prefix match for "judge"
		if event == AiLoopEventJudge || event == AiLoopEventJudgeBegin {
			if p.evaluateGuard(`_scxml_truthy(_event.data.done)`, engine) {
			return &transitionInfo{
				source:          AiLoopStateJudging,
				target:          AiLoopStateClosing,
				transitionIndex: 0,
				hasActions:      false,
				isInternal:      false,
				isTargetless:    false,
			}
			}
		}
		// W3C SCXML 5.9.3: Precomputed prefix match for "judge"
		if event == AiLoopEventJudge || event == AiLoopEventJudgeBegin {
			if p.evaluateGuard(`(turns_since_reflect >= reflect_every)`, engine) {
			return &transitionInfo{
				source:          AiLoopStateJudging,
				target:          AiLoopStateReflecting,
				transitionIndex: 1,
				hasActions:      false,
				isInternal:      false,
				isTargetless:    false,
			}
			}
		}
		// W3C SCXML 5.9.3: Precomputed prefix match for "judge"
		if event == AiLoopEventJudge || event == AiLoopEventJudgeBegin {
			return &transitionInfo{
				source:          AiLoopStateJudging,
				target:          AiLoopStateWorking,
				transitionIndex: 2,
				hasActions:      true,
				isInternal:      false,
				isTargetless:    false,
			}
		}
	case AiLoopStatePaused:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventTurnDone {
			return &transitionInfo{
				source:          AiLoopStatePaused,
				target:          AiLoopStateJudging,
				transitionIndex: 0,
				hasActions:      true,
				isInternal:      false,
				isTargetless:    false,
			}
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventTurnInterrupted {
			return &transitionInfo{
				source:          AiLoopStatePaused,
				target:          AiLoopStatePaused,
				transitionIndex: 1,
				hasActions:      false,
				isInternal:      false,
				isTargetless:    false,
			}
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventResume {
			return &transitionInfo{
				source:          AiLoopStatePaused,
				target:          p.resolveHistoryTargetWhere(),
				transitionIndex: 2,
				hasActions:      false,
				isInternal:      false,
				isTargetless:    false,
			}
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventUnattended {
			return &transitionInfo{
				source:          AiLoopStatePaused,
				target:          AiLoopStateAbandoned,
				transitionIndex: 3,
				hasActions:      false,
				isInternal:      false,
				isTargetless:    false,
			}
		}
	case AiLoopStatePriming:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventPromptSent {
			return &transitionInfo{
				source:          AiLoopStatePriming,
				target:          AiLoopStateWorking,
				transitionIndex: 0,
				hasActions:      false,
				isInternal:      false,
				isTargetless:    false,
			}
		}
	case AiLoopStateRebuilding:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventSessionReady {
			return &transitionInfo{
				source:          AiLoopStateRebuilding,
				target:          AiLoopStateAlive,
				transitionIndex: 0,
				hasActions:      false,
				isInternal:      false,
				isTargetless:    false,
			}
		}
	case AiLoopStateReflecting:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventReflectApplied {
			return &transitionInfo{
				source:          AiLoopStateReflecting,
				target:          AiLoopStateRestarting,
				transitionIndex: 0,
				hasActions:      true,
				isInternal:      false,
				isTargetless:    false,
			}
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventReflectNone {
			return &transitionInfo{
				source:          AiLoopStateReflecting,
				target:          AiLoopStateWorking,
				transitionIndex: 1,
				hasActions:      true,
				isInternal:      false,
				isTargetless:    false,
			}
		}
	case AiLoopStateRestarting:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventSessionReady {
			if p.evaluateGuard(`(restarts > max_restarts)`, engine) {
			return &transitionInfo{
				source:          AiLoopStateRestarting,
				target:          AiLoopStateStuck,
				transitionIndex: 0,
				hasActions:      false,
				isInternal:      false,
				isTargetless:    false,
			}
			}
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventSessionReady {
			return &transitionInfo{
				source:          AiLoopStateRestarting,
				target:          AiLoopStatePriming,
				transitionIndex: 1,
				hasActions:      false,
				isInternal:      false,
				isTargetless:    false,
			}
		}
	case AiLoopStateRun:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventRunConverged {
			return &transitionInfo{
				source:          AiLoopStateRun,
				target:          AiLoopStateConverged,
				transitionIndex: 0,
				hasActions:      false,
				isInternal:      false,
				isTargetless:    false,
			}
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventRunExhausted {
			return &transitionInfo{
				source:          AiLoopStateRun,
				target:          AiLoopStateExhausted,
				transitionIndex: 1,
				hasActions:      false,
				isInternal:      false,
				isTargetless:    false,
			}
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventRunBlocked {
			return &transitionInfo{
				source:          AiLoopStateRun,
				target:          AiLoopStateBlocked,
				transitionIndex: 2,
				hasActions:      false,
				isInternal:      false,
				isTargetless:    false,
			}
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventFail {
			return &transitionInfo{
				source:          AiLoopStateRun,
				target:          AiLoopStateFailed,
				transitionIndex: 3,
				hasActions:      false,
				isInternal:      false,
				isTargetless:    false,
			}
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventCancel {
			return &transitionInfo{
				source:          AiLoopStateRun,
				target:          AiLoopStateCancelled,
				transitionIndex: 4,
				hasActions:      false,
				isInternal:      false,
				isTargetless:    false,
			}
		}
	case AiLoopStateScreening:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventScreenMatched {
			return &transitionInfo{
				source:          AiLoopStateScreening,
				target:          AiLoopStateWorking,
				transitionIndex: 0,
				hasActions:      false,
				isInternal:      false,
				isTargetless:    false,
			}
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventScreenNone {
			return &transitionInfo{
				source:          AiLoopStateScreening,
				target:          AiLoopStatePaused,
				transitionIndex: 1,
				hasActions:      false,
				isInternal:      false,
				isTargetless:    false,
			}
		}
	case AiLoopStateWithin:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventTurnDone {
			if p.evaluateGuard(`(_scxml_add(turns, 1) >= max_turns)`, engine) {
			return &transitionInfo{
				source:          AiLoopStateWithin,
				target:          AiLoopStateSpent,
				transitionIndex: 0,
				hasActions:      true,
				isInternal:      false,
				isTargetless:    false,
			}
			}
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventTurnDone {
			return &transitionInfo{
				source:          AiLoopStateWithin,
				target:          AiLoopStateWithin,
				transitionIndex: 1,
				hasActions:      true,
				isInternal:      false,
				isTargetless:    false,
			}
		}
	case AiLoopStateWorking:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventTurnDone {
			return &transitionInfo{
				source:          AiLoopStateWorking,
				target:          AiLoopStateJudging,
				transitionIndex: 0,
				hasActions:      true,
				isInternal:      false,
				isTargetless:    false,
			}
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventTurnBlocked {
			return &transitionInfo{
				source:          AiLoopStateWorking,
				target:          AiLoopStateScreening,
				transitionIndex: 1,
				hasActions:      false,
				isInternal:      false,
				isTargetless:    false,
			}
		}
	}
	return nil
}

// tryTransitionInState checks transitions for a single state.
//line ai_loop.scxml:127
func (p *AiLoopPolicy) tryTransitionInState(checkState AiLoopState, event AiLoopEvent, currentState *AiLoopState, engine *sce.Engine[AiLoopState, AiLoopEvent]) bool {
	switch checkState {
	case AiLoopStateAlive:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventSessionLost {
			*currentState = AiLoopStateRebuilding
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AiLoopStateAlive
			p.lastTransitionIndex = 0
			p.hasTransitionActions = false
			return true
		}
	case AiLoopStateClosing:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventTurnDone {
			*currentState = AiLoopStateReported
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AiLoopStateClosing
			p.lastTransitionIndex = 0
			p.hasTransitionActions = false
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventTurnBlocked {
			*currentState = AiLoopStateScreening
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AiLoopStateClosing
			p.lastTransitionIndex = 1
			p.hasTransitionActions = false
			return true
		}
	case AiLoopStateDrive:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventHold {
			*currentState = AiLoopStatePaused
			p.lastTransitionIsInternal = true
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AiLoopStateDrive
			p.lastTransitionIndex = 0
			p.hasTransitionActions = false
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventTurnInterrupted {
			*currentState = AiLoopStatePaused
			p.lastTransitionIsInternal = true
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AiLoopStateDrive
			p.lastTransitionIndex = 1
			p.hasTransitionActions = false
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventSessionLost {
			*currentState = AiLoopStateRestarting
			p.lastTransitionIsInternal = true
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AiLoopStateDrive
			p.lastTransitionIndex = 2
			p.hasTransitionActions = false
			return true
		}
	case AiLoopStateJudging:
		// W3C SCXML 5.9.3: Precomputed prefix match for "judge"
		if event == AiLoopEventJudge || event == AiLoopEventJudgeBegin {
			if p.evaluateGuard(`_scxml_truthy(_event.data.done)`, engine) {
			*currentState = AiLoopStateClosing
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AiLoopStateJudging
			p.lastTransitionIndex = 0
			p.hasTransitionActions = false
			return true
			}
		}
		// W3C SCXML 5.9.3: Precomputed prefix match for "judge"
		if event == AiLoopEventJudge || event == AiLoopEventJudgeBegin {
			if p.evaluateGuard(`(turns_since_reflect >= reflect_every)`, engine) {
			*currentState = AiLoopStateReflecting
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AiLoopStateJudging
			p.lastTransitionIndex = 1
			p.hasTransitionActions = false
			return true
			}
		}
		// W3C SCXML 5.9.3: Precomputed prefix match for "judge"
		if event == AiLoopEventJudge || event == AiLoopEventJudgeBegin {
			*currentState = AiLoopStateWorking
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AiLoopStateJudging
			p.lastTransitionIndex = 2
			p.hasTransitionActions = true
			return true
		}
	case AiLoopStatePaused:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventTurnDone {
			*currentState = AiLoopStateJudging
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AiLoopStatePaused
			p.lastTransitionIndex = 0
			p.hasTransitionActions = true
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventTurnInterrupted {
			*currentState = AiLoopStatePaused
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AiLoopStatePaused
			p.lastTransitionIndex = 1
			p.hasTransitionActions = false
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventResume {
			// W3C SCXML 3.11: History transition — resolve at runtime
			*currentState = p.resolveHistoryTargetWhere()
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AiLoopStatePaused
			p.lastTransitionIndex = 2
			p.hasTransitionActions = false
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventUnattended {
			*currentState = AiLoopStateAbandoned
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AiLoopStatePaused
			p.lastTransitionIndex = 3
			p.hasTransitionActions = false
			return true
		}
	case AiLoopStatePriming:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventPromptSent {
			*currentState = AiLoopStateWorking
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AiLoopStatePriming
			p.lastTransitionIndex = 0
			p.hasTransitionActions = false
			return true
		}
	case AiLoopStateRebuilding:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventSessionReady {
			*currentState = AiLoopStateAlive
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AiLoopStateRebuilding
			p.lastTransitionIndex = 0
			p.hasTransitionActions = false
			return true
		}
	case AiLoopStateReflecting:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventReflectApplied {
			*currentState = AiLoopStateRestarting
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AiLoopStateReflecting
			p.lastTransitionIndex = 0
			p.hasTransitionActions = true
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventReflectNone {
			*currentState = AiLoopStateWorking
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AiLoopStateReflecting
			p.lastTransitionIndex = 1
			p.hasTransitionActions = true
			return true
		}
	case AiLoopStateRestarting:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventSessionReady {
			if p.evaluateGuard(`(restarts > max_restarts)`, engine) {
			*currentState = AiLoopStateStuck
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AiLoopStateRestarting
			p.lastTransitionIndex = 0
			p.hasTransitionActions = false
			return true
			}
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventSessionReady {
			*currentState = AiLoopStatePriming
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AiLoopStateRestarting
			p.lastTransitionIndex = 1
			p.hasTransitionActions = false
			return true
		}
	case AiLoopStateRun:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventRunConverged {
			*currentState = AiLoopStateConverged
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AiLoopStateRun
			p.lastTransitionIndex = 0
			p.hasTransitionActions = false
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventRunExhausted {
			*currentState = AiLoopStateExhausted
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AiLoopStateRun
			p.lastTransitionIndex = 1
			p.hasTransitionActions = false
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventRunBlocked {
			*currentState = AiLoopStateBlocked
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AiLoopStateRun
			p.lastTransitionIndex = 2
			p.hasTransitionActions = false
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventFail {
			*currentState = AiLoopStateFailed
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AiLoopStateRun
			p.lastTransitionIndex = 3
			p.hasTransitionActions = false
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventCancel {
			*currentState = AiLoopStateCancelled
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AiLoopStateRun
			p.lastTransitionIndex = 4
			p.hasTransitionActions = false
			return true
		}
	case AiLoopStateScreening:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventScreenMatched {
			*currentState = AiLoopStateWorking
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AiLoopStateScreening
			p.lastTransitionIndex = 0
			p.hasTransitionActions = false
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventScreenNone {
			*currentState = AiLoopStatePaused
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AiLoopStateScreening
			p.lastTransitionIndex = 1
			p.hasTransitionActions = false
			return true
		}
	case AiLoopStateWithin:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventTurnDone {
			if p.evaluateGuard(`(_scxml_add(turns, 1) >= max_turns)`, engine) {
			*currentState = AiLoopStateSpent
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AiLoopStateWithin
			p.lastTransitionIndex = 0
			p.hasTransitionActions = true
			return true
			}
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventTurnDone {
			*currentState = AiLoopStateWithin
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AiLoopStateWithin
			p.lastTransitionIndex = 1
			p.hasTransitionActions = true
			return true
		}
	case AiLoopStateWorking:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventTurnDone {
			*currentState = AiLoopStateJudging
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AiLoopStateWorking
			p.lastTransitionIndex = 0
			p.hasTransitionActions = true
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AiLoopEventTurnBlocked {
			*currentState = AiLoopStateScreening
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AiLoopStateWorking
			p.lastTransitionIndex = 1
			p.hasTransitionActions = false
			return true
		}
	}
	return false
}

// ExecuteTransitionActions executes actions for the last taken transition (W3C SCXML 3.13).
//line ai_loop.scxml:127
func (p *AiLoopPolicy) ExecuteTransitionActions(engine *sce.Engine[AiLoopState, AiLoopEvent]) {
	p.ensureScriptEngine()
	if !p.hasTransitionActions {
		return
	}
	source := p.lastTransitionSourceState
	idx := p.lastTransitionIndex
	if source == AiLoopStateJudging && idx == 2 {
		//line ai_loop.scxml:334

	// W3C SCXML 6.2: send id="__send_2"
	// W3C SCXML 6.2: Evaluate <param>/namelist expressions at send time
	{
		p.ensureScriptEngine()
		se := p.ScriptEngine
		// W3C SCXML 6.2 / test178: a name may repeat and every value must be
		// delivered, so this is an ordered list rather than a map. The typed
		// value is kept rather than its text — a receiver reading
		// `_event.data.value === 42` finds the string "42" unequal.
		parts := make([]sce.EventDataParam, 0)
		if paramVal, paramErr := se.EvaluateExpression(p.SessionID, `turn_prompt`); paramErr == nil {
			parts = append(parts, sce.EventDataParam{Name: "text", Value: paramVal})
		} else {
			engine.Raise(sce.NewPlatformError(AiLoopEventErrorExecution, "<send> <param name='text'> expr failed to evaluate"))
		}
		eventDataStr := sce.BuildJSONFromTypedParams(parts)
		_ = eventDataStr
	// §scxml-6.2.5: "x-sce-host" is served by the host, which
	// declared it to this build. Dispatch rather than refuse — and take the
	// whole send, because a processor the host serves owns delivery; falling
	// through would also enqueue the event locally and the document would see
	// the act twice.
	{
		hostParams := sce.WireParamsFromTypedParams(parts)
		hostRequest := sce.HostSendRequest{
			ProcessorType: "x-sce-host",
			EventName:     "prompt.turn",
			Target:        "",
			Content:       "",
			Params:        hostParams,
			SendID:        "__send_2",
		}
		_, hostServed := engine.PerformHostSend(hostRequest)
		// W3C SCXML 6.2: a declared type with no handler registered is, from
		// the document's side, a processor the platform does not support — the
		// act it asked for was performed by nobody. Same event as an
		// undeclared type, so a wiring mistake cannot read as success.
		if !hostServed && !engine.HasEventProcessor("x-sce-host") {
			errEvt := sce.NewPlatformError(AiLoopEventErrorExecution, "<send type='x-sce-host'> names a processor the host declared but never registered")
			errEvt.Metadata.SendID = "__send_2"
			engine.Raise(errEvt)
		}
	}
	}

		return
	}
	if source == AiLoopStatePaused && idx == 0 {
		//line ai_loop.scxml:440

	// W3C SCXML 5.3: <assign location="turns_since_reflect" expr="turns_since_reflect + 1">
	if err := p.assignVariable(`turns_since_reflect`, `_scxml_add(turns_since_reflect, 1)`); err != nil {
		engine.Raise(sce.NewPlatformError(AiLoopEventErrorExecution, "<assign> to 'turns_since_reflect' failed"))
	}

		return
	}
	if source == AiLoopStateReflecting && idx == 0 {
		//line ai_loop.scxml:351

	// W3C SCXML 5.3: <assign location="start_prompt" expr="_event.data.start_prompt">
	if err := p.assignVariable(`start_prompt`, `_event.data.start_prompt`); err != nil {
		engine.Raise(sce.NewPlatformError(AiLoopEventErrorExecution, "<assign> to 'start_prompt' failed"))
	}


	// W3C SCXML 5.3: <assign location="turn_prompt" expr="_event.data.turn_prompt">
	if err := p.assignVariable(`turn_prompt`, `_event.data.turn_prompt`); err != nil {
		engine.Raise(sce.NewPlatformError(AiLoopEventErrorExecution, "<assign> to 'turn_prompt' failed"))
	}


	// W3C SCXML 5.3: <assign location="milestone" expr="_event.data.milestone">
	if err := p.assignVariable(`milestone`, `_event.data.milestone`); err != nil {
		engine.Raise(sce.NewPlatformError(AiLoopEventErrorExecution, "<assign> to 'milestone' failed"))
	}

		return
	}
	if source == AiLoopStateReflecting && idx == 1 {
		//line ai_loop.scxml:357

	// W3C SCXML 6.2: send id="__send_4"
	// W3C SCXML 6.2: Evaluate <param>/namelist expressions at send time
	{
		p.ensureScriptEngine()
		se := p.ScriptEngine
		// W3C SCXML 6.2 / test178: a name may repeat and every value must be
		// delivered, so this is an ordered list rather than a map. The typed
		// value is kept rather than its text — a receiver reading
		// `_event.data.value === 42` finds the string "42" unequal.
		parts := make([]sce.EventDataParam, 0)
		if paramVal, paramErr := se.EvaluateExpression(p.SessionID, `turn_prompt`); paramErr == nil {
			parts = append(parts, sce.EventDataParam{Name: "text", Value: paramVal})
		} else {
			engine.Raise(sce.NewPlatformError(AiLoopEventErrorExecution, "<send> <param name='text'> expr failed to evaluate"))
		}
		eventDataStr := sce.BuildJSONFromTypedParams(parts)
		_ = eventDataStr
	// §scxml-6.2.5: "x-sce-host" is served by the host, which
	// declared it to this build. Dispatch rather than refuse — and take the
	// whole send, because a processor the host serves owns delivery; falling
	// through would also enqueue the event locally and the document would see
	// the act twice.
	{
		hostParams := sce.WireParamsFromTypedParams(parts)
		hostRequest := sce.HostSendRequest{
			ProcessorType: "x-sce-host",
			EventName:     "prompt.turn",
			Target:        "",
			Content:       "",
			Params:        hostParams,
			SendID:        "__send_4",
		}
		_, hostServed := engine.PerformHostSend(hostRequest)
		// W3C SCXML 6.2: a declared type with no handler registered is, from
		// the document's side, a processor the platform does not support — the
		// act it asked for was performed by nobody. Same event as an
		// undeclared type, so a wiring mistake cannot read as success.
		if !hostServed && !engine.HasEventProcessor("x-sce-host") {
			errEvt := sce.NewPlatformError(AiLoopEventErrorExecution, "<send type='x-sce-host'> names a processor the host declared but never registered")
			errEvt.Metadata.SendID = "__send_4"
			engine.Raise(errEvt)
		}
	}
	}

		return
	}
	if source == AiLoopStateWithin && idx == 0 {
		//line ai_loop.scxml:494

	// W3C SCXML 5.3: <assign location="turns" expr="turns + 1">
	if err := p.assignVariable(`turns`, `_scxml_add(turns, 1)`); err != nil {
		engine.Raise(sce.NewPlatformError(AiLoopEventErrorExecution, "<assign> to 'turns' failed"))
	}

		return
	}
	if source == AiLoopStateWithin && idx == 1 {
		//line ai_loop.scxml:497

	// W3C SCXML 5.3: <assign location="turns" expr="turns + 1">
	if err := p.assignVariable(`turns`, `_scxml_add(turns, 1)`); err != nil {
		engine.Raise(sce.NewPlatformError(AiLoopEventErrorExecution, "<assign> to 'turns' failed"))
	}

		return
	}
	if source == AiLoopStateWorking && idx == 0 {
		//line ai_loop.scxml:283

	// W3C SCXML 5.3: <assign location="turns_since_reflect" expr="turns_since_reflect + 1">
	if err := p.assignVariable(`turns_since_reflect`, `_scxml_add(turns_since_reflect, 1)`); err != nil {
		engine.Raise(sce.NewPlatformError(AiLoopEventErrorExecution, "<assign> to 'turns_since_reflect' failed"))
	}

		return
	}
}

// transitionInfo describes a transition for conflict resolution (W3C SCXML Appendix D).
type transitionInfo struct {
	source          AiLoopState
	target          AiLoopState
	transitionIndex int
	hasActions      bool
	isInternal      bool
	isTargetless    bool
}

// selectOptimalTransitions resolves conflicts between parallel transitions (Appendix D removeConflictingTransitions).
// Port of Rust remove_conflicting_transitions().
func (p *AiLoopPolicy) selectOptimalTransitions(transitions []transitionInfo) []transitionInfo {
	if len(transitions) <= 1 {
		return transitions
	}
	var filtered []transitionInfo

	for _, t1 := range transitions {
		// Appendix D selectTransitions: the enabled set is an ORDERED SET, and
		// the same transition reached from two different region leaves is ONE
		// element of it, not two. A transition written on a <parallel>, or on an
		// ancestor above one, is selected once per region by the bubbling walk —
		// W3C test 403b turns on a <parallel>-level <assign> running exactly
		// once. Selection stops at the first enabled transition of a state, so
		// within one microstep a source contributes at most one transition and
		// (source, target) identifies it.
		//
		// This is what the removed target/source criterion stood in for: a
		// targetless duplicate is spelled source -> source, so that check
		// happened to fire on it. Stated here it is the set semantics
		// themselves, independent of how a targetless transition is spelled.
		alreadySelected := false
		for _, seen := range filtered {
			if seen.source == t1.source && seen.target == t1.target {
				alreadySelected = true
				break
			}
		}
		if alreadySelected {
			continue
		}

		dominated := false
		var toRemove []int

		for idx, t2 := range filtered {
			// Appendix D removeConflictingTransitions: Check if exit sets intersect
			t1Exits := p.computeExitSetForConflict(t1)
			t2Exits := p.computeExitSetForConflict(t2)

			// Appendix D removeConflictingTransitions: two transitions conflict
			// when their EXIT SETS intersect. That is the whole test the appendix
			// states, and it is now the whole test made here.
			//
			// Three rules used to sit beside it — a target/source equality check
			// and a <parallel>-ancestor check in each direction — and none is in
			// the appendix. They stood in for an exit set this generator could
			// not compute: assembled from the source's own ancestor chain, a set
			// could not name the sibling regions a transition leaving the
			// <parallel> exits, so the intersection came back empty for
			// transitions that plainly conflict. Appendix D computeExitSet reads
			// p.activeStates now, so the intersection answers on its own.
			//
			// A transition that exits nothing still conflicts with nothing and
			// can never be preempted — that is a targetless transition, which
			// the appendix gives an empty exit set, and it is what W3C test 403c
			// means by "this transition never gets preempted, should fire twice".
			// The removed rules each read a targetless transition as a
			// self-transition on its own source, which is why they needed the
			// empty-exit-set gate and the isTargetless guards that stood here.
			hasConflict := false
			for _, s1 := range t1Exits {
				for _, s2 := range t2Exits {
					if s1 == s2 {
						hasConflict = true
						break
					}
				}
				if hasConflict {
					break
				}
			}
			if !hasConflict {
				continue
			}

			// Appendix D removeConflictingTransitions: the descendant source wins
			if p.IsDescendantOf(t1.source, t2.source) {
				toRemove = append(toRemove, idx)
			} else {
				dominated = true
				break
			}
		}

		if !dominated {
			// Remove preempted transitions in reverse order
			for i := len(toRemove) - 1; i >= 0; i-- {
				idx := toRemove[i]
				filtered = append(filtered[:idx], filtered[idx+1:]...)
			}
			filtered = append(filtered, t1)
		}
	}

	return filtered
}

// transitionDomain is Appendix D getTransitionDomain — the state every exited
// and entered state descends from. A nil answer is the <scxml> element, which
// has no State constant here; callers read it as "every active state is below".
func (p *AiLoopPolicy) transitionDomain(source, target AiLoopState, isInternal bool) *AiLoopState {
	// W3C SCXML 3.13: Internal transition to a compound descendant — the SOURCE
	// is the domain, so it stays active while its active descendants are exited.
	if isInternal &&
		p.IsCompoundState(source) && !p.IsParallelState(source) &&
		p.IsDescendantOf(target, source) && target != source {
		d := source
		return &d
	}

	// Appendix D findLCCA: walk up from source for the lowest CANDIDATE ancestor
	// that contains target. The candidates are the ones
	// isCompoundStateOrScxmlElement admits, so a <parallel> is skipped.
	//
	// If no candidate contains target (top-level siblings, or a region root's
	// external transition whose only non-candidate ancestor is the <parallel>),
	// the domain is the <scxml> element.
	current := source
	for {
		parent, hasParent := p.GetParent(current)
		if !hasParent {
			return nil
		}
		isDomainCandidate := p.IsCompoundState(parent) && !p.IsParallelState(parent)
		if isDomainCandidate && (p.IsDescendantOf(target, parent) || target == parent) {
			return &parent
		}
		current = parent
	}
}

// computeExitSetForConflict is Appendix D computeExitSet: the ACTIVE states that
// are proper descendants of the transition's domain.
//
// It reads p.activeStates, because that is what the appendix computes over.
// Walking the source's own ancestor chain instead — what stood here — names the
// same states only while no <parallel> is active below the domain: a sibling
// region descends from the domain and is not on that chain. It left this
// generator with TWO exit sets, one for conflict resolution and one for
// executeMicrostep, which now share this procedure and cannot disagree.
func (p *AiLoopPolicy) computeExitSetForConflict(t transitionInfo) []AiLoopState {
	// Appendix D computeExitSet guards the whole computation with `if t.target`:
	// a transition without one exits nothing and conflicts with nothing.
	if t.isTargetless {
		return nil
	}

	domain := p.transitionDomain(t.source, t.target, t.isInternal)

	var exitSet []AiLoopState
	for _, activeState := range p.activeStates {
		exits := true
		if domain != nil {
			// The domain itself is not exited; everything active below it is.
			exits = activeState != *domain && p.IsDescendantOf(activeState, *domain)
		}
		if exits {
			exitSet = append(exitSet, activeState)
		}
	}
	return exitSet
}

// executeMicrostep performs a complete microstep for the given transitions
// (Appendix D microstepProcedure Steps 1-5: compute exit set, exit, actions, enter).
func (p *AiLoopPolicy) executeMicrostep(transitions []transitionInfo, currentState *AiLoopState, engine *sce.Engine[AiLoopState, AiLoopEvent]) {
	if len(transitions) == 0 {
		return
	}

	// Appendix D computeExitSet Step 1-2: Compute states to exit
	var statesToExit []AiLoopState
	for _, trans := range transitions {
		if trans.isTargetless {
			continue
		}
		// Appendix D computeExitSet: the SAME procedure selectOptimalTransitions
		// intersects. A microstep that exits a different set from the one the
		// resolver judged cannot be reasoned about, and this walked the
		// configuration while computeExitSetForConflict walked source's chain.
		for _, activeState := range p.computeExitSetForConflict(trans) {
			found := false
			for _, s := range statesToExit {
				if s == activeState {
					found = true
					break
				}
			}
			if !found {
				statesToExit = append(statesToExit, activeState)
			}
		}
	}

	// Sort by reverse document order (deepest first)
	sort.Slice(statesToExit, func(i, j int) bool {
		return p.GetDocumentOrder(statesToExit[i]) > p.GetDocumentOrder(statesToExit[j])
	})

	// Snapshot active states for history recording
	activeSnapshot := make([]AiLoopState, len(p.activeStates))
	copy(activeSnapshot, p.activeStates)

	// Appendix D exitStates Step 2: Exit states
	for _, state := range statesToExit {
		p.ExecuteExitActions(state, engine, activeSnapshot)
	}

	// Appendix D executeTransitionContent Step 3: Execute transition content
	// Sort transitions by source document order
	sortedTransitions := make([]transitionInfo, len(transitions))
	copy(sortedTransitions, transitions)
	sort.Slice(sortedTransitions, func(i, j int) bool {
		return p.GetDocumentOrder(sortedTransitions[i].source) < p.GetDocumentOrder(sortedTransitions[j].source)
	})

	for _, trans := range sortedTransitions {
		if trans.hasActions {
			p.lastTransitionSourceState = trans.source
			p.lastTransitionIndex = trans.transitionIndex
			p.hasTransitionActions = true
			p.ExecuteTransitionActions(engine)
			p.hasTransitionActions = false
		}
	}

	// Appendix D enterStates Step 4-5: Enter target states
	sort.Slice(sortedTransitions, func(i, j int) bool {
		return p.GetDocumentOrder(sortedTransitions[i].target) < p.GetDocumentOrder(sortedTransitions[j].target)
	})

	for _, trans := range sortedTransitions {
		if trans.isTargetless {
			continue
		}

		target := trans.target
		*currentState = target

		// W3C SCXML 3.13: Build hierarchical entry chain from root to target
		var entryChain []AiLoopState
		{
			current := target
			for {
				entryChain = append([]AiLoopState{current}, entryChain...)
				parent, hasParent := p.GetParent(current)
				if !hasParent {
					break
				}
				current = parent
			}
		}

		// §scxml-D: every link but the last is an ANCESTOR of the target, and
		// addAncestorStatesToEnter adds an ancestor WITHOUT its default initial
		// child — the entry set already holds the next link. Only the target
		// itself goes through addDescendantStatesToEnter. Passing the next link
		// as pathChild is what expresses that, and it is also what stops a
		// <parallel> ancestor from handing a default to the very region the
		// chain is descending into.
		for chainIdx := range entryChain {
			state := entryChain[chainIdx]
			var pathChild *AiLoopState
			if chainIdx+1 < len(entryChain) {
				pathChild = &entryChain[chainIdx+1]
			}
			alreadyActive := false
			for _, as := range p.activeStates {
				if as == state {
					alreadyActive = true
					break
				}
			}
			if alreadyActive {
				// W3C SCXML 3.13: Already active - handle parallel region re-entry
				if p.IsParallelState(state) {
					regions := p.GetParallelRegions(state)
					for _, region := range regions {
						if pathChild != nil && *pathChild == region {
							continue
						}
						regionActive := false
						for _, as := range p.activeStates {
							if as == region {
								regionActive = true
								break
							}
						}
						if !regionActive {
							p.ExecuteEntryActions(region, engine, nil)
							if p.IsCompoundState(region) {
								initialChild := p.GetInitialOrHistoryChild(region)
								if initialChild != region {
									p.ExecuteEntryActions(initialChild, engine, nil)
								}
							}
						}
					}
				}
				continue
			}
			p.ExecuteEntryActions(state, engine, pathChild)
		}

		// W3C SCXML 3.4: For parallel states, maintain currentState at parallel level
		if parent, hasParent := p.GetParent(target); hasParent {
			if p.IsParallelState(parent) {
				*currentState = parent
			}
		}
	}
}
