// SCE-GENERATED — DO NOT EDIT
// source-hash: 387554cf9d8d5415c8347a9554c4bb2db1133a43787a7fb935ba3f3f9103b433


// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) 2025-2026 [Author of input SCXML file]
//
// Generated by SCE Code Generator (Go backend)
// From: integration_resources/invoke_param_error_starts_the_child/invoke_param_error_starts_the_child.scxml
//
// This generated code is MIT licensed and may be freely used in any project.
// Runtime engine dependency (sce-go-runtime): LGPL-2.1-or-later WITH LicenseRef-SCE-Linking-Exception OR LicenseRef-SCE-Commercial
// Full license: https://github.com/newmassrael/scxml-core-engine/blob/main/LICENSE
// Generated Go state machine for `invoke_param_error_starts_the_child`.
//
// Generated by SCE Code Generator (Go backend).
// Implements sce.StatePolicy with full transition logic,
// entry/exit actions, and event processing.


// SCE-MAP: invoke_param_error_starts_the_child.scxml:52 :: _machine

package invoke_param_error_starts_the_child

import (
	"fmt"
	"path/filepath"
	"strings"
	"time"

	sce "github.com/newmassrael/sce-go-runtime"
)

// Ensure imports are used
var (
	_ = fmt.Sprintf
	_ = filepath.Join
	_ = strings.HasPrefix
	_ = time.Duration(0)
)

// ======================================================================
// State type (W3C SCXML 3.3)
// ======================================================================

type InvokeParamErrorStartsTheChildState int

const (
	InvokeParamErrorStartsTheChildStateFailBrokenParamSeeded InvokeParamErrorStartsTheChildState = 0
	InvokeParamErrorStartsTheChildStateFailGoodParamLost InvokeParamErrorStartsTheChildState = 1
	InvokeParamErrorStartsTheChildStateFailInvokeNotStarted InvokeParamErrorStartsTheChildState = 2
	InvokeParamErrorStartsTheChildStateFailNoParamError InvokeParamErrorStartsTheChildState = 3
	InvokeParamErrorStartsTheChildStateParamPhase InvokeParamErrorStartsTheChildState = 4
	InvokeParamErrorStartsTheChildStatePass InvokeParamErrorStartsTheChildState = 5
)

func (s InvokeParamErrorStartsTheChildState) String() string {
	switch s {
	case InvokeParamErrorStartsTheChildStateFailBrokenParamSeeded:
		return "failBrokenParamSeeded"
	case InvokeParamErrorStartsTheChildStateFailGoodParamLost:
		return "failGoodParamLost"
	case InvokeParamErrorStartsTheChildStateFailInvokeNotStarted:
		return "failInvokeNotStarted"
	case InvokeParamErrorStartsTheChildStateFailNoParamError:
		return "failNoParamError"
	case InvokeParamErrorStartsTheChildStateParamPhase:
		return "paramPhase"
	case InvokeParamErrorStartsTheChildStatePass:
		return "pass"
	}
	return "unknown"
}

// InvokeParamErrorStartsTheChildStateFromName is the read half of the pair above — a state
// id back into the state it names (W3C SCXML 3.3).
//
// A host that persists where a machine was has to write it down as TEXT: the
// constants above are a build artefact of one binary, and the process that
// resumes is a different one. String publishes the name and this reads it back,
// which is what lets a journal survive its own record and reach
// sce.Engine.EnterAt.
//
// The second return is false for a name this document does not declare. A name
// guessed at rather than refused is how a restore reaches a configuration
// nobody recorded — and the refusal is what makes a typo in a journal a
// reported failure instead of a machine quietly somewhere else.
//
// Emitted from the same loop over the document's states as String, so the two
// age together.
func InvokeParamErrorStartsTheChildStateFromName(name string) (InvokeParamErrorStartsTheChildState, bool) {
	switch name {
	case "failBrokenParamSeeded":
		return InvokeParamErrorStartsTheChildStateFailBrokenParamSeeded, true
	case "failGoodParamLost":
		return InvokeParamErrorStartsTheChildStateFailGoodParamLost, true
	case "failInvokeNotStarted":
		return InvokeParamErrorStartsTheChildStateFailInvokeNotStarted, true
	case "failNoParamError":
		return InvokeParamErrorStartsTheChildStateFailNoParamError, true
	case "paramPhase":
		return InvokeParamErrorStartsTheChildStateParamPhase, true
	case "pass":
		return InvokeParamErrorStartsTheChildStatePass, true
	}
	var zero InvokeParamErrorStartsTheChildState
	return zero, false
}

// InvokeParamErrorStartsTheChildAllStates is every state this document declares, in the
// order the constants above are issued.
//
// Emitted from the same loop, so a walk over it is a walk over the document
// rather than over a list somebody maintained beside it — a test that spells
// its own list goes on passing when the document grows a state.
var InvokeParamErrorStartsTheChildAllStates = []InvokeParamErrorStartsTheChildState{
	InvokeParamErrorStartsTheChildStateFailBrokenParamSeeded,
	InvokeParamErrorStartsTheChildStateFailGoodParamLost,
	InvokeParamErrorStartsTheChildStateFailInvokeNotStarted,
	InvokeParamErrorStartsTheChildStateFailNoParamError,
	InvokeParamErrorStartsTheChildStateParamPhase,
	InvokeParamErrorStartsTheChildStatePass,
}

// InvokeParamErrorStartsTheChildTarget is one token of a target list, as the document wrote
// it (W3C SCXML 3.13): a state, or a <history> the engine dereferences.
type InvokeParamErrorStartsTheChildTarget = sce.EntryTarget[InvokeParamErrorStartsTheChildState, sce.HistoryID]

// ======================================================================
// Document structure (W3C SCXML 3.2-3.4, 3.10)
//
// Package-level tables, because the structure is a fact about the document
// and not about a run: the engine's Appendix D procedures read them through
// the policy methods below, and a transition's target list is handed out as
// a slice of them rather than rebuilt on every selection.
// ======================================================================

// childStatesOfInvokeParamErrorStartsTheChild is §scxml-D-getChildStates per state: its
// <state>, <parallel> and <final> children, in document order.
var childStatesOfInvokeParamErrorStartsTheChild = [6][]InvokeParamErrorStartsTheChildState{
}

// initialTargetsOfInvokeParamErrorStartsTheChild is each compound state's initial transition
// target, as written (§scxml-3.3).
var initialTargetsOfInvokeParamErrorStartsTheChild = [6][]InvokeParamErrorStartsTheChildTarget{
}

// documentInitialTargetsOfInvokeParamErrorStartsTheChild is the target of the document's own
// initial transition, as written (§scxml-3.2).
var documentInitialTargetsOfInvokeParamErrorStartsTheChild = []InvokeParamErrorStartsTheChildTarget{sce.StateTarget[InvokeParamErrorStartsTheChildState, sce.HistoryID](InvokeParamErrorStartsTheChildStateParamPhase)}

// transitionTargetsOfInvokeParamErrorStartsTheChild is each transition's target list, as
// written (§scxml-3.13), by source state and the transition's index among its
// source's own transitions. A targetless transition's entry is empty.
var transitionTargetsOfInvokeParamErrorStartsTheChild = [6][][]InvokeParamErrorStartsTheChildTarget{
	InvokeParamErrorStartsTheChildStateParamPhase: {
		1: {sce.StateTarget[InvokeParamErrorStartsTheChildState, sce.HistoryID](InvokeParamErrorStartsTheChildStateFailNoParamError)},
		2: {sce.StateTarget[InvokeParamErrorStartsTheChildState, sce.HistoryID](InvokeParamErrorStartsTheChildStateFailGoodParamLost)},
		3: {sce.StateTarget[InvokeParamErrorStartsTheChildState, sce.HistoryID](InvokeParamErrorStartsTheChildStateFailBrokenParamSeeded)},
		4: {sce.StateTarget[InvokeParamErrorStartsTheChildState, sce.HistoryID](InvokeParamErrorStartsTheChildStatePass)},
		5: {sce.StateTarget[InvokeParamErrorStartsTheChildState, sce.HistoryID](InvokeParamErrorStartsTheChildStateFailInvokeNotStarted)},
	},
}

// ======================================================================
// Event type (W3C SCXML 3.12)
// ======================================================================

type InvokeParamErrorStartsTheChildEvent int

const (
	InvokeParamErrorStartsTheChildEventCancelInvoke InvokeParamErrorStartsTheChildEvent = 0
	InvokeParamErrorStartsTheChildEventChildUp InvokeParamErrorStartsTheChildEvent = 1
	InvokeParamErrorStartsTheChildEventDoneInvoke InvokeParamErrorStartsTheChildEvent = 2
	InvokeParamErrorStartsTheChildEventErrorExecution InvokeParamErrorStartsTheChildEvent = 3
	InvokeParamErrorStartsTheChildEventTimeout InvokeParamErrorStartsTheChildEvent = 4
	// W3C SCXML 3.13: Sentinel for eventless transition dispatch
	InvokeParamErrorStartsTheChildEventNull InvokeParamErrorStartsTheChildEvent = 5
)

func (e InvokeParamErrorStartsTheChildEvent) String() string {
	switch e {
	case InvokeParamErrorStartsTheChildEventCancelInvoke:
		return "cancel.invoke"
	case InvokeParamErrorStartsTheChildEventChildUp:
		return "childUp"
	case InvokeParamErrorStartsTheChildEventDoneInvoke:
		return "done.invoke"
	case InvokeParamErrorStartsTheChildEventErrorExecution:
		return "error.execution"
	case InvokeParamErrorStartsTheChildEventTimeout:
		return "timeout"
	case InvokeParamErrorStartsTheChildEventNull:
		return ""
	}
	return "unknown"
}

// ======================================================================
// Policy struct
// ======================================================================

type InvokeParamErrorStartsTheChildPolicy struct {
	// W3C SCXML 5.10.1: External event flag
	nextEventIsExternal bool
	pendingEventName string
	pendingEventData string
	pendingEventType string
	pendingEventSendid string
	pendingEventOrigin string
	pendingEventOrigintype string
	pendingEventInvokeid string
	// W3C SCXML 5.10: Session ID
	SessionID string
	// W3C SCXML B.1: Script engine handle (per-instance, Path B+ Q1=(d) Go=interface ref)
	ScriptEngine sce.IScriptEngine
	scriptEngineInitialized bool
	// W3C SCXML C.2.3: inbound BasicHTTP endpoint serving this machine,
	// declared by the deployment before Initialize(). The address belongs to
	// whoever runs the listener, so the machine takes it from here rather than
	// guessing one. Empty means no such endpoint is deployed, and no BasicHTTP
	// entry is published in _ioprocessors.
	BasicHTTPAccessURI string
	pendingInvokes []sce.PendingInvoke[InvokeParamErrorStartsTheChildState]
	activeInvokes  map[string]*sce.ChildSession
	childInvProbe sce.ChildEngine
	pendingDoneInvokeInvProbe bool
	// W3C SCXML 6.4: Parent communication
	ParentExternalQueue chan sce.ParentEvent
	InvokeID           string
	ChildSessionID     string
	// §scxml-6.4.3: invoke param VALUES staged by the parent, applied after
	// this machine's datamodel init so they override its `<data>` defaults.
	pendingParams      map[string]interface{}
}

// NewInvokeParamErrorStartsTheChildPolicy creates a new policy with default values.
func NewInvokeParamErrorStartsTheChildPolicy() InvokeParamErrorStartsTheChildPolicy {
	return InvokeParamErrorStartsTheChildPolicy{
		pendingInvokes: make([]sce.PendingInvoke[InvokeParamErrorStartsTheChildState], 0),
		activeInvokes:  make(map[string]*sce.ChildSession),
	}
}

// SawParamError reports what the `sawParamError` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `sawParamError` was assigned a value of another type, or the engine refused.
func (p *InvokeParamErrorStartsTheChildPolicy) SawParamError() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "sawParamError")
}




// ensureScriptEngine lazily initializes the script engine session.
//
// Engine DI Parity RFC: panics if ScriptEngine was not set on the policy
// before Initialize() — mirrors Kotlin's mandatory constructor parameter
// contract. Silent no-op would otherwise turn missed injection into a
// hidden runtime regression (every <assign>/<log>/guard becomes a no-op
// and the SM walks to its default-state final without diagnostic).
func (p *InvokeParamErrorStartsTheChildPolicy) ensureScriptEngine() {
	if p.scriptEngineInitialized {
		return
	}
	engine := p.ScriptEngine
	if engine == nil {
		panic("InvokeParamErrorStartsTheChildPolicy: ScriptEngine field must be set before Initialize()")
	}
	sessionID := p.SessionID
	if sessionID == "" {
		sessionID = sce.GenerateSessionID()
		p.SessionID = sessionID
	}
	if !engine.HasSession(sessionID) {
		_ = engine.CreateSession(sessionID)
	}
	// W3C SCXML C.1.1 / C.2.3: the _ioprocessors entries come from the same
	// helper every other backend uses, so a machine reads the same entry names
	// and the same addresses whichever one runs it.
	_ = engine.SetupSystemVariables(sessionID, "invoke_param_error_starts_the_child",
		sce.BuildIoProcessors(sessionID, p.BasicHTTPAccessURI))
	p.scriptEngineInitialized = true
}

// InitializeDataModel sets up datamodel via script engine (W3C SCXML 5.3).
func (p *InvokeParamErrorStartsTheChildPolicy) InitializeDataModel(eng *sce.Engine[InvokeParamErrorStartsTheChildState, InvokeParamErrorStartsTheChildEvent]) {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	sessionID := p.SessionID

	// W3C SCXML 5.3: Initialize _sessionid
	_ = engine.SetVariable(sessionID, "_sessionid", sessionID)

	// W3C SCXML 5.3: Initialize _name
	_ = engine.SetVariable(sessionID, "_name", "invoke_param_error_starts_the_child")

	// W3C SCXML 5.2.2: Initialize global datamodel variables
	// W3C SCXML 5.2/5.3: Initialize nothing from expr="null"
	{
		result, err := engine.EvaluateExpression(sessionID, `nil`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "nothing", result)
		} else {
			eng.Raise(sce.NewPlatformError(InvokeParamErrorStartsTheChildEventErrorExecution, "<data id='nothing'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "nothing", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize sawParamError from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "sawParamError", result)
		} else {
			eng.Raise(sce.NewPlatformError(InvokeParamErrorStartsTheChildEventErrorExecution, "<data id='sawParamError'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "sawParamError", nil)
		}
	}



	// §scxml-6.4.3: apply the invoke params the parent staged — overriding the
	// `<data>` default this block just seeded, and only for a name the child
	// declares ("if the names do not match, the Processor MUST NOT add the
	// value ... to the invoked session's data model"). `HasVariable` is that
	// filter, and it is this channel's only copy of the rule.
	if len(p.pendingParams) > 0 {
		for paramName, paramValue := range p.pendingParams {
			if engine.HasVariable(sessionID, paramName) {
				_ = engine.SetVariable(sessionID, paramName, paramValue)
			}
		}
		p.pendingParams = nil
	}
}

// evaluateGuard evaluates a guard condition via script engine.
// W3C SCXML 4.3.2: If a condition expression cannot be evaluated, the processor
// MUST treat it as false and MUST raise error.execution.
func (p *InvokeParamErrorStartsTheChildPolicy) evaluateGuard(guard string, eng *sce.Engine[InvokeParamErrorStartsTheChildState, InvokeParamErrorStartsTheChildEvent]) bool {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	result, err := engine.EvaluateExpression(p.SessionID, guard)
	if err != nil {
		eng.Raise(sce.NewPlatformError(InvokeParamErrorStartsTheChildEventErrorExecution, "a <transition> cond failed to evaluate"))
		return false
	}
	return sce.ScriptToBool(result)
}

// executeScript executes a script block via script engine.
func (p *InvokeParamErrorStartsTheChildPolicy) executeScript(script string) error {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	return engine.ExecuteScript(p.SessionID, script)
}

// setCurrentEvent binds _event system variable (W3C SCXML 5.10) and answers
// which rung of §scxml-B-2-8-1 the payload got, because the binding is the
// only place that knows.
//
// This used to be `_ = engine.SetCurrentEvent(...)`: the ladder decided
// between a DOM, a value and a space-normalized string, and the decision was
// dropped one line later. A payload that announced structure and would not
// parse therefore reached the document as raw characters, every
// _event.data.<field> read empty, and nothing anywhere could say so — see
// sce.PayloadReading.
func (p *InvokeParamErrorStartsTheChildPolicy) setCurrentEvent(name string) sce.PayloadReading {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	data := p.pendingEventData
	eventType := p.pendingEventType
	sendID := p.pendingEventSendid
	// W3C SCXML C.1: `_event.origin` is the sender's published
	// `_ioprocessors` location, not its bare session id — and this is the one
	// place that publishes `_event` to the document, so this is where the id
	// becomes a location. The engine keeps the bare id in
	// `EventMetadata.Origin` because its session-keyed lookups (<finalize>
	// dispatch, cancelled-invoke filtering) match on it; converting at the
	// raise would make one value serve two consumers that need different
	// spellings. The conversion itself lives in `sce.PublishedOrigin`, the
	// port of the `IOProcessorHelper::publishedOrigin` the C++ engines share:
	// a second spelling of the rule is how the backends would stop agreeing.
	origin := sce.PublishedOrigin(p.pendingEventOrigin)
	originType := p.pendingEventOrigintype
	invokeID := p.pendingEventInvokeid
	reading, err := engine.SetCurrentEvent(p.SessionID, sce.SetCurrentEventArgs{
		Name:       name,
		Data:       data,
		Type:       eventType,
		SendID:     sendID,
		Origin:     origin,
		OriginType: originType,
		InvokeID:   invokeID,
	})
	if err != nil {
		// An engine that could not bind _event at all has a larger problem
		// than an unreadable payload, and reporting PayloadUndecodable for it
		// would point a host at the wrong thing.
		return sce.PayloadAbsent
	}
	return reading
}

// assignVariable assigns a value to a datamodel variable via script engine.
func (p *InvokeParamErrorStartsTheChildPolicy) assignVariable(location, expr string) error {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	script := location + " = " + expr
	return engine.ExecuteScript(p.SessionID, script)
}

// getVariable reads a datamodel variable from the script engine.
func (p *InvokeParamErrorStartsTheChildPolicy) getVariable(name string) interface{} {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	result, err := engine.GetVariable(p.SessionID, name)
	if err != nil {
		return nil
	}
	return result
}

// SetParamValueInScriptEngine stores an invoke param's VALUE for deferred
// application after datamodel init (§scxml-6.4.3).
//
// Params are applied at the end of InitializeDataModel, overriding the
// `<data>` default only for names the child declares. The value crosses as a
// value: this took the parent's value rendered as Lua source and
// re-evaluated it here until 2026-08-21, which survived every value a test
// had tried and lost the ones Lua cannot spell — `ToScriptLiteral` renders a
// non-finite number as `+Inf`, which is not a Lua expression, so an
// `<invoke>` `<param expr="1/0"/>` arrived as nothing at all.
func (p *InvokeParamErrorStartsTheChildPolicy) SetParamValueInScriptEngine(name string, value interface{}) {
	if p.pendingParams == nil {
		p.pendingParams = make(map[string]interface{})
	}
	p.pendingParams[name] = value
}


// ExecutePendingInvokes starts deferred child state machines (W3C SCXML 6.4).
// Port of Rust do_execute_pending_invokes().
func (p *InvokeParamErrorStartsTheChildPolicy) ExecutePendingInvokes(engine *sce.Engine[InvokeParamErrorStartsTheChildState, InvokeParamErrorStartsTheChildEvent]) {
	if len(p.pendingInvokes) == 0 {
		return
	}

	// W3C SCXML 6.4: Copy-and-clear to prevent iterator invalidation
	invokesToExecute := make([]sce.PendingInvoke[InvokeParamErrorStartsTheChildState], len(p.pendingInvokes))
	copy(invokesToExecute, p.pendingInvokes)
	p.pendingInvokes = p.pendingInvokes[:0]

	for _, pending := range invokesToExecute {
		if pending.DocumentID == "inv_probe" {

			// W3C SCXML 6.5: Generate child session ID for finalize origin matching
			childSessionID := fmt.Sprintf("%s.%s", p.SessionID, pending.InvokeID)

			// W3C SCXML 6.4: Create child state machine
			childPolicy := NewInvokeParamErrorStartsTheChildSceSynthInvokeInvProbePolicy()
			childPolicy.ScriptEngine = p.ScriptEngine
			parentQueue := make(chan sce.ParentEvent, 100)
			childPolicy.ParentExternalQueue = parentQueue
			childPolicy.InvokeID = pending.InvokeID
			childPolicy.ChildSessionID = childSessionID
			// W3C SCXML C.1: the child adopts the id its parent recorded for
			// it. The parent mints `parentSession.invokeId` and keys
			// `activeInvokes` on it, so an event from this child reaches the
			// parent carrying that id as its origin; if the child also minted
			// an id of its own, the location it publishes in `_ioprocessors`
			// and the origin the parent sees would be two names for one
			// session that can never be compared — which is exactly what C.1
			// requires of them.
			childPolicy.SessionID = childSessionID


			// W3C SCXML 6.4.1: Pass params to child datamodel before initialization
			{
				p.ensureScriptEngine()
				se := p.ScriptEngine
				if val, err := se.EvaluateExpression(p.SessionID, `"here"`); err == nil {
					// §scxml-6.4.3: the VALUE of the param element becomes the
					// child `<data>`'s initial value.
					childPolicy.SetParamValueInScriptEngine("kept", val)
				} else {
					// §scxml-5.7.1: BOTH halves — `error.execution` on the internal
					// queue AND the name and value ignored. Only the silent half was
					// here, so a document that miscomputed one `<param>` got a child
					// with a `<data>` nothing explained and no event to act on.
					//
					// The clause delegates only the SUCCESSFUL name and value to the
					// context ("Otherwise the use of the name and value depends on
					// the context ... See 5.5 <donedata>, 6.2 <send> and 6.4
					// <invoke>"), so §scxml-6.4.2's "terminate the processing of the
					// element" is not what a failing `<param>` costs: the child
					// still starts, one pair short.
					engine.Raise(sce.NewPlatformError(InvokeParamErrorStartsTheChildEventErrorExecution, "<invoke> <param name='kept'> expr failed to evaluate"))
				}
				if val, err := se.EvaluateExpression(p.SessionID, `nothing.deep`); err == nil {
					// §scxml-6.4.3: the VALUE of the param element becomes the
					// child `<data>`'s initial value.
					childPolicy.SetParamValueInScriptEngine("broken", val)
				} else {
					// §scxml-5.7.1: BOTH halves — `error.execution` on the internal
					// queue AND the name and value ignored. Only the silent half was
					// here, so a document that miscomputed one `<param>` got a child
					// with a `<data>` nothing explained and no event to act on.
					//
					// The clause delegates only the SUCCESSFUL name and value to the
					// context ("Otherwise the use of the name and value depends on
					// the context ... See 5.5 <donedata>, 6.2 <send> and 6.4
					// <invoke>"), so §scxml-6.4.2's "terminate the processing of the
					// element" is not what a failing `<param>` costs: the child
					// still starts, one pair short.
					engine.Raise(sce.NewPlatformError(InvokeParamErrorStartsTheChildEventErrorExecution, "<invoke> <param name='broken'> expr failed to evaluate"))
				}
			}

			// W3C SCXML 6.4.1: Track active invoke session BEFORE initialize
			p.activeInvokes["inv_probe"] = &sce.ChildSession{
				SessionID:       childSessionID,
				InvokeID:        "inv_probe",
				ParentSessionID: p.SessionID,
				Autoforward:     false,
				FinalizeScript:  "",
			}

			childEngine := sce.NewEngine[InvokeParamErrorStartsTheChildSceSynthInvokeInvProbeState, InvokeParamErrorStartsTheChildSceSynthInvokeInvProbeEvent](&childPolicy)
			// §scxml-6.4: the child's delayed sends are measured against the
			// same clock as ours. A child reading its own would start its
			// origin at construction time, so <send delay="100ms"> on either
			// side of the boundary would mean two different absolute instants —
			// and on a host-owned clock the child would not move at all,
			// because the host advances the engine it holds, not the ones that
			// engine invoked.
			childEngine.SetClock(engine.Clock())
			childEngine.SetCompletionCallback(func() {})
			childEngine.Initialize()

			// W3C SCXML 6.4: Store child and create wrapper
			wrapper := &childEngineWrapperInvProbe{engine: childEngine, policy: &childPolicy}
			p.childInvProbe = wrapper

			// W3C SCXML 6.4: Drain child-to-parent events raised during initialize
			sce.DrainAndRaiseChildEvents(wrapper, p.activeInvokes, "inv_probe", engine)

			// W3C SCXML 6.4: Check if child completed during initialize
			if wrapper.IsInFinalState() {
				p.pendingDoneInvokeInvProbe = true
				// W3C SCXML 5.5 + 6.3.1: Lift the child's stashed donedata onto
				// done.invoke.<id>._event.data. Mirrors the C++ AOT contract
				// and the Rust raise_done_invoke(donedata) 3-arg signature.
				sce.RaiseDoneInvoke("inv_probe", wrapper.DonedataAtFinal(), engine)
			}
			continue
		}
	}
}

// TickChildren ticks child state machines (W3C SCXML 6.4).
// Port of Rust do_tick_children().
func (p *InvokeParamErrorStartsTheChildPolicy) TickChildren(engine *sce.Engine[InvokeParamErrorStartsTheChildState, InvokeParamErrorStartsTheChildEvent]) {
	if !p.pendingDoneInvokeInvProbe && p.childInvProbe != nil {
		child := p.childInvProbe
		sce.DrainAndRaiseChildEvents(child, p.activeInvokes, "inv_probe", engine)
		child.Tick()
		sce.DrainAndRaiseChildEvents(child, p.activeInvokes, "inv_probe", engine)
		if child.IsInFinalState() && !p.pendingDoneInvokeInvProbe {
			p.pendingDoneInvokeInvProbe = true
			// W3C SCXML 5.5 + 6.3.1: Lift the child's stashed donedata onto
			// done.invoke.<id>._event.data.
			sce.RaiseDoneInvoke("inv_probe", child.DonedataAtFinal(), engine)
		}
	}
}


// DeliverToChildSession delivers an event addressed to a child's published
// location (W3C SCXML C.1).
//
// The parent mints `parentSession.invokeId` for each child and that id is what
// the child's `_ioprocessors` entry names, so a <send> whose target decodes to
// one of them is addressed to that child rather than to this machine. A false
// return means the address names no live child of ours and the event takes the
// normal external path — the routing half of C.1 is what makes the published
// location a usable target rather than a string that merely compares equal.
func (p *InvokeParamErrorStartsTheChildPolicy) DeliverToChildSession(childSessionID, eventName, eventData string) bool {
	if childSessionID == "" {
		return false
	}
	if cs, ok := p.activeInvokes["inv_probe"]; ok && cs.SessionID == childSessionID {
		if p.childInvProbe != nil {
			p.childInvProbe.RaiseExternalByName(eventName, eventData)
			return true
		}
	}
	return false
}


// ── Child engine wrappers (implement sce.ChildEngine interface) ──

type childEngineWrapperInvProbe struct {
	engine *sce.Engine[InvokeParamErrorStartsTheChildSceSynthInvokeInvProbeState, InvokeParamErrorStartsTheChildSceSynthInvokeInvProbeEvent]
	policy *InvokeParamErrorStartsTheChildSceSynthInvokeInvProbePolicy
}
func (w *childEngineWrapperInvProbe) Initialize() { w.engine.Initialize() }
func (w *childEngineWrapperInvProbe) Tick() { w.engine.Tick() }
func (w *childEngineWrapperInvProbe) Stop() { w.engine.Stop() }
func (w *childEngineWrapperInvProbe) IsInFinalState() bool { return w.engine.IsInFinalState() }
func (w *childEngineWrapperInvProbe) RaiseExternalByName(name, data string) { w.engine.RaiseExternalByName(name, data) }
func (w *childEngineWrapperInvProbe) RaiseExternalByNameWithMeta(name string, metadata sce.EventMetadata) { w.engine.RaiseExternalByNameWithMeta(name, metadata) }
func (w *childEngineWrapperInvProbe) SetCompletionCallback(cb func()) { w.engine.SetCompletionCallback(cb) }
func (w *childEngineWrapperInvProbe) GetParentEventQueue() chan sce.ParentEvent { return w.policy.ParentExternalQueue }
func (w *childEngineWrapperInvProbe) DonedataAtFinal() string { return w.engine.DonedataAtFinal() }


// ======================================================================
// StatePolicy interface implementation
// ======================================================================

// InitialState returns the initial state (W3C SCXML 3.3).
func (p *InvokeParamErrorStartsTheChildPolicy) InitialState() InvokeParamErrorStartsTheChildState {
	return InvokeParamErrorStartsTheChildStateParamPhase
}

// IsFinalState returns true if state is a <final> state (W3C SCXML 3.7).
func (p *InvokeParamErrorStartsTheChildPolicy) IsFinalState(state InvokeParamErrorStartsTheChildState) bool {
	switch state {
	case InvokeParamErrorStartsTheChildStateFailBrokenParamSeeded:
		return true
	case InvokeParamErrorStartsTheChildStateFailGoodParamLost:
		return true
	case InvokeParamErrorStartsTheChildStateFailInvokeNotStarted:
		return true
	case InvokeParamErrorStartsTheChildStateFailNoParamError:
		return true
	case InvokeParamErrorStartsTheChildStatePass:
		return true
	}
	return false
}

// GetParent returns the parent of state, or (zero, false) if root (W3C SCXML 3.3).
func (p *InvokeParamErrorStartsTheChildPolicy) GetParent(state InvokeParamErrorStartsTheChildState) (InvokeParamErrorStartsTheChildState, bool) {
	switch state {
	}
	return 0, false
}

// IsCompoundState returns true if state is a <state> with child states — exactly
// the states that have an initial transition. A <parallel> is not compound
// (W3C SCXML 3.3).
func (p *InvokeParamErrorStartsTheChildPolicy) IsCompoundState(state InvokeParamErrorStartsTheChildState) bool {
	return len(initialTargetsOfInvokeParamErrorStartsTheChild[state]) > 0
}

func (p *InvokeParamErrorStartsTheChildPolicy) IsParallelState(_ InvokeParamErrorStartsTheChildState) bool { return false }

// GetChildStates returns state's <state>, <parallel> and <final> children, in
// document order — for a <parallel>, its regions (§scxml-D-getChildStates).
func (p *InvokeParamErrorStartsTheChildPolicy) GetChildStates(state InvokeParamErrorStartsTheChildState) []InvokeParamErrorStartsTheChildState {
	return childStatesOfInvokeParamErrorStartsTheChild[state]
}

// GetInitialTargets returns a compound state's initial transition target, as
// written; the engine's entry procedures dereference a <history> among them
// (W3C SCXML 3.3).
func (p *InvokeParamErrorStartsTheChildPolicy) GetInitialTargets(state InvokeParamErrorStartsTheChildState) []InvokeParamErrorStartsTheChildTarget {
	return initialTargetsOfInvokeParamErrorStartsTheChild[state]
}

// GetDocumentInitialTargets returns the target of the document's own initial
// transition, as written (W3C SCXML 3.2).
func (p *InvokeParamErrorStartsTheChildPolicy) GetDocumentInitialTargets() []InvokeParamErrorStartsTheChildTarget {
	return documentInitialTargetsOfInvokeParamErrorStartsTheChild
}

// W3C SCXML 3.10: this document declares no <history>, so no target list names
// one and the engine never asks the two below; answering would mean inventing
// one.
func (p *InvokeParamErrorStartsTheChildPolicy) GetHistoryParent(history sce.HistoryID) InvokeParamErrorStartsTheChildState {
	panic(fmt.Sprintf("InvokeParamErrorStartsTheChildPolicy declares no <history>; asked for %d", history))
}
func (p *InvokeParamErrorStartsTheChildPolicy) GetHistoryDefaultTargets(history sce.HistoryID) []InvokeParamErrorStartsTheChildTarget {
	panic(fmt.Sprintf("InvokeParamErrorStartsTheChildPolicy declares no <history>; asked for %d", history))
}
func (p *InvokeParamErrorStartsTheChildPolicy) HistoryValue(_ sce.HistoryID) ([]InvokeParamErrorStartsTheChildState, bool) {
	return nil, false
}

// GetDocumentOrder returns the document order index (W3C SCXML Appendix D).
func (p *InvokeParamErrorStartsTheChildPolicy) GetDocumentOrder(state InvokeParamErrorStartsTheChildState) int {
	switch state {
	case InvokeParamErrorStartsTheChildStateFailBrokenParamSeeded:
		return 5
	case InvokeParamErrorStartsTheChildStateFailGoodParamLost:
		return 4
	case InvokeParamErrorStartsTheChildStateFailInvokeNotStarted:
		return 3
	case InvokeParamErrorStartsTheChildStateFailNoParamError:
		return 2
	case InvokeParamErrorStartsTheChildStateParamPhase:
		return 0
	case InvokeParamErrorStartsTheChildStatePass:
		return 1
	}
	return -1
}

// GetEventName returns the string name of an event (W3C SCXML 3.12).
func (p *InvokeParamErrorStartsTheChildPolicy) GetEventName(event InvokeParamErrorStartsTheChildEvent) string {
	return event.String()
}

// GetEventFromName looks up an event by name (W3C SCXML 3.12).
func (p *InvokeParamErrorStartsTheChildPolicy) GetEventFromName(name string) (InvokeParamErrorStartsTheChildEvent, bool) {
	switch name {
	case "cancel.invoke":
		return InvokeParamErrorStartsTheChildEventCancelInvoke, true
	case "childUp":
		return InvokeParamErrorStartsTheChildEventChildUp, true
	case "done.invoke":
		return InvokeParamErrorStartsTheChildEventDoneInvoke, true
	case "error.execution":
		return InvokeParamErrorStartsTheChildEventErrorExecution, true
	case "timeout":
		return InvokeParamErrorStartsTheChildEventTimeout, true
	}
	return InvokeParamErrorStartsTheChildEventNull, false
}

// GetStateName returns the string name of a state (W3C SCXML 3.3).
func (p *InvokeParamErrorStartsTheChildPolicy) GetStateName(state InvokeParamErrorStartsTheChildState) string {
	return state.String()
}

// GetStateFromName reads a state id back into the state it names (W3C SCXML 3.3).
// The reverse of GetStateName, and what turns a host's recorded configuration
// back into the argument sce.Engine.EnterAt takes.
func (p *InvokeParamErrorStartsTheChildPolicy) GetStateFromName(name string) (InvokeParamErrorStartsTheChildState, bool) {
	return InvokeParamErrorStartsTheChildStateFromName(name)
}

// NullEvent returns the sentinel for eventless transition dispatch (W3C SCXML 3.13).
func (p *InvokeParamErrorStartsTheChildPolicy) NullEvent() InvokeParamErrorStartsTheChildEvent {
	return InvokeParamErrorStartsTheChildEventNull
}


// SetNextEventIsExternal sets the external event flag (W3C SCXML 5.10.1).
func (p *InvokeParamErrorStartsTheChildPolicy) SetNextEventIsExternal(value bool) {
	p.nextEventIsExternal = value
}

// HasParallelStates returns whether the SM has parallel states.
func (p *InvokeParamErrorStartsTheChildPolicy) HasParallelStates() bool {
	return false
}

// NeedsScriptEngine returns whether the SM needs a script engine.
func (p *InvokeParamErrorStartsTheChildPolicy) NeedsScriptEngine() bool {
	return true
}

// NeedsEventScheduler reports whether a host must drive this machine with
// Tick rather than Step alone.
//
// W3C SCXML 6.2: Step runs a macrostep and never drains the delayed-send
// scheduler nor ticks an invoked child, so a host that only ever calls it
// waits forever with nothing said. Same verdict the generate manifest
// publishes as needs_event_scheduler.
func (p *InvokeParamErrorStartsTheChildPolicy) NeedsEventScheduler() bool {
	return true
}

// NeedsDataModelInit returns whether the SM needs datamodel init.
func (p *InvokeParamErrorStartsTheChildPolicy) NeedsDataModelInit() bool {
	return true
}

// HasInvokeSupport returns true for invoke-capable SMs.
func (p *InvokeParamErrorStartsTheChildPolicy) HasInvokeSupport() bool { return true }
// HasChildTick returns true for child-ticking SMs.
func (p *InvokeParamErrorStartsTheChildPolicy) HasChildTick() bool { return true }
func (p *InvokeParamErrorStartsTheChildPolicy) HasFinalize() bool { return false }
func (p *InvokeParamErrorStartsTheChildPolicy) HasAutoforward() bool { return false }
func (p *InvokeParamErrorStartsTheChildPolicy) HasActiveStates() bool { return false }
func (p *InvokeParamErrorStartsTheChildPolicy) GetActiveStates() []InvokeParamErrorStartsTheChildState { return nil }
// W3C SCXML 3.4: this machine keeps no active set — its configuration is the
// parent walk from the current state — so there is nothing for a restore to
// hand back here. sce.Engine.EnterAt reaches this only through HasActiveStates,
// which is false above; the method exists because the interface is one contract.
func (p *InvokeParamErrorStartsTheChildPolicy) SetActiveStates(_ []InvokeParamErrorStartsTheChildState) {}
func (p *InvokeParamErrorStartsTheChildPolicy) HasExternalEventFlag() bool { return true }
// ExecuteFinalizeForChildEvent is a no-op (no finalize invokes).
func (p *InvokeParamErrorStartsTheChildPolicy) ExecuteFinalizeForChildEvent(_ *sce.EventWithMetadata[InvokeParamErrorStartsTheChildEvent], _ *sce.Engine[InvokeParamErrorStartsTheChildState, InvokeParamErrorStartsTheChildEvent]) {}
// ForwardToAutoforwardChildren is a no-op (no autoforward invokes).
func (p *InvokeParamErrorStartsTheChildPolicy) ForwardToAutoforwardChildren(_ string, _ sce.EventMetadata, _ *sce.Engine[InvokeParamErrorStartsTheChildState, InvokeParamErrorStartsTheChildEvent]) {}

// PopulateEventMetadata stores pending event metadata (W3C SCXML 5.10).
// Note: event name is set separately via setCurrentEvent(), not from metadata.
func (p *InvokeParamErrorStartsTheChildPolicy) PopulateEventMetadata(meta *sce.EventMetadata) {
	p.pendingEventData = meta.Data
	p.pendingEventType = meta.EventType.String()
	p.pendingEventSendid = meta.SendID
	p.pendingEventOrigin = meta.Origin
	p.pendingEventOrigintype = meta.OriginType
	p.pendingEventInvokeid = meta.InvokeID
}

// LiftTypedPayload binds the dequeued event's typed `_event.data` view from
// the data it carries (NL→IR Item C1 Path A).
//
// The typed carrier PopulateEventMetadata reads is filled by one producer, the
// generated Raise<Event> inject seam. Every other producer — <send> with
// <param>, an invoke forwarding either way, autoforward, BasicHTTP, mesh —
// fills EventMetadata.Data, so the schema's fields are read out of that here.
// A refusal is returned for the engine to raise as error.execution, which is
// what W3C SCXML 3.13 gives for a guard that cannot be evaluated.
func (p *InvokeParamErrorStartsTheChildPolicy) LiftTypedPayload(event InvokeParamErrorStartsTheChildEvent, meta *sce.EventMetadata) error {
	// This document lowered no typed guard, so no event carries a typed view.
	return nil
}

// ClearEventMetadata resets pending event metadata (W3C SCXML 5.10).
func (p *InvokeParamErrorStartsTheChildPolicy) ClearEventMetadata() {
	p.pendingEventName = ""
	p.pendingEventData = ""
	p.pendingEventType = ""
	p.pendingEventSendid = ""
	p.pendingEventOrigin = ""
	p.pendingEventOrigintype = ""
	p.pendingEventInvokeid = ""
}



// ExecuteEntryActions enters one state (W3C SCXML 3.8): adds it to the
// configuration, runs its <onentry>, and its <initial> transition's content when
// its initial state is entered by default.
//line invoke_param_error_starts_the_child.scxml:52
func (p *InvokeParamErrorStartsTheChildPolicy) ExecuteEntryActions(state InvokeParamErrorStartsTheChildState, engine *sce.Engine[InvokeParamErrorStartsTheChildState, InvokeParamErrorStartsTheChildEvent], isDefaultEntry bool) {
	p.ensureScriptEngine()
	switch state {
	case InvokeParamErrorStartsTheChildStateParamPhase:
		//line invoke_param_error_starts_the_child.scxml:70
		// W3C SCXML 3.8: onentry block 0 (break on error stops subsequent actions)
		for actionBlock0 := 0; actionBlock0 < 1; actionBlock0++ {
			_ = actionBlock0

	// W3C SCXML 6.2: send id="__send_0"
	{
		eventDataStr := ""
		_ = eventDataStr
	// W3C SCXML 6.2: Delayed send
	{
		delayDur := sce.ParseDelay("3s")
		delayEvtName := "timeout"
		if delayEvt, delayOk := p.GetEventFromName(delayEvtName); delayOk {
			engine.ScheduleEvent(delayEvt, delayDur, "__send_0", eventDataStr)
		}
	}
	}

		}
		// W3C SCXML 6.4: Defer invoke execution until macrostep end
		{
			generatedInvokeID := fmt.Sprintf("%s.%d.inv_probe", "paramPhase", sce.NextInvokeCounter())
			sce.DeferInvoke(&p.pendingInvokes, sce.PendingInvoke[InvokeParamErrorStartsTheChildState]{
				InvokeID:   generatedInvokeID,
				State:      InvokeParamErrorStartsTheChildStateParamPhase,
				DocumentID: "inv_probe",
			})
		}
	default:
		// No entry actions
	}
}

// ExecuteHistoryDefaultContent runs a <history>'s default transition content
// (W3C SCXML 3.10.2), after its parent's onentry (and after the parent's own
// <initial> content) when the history was taken with nothing recorded. The
// engine asks for it by the entry set's defaultHistoryContent answer; a history
// that restored what it recorded runs nothing.
//line invoke_param_error_starts_the_child.scxml:52
func (p *InvokeParamErrorStartsTheChildPolicy) ExecuteHistoryDefaultContent(history sce.HistoryID, engine *sce.Engine[InvokeParamErrorStartsTheChildState, InvokeParamErrorStartsTheChildEvent]) {
	// W3C SCXML 3.10.2: no <history> in this document has default content.
}

// ExecuteExitActions exits one state (W3C SCXML 3.9): records its histories,
// runs its <onexit>, cancels its invocations and removes it from the
// configuration — §scxml-D-exitStates's order.
//line invoke_param_error_starts_the_child.scxml:52
func (p *InvokeParamErrorStartsTheChildPolicy) ExecuteExitActions(state InvokeParamErrorStartsTheChildState, engine *sce.Engine[InvokeParamErrorStartsTheChildState, InvokeParamErrorStartsTheChildEvent], configurationBeforeExit []InvokeParamErrorStartsTheChildState) {
	p.ensureScriptEngine()
	// §scxml-D-exitStates orders one state's exit as onexit, then
	// cancelInvoke, then configuration.delete(s), so `In(s)` inside s's own
	// handler is true: the <onexit> switch comes first and the removal from
	// the configuration last.
	switch state {
	default:
		// No exit actions
	}
	// W3C SCXML 6.4: Cancel pending invokes and cleanup active children on state exit
	switch state {
	case InvokeParamErrorStartsTheChildStateParamPhase:
		sce.CancelInvokesForState(&p.pendingInvokes, InvokeParamErrorStartsTheChildStateParamPhase)
		if p.childInvProbe != nil {
			// §scxml-D-exitInterpreter: a cancelled session is exited -- its
			// states' <onexit> run -- before it is dropped; what that exit
			// sends to #_parent is dropped with it (§scxml-6.4, test252).
			p.childInvProbe.Stop()
			p.childInvProbe = nil
		}
		delete(p.activeInvokes, "inv_probe")
		p.pendingDoneInvokeInvProbe = false
	default:
	}
}



// BindCurrentEvent binds the event whose transitions are about to be selected as
// the _event their guards read (W3C SCXML 5.10) — before the first guard runs,
// and not for an eventless selection, which has no event of its own.
//line invoke_param_error_starts_the_child.scxml:52
func (p *InvokeParamErrorStartsTheChildPolicy) BindCurrentEvent(event InvokeParamErrorStartsTheChildEvent, engine *sce.Engine[InvokeParamErrorStartsTheChildState, InvokeParamErrorStartsTheChildEvent]) {
	if event != InvokeParamErrorStartsTheChildEventNull {
		// §scxml-B-2-8-1: the rung the payload got, handed to the engine
		// rather than dropped. This is the only frame that has both the
		// reading and the event it belongs to.
		engine.NotePayloadReading(event, p.setCurrentEvent(p.GetEventName(event)))
	}
}

// FirstEnabledTransition is Appendix D selectTransitions, the half only this
// document can answer: the first of state's own transitions, in document order,
// that event enables and whose guard holds. The engine walks the atomic states
// and their ancestors and keeps the ordered set; the null event asks for
// eventless transitions.
//line invoke_param_error_starts_the_child.scxml:52
func (p *InvokeParamErrorStartsTheChildPolicy) FirstEnabledTransition(state InvokeParamErrorStartsTheChildState, event InvokeParamErrorStartsTheChildEvent, engine *sce.Engine[InvokeParamErrorStartsTheChildState, InvokeParamErrorStartsTheChildEvent]) (sce.EnabledTransition[InvokeParamErrorStartsTheChildState, sce.HistoryID], bool) {
	switch state {
	case InvokeParamErrorStartsTheChildStateParamPhase:
		if event == InvokeParamErrorStartsTheChildEventErrorExecution {
			{
				return sce.EnabledTransition[InvokeParamErrorStartsTheChildState, sce.HistoryID]{
					Source:          state,
					TransitionIndex: 0,
					HasActions:      true,
					IsInternal:      false,
				}, true
			}
		}
		if event == InvokeParamErrorStartsTheChildEventChildUp {
			if p.evaluateGuard(`(sawParamError ~= 1)`, engine) {
				return sce.EnabledTransition[InvokeParamErrorStartsTheChildState, sce.HistoryID]{
					Source:          state,
					Targets:         transitionTargetsOfInvokeParamErrorStartsTheChild[state][1],
					TransitionIndex: 1,
					HasActions:      false,
					IsInternal:      false,
				}, true
			}
		}
		if event == InvokeParamErrorStartsTheChildEventChildUp {
			if p.evaluateGuard(`(_event.data.kept ~= "here")`, engine) {
				return sce.EnabledTransition[InvokeParamErrorStartsTheChildState, sce.HistoryID]{
					Source:          state,
					Targets:         transitionTargetsOfInvokeParamErrorStartsTheChild[state][2],
					TransitionIndex: 2,
					HasActions:      false,
					IsInternal:      false,
				}, true
			}
		}
		if event == InvokeParamErrorStartsTheChildEventChildUp {
			if p.evaluateGuard(`(_event.data.brokenPlaceholder == true)`, engine) {
				return sce.EnabledTransition[InvokeParamErrorStartsTheChildState, sce.HistoryID]{
					Source:          state,
					Targets:         transitionTargetsOfInvokeParamErrorStartsTheChild[state][3],
					TransitionIndex: 3,
					HasActions:      false,
					IsInternal:      false,
				}, true
			}
		}
		if event == InvokeParamErrorStartsTheChildEventChildUp {
			{
				return sce.EnabledTransition[InvokeParamErrorStartsTheChildState, sce.HistoryID]{
					Source:          state,
					Targets:         transitionTargetsOfInvokeParamErrorStartsTheChild[state][4],
					TransitionIndex: 4,
					HasActions:      false,
					IsInternal:      false,
				}, true
			}
		}
		if event == InvokeParamErrorStartsTheChildEventTimeout {
			{
				return sce.EnabledTransition[InvokeParamErrorStartsTheChildState, sce.HistoryID]{
					Source:          state,
					Targets:         transitionTargetsOfInvokeParamErrorStartsTheChild[state][5],
					TransitionIndex: 5,
					HasActions:      false,
					IsInternal:      false,
				}, true
			}
		}
	}
	return sce.EnabledTransition[InvokeParamErrorStartsTheChildState, sce.HistoryID]{}, false
}

// ExecuteTransitionContent runs one transition's executable content (W3C SCXML
// 3.13), between the microstep's exits and its entries.
//line invoke_param_error_starts_the_child.scxml:52
func (p *InvokeParamErrorStartsTheChildPolicy) ExecuteTransitionContent(source InvokeParamErrorStartsTheChildState, transitionIndex int, engine *sce.Engine[InvokeParamErrorStartsTheChildState, InvokeParamErrorStartsTheChildEvent]) {
	p.ensureScriptEngine()
	switch source {
	case InvokeParamErrorStartsTheChildStateParamPhase:
		switch transitionIndex {
		case 0:
			//line invoke_param_error_starts_the_child.scxml:117

	// W3C SCXML 5.3: <assign location="sawParamError" expr="1">
	if err := p.assignVariable(`sawParamError`, `1`); err != nil {
		engine.Raise(sce.NewPlatformError(InvokeParamErrorStartsTheChildEventErrorExecution, "<assign> to 'sawParamError' failed"))
	}

		}
	}
}
