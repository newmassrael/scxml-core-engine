// SCE-GENERATED — DO NOT EDIT
// source-hash: fb387f95a7c2fa236925e899bbae58850631699c7da1b9cb10d68b996ce013cb


// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) 2025-2026 [Author of input SCXML file]
//
// Generated by SCE Code Generator (Go backend)
// From: integration_resources/typed_reader_names/typed_reader_names.scxml
//
// This generated code is MIT licensed and may be freely used in any project.
// Runtime engine dependency (sce-go-runtime): LGPL-2.1-or-later WITH LicenseRef-SCE-Linking-Exception OR LicenseRef-SCE-Commercial
// Full license: https://github.com/newmassrael/scxml-core-engine/blob/main/LICENSE
// Generated Go state machine for `typed_reader_names`.
//
// Generated by SCE Code Generator (Go backend).
// Implements sce.StatePolicy with full transition logic,
// entry/exit actions, and event processing.


// SCE-MAP: typed_reader_names.scxml:31 :: _machine

package typed_reader_names

import (
	"fmt"
	"path/filepath"
	"strings"
	"time"

	sce "github.com/newmassrael/sce-go-runtime"
)

// Ensure imports are used
var (
	_ = fmt.Sprintf
	_ = filepath.Join
	_ = strings.HasPrefix
	_ = time.Duration(0)
)

// ======================================================================
// State type (W3C SCXML 3.3)
// ======================================================================

type TypedReaderNamesState int

const (
	TypedReaderNamesStateDone TypedReaderNamesState = 0
	TypedReaderNamesStateIdle TypedReaderNamesState = 1
)

func (s TypedReaderNamesState) String() string {
	switch s {
	case TypedReaderNamesStateDone:
		return "done"
	case TypedReaderNamesStateIdle:
		return "idle"
	}
	return "unknown"
}

// TypedReaderNamesStateFromName is the read half of the pair above — a state
// id back into the state it names (W3C SCXML 3.3).
//
// A host that persists where a machine was has to write it down as TEXT: the
// constants above are a build artefact of one binary, and the process that
// resumes is a different one. String publishes the name and this reads it back,
// which is what lets a journal survive its own record and reach
// sce.Engine.EnterAt.
//
// The second return is false for a name this document does not declare. A name
// guessed at rather than refused is how a restore reaches a configuration
// nobody recorded — and the refusal is what makes a typo in a journal a
// reported failure instead of a machine quietly somewhere else.
//
// Emitted from the same loop over the document's states as String, so the two
// age together.
func TypedReaderNamesStateFromName(name string) (TypedReaderNamesState, bool) {
	switch name {
	case "done":
		return TypedReaderNamesStateDone, true
	case "idle":
		return TypedReaderNamesStateIdle, true
	}
	var zero TypedReaderNamesState
	return zero, false
}

// TypedReaderNamesAllStates is every state this document declares, in the
// order the constants above are issued.
//
// Emitted from the same loop, so a walk over it is a walk over the document
// rather than over a list somebody maintained beside it — a test that spells
// its own list goes on passing when the document grows a state.
var TypedReaderNamesAllStates = []TypedReaderNamesState{
	TypedReaderNamesStateDone,
	TypedReaderNamesStateIdle,
}

// TypedReaderNamesTarget is one token of a target list, as the document wrote
// it (W3C SCXML 3.13): a state, or a <history> the engine dereferences.
type TypedReaderNamesTarget = sce.EntryTarget[TypedReaderNamesState, sce.HistoryID]

// ======================================================================
// Document structure (W3C SCXML 3.2-3.4, 3.10)
//
// Package-level tables, because the structure is a fact about the document
// and not about a run: the engine's Appendix D procedures read them through
// the policy methods below, and a transition's target list is handed out as
// a slice of them rather than rebuilt on every selection.
// ======================================================================

// childStatesOfTypedReaderNames is §scxml-D-getChildStates per state: its
// <state>, <parallel> and <final> children, in document order.
var childStatesOfTypedReaderNames = [2][]TypedReaderNamesState{
}

// initialTargetsOfTypedReaderNames is each compound state's initial transition
// target, as written (§scxml-3.3).
var initialTargetsOfTypedReaderNames = [2][]TypedReaderNamesTarget{
}

// documentInitialTargetsOfTypedReaderNames is the target of the document's own
// initial transition, as written (§scxml-3.2).
var documentInitialTargetsOfTypedReaderNames = []TypedReaderNamesTarget{sce.StateTarget[TypedReaderNamesState, sce.HistoryID](TypedReaderNamesStateIdle)}

// transitionTargetsOfTypedReaderNames is each transition's target list, as
// written (§scxml-3.13), by source state and the transition's index among its
// source's own transitions. A targetless transition's entry is empty.
var transitionTargetsOfTypedReaderNames = [2][][]TypedReaderNamesTarget{
	TypedReaderNamesStateIdle: {
		1: {sce.StateTarget[TypedReaderNamesState, sce.HistoryID](TypedReaderNamesStateDone)},
	},
}

// ======================================================================
// Event type (W3C SCXML 3.12)
// ======================================================================

type TypedReaderNamesEvent int

const (
	TypedReaderNamesEventBump TypedReaderNamesEvent = 0
	TypedReaderNamesEventErrorExecution TypedReaderNamesEvent = 1
	TypedReaderNamesEventFinish TypedReaderNamesEvent = 2
	// W3C SCXML 3.13: Sentinel for eventless transition dispatch
	TypedReaderNamesEventNull TypedReaderNamesEvent = 3
)

func (e TypedReaderNamesEvent) String() string {
	switch e {
	case TypedReaderNamesEventBump:
		return "bump"
	case TypedReaderNamesEventErrorExecution:
		return "error.execution"
	case TypedReaderNamesEventFinish:
		return "finish"
	case TypedReaderNamesEventNull:
		return ""
	}
	return "unknown"
}

// ======================================================================
// Policy struct
// ======================================================================

type TypedReaderNamesPolicy struct {
	// W3C SCXML 5.10.1: External event flag
	nextEventIsExternal bool
	pendingEventName string
	pendingEventData string
	pendingEventType string
	pendingEventSendid string
	pendingEventOrigin string
	pendingEventOrigintype string
	pendingEventInvokeid string
	// W3C SCXML 5.10: Session ID
	SessionID string
	// W3C SCXML B.1: Script engine handle (per-instance, Path B+ Q1=(d) Go=interface ref)
	ScriptEngine sce.IScriptEngine
	scriptEngineInitialized bool
	// W3C SCXML C.2.3: inbound BasicHTTP endpoint serving this machine,
	// declared by the deployment before Initialize(). The address belongs to
	// whoever runs the listener, so the machine takes it from here rather than
	// guessing one. Empty means no such endpoint is deployed, and no BasicHTTP
	// entry is published in _ioprocessors.
	BasicHTTPAccessURI string
	// W3C SCXML 6.4: Parent communication
	ParentExternalQueue chan sce.ParentEvent
	InvokeID           string
	ChildSessionID     string
	// §scxml-6.4.3: invoke param VALUES staged by the parent, applied after
	// this machine's datamodel init so they override its `<data>` defaults.
	pendingParams      map[string]interface{}
}

// NewTypedReaderNamesPolicy creates a new policy with default values.
func NewTypedReaderNamesPolicy() TypedReaderNamesPolicy {
	return TypedReaderNamesPolicy{
	}
}

// Box reports what the `box` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `box` was assigned a value of another type, or the engine refused.
func (p *TypedReaderNamesPolicy) Box() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "box")
}

// Object reports what the `object` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `object` was assigned a value of another type, or the engine refused.
func (p *TypedReaderNamesPolicy) Object() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "object")
}

// Pass reports what the `pass` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `pass` was assigned a value of another type, or the engine refused.
func (p *TypedReaderNamesPolicy) Pass() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "pass")
}

// ScreenRules reports what the `screen-rules` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `screen-rules` was assigned a value of another type, or the engine refused.
func (p *TypedReaderNamesPolicy) ScreenRules() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "screen-rules")
}

// AB reports what the `a_b` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `a_b` was assigned a value of another type, or the engine refused.
func (p *TypedReaderNamesPolicy) AB() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "a_b")
}




// ensureScriptEngine lazily initializes the script engine session.
//
// Engine DI Parity RFC: panics if ScriptEngine was not set on the policy
// before Initialize() — mirrors Kotlin's mandatory constructor parameter
// contract. Silent no-op would otherwise turn missed injection into a
// hidden runtime regression (every <assign>/<log>/guard becomes a no-op
// and the SM walks to its default-state final without diagnostic).
func (p *TypedReaderNamesPolicy) ensureScriptEngine() {
	if p.scriptEngineInitialized {
		return
	}
	engine := p.ScriptEngine
	if engine == nil {
		panic("TypedReaderNamesPolicy: ScriptEngine field must be set before Initialize()")
	}
	sessionID := p.SessionID
	if sessionID == "" {
		sessionID = sce.GenerateSessionID()
		p.SessionID = sessionID
	}
	if !engine.HasSession(sessionID) {
		_ = engine.CreateSession(sessionID)
	}
	// W3C SCXML C.1.1 / C.2.3: the _ioprocessors entries come from the same
	// helper every other backend uses, so a machine reads the same entry names
	// and the same addresses whichever one runs it.
	_ = engine.SetupSystemVariables(sessionID, "typed_reader_names",
		sce.BuildIoProcessors(sessionID, p.BasicHTTPAccessURI))
	p.scriptEngineInitialized = true
}


// InitializeDataModel sets up datamodel via script engine (W3C SCXML 5.3).
func (p *TypedReaderNamesPolicy) InitializeDataModel(eng *sce.Engine[TypedReaderNamesState, TypedReaderNamesEvent]) {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	sessionID := p.SessionID

	// W3C SCXML 5.3: Initialize _sessionid
	_ = engine.SetVariable(sessionID, "_sessionid", sessionID)

	// W3C SCXML 5.3: Initialize _name
	_ = engine.SetVariable(sessionID, "_name", "typed_reader_names")

	// W3C SCXML 5.2.2: Initialize global datamodel variables
	// W3C SCXML 5.2/5.3: Initialize box from expr="1"
	{
		result, err := engine.EvaluateExpression(sessionID, `1`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "box", result)
		} else {
			eng.Raise(sce.NewPlatformError(TypedReaderNamesEventErrorExecution, "<data id='box'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "box", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize object from expr="2"
	{
		result, err := engine.EvaluateExpression(sessionID, `2`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "object", result)
		} else {
			eng.Raise(sce.NewPlatformError(TypedReaderNamesEventErrorExecution, "<data id='object'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "object", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize pass from expr="3"
	{
		result, err := engine.EvaluateExpression(sessionID, `3`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "pass", result)
		} else {
			eng.Raise(sce.NewPlatformError(TypedReaderNamesEventErrorExecution, "<data id='pass'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "pass", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize screen-rules from expr="4"
	{
		result, err := engine.EvaluateExpression(sessionID, `4`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "screen-rules", result)
		} else {
			eng.Raise(sce.NewPlatformError(TypedReaderNamesEventErrorExecution, "<data id='screen-rules'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "screen-rules", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize auto from expr="5"
	{
		result, err := engine.EvaluateExpression(sessionID, `5`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "auto", result)
		} else {
			eng.Raise(sce.NewPlatformError(TypedReaderNamesEventErrorExecution, "<data id='auto'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "auto", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize self from expr="6"
	{
		result, err := engine.EvaluateExpression(sessionID, `6`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "self", result)
		} else {
			eng.Raise(sce.NewPlatformError(TypedReaderNamesEventErrorExecution, "<data id='self'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "self", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize new from expr="7"
	{
		result, err := engine.EvaluateExpression(sessionID, `7`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "new", result)
		} else {
			eng.Raise(sce.NewPlatformError(TypedReaderNamesEventErrorExecution, "<data id='new'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "new", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize start from expr="8"
	{
		result, err := engine.EvaluateExpression(sessionID, `8`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "start", result)
		} else {
			eng.Raise(sce.NewPlatformError(TypedReaderNamesEventErrorExecution, "<data id='start'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "start", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize t from expr="9"
	{
		result, err := engine.EvaluateExpression(sessionID, `9`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "t", result)
		} else {
			eng.Raise(sce.NewPlatformError(TypedReaderNamesEventErrorExecution, "<data id='t'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "t", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize a_b from expr="10"
	{
		result, err := engine.EvaluateExpression(sessionID, `10`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "a_b", result)
		} else {
			eng.Raise(sce.NewPlatformError(TypedReaderNamesEventErrorExecution, "<data id='a_b'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "a_b", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize a-b from expr="11"
	{
		result, err := engine.EvaluateExpression(sessionID, `11`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "a-b", result)
		} else {
			eng.Raise(sce.NewPlatformError(TypedReaderNamesEventErrorExecution, "<data id='a-b'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "a-b", nil)
		}
	}



	// §scxml-6.4.3: apply the invoke params the parent staged — overriding the
	// `<data>` default this block just seeded, and only for a name the child
	// declares ("if the names do not match, the Processor MUST NOT add the
	// value ... to the invoked session's data model"). `HasVariable` is that
	// filter, and it is this channel's only copy of the rule.
	if len(p.pendingParams) > 0 {
		for paramName, paramValue := range p.pendingParams {
			if engine.HasVariable(sessionID, paramName) {
				_ = engine.SetVariable(sessionID, paramName, paramValue)
			}
		}
		p.pendingParams = nil
	}
}

// evaluateGuard evaluates a guard condition via script engine.
// W3C SCXML 4.3.2: If a condition expression cannot be evaluated, the processor
// MUST treat it as false and MUST raise error.execution.
func (p *TypedReaderNamesPolicy) evaluateGuard(guard string, eng *sce.Engine[TypedReaderNamesState, TypedReaderNamesEvent]) bool {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	result, err := engine.EvaluateExpression(p.SessionID, guard)
	if err != nil {
		eng.Raise(sce.NewPlatformError(TypedReaderNamesEventErrorExecution, "a <transition> cond failed to evaluate"))
		return false
	}
	return sce.ScriptToBool(result)
}

// executeScript executes a script block via script engine.
func (p *TypedReaderNamesPolicy) executeScript(script string) error {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	return engine.ExecuteScript(p.SessionID, script)
}

// setCurrentEvent binds _event system variable (W3C SCXML 5.10) and answers
// which rung of §scxml-B-2-8-1 the payload got, because the binding is the
// only place that knows.
//
// This used to be `_ = engine.SetCurrentEvent(...)`: the ladder decided
// between a DOM, a value and a space-normalized string, and the decision was
// dropped one line later. A payload that announced structure and would not
// parse therefore reached the document as raw characters, every
// _event.data.<field> read empty, and nothing anywhere could say so — see
// sce.PayloadReading.
func (p *TypedReaderNamesPolicy) setCurrentEvent(name string) sce.PayloadReading {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	data := p.pendingEventData
	eventType := p.pendingEventType
	sendID := p.pendingEventSendid
	// W3C SCXML C.1: `_event.origin` is the sender's published
	// `_ioprocessors` location, not its bare session id — and this is the one
	// place that publishes `_event` to the document, so this is where the id
	// becomes a location. The engine keeps the bare id in
	// `EventMetadata.Origin` because its session-keyed lookups (<finalize>
	// dispatch, cancelled-invoke filtering) match on it; converting at the
	// raise would make one value serve two consumers that need different
	// spellings. The conversion itself lives in `sce.PublishedOrigin`, the
	// port of the `IOProcessorHelper::publishedOrigin` the C++ engines share:
	// a second spelling of the rule is how the backends would stop agreeing.
	origin := sce.PublishedOrigin(p.pendingEventOrigin)
	originType := p.pendingEventOrigintype
	invokeID := p.pendingEventInvokeid
	reading, err := engine.SetCurrentEvent(p.SessionID, sce.SetCurrentEventArgs{
		Name:       name,
		Data:       data,
		Type:       eventType,
		SendID:     sendID,
		Origin:     origin,
		OriginType: originType,
		InvokeID:   invokeID,
	})
	if err != nil {
		// An engine that could not bind _event at all has a larger problem
		// than an unreadable payload, and reporting PayloadUndecodable for it
		// would point a host at the wrong thing.
		return sce.PayloadAbsent
	}
	return reading
}

// assignVariable assigns a value to a datamodel variable via script engine.
func (p *TypedReaderNamesPolicy) assignVariable(location, expr string) error {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	script := location + " = " + expr
	return engine.ExecuteScript(p.SessionID, script)
}

// getVariable reads a datamodel variable from the script engine.
func (p *TypedReaderNamesPolicy) getVariable(name string) interface{} {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	result, err := engine.GetVariable(p.SessionID, name)
	if err != nil {
		return nil
	}
	return result
}

// SetParamValueInScriptEngine stores an invoke param's VALUE for deferred
// application after datamodel init (§scxml-6.4.3).
//
// Params are applied at the end of InitializeDataModel, overriding the
// `<data>` default only for names the child declares. The value crosses as a
// value: this took the parent's value rendered as Lua source and
// re-evaluated it here until 2026-08-21, which survived every value a test
// had tried and lost the ones Lua cannot spell — `ToScriptLiteral` renders a
// non-finite number as `+Inf`, which is not a Lua expression, so an
// `<invoke>` `<param expr="1/0"/>` arrived as nothing at all.
func (p *TypedReaderNamesPolicy) SetParamValueInScriptEngine(name string, value interface{}) {
	if p.pendingParams == nil {
		p.pendingParams = make(map[string]interface{})
	}
	p.pendingParams[name] = value
}


// ExecutePendingInvokes is a no-op (no invokes in this SM).
func (p *TypedReaderNamesPolicy) ExecutePendingInvokes(engine *sce.Engine[TypedReaderNamesState, TypedReaderNamesEvent]) {}

// TickChildren is a no-op (no invokes in this SM).
func (p *TypedReaderNamesPolicy) TickChildren(engine *sce.Engine[TypedReaderNamesState, TypedReaderNamesEvent]) {}

// ======================================================================
// StatePolicy interface implementation
// ======================================================================

// InitialState returns the initial state (W3C SCXML 3.3).
func (p *TypedReaderNamesPolicy) InitialState() TypedReaderNamesState {
	return TypedReaderNamesStateIdle
}

// IsFinalState returns true if state is a <final> state (W3C SCXML 3.7).
func (p *TypedReaderNamesPolicy) IsFinalState(state TypedReaderNamesState) bool {
	switch state {
	case TypedReaderNamesStateDone:
		return true
	}
	return false
}

// GetParent returns the parent of state, or (zero, false) if root (W3C SCXML 3.3).
func (p *TypedReaderNamesPolicy) GetParent(state TypedReaderNamesState) (TypedReaderNamesState, bool) {
	switch state {
	}
	return 0, false
}

// IsCompoundState returns true if state is a <state> with child states — exactly
// the states that have an initial transition. A <parallel> is not compound
// (W3C SCXML 3.3).
func (p *TypedReaderNamesPolicy) IsCompoundState(state TypedReaderNamesState) bool {
	return len(initialTargetsOfTypedReaderNames[state]) > 0
}

func (p *TypedReaderNamesPolicy) IsParallelState(_ TypedReaderNamesState) bool { return false }

// GetChildStates returns state's <state>, <parallel> and <final> children, in
// document order — for a <parallel>, its regions (§scxml-D-getChildStates).
func (p *TypedReaderNamesPolicy) GetChildStates(state TypedReaderNamesState) []TypedReaderNamesState {
	return childStatesOfTypedReaderNames[state]
}

// GetInitialTargets returns a compound state's initial transition target, as
// written; the engine's entry procedures dereference a <history> among them
// (W3C SCXML 3.3).
func (p *TypedReaderNamesPolicy) GetInitialTargets(state TypedReaderNamesState) []TypedReaderNamesTarget {
	return initialTargetsOfTypedReaderNames[state]
}

// GetDocumentInitialTargets returns the target of the document's own initial
// transition, as written (W3C SCXML 3.2).
func (p *TypedReaderNamesPolicy) GetDocumentInitialTargets() []TypedReaderNamesTarget {
	return documentInitialTargetsOfTypedReaderNames
}

// W3C SCXML 3.10: this document declares no <history>, so no target list names
// one and the engine never asks the two below; answering would mean inventing
// one.
func (p *TypedReaderNamesPolicy) GetHistoryParent(history sce.HistoryID) TypedReaderNamesState {
	panic(fmt.Sprintf("TypedReaderNamesPolicy declares no <history>; asked for %d", history))
}
func (p *TypedReaderNamesPolicy) GetHistoryDefaultTargets(history sce.HistoryID) []TypedReaderNamesTarget {
	panic(fmt.Sprintf("TypedReaderNamesPolicy declares no <history>; asked for %d", history))
}
func (p *TypedReaderNamesPolicy) HistoryValue(_ sce.HistoryID) ([]TypedReaderNamesState, bool) {
	return nil, false
}

// GetDocumentOrder returns the document order index (W3C SCXML Appendix D).
func (p *TypedReaderNamesPolicy) GetDocumentOrder(state TypedReaderNamesState) int {
	switch state {
	case TypedReaderNamesStateDone:
		return 1
	case TypedReaderNamesStateIdle:
		return 0
	}
	return -1
}

// GetEventName returns the string name of an event (W3C SCXML 3.12).
func (p *TypedReaderNamesPolicy) GetEventName(event TypedReaderNamesEvent) string {
	return event.String()
}

// GetEventFromName looks up an event by name (W3C SCXML 3.12).
func (p *TypedReaderNamesPolicy) GetEventFromName(name string) (TypedReaderNamesEvent, bool) {
	switch name {
	case "bump":
		return TypedReaderNamesEventBump, true
	case "error.execution":
		return TypedReaderNamesEventErrorExecution, true
	case "finish":
		return TypedReaderNamesEventFinish, true
	}
	return TypedReaderNamesEventNull, false
}

// GetStateName returns the string name of a state (W3C SCXML 3.3).
func (p *TypedReaderNamesPolicy) GetStateName(state TypedReaderNamesState) string {
	return state.String()
}

// GetStateFromName reads a state id back into the state it names (W3C SCXML 3.3).
// The reverse of GetStateName, and what turns a host's recorded configuration
// back into the argument sce.Engine.EnterAt takes.
func (p *TypedReaderNamesPolicy) GetStateFromName(name string) (TypedReaderNamesState, bool) {
	return TypedReaderNamesStateFromName(name)
}

// NullEvent returns the sentinel for eventless transition dispatch (W3C SCXML 3.13).
func (p *TypedReaderNamesPolicy) NullEvent() TypedReaderNamesEvent {
	return TypedReaderNamesEventNull
}


// SetNextEventIsExternal sets the external event flag (W3C SCXML 5.10.1).
func (p *TypedReaderNamesPolicy) SetNextEventIsExternal(value bool) {
	p.nextEventIsExternal = value
}

// HasParallelStates returns whether the SM has parallel states.
func (p *TypedReaderNamesPolicy) HasParallelStates() bool {
	return false
}

// NeedsScriptEngine returns whether the SM needs a script engine.
func (p *TypedReaderNamesPolicy) NeedsScriptEngine() bool {
	return true
}

// NeedsEventScheduler reports whether a host must drive this machine with
// Tick rather than Step alone.
//
// W3C SCXML 6.2: Step runs a macrostep and never drains the delayed-send
// scheduler nor ticks an invoked child, so a host that only ever calls it
// waits forever with nothing said. Same verdict the generate manifest
// publishes as needs_event_scheduler.
func (p *TypedReaderNamesPolicy) NeedsEventScheduler() bool {
	return false
}

// NeedsDataModelInit returns whether the SM needs datamodel init.
func (p *TypedReaderNamesPolicy) NeedsDataModelInit() bool {
	return true
}

func (p *TypedReaderNamesPolicy) HasInvokeSupport() bool { return false }
func (p *TypedReaderNamesPolicy) HasChildTick() bool { return false }
func (p *TypedReaderNamesPolicy) HasFinalize() bool { return false }
func (p *TypedReaderNamesPolicy) HasAutoforward() bool { return false }
func (p *TypedReaderNamesPolicy) HasActiveStates() bool { return false }
func (p *TypedReaderNamesPolicy) GetActiveStates() []TypedReaderNamesState { return nil }
// W3C SCXML 3.4: this machine keeps no active set — its configuration is the
// parent walk from the current state — so there is nothing for a restore to
// hand back here. sce.Engine.EnterAt reaches this only through HasActiveStates,
// which is false above; the method exists because the interface is one contract.
func (p *TypedReaderNamesPolicy) SetActiveStates(_ []TypedReaderNamesState) {}
func (p *TypedReaderNamesPolicy) HasExternalEventFlag() bool { return true }
// ExecuteFinalizeForChildEvent is a no-op (no finalize invokes).
func (p *TypedReaderNamesPolicy) ExecuteFinalizeForChildEvent(_ *sce.EventWithMetadata[TypedReaderNamesEvent], _ *sce.Engine[TypedReaderNamesState, TypedReaderNamesEvent]) {}
// ForwardToAutoforwardChildren is a no-op (no autoforward invokes).
func (p *TypedReaderNamesPolicy) ForwardToAutoforwardChildren(_ string, _ sce.EventMetadata, _ *sce.Engine[TypedReaderNamesState, TypedReaderNamesEvent]) {}

// PopulateEventMetadata stores pending event metadata (W3C SCXML 5.10).
// Note: event name is set separately via setCurrentEvent(), not from metadata.
func (p *TypedReaderNamesPolicy) PopulateEventMetadata(meta *sce.EventMetadata) {
	p.pendingEventData = meta.Data
	p.pendingEventType = meta.EventType.String()
	p.pendingEventSendid = meta.SendID
	p.pendingEventOrigin = meta.Origin
	p.pendingEventOrigintype = meta.OriginType
	p.pendingEventInvokeid = meta.InvokeID
}

// LiftTypedPayload binds the dequeued event's typed `_event.data` view from
// the data it carries (NL→IR Item C1 Path A).
//
// The typed carrier PopulateEventMetadata reads is filled by one producer, the
// generated Raise<Event> inject seam. Every other producer — <send> with
// <param>, an invoke forwarding either way, autoforward, BasicHTTP, mesh —
// fills EventMetadata.Data, so the schema's fields are read out of that here.
// A refusal is returned for the engine to raise as error.execution, which is
// what W3C SCXML 3.13 gives for a guard that cannot be evaluated.
func (p *TypedReaderNamesPolicy) LiftTypedPayload(event TypedReaderNamesEvent, meta *sce.EventMetadata) error {
	// This document lowered no typed guard, so no event carries a typed view.
	return nil
}

// ClearEventMetadata resets pending event metadata (W3C SCXML 5.10).
func (p *TypedReaderNamesPolicy) ClearEventMetadata() {
	p.pendingEventName = ""
	p.pendingEventData = ""
	p.pendingEventType = ""
	p.pendingEventSendid = ""
	p.pendingEventOrigin = ""
	p.pendingEventOrigintype = ""
	p.pendingEventInvokeid = ""
}



// ExecuteEntryActions enters one state (W3C SCXML 3.8): adds it to the
// configuration, runs its <onentry>, and its <initial> transition's content when
// its initial state is entered by default.
//line typed_reader_names.scxml:31
func (p *TypedReaderNamesPolicy) ExecuteEntryActions(state TypedReaderNamesState, engine *sce.Engine[TypedReaderNamesState, TypedReaderNamesEvent], isDefaultEntry bool) {
	p.ensureScriptEngine()
	switch state {
	default:
		// No entry actions
	}
}

// ExecuteHistoryDefaultContent runs a <history>'s default transition content
// (W3C SCXML 3.10.2), after its parent's onentry (and after the parent's own
// <initial> content) when the history was taken with nothing recorded. The
// engine asks for it by the entry set's defaultHistoryContent answer; a history
// that restored what it recorded runs nothing.
//line typed_reader_names.scxml:31
func (p *TypedReaderNamesPolicy) ExecuteHistoryDefaultContent(history sce.HistoryID, engine *sce.Engine[TypedReaderNamesState, TypedReaderNamesEvent]) {
	// W3C SCXML 3.10.2: no <history> in this document has default content.
}

// ExecuteExitActions exits one state (W3C SCXML 3.9): records its histories,
// runs its <onexit>, cancels its invocations and removes it from the
// configuration — §scxml-D-exitStates's order.
//line typed_reader_names.scxml:31
func (p *TypedReaderNamesPolicy) ExecuteExitActions(state TypedReaderNamesState, engine *sce.Engine[TypedReaderNamesState, TypedReaderNamesEvent], configurationBeforeExit []TypedReaderNamesState) {
	p.ensureScriptEngine()
	// §scxml-D-exitStates orders one state's exit as onexit, then
	// cancelInvoke, then configuration.delete(s), so `In(s)` inside s's own
	// handler is true: the <onexit> switch comes first and the removal from
	// the configuration last.
	switch state {
	default:
		// No exit actions
	}
}



// BindCurrentEvent binds the event whose transitions are about to be selected as
// the _event their guards read (W3C SCXML 5.10) — before the first guard runs,
// and not for an eventless selection, which has no event of its own.
//line typed_reader_names.scxml:31
func (p *TypedReaderNamesPolicy) BindCurrentEvent(event TypedReaderNamesEvent, engine *sce.Engine[TypedReaderNamesState, TypedReaderNamesEvent]) {
	if event != TypedReaderNamesEventNull {
		// §scxml-B-2-8-1: the rung the payload got, handed to the engine
		// rather than dropped. This is the only frame that has both the
		// reading and the event it belongs to.
		engine.NotePayloadReading(event, p.setCurrentEvent(p.GetEventName(event)))
	}
}

// FirstEnabledTransition is Appendix D selectTransitions, the half only this
// document can answer: the first of state's own transitions, in document order,
// that event enables and whose guard holds. The engine walks the atomic states
// and their ancestors and keeps the ordered set; the null event asks for
// eventless transitions.
//line typed_reader_names.scxml:31
func (p *TypedReaderNamesPolicy) FirstEnabledTransition(state TypedReaderNamesState, event TypedReaderNamesEvent, engine *sce.Engine[TypedReaderNamesState, TypedReaderNamesEvent]) (sce.EnabledTransition[TypedReaderNamesState, sce.HistoryID], bool) {
	switch state {
	case TypedReaderNamesStateIdle:
		if event == TypedReaderNamesEventBump {
			{
				return sce.EnabledTransition[TypedReaderNamesState, sce.HistoryID]{
					Source:          state,
					TransitionIndex: 0,
					HasActions:      true,
					IsInternal:      false,
				}, true
			}
		}
		if event == TypedReaderNamesEventFinish {
			{
				return sce.EnabledTransition[TypedReaderNamesState, sce.HistoryID]{
					Source:          state,
					Targets:         transitionTargetsOfTypedReaderNames[state][1],
					TransitionIndex: 1,
					HasActions:      false,
					IsInternal:      false,
				}, true
			}
		}
	}
	return sce.EnabledTransition[TypedReaderNamesState, sce.HistoryID]{}, false
}

// ExecuteTransitionContent runs one transition's executable content (W3C SCXML
// 3.13), between the microstep's exits and its entries.
//line typed_reader_names.scxml:31
func (p *TypedReaderNamesPolicy) ExecuteTransitionContent(source TypedReaderNamesState, transitionIndex int, engine *sce.Engine[TypedReaderNamesState, TypedReaderNamesEvent]) {
	p.ensureScriptEngine()
	switch source {
	case TypedReaderNamesStateIdle:
		switch transitionIndex {
		case 0:
			//line typed_reader_names.scxml:49

	// W3C SCXML 5.3: <assign location="box" expr="box + 10">
	if err := p.assignVariable(`box`, `_scxml_add(box, 10)`); err != nil {
		engine.Raise(sce.NewPlatformError(TypedReaderNamesEventErrorExecution, "<assign> to 'box' failed"))
	}


	// W3C SCXML 5.3: <assign location="object" expr="object + 10">
	if err := p.assignVariable(`object`, `_scxml_add(object, 10)`); err != nil {
		engine.Raise(sce.NewPlatformError(TypedReaderNamesEventErrorExecution, "<assign> to 'object' failed"))
	}


	// W3C SCXML 5.3: <assign location="pass" expr="pass + 10">
	if err := p.assignVariable(`pass`, `_scxml_add(pass, 10)`); err != nil {
		engine.Raise(sce.NewPlatformError(TypedReaderNamesEventErrorExecution, "<assign> to 'pass' failed"))
	}


	// W3C SCXML 5.3: <assign location="a_b" expr="a_b + 10">
	if err := p.assignVariable(`a_b`, `_scxml_add(a_b, 10)`); err != nil {
		engine.Raise(sce.NewPlatformError(TypedReaderNamesEventErrorExecution, "<assign> to 'a_b' failed"))
	}

		}
	}
}
