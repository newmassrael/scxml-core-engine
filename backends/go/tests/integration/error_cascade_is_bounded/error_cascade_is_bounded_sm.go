// SCE-GENERATED — DO NOT EDIT
// source-hash: 4731a6ba40787ab928e39e6fce63f290cd233b0d7081f439713483c0324e40fe
// template-hash: 18f91ed61fbcea991ce606d44d6910fe1df7095e74ca3c5a78065a37d763c7a4
// generated-at: 0


// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) 2025-2026 [Author of input SCXML file]
//
// Generated by SCE Code Generator (Go backend)
// From: integration_resources/error_cascade_is_bounded/error_cascade_is_bounded.scxml
//
// This generated code is MIT licensed and may be freely used in any project.
// Runtime engine dependency (sce-go-runtime): LGPL-2.1-or-later WITH LicenseRef-SCE-Linking-Exception OR LicenseRef-SCE-Commercial
// Full license: https://github.com/newmassrael/scxml-core-engine/blob/main/LICENSE
// Generated Go state machine for `error_cascade_is_bounded`.
//
// Generated by SCE Code Generator (Go backend).
// Implements sce.StatePolicy with full transition logic,
// entry/exit actions, and event processing.


// SCE-MAP: error_cascade_is_bounded.scxml:45 :: _machine

package error_cascade_is_bounded

import (
	"fmt"
	"path/filepath"
	"strings"
	"time"

	sce "github.com/newmassrael/sce-go-runtime"
)

// Ensure imports are used
var (
	_ = fmt.Sprintf
	_ = filepath.Join
	_ = strings.HasPrefix
	_ = time.Duration(0)
)

// ======================================================================
// State type (W3C SCXML 3.3)
// ======================================================================

type ErrorCascadeIsBoundedState int

const (
	ErrorCascadeIsBoundedStateIdle ErrorCascadeIsBoundedState = 0
	ErrorCascadeIsBoundedStateRunaway ErrorCascadeIsBoundedState = 1
	ErrorCascadeIsBoundedStateSettling ErrorCascadeIsBoundedState = 2
)

func (s ErrorCascadeIsBoundedState) String() string {
	switch s {
	case ErrorCascadeIsBoundedStateIdle:
		return "idle"
	case ErrorCascadeIsBoundedStateRunaway:
		return "runaway"
	case ErrorCascadeIsBoundedStateSettling:
		return "settling"
	}
	return "unknown"
}

// ======================================================================
// Event type (W3C SCXML 3.12)
// ======================================================================

type ErrorCascadeIsBoundedEvent int

const (
	ErrorCascadeIsBoundedEventBoom ErrorCascadeIsBoundedEvent = 0
	ErrorCascadeIsBoundedEventErrorExecution ErrorCascadeIsBoundedEvent = 1
	ErrorCascadeIsBoundedEventPoke ErrorCascadeIsBoundedEvent = 2
	ErrorCascadeIsBoundedEventReset ErrorCascadeIsBoundedEvent = 3
	ErrorCascadeIsBoundedEventSettle ErrorCascadeIsBoundedEvent = 4
	ErrorCascadeIsBoundedEventSpin ErrorCascadeIsBoundedEvent = 5
	ErrorCascadeIsBoundedEventTick ErrorCascadeIsBoundedEvent = 6
	// W3C SCXML 3.13: Sentinel for eventless transition dispatch
	ErrorCascadeIsBoundedEventNull ErrorCascadeIsBoundedEvent = 7
)

func (e ErrorCascadeIsBoundedEvent) String() string {
	switch e {
	case ErrorCascadeIsBoundedEventBoom:
		return "boom"
	case ErrorCascadeIsBoundedEventErrorExecution:
		return "error.execution"
	case ErrorCascadeIsBoundedEventPoke:
		return "poke"
	case ErrorCascadeIsBoundedEventReset:
		return "reset"
	case ErrorCascadeIsBoundedEventSettle:
		return "settle"
	case ErrorCascadeIsBoundedEventSpin:
		return "spin"
	case ErrorCascadeIsBoundedEventTick:
		return "tick"
	case ErrorCascadeIsBoundedEventNull:
		return ""
	}
	return "unknown"
}

// ======================================================================
// Policy struct
// ======================================================================

type ErrorCascadeIsBoundedPolicy struct {
	// W3C SCXML 3.13: Last transition metadata
	lastTransitionIsInternal  bool
	lastTransitionIsTargetless bool
	lastTransitionSourceState ErrorCascadeIsBoundedState
	// W3C SCXML 3.13: Transition action tracking
	lastTransitionIndex   int
	hasTransitionActions   bool
	// W3C SCXML 5.10.1: External event flag
	nextEventIsExternal bool
	pendingEventName string
	pendingEventData string
	pendingEventType string
	pendingEventSendid string
	pendingEventOrigin string
	pendingEventOrigintype string
	pendingEventInvokeid string
	// W3C SCXML 5.10: Session ID
	SessionID string
	// W3C SCXML B.1: Script engine handle (per-instance, Path B+ Q1=(d) Go=interface ref)
	ScriptEngine sce.IScriptEngine
	scriptEngineInitialized bool
	// W3C SCXML C.2.3: inbound BasicHTTP endpoint serving this machine,
	// declared by the deployment before Initialize(). The address belongs to
	// whoever runs the listener, so the machine takes it from here rather than
	// guessing one. Empty means no such endpoint is deployed, and no BasicHTTP
	// entry is published in _ioprocessors.
	BasicHTTPAccessURI string
	// W3C SCXML 6.4: Parent communication
	ParentExternalQueue chan sce.ParentEvent
	InvokeID           string
	ChildSessionID     string
	// §scxml-6.4.3: invoke param VALUES staged by the parent, applied after
	// this machine's datamodel init so they override its `<data>` defaults.
	pendingParams      map[string]interface{}
}

// NewErrorCascadeIsBoundedPolicy creates a new policy with default values.
func NewErrorCascadeIsBoundedPolicy() ErrorCascadeIsBoundedPolicy {
	return ErrorCascadeIsBoundedPolicy{
		lastTransitionSourceState: ErrorCascadeIsBoundedStateIdle,
	}
}

// Pokes reports what the `pokes` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `pokes` was assigned a value of another type, or the engine refused.
func (p *ErrorCascadeIsBoundedPolicy) Pokes() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "pokes")
}

// Repairs reports what the `repairs` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `repairs` was assigned a value of another type, or the engine refused.
func (p *ErrorCascadeIsBoundedPolicy) Repairs() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "repairs")
}

// Runs reports what the `runs` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `runs` was assigned a value of another type, or the engine refused.
func (p *ErrorCascadeIsBoundedPolicy) Runs() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "runs")
}

// Ticks reports what the `ticks` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `ticks` was assigned a value of another type, or the engine refused.
func (p *ErrorCascadeIsBoundedPolicy) Ticks() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "ticks")
}




// ensureScriptEngine lazily initializes the script engine session.
//
// Engine DI Parity RFC: panics if ScriptEngine was not set on the policy
// before Initialize() — mirrors Kotlin's mandatory constructor parameter
// contract. Silent no-op would otherwise turn missed injection into a
// hidden runtime regression (every <assign>/<log>/guard becomes a no-op
// and the SM walks to its default-state final without diagnostic).
func (p *ErrorCascadeIsBoundedPolicy) ensureScriptEngine() {
	if p.scriptEngineInitialized {
		return
	}
	engine := p.ScriptEngine
	if engine == nil {
		panic("ErrorCascadeIsBoundedPolicy: ScriptEngine field must be set before Initialize()")
	}
	sessionID := p.SessionID
	if sessionID == "" {
		sessionID = sce.GenerateSessionID()
		p.SessionID = sessionID
	}
	if !engine.HasSession(sessionID) {
		_ = engine.CreateSession(sessionID)
	}
	// W3C SCXML C.1.1 / C.2.3: the _ioprocessors entries come from the same
	// helper every other backend uses, so a machine reads the same entry names
	// and the same addresses whichever one runs it.
	_ = engine.SetupSystemVariables(sessionID, "error_cascade_is_bounded",
		sce.BuildIoProcessors(sessionID, p.BasicHTTPAccessURI))
	p.scriptEngineInitialized = true
}

// InitializeDataModel sets up datamodel via script engine (W3C SCXML 5.3).
func (p *ErrorCascadeIsBoundedPolicy) InitializeDataModel(eng *sce.Engine[ErrorCascadeIsBoundedState, ErrorCascadeIsBoundedEvent]) {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	sessionID := p.SessionID

	// W3C SCXML 5.3: Initialize _sessionid
	_ = engine.SetVariable(sessionID, "_sessionid", sessionID)

	// W3C SCXML 5.3: Initialize _name
	_ = engine.SetVariable(sessionID, "_name", "error_cascade_is_bounded")

	// W3C SCXML 5.2.2: Initialize global datamodel variables
	// W3C SCXML 5.2/5.3: Initialize pokes from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "pokes", result)
		} else {
			eng.Raise(sce.NewPlatformError(ErrorCascadeIsBoundedEventErrorExecution, "<data id='pokes'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "pokes", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize repairs from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "repairs", result)
		} else {
			eng.Raise(sce.NewPlatformError(ErrorCascadeIsBoundedEventErrorExecution, "<data id='repairs'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "repairs", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize runs from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "runs", result)
		} else {
			eng.Raise(sce.NewPlatformError(ErrorCascadeIsBoundedEventErrorExecution, "<data id='runs'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "runs", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize ticks from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "ticks", result)
		} else {
			eng.Raise(sce.NewPlatformError(ErrorCascadeIsBoundedEventErrorExecution, "<data id='ticks'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "ticks", nil)
		}
	}



	// §scxml-6.4.3: apply the invoke params the parent staged — overriding the
	// `<data>` default this block just seeded, and only for a name the child
	// declares ("if the names do not match, the Processor MUST NOT add the
	// value ... to the invoked session's data model"). `HasVariable` is that
	// filter, and it is this channel's only copy of the rule.
	if len(p.pendingParams) > 0 {
		for paramName, paramValue := range p.pendingParams {
			if engine.HasVariable(sessionID, paramName) {
				_ = engine.SetVariable(sessionID, paramName, paramValue)
			}
		}
		p.pendingParams = nil
	}
}

// evaluateGuard evaluates a guard condition via script engine.
// W3C SCXML 4.3.2: If a condition expression cannot be evaluated, the processor
// MUST treat it as false and MUST raise error.execution.
func (p *ErrorCascadeIsBoundedPolicy) evaluateGuard(guard string, eng *sce.Engine[ErrorCascadeIsBoundedState, ErrorCascadeIsBoundedEvent]) bool {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	result, err := engine.EvaluateExpression(p.SessionID, guard)
	if err != nil {
		eng.Raise(sce.NewPlatformError(ErrorCascadeIsBoundedEventErrorExecution, "a <transition> cond failed to evaluate"))
		return false
	}
	return sce.ScriptToBool(result)
}

// executeScript executes a script block via script engine.
func (p *ErrorCascadeIsBoundedPolicy) executeScript(script string) error {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	return engine.ExecuteScript(p.SessionID, script)
}

// setCurrentEvent binds _event system variable (W3C SCXML 5.10) and answers
// which rung of §scxml-B-2-8-1 the payload got, because the binding is the
// only place that knows.
//
// This used to be `_ = engine.SetCurrentEvent(...)`: the ladder decided
// between a DOM, a value and a space-normalized string, and the decision was
// dropped one line later. A payload that announced structure and would not
// parse therefore reached the document as raw characters, every
// _event.data.<field> read empty, and nothing anywhere could say so — see
// sce.PayloadReading.
func (p *ErrorCascadeIsBoundedPolicy) setCurrentEvent(name string) sce.PayloadReading {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	data := p.pendingEventData
	eventType := p.pendingEventType
	sendID := p.pendingEventSendid
	// W3C SCXML C.1: `_event.origin` is the sender's published
	// `_ioprocessors` location, not its bare session id — and this is the one
	// place that publishes `_event` to the document, so this is where the id
	// becomes a location. The engine keeps the bare id in
	// `EventMetadata.Origin` because its session-keyed lookups (<finalize>
	// dispatch, cancelled-invoke filtering) match on it; converting at the
	// raise would make one value serve two consumers that need different
	// spellings. The conversion itself lives in `sce.PublishedOrigin`, the
	// port of the `IOProcessorHelper::publishedOrigin` the C++ engines share:
	// a second spelling of the rule is how the backends would stop agreeing.
	origin := sce.PublishedOrigin(p.pendingEventOrigin)
	originType := p.pendingEventOrigintype
	invokeID := p.pendingEventInvokeid
	reading, err := engine.SetCurrentEvent(p.SessionID, sce.SetCurrentEventArgs{
		Name:       name,
		Data:       data,
		Type:       eventType,
		SendID:     sendID,
		Origin:     origin,
		OriginType: originType,
		InvokeID:   invokeID,
	})
	if err != nil {
		// An engine that could not bind _event at all has a larger problem
		// than an unreadable payload, and reporting PayloadUndecodable for it
		// would point a host at the wrong thing.
		return sce.PayloadAbsent
	}
	return reading
}

// assignVariable assigns a value to a datamodel variable via script engine.
func (p *ErrorCascadeIsBoundedPolicy) assignVariable(location, expr string) error {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	script := location + " = " + expr
	return engine.ExecuteScript(p.SessionID, script)
}

// getVariable reads a datamodel variable from the script engine.
func (p *ErrorCascadeIsBoundedPolicy) getVariable(name string) interface{} {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	result, err := engine.GetVariable(p.SessionID, name)
	if err != nil {
		return nil
	}
	return result
}

// SetParamValueInScriptEngine stores an invoke param's VALUE for deferred
// application after datamodel init (§scxml-6.4.3).
//
// Params are applied at the end of InitializeDataModel, overriding the
// `<data>` default only for names the child declares. The value crosses as a
// value: this took the parent's value rendered as Lua source and
// re-evaluated it here until 2026-08-21, which survived every value a test
// had tried and lost the ones Lua cannot spell — `ToScriptLiteral` renders a
// non-finite number as `+Inf`, which is not a Lua expression, so an
// `<invoke>` `<param expr="1/0"/>` arrived as nothing at all.
func (p *ErrorCascadeIsBoundedPolicy) SetParamValueInScriptEngine(name string, value interface{}) {
	if p.pendingParams == nil {
		p.pendingParams = make(map[string]interface{})
	}
	p.pendingParams[name] = value
}


// ExecutePendingInvokes is a no-op (no invokes in this SM).
func (p *ErrorCascadeIsBoundedPolicy) ExecutePendingInvokes(engine *sce.Engine[ErrorCascadeIsBoundedState, ErrorCascadeIsBoundedEvent]) {}

// TickChildren is a no-op (no invokes in this SM).
func (p *ErrorCascadeIsBoundedPolicy) TickChildren(engine *sce.Engine[ErrorCascadeIsBoundedState, ErrorCascadeIsBoundedEvent]) {}

// ======================================================================
// StatePolicy interface implementation
// ======================================================================

// InitialState returns the initial state (W3C SCXML 3.3).
func (p *ErrorCascadeIsBoundedPolicy) InitialState() ErrorCascadeIsBoundedState {
	return ErrorCascadeIsBoundedStateIdle
}

// IsFinalState returns true if state is a <final> state (W3C SCXML 3.7).
func (p *ErrorCascadeIsBoundedPolicy) IsFinalState(state ErrorCascadeIsBoundedState) bool {
	switch state {
	}
	return false
}

// GetParent returns the parent of state, or (zero, false) if root (W3C SCXML 3.3).
func (p *ErrorCascadeIsBoundedPolicy) GetParent(state ErrorCascadeIsBoundedState) (ErrorCascadeIsBoundedState, bool) {
	switch state {
	}
	return 0, false
}

// IsCompoundState returns true if state has children (W3C SCXML 3.3).
func (p *ErrorCascadeIsBoundedPolicy) IsCompoundState(state ErrorCascadeIsBoundedState) bool {
	switch state {
	}
	return false
}

func (p *ErrorCascadeIsBoundedPolicy) IsParallelState(_ ErrorCascadeIsBoundedState) bool { return false }
func (p *ErrorCascadeIsBoundedPolicy) GetParallelRegions(_ ErrorCascadeIsBoundedState) []ErrorCascadeIsBoundedState { return nil }

// IsDescendantOf returns true if desc is a descendant of anc (W3C SCXML 3.12).
func (p *ErrorCascadeIsBoundedPolicy) IsDescendantOf(desc, anc ErrorCascadeIsBoundedState) bool {
	current := desc
	for {
		parent, ok := p.GetParent(current)
		if !ok {
			return false
		}
		if parent == anc {
			return true
		}
		current = parent
	}
}

// GetDocumentOrder returns the document order index (W3C SCXML Appendix D).
func (p *ErrorCascadeIsBoundedPolicy) GetDocumentOrder(state ErrorCascadeIsBoundedState) int {
	switch state {
	case ErrorCascadeIsBoundedStateIdle:
		return 0
	case ErrorCascadeIsBoundedStateRunaway:
		return 2
	case ErrorCascadeIsBoundedStateSettling:
		return 1
	}
	return -1
}

// GetEventName returns the string name of an event (W3C SCXML 3.12).
func (p *ErrorCascadeIsBoundedPolicy) GetEventName(event ErrorCascadeIsBoundedEvent) string {
	return event.String()
}

// GetEventFromName looks up an event by name (W3C SCXML 3.12).
func (p *ErrorCascadeIsBoundedPolicy) GetEventFromName(name string) (ErrorCascadeIsBoundedEvent, bool) {
	switch name {
	case "boom":
		return ErrorCascadeIsBoundedEventBoom, true
	case "error.execution":
		return ErrorCascadeIsBoundedEventErrorExecution, true
	case "poke":
		return ErrorCascadeIsBoundedEventPoke, true
	case "reset":
		return ErrorCascadeIsBoundedEventReset, true
	case "settle":
		return ErrorCascadeIsBoundedEventSettle, true
	case "spin":
		return ErrorCascadeIsBoundedEventSpin, true
	case "tick":
		return ErrorCascadeIsBoundedEventTick, true
	}
	return ErrorCascadeIsBoundedEventNull, false
}

// GetStateName returns the string name of a state (W3C SCXML 3.3).
func (p *ErrorCascadeIsBoundedPolicy) GetStateName(state ErrorCascadeIsBoundedState) string {
	return state.String()
}

// NullEvent returns the sentinel for eventless transition dispatch (W3C SCXML 3.13).
func (p *ErrorCascadeIsBoundedPolicy) NullEvent() ErrorCascadeIsBoundedEvent {
	return ErrorCascadeIsBoundedEventNull
}

// GetInitialChildren returns initial children of a compound state (W3C SCXML 3.6).
func (p *ErrorCascadeIsBoundedPolicy) GetInitialChildren(state ErrorCascadeIsBoundedState) []ErrorCascadeIsBoundedState {
	switch state {
	}
	return nil
}

// LastTransitionIsInternal returns the internal transition flag (W3C SCXML 3.13).
func (p *ErrorCascadeIsBoundedPolicy) LastTransitionIsInternal() bool {
	return p.lastTransitionIsInternal
}

// SetLastTransitionIsInternal sets the internal transition flag.
func (p *ErrorCascadeIsBoundedPolicy) SetLastTransitionIsInternal(value bool) {
	p.lastTransitionIsInternal = value
}

// LastTransitionIsTargetless returns the targetless transition flag (W3C SCXML 3.13).
func (p *ErrorCascadeIsBoundedPolicy) LastTransitionIsTargetless() bool {
	return p.lastTransitionIsTargetless
}

// SetLastTransitionIsTargetless sets the targetless transition flag.
func (p *ErrorCascadeIsBoundedPolicy) SetLastTransitionIsTargetless(value bool) {
	p.lastTransitionIsTargetless = value
}

// LastTransitionSourceState returns the source state of the last transition.
func (p *ErrorCascadeIsBoundedPolicy) LastTransitionSourceState() ErrorCascadeIsBoundedState {
	return p.lastTransitionSourceState
}

// SetLastTransitionSourceState sets the source state of the last transition.
func (p *ErrorCascadeIsBoundedPolicy) SetLastTransitionSourceState(state ErrorCascadeIsBoundedState) {
	p.lastTransitionSourceState = state
}


// SetNextEventIsExternal sets the external event flag (W3C SCXML 5.10.1).
func (p *ErrorCascadeIsBoundedPolicy) SetNextEventIsExternal(value bool) {
	p.nextEventIsExternal = value
}

// HasParallelStates returns whether the SM has parallel states.
func (p *ErrorCascadeIsBoundedPolicy) HasParallelStates() bool {
	return false
}

// NeedsScriptEngine returns whether the SM needs a script engine.
func (p *ErrorCascadeIsBoundedPolicy) NeedsScriptEngine() bool {
	return true
}

// NeedsEventScheduler reports whether a host must drive this machine with
// Tick rather than Step alone.
//
// W3C SCXML 6.2: Step runs a macrostep and never drains the delayed-send
// scheduler nor ticks an invoked child, so a host that only ever calls it
// waits forever with nothing said. Same verdict the generate manifest
// publishes as needs_event_scheduler.
func (p *ErrorCascadeIsBoundedPolicy) NeedsEventScheduler() bool {
	return false
}

// NeedsDataModelInit returns whether the SM needs datamodel init.
func (p *ErrorCascadeIsBoundedPolicy) NeedsDataModelInit() bool {
	return true
}

func (p *ErrorCascadeIsBoundedPolicy) HasInvokeSupport() bool { return false }
func (p *ErrorCascadeIsBoundedPolicy) HasChildTick() bool { return false }
func (p *ErrorCascadeIsBoundedPolicy) HasFinalize() bool { return false }
func (p *ErrorCascadeIsBoundedPolicy) HasAutoforward() bool { return false }
func (p *ErrorCascadeIsBoundedPolicy) HasActiveStates() bool { return false }
func (p *ErrorCascadeIsBoundedPolicy) GetActiveStates() []ErrorCascadeIsBoundedState { return nil }
func (p *ErrorCascadeIsBoundedPolicy) HasExternalEventFlag() bool { return true }
// GetInitialOrHistoryChild returns the initial child considering history (W3C SCXML 3.11).
func (p *ErrorCascadeIsBoundedPolicy) GetInitialOrHistoryChild(state ErrorCascadeIsBoundedState) ErrorCascadeIsBoundedState {
	children := p.GetInitialChildren(state)
	if len(children) > 0 {
		return children[0]
	}
	return state
}
// ExecuteFinalizeForChildEvent is a no-op (no finalize invokes).
func (p *ErrorCascadeIsBoundedPolicy) ExecuteFinalizeForChildEvent(_ *sce.EventWithMetadata[ErrorCascadeIsBoundedEvent], _ *sce.Engine[ErrorCascadeIsBoundedState, ErrorCascadeIsBoundedEvent]) {}
// ForwardToAutoforwardChildren is a no-op (no autoforward invokes).
func (p *ErrorCascadeIsBoundedPolicy) ForwardToAutoforwardChildren(_ string, _ sce.EventMetadata, _ *sce.Engine[ErrorCascadeIsBoundedState, ErrorCascadeIsBoundedEvent]) {}

// PopulateEventMetadata stores pending event metadata (W3C SCXML 5.10).
// Note: event name is set separately via setCurrentEvent(), not from metadata.
func (p *ErrorCascadeIsBoundedPolicy) PopulateEventMetadata(meta *sce.EventMetadata) {
	p.pendingEventData = meta.Data
	p.pendingEventType = meta.EventType.String()
	p.pendingEventSendid = meta.SendID
	p.pendingEventOrigin = meta.Origin
	p.pendingEventOrigintype = meta.OriginType
	p.pendingEventInvokeid = meta.InvokeID
}

// ClearEventMetadata resets pending event metadata (W3C SCXML 5.10).
func (p *ErrorCascadeIsBoundedPolicy) ClearEventMetadata() {
	p.pendingEventName = ""
	p.pendingEventData = ""
	p.pendingEventType = ""
	p.pendingEventSendid = ""
	p.pendingEventOrigin = ""
	p.pendingEventOrigintype = ""
	p.pendingEventInvokeid = ""
}




// ExecuteEntryActions executes onentry actions for a state (W3C SCXML 3.8).
//line error_cascade_is_bounded.scxml:45
func (p *ErrorCascadeIsBoundedPolicy) ExecuteEntryActions(state ErrorCascadeIsBoundedState, engine *sce.Engine[ErrorCascadeIsBoundedState, ErrorCascadeIsBoundedEvent], pathChild *ErrorCascadeIsBoundedState) {
	// Only a `<parallel>` machine descends into defaults here, so a machine
	// without one has nothing to tell an ancestor entry from a target entry.
	_ = pathChild
	p.ensureScriptEngine()
	switch state {
	case ErrorCascadeIsBoundedStateRunaway:
		//line error_cascade_is_bounded.scxml:103
		// W3C SCXML 3.8: onentry block 0 (break on error stops subsequent actions)
		for actionBlock0 := 0; actionBlock0 < 1; actionBlock0++ {
			_ = actionBlock0

	// W3C SCXML 5.3/B.2: Invalid or read-only location ""
	engine.Raise(sce.NewPlatformError(ErrorCascadeIsBoundedEventErrorExecution, "<assign> has an invalid or read-only location ''"))
	break

		}
	case ErrorCascadeIsBoundedStateSettling:
		//line error_cascade_is_bounded.scxml:85
		// W3C SCXML 3.8: onentry block 0 (break on error stops subsequent actions)
		for actionBlock0 := 0; actionBlock0 < 1; actionBlock0++ {
			_ = actionBlock0

	// W3C SCXML 5.3/B.2: Invalid or read-only location ""
	engine.Raise(sce.NewPlatformError(ErrorCascadeIsBoundedEventErrorExecution, "<assign> has an invalid or read-only location ''"))
	break

		}
	default:
		// No entry actions
	}
}

// ExecuteExitActions executes onexit actions for a state (W3C SCXML 3.9).
//line error_cascade_is_bounded.scxml:45
func (p *ErrorCascadeIsBoundedPolicy) ExecuteExitActions(state ErrorCascadeIsBoundedState, engine *sce.Engine[ErrorCascadeIsBoundedState, ErrorCascadeIsBoundedEvent], preTransitionActive []ErrorCascadeIsBoundedState) {
	p.ensureScriptEngine()
	switch state {
	default:
		// No exit actions
	}
}

// ProcessTransition evaluates guards and takes a matching transition (W3C SCXML 3.13).
// Returns true if a transition was taken.
//line error_cascade_is_bounded.scxml:45
func (p *ErrorCascadeIsBoundedPolicy) ProcessTransition(currentState *ErrorCascadeIsBoundedState, event ErrorCascadeIsBoundedEvent, engine *sce.Engine[ErrorCascadeIsBoundedState, ErrorCascadeIsBoundedEvent]) bool {
	// W3C SCXML 5.10: Bind _event system variable for guard evaluation
	if event != ErrorCascadeIsBoundedEventNull {
		// §scxml-B-2-8-1: the rung the payload got, handed to the engine
		// rather than dropped. This is the only frame that has both the
		// reading and the event it belongs to.
		engine.NotePayloadReading(event, p.setCurrentEvent(p.GetEventName(event)))
	}

	// W3C SCXML 3.12: Try transitions in current state first
	if p.tryTransitionInState(*currentState, event, currentState, engine) {
		return true
	}


	return false
}


// tryTransitionInState checks transitions for a single state.
//line error_cascade_is_bounded.scxml:45
func (p *ErrorCascadeIsBoundedPolicy) tryTransitionInState(checkState ErrorCascadeIsBoundedState, event ErrorCascadeIsBoundedEvent, currentState *ErrorCascadeIsBoundedState, engine *sce.Engine[ErrorCascadeIsBoundedState, ErrorCascadeIsBoundedEvent]) bool {
	switch checkState {
	case ErrorCascadeIsBoundedStateIdle:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == ErrorCascadeIsBoundedEventPoke {
			*currentState = ErrorCascadeIsBoundedStateIdle
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = ErrorCascadeIsBoundedStateIdle
			p.lastTransitionIndex = 0
			p.hasTransitionActions = true
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == ErrorCascadeIsBoundedEventBoom {
			*currentState = ErrorCascadeIsBoundedStateIdle
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = ErrorCascadeIsBoundedStateIdle
			p.lastTransitionIndex = 1
			p.hasTransitionActions = true
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == ErrorCascadeIsBoundedEventSettle {
			*currentState = ErrorCascadeIsBoundedStateSettling
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = ErrorCascadeIsBoundedStateIdle
			p.lastTransitionIndex = 2
			p.hasTransitionActions = false
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == ErrorCascadeIsBoundedEventSpin {
			*currentState = ErrorCascadeIsBoundedStateRunaway
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = ErrorCascadeIsBoundedStateIdle
			p.lastTransitionIndex = 3
			p.hasTransitionActions = false
			return true
		}
	case ErrorCascadeIsBoundedStateRunaway:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == ErrorCascadeIsBoundedEventErrorExecution {
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = true
			p.lastTransitionSourceState = ErrorCascadeIsBoundedStateRunaway
			p.lastTransitionIndex = 0
			p.hasTransitionActions = true
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == ErrorCascadeIsBoundedEventTick {
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = true
			p.lastTransitionSourceState = ErrorCascadeIsBoundedStateRunaway
			p.lastTransitionIndex = 1
			p.hasTransitionActions = true
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == ErrorCascadeIsBoundedEventPoke {
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = true
			p.lastTransitionSourceState = ErrorCascadeIsBoundedStateRunaway
			p.lastTransitionIndex = 2
			p.hasTransitionActions = true
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == ErrorCascadeIsBoundedEventReset {
			*currentState = ErrorCascadeIsBoundedStateIdle
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = ErrorCascadeIsBoundedStateRunaway
			p.lastTransitionIndex = 3
			p.hasTransitionActions = false
			return true
		}
	case ErrorCascadeIsBoundedStateSettling:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == ErrorCascadeIsBoundedEventErrorExecution {
			if p.evaluateGuard(`(repairs < 3)`, engine) {
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = true
			p.lastTransitionSourceState = ErrorCascadeIsBoundedStateSettling
			p.lastTransitionIndex = 0
			p.hasTransitionActions = true
			return true
			}
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == ErrorCascadeIsBoundedEventPoke {
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = true
			p.lastTransitionSourceState = ErrorCascadeIsBoundedStateSettling
			p.lastTransitionIndex = 1
			p.hasTransitionActions = true
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == ErrorCascadeIsBoundedEventReset {
			*currentState = ErrorCascadeIsBoundedStateIdle
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = ErrorCascadeIsBoundedStateSettling
			p.lastTransitionIndex = 2
			p.hasTransitionActions = false
			return true
		}
	}
	return false
}

// ExecuteTransitionActions executes actions for the last taken transition (W3C SCXML 3.13).
//line error_cascade_is_bounded.scxml:45
func (p *ErrorCascadeIsBoundedPolicy) ExecuteTransitionActions(engine *sce.Engine[ErrorCascadeIsBoundedState, ErrorCascadeIsBoundedEvent]) {
	p.ensureScriptEngine()
	if !p.hasTransitionActions {
		return
	}
	source := p.lastTransitionSourceState
	idx := p.lastTransitionIndex
	if source == ErrorCascadeIsBoundedStateIdle && idx == 0 {
		//line error_cascade_is_bounded.scxml:68

	// W3C SCXML 5.3: <assign location="pokes" expr="pokes + 1">
	if err := p.assignVariable(`pokes`, `_scxml_add(pokes, 1)`); err != nil {
		engine.Raise(sce.NewPlatformError(ErrorCascadeIsBoundedEventErrorExecution, "<assign> to 'pokes' failed"))
	}

		return
	}
	if source == ErrorCascadeIsBoundedStateIdle && idx == 1 {
		//line error_cascade_is_bounded.scxml:74

	// W3C SCXML 5.3/B.2: Invalid or read-only location ""
	engine.Raise(sce.NewPlatformError(ErrorCascadeIsBoundedEventErrorExecution, "<assign> has an invalid or read-only location ''"))

		return
	}
	if source == ErrorCascadeIsBoundedStateRunaway && idx == 0 {
		//line error_cascade_is_bounded.scxml:107

	// W3C SCXML 5.3: <assign location="runs" expr="runs + 1">
	if err := p.assignVariable(`runs`, `_scxml_add(runs, 1)`); err != nil {
		engine.Raise(sce.NewPlatformError(ErrorCascadeIsBoundedEventErrorExecution, "<assign> to 'runs' failed"))
	}


	engine.Raise(sce.NewEventWithMetadata(ErrorCascadeIsBoundedEventTick))


	// W3C SCXML 5.3/B.2: Invalid or read-only location ""
	engine.Raise(sce.NewPlatformError(ErrorCascadeIsBoundedEventErrorExecution, "<assign> has an invalid or read-only location ''"))

		return
	}
	if source == ErrorCascadeIsBoundedStateRunaway && idx == 1 {
		//line error_cascade_is_bounded.scxml:120

	// W3C SCXML 5.3: <assign location="ticks" expr="ticks + 1">
	if err := p.assignVariable(`ticks`, `_scxml_add(ticks, 1)`); err != nil {
		engine.Raise(sce.NewPlatformError(ErrorCascadeIsBoundedEventErrorExecution, "<assign> to 'ticks' failed"))
	}

		return
	}
	if source == ErrorCascadeIsBoundedStateRunaway && idx == 2 {
		//line error_cascade_is_bounded.scxml:123

	// W3C SCXML 5.3: <assign location="pokes" expr="pokes + 1">
	if err := p.assignVariable(`pokes`, `_scxml_add(pokes, 1)`); err != nil {
		engine.Raise(sce.NewPlatformError(ErrorCascadeIsBoundedEventErrorExecution, "<assign> to 'pokes' failed"))
	}

		return
	}
	if source == ErrorCascadeIsBoundedStateSettling && idx == 0 {
		//line error_cascade_is_bounded.scxml:89

	// W3C SCXML 5.3: <assign location="repairs" expr="repairs + 1">
	if err := p.assignVariable(`repairs`, `_scxml_add(repairs, 1)`); err != nil {
		engine.Raise(sce.NewPlatformError(ErrorCascadeIsBoundedEventErrorExecution, "<assign> to 'repairs' failed"))
	}


	// W3C SCXML 5.3/B.2: Invalid or read-only location ""
	engine.Raise(sce.NewPlatformError(ErrorCascadeIsBoundedEventErrorExecution, "<assign> has an invalid or read-only location ''"))

		return
	}
	if source == ErrorCascadeIsBoundedStateSettling && idx == 1 {
		//line error_cascade_is_bounded.scxml:93

	// W3C SCXML 5.3: <assign location="pokes" expr="pokes + 1">
	if err := p.assignVariable(`pokes`, `_scxml_add(pokes, 1)`); err != nil {
		engine.Raise(sce.NewPlatformError(ErrorCascadeIsBoundedEventErrorExecution, "<assign> to 'pokes' failed"))
	}

		return
	}
}
