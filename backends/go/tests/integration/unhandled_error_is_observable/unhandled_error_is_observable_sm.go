// SCE-GENERATED — DO NOT EDIT
// source-hash: 88c46d955f89d1b6f7eb00aaedced29c5fbacc4db8ed4464fa38145a023ef16c
// template-hash: 6d29ccd65cc69c7036210e21d4c9d2a46b7717262dc7e045f86a45620f80383f
// generated-at: 0


// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) 2025-2026 [Author of input SCXML file]
//
// Generated by SCE Code Generator (Go backend)
// From: integration_resources/unhandled_error_is_observable/unhandled_error_is_observable.scxml
//
// This generated code is MIT licensed and may be freely used in any project.
// Runtime engine dependency (sce-go-runtime): LGPL-2.1-or-later WITH LicenseRef-SCE-Linking-Exception OR LicenseRef-SCE-Commercial
// Full license: https://github.com/newmassrael/scxml-core-engine/blob/main/LICENSE
// Generated Go state machine for `unhandled_error_is_observable`.
//
// Generated by SCE Code Generator (Go backend).
// Implements sce.StatePolicy with full transition logic,
// entry/exit actions, and event processing.


// SCE-MAP: unhandled_error_is_observable.scxml:40 :: _machine

package unhandled_error_is_observable

import (
	"fmt"
	"path/filepath"
	"strings"
	"time"

	sce "github.com/newmassrael/sce-go-runtime"
)

// Ensure imports are used
var (
	_ = fmt.Sprintf
	_ = filepath.Join
	_ = strings.HasPrefix
	_ = time.Duration(0)
)

// ======================================================================
// State type (W3C SCXML 3.3)
// ======================================================================

type UnhandledErrorIsObservableState int

const (
	UnhandledErrorIsObservableStateGuarded UnhandledErrorIsObservableState = 0
	UnhandledErrorIsObservableStateIdle UnhandledErrorIsObservableState = 1
)

func (s UnhandledErrorIsObservableState) String() string {
	switch s {
	case UnhandledErrorIsObservableStateGuarded:
		return "guarded"
	case UnhandledErrorIsObservableStateIdle:
		return "idle"
	}
	return "unknown"
}

// UnhandledErrorIsObservableStateFromName is the read half of the pair above — a state
// id back into the state it names (W3C SCXML 3.3).
//
// A host that persists where a machine was has to write it down as TEXT: the
// constants above are a build artefact of one binary, and the process that
// resumes is a different one. String publishes the name and this reads it back,
// which is what lets a journal survive its own record and reach
// sce.Engine.EnterAt.
//
// The second return is false for a name this document does not declare. A name
// guessed at rather than refused is how a restore reaches a configuration
// nobody recorded — and the refusal is what makes a typo in a journal a
// reported failure instead of a machine quietly somewhere else.
//
// Emitted from the same loop over the document's states as String, so the two
// age together.
func UnhandledErrorIsObservableStateFromName(name string) (UnhandledErrorIsObservableState, bool) {
	switch name {
	case "guarded":
		return UnhandledErrorIsObservableStateGuarded, true
	case "idle":
		return UnhandledErrorIsObservableStateIdle, true
	}
	var zero UnhandledErrorIsObservableState
	return zero, false
}

// UnhandledErrorIsObservableAllStates is every state this document declares, in the
// order the constants above are issued.
//
// Emitted from the same loop, so a walk over it is a walk over the document
// rather than over a list somebody maintained beside it — a test that spells
// its own list goes on passing when the document grows a state.
var UnhandledErrorIsObservableAllStates = []UnhandledErrorIsObservableState{
	UnhandledErrorIsObservableStateGuarded,
	UnhandledErrorIsObservableStateIdle,
}

// ======================================================================
// Event type (W3C SCXML 3.12)
// ======================================================================

type UnhandledErrorIsObservableEvent int

const (
	UnhandledErrorIsObservableEventBoom UnhandledErrorIsObservableEvent = 0
	UnhandledErrorIsObservableEventErrorExecution UnhandledErrorIsObservableEvent = 1
	UnhandledErrorIsObservableEventGo UnhandledErrorIsObservableEvent = 2
	UnhandledErrorIsObservableEventHeard UnhandledErrorIsObservableEvent = 3
	UnhandledErrorIsObservableEventPoke UnhandledErrorIsObservableEvent = 4
	UnhandledErrorIsObservableEventRetryErrorExecution UnhandledErrorIsObservableEvent = 5
	UnhandledErrorIsObservableEventUnheard UnhandledErrorIsObservableEvent = 6
	UnhandledErrorIsObservableEventWhisper UnhandledErrorIsObservableEvent = 7
	// W3C SCXML 3.13: Sentinel for eventless transition dispatch
	UnhandledErrorIsObservableEventNull UnhandledErrorIsObservableEvent = 8
)

func (e UnhandledErrorIsObservableEvent) String() string {
	switch e {
	case UnhandledErrorIsObservableEventBoom:
		return "boom"
	case UnhandledErrorIsObservableEventErrorExecution:
		return "error.execution"
	case UnhandledErrorIsObservableEventGo:
		return "go"
	case UnhandledErrorIsObservableEventHeard:
		return "heard"
	case UnhandledErrorIsObservableEventPoke:
		return "poke"
	case UnhandledErrorIsObservableEventRetryErrorExecution:
		return "retry.error.execution"
	case UnhandledErrorIsObservableEventUnheard:
		return "unheard"
	case UnhandledErrorIsObservableEventWhisper:
		return "whisper"
	case UnhandledErrorIsObservableEventNull:
		return ""
	}
	return "unknown"
}

// ======================================================================
// Policy struct
// ======================================================================

type UnhandledErrorIsObservablePolicy struct {
	// W3C SCXML 3.13: Last transition metadata
	lastTransitionIsInternal  bool
	lastTransitionIsTargetless bool
	lastTransitionSourceState UnhandledErrorIsObservableState
	// W3C SCXML 3.13: Transition action tracking
	lastTransitionIndex   int
	hasTransitionActions   bool
	// W3C SCXML 5.10.1: External event flag
	nextEventIsExternal bool
	pendingEventName string
	pendingEventData string
	pendingEventType string
	pendingEventSendid string
	pendingEventOrigin string
	pendingEventOrigintype string
	pendingEventInvokeid string
	// W3C SCXML 5.10: Session ID
	SessionID string
	// W3C SCXML B.1: Script engine handle (per-instance, Path B+ Q1=(d) Go=interface ref)
	ScriptEngine sce.IScriptEngine
	scriptEngineInitialized bool
	// W3C SCXML C.2.3: inbound BasicHTTP endpoint serving this machine,
	// declared by the deployment before Initialize(). The address belongs to
	// whoever runs the listener, so the machine takes it from here rather than
	// guessing one. Empty means no such endpoint is deployed, and no BasicHTTP
	// entry is published in _ioprocessors.
	BasicHTTPAccessURI string
	// W3C SCXML 6.4: Parent communication
	ParentExternalQueue chan sce.ParentEvent
	InvokeID           string
	ChildSessionID     string
	// §scxml-6.4.3: invoke param VALUES staged by the parent, applied after
	// this machine's datamodel init so they override its `<data>` defaults.
	pendingParams      map[string]interface{}
}

// NewUnhandledErrorIsObservablePolicy creates a new policy with default values.
func NewUnhandledErrorIsObservablePolicy() UnhandledErrorIsObservablePolicy {
	return UnhandledErrorIsObservablePolicy{
		lastTransitionSourceState: UnhandledErrorIsObservableStateIdle,
	}
}

// Pokes reports what the `pokes` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `pokes` was assigned a value of another type, or the engine refused.
func (p *UnhandledErrorIsObservablePolicy) Pokes() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "pokes")
}

// Booms reports what the `booms` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `booms` was assigned a value of another type, or the engine refused.
func (p *UnhandledErrorIsObservablePolicy) Booms() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "booms")
}

// Caught reports what the `caught` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `caught` was assigned a value of another type, or the engine refused.
func (p *UnhandledErrorIsObservablePolicy) Caught() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "caught")
}

// Detail reports what the `detail` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `detail` was assigned a value of another type, or the engine refused.
func (p *UnhandledErrorIsObservablePolicy) Detail() (string, bool) {
	return sce.ReadDatamodelString(p.ScriptEngine, p.SessionID, "detail")
}

// Heards reports what the `heards` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `heards` was assigned a value of another type, or the engine refused.
func (p *UnhandledErrorIsObservablePolicy) Heards() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "heards")
}




// ensureScriptEngine lazily initializes the script engine session.
//
// Engine DI Parity RFC: panics if ScriptEngine was not set on the policy
// before Initialize() — mirrors Kotlin's mandatory constructor parameter
// contract. Silent no-op would otherwise turn missed injection into a
// hidden runtime regression (every <assign>/<log>/guard becomes a no-op
// and the SM walks to its default-state final without diagnostic).
func (p *UnhandledErrorIsObservablePolicy) ensureScriptEngine() {
	if p.scriptEngineInitialized {
		return
	}
	engine := p.ScriptEngine
	if engine == nil {
		panic("UnhandledErrorIsObservablePolicy: ScriptEngine field must be set before Initialize()")
	}
	sessionID := p.SessionID
	if sessionID == "" {
		sessionID = sce.GenerateSessionID()
		p.SessionID = sessionID
	}
	if !engine.HasSession(sessionID) {
		_ = engine.CreateSession(sessionID)
	}
	// W3C SCXML C.1.1 / C.2.3: the _ioprocessors entries come from the same
	// helper every other backend uses, so a machine reads the same entry names
	// and the same addresses whichever one runs it.
	_ = engine.SetupSystemVariables(sessionID, "unhandled_error_is_observable",
		sce.BuildIoProcessors(sessionID, p.BasicHTTPAccessURI))
	p.scriptEngineInitialized = true
}

// InitializeDataModel sets up datamodel via script engine (W3C SCXML 5.3).
func (p *UnhandledErrorIsObservablePolicy) InitializeDataModel(eng *sce.Engine[UnhandledErrorIsObservableState, UnhandledErrorIsObservableEvent]) {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	sessionID := p.SessionID

	// W3C SCXML 5.3: Initialize _sessionid
	_ = engine.SetVariable(sessionID, "_sessionid", sessionID)

	// W3C SCXML 5.3: Initialize _name
	_ = engine.SetVariable(sessionID, "_name", "unhandled_error_is_observable")

	// W3C SCXML 5.2.2: Initialize global datamodel variables
	// W3C SCXML 5.2/5.3: Initialize pokes from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "pokes", result)
		} else {
			eng.Raise(sce.NewPlatformError(UnhandledErrorIsObservableEventErrorExecution, "<data id='pokes'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "pokes", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize booms from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "booms", result)
		} else {
			eng.Raise(sce.NewPlatformError(UnhandledErrorIsObservableEventErrorExecution, "<data id='booms'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "booms", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize caught from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "caught", result)
		} else {
			eng.Raise(sce.NewPlatformError(UnhandledErrorIsObservableEventErrorExecution, "<data id='caught'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "caught", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize detail from expr="'none'"
	{
		result, err := engine.EvaluateExpression(sessionID, `"none"`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "detail", result)
		} else {
			eng.Raise(sce.NewPlatformError(UnhandledErrorIsObservableEventErrorExecution, "<data id='detail'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "detail", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize heards from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "heards", result)
		} else {
			eng.Raise(sce.NewPlatformError(UnhandledErrorIsObservableEventErrorExecution, "<data id='heards'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "heards", nil)
		}
	}



	// §scxml-6.4.3: apply the invoke params the parent staged — overriding the
	// `<data>` default this block just seeded, and only for a name the child
	// declares ("if the names do not match, the Processor MUST NOT add the
	// value ... to the invoked session's data model"). `HasVariable` is that
	// filter, and it is this channel's only copy of the rule.
	if len(p.pendingParams) > 0 {
		for paramName, paramValue := range p.pendingParams {
			if engine.HasVariable(sessionID, paramName) {
				_ = engine.SetVariable(sessionID, paramName, paramValue)
			}
		}
		p.pendingParams = nil
	}
}

// evaluateGuard evaluates a guard condition via script engine.
// W3C SCXML 4.3.2: If a condition expression cannot be evaluated, the processor
// MUST treat it as false and MUST raise error.execution.
func (p *UnhandledErrorIsObservablePolicy) evaluateGuard(guard string, eng *sce.Engine[UnhandledErrorIsObservableState, UnhandledErrorIsObservableEvent]) bool {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	result, err := engine.EvaluateExpression(p.SessionID, guard)
	if err != nil {
		eng.Raise(sce.NewPlatformError(UnhandledErrorIsObservableEventErrorExecution, "a <transition> cond failed to evaluate"))
		return false
	}
	return sce.ScriptToBool(result)
}

// executeScript executes a script block via script engine.
func (p *UnhandledErrorIsObservablePolicy) executeScript(script string) error {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	return engine.ExecuteScript(p.SessionID, script)
}

// setCurrentEvent binds _event system variable (W3C SCXML 5.10) and answers
// which rung of §scxml-B-2-8-1 the payload got, because the binding is the
// only place that knows.
//
// This used to be `_ = engine.SetCurrentEvent(...)`: the ladder decided
// between a DOM, a value and a space-normalized string, and the decision was
// dropped one line later. A payload that announced structure and would not
// parse therefore reached the document as raw characters, every
// _event.data.<field> read empty, and nothing anywhere could say so — see
// sce.PayloadReading.
func (p *UnhandledErrorIsObservablePolicy) setCurrentEvent(name string) sce.PayloadReading {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	data := p.pendingEventData
	eventType := p.pendingEventType
	sendID := p.pendingEventSendid
	// W3C SCXML C.1: `_event.origin` is the sender's published
	// `_ioprocessors` location, not its bare session id — and this is the one
	// place that publishes `_event` to the document, so this is where the id
	// becomes a location. The engine keeps the bare id in
	// `EventMetadata.Origin` because its session-keyed lookups (<finalize>
	// dispatch, cancelled-invoke filtering) match on it; converting at the
	// raise would make one value serve two consumers that need different
	// spellings. The conversion itself lives in `sce.PublishedOrigin`, the
	// port of the `IOProcessorHelper::publishedOrigin` the C++ engines share:
	// a second spelling of the rule is how the backends would stop agreeing.
	origin := sce.PublishedOrigin(p.pendingEventOrigin)
	originType := p.pendingEventOrigintype
	invokeID := p.pendingEventInvokeid
	reading, err := engine.SetCurrentEvent(p.SessionID, sce.SetCurrentEventArgs{
		Name:       name,
		Data:       data,
		Type:       eventType,
		SendID:     sendID,
		Origin:     origin,
		OriginType: originType,
		InvokeID:   invokeID,
	})
	if err != nil {
		// An engine that could not bind _event at all has a larger problem
		// than an unreadable payload, and reporting PayloadUndecodable for it
		// would point a host at the wrong thing.
		return sce.PayloadAbsent
	}
	return reading
}

// assignVariable assigns a value to a datamodel variable via script engine.
func (p *UnhandledErrorIsObservablePolicy) assignVariable(location, expr string) error {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	script := location + " = " + expr
	return engine.ExecuteScript(p.SessionID, script)
}

// getVariable reads a datamodel variable from the script engine.
func (p *UnhandledErrorIsObservablePolicy) getVariable(name string) interface{} {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	result, err := engine.GetVariable(p.SessionID, name)
	if err != nil {
		return nil
	}
	return result
}

// SetParamValueInScriptEngine stores an invoke param's VALUE for deferred
// application after datamodel init (§scxml-6.4.3).
//
// Params are applied at the end of InitializeDataModel, overriding the
// `<data>` default only for names the child declares. The value crosses as a
// value: this took the parent's value rendered as Lua source and
// re-evaluated it here until 2026-08-21, which survived every value a test
// had tried and lost the ones Lua cannot spell — `ToScriptLiteral` renders a
// non-finite number as `+Inf`, which is not a Lua expression, so an
// `<invoke>` `<param expr="1/0"/>` arrived as nothing at all.
func (p *UnhandledErrorIsObservablePolicy) SetParamValueInScriptEngine(name string, value interface{}) {
	if p.pendingParams == nil {
		p.pendingParams = make(map[string]interface{})
	}
	p.pendingParams[name] = value
}


// ExecutePendingInvokes is a no-op (no invokes in this SM).
func (p *UnhandledErrorIsObservablePolicy) ExecutePendingInvokes(engine *sce.Engine[UnhandledErrorIsObservableState, UnhandledErrorIsObservableEvent]) {}

// TickChildren is a no-op (no invokes in this SM).
func (p *UnhandledErrorIsObservablePolicy) TickChildren(engine *sce.Engine[UnhandledErrorIsObservableState, UnhandledErrorIsObservableEvent]) {}

// ======================================================================
// StatePolicy interface implementation
// ======================================================================

// InitialState returns the initial state (W3C SCXML 3.3).
func (p *UnhandledErrorIsObservablePolicy) InitialState() UnhandledErrorIsObservableState {
	return UnhandledErrorIsObservableStateIdle
}

// IsFinalState returns true if state is a <final> state (W3C SCXML 3.7).
func (p *UnhandledErrorIsObservablePolicy) IsFinalState(state UnhandledErrorIsObservableState) bool {
	switch state {
	}
	return false
}

// GetParent returns the parent of state, or (zero, false) if root (W3C SCXML 3.3).
func (p *UnhandledErrorIsObservablePolicy) GetParent(state UnhandledErrorIsObservableState) (UnhandledErrorIsObservableState, bool) {
	switch state {
	}
	return 0, false
}

// IsCompoundState returns true if state has children (W3C SCXML 3.3).
func (p *UnhandledErrorIsObservablePolicy) IsCompoundState(state UnhandledErrorIsObservableState) bool {
	switch state {
	}
	return false
}

func (p *UnhandledErrorIsObservablePolicy) IsParallelState(_ UnhandledErrorIsObservableState) bool { return false }
func (p *UnhandledErrorIsObservablePolicy) GetParallelRegions(_ UnhandledErrorIsObservableState) []UnhandledErrorIsObservableState { return nil }

// IsDescendantOf returns true if desc is a descendant of anc (W3C SCXML 3.12).
func (p *UnhandledErrorIsObservablePolicy) IsDescendantOf(desc, anc UnhandledErrorIsObservableState) bool {
	current := desc
	for {
		parent, ok := p.GetParent(current)
		if !ok {
			return false
		}
		if parent == anc {
			return true
		}
		current = parent
	}
}

// GetDocumentOrder returns the document order index (W3C SCXML Appendix D).
func (p *UnhandledErrorIsObservablePolicy) GetDocumentOrder(state UnhandledErrorIsObservableState) int {
	switch state {
	case UnhandledErrorIsObservableStateGuarded:
		return 1
	case UnhandledErrorIsObservableStateIdle:
		return 0
	}
	return -1
}

// GetEventName returns the string name of an event (W3C SCXML 3.12).
func (p *UnhandledErrorIsObservablePolicy) GetEventName(event UnhandledErrorIsObservableEvent) string {
	return event.String()
}

// GetEventFromName looks up an event by name (W3C SCXML 3.12).
func (p *UnhandledErrorIsObservablePolicy) GetEventFromName(name string) (UnhandledErrorIsObservableEvent, bool) {
	switch name {
	case "boom":
		return UnhandledErrorIsObservableEventBoom, true
	case "error.execution":
		return UnhandledErrorIsObservableEventErrorExecution, true
	case "go":
		return UnhandledErrorIsObservableEventGo, true
	case "heard":
		return UnhandledErrorIsObservableEventHeard, true
	case "poke":
		return UnhandledErrorIsObservableEventPoke, true
	case "retry.error.execution":
		return UnhandledErrorIsObservableEventRetryErrorExecution, true
	case "unheard":
		return UnhandledErrorIsObservableEventUnheard, true
	case "whisper":
		return UnhandledErrorIsObservableEventWhisper, true
	}
	return UnhandledErrorIsObservableEventNull, false
}

// GetStateName returns the string name of a state (W3C SCXML 3.3).
func (p *UnhandledErrorIsObservablePolicy) GetStateName(state UnhandledErrorIsObservableState) string {
	return state.String()
}

// GetStateFromName reads a state id back into the state it names (W3C SCXML 3.3).
// The reverse of GetStateName, and what turns a host's recorded configuration
// back into the argument sce.Engine.EnterAt takes.
func (p *UnhandledErrorIsObservablePolicy) GetStateFromName(name string) (UnhandledErrorIsObservableState, bool) {
	return UnhandledErrorIsObservableStateFromName(name)
}

// NullEvent returns the sentinel for eventless transition dispatch (W3C SCXML 3.13).
func (p *UnhandledErrorIsObservablePolicy) NullEvent() UnhandledErrorIsObservableEvent {
	return UnhandledErrorIsObservableEventNull
}

// GetInitialChildren returns initial children of a compound state (W3C SCXML 3.6).
func (p *UnhandledErrorIsObservablePolicy) GetInitialChildren(state UnhandledErrorIsObservableState) []UnhandledErrorIsObservableState {
	switch state {
	}
	return nil
}

// LastTransitionIsInternal returns the internal transition flag (W3C SCXML 3.13).
func (p *UnhandledErrorIsObservablePolicy) LastTransitionIsInternal() bool {
	return p.lastTransitionIsInternal
}

// SetLastTransitionIsInternal sets the internal transition flag.
func (p *UnhandledErrorIsObservablePolicy) SetLastTransitionIsInternal(value bool) {
	p.lastTransitionIsInternal = value
}

// LastTransitionIsTargetless returns the targetless transition flag (W3C SCXML 3.13).
func (p *UnhandledErrorIsObservablePolicy) LastTransitionIsTargetless() bool {
	return p.lastTransitionIsTargetless
}

// SetLastTransitionIsTargetless sets the targetless transition flag.
func (p *UnhandledErrorIsObservablePolicy) SetLastTransitionIsTargetless(value bool) {
	p.lastTransitionIsTargetless = value
}

// LastTransitionSourceState returns the source state of the last transition.
func (p *UnhandledErrorIsObservablePolicy) LastTransitionSourceState() UnhandledErrorIsObservableState {
	return p.lastTransitionSourceState
}

// SetLastTransitionSourceState sets the source state of the last transition.
func (p *UnhandledErrorIsObservablePolicy) SetLastTransitionSourceState(state UnhandledErrorIsObservableState) {
	p.lastTransitionSourceState = state
}


// SetNextEventIsExternal sets the external event flag (W3C SCXML 5.10.1).
func (p *UnhandledErrorIsObservablePolicy) SetNextEventIsExternal(value bool) {
	p.nextEventIsExternal = value
}

// HasParallelStates returns whether the SM has parallel states.
func (p *UnhandledErrorIsObservablePolicy) HasParallelStates() bool {
	return false
}

// NeedsScriptEngine returns whether the SM needs a script engine.
func (p *UnhandledErrorIsObservablePolicy) NeedsScriptEngine() bool {
	return true
}

// NeedsEventScheduler reports whether a host must drive this machine with
// Tick rather than Step alone.
//
// W3C SCXML 6.2: Step runs a macrostep and never drains the delayed-send
// scheduler nor ticks an invoked child, so a host that only ever calls it
// waits forever with nothing said. Same verdict the generate manifest
// publishes as needs_event_scheduler.
func (p *UnhandledErrorIsObservablePolicy) NeedsEventScheduler() bool {
	return false
}

// NeedsDataModelInit returns whether the SM needs datamodel init.
func (p *UnhandledErrorIsObservablePolicy) NeedsDataModelInit() bool {
	return true
}

func (p *UnhandledErrorIsObservablePolicy) HasInvokeSupport() bool { return false }
func (p *UnhandledErrorIsObservablePolicy) HasChildTick() bool { return false }
func (p *UnhandledErrorIsObservablePolicy) HasFinalize() bool { return false }
func (p *UnhandledErrorIsObservablePolicy) HasAutoforward() bool { return false }
func (p *UnhandledErrorIsObservablePolicy) HasActiveStates() bool { return false }
func (p *UnhandledErrorIsObservablePolicy) GetActiveStates() []UnhandledErrorIsObservableState { return nil }
// W3C SCXML 3.4: this machine keeps no active set — its configuration is the
// parent walk from the current state — so there is nothing for a restore to
// hand back here. sce.Engine.EnterAt reaches this only through HasActiveStates,
// which is false above; the method exists because the interface is one contract.
func (p *UnhandledErrorIsObservablePolicy) SetActiveStates(_ []UnhandledErrorIsObservableState) {}
func (p *UnhandledErrorIsObservablePolicy) HasExternalEventFlag() bool { return true }
// GetInitialOrHistoryChild returns the initial child considering history (W3C SCXML 3.11).
func (p *UnhandledErrorIsObservablePolicy) GetInitialOrHistoryChild(state UnhandledErrorIsObservableState) UnhandledErrorIsObservableState {
	children := p.GetInitialChildren(state)
	if len(children) > 0 {
		return children[0]
	}
	return state
}
// ExecuteFinalizeForChildEvent is a no-op (no finalize invokes).
func (p *UnhandledErrorIsObservablePolicy) ExecuteFinalizeForChildEvent(_ *sce.EventWithMetadata[UnhandledErrorIsObservableEvent], _ *sce.Engine[UnhandledErrorIsObservableState, UnhandledErrorIsObservableEvent]) {}
// ForwardToAutoforwardChildren is a no-op (no autoforward invokes).
func (p *UnhandledErrorIsObservablePolicy) ForwardToAutoforwardChildren(_ string, _ sce.EventMetadata, _ *sce.Engine[UnhandledErrorIsObservableState, UnhandledErrorIsObservableEvent]) {}

// PopulateEventMetadata stores pending event metadata (W3C SCXML 5.10).
// Note: event name is set separately via setCurrentEvent(), not from metadata.
func (p *UnhandledErrorIsObservablePolicy) PopulateEventMetadata(meta *sce.EventMetadata) {
	p.pendingEventData = meta.Data
	p.pendingEventType = meta.EventType.String()
	p.pendingEventSendid = meta.SendID
	p.pendingEventOrigin = meta.Origin
	p.pendingEventOrigintype = meta.OriginType
	p.pendingEventInvokeid = meta.InvokeID
}

// ClearEventMetadata resets pending event metadata (W3C SCXML 5.10).
func (p *UnhandledErrorIsObservablePolicy) ClearEventMetadata() {
	p.pendingEventName = ""
	p.pendingEventData = ""
	p.pendingEventType = ""
	p.pendingEventSendid = ""
	p.pendingEventOrigin = ""
	p.pendingEventOrigintype = ""
	p.pendingEventInvokeid = ""
}




// ExecuteEntryActions executes onentry actions for a state (W3C SCXML 3.8).
//line unhandled_error_is_observable.scxml:40
func (p *UnhandledErrorIsObservablePolicy) ExecuteEntryActions(state UnhandledErrorIsObservableState, engine *sce.Engine[UnhandledErrorIsObservableState, UnhandledErrorIsObservableEvent], pathChild *UnhandledErrorIsObservableState) {
	// Only a `<parallel>` machine descends into defaults here, so a machine
	// without one has nothing to tell an ancestor entry from a target entry.
	_ = pathChild
	p.ensureScriptEngine()
	switch state {
	default:
		// No entry actions
	}
}

// ExecuteExitActions executes onexit actions for a state (W3C SCXML 3.9).
//line unhandled_error_is_observable.scxml:40
func (p *UnhandledErrorIsObservablePolicy) ExecuteExitActions(state UnhandledErrorIsObservableState, engine *sce.Engine[UnhandledErrorIsObservableState, UnhandledErrorIsObservableEvent], preTransitionActive []UnhandledErrorIsObservableState) {
	p.ensureScriptEngine()
	switch state {
	default:
		// No exit actions
	}
}

// ProcessTransition evaluates guards and takes a matching transition (W3C SCXML 3.13).
// Returns true if a transition was taken.
//line unhandled_error_is_observable.scxml:40
func (p *UnhandledErrorIsObservablePolicy) ProcessTransition(currentState *UnhandledErrorIsObservableState, event UnhandledErrorIsObservableEvent, engine *sce.Engine[UnhandledErrorIsObservableState, UnhandledErrorIsObservableEvent]) bool {
	// W3C SCXML 5.10: Bind _event system variable for guard evaluation
	if event != UnhandledErrorIsObservableEventNull {
		// §scxml-B-2-8-1: the rung the payload got, handed to the engine
		// rather than dropped. This is the only frame that has both the
		// reading and the event it belongs to.
		engine.NotePayloadReading(event, p.setCurrentEvent(p.GetEventName(event)))
	}

	// W3C SCXML 3.12: Try transitions in current state first
	if p.tryTransitionInState(*currentState, event, currentState, engine) {
		return true
	}


	return false
}


// tryTransitionInState checks transitions for a single state.
//line unhandled_error_is_observable.scxml:40
func (p *UnhandledErrorIsObservablePolicy) tryTransitionInState(checkState UnhandledErrorIsObservableState, event UnhandledErrorIsObservableEvent, currentState *UnhandledErrorIsObservableState, engine *sce.Engine[UnhandledErrorIsObservableState, UnhandledErrorIsObservableEvent]) bool {
	switch checkState {
	case UnhandledErrorIsObservableStateGuarded:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == UnhandledErrorIsObservableEventBoom {
			*currentState = UnhandledErrorIsObservableStateGuarded
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = UnhandledErrorIsObservableStateGuarded
			p.lastTransitionIndex = 0
			p.hasTransitionActions = true
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == UnhandledErrorIsObservableEventErrorExecution {
			*currentState = UnhandledErrorIsObservableStateGuarded
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = UnhandledErrorIsObservableStateGuarded
			p.lastTransitionIndex = 1
			p.hasTransitionActions = true
			return true
		}
	case UnhandledErrorIsObservableStateIdle:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == UnhandledErrorIsObservableEventPoke {
			*currentState = UnhandledErrorIsObservableStateIdle
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = UnhandledErrorIsObservableStateIdle
			p.lastTransitionIndex = 0
			p.hasTransitionActions = true
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == UnhandledErrorIsObservableEventWhisper {
			*currentState = UnhandledErrorIsObservableStateIdle
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = UnhandledErrorIsObservableStateIdle
			p.lastTransitionIndex = 1
			p.hasTransitionActions = true
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == UnhandledErrorIsObservableEventHeard {
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = true
			p.lastTransitionSourceState = UnhandledErrorIsObservableStateIdle
			p.lastTransitionIndex = 2
			p.hasTransitionActions = true
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == UnhandledErrorIsObservableEventBoom {
			*currentState = UnhandledErrorIsObservableStateIdle
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = UnhandledErrorIsObservableStateIdle
			p.lastTransitionIndex = 3
			p.hasTransitionActions = true
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == UnhandledErrorIsObservableEventGo {
			*currentState = UnhandledErrorIsObservableStateGuarded
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = UnhandledErrorIsObservableStateIdle
			p.lastTransitionIndex = 4
			p.hasTransitionActions = false
			return true
		}
	}
	return false
}

// ExecuteTransitionActions executes actions for the last taken transition (W3C SCXML 3.13).
//line unhandled_error_is_observable.scxml:40
func (p *UnhandledErrorIsObservablePolicy) ExecuteTransitionActions(engine *sce.Engine[UnhandledErrorIsObservableState, UnhandledErrorIsObservableEvent]) {
	p.ensureScriptEngine()
	if !p.hasTransitionActions {
		return
	}
	source := p.lastTransitionSourceState
	idx := p.lastTransitionIndex
	if source == UnhandledErrorIsObservableStateGuarded && idx == 0 {
		//line unhandled_error_is_observable.scxml:90

	// W3C SCXML 5.3: <assign location="booms" expr="booms + 1">
	if err := p.assignVariable(`booms`, `_scxml_add(booms, 1)`); err != nil {
		engine.Raise(sce.NewPlatformError(UnhandledErrorIsObservableEventErrorExecution, "<assign> to 'booms' failed"))
	}


	// W3C SCXML 5.3/B.2: Invalid or read-only location ""
	engine.Raise(sce.NewPlatformError(UnhandledErrorIsObservableEventErrorExecution, "<assign> has an invalid or read-only location ''"))

		return
	}
	if source == UnhandledErrorIsObservableStateGuarded && idx == 1 {
		//line unhandled_error_is_observable.scxml:94

	// W3C SCXML 5.3: <assign location="caught" expr="caught + 1">
	if err := p.assignVariable(`caught`, `_scxml_add(caught, 1)`); err != nil {
		engine.Raise(sce.NewPlatformError(UnhandledErrorIsObservableEventErrorExecution, "<assign> to 'caught' failed"))
	}


	// W3C SCXML 5.3: <assign location="detail" expr="_event.name">
	if err := p.assignVariable(`detail`, `_event.name`); err != nil {
		engine.Raise(sce.NewPlatformError(UnhandledErrorIsObservableEventErrorExecution, "<assign> to 'detail' failed"))
	}

		return
	}
	if source == UnhandledErrorIsObservableStateIdle && idx == 0 {
		//line unhandled_error_is_observable.scxml:55

	// W3C SCXML 5.3: <assign location="pokes" expr="pokes + 1">
	if err := p.assignVariable(`pokes`, `_scxml_add(pokes, 1)`); err != nil {
		engine.Raise(sce.NewPlatformError(UnhandledErrorIsObservableEventErrorExecution, "<assign> to 'pokes' failed"))
	}

		return
	}
	if source == UnhandledErrorIsObservableStateIdle && idx == 1 {
		//line unhandled_error_is_observable.scxml:58

	engine.Raise(sce.NewEventWithMetadata(UnhandledErrorIsObservableEventUnheard))


	engine.Raise(sce.NewEventWithMetadata(UnhandledErrorIsObservableEventRetryErrorExecution))


	engine.Raise(sce.NewEventWithMetadata(UnhandledErrorIsObservableEventHeard))

		return
	}
	if source == UnhandledErrorIsObservableStateIdle && idx == 2 {
		//line unhandled_error_is_observable.scxml:80

	// W3C SCXML 5.3: <assign location="heards" expr="heards + 1">
	if err := p.assignVariable(`heards`, `_scxml_add(heards, 1)`); err != nil {
		engine.Raise(sce.NewPlatformError(UnhandledErrorIsObservableEventErrorExecution, "<assign> to 'heards' failed"))
	}

		return
	}
	if source == UnhandledErrorIsObservableStateIdle && idx == 3 {
		//line unhandled_error_is_observable.scxml:83

	// W3C SCXML 5.3: <assign location="booms" expr="booms + 1">
	if err := p.assignVariable(`booms`, `_scxml_add(booms, 1)`); err != nil {
		engine.Raise(sce.NewPlatformError(UnhandledErrorIsObservableEventErrorExecution, "<assign> to 'booms' failed"))
	}


	// W3C SCXML 5.3/B.2: Invalid or read-only location ""
	engine.Raise(sce.NewPlatformError(UnhandledErrorIsObservableEventErrorExecution, "<assign> has an invalid or read-only location ''"))

		return
	}
}
