// SCE-GENERATED — DO NOT EDIT
// source-hash: 215c3b8c048d546a929c95bb520cc0c508e71ce4c95c9630e94bb32b22528dc2
// template-hash: 2999a09c910b968e408271dc62f423daf659e11e3dbdea0cdf9857029573f331
// generated-at: 0


// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) 2025-2026 [Author of input SCXML file]
//
// Generated by SCE Code Generator (Go backend)
// From: integration_resources/ancestor_entry_is_not_default_entry/ancestor_entry_is_not_default_entry.scxml
//
// This generated code is MIT licensed and may be freely used in any project.
// Runtime engine dependency (sce-go-runtime): LGPL-2.1-or-later WITH LicenseRef-SCE-Linking-Exception OR LicenseRef-SCE-Commercial
// Full license: https://github.com/newmassrael/scxml-core-engine/blob/main/LICENSE
// Generated Go state machine for `ancestor_entry_is_not_default_entry`.
//
// Generated by SCE Code Generator (Go backend).
// Implements sce.StatePolicy with full transition logic,
// entry/exit actions, and event processing.


// SCE-MAP: ancestor_entry_is_not_default_entry.scxml:69 :: _machine

package ancestor_entry_is_not_default_entry

import (
	"fmt"
	"path/filepath"
	"sort"
	"strings"
	"time"

	sce "github.com/newmassrael/sce-go-runtime"
)

// Ensure imports are used
var (
	_ = fmt.Sprintf
	_ = filepath.Join
	_ = sort.Slice
	_ = strings.HasPrefix
	_ = time.Duration(0)
)

// ======================================================================
// State type (W3C SCXML 3.3)
// ======================================================================

type AncestorEntryIsNotDefaultEntryState int

const (
	AncestorEntryIsNotDefaultEntryStateAway AncestorEntryIsNotDefaultEntryState = 0
	AncestorEntryIsNotDefaultEntryStateByDefault AncestorEntryIsNotDefaultEntryState = 1
	AncestorEntryIsNotDefaultEntryStateChosen AncestorEntryIsNotDefaultEntryState = 2
	AncestorEntryIsNotDefaultEntryStateDrive AncestorEntryIsNotDefaultEntryState = 3
	AncestorEntryIsNotDefaultEntryStateFailDefaulted AncestorEntryIsNotDefaultEntryState = 4
	AncestorEntryIsNotDefaultEntryStateFailIdled AncestorEntryIsNotDefaultEntryState = 5
	AncestorEntryIsNotDefaultEntryStateFailLobbied AncestorEntryIsNotDefaultEntryState = 6
	AncestorEntryIsNotDefaultEntryStateFailTargeted AncestorEntryIsNotDefaultEntryState = 7
	AncestorEntryIsNotDefaultEntryStateIdle AncestorEntryIsNotDefaultEntryState = 8
	AncestorEntryIsNotDefaultEntryStateLobby AncestorEntryIsNotDefaultEntryState = 9
	AncestorEntryIsNotDefaultEntryStateOuter AncestorEntryIsNotDefaultEntryState = 10
	AncestorEntryIsNotDefaultEntryStateRun AncestorEntryIsNotDefaultEntryState = 11
	AncestorEntryIsNotDefaultEntryStateSettled AncestorEntryIsNotDefaultEntryState = 12
	AncestorEntryIsNotDefaultEntryStateWatch AncestorEntryIsNotDefaultEntryState = 13
)

func (s AncestorEntryIsNotDefaultEntryState) String() string {
	switch s {
	case AncestorEntryIsNotDefaultEntryStateAway:
		return "away"
	case AncestorEntryIsNotDefaultEntryStateByDefault:
		return "by_default"
	case AncestorEntryIsNotDefaultEntryStateChosen:
		return "chosen"
	case AncestorEntryIsNotDefaultEntryStateDrive:
		return "drive"
	case AncestorEntryIsNotDefaultEntryStateFailDefaulted:
		return "failDefaulted"
	case AncestorEntryIsNotDefaultEntryStateFailIdled:
		return "failIdled"
	case AncestorEntryIsNotDefaultEntryStateFailLobbied:
		return "failLobbied"
	case AncestorEntryIsNotDefaultEntryStateFailTargeted:
		return "failTargeted"
	case AncestorEntryIsNotDefaultEntryStateIdle:
		return "idle"
	case AncestorEntryIsNotDefaultEntryStateLobby:
		return "lobby"
	case AncestorEntryIsNotDefaultEntryStateOuter:
		return "outer"
	case AncestorEntryIsNotDefaultEntryStateRun:
		return "run"
	case AncestorEntryIsNotDefaultEntryStateSettled:
		return "settled"
	case AncestorEntryIsNotDefaultEntryStateWatch:
		return "watch"
	}
	return "unknown"
}

// AncestorEntryIsNotDefaultEntryStateFromName is the read half of the pair above — a state
// id back into the state it names (W3C SCXML 3.3).
//
// A host that persists where a machine was has to write it down as TEXT: the
// constants above are a build artefact of one binary, and the process that
// resumes is a different one. String publishes the name and this reads it back,
// which is what lets a journal survive its own record and reach
// sce.Engine.EnterAt.
//
// The second return is false for a name this document does not declare. A name
// guessed at rather than refused is how a restore reaches a configuration
// nobody recorded — and the refusal is what makes a typo in a journal a
// reported failure instead of a machine quietly somewhere else.
//
// Emitted from the same loop over the document's states as String, so the two
// age together.
func AncestorEntryIsNotDefaultEntryStateFromName(name string) (AncestorEntryIsNotDefaultEntryState, bool) {
	switch name {
	case "away":
		return AncestorEntryIsNotDefaultEntryStateAway, true
	case "by_default":
		return AncestorEntryIsNotDefaultEntryStateByDefault, true
	case "chosen":
		return AncestorEntryIsNotDefaultEntryStateChosen, true
	case "drive":
		return AncestorEntryIsNotDefaultEntryStateDrive, true
	case "failDefaulted":
		return AncestorEntryIsNotDefaultEntryStateFailDefaulted, true
	case "failIdled":
		return AncestorEntryIsNotDefaultEntryStateFailIdled, true
	case "failLobbied":
		return AncestorEntryIsNotDefaultEntryStateFailLobbied, true
	case "failTargeted":
		return AncestorEntryIsNotDefaultEntryStateFailTargeted, true
	case "idle":
		return AncestorEntryIsNotDefaultEntryStateIdle, true
	case "lobby":
		return AncestorEntryIsNotDefaultEntryStateLobby, true
	case "outer":
		return AncestorEntryIsNotDefaultEntryStateOuter, true
	case "run":
		return AncestorEntryIsNotDefaultEntryStateRun, true
	case "settled":
		return AncestorEntryIsNotDefaultEntryStateSettled, true
	case "watch":
		return AncestorEntryIsNotDefaultEntryStateWatch, true
	}
	var zero AncestorEntryIsNotDefaultEntryState
	return zero, false
}

// AncestorEntryIsNotDefaultEntryAllStates is every state this document declares, in the
// order the constants above are issued.
//
// Emitted from the same loop, so a walk over it is a walk over the document
// rather than over a list somebody maintained beside it — a test that spells
// its own list goes on passing when the document grows a state.
var AncestorEntryIsNotDefaultEntryAllStates = []AncestorEntryIsNotDefaultEntryState{
	AncestorEntryIsNotDefaultEntryStateAway,
	AncestorEntryIsNotDefaultEntryStateByDefault,
	AncestorEntryIsNotDefaultEntryStateChosen,
	AncestorEntryIsNotDefaultEntryStateDrive,
	AncestorEntryIsNotDefaultEntryStateFailDefaulted,
	AncestorEntryIsNotDefaultEntryStateFailIdled,
	AncestorEntryIsNotDefaultEntryStateFailLobbied,
	AncestorEntryIsNotDefaultEntryStateFailTargeted,
	AncestorEntryIsNotDefaultEntryStateIdle,
	AncestorEntryIsNotDefaultEntryStateLobby,
	AncestorEntryIsNotDefaultEntryStateOuter,
	AncestorEntryIsNotDefaultEntryStateRun,
	AncestorEntryIsNotDefaultEntryStateSettled,
	AncestorEntryIsNotDefaultEntryStateWatch,
}

// ======================================================================
// Event type (W3C SCXML 3.12)
// ======================================================================

type AncestorEntryIsNotDefaultEntryEvent int

const (
	AncestorEntryIsNotDefaultEntryEventAgain AncestorEntryIsNotDefaultEntryEvent = 0
	AncestorEntryIsNotDefaultEntryEventBack AncestorEntryIsNotDefaultEntryEvent = 1
	AncestorEntryIsNotDefaultEntryEventCheck AncestorEntryIsNotDefaultEntryEvent = 2
	AncestorEntryIsNotDefaultEntryEventCross AncestorEntryIsNotDefaultEntryEvent = 3
	AncestorEntryIsNotDefaultEntryEventErrorExecution AncestorEntryIsNotDefaultEntryEvent = 4
	// W3C SCXML 3.13: Sentinel for eventless transition dispatch
	AncestorEntryIsNotDefaultEntryEventNull AncestorEntryIsNotDefaultEntryEvent = 5
)

func (e AncestorEntryIsNotDefaultEntryEvent) String() string {
	switch e {
	case AncestorEntryIsNotDefaultEntryEventAgain:
		return "again"
	case AncestorEntryIsNotDefaultEntryEventBack:
		return "back"
	case AncestorEntryIsNotDefaultEntryEventCheck:
		return "check"
	case AncestorEntryIsNotDefaultEntryEventCross:
		return "cross"
	case AncestorEntryIsNotDefaultEntryEventErrorExecution:
		return "error.execution"
	case AncestorEntryIsNotDefaultEntryEventNull:
		return ""
	}
	return "unknown"
}

// ======================================================================
// Policy struct
// ======================================================================

type AncestorEntryIsNotDefaultEntryPolicy struct {
	// W3C SCXML 3.13: Last transition metadata
	lastTransitionIsInternal  bool
	lastTransitionIsTargetless bool
	lastTransitionSourceState AncestorEntryIsNotDefaultEntryState
	// W3C SCXML 3.4: Active state configuration for parallel states / In() predicate
	activeStates []AncestorEntryIsNotDefaultEntryState
	// W3C SCXML 5.10.1: External event flag
	nextEventIsExternal bool
	pendingEventName string
	pendingEventData string
	pendingEventType string
	pendingEventSendid string
	pendingEventOrigin string
	pendingEventOrigintype string
	pendingEventInvokeid string
	// W3C SCXML 5.10: Session ID
	SessionID string
	// W3C SCXML B.1: Script engine handle (per-instance, Path B+ Q1=(d) Go=interface ref)
	ScriptEngine sce.IScriptEngine
	scriptEngineInitialized bool
	// W3C SCXML C.2.3: inbound BasicHTTP endpoint serving this machine,
	// declared by the deployment before Initialize(). The address belongs to
	// whoever runs the listener, so the machine takes it from here rather than
	// guessing one. Empty means no such endpoint is deployed, and no BasicHTTP
	// entry is published in _ioprocessors.
	BasicHTTPAccessURI string
	inPredicateStates []string
	// W3C SCXML 6.4: Parent communication
	ParentExternalQueue chan sce.ParentEvent
	InvokeID           string
	ChildSessionID     string
	// §scxml-6.4.3: invoke param VALUES staged by the parent, applied after
	// this machine's datamodel init so they override its `<data>` defaults.
	pendingParams      map[string]interface{}
}

// NewAncestorEntryIsNotDefaultEntryPolicy creates a new policy with default values.
func NewAncestorEntryIsNotDefaultEntryPolicy() AncestorEntryIsNotDefaultEntryPolicy {
	return AncestorEntryIsNotDefaultEntryPolicy{
		lastTransitionSourceState: AncestorEntryIsNotDefaultEntryStateAway,
		activeStates: make([]AncestorEntryIsNotDefaultEntryState, 0),
	}
}

// Defaulted reports what the `defaulted` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `defaulted` was assigned a value of another type, or the engine refused.
func (p *AncestorEntryIsNotDefaultEntryPolicy) Defaulted() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "defaulted")
}

// Lobbied reports what the `lobbied` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `lobbied` was assigned a value of another type, or the engine refused.
func (p *AncestorEntryIsNotDefaultEntryPolicy) Lobbied() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "lobbied")
}

// Idled reports what the `idled` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `idled` was assigned a value of another type, or the engine refused.
func (p *AncestorEntryIsNotDefaultEntryPolicy) Idled() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "idled")
}

// Targeted reports what the `targeted` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `targeted` was assigned a value of another type, or the engine refused.
func (p *AncestorEntryIsNotDefaultEntryPolicy) Targeted() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "targeted")
}



// IsStateActive checks if a state is active by name (W3C SCXML 5.9.2: In() predicate).
func (p *AncestorEntryIsNotDefaultEntryPolicy) IsStateActive(stateID string) bool {
	for _, s := range p.activeStates {
		if p.GetStateName(s) == stateID {
			return true
		}
	}
	return false
}


// ensureScriptEngine lazily initializes the script engine session.
//
// Engine DI Parity RFC: panics if ScriptEngine was not set on the policy
// before Initialize() — mirrors Kotlin's mandatory constructor parameter
// contract. Silent no-op would otherwise turn missed injection into a
// hidden runtime regression (every <assign>/<log>/guard becomes a no-op
// and the SM walks to its default-state final without diagnostic).
func (p *AncestorEntryIsNotDefaultEntryPolicy) ensureScriptEngine() {
	if p.scriptEngineInitialized {
		return
	}
	engine := p.ScriptEngine
	if engine == nil {
		panic("AncestorEntryIsNotDefaultEntryPolicy: ScriptEngine field must be set before Initialize()")
	}
	sessionID := p.SessionID
	if sessionID == "" {
		sessionID = sce.GenerateSessionID()
		p.SessionID = sessionID
	}
	if !engine.HasSession(sessionID) {
		_ = engine.CreateSession(sessionID)
	}
	// W3C SCXML C.1.1 / C.2.3: the _ioprocessors entries come from the same
	// helper every other backend uses, so a machine reads the same entry names
	// and the same addresses whichever one runs it.
	_ = engine.SetupSystemVariables(sessionID, "ancestor_entry_is_not_default_entry",
		sce.BuildIoProcessors(sessionID, p.BasicHTTPAccessURI))
	// W3C SCXML 5.9.2: Register In() predicate callback
	engine.SetStateQueryCallback(sessionID, func(stateID string) bool {
		return p.IsStateActive(stateID)
	})
	p.scriptEngineInitialized = true
}

// InitializeDataModel sets up datamodel via script engine (W3C SCXML 5.3).
func (p *AncestorEntryIsNotDefaultEntryPolicy) InitializeDataModel(eng *sce.Engine[AncestorEntryIsNotDefaultEntryState, AncestorEntryIsNotDefaultEntryEvent]) {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	sessionID := p.SessionID

	// W3C SCXML 5.3: Initialize _sessionid
	_ = engine.SetVariable(sessionID, "_sessionid", sessionID)

	// W3C SCXML 5.3: Initialize _name
	_ = engine.SetVariable(sessionID, "_name", "ancestor_entry_is_not_default_entry")

	// W3C SCXML 5.2.2: Initialize global datamodel variables
	// W3C SCXML 5.2/5.3: Initialize defaulted from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "defaulted", result)
		} else {
			eng.Raise(sce.NewPlatformError(AncestorEntryIsNotDefaultEntryEventErrorExecution, "<data id='defaulted'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "defaulted", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize lobbied from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "lobbied", result)
		} else {
			eng.Raise(sce.NewPlatformError(AncestorEntryIsNotDefaultEntryEventErrorExecution, "<data id='lobbied'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "lobbied", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize idled from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "idled", result)
		} else {
			eng.Raise(sce.NewPlatformError(AncestorEntryIsNotDefaultEntryEventErrorExecution, "<data id='idled'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "idled", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize targeted from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "targeted", result)
		} else {
			eng.Raise(sce.NewPlatformError(AncestorEntryIsNotDefaultEntryEventErrorExecution, "<data id='targeted'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "targeted", nil)
		}
	}



	// §scxml-6.4.3: apply the invoke params the parent staged — overriding the
	// `<data>` default this block just seeded, and only for a name the child
	// declares ("if the names do not match, the Processor MUST NOT add the
	// value ... to the invoked session's data model"). `HasVariable` is that
	// filter, and it is this channel's only copy of the rule.
	if len(p.pendingParams) > 0 {
		for paramName, paramValue := range p.pendingParams {
			if engine.HasVariable(sessionID, paramName) {
				_ = engine.SetVariable(sessionID, paramName, paramValue)
			}
		}
		p.pendingParams = nil
	}
}

// evaluateGuard evaluates a guard condition via script engine.
// W3C SCXML 4.3.2: If a condition expression cannot be evaluated, the processor
// MUST treat it as false and MUST raise error.execution.
func (p *AncestorEntryIsNotDefaultEntryPolicy) evaluateGuard(guard string, eng *sce.Engine[AncestorEntryIsNotDefaultEntryState, AncestorEntryIsNotDefaultEntryEvent]) bool {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	result, err := engine.EvaluateExpression(p.SessionID, guard)
	if err != nil {
		eng.Raise(sce.NewPlatformError(AncestorEntryIsNotDefaultEntryEventErrorExecution, "a <transition> cond failed to evaluate"))
		return false
	}
	return sce.ScriptToBool(result)
}

// executeScript executes a script block via script engine.
func (p *AncestorEntryIsNotDefaultEntryPolicy) executeScript(script string) error {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	return engine.ExecuteScript(p.SessionID, script)
}

// setCurrentEvent binds _event system variable (W3C SCXML 5.10) and answers
// which rung of §scxml-B-2-8-1 the payload got, because the binding is the
// only place that knows.
//
// This used to be `_ = engine.SetCurrentEvent(...)`: the ladder decided
// between a DOM, a value and a space-normalized string, and the decision was
// dropped one line later. A payload that announced structure and would not
// parse therefore reached the document as raw characters, every
// _event.data.<field> read empty, and nothing anywhere could say so — see
// sce.PayloadReading.
func (p *AncestorEntryIsNotDefaultEntryPolicy) setCurrentEvent(name string) sce.PayloadReading {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	data := p.pendingEventData
	eventType := p.pendingEventType
	sendID := p.pendingEventSendid
	// W3C SCXML C.1: `_event.origin` is the sender's published
	// `_ioprocessors` location, not its bare session id — and this is the one
	// place that publishes `_event` to the document, so this is where the id
	// becomes a location. The engine keeps the bare id in
	// `EventMetadata.Origin` because its session-keyed lookups (<finalize>
	// dispatch, cancelled-invoke filtering) match on it; converting at the
	// raise would make one value serve two consumers that need different
	// spellings. The conversion itself lives in `sce.PublishedOrigin`, the
	// port of the `IOProcessorHelper::publishedOrigin` the C++ engines share:
	// a second spelling of the rule is how the backends would stop agreeing.
	origin := sce.PublishedOrigin(p.pendingEventOrigin)
	originType := p.pendingEventOrigintype
	invokeID := p.pendingEventInvokeid
	reading, err := engine.SetCurrentEvent(p.SessionID, sce.SetCurrentEventArgs{
		Name:       name,
		Data:       data,
		Type:       eventType,
		SendID:     sendID,
		Origin:     origin,
		OriginType: originType,
		InvokeID:   invokeID,
	})
	if err != nil {
		// An engine that could not bind _event at all has a larger problem
		// than an unreadable payload, and reporting PayloadUndecodable for it
		// would point a host at the wrong thing.
		return sce.PayloadAbsent
	}
	return reading
}

// assignVariable assigns a value to a datamodel variable via script engine.
func (p *AncestorEntryIsNotDefaultEntryPolicy) assignVariable(location, expr string) error {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	script := location + " = " + expr
	return engine.ExecuteScript(p.SessionID, script)
}

// getVariable reads a datamodel variable from the script engine.
func (p *AncestorEntryIsNotDefaultEntryPolicy) getVariable(name string) interface{} {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	result, err := engine.GetVariable(p.SessionID, name)
	if err != nil {
		return nil
	}
	return result
}

// SetParamValueInScriptEngine stores an invoke param's VALUE for deferred
// application after datamodel init (§scxml-6.4.3).
//
// Params are applied at the end of InitializeDataModel, overriding the
// `<data>` default only for names the child declares. The value crosses as a
// value: this took the parent's value rendered as Lua source and
// re-evaluated it here until 2026-08-21, which survived every value a test
// had tried and lost the ones Lua cannot spell — `ToScriptLiteral` renders a
// non-finite number as `+Inf`, which is not a Lua expression, so an
// `<invoke>` `<param expr="1/0"/>` arrived as nothing at all.
func (p *AncestorEntryIsNotDefaultEntryPolicy) SetParamValueInScriptEngine(name string, value interface{}) {
	if p.pendingParams == nil {
		p.pendingParams = make(map[string]interface{})
	}
	p.pendingParams[name] = value
}


// ExecutePendingInvokes is a no-op (no invokes in this SM).
func (p *AncestorEntryIsNotDefaultEntryPolicy) ExecutePendingInvokes(engine *sce.Engine[AncestorEntryIsNotDefaultEntryState, AncestorEntryIsNotDefaultEntryEvent]) {}

// TickChildren is a no-op (no invokes in this SM).
func (p *AncestorEntryIsNotDefaultEntryPolicy) TickChildren(engine *sce.Engine[AncestorEntryIsNotDefaultEntryState, AncestorEntryIsNotDefaultEntryEvent]) {}

// ======================================================================
// StatePolicy interface implementation
// ======================================================================

// InitialState returns the initial state (W3C SCXML 3.3).
func (p *AncestorEntryIsNotDefaultEntryPolicy) InitialState() AncestorEntryIsNotDefaultEntryState {
	return AncestorEntryIsNotDefaultEntryStateAway
}

// IsFinalState returns true if state is a <final> state (W3C SCXML 3.7).
func (p *AncestorEntryIsNotDefaultEntryPolicy) IsFinalState(state AncestorEntryIsNotDefaultEntryState) bool {
	switch state {
	case AncestorEntryIsNotDefaultEntryStateFailDefaulted:
		return true
	case AncestorEntryIsNotDefaultEntryStateFailIdled:
		return true
	case AncestorEntryIsNotDefaultEntryStateFailLobbied:
		return true
	case AncestorEntryIsNotDefaultEntryStateFailTargeted:
		return true
	case AncestorEntryIsNotDefaultEntryStateSettled:
		return true
	}
	return false
}

// GetParent returns the parent of state, or (zero, false) if root (W3C SCXML 3.3).
func (p *AncestorEntryIsNotDefaultEntryPolicy) GetParent(state AncestorEntryIsNotDefaultEntryState) (AncestorEntryIsNotDefaultEntryState, bool) {
	switch state {
	case AncestorEntryIsNotDefaultEntryStateByDefault:
		return AncestorEntryIsNotDefaultEntryStateOuter, true
	case AncestorEntryIsNotDefaultEntryStateChosen:
		return AncestorEntryIsNotDefaultEntryStateOuter, true
	case AncestorEntryIsNotDefaultEntryStateDrive:
		return AncestorEntryIsNotDefaultEntryStateRun, true
	case AncestorEntryIsNotDefaultEntryStateIdle:
		return AncestorEntryIsNotDefaultEntryStateWatch, true
	case AncestorEntryIsNotDefaultEntryStateLobby:
		return AncestorEntryIsNotDefaultEntryStateDrive, true
	case AncestorEntryIsNotDefaultEntryStateOuter:
		return AncestorEntryIsNotDefaultEntryStateDrive, true
	case AncestorEntryIsNotDefaultEntryStateWatch:
		return AncestorEntryIsNotDefaultEntryStateRun, true
	}
	return 0, false
}

// IsCompoundState returns true if state has children (W3C SCXML 3.3).
func (p *AncestorEntryIsNotDefaultEntryPolicy) IsCompoundState(state AncestorEntryIsNotDefaultEntryState) bool {
	switch state {
	case AncestorEntryIsNotDefaultEntryStateDrive:
		return true
	case AncestorEntryIsNotDefaultEntryStateOuter:
		return true
	case AncestorEntryIsNotDefaultEntryStateWatch:
		return true
	}
	return false
}

// IsParallelState returns true if state is a <parallel> state (W3C SCXML 3.4).
func (p *AncestorEntryIsNotDefaultEntryPolicy) IsParallelState(state AncestorEntryIsNotDefaultEntryState) bool {
	switch state {
	case AncestorEntryIsNotDefaultEntryStateRun:
		return true
	}
	return false
}

// GetParallelRegions returns child regions of a parallel state (W3C SCXML 3.4).
func (p *AncestorEntryIsNotDefaultEntryPolicy) GetParallelRegions(state AncestorEntryIsNotDefaultEntryState) []AncestorEntryIsNotDefaultEntryState {
	switch state {
	case AncestorEntryIsNotDefaultEntryStateRun:
		return []AncestorEntryIsNotDefaultEntryState{
			AncestorEntryIsNotDefaultEntryStateDrive,
			AncestorEntryIsNotDefaultEntryStateWatch,
		}
	}
	return nil
}

// IsDescendantOf returns true if desc is a descendant of anc (W3C SCXML 3.12).
func (p *AncestorEntryIsNotDefaultEntryPolicy) IsDescendantOf(desc, anc AncestorEntryIsNotDefaultEntryState) bool {
	current := desc
	for {
		parent, ok := p.GetParent(current)
		if !ok {
			return false
		}
		if parent == anc {
			return true
		}
		current = parent
	}
}

// GetDocumentOrder returns the document order index (W3C SCXML Appendix D).
func (p *AncestorEntryIsNotDefaultEntryPolicy) GetDocumentOrder(state AncestorEntryIsNotDefaultEntryState) int {
	switch state {
	case AncestorEntryIsNotDefaultEntryStateAway:
		return 0
	case AncestorEntryIsNotDefaultEntryStateByDefault:
		return 5
	case AncestorEntryIsNotDefaultEntryStateChosen:
		return 6
	case AncestorEntryIsNotDefaultEntryStateDrive:
		return 2
	case AncestorEntryIsNotDefaultEntryStateFailDefaulted:
		return 10
	case AncestorEntryIsNotDefaultEntryStateFailIdled:
		return 12
	case AncestorEntryIsNotDefaultEntryStateFailLobbied:
		return 11
	case AncestorEntryIsNotDefaultEntryStateFailTargeted:
		return 13
	case AncestorEntryIsNotDefaultEntryStateIdle:
		return 8
	case AncestorEntryIsNotDefaultEntryStateLobby:
		return 3
	case AncestorEntryIsNotDefaultEntryStateOuter:
		return 4
	case AncestorEntryIsNotDefaultEntryStateRun:
		return 1
	case AncestorEntryIsNotDefaultEntryStateSettled:
		return 9
	case AncestorEntryIsNotDefaultEntryStateWatch:
		return 7
	}
	return -1
}

// GetEventName returns the string name of an event (W3C SCXML 3.12).
func (p *AncestorEntryIsNotDefaultEntryPolicy) GetEventName(event AncestorEntryIsNotDefaultEntryEvent) string {
	return event.String()
}

// GetEventFromName looks up an event by name (W3C SCXML 3.12).
func (p *AncestorEntryIsNotDefaultEntryPolicy) GetEventFromName(name string) (AncestorEntryIsNotDefaultEntryEvent, bool) {
	switch name {
	case "again":
		return AncestorEntryIsNotDefaultEntryEventAgain, true
	case "back":
		return AncestorEntryIsNotDefaultEntryEventBack, true
	case "check":
		return AncestorEntryIsNotDefaultEntryEventCheck, true
	case "cross":
		return AncestorEntryIsNotDefaultEntryEventCross, true
	case "error.execution":
		return AncestorEntryIsNotDefaultEntryEventErrorExecution, true
	}
	return AncestorEntryIsNotDefaultEntryEventNull, false
}

// GetStateName returns the string name of a state (W3C SCXML 3.3).
func (p *AncestorEntryIsNotDefaultEntryPolicy) GetStateName(state AncestorEntryIsNotDefaultEntryState) string {
	return state.String()
}

// GetStateFromName reads a state id back into the state it names (W3C SCXML 3.3).
// The reverse of GetStateName, and what turns a host's recorded configuration
// back into the argument sce.Engine.EnterAt takes.
func (p *AncestorEntryIsNotDefaultEntryPolicy) GetStateFromName(name string) (AncestorEntryIsNotDefaultEntryState, bool) {
	return AncestorEntryIsNotDefaultEntryStateFromName(name)
}

// NullEvent returns the sentinel for eventless transition dispatch (W3C SCXML 3.13).
func (p *AncestorEntryIsNotDefaultEntryPolicy) NullEvent() AncestorEntryIsNotDefaultEntryEvent {
	return AncestorEntryIsNotDefaultEntryEventNull
}

// GetInitialChildren returns initial children of a compound state (W3C SCXML 3.6).
func (p *AncestorEntryIsNotDefaultEntryPolicy) GetInitialChildren(state AncestorEntryIsNotDefaultEntryState) []AncestorEntryIsNotDefaultEntryState {
	switch state {
	case AncestorEntryIsNotDefaultEntryStateDrive:
		return []AncestorEntryIsNotDefaultEntryState{
			AncestorEntryIsNotDefaultEntryStateLobby,
		}
	case AncestorEntryIsNotDefaultEntryStateOuter:
		return []AncestorEntryIsNotDefaultEntryState{
			AncestorEntryIsNotDefaultEntryStateByDefault,
		}
	case AncestorEntryIsNotDefaultEntryStateWatch:
		return []AncestorEntryIsNotDefaultEntryState{
			AncestorEntryIsNotDefaultEntryStateIdle,
		}
	}
	return nil
}

// LastTransitionIsInternal returns the internal transition flag (W3C SCXML 3.13).
func (p *AncestorEntryIsNotDefaultEntryPolicy) LastTransitionIsInternal() bool {
	return p.lastTransitionIsInternal
}

// SetLastTransitionIsInternal sets the internal transition flag.
func (p *AncestorEntryIsNotDefaultEntryPolicy) SetLastTransitionIsInternal(value bool) {
	p.lastTransitionIsInternal = value
}

// LastTransitionIsTargetless returns the targetless transition flag (W3C SCXML 3.13).
func (p *AncestorEntryIsNotDefaultEntryPolicy) LastTransitionIsTargetless() bool {
	return p.lastTransitionIsTargetless
}

// SetLastTransitionIsTargetless sets the targetless transition flag.
func (p *AncestorEntryIsNotDefaultEntryPolicy) SetLastTransitionIsTargetless(value bool) {
	p.lastTransitionIsTargetless = value
}

// LastTransitionSourceState returns the source state of the last transition.
func (p *AncestorEntryIsNotDefaultEntryPolicy) LastTransitionSourceState() AncestorEntryIsNotDefaultEntryState {
	return p.lastTransitionSourceState
}

// SetLastTransitionSourceState sets the source state of the last transition.
func (p *AncestorEntryIsNotDefaultEntryPolicy) SetLastTransitionSourceState(state AncestorEntryIsNotDefaultEntryState) {
	p.lastTransitionSourceState = state
}

// GetActiveStates returns the active state configuration (W3C SCXML 3.4).
func (p *AncestorEntryIsNotDefaultEntryPolicy) GetActiveStates() []AncestorEntryIsNotDefaultEntryState {
	return p.activeStates
}

// SetActiveStates hands this machine an active set whole (W3C SCXML 3.4).
// The write half of GetActiveStates: entry and exit grow the set one state at a
// time as they walk, and sce.Engine.EnterAt hands back a configuration an
// earlier run already settled on. Copied rather than aliased so a caller's slice
// cannot go on changing this machine's configuration behind it.
func (p *AncestorEntryIsNotDefaultEntryPolicy) SetActiveStates(states []AncestorEntryIsNotDefaultEntryState) {
	p.activeStates = append(p.activeStates[:0:0], states...)
}

// SetNextEventIsExternal sets the external event flag (W3C SCXML 5.10.1).
func (p *AncestorEntryIsNotDefaultEntryPolicy) SetNextEventIsExternal(value bool) {
	p.nextEventIsExternal = value
}

// HasParallelStates returns whether the SM has parallel states.
func (p *AncestorEntryIsNotDefaultEntryPolicy) HasParallelStates() bool {
	return true
}

// NeedsScriptEngine returns whether the SM needs a script engine.
func (p *AncestorEntryIsNotDefaultEntryPolicy) NeedsScriptEngine() bool {
	return true
}

// NeedsEventScheduler reports whether a host must drive this machine with
// Tick rather than Step alone.
//
// W3C SCXML 6.2: Step runs a macrostep and never drains the delayed-send
// scheduler nor ticks an invoked child, so a host that only ever calls it
// waits forever with nothing said. Same verdict the generate manifest
// publishes as needs_event_scheduler.
func (p *AncestorEntryIsNotDefaultEntryPolicy) NeedsEventScheduler() bool {
	return false
}

// NeedsDataModelInit returns whether the SM needs datamodel init.
func (p *AncestorEntryIsNotDefaultEntryPolicy) NeedsDataModelInit() bool {
	return true
}

func (p *AncestorEntryIsNotDefaultEntryPolicy) HasInvokeSupport() bool { return false }
func (p *AncestorEntryIsNotDefaultEntryPolicy) HasChildTick() bool { return false }
func (p *AncestorEntryIsNotDefaultEntryPolicy) HasFinalize() bool { return false }
func (p *AncestorEntryIsNotDefaultEntryPolicy) HasAutoforward() bool { return false }
func (p *AncestorEntryIsNotDefaultEntryPolicy) HasActiveStates() bool { return true }
func (p *AncestorEntryIsNotDefaultEntryPolicy) HasExternalEventFlag() bool { return true }
// GetInitialOrHistoryChild returns the initial child considering history (W3C SCXML 3.11).
func (p *AncestorEntryIsNotDefaultEntryPolicy) GetInitialOrHistoryChild(state AncestorEntryIsNotDefaultEntryState) AncestorEntryIsNotDefaultEntryState {
	children := p.GetInitialChildren(state)
	if len(children) > 0 {
		return children[0]
	}
	return state
}
// ExecuteFinalizeForChildEvent is a no-op (no finalize invokes).
func (p *AncestorEntryIsNotDefaultEntryPolicy) ExecuteFinalizeForChildEvent(_ *sce.EventWithMetadata[AncestorEntryIsNotDefaultEntryEvent], _ *sce.Engine[AncestorEntryIsNotDefaultEntryState, AncestorEntryIsNotDefaultEntryEvent]) {}
// ForwardToAutoforwardChildren is a no-op (no autoforward invokes).
func (p *AncestorEntryIsNotDefaultEntryPolicy) ForwardToAutoforwardChildren(_ string, _ sce.EventMetadata, _ *sce.Engine[AncestorEntryIsNotDefaultEntryState, AncestorEntryIsNotDefaultEntryEvent]) {}

// PopulateEventMetadata stores pending event metadata (W3C SCXML 5.10).
// Note: event name is set separately via setCurrentEvent(), not from metadata.
func (p *AncestorEntryIsNotDefaultEntryPolicy) PopulateEventMetadata(meta *sce.EventMetadata) {
	p.pendingEventData = meta.Data
	p.pendingEventType = meta.EventType.String()
	p.pendingEventSendid = meta.SendID
	p.pendingEventOrigin = meta.Origin
	p.pendingEventOrigintype = meta.OriginType
	p.pendingEventInvokeid = meta.InvokeID
}

// ClearEventMetadata resets pending event metadata (W3C SCXML 5.10).
func (p *AncestorEntryIsNotDefaultEntryPolicy) ClearEventMetadata() {
	p.pendingEventName = ""
	p.pendingEventData = ""
	p.pendingEventType = ""
	p.pendingEventSendid = ""
	p.pendingEventOrigin = ""
	p.pendingEventOrigintype = ""
	p.pendingEventInvokeid = ""
}




// ExecuteEntryActions executes onentry actions for a state (W3C SCXML 3.8).
//line ancestor_entry_is_not_default_entry.scxml:69
func (p *AncestorEntryIsNotDefaultEntryPolicy) ExecuteEntryActions(state AncestorEntryIsNotDefaultEntryState, engine *sce.Engine[AncestorEntryIsNotDefaultEntryState, AncestorEntryIsNotDefaultEntryEvent], pathChild *AncestorEntryIsNotDefaultEntryState) {
	p.ensureScriptEngine()
	// W3C SCXML 3.4/3.12.1: Add state to active configuration for parallel states and In() predicate
	for _, s := range p.activeStates {
		if s == state {
			return // W3C SCXML 3.8: Skip onentry actions for duplicate state entry
		}
	}
	p.activeStates = append(p.activeStates, state)
	switch state {
	case AncestorEntryIsNotDefaultEntryStateByDefault:
		//line ancestor_entry_is_not_default_entry.scxml:116
		// W3C SCXML 3.8: onentry block 0 (break on error stops subsequent actions)
		for actionBlock0 := 0; actionBlock0 < 1; actionBlock0++ {
			_ = actionBlock0

	// W3C SCXML 5.3: <assign location="defaulted" expr="defaulted + 1">
	if err := p.assignVariable(`defaulted`, `_scxml_add(defaulted, 1)`); err != nil {
		engine.Raise(sce.NewPlatformError(AncestorEntryIsNotDefaultEntryEventErrorExecution, "<assign> to 'defaulted' failed"))
		break
	}

		}
	case AncestorEntryIsNotDefaultEntryStateChosen:
		//line ancestor_entry_is_not_default_entry.scxml:127
		// W3C SCXML 3.8: onentry block 0 (break on error stops subsequent actions)
		for actionBlock0 := 0; actionBlock0 < 1; actionBlock0++ {
			_ = actionBlock0

	// W3C SCXML 5.3: <assign location="targeted" expr="targeted + 1">
	if err := p.assignVariable(`targeted`, `_scxml_add(targeted, 1)`); err != nil {
		engine.Raise(sce.NewPlatformError(AncestorEntryIsNotDefaultEntryEventErrorExecution, "<assign> to 'targeted' failed"))
		break
	}

		}
	case AncestorEntryIsNotDefaultEntryStateIdle:
		//line ancestor_entry_is_not_default_entry.scxml:150
		// W3C SCXML 3.8: onentry block 0 (break on error stops subsequent actions)
		for actionBlock0 := 0; actionBlock0 < 1; actionBlock0++ {
			_ = actionBlock0

	// W3C SCXML 5.3: <assign location="idled" expr="idled + 1">
	if err := p.assignVariable(`idled`, `_scxml_add(idled, 1)`); err != nil {
		engine.Raise(sce.NewPlatformError(AncestorEntryIsNotDefaultEntryEventErrorExecution, "<assign> to 'idled' failed"))
		break
	}

		}
	case AncestorEntryIsNotDefaultEntryStateLobby:
		//line ancestor_entry_is_not_default_entry.scxml:102
		// W3C SCXML 3.8: onentry block 0 (break on error stops subsequent actions)
		for actionBlock0 := 0; actionBlock0 < 1; actionBlock0++ {
			_ = actionBlock0

	// W3C SCXML 5.3: <assign location="lobbied" expr="lobbied + 1">
	if err := p.assignVariable(`lobbied`, `_scxml_add(lobbied, 1)`); err != nil {
		engine.Raise(sce.NewPlatformError(AncestorEntryIsNotDefaultEntryEventErrorExecution, "<assign> to 'lobbied' failed"))
		break
	}

		}
	default:
		// No entry actions
	}

	// W3C SCXML 3.4: If entering parallel state, enter all child regions
	//
	// §scxml-D-addDescendantStatesToEnter: a <parallel> hands out defaults even
	// when it is only an ancestor — Appendix D's one exception to the ancestor
	// rule. The exception has its own exception: not the region the entry set is
	// already descending into. pathChild names it, and giving that one its
	// default too is what leaves two children of it active at once.
	if p.IsParallelState(state) {
		regions := p.GetParallelRegions(state)
		for _, region := range regions {
			if pathChild != nil && *pathChild == region {
				continue
			}
			p.ExecuteEntryActions(region, engine, nil)

			// W3C SCXML 3.3: If region is compound, enter initial child
			if p.IsCompoundState(region) {
				initialChild := p.GetInitialOrHistoryChild(region)
				if initialChild != region {
					p.ExecuteEntryActions(initialChild, engine, nil)
				}
			}
		}
	}

	// W3C SCXML 3.3: If entering compound state (non-parallel), enter initial child
	//
	// §scxml-D-addAncestorStatesToEnter: not when `state` is merely on the way
	// to a deeper target. The entry set already holds pathChild, and a compound
	// state holds one child at a time.
	if p.IsCompoundState(state) && !p.IsParallelState(state) && pathChild == nil {
		initialChildren := p.GetInitialChildren(state)
		if len(initialChildren) > 0 {
			// W3C SCXML 3.6: Enter through hierarchy to reach initial target(s)
			for _, target := range initialChildren {
				var chain []AncestorEntryIsNotDefaultEntryState
				current := target
				for current != state {
					chain = append([]AncestorEntryIsNotDefaultEntryState{current}, chain...)
					parent, hasParent := p.GetParent(current)
					if !hasParent {
						break
					}
					current = parent
				}
				// Each link but the last is an ancestor of the initial target,
				// so it takes no default of its own — the chain already names
				// the child that is entering.
				for i := range chain {
					var next *AncestorEntryIsNotDefaultEntryState
					if i+1 < len(chain) {
						next = &chain[i+1]
					}
					p.ExecuteEntryActions(chain[i], engine, next)
				}
			}
		} else {
			initialChild := p.GetInitialOrHistoryChild(state)
			if initialChild != state {
				p.ExecuteEntryActions(initialChild, engine, nil)
			}
		}
	}
}

// ExecuteExitActions executes onexit actions for a state (W3C SCXML 3.9).
//line ancestor_entry_is_not_default_entry.scxml:69
func (p *AncestorEntryIsNotDefaultEntryPolicy) ExecuteExitActions(state AncestorEntryIsNotDefaultEntryState, engine *sce.Engine[AncestorEntryIsNotDefaultEntryState, AncestorEntryIsNotDefaultEntryEvent], preTransitionActive []AncestorEntryIsNotDefaultEntryState) {
	p.ensureScriptEngine()
	// W3C SCXML 3.4 + 3.13: Parallel state exit order — exit all active descendants first
	if p.IsParallelState(state) {
		var descendantsToExit []AncestorEntryIsNotDefaultEntryState
		for _, s := range p.activeStates {
			if s != state && p.IsDescendantOf(s, state) {
				descendantsToExit = append(descendantsToExit, s)
			}
		}
		// W3C SCXML 3.13: Sort descendants by reverse document order (deepest first)
		sort.Slice(descendantsToExit, func(i, j int) bool {
			return p.GetDocumentOrder(descendantsToExit[i]) > p.GetDocumentOrder(descendantsToExit[j])
		})
		for _, descendant := range descendantsToExit {
			p.ExecuteExitActions(descendant, engine, preTransitionActive)
		}
	}
	// W3C SCXML 3.4/3.12.1: Remove state from active configuration
	p.activeStates = func() []AncestorEntryIsNotDefaultEntryState {
		var result []AncestorEntryIsNotDefaultEntryState
		for _, s := range p.activeStates {
			if s != state {
				result = append(result, s)
			}
		}
		return result
	}()
	switch state {
	default:
		// No exit actions
	}
}

// ProcessTransition evaluates guards and takes a matching transition (W3C SCXML 3.13).
// Returns true if a transition was taken.
//line ancestor_entry_is_not_default_entry.scxml:69
func (p *AncestorEntryIsNotDefaultEntryPolicy) ProcessTransition(currentState *AncestorEntryIsNotDefaultEntryState, event AncestorEntryIsNotDefaultEntryEvent, engine *sce.Engine[AncestorEntryIsNotDefaultEntryState, AncestorEntryIsNotDefaultEntryEvent]) bool {
	// W3C SCXML 5.10: Bind _event system variable for guard evaluation
	if event != AncestorEntryIsNotDefaultEntryEventNull {
		// §scxml-B-2-8-1: the rung the payload got, handed to the engine
		// rather than dropped. This is the only frame that has both the
		// reading and the event it belongs to.
		engine.NotePayloadReading(event, p.setCurrentEvent(p.GetEventName(event)))
	}

	// W3C SCXML 3.4 + 3.12 + Appendix D: Parallel state transition handling
	if event == AncestorEntryIsNotDefaultEntryEventNull {
		// W3C SCXML Appendix D: Eventless transitions - collect from all active states
		var enabledTransitions []transitionInfo
		statesToCheck := make([]AncestorEntryIsNotDefaultEntryState, len(p.activeStates))
		copy(statesToCheck, p.activeStates)

		// Sort by document order for consistent processing
		sort.Slice(statesToCheck, func(i, j int) bool {
			return p.GetDocumentOrder(statesToCheck[i]) < p.GetDocumentOrder(statesToCheck[j])
		})

		for _, activeState := range statesToCheck {
			// W3C SCXML 3.13: Eventless transitions do NOT bubble to parent states
			if trans := p.tryCollectTransition(activeState, event, engine); trans != nil {
				enabledTransitions = append(enabledTransitions, *trans)
			}
		}

		// Appendix D removeConflictingTransitions: Remove conflicting transitions
		if len(enabledTransitions) > 0 {
			enabledTransitions = p.selectOptimalTransitions(enabledTransitions)
		}

		// W3C SCXML Appendix D Steps 2-5: Execute as atomic microstep
		if len(enabledTransitions) > 0 {
			p.executeMicrostep(enabledTransitions, currentState, engine)
			return true
		}
	} else {
		// W3C SCXML Appendix D: External events - collect from active leaf states
		var enabledTransitions []transitionInfo

		for _, activeState := range p.activeStates {
			isNonAtomic := p.IsCompoundState(activeState) || p.IsParallelState(activeState)

			// W3C SCXML 3.13: Check if this is a done.state event
			eventName := p.GetEventName(event)
			isDoneStateEvent := event != AncestorEntryIsNotDefaultEntryEventNull && strings.HasPrefix(eventName, "done.state.")

			// Skip non-atomic states UNLESS processing done.state event
			if isNonAtomic && !isDoneStateEvent {
				continue
			}

			// W3C SCXML 3.12: Hierarchical event bubbling
			checkState := activeState
			for {
				if trans := p.tryCollectTransition(checkState, event, engine); trans != nil {
					enabledTransitions = append(enabledTransitions, *trans)
					break
				}
				parent, hasParent := p.GetParent(checkState)
				if !hasParent {
					break
				}
				checkState = parent
			}
		}

		// W3C SCXML 3.13: Deduplicate transitions from multiple descendants
		seen := make(map[[2]int]bool)
		var deduped []transitionInfo
		for _, t := range enabledTransitions {
			key := [2]int{int(t.source), t.transitionIndex}
			if !seen[key] {
				seen[key] = true
				deduped = append(deduped, t)
			}
		}
		enabledTransitions = deduped

		// Appendix D removeConflictingTransitions: Remove conflicting transitions
		if len(enabledTransitions) > 0 {
			enabledTransitions = p.selectOptimalTransitions(enabledTransitions)
		}

		// W3C SCXML Appendix D Steps 2-5: Execute as atomic microstep
		if len(enabledTransitions) > 0 {
			p.executeMicrostep(enabledTransitions, currentState, engine)
			return true
		}
	}

	return false
}

// tryCollectTransition checks if a state has a matching transition and returns it as transitionInfo.
//line ancestor_entry_is_not_default_entry.scxml:69
func (p *AncestorEntryIsNotDefaultEntryPolicy) tryCollectTransition(checkState AncestorEntryIsNotDefaultEntryState, event AncestorEntryIsNotDefaultEntryEvent, engine *sce.Engine[AncestorEntryIsNotDefaultEntryState, AncestorEntryIsNotDefaultEntryEvent]) *transitionInfo {
	switch checkState {
	case AncestorEntryIsNotDefaultEntryStateAway:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AncestorEntryIsNotDefaultEntryEventCross {
			return &transitionInfo{
				source:          AncestorEntryIsNotDefaultEntryStateAway,
				target:          AncestorEntryIsNotDefaultEntryStateChosen,
				transitionIndex: 0,
				hasActions:      false,
				isInternal:      false,
				isTargetless:    false,
			}
		}
	case AncestorEntryIsNotDefaultEntryStateChosen:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AncestorEntryIsNotDefaultEntryEventBack {
			return &transitionInfo{
				source:          AncestorEntryIsNotDefaultEntryStateChosen,
				target:          AncestorEntryIsNotDefaultEntryStateLobby,
				transitionIndex: 0,
				hasActions:      false,
				isInternal:      false,
				isTargetless:    false,
			}
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AncestorEntryIsNotDefaultEntryEventCheck {
			if p.evaluateGuard(`(not _scxml_eq(defaulted, 0))`, engine) {
			return &transitionInfo{
				source:          AncestorEntryIsNotDefaultEntryStateChosen,
				target:          AncestorEntryIsNotDefaultEntryStateFailDefaulted,
				transitionIndex: 1,
				hasActions:      false,
				isInternal:      false,
				isTargetless:    false,
			}
			}
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AncestorEntryIsNotDefaultEntryEventCheck {
			if p.evaluateGuard(`(not _scxml_eq(lobbied, 1))`, engine) {
			return &transitionInfo{
				source:          AncestorEntryIsNotDefaultEntryStateChosen,
				target:          AncestorEntryIsNotDefaultEntryStateFailLobbied,
				transitionIndex: 2,
				hasActions:      false,
				isInternal:      false,
				isTargetless:    false,
			}
			}
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AncestorEntryIsNotDefaultEntryEventCheck {
			if p.evaluateGuard(`(not _scxml_eq(idled, 1))`, engine) {
			return &transitionInfo{
				source:          AncestorEntryIsNotDefaultEntryStateChosen,
				target:          AncestorEntryIsNotDefaultEntryStateFailIdled,
				transitionIndex: 3,
				hasActions:      false,
				isInternal:      false,
				isTargetless:    false,
			}
			}
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AncestorEntryIsNotDefaultEntryEventCheck {
			if p.evaluateGuard(`(not _scxml_eq(targeted, 2))`, engine) {
			return &transitionInfo{
				source:          AncestorEntryIsNotDefaultEntryStateChosen,
				target:          AncestorEntryIsNotDefaultEntryStateFailTargeted,
				transitionIndex: 4,
				hasActions:      false,
				isInternal:      false,
				isTargetless:    false,
			}
			}
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AncestorEntryIsNotDefaultEntryEventCheck {
			return &transitionInfo{
				source:          AncestorEntryIsNotDefaultEntryStateChosen,
				target:          AncestorEntryIsNotDefaultEntryStateSettled,
				transitionIndex: 5,
				hasActions:      false,
				isInternal:      false,
				isTargetless:    false,
			}
		}
	case AncestorEntryIsNotDefaultEntryStateLobby:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AncestorEntryIsNotDefaultEntryEventAgain {
			return &transitionInfo{
				source:          AncestorEntryIsNotDefaultEntryStateLobby,
				target:          AncestorEntryIsNotDefaultEntryStateChosen,
				transitionIndex: 0,
				hasActions:      false,
				isInternal:      false,
				isTargetless:    false,
			}
		}
	}
	return nil
}

// tryTransitionInState checks transitions for a single state.
//line ancestor_entry_is_not_default_entry.scxml:69
func (p *AncestorEntryIsNotDefaultEntryPolicy) tryTransitionInState(checkState AncestorEntryIsNotDefaultEntryState, event AncestorEntryIsNotDefaultEntryEvent, currentState *AncestorEntryIsNotDefaultEntryState, engine *sce.Engine[AncestorEntryIsNotDefaultEntryState, AncestorEntryIsNotDefaultEntryEvent]) bool {
	switch checkState {
	case AncestorEntryIsNotDefaultEntryStateAway:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AncestorEntryIsNotDefaultEntryEventCross {
			*currentState = AncestorEntryIsNotDefaultEntryStateChosen
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AncestorEntryIsNotDefaultEntryStateAway
			return true
		}
	case AncestorEntryIsNotDefaultEntryStateChosen:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AncestorEntryIsNotDefaultEntryEventBack {
			*currentState = AncestorEntryIsNotDefaultEntryStateLobby
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AncestorEntryIsNotDefaultEntryStateChosen
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AncestorEntryIsNotDefaultEntryEventCheck {
			if p.evaluateGuard(`(not _scxml_eq(defaulted, 0))`, engine) {
			*currentState = AncestorEntryIsNotDefaultEntryStateFailDefaulted
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AncestorEntryIsNotDefaultEntryStateChosen
			return true
			}
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AncestorEntryIsNotDefaultEntryEventCheck {
			if p.evaluateGuard(`(not _scxml_eq(lobbied, 1))`, engine) {
			*currentState = AncestorEntryIsNotDefaultEntryStateFailLobbied
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AncestorEntryIsNotDefaultEntryStateChosen
			return true
			}
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AncestorEntryIsNotDefaultEntryEventCheck {
			if p.evaluateGuard(`(not _scxml_eq(idled, 1))`, engine) {
			*currentState = AncestorEntryIsNotDefaultEntryStateFailIdled
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AncestorEntryIsNotDefaultEntryStateChosen
			return true
			}
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AncestorEntryIsNotDefaultEntryEventCheck {
			if p.evaluateGuard(`(not _scxml_eq(targeted, 2))`, engine) {
			*currentState = AncestorEntryIsNotDefaultEntryStateFailTargeted
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AncestorEntryIsNotDefaultEntryStateChosen
			return true
			}
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AncestorEntryIsNotDefaultEntryEventCheck {
			*currentState = AncestorEntryIsNotDefaultEntryStateSettled
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AncestorEntryIsNotDefaultEntryStateChosen
			return true
		}
	case AncestorEntryIsNotDefaultEntryStateLobby:
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == AncestorEntryIsNotDefaultEntryEventAgain {
			*currentState = AncestorEntryIsNotDefaultEntryStateChosen
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = AncestorEntryIsNotDefaultEntryStateLobby
			return true
		}
	}
	return false
}

// ExecuteTransitionActions executes actions for the last taken transition (W3C SCXML 3.13).
//line ancestor_entry_is_not_default_entry.scxml:69
func (p *AncestorEntryIsNotDefaultEntryPolicy) ExecuteTransitionActions(engine *sce.Engine[AncestorEntryIsNotDefaultEntryState, AncestorEntryIsNotDefaultEntryEvent]) {
}

// transitionInfo describes a transition for conflict resolution (W3C SCXML Appendix D).
type transitionInfo struct {
	source          AncestorEntryIsNotDefaultEntryState
	target          AncestorEntryIsNotDefaultEntryState
	transitionIndex int
	hasActions      bool
	isInternal      bool
	isTargetless    bool
}

// selectOptimalTransitions resolves conflicts between parallel transitions (Appendix D removeConflictingTransitions).
// Port of Rust remove_conflicting_transitions().
func (p *AncestorEntryIsNotDefaultEntryPolicy) selectOptimalTransitions(transitions []transitionInfo) []transitionInfo {
	if len(transitions) <= 1 {
		return transitions
	}
	var filtered []transitionInfo

	for _, t1 := range transitions {
		// Appendix D selectTransitions: the enabled set is an ORDERED SET, and
		// the same transition reached from two different region leaves is ONE
		// element of it, not two. A transition written on a <parallel>, or on an
		// ancestor above one, is selected once per region by the bubbling walk —
		// W3C test 403b turns on a <parallel>-level <assign> running exactly
		// once. Selection stops at the first enabled transition of a state, so
		// within one microstep a source contributes at most one transition and
		// (source, target) identifies it.
		//
		// This is what the removed target/source criterion stood in for: a
		// targetless duplicate is spelled source -> source, so that check
		// happened to fire on it. Stated here it is the set semantics
		// themselves, independent of how a targetless transition is spelled.
		alreadySelected := false
		for _, seen := range filtered {
			if seen.source == t1.source && seen.target == t1.target {
				alreadySelected = true
				break
			}
		}
		if alreadySelected {
			continue
		}

		dominated := false
		var toRemove []int

		for idx, t2 := range filtered {
			// Appendix D removeConflictingTransitions: Check if exit sets intersect
			t1Exits := p.computeExitSetForConflict(t1)
			t2Exits := p.computeExitSetForConflict(t2)

			// Appendix D removeConflictingTransitions: two transitions conflict
			// when their EXIT SETS intersect. That is the whole test the appendix
			// states, and it is now the whole test made here.
			//
			// Three rules used to sit beside it — a target/source equality check
			// and a <parallel>-ancestor check in each direction — and none is in
			// the appendix. They stood in for an exit set this generator could
			// not compute: assembled from the source's own ancestor chain, a set
			// could not name the sibling regions a transition leaving the
			// <parallel> exits, so the intersection came back empty for
			// transitions that plainly conflict. Appendix D computeExitSet reads
			// p.activeStates now, so the intersection answers on its own.
			//
			// A transition that exits nothing still conflicts with nothing and
			// can never be preempted — that is a targetless transition, which
			// the appendix gives an empty exit set, and it is what W3C test 403c
			// means by "this transition never gets preempted, should fire twice".
			// The removed rules each read a targetless transition as a
			// self-transition on its own source, which is why they needed the
			// empty-exit-set gate and the isTargetless guards that stood here.
			hasConflict := false
			for _, s1 := range t1Exits {
				for _, s2 := range t2Exits {
					if s1 == s2 {
						hasConflict = true
						break
					}
				}
				if hasConflict {
					break
				}
			}
			if !hasConflict {
				continue
			}

			// Appendix D removeConflictingTransitions: the descendant source wins
			if p.IsDescendantOf(t1.source, t2.source) {
				toRemove = append(toRemove, idx)
			} else {
				dominated = true
				break
			}
		}

		if !dominated {
			// Remove preempted transitions in reverse order
			for i := len(toRemove) - 1; i >= 0; i-- {
				idx := toRemove[i]
				filtered = append(filtered[:idx], filtered[idx+1:]...)
			}
			filtered = append(filtered, t1)
		}
	}

	return filtered
}

// transitionDomain is Appendix D getTransitionDomain — the state every exited
// and entered state descends from. A nil answer is the <scxml> element, which
// has no State constant here; callers read it as "every active state is below".
func (p *AncestorEntryIsNotDefaultEntryPolicy) transitionDomain(source, target AncestorEntryIsNotDefaultEntryState, isInternal bool) *AncestorEntryIsNotDefaultEntryState {
	// W3C SCXML 3.13: Internal transition to a compound descendant — the SOURCE
	// is the domain, so it stays active while its active descendants are exited.
	if isInternal &&
		p.IsCompoundState(source) && !p.IsParallelState(source) &&
		p.IsDescendantOf(target, source) && target != source {
		d := source
		return &d
	}

	// Appendix D findLCCA: walk up from source for the lowest CANDIDATE ancestor
	// that contains target. The candidates are the ones
	// isCompoundStateOrScxmlElement admits, so a <parallel> is skipped.
	//
	// If no candidate contains target (top-level siblings, or a region root's
	// external transition whose only non-candidate ancestor is the <parallel>),
	// the domain is the <scxml> element.
	current := source
	for {
		parent, hasParent := p.GetParent(current)
		if !hasParent {
			return nil
		}
		isDomainCandidate := p.IsCompoundState(parent) && !p.IsParallelState(parent)
		if isDomainCandidate && (p.IsDescendantOf(target, parent) || target == parent) {
			return &parent
		}
		current = parent
	}
}

// computeExitSetForConflict is Appendix D computeExitSet: the ACTIVE states that
// are proper descendants of the transition's domain.
//
// It reads p.activeStates, because that is what the appendix computes over.
// Walking the source's own ancestor chain instead — what stood here — names the
// same states only while no <parallel> is active below the domain: a sibling
// region descends from the domain and is not on that chain. It left this
// generator with TWO exit sets, one for conflict resolution and one for
// executeMicrostep, which now share this procedure and cannot disagree.
func (p *AncestorEntryIsNotDefaultEntryPolicy) computeExitSetForConflict(t transitionInfo) []AncestorEntryIsNotDefaultEntryState {
	// Appendix D computeExitSet guards the whole computation with `if t.target`:
	// a transition without one exits nothing and conflicts with nothing.
	if t.isTargetless {
		return nil
	}

	domain := p.transitionDomain(t.source, t.target, t.isInternal)

	var exitSet []AncestorEntryIsNotDefaultEntryState
	for _, activeState := range p.activeStates {
		exits := true
		if domain != nil {
			// The domain itself is not exited; everything active below it is.
			exits = activeState != *domain && p.IsDescendantOf(activeState, *domain)
		}
		if exits {
			exitSet = append(exitSet, activeState)
		}
	}
	return exitSet
}

// executeMicrostep performs a complete microstep for the given transitions
// (Appendix D microstepProcedure Steps 1-5: compute exit set, exit, actions, enter).
func (p *AncestorEntryIsNotDefaultEntryPolicy) executeMicrostep(transitions []transitionInfo, currentState *AncestorEntryIsNotDefaultEntryState, engine *sce.Engine[AncestorEntryIsNotDefaultEntryState, AncestorEntryIsNotDefaultEntryEvent]) {
	if len(transitions) == 0 {
		return
	}

	// Appendix D computeExitSet Step 1-2: Compute states to exit
	var statesToExit []AncestorEntryIsNotDefaultEntryState
	for _, trans := range transitions {
		if trans.isTargetless {
			continue
		}
		// Appendix D computeExitSet: the SAME procedure selectOptimalTransitions
		// intersects. A microstep that exits a different set from the one the
		// resolver judged cannot be reasoned about, and this walked the
		// configuration while computeExitSetForConflict walked source's chain.
		for _, activeState := range p.computeExitSetForConflict(trans) {
			found := false
			for _, s := range statesToExit {
				if s == activeState {
					found = true
					break
				}
			}
			if !found {
				statesToExit = append(statesToExit, activeState)
			}
		}
	}

	// Sort by reverse document order (deepest first)
	sort.Slice(statesToExit, func(i, j int) bool {
		return p.GetDocumentOrder(statesToExit[i]) > p.GetDocumentOrder(statesToExit[j])
	})

	// Snapshot active states for history recording
	activeSnapshot := make([]AncestorEntryIsNotDefaultEntryState, len(p.activeStates))
	copy(activeSnapshot, p.activeStates)

	// Appendix D exitStates Step 2: Exit states
	for _, state := range statesToExit {
		p.ExecuteExitActions(state, engine, activeSnapshot)
	}

	// Appendix D executeTransitionContent Step 3: Execute transition content
	// Sort transitions by source document order
	sortedTransitions := make([]transitionInfo, len(transitions))
	copy(sortedTransitions, transitions)
	sort.Slice(sortedTransitions, func(i, j int) bool {
		return p.GetDocumentOrder(sortedTransitions[i].source) < p.GetDocumentOrder(sortedTransitions[j].source)
	})

	for _, trans := range sortedTransitions {
		if trans.hasActions {
			p.lastTransitionSourceState = trans.source
			p.ExecuteTransitionActions(engine)
		}
	}

	// Appendix D enterStates Step 4-5: Enter target states
	sort.Slice(sortedTransitions, func(i, j int) bool {
		return p.GetDocumentOrder(sortedTransitions[i].target) < p.GetDocumentOrder(sortedTransitions[j].target)
	})

	for _, trans := range sortedTransitions {
		if trans.isTargetless {
			continue
		}

		target := trans.target
		*currentState = target

		// W3C SCXML 3.13: Build hierarchical entry chain from root to target
		var entryChain []AncestorEntryIsNotDefaultEntryState
		{
			current := target
			for {
				entryChain = append([]AncestorEntryIsNotDefaultEntryState{current}, entryChain...)
				parent, hasParent := p.GetParent(current)
				if !hasParent {
					break
				}
				current = parent
			}
		}

		// §scxml-D: every link but the last is an ANCESTOR of the target, and
		// addAncestorStatesToEnter adds an ancestor WITHOUT its default initial
		// child — the entry set already holds the next link. Only the target
		// itself goes through addDescendantStatesToEnter. Passing the next link
		// as pathChild is what expresses that, and it is also what stops a
		// <parallel> ancestor from handing a default to the very region the
		// chain is descending into.
		for chainIdx := range entryChain {
			state := entryChain[chainIdx]
			var pathChild *AncestorEntryIsNotDefaultEntryState
			if chainIdx+1 < len(entryChain) {
				pathChild = &entryChain[chainIdx+1]
			}
			alreadyActive := false
			for _, as := range p.activeStates {
				if as == state {
					alreadyActive = true
					break
				}
			}
			if alreadyActive {
				// W3C SCXML 3.13: Already active - handle parallel region re-entry
				if p.IsParallelState(state) {
					regions := p.GetParallelRegions(state)
					for _, region := range regions {
						if pathChild != nil && *pathChild == region {
							continue
						}
						regionActive := false
						for _, as := range p.activeStates {
							if as == region {
								regionActive = true
								break
							}
						}
						if !regionActive {
							p.ExecuteEntryActions(region, engine, nil)
							if p.IsCompoundState(region) {
								initialChild := p.GetInitialOrHistoryChild(region)
								if initialChild != region {
									p.ExecuteEntryActions(initialChild, engine, nil)
								}
							}
						}
					}
				}
				continue
			}
			p.ExecuteEntryActions(state, engine, pathChild)
		}

		// W3C SCXML 3.4: For parallel states, maintain currentState at parallel level
		if parent, hasParent := p.GetParent(target); hasParent {
			if p.IsParallelState(parent) {
				*currentState = parent
			}
		}
	}
}
