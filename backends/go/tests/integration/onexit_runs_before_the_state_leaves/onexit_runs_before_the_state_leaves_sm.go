// SCE-GENERATED — DO NOT EDIT
// source-hash: 9f1d28aedc299ea1c7281cab0278613454849fe4e66eb28d8ffe4b4c74733c7f


// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) 2025-2026 [Author of input SCXML file]
//
// Generated by SCE Code Generator (Go backend)
// From: integration_resources/onexit_runs_before_the_state_leaves/onexit_runs_before_the_state_leaves.scxml
//
// This generated code is MIT licensed and may be freely used in any project.
// Runtime engine dependency (sce-go-runtime): LGPL-2.1-or-later WITH LicenseRef-SCE-Linking-Exception OR LicenseRef-SCE-Commercial
// Full license: https://github.com/newmassrael/scxml-core-engine/blob/main/LICENSE
// Generated Go state machine for `onexit_runs_before_the_state_leaves`.
//
// Generated by SCE Code Generator (Go backend).
// Implements sce.StatePolicy with full transition logic,
// entry/exit actions, and event processing.


// SCE-MAP: onexit_runs_before_the_state_leaves.scxml:33 :: _machine

package onexit_runs_before_the_state_leaves

import (
	"fmt"
	"path/filepath"
	"strings"
	"time"

	sce "github.com/newmassrael/sce-go-runtime"
)

// Ensure imports are used
var (
	_ = fmt.Sprintf
	_ = filepath.Join
	_ = strings.HasPrefix
	_ = time.Duration(0)
)

// ======================================================================
// State type (W3C SCXML 3.3)
// ======================================================================

type OnexitRunsBeforeTheStateLeavesState int

const (
	OnexitRunsBeforeTheStateLeavesStateCheck OnexitRunsBeforeTheStateLeavesState = 0
	OnexitRunsBeforeTheStateLeavesStateFailChildInOuter OnexitRunsBeforeTheStateLeavesState = 1
	OnexitRunsBeforeTheStateLeavesStateFailExits OnexitRunsBeforeTheStateLeavesState = 2
	OnexitRunsBeforeTheStateLeavesStateFailParentInInner OnexitRunsBeforeTheStateLeavesState = 3
	OnexitRunsBeforeTheStateLeavesStateFailSelfInInner OnexitRunsBeforeTheStateLeavesState = 4
	OnexitRunsBeforeTheStateLeavesStateFailSelfInOuter OnexitRunsBeforeTheStateLeavesState = 5
	OnexitRunsBeforeTheStateLeavesStateInner OnexitRunsBeforeTheStateLeavesState = 6
	OnexitRunsBeforeTheStateLeavesStateOuter OnexitRunsBeforeTheStateLeavesState = 7
	OnexitRunsBeforeTheStateLeavesStateSettled OnexitRunsBeforeTheStateLeavesState = 8
)

func (s OnexitRunsBeforeTheStateLeavesState) String() string {
	switch s {
	case OnexitRunsBeforeTheStateLeavesStateCheck:
		return "check"
	case OnexitRunsBeforeTheStateLeavesStateFailChildInOuter:
		return "failChildInOuter"
	case OnexitRunsBeforeTheStateLeavesStateFailExits:
		return "failExits"
	case OnexitRunsBeforeTheStateLeavesStateFailParentInInner:
		return "failParentInInner"
	case OnexitRunsBeforeTheStateLeavesStateFailSelfInInner:
		return "failSelfInInner"
	case OnexitRunsBeforeTheStateLeavesStateFailSelfInOuter:
		return "failSelfInOuter"
	case OnexitRunsBeforeTheStateLeavesStateInner:
		return "inner"
	case OnexitRunsBeforeTheStateLeavesStateOuter:
		return "outer"
	case OnexitRunsBeforeTheStateLeavesStateSettled:
		return "settled"
	}
	return "unknown"
}

// OnexitRunsBeforeTheStateLeavesStateFromName is the read half of the pair above — a state
// id back into the state it names (W3C SCXML 3.3).
//
// A host that persists where a machine was has to write it down as TEXT: the
// constants above are a build artefact of one binary, and the process that
// resumes is a different one. String publishes the name and this reads it back,
// which is what lets a journal survive its own record and reach
// sce.Engine.EnterAt.
//
// The second return is false for a name this document does not declare. A name
// guessed at rather than refused is how a restore reaches a configuration
// nobody recorded — and the refusal is what makes a typo in a journal a
// reported failure instead of a machine quietly somewhere else.
//
// Emitted from the same loop over the document's states as String, so the two
// age together.
func OnexitRunsBeforeTheStateLeavesStateFromName(name string) (OnexitRunsBeforeTheStateLeavesState, bool) {
	switch name {
	case "check":
		return OnexitRunsBeforeTheStateLeavesStateCheck, true
	case "failChildInOuter":
		return OnexitRunsBeforeTheStateLeavesStateFailChildInOuter, true
	case "failExits":
		return OnexitRunsBeforeTheStateLeavesStateFailExits, true
	case "failParentInInner":
		return OnexitRunsBeforeTheStateLeavesStateFailParentInInner, true
	case "failSelfInInner":
		return OnexitRunsBeforeTheStateLeavesStateFailSelfInInner, true
	case "failSelfInOuter":
		return OnexitRunsBeforeTheStateLeavesStateFailSelfInOuter, true
	case "inner":
		return OnexitRunsBeforeTheStateLeavesStateInner, true
	case "outer":
		return OnexitRunsBeforeTheStateLeavesStateOuter, true
	case "settled":
		return OnexitRunsBeforeTheStateLeavesStateSettled, true
	}
	var zero OnexitRunsBeforeTheStateLeavesState
	return zero, false
}

// OnexitRunsBeforeTheStateLeavesAllStates is every state this document declares, in the
// order the constants above are issued.
//
// Emitted from the same loop, so a walk over it is a walk over the document
// rather than over a list somebody maintained beside it — a test that spells
// its own list goes on passing when the document grows a state.
var OnexitRunsBeforeTheStateLeavesAllStates = []OnexitRunsBeforeTheStateLeavesState{
	OnexitRunsBeforeTheStateLeavesStateCheck,
	OnexitRunsBeforeTheStateLeavesStateFailChildInOuter,
	OnexitRunsBeforeTheStateLeavesStateFailExits,
	OnexitRunsBeforeTheStateLeavesStateFailParentInInner,
	OnexitRunsBeforeTheStateLeavesStateFailSelfInInner,
	OnexitRunsBeforeTheStateLeavesStateFailSelfInOuter,
	OnexitRunsBeforeTheStateLeavesStateInner,
	OnexitRunsBeforeTheStateLeavesStateOuter,
	OnexitRunsBeforeTheStateLeavesStateSettled,
}

// OnexitRunsBeforeTheStateLeavesTarget is one token of a target list, as the document wrote
// it (W3C SCXML 3.13): a state, or a <history> the engine dereferences.
type OnexitRunsBeforeTheStateLeavesTarget = sce.EntryTarget[OnexitRunsBeforeTheStateLeavesState, sce.HistoryID]

// ======================================================================
// Document structure (W3C SCXML 3.2-3.4, 3.10)
//
// Package-level tables, because the structure is a fact about the document
// and not about a run: the engine's Appendix D procedures read them through
// the policy methods below, and a transition's target list is handed out as
// a slice of them rather than rebuilt on every selection.
// ======================================================================

// childStatesOfOnexitRunsBeforeTheStateLeaves is §scxml-D-getChildStates per state: its
// <state>, <parallel> and <final> children, in document order.
var childStatesOfOnexitRunsBeforeTheStateLeaves = [9][]OnexitRunsBeforeTheStateLeavesState{
	OnexitRunsBeforeTheStateLeavesStateOuter: {OnexitRunsBeforeTheStateLeavesStateInner},
}

// initialTargetsOfOnexitRunsBeforeTheStateLeaves is each compound state's initial transition
// target, as written (§scxml-3.3).
var initialTargetsOfOnexitRunsBeforeTheStateLeaves = [9][]OnexitRunsBeforeTheStateLeavesTarget{
	OnexitRunsBeforeTheStateLeavesStateOuter: {sce.StateTarget[OnexitRunsBeforeTheStateLeavesState, sce.HistoryID](OnexitRunsBeforeTheStateLeavesStateInner)},
}

// documentInitialTargetsOfOnexitRunsBeforeTheStateLeaves is the target of the document's own
// initial transition, as written (§scxml-3.2).
var documentInitialTargetsOfOnexitRunsBeforeTheStateLeaves = []OnexitRunsBeforeTheStateLeavesTarget{sce.StateTarget[OnexitRunsBeforeTheStateLeavesState, sce.HistoryID](OnexitRunsBeforeTheStateLeavesStateOuter)}

// transitionTargetsOfOnexitRunsBeforeTheStateLeaves is each transition's target list, as
// written (§scxml-3.13), by source state and the transition's index among its
// source's own transitions. A targetless transition's entry is empty.
var transitionTargetsOfOnexitRunsBeforeTheStateLeaves = [9][][]OnexitRunsBeforeTheStateLeavesTarget{
	OnexitRunsBeforeTheStateLeavesStateCheck: {
		0: {sce.StateTarget[OnexitRunsBeforeTheStateLeavesState, sce.HistoryID](OnexitRunsBeforeTheStateLeavesStateFailExits)},
		1: {sce.StateTarget[OnexitRunsBeforeTheStateLeavesState, sce.HistoryID](OnexitRunsBeforeTheStateLeavesStateFailSelfInInner)},
		2: {sce.StateTarget[OnexitRunsBeforeTheStateLeavesState, sce.HistoryID](OnexitRunsBeforeTheStateLeavesStateFailParentInInner)},
		3: {sce.StateTarget[OnexitRunsBeforeTheStateLeavesState, sce.HistoryID](OnexitRunsBeforeTheStateLeavesStateFailSelfInOuter)},
		4: {sce.StateTarget[OnexitRunsBeforeTheStateLeavesState, sce.HistoryID](OnexitRunsBeforeTheStateLeavesStateFailChildInOuter)},
		5: {sce.StateTarget[OnexitRunsBeforeTheStateLeavesState, sce.HistoryID](OnexitRunsBeforeTheStateLeavesStateSettled)},
	},
	OnexitRunsBeforeTheStateLeavesStateInner: {
		0: {sce.StateTarget[OnexitRunsBeforeTheStateLeavesState, sce.HistoryID](OnexitRunsBeforeTheStateLeavesStateCheck)},
	},
}

// ======================================================================
// Event type (W3C SCXML 3.12)
// ======================================================================

type OnexitRunsBeforeTheStateLeavesEvent int

const (
	OnexitRunsBeforeTheStateLeavesEventErrorExecution OnexitRunsBeforeTheStateLeavesEvent = 0
	OnexitRunsBeforeTheStateLeavesEventLeave OnexitRunsBeforeTheStateLeavesEvent = 1
	// W3C SCXML 3.13: Sentinel for eventless transition dispatch
	OnexitRunsBeforeTheStateLeavesEventNull OnexitRunsBeforeTheStateLeavesEvent = 2
)

func (e OnexitRunsBeforeTheStateLeavesEvent) String() string {
	switch e {
	case OnexitRunsBeforeTheStateLeavesEventErrorExecution:
		return "error.execution"
	case OnexitRunsBeforeTheStateLeavesEventLeave:
		return "leave"
	case OnexitRunsBeforeTheStateLeavesEventNull:
		return ""
	}
	return "unknown"
}

// ======================================================================
// Policy struct
// ======================================================================

type OnexitRunsBeforeTheStateLeavesPolicy struct {
	// W3C SCXML 3.4: Active state configuration for parallel states / In() predicate
	activeStates []OnexitRunsBeforeTheStateLeavesState
	// W3C SCXML 5.10.1: External event flag
	nextEventIsExternal bool
	pendingEventName string
	pendingEventData string
	pendingEventType string
	pendingEventSendid string
	pendingEventOrigin string
	pendingEventOrigintype string
	pendingEventInvokeid string
	// W3C SCXML 5.10: Session ID
	SessionID string
	// W3C SCXML B.1: Script engine handle (per-instance, Path B+ Q1=(d) Go=interface ref)
	ScriptEngine sce.IScriptEngine
	scriptEngineInitialized bool
	// W3C SCXML C.2.3: inbound BasicHTTP endpoint serving this machine,
	// declared by the deployment before Initialize(). The address belongs to
	// whoever runs the listener, so the machine takes it from here rather than
	// guessing one. Empty means no such endpoint is deployed, and no BasicHTTP
	// entry is published in _ioprocessors.
	BasicHTTPAccessURI string
	inPredicateStates []string
	// W3C SCXML 6.4: Parent communication
	ParentExternalQueue chan sce.ParentEvent
	InvokeID           string
	ChildSessionID     string
	// §scxml-6.4.3: invoke param VALUES staged by the parent, applied after
	// this machine's datamodel init so they override its `<data>` defaults.
	pendingParams      map[string]interface{}
}

// NewOnexitRunsBeforeTheStateLeavesPolicy creates a new policy with default values.
func NewOnexitRunsBeforeTheStateLeavesPolicy() OnexitRunsBeforeTheStateLeavesPolicy {
	return OnexitRunsBeforeTheStateLeavesPolicy{
		activeStates: make([]OnexitRunsBeforeTheStateLeavesState, 0),
	}
}

// SelfInInner reports what the `selfInInner` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `selfInInner` was assigned a value of another type, or the engine refused.
func (p *OnexitRunsBeforeTheStateLeavesPolicy) SelfInInner() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "selfInInner")
}

// ParentInInner reports what the `parentInInner` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `parentInInner` was assigned a value of another type, or the engine refused.
func (p *OnexitRunsBeforeTheStateLeavesPolicy) ParentInInner() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "parentInInner")
}

// SelfInOuter reports what the `selfInOuter` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `selfInOuter` was assigned a value of another type, or the engine refused.
func (p *OnexitRunsBeforeTheStateLeavesPolicy) SelfInOuter() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "selfInOuter")
}

// ChildInOuter reports what the `childInOuter` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `childInOuter` was assigned a value of another type, or the engine refused.
func (p *OnexitRunsBeforeTheStateLeavesPolicy) ChildInOuter() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "childInOuter")
}

// Exits reports what the `exits` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `exits` was assigned a value of another type, or the engine refused.
func (p *OnexitRunsBeforeTheStateLeavesPolicy) Exits() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "exits")
}



// IsStateActive checks if a state is active by name (W3C SCXML 5.9.2: In() predicate).
func (p *OnexitRunsBeforeTheStateLeavesPolicy) IsStateActive(stateID string) bool {
	for _, s := range p.activeStates {
		if p.GetStateName(s) == stateID {
			return true
		}
	}
	return false
}


// ensureScriptEngine lazily initializes the script engine session.
//
// Engine DI Parity RFC: panics if ScriptEngine was not set on the policy
// before Initialize() — mirrors Kotlin's mandatory constructor parameter
// contract. Silent no-op would otherwise turn missed injection into a
// hidden runtime regression (every <assign>/<log>/guard becomes a no-op
// and the SM walks to its default-state final without diagnostic).
func (p *OnexitRunsBeforeTheStateLeavesPolicy) ensureScriptEngine() {
	if p.scriptEngineInitialized {
		return
	}
	engine := p.ScriptEngine
	if engine == nil {
		panic("OnexitRunsBeforeTheStateLeavesPolicy: ScriptEngine field must be set before Initialize()")
	}
	sessionID := p.SessionID
	if sessionID == "" {
		sessionID = sce.GenerateSessionID()
		p.SessionID = sessionID
	}
	if !engine.HasSession(sessionID) {
		_ = engine.CreateSession(sessionID)
	}
	// W3C SCXML C.1.1 / C.2.3: the _ioprocessors entries come from the same
	// helper every other backend uses, so a machine reads the same entry names
	// and the same addresses whichever one runs it.
	_ = engine.SetupSystemVariables(sessionID, "onexit_runs_before_the_state_leaves",
		sce.BuildIoProcessors(sessionID, p.BasicHTTPAccessURI))
	// W3C SCXML 5.9.2: Register In() predicate callback
	engine.SetStateQueryCallback(sessionID, func(stateID string) bool {
		return p.IsStateActive(stateID)
	})
	p.scriptEngineInitialized = true
}


// InitializeDataModel sets up datamodel via script engine (W3C SCXML 5.3).
func (p *OnexitRunsBeforeTheStateLeavesPolicy) InitializeDataModel(eng *sce.Engine[OnexitRunsBeforeTheStateLeavesState, OnexitRunsBeforeTheStateLeavesEvent]) {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	sessionID := p.SessionID

	// W3C SCXML 5.3: Initialize _sessionid
	_ = engine.SetVariable(sessionID, "_sessionid", sessionID)

	// W3C SCXML 5.3: Initialize _name
	_ = engine.SetVariable(sessionID, "_name", "onexit_runs_before_the_state_leaves")

	// W3C SCXML 5.2.2: Initialize global datamodel variables
	// W3C SCXML 5.2/5.3: Initialize selfInInner from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "selfInInner", result)
		} else {
			eng.Raise(sce.NewPlatformError(OnexitRunsBeforeTheStateLeavesEventErrorExecution, "<data id='selfInInner'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "selfInInner", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize parentInInner from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "parentInInner", result)
		} else {
			eng.Raise(sce.NewPlatformError(OnexitRunsBeforeTheStateLeavesEventErrorExecution, "<data id='parentInInner'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "parentInInner", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize selfInOuter from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "selfInOuter", result)
		} else {
			eng.Raise(sce.NewPlatformError(OnexitRunsBeforeTheStateLeavesEventErrorExecution, "<data id='selfInOuter'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "selfInOuter", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize childInOuter from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "childInOuter", result)
		} else {
			eng.Raise(sce.NewPlatformError(OnexitRunsBeforeTheStateLeavesEventErrorExecution, "<data id='childInOuter'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "childInOuter", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize exits from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "exits", result)
		} else {
			eng.Raise(sce.NewPlatformError(OnexitRunsBeforeTheStateLeavesEventErrorExecution, "<data id='exits'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "exits", nil)
		}
	}



	// §scxml-6.4.3: apply the invoke params the parent staged — overriding the
	// `<data>` default this block just seeded, and only for a name the child
	// declares ("if the names do not match, the Processor MUST NOT add the
	// value ... to the invoked session's data model"). `HasVariable` is that
	// filter, and it is this channel's only copy of the rule.
	if len(p.pendingParams) > 0 {
		for paramName, paramValue := range p.pendingParams {
			if engine.HasVariable(sessionID, paramName) {
				_ = engine.SetVariable(sessionID, paramName, paramValue)
			}
		}
		p.pendingParams = nil
	}
}

// evaluateGuard evaluates a guard condition via script engine.
// W3C SCXML 4.3.2: If a condition expression cannot be evaluated, the processor
// MUST treat it as false and MUST raise error.execution.
func (p *OnexitRunsBeforeTheStateLeavesPolicy) evaluateGuard(guard string, eng *sce.Engine[OnexitRunsBeforeTheStateLeavesState, OnexitRunsBeforeTheStateLeavesEvent]) bool {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	result, err := engine.EvaluateExpression(p.SessionID, guard)
	if err != nil {
		eng.Raise(sce.NewPlatformError(OnexitRunsBeforeTheStateLeavesEventErrorExecution, "a <transition> cond failed to evaluate"))
		return false
	}
	return sce.ScriptToBool(result)
}

// executeScript executes a script block via script engine.
func (p *OnexitRunsBeforeTheStateLeavesPolicy) executeScript(script string) error {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	return engine.ExecuteScript(p.SessionID, script)
}

// setCurrentEvent binds _event system variable (W3C SCXML 5.10) and answers
// which rung of §scxml-B-2-8-1 the payload got, because the binding is the
// only place that knows.
//
// This used to be `_ = engine.SetCurrentEvent(...)`: the ladder decided
// between a DOM, a value and a space-normalized string, and the decision was
// dropped one line later. A payload that announced structure and would not
// parse therefore reached the document as raw characters, every
// _event.data.<field> read empty, and nothing anywhere could say so — see
// sce.PayloadReading.
func (p *OnexitRunsBeforeTheStateLeavesPolicy) setCurrentEvent(name string) sce.PayloadReading {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	data := p.pendingEventData
	eventType := p.pendingEventType
	sendID := p.pendingEventSendid
	// W3C SCXML C.1: `_event.origin` is the sender's published
	// `_ioprocessors` location, not its bare session id — and this is the one
	// place that publishes `_event` to the document, so this is where the id
	// becomes a location. The engine keeps the bare id in
	// `EventMetadata.Origin` because its session-keyed lookups (<finalize>
	// dispatch, cancelled-invoke filtering) match on it; converting at the
	// raise would make one value serve two consumers that need different
	// spellings. The conversion itself lives in `sce.PublishedOrigin`, the
	// port of the `IOProcessorHelper::publishedOrigin` the C++ engines share:
	// a second spelling of the rule is how the backends would stop agreeing.
	origin := sce.PublishedOrigin(p.pendingEventOrigin)
	originType := p.pendingEventOrigintype
	invokeID := p.pendingEventInvokeid
	reading, err := engine.SetCurrentEvent(p.SessionID, sce.SetCurrentEventArgs{
		Name:       name,
		Data:       data,
		Type:       eventType,
		SendID:     sendID,
		Origin:     origin,
		OriginType: originType,
		InvokeID:   invokeID,
	})
	if err != nil {
		// An engine that could not bind _event at all has a larger problem
		// than an unreadable payload, and reporting PayloadUndecodable for it
		// would point a host at the wrong thing.
		return sce.PayloadAbsent
	}
	return reading
}

// assignVariable assigns a value to a datamodel variable via script engine.
func (p *OnexitRunsBeforeTheStateLeavesPolicy) assignVariable(location, expr string) error {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	script := location + " = " + expr
	return engine.ExecuteScript(p.SessionID, script)
}

// getVariable reads a datamodel variable from the script engine.
func (p *OnexitRunsBeforeTheStateLeavesPolicy) getVariable(name string) interface{} {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	result, err := engine.GetVariable(p.SessionID, name)
	if err != nil {
		return nil
	}
	return result
}

// SetParamValueInScriptEngine stores an invoke param's VALUE for deferred
// application after datamodel init (§scxml-6.4.3).
//
// Params are applied at the end of InitializeDataModel, overriding the
// `<data>` default only for names the child declares. The value crosses as a
// value: this took the parent's value rendered as Lua source and
// re-evaluated it here until 2026-08-21, which survived every value a test
// had tried and lost the ones Lua cannot spell — `ToScriptLiteral` renders a
// non-finite number as `+Inf`, which is not a Lua expression, so an
// `<invoke>` `<param expr="1/0"/>` arrived as nothing at all.
func (p *OnexitRunsBeforeTheStateLeavesPolicy) SetParamValueInScriptEngine(name string, value interface{}) {
	if p.pendingParams == nil {
		p.pendingParams = make(map[string]interface{})
	}
	p.pendingParams[name] = value
}


// ExecutePendingInvokes is a no-op (no invokes in this SM).
func (p *OnexitRunsBeforeTheStateLeavesPolicy) ExecutePendingInvokes(engine *sce.Engine[OnexitRunsBeforeTheStateLeavesState, OnexitRunsBeforeTheStateLeavesEvent]) {}

// TickChildren is a no-op (no invokes in this SM).
func (p *OnexitRunsBeforeTheStateLeavesPolicy) TickChildren(engine *sce.Engine[OnexitRunsBeforeTheStateLeavesState, OnexitRunsBeforeTheStateLeavesEvent]) {}

// ======================================================================
// StatePolicy interface implementation
// ======================================================================

// InitialState returns the initial state (W3C SCXML 3.3).
func (p *OnexitRunsBeforeTheStateLeavesPolicy) InitialState() OnexitRunsBeforeTheStateLeavesState {
	return OnexitRunsBeforeTheStateLeavesStateInner
}

// IsFinalState returns true if state is a <final> state (W3C SCXML 3.7).
func (p *OnexitRunsBeforeTheStateLeavesPolicy) IsFinalState(state OnexitRunsBeforeTheStateLeavesState) bool {
	switch state {
	case OnexitRunsBeforeTheStateLeavesStateFailChildInOuter:
		return true
	case OnexitRunsBeforeTheStateLeavesStateFailExits:
		return true
	case OnexitRunsBeforeTheStateLeavesStateFailParentInInner:
		return true
	case OnexitRunsBeforeTheStateLeavesStateFailSelfInInner:
		return true
	case OnexitRunsBeforeTheStateLeavesStateFailSelfInOuter:
		return true
	case OnexitRunsBeforeTheStateLeavesStateSettled:
		return true
	}
	return false
}

// GetParent returns the parent of state, or (zero, false) if root (W3C SCXML 3.3).
func (p *OnexitRunsBeforeTheStateLeavesPolicy) GetParent(state OnexitRunsBeforeTheStateLeavesState) (OnexitRunsBeforeTheStateLeavesState, bool) {
	switch state {
	case OnexitRunsBeforeTheStateLeavesStateInner:
		return OnexitRunsBeforeTheStateLeavesStateOuter, true
	}
	return 0, false
}

// IsCompoundState returns true if state is a <state> with child states — exactly
// the states that have an initial transition. A <parallel> is not compound
// (W3C SCXML 3.3).
func (p *OnexitRunsBeforeTheStateLeavesPolicy) IsCompoundState(state OnexitRunsBeforeTheStateLeavesState) bool {
	return len(initialTargetsOfOnexitRunsBeforeTheStateLeaves[state]) > 0
}

func (p *OnexitRunsBeforeTheStateLeavesPolicy) IsParallelState(_ OnexitRunsBeforeTheStateLeavesState) bool { return false }

// GetChildStates returns state's <state>, <parallel> and <final> children, in
// document order — for a <parallel>, its regions (§scxml-D-getChildStates).
func (p *OnexitRunsBeforeTheStateLeavesPolicy) GetChildStates(state OnexitRunsBeforeTheStateLeavesState) []OnexitRunsBeforeTheStateLeavesState {
	return childStatesOfOnexitRunsBeforeTheStateLeaves[state]
}

// GetInitialTargets returns a compound state's initial transition target, as
// written; the engine's entry procedures dereference a <history> among them
// (W3C SCXML 3.3).
func (p *OnexitRunsBeforeTheStateLeavesPolicy) GetInitialTargets(state OnexitRunsBeforeTheStateLeavesState) []OnexitRunsBeforeTheStateLeavesTarget {
	return initialTargetsOfOnexitRunsBeforeTheStateLeaves[state]
}

// GetDocumentInitialTargets returns the target of the document's own initial
// transition, as written (W3C SCXML 3.2).
func (p *OnexitRunsBeforeTheStateLeavesPolicy) GetDocumentInitialTargets() []OnexitRunsBeforeTheStateLeavesTarget {
	return documentInitialTargetsOfOnexitRunsBeforeTheStateLeaves
}

// W3C SCXML 3.10: this document declares no <history>, so no target list names
// one and the engine never asks the two below; answering would mean inventing
// one.
func (p *OnexitRunsBeforeTheStateLeavesPolicy) GetHistoryParent(history sce.HistoryID) OnexitRunsBeforeTheStateLeavesState {
	panic(fmt.Sprintf("OnexitRunsBeforeTheStateLeavesPolicy declares no <history>; asked for %d", history))
}
func (p *OnexitRunsBeforeTheStateLeavesPolicy) GetHistoryDefaultTargets(history sce.HistoryID) []OnexitRunsBeforeTheStateLeavesTarget {
	panic(fmt.Sprintf("OnexitRunsBeforeTheStateLeavesPolicy declares no <history>; asked for %d", history))
}
func (p *OnexitRunsBeforeTheStateLeavesPolicy) HistoryValue(_ sce.HistoryID) ([]OnexitRunsBeforeTheStateLeavesState, bool) {
	return nil, false
}

// GetDocumentOrder returns the document order index (W3C SCXML Appendix D).
func (p *OnexitRunsBeforeTheStateLeavesPolicy) GetDocumentOrder(state OnexitRunsBeforeTheStateLeavesState) int {
	switch state {
	case OnexitRunsBeforeTheStateLeavesStateCheck:
		return 2
	case OnexitRunsBeforeTheStateLeavesStateFailChildInOuter:
		return 8
	case OnexitRunsBeforeTheStateLeavesStateFailExits:
		return 4
	case OnexitRunsBeforeTheStateLeavesStateFailParentInInner:
		return 6
	case OnexitRunsBeforeTheStateLeavesStateFailSelfInInner:
		return 5
	case OnexitRunsBeforeTheStateLeavesStateFailSelfInOuter:
		return 7
	case OnexitRunsBeforeTheStateLeavesStateInner:
		return 1
	case OnexitRunsBeforeTheStateLeavesStateOuter:
		return 0
	case OnexitRunsBeforeTheStateLeavesStateSettled:
		return 3
	}
	return -1
}

// GetEventName returns the string name of an event (W3C SCXML 3.12).
func (p *OnexitRunsBeforeTheStateLeavesPolicy) GetEventName(event OnexitRunsBeforeTheStateLeavesEvent) string {
	return event.String()
}

// GetEventFromName looks up an event by name (W3C SCXML 3.12).
func (p *OnexitRunsBeforeTheStateLeavesPolicy) GetEventFromName(name string) (OnexitRunsBeforeTheStateLeavesEvent, bool) {
	switch name {
	case "error.execution":
		return OnexitRunsBeforeTheStateLeavesEventErrorExecution, true
	case "leave":
		return OnexitRunsBeforeTheStateLeavesEventLeave, true
	}
	return OnexitRunsBeforeTheStateLeavesEventNull, false
}

// GetStateName returns the string name of a state (W3C SCXML 3.3).
func (p *OnexitRunsBeforeTheStateLeavesPolicy) GetStateName(state OnexitRunsBeforeTheStateLeavesState) string {
	return state.String()
}

// GetStateFromName reads a state id back into the state it names (W3C SCXML 3.3).
// The reverse of GetStateName, and what turns a host's recorded configuration
// back into the argument sce.Engine.EnterAt takes.
func (p *OnexitRunsBeforeTheStateLeavesPolicy) GetStateFromName(name string) (OnexitRunsBeforeTheStateLeavesState, bool) {
	return OnexitRunsBeforeTheStateLeavesStateFromName(name)
}

// NullEvent returns the sentinel for eventless transition dispatch (W3C SCXML 3.13).
func (p *OnexitRunsBeforeTheStateLeavesPolicy) NullEvent() OnexitRunsBeforeTheStateLeavesEvent {
	return OnexitRunsBeforeTheStateLeavesEventNull
}

// GetActiveStates returns the active state configuration (W3C SCXML 3.4).
func (p *OnexitRunsBeforeTheStateLeavesPolicy) GetActiveStates() []OnexitRunsBeforeTheStateLeavesState {
	return p.activeStates
}

// SetActiveStates hands this machine an active set whole (W3C SCXML 3.4).
// The write half of GetActiveStates: entry and exit grow the set one state at a
// time as they walk, and sce.Engine.EnterAt hands back a configuration an
// earlier run already settled on. Copied rather than aliased so a caller's slice
// cannot go on changing this machine's configuration behind it.
func (p *OnexitRunsBeforeTheStateLeavesPolicy) SetActiveStates(states []OnexitRunsBeforeTheStateLeavesState) {
	p.activeStates = append(p.activeStates[:0:0], states...)
}

// SetNextEventIsExternal sets the external event flag (W3C SCXML 5.10.1).
func (p *OnexitRunsBeforeTheStateLeavesPolicy) SetNextEventIsExternal(value bool) {
	p.nextEventIsExternal = value
}

// HasParallelStates returns whether the SM has parallel states.
func (p *OnexitRunsBeforeTheStateLeavesPolicy) HasParallelStates() bool {
	return false
}

// NeedsScriptEngine returns whether the SM needs a script engine.
func (p *OnexitRunsBeforeTheStateLeavesPolicy) NeedsScriptEngine() bool {
	return true
}

// NeedsEventScheduler reports whether a host must drive this machine with
// Tick rather than Step alone.
//
// W3C SCXML 6.2: Step runs a macrostep and never drains the delayed-send
// scheduler nor ticks an invoked child, so a host that only ever calls it
// waits forever with nothing said. Same verdict the generate manifest
// publishes as needs_event_scheduler.
func (p *OnexitRunsBeforeTheStateLeavesPolicy) NeedsEventScheduler() bool {
	return false
}

// NeedsDataModelInit returns whether the SM needs datamodel init.
func (p *OnexitRunsBeforeTheStateLeavesPolicy) NeedsDataModelInit() bool {
	return true
}

func (p *OnexitRunsBeforeTheStateLeavesPolicy) HasInvokeSupport() bool { return false }
func (p *OnexitRunsBeforeTheStateLeavesPolicy) HasChildTick() bool { return false }
func (p *OnexitRunsBeforeTheStateLeavesPolicy) HasFinalize() bool { return false }
func (p *OnexitRunsBeforeTheStateLeavesPolicy) HasAutoforward() bool { return false }
func (p *OnexitRunsBeforeTheStateLeavesPolicy) HasActiveStates() bool { return true }
func (p *OnexitRunsBeforeTheStateLeavesPolicy) HasExternalEventFlag() bool { return true }
// ExecuteFinalizeForChildEvent is a no-op (no finalize invokes).
func (p *OnexitRunsBeforeTheStateLeavesPolicy) ExecuteFinalizeForChildEvent(_ *sce.EventWithMetadata[OnexitRunsBeforeTheStateLeavesEvent], _ *sce.Engine[OnexitRunsBeforeTheStateLeavesState, OnexitRunsBeforeTheStateLeavesEvent]) {}
// ForwardToAutoforwardChildren is a no-op (no autoforward invokes).
func (p *OnexitRunsBeforeTheStateLeavesPolicy) ForwardToAutoforwardChildren(_ string, _ sce.EventMetadata, _ *sce.Engine[OnexitRunsBeforeTheStateLeavesState, OnexitRunsBeforeTheStateLeavesEvent]) {}

// PopulateEventMetadata stores pending event metadata (W3C SCXML 5.10).
// Note: event name is set separately via setCurrentEvent(), not from metadata.
func (p *OnexitRunsBeforeTheStateLeavesPolicy) PopulateEventMetadata(meta *sce.EventMetadata) {
	p.pendingEventData = meta.Data
	p.pendingEventType = meta.EventType.String()
	p.pendingEventSendid = meta.SendID
	p.pendingEventOrigin = meta.Origin
	p.pendingEventOrigintype = meta.OriginType
	p.pendingEventInvokeid = meta.InvokeID
}

// LiftTypedPayload binds the dequeued event's typed `_event.data` view from
// the data it carries (NL→IR Item C1 Path A).
//
// The typed carrier PopulateEventMetadata reads is filled by one producer, the
// generated Raise<Event> inject seam. Every other producer — <send> with
// <param>, an invoke forwarding either way, autoforward, BasicHTTP, mesh —
// fills EventMetadata.Data, so the schema's fields are read out of that here.
// A refusal is returned for the engine to raise as error.execution, which is
// what W3C SCXML 3.13 gives for a guard that cannot be evaluated.
func (p *OnexitRunsBeforeTheStateLeavesPolicy) LiftTypedPayload(event OnexitRunsBeforeTheStateLeavesEvent, meta *sce.EventMetadata) error {
	// This document lowered no typed guard, so no event carries a typed view.
	return nil
}

// ClearEventMetadata resets pending event metadata (W3C SCXML 5.10).
func (p *OnexitRunsBeforeTheStateLeavesPolicy) ClearEventMetadata() {
	p.pendingEventName = ""
	p.pendingEventData = ""
	p.pendingEventType = ""
	p.pendingEventSendid = ""
	p.pendingEventOrigin = ""
	p.pendingEventOrigintype = ""
	p.pendingEventInvokeid = ""
}



// ExecuteEntryActions enters one state (W3C SCXML 3.8): adds it to the
// configuration, runs its <onentry>, and its <initial> transition's content when
// its initial state is entered by default.
//line onexit_runs_before_the_state_leaves.scxml:33
func (p *OnexitRunsBeforeTheStateLeavesPolicy) ExecuteEntryActions(state OnexitRunsBeforeTheStateLeavesState, engine *sce.Engine[OnexitRunsBeforeTheStateLeavesState, OnexitRunsBeforeTheStateLeavesEvent], isDefaultEntry bool) {
	p.ensureScriptEngine()
	// W3C SCXML 3.4/3.12.1: Add state to active configuration for parallel states and In() predicate
	for _, s := range p.activeStates {
		if s == state {
			return // W3C SCXML 3.8: Skip onentry actions for duplicate state entry
		}
	}
	p.activeStates = append(p.activeStates, state)
	switch state {
	default:
		// No entry actions
	}
}

// ExecuteHistoryDefaultContent runs a <history>'s default transition content
// (W3C SCXML 3.10.2), after its parent's onentry (and after the parent's own
// <initial> content) when the history was taken with nothing recorded. The
// engine asks for it by the entry set's defaultHistoryContent answer; a history
// that restored what it recorded runs nothing.
//line onexit_runs_before_the_state_leaves.scxml:33
func (p *OnexitRunsBeforeTheStateLeavesPolicy) ExecuteHistoryDefaultContent(history sce.HistoryID, engine *sce.Engine[OnexitRunsBeforeTheStateLeavesState, OnexitRunsBeforeTheStateLeavesEvent]) {
	// W3C SCXML 3.10.2: no <history> in this document has default content.
}

// ExecuteExitActions exits one state (W3C SCXML 3.9): records its histories,
// runs its <onexit>, cancels its invocations and removes it from the
// configuration — §scxml-D-exitStates's order.
//line onexit_runs_before_the_state_leaves.scxml:33
func (p *OnexitRunsBeforeTheStateLeavesPolicy) ExecuteExitActions(state OnexitRunsBeforeTheStateLeavesState, engine *sce.Engine[OnexitRunsBeforeTheStateLeavesState, OnexitRunsBeforeTheStateLeavesEvent], configurationBeforeExit []OnexitRunsBeforeTheStateLeavesState) {
	p.ensureScriptEngine()
	// §scxml-D-exitStates orders one state's exit as onexit, then
	// cancelInvoke, then configuration.delete(s), so `In(s)` inside s's own
	// handler is true: the <onexit> switch comes first and the removal from
	// the configuration last.
	switch state {
	case OnexitRunsBeforeTheStateLeavesStateInner:
		//line onexit_runs_before_the_state_leaves.scxml:56
		// W3C SCXML 3.9: onexit block 0
		for exitBlock0 := 0; exitBlock0 < 1; exitBlock0++ {
			_ = exitBlock0

	// W3C SCXML 5.3: <assign location="exits" expr="exits + 1">
	if err := p.assignVariable(`exits`, `_scxml_add(exits, 1)`); err != nil {
		engine.Raise(sce.NewPlatformError(OnexitRunsBeforeTheStateLeavesEventErrorExecution, "<assign> to 'exits' failed"))
		break
	}


	if p.IsStateActive("inner") {

	// W3C SCXML 5.3: <assign location="selfInInner" expr="1">
	if err := p.assignVariable(`selfInInner`, `1`); err != nil {
		engine.Raise(sce.NewPlatformError(OnexitRunsBeforeTheStateLeavesEventErrorExecution, "<assign> to 'selfInInner' failed"))
		break
	}

	}

	if p.IsStateActive("outer") {

	// W3C SCXML 5.3: <assign location="parentInInner" expr="1">
	if err := p.assignVariable(`parentInInner`, `1`); err != nil {
		engine.Raise(sce.NewPlatformError(OnexitRunsBeforeTheStateLeavesEventErrorExecution, "<assign> to 'parentInInner' failed"))
		break
	}

	}
		}
	case OnexitRunsBeforeTheStateLeavesStateOuter:
		//line onexit_runs_before_the_state_leaves.scxml:45
		// W3C SCXML 3.9: onexit block 0
		for exitBlock0 := 0; exitBlock0 < 1; exitBlock0++ {
			_ = exitBlock0

	// W3C SCXML 5.3: <assign location="exits" expr="exits + 1">
	if err := p.assignVariable(`exits`, `_scxml_add(exits, 1)`); err != nil {
		engine.Raise(sce.NewPlatformError(OnexitRunsBeforeTheStateLeavesEventErrorExecution, "<assign> to 'exits' failed"))
		break
	}


	if p.IsStateActive("outer") {

	// W3C SCXML 5.3: <assign location="selfInOuter" expr="1">
	if err := p.assignVariable(`selfInOuter`, `1`); err != nil {
		engine.Raise(sce.NewPlatformError(OnexitRunsBeforeTheStateLeavesEventErrorExecution, "<assign> to 'selfInOuter' failed"))
		break
	}

	}

	if p.IsStateActive("inner") {

	// W3C SCXML 5.3: <assign location="childInOuter" expr="1">
	if err := p.assignVariable(`childInOuter`, `1`); err != nil {
		engine.Raise(sce.NewPlatformError(OnexitRunsBeforeTheStateLeavesEventErrorExecution, "<assign> to 'childInOuter' failed"))
		break
	}

	}
		}
	default:
		// No exit actions
	}
	// §scxml-D-exitStates: out of the configuration only after its onexit
	// and invoke cancellation — the configuration.delete(s) step.
	p.activeStates = func() []OnexitRunsBeforeTheStateLeavesState {
		var result []OnexitRunsBeforeTheStateLeavesState
		for _, s := range p.activeStates {
			if s != state {
				result = append(result, s)
			}
		}
		return result
	}()
}



// BindCurrentEvent binds the event whose transitions are about to be selected as
// the _event their guards read (W3C SCXML 5.10) — before the first guard runs,
// and not for an eventless selection, which has no event of its own.
//line onexit_runs_before_the_state_leaves.scxml:33
func (p *OnexitRunsBeforeTheStateLeavesPolicy) BindCurrentEvent(event OnexitRunsBeforeTheStateLeavesEvent, engine *sce.Engine[OnexitRunsBeforeTheStateLeavesState, OnexitRunsBeforeTheStateLeavesEvent]) {
	if event != OnexitRunsBeforeTheStateLeavesEventNull {
		// §scxml-B-2-8-1: the rung the payload got, handed to the engine
		// rather than dropped. This is the only frame that has both the
		// reading and the event it belongs to.
		engine.NotePayloadReading(event, p.setCurrentEvent(p.GetEventName(event)))
	}
}

// FirstEnabledTransition is Appendix D selectTransitions, the half only this
// document can answer: the first of state's own transitions, in document order,
// that event enables and whose guard holds. The engine walks the atomic states
// and their ancestors and keeps the ordered set; the null event asks for
// eventless transitions.
//line onexit_runs_before_the_state_leaves.scxml:33
func (p *OnexitRunsBeforeTheStateLeavesPolicy) FirstEnabledTransition(state OnexitRunsBeforeTheStateLeavesState, event OnexitRunsBeforeTheStateLeavesEvent, engine *sce.Engine[OnexitRunsBeforeTheStateLeavesState, OnexitRunsBeforeTheStateLeavesEvent]) (sce.EnabledTransition[OnexitRunsBeforeTheStateLeavesState, sce.HistoryID], bool) {
	switch state {
	case OnexitRunsBeforeTheStateLeavesStateCheck:
		if event == OnexitRunsBeforeTheStateLeavesEventNull {
			if p.evaluateGuard(`(not _scxml_eq(exits, 2))`, engine) {
				return sce.EnabledTransition[OnexitRunsBeforeTheStateLeavesState, sce.HistoryID]{
					Source:          state,
					Targets:         transitionTargetsOfOnexitRunsBeforeTheStateLeaves[state][0],
					TransitionIndex: 0,
					HasActions:      false,
					IsInternal:      false,
				}, true
			}
		}
		if event == OnexitRunsBeforeTheStateLeavesEventNull {
			if p.evaluateGuard(`(not _scxml_eq(selfInInner, 1))`, engine) {
				return sce.EnabledTransition[OnexitRunsBeforeTheStateLeavesState, sce.HistoryID]{
					Source:          state,
					Targets:         transitionTargetsOfOnexitRunsBeforeTheStateLeaves[state][1],
					TransitionIndex: 1,
					HasActions:      false,
					IsInternal:      false,
				}, true
			}
		}
		if event == OnexitRunsBeforeTheStateLeavesEventNull {
			if p.evaluateGuard(`(not _scxml_eq(parentInInner, 1))`, engine) {
				return sce.EnabledTransition[OnexitRunsBeforeTheStateLeavesState, sce.HistoryID]{
					Source:          state,
					Targets:         transitionTargetsOfOnexitRunsBeforeTheStateLeaves[state][2],
					TransitionIndex: 2,
					HasActions:      false,
					IsInternal:      false,
				}, true
			}
		}
		if event == OnexitRunsBeforeTheStateLeavesEventNull {
			if p.evaluateGuard(`(not _scxml_eq(selfInOuter, 1))`, engine) {
				return sce.EnabledTransition[OnexitRunsBeforeTheStateLeavesState, sce.HistoryID]{
					Source:          state,
					Targets:         transitionTargetsOfOnexitRunsBeforeTheStateLeaves[state][3],
					TransitionIndex: 3,
					HasActions:      false,
					IsInternal:      false,
				}, true
			}
		}
		if event == OnexitRunsBeforeTheStateLeavesEventNull {
			if p.evaluateGuard(`(not _scxml_eq(childInOuter, 0))`, engine) {
				return sce.EnabledTransition[OnexitRunsBeforeTheStateLeavesState, sce.HistoryID]{
					Source:          state,
					Targets:         transitionTargetsOfOnexitRunsBeforeTheStateLeaves[state][4],
					TransitionIndex: 4,
					HasActions:      false,
					IsInternal:      false,
				}, true
			}
		}
		if event == OnexitRunsBeforeTheStateLeavesEventNull {
			{
				return sce.EnabledTransition[OnexitRunsBeforeTheStateLeavesState, sce.HistoryID]{
					Source:          state,
					Targets:         transitionTargetsOfOnexitRunsBeforeTheStateLeaves[state][5],
					TransitionIndex: 5,
					HasActions:      false,
					IsInternal:      false,
				}, true
			}
		}
	case OnexitRunsBeforeTheStateLeavesStateInner:
		if event == OnexitRunsBeforeTheStateLeavesEventLeave {
			{
				return sce.EnabledTransition[OnexitRunsBeforeTheStateLeavesState, sce.HistoryID]{
					Source:          state,
					Targets:         transitionTargetsOfOnexitRunsBeforeTheStateLeaves[state][0],
					TransitionIndex: 0,
					HasActions:      false,
					IsInternal:      false,
				}, true
			}
		}
	}
	return sce.EnabledTransition[OnexitRunsBeforeTheStateLeavesState, sce.HistoryID]{}, false
}

// ExecuteTransitionContent runs one transition's executable content (W3C SCXML
// 3.13), between the microstep's exits and its entries.
//line onexit_runs_before_the_state_leaves.scxml:33
func (p *OnexitRunsBeforeTheStateLeavesPolicy) ExecuteTransitionContent(source OnexitRunsBeforeTheStateLeavesState, transitionIndex int, engine *sce.Engine[OnexitRunsBeforeTheStateLeavesState, OnexitRunsBeforeTheStateLeavesEvent]) {
	// W3C SCXML 3.13: no transition in this document has content.
}
