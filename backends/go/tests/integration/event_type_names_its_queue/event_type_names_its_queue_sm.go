// SCE-GENERATED — DO NOT EDIT
// source-hash: 0e632ea18e896bea24cf74313657aba02d906a276b3b64be501fc7738588903c


// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) 2025-2026 [Author of input SCXML file]
//
// Generated by SCE Code Generator (Go backend)
// From: integration_resources/event_type_names_its_queue/event_type_names_its_queue.scxml
//
// This generated code is MIT licensed and may be freely used in any project.
// Runtime engine dependency (sce-go-runtime): LGPL-2.1-or-later WITH LicenseRef-SCE-Linking-Exception OR LicenseRef-SCE-Commercial
// Full license: https://github.com/newmassrael/scxml-core-engine/blob/main/LICENSE
// Generated Go state machine for `event_type_names_its_queue`.
//
// Generated by SCE Code Generator (Go backend).
// Implements sce.StatePolicy with full transition logic,
// entry/exit actions, and event processing.


// SCE-MAP: event_type_names_its_queue.scxml:29 :: _machine

package event_type_names_its_queue

import (
	"fmt"
	"path/filepath"
	"strings"
	"time"

	sce "github.com/newmassrael/sce-go-runtime"
)

// Ensure imports are used
var (
	_ = fmt.Sprintf
	_ = filepath.Join
	_ = strings.HasPrefix
	_ = time.Duration(0)
)

// ======================================================================
// State type (W3C SCXML 3.3)
// ======================================================================

type EventTypeNamesItsQueueState int

const (
	EventTypeNamesItsQueueStateDone EventTypeNamesItsQueueState = 0
	EventTypeNamesItsQueueStateS0 EventTypeNamesItsQueueState = 1
)

func (s EventTypeNamesItsQueueState) String() string {
	switch s {
	case EventTypeNamesItsQueueStateDone:
		return "done"
	case EventTypeNamesItsQueueStateS0:
		return "s0"
	}
	return "unknown"
}

// EventTypeNamesItsQueueStateFromName is the read half of the pair above — a state
// id back into the state it names (W3C SCXML 3.3).
//
// A host that persists where a machine was has to write it down as TEXT: the
// constants above are a build artefact of one binary, and the process that
// resumes is a different one. String publishes the name and this reads it back,
// which is what lets a journal survive its own record and reach
// sce.Engine.EnterAt.
//
// The second return is false for a name this document does not declare. A name
// guessed at rather than refused is how a restore reaches a configuration
// nobody recorded — and the refusal is what makes a typo in a journal a
// reported failure instead of a machine quietly somewhere else.
//
// Emitted from the same loop over the document's states as String, so the two
// age together.
func EventTypeNamesItsQueueStateFromName(name string) (EventTypeNamesItsQueueState, bool) {
	switch name {
	case "done":
		return EventTypeNamesItsQueueStateDone, true
	case "s0":
		return EventTypeNamesItsQueueStateS0, true
	}
	var zero EventTypeNamesItsQueueState
	return zero, false
}

// EventTypeNamesItsQueueAllStates is every state this document declares, in the
// order the constants above are issued.
//
// Emitted from the same loop, so a walk over it is a walk over the document
// rather than over a list somebody maintained beside it — a test that spells
// its own list goes on passing when the document grows a state.
var EventTypeNamesItsQueueAllStates = []EventTypeNamesItsQueueState{
	EventTypeNamesItsQueueStateDone,
	EventTypeNamesItsQueueStateS0,
}

// EventTypeNamesItsQueueTarget is one token of a target list, as the document wrote
// it (W3C SCXML 3.13): a state, or a <history> the engine dereferences.
type EventTypeNamesItsQueueTarget = sce.EntryTarget[EventTypeNamesItsQueueState, sce.HistoryID]

// ======================================================================
// Document structure (W3C SCXML 3.2-3.4, 3.10)
//
// Package-level tables, because the structure is a fact about the document
// and not about a run: the engine's Appendix D procedures read them through
// the policy methods below, and a transition's target list is handed out as
// a slice of them rather than rebuilt on every selection.
// ======================================================================

// childStatesOfEventTypeNamesItsQueue is §scxml-D-getChildStates per state: its
// <state>, <parallel> and <final> children, in document order.
var childStatesOfEventTypeNamesItsQueue = [2][]EventTypeNamesItsQueueState{
}

// initialTargetsOfEventTypeNamesItsQueue is each compound state's initial transition
// target, as written (§scxml-3.3).
var initialTargetsOfEventTypeNamesItsQueue = [2][]EventTypeNamesItsQueueTarget{
}

// documentInitialTargetsOfEventTypeNamesItsQueue is the target of the document's own
// initial transition, as written (§scxml-3.2).
var documentInitialTargetsOfEventTypeNamesItsQueue = []EventTypeNamesItsQueueTarget{sce.StateTarget[EventTypeNamesItsQueueState, sce.HistoryID](EventTypeNamesItsQueueStateS0)}

// transitionTargetsOfEventTypeNamesItsQueue is each transition's target list, as
// written (§scxml-3.13), by source state and the transition's index among its
// source's own transitions. A targetless transition's entry is empty.
var transitionTargetsOfEventTypeNamesItsQueue = [2][][]EventTypeNamesItsQueueTarget{
	EventTypeNamesItsQueueStateS0: {
		2: {sce.StateTarget[EventTypeNamesItsQueueState, sce.HistoryID](EventTypeNamesItsQueueStateDone)},
	},
}

// ======================================================================
// Event type (W3C SCXML 3.12)
// ======================================================================

type EventTypeNamesItsQueueEvent int

const (
	EventTypeNamesItsQueueEventErrorExecution EventTypeNamesItsQueueEvent = 0
	EventTypeNamesItsQueueEventExt EventTypeNamesItsQueueEvent = 1
	EventTypeNamesItsQueueEventInt EventTypeNamesItsQueueEvent = 2
	EventTypeNamesItsQueueEventViaInternalSend EventTypeNamesItsQueueEvent = 3
	// W3C SCXML 3.13: Sentinel for eventless transition dispatch
	EventTypeNamesItsQueueEventNull EventTypeNamesItsQueueEvent = 4
)

func (e EventTypeNamesItsQueueEvent) String() string {
	switch e {
	case EventTypeNamesItsQueueEventErrorExecution:
		return "error.execution"
	case EventTypeNamesItsQueueEventExt:
		return "ext"
	case EventTypeNamesItsQueueEventInt:
		return "int"
	case EventTypeNamesItsQueueEventViaInternalSend:
		return "viaInternalSend"
	case EventTypeNamesItsQueueEventNull:
		return ""
	}
	return "unknown"
}

// ======================================================================
// Policy struct
// ======================================================================

type EventTypeNamesItsQueuePolicy struct {
	pendingEventName string
	pendingEventData string
	pendingEventType string
	pendingEventSendid string
	pendingEventOrigin string
	pendingEventOrigintype string
	pendingEventInvokeid string
	// W3C SCXML 5.10: Session ID
	SessionID string
	// W3C SCXML B.1: Script engine handle (per-instance, Path B+ Q1=(d) Go=interface ref)
	ScriptEngine sce.IScriptEngine
	scriptEngineInitialized bool
	// W3C SCXML C.2.3: inbound BasicHTTP endpoint serving this machine,
	// declared by the deployment before Initialize(). The address belongs to
	// whoever runs the listener, so the machine takes it from here rather than
	// guessing one. Empty means no such endpoint is deployed, and no BasicHTTP
	// entry is published in _ioprocessors.
	BasicHTTPAccessURI string
	// W3C SCXML 6.4: Parent communication
	ParentExternalQueue chan sce.ParentEvent
	InvokeID           string
	ChildSessionID     string
	// §scxml-6.4.3: invoke param VALUES staged by the parent, applied after
	// this machine's datamodel init so they override its `<data>` defaults.
	pendingParams      map[string]interface{}
}

// NewEventTypeNamesItsQueuePolicy creates a new policy with default values.
func NewEventTypeNamesItsQueuePolicy() EventTypeNamesItsQueuePolicy {
	return EventTypeNamesItsQueuePolicy{
	}
}

// IntCode reports what the `intCode` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `intCode` was assigned a value of another type, or the engine refused.
func (p *EventTypeNamesItsQueuePolicy) IntCode() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "intCode")
}

// SendCode reports what the `sendCode` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `sendCode` was assigned a value of another type, or the engine refused.
func (p *EventTypeNamesItsQueuePolicy) SendCode() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "sendCode")
}

// ExtCode reports what the `extCode` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `extCode` was assigned a value of another type, or the engine refused.
func (p *EventTypeNamesItsQueuePolicy) ExtCode() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "extCode")
}




// ensureScriptEngine lazily initializes the script engine session.
//
// Engine DI Parity RFC: panics if ScriptEngine was not set on the policy
// before Initialize() — mirrors Kotlin's mandatory constructor parameter
// contract. Silent no-op would otherwise turn missed injection into a
// hidden runtime regression (every <assign>/<log>/guard becomes a no-op
// and the SM walks to its default-state final without diagnostic).
func (p *EventTypeNamesItsQueuePolicy) ensureScriptEngine() {
	if p.scriptEngineInitialized {
		return
	}
	engine := p.ScriptEngine
	if engine == nil {
		panic("EventTypeNamesItsQueuePolicy: ScriptEngine field must be set before Initialize()")
	}
	sessionID := p.SessionID
	if sessionID == "" {
		sessionID = sce.GenerateSessionID()
		p.SessionID = sessionID
	}
	if !engine.HasSession(sessionID) {
		_ = engine.CreateSession(sessionID)
	}
	// W3C SCXML C.1.1 / C.2.3: the _ioprocessors entries come from the same
	// helper every other backend uses, so a machine reads the same entry names
	// and the same addresses whichever one runs it.
	_ = engine.SetupSystemVariables(sessionID, "event_type_names_its_queue",
		sce.BuildIoProcessors(sessionID, p.BasicHTTPAccessURI))
	p.scriptEngineInitialized = true
}


// InitializeDataModel sets up datamodel via script engine (W3C SCXML 5.3).
func (p *EventTypeNamesItsQueuePolicy) InitializeDataModel(eng *sce.Engine[EventTypeNamesItsQueueState, EventTypeNamesItsQueueEvent]) {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	sessionID := p.SessionID

	// W3C SCXML 5.3: Initialize _sessionid
	_ = engine.SetVariable(sessionID, "_sessionid", sessionID)

	// W3C SCXML 5.3: Initialize _name
	_ = engine.SetVariable(sessionID, "_name", "event_type_names_its_queue")

	// W3C SCXML 5.2.2: Initialize global datamodel variables
	// W3C SCXML 5.2/5.3: Initialize intCode from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "intCode", result)
		} else {
			eng.Raise(sce.NewPlatformError(EventTypeNamesItsQueueEventErrorExecution, "<data id='intCode'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "intCode", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize sendCode from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "sendCode", result)
		} else {
			eng.Raise(sce.NewPlatformError(EventTypeNamesItsQueueEventErrorExecution, "<data id='sendCode'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "sendCode", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize extCode from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "extCode", result)
		} else {
			eng.Raise(sce.NewPlatformError(EventTypeNamesItsQueueEventErrorExecution, "<data id='extCode'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "extCode", nil)
		}
	}



	// §scxml-6.4.3: apply the invoke params the parent staged — overriding the
	// `<data>` default this block just seeded, and only for a name the child
	// declares ("if the names do not match, the Processor MUST NOT add the
	// value ... to the invoked session's data model"). `HasVariable` is that
	// filter, and it is this channel's only copy of the rule.
	if len(p.pendingParams) > 0 {
		for paramName, paramValue := range p.pendingParams {
			if engine.HasVariable(sessionID, paramName) {
				_ = engine.SetVariable(sessionID, paramName, paramValue)
			}
		}
		p.pendingParams = nil
	}
}

// evaluateGuard evaluates a guard condition via script engine.
// W3C SCXML 4.3.2: If a condition expression cannot be evaluated, the processor
// MUST treat it as false and MUST raise error.execution.
func (p *EventTypeNamesItsQueuePolicy) evaluateGuard(guard string, eng *sce.Engine[EventTypeNamesItsQueueState, EventTypeNamesItsQueueEvent]) bool {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	result, err := engine.EvaluateExpression(p.SessionID, guard)
	if err != nil {
		eng.Raise(sce.NewPlatformError(EventTypeNamesItsQueueEventErrorExecution, "a <transition> cond failed to evaluate"))
		return false
	}
	return sce.ScriptToBool(result)
}

// executeScript executes a script block via script engine.
func (p *EventTypeNamesItsQueuePolicy) executeScript(script string) error {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	return engine.ExecuteScript(p.SessionID, script)
}

// setCurrentEvent binds _event system variable (W3C SCXML 5.10) and answers
// which rung of §scxml-B-2-8-1 the payload got, because the binding is the
// only place that knows.
//
// This used to be `_ = engine.SetCurrentEvent(...)`: the ladder decided
// between a DOM, a value and a space-normalized string, and the decision was
// dropped one line later. A payload that announced structure and would not
// parse therefore reached the document as raw characters, every
// _event.data.<field> read empty, and nothing anywhere could say so — see
// sce.PayloadReading.
func (p *EventTypeNamesItsQueuePolicy) setCurrentEvent(name string) sce.PayloadReading {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	data := p.pendingEventData
	eventType := p.pendingEventType
	sendID := p.pendingEventSendid
	// W3C SCXML C.1: `_event.origin` is the sender's published
	// `_ioprocessors` location, not its bare session id — and this is the one
	// place that publishes `_event` to the document, so this is where the id
	// becomes a location. The engine keeps the bare id in
	// `EventMetadata.Origin` because its session-keyed lookups (<finalize>
	// dispatch, cancelled-invoke filtering) match on it; converting at the
	// raise would make one value serve two consumers that need different
	// spellings. The conversion itself lives in `sce.PublishedOrigin`, the
	// port of the `IOProcessorHelper::publishedOrigin` the C++ engines share:
	// a second spelling of the rule is how the backends would stop agreeing.
	origin := sce.PublishedOrigin(p.pendingEventOrigin)
	originType := p.pendingEventOrigintype
	invokeID := p.pendingEventInvokeid
	reading, err := engine.SetCurrentEvent(p.SessionID, sce.SetCurrentEventArgs{
		Name:       name,
		Data:       data,
		Type:       eventType,
		SendID:     sendID,
		Origin:     origin,
		OriginType: originType,
		InvokeID:   invokeID,
	})
	if err != nil {
		// An engine that could not bind _event at all has a larger problem
		// than an unreadable payload, and reporting PayloadUndecodable for it
		// would point a host at the wrong thing.
		return sce.PayloadAbsent
	}
	return reading
}

// assignVariable assigns a value to a datamodel variable via script engine.
func (p *EventTypeNamesItsQueuePolicy) assignVariable(location, expr string) error {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	script := location + " = " + expr
	return engine.ExecuteScript(p.SessionID, script)
}

// getVariable reads a datamodel variable from the script engine.
func (p *EventTypeNamesItsQueuePolicy) getVariable(name string) interface{} {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	result, err := engine.GetVariable(p.SessionID, name)
	if err != nil {
		return nil
	}
	return result
}

// SetParamValueInScriptEngine stores an invoke param's VALUE for deferred
// application after datamodel init (§scxml-6.4.3).
//
// Params are applied at the end of InitializeDataModel, overriding the
// `<data>` default only for names the child declares. The value crosses as a
// value: this took the parent's value rendered as Lua source and
// re-evaluated it here until 2026-08-21, which survived every value a test
// had tried and lost the ones Lua cannot spell — `ToScriptLiteral` renders a
// non-finite number as `+Inf`, which is not a Lua expression, so an
// `<invoke>` `<param expr="1/0"/>` arrived as nothing at all.
func (p *EventTypeNamesItsQueuePolicy) SetParamValueInScriptEngine(name string, value interface{}) {
	if p.pendingParams == nil {
		p.pendingParams = make(map[string]interface{})
	}
	p.pendingParams[name] = value
}


// ExecutePendingInvokes is a no-op (no invokes in this SM).
func (p *EventTypeNamesItsQueuePolicy) ExecutePendingInvokes(engine *sce.Engine[EventTypeNamesItsQueueState, EventTypeNamesItsQueueEvent]) {}

// TickChildren is a no-op (no invokes in this SM).
func (p *EventTypeNamesItsQueuePolicy) TickChildren(engine *sce.Engine[EventTypeNamesItsQueueState, EventTypeNamesItsQueueEvent]) {}

// ======================================================================
// StatePolicy interface implementation
// ======================================================================

// InitialState returns the initial state (W3C SCXML 3.3).
func (p *EventTypeNamesItsQueuePolicy) InitialState() EventTypeNamesItsQueueState {
	return EventTypeNamesItsQueueStateS0
}

// IsFinalState returns true if state is a <final> state (W3C SCXML 3.7).
func (p *EventTypeNamesItsQueuePolicy) IsFinalState(state EventTypeNamesItsQueueState) bool {
	switch state {
	case EventTypeNamesItsQueueStateDone:
		return true
	}
	return false
}

// GetParent returns the parent of state, or (zero, false) if root (W3C SCXML 3.3).
func (p *EventTypeNamesItsQueuePolicy) GetParent(state EventTypeNamesItsQueueState) (EventTypeNamesItsQueueState, bool) {
	switch state {
	}
	return 0, false
}

// IsCompoundState returns true if state is a <state> with child states — exactly
// the states that have an initial transition. A <parallel> is not compound
// (W3C SCXML 3.3).
func (p *EventTypeNamesItsQueuePolicy) IsCompoundState(state EventTypeNamesItsQueueState) bool {
	return len(initialTargetsOfEventTypeNamesItsQueue[state]) > 0
}

func (p *EventTypeNamesItsQueuePolicy) IsParallelState(_ EventTypeNamesItsQueueState) bool { return false }

// GetChildStates returns state's <state>, <parallel> and <final> children, in
// document order — for a <parallel>, its regions (§scxml-D-getChildStates).
func (p *EventTypeNamesItsQueuePolicy) GetChildStates(state EventTypeNamesItsQueueState) []EventTypeNamesItsQueueState {
	return childStatesOfEventTypeNamesItsQueue[state]
}

// GetInitialTargets returns a compound state's initial transition target, as
// written; the engine's entry procedures dereference a <history> among them
// (W3C SCXML 3.3).
func (p *EventTypeNamesItsQueuePolicy) GetInitialTargets(state EventTypeNamesItsQueueState) []EventTypeNamesItsQueueTarget {
	return initialTargetsOfEventTypeNamesItsQueue[state]
}

// GetDocumentInitialTargets returns the target of the document's own initial
// transition, as written (W3C SCXML 3.2).
func (p *EventTypeNamesItsQueuePolicy) GetDocumentInitialTargets() []EventTypeNamesItsQueueTarget {
	return documentInitialTargetsOfEventTypeNamesItsQueue
}

// W3C SCXML 3.10: this document declares no <history>, so no target list names
// one and the engine never asks the two below; answering would mean inventing
// one.
func (p *EventTypeNamesItsQueuePolicy) GetHistoryParent(history sce.HistoryID) EventTypeNamesItsQueueState {
	panic(fmt.Sprintf("EventTypeNamesItsQueuePolicy declares no <history>; asked for %d", history))
}
func (p *EventTypeNamesItsQueuePolicy) GetHistoryDefaultTargets(history sce.HistoryID) []EventTypeNamesItsQueueTarget {
	panic(fmt.Sprintf("EventTypeNamesItsQueuePolicy declares no <history>; asked for %d", history))
}
func (p *EventTypeNamesItsQueuePolicy) HistoryValue(_ sce.HistoryID) ([]EventTypeNamesItsQueueState, bool) {
	return nil, false
}

// GetDocumentOrder returns the document order index (W3C SCXML Appendix D).
func (p *EventTypeNamesItsQueuePolicy) GetDocumentOrder(state EventTypeNamesItsQueueState) int {
	switch state {
	case EventTypeNamesItsQueueStateDone:
		return 1
	case EventTypeNamesItsQueueStateS0:
		return 0
	}
	return -1
}

// GetEventName returns the string name of an event (W3C SCXML 3.12).
func (p *EventTypeNamesItsQueuePolicy) GetEventName(event EventTypeNamesItsQueueEvent) string {
	return event.String()
}

// GetEventFromName looks up an event by name (W3C SCXML 3.12).
func (p *EventTypeNamesItsQueuePolicy) GetEventFromName(name string) (EventTypeNamesItsQueueEvent, bool) {
	switch name {
	case "error.execution":
		return EventTypeNamesItsQueueEventErrorExecution, true
	case "ext":
		return EventTypeNamesItsQueueEventExt, true
	case "int":
		return EventTypeNamesItsQueueEventInt, true
	case "viaInternalSend":
		return EventTypeNamesItsQueueEventViaInternalSend, true
	}
	return EventTypeNamesItsQueueEventNull, false
}

// GetStateName returns the string name of a state (W3C SCXML 3.3).
func (p *EventTypeNamesItsQueuePolicy) GetStateName(state EventTypeNamesItsQueueState) string {
	return state.String()
}

// GetStateFromName reads a state id back into the state it names (W3C SCXML 3.3).
// The reverse of GetStateName, and what turns a host's recorded configuration
// back into the argument sce.Engine.EnterAt takes.
func (p *EventTypeNamesItsQueuePolicy) GetStateFromName(name string) (EventTypeNamesItsQueueState, bool) {
	return EventTypeNamesItsQueueStateFromName(name)
}

// NullEvent returns the sentinel for eventless transition dispatch (W3C SCXML 3.13).
func (p *EventTypeNamesItsQueuePolicy) NullEvent() EventTypeNamesItsQueueEvent {
	return EventTypeNamesItsQueueEventNull
}


// HasParallelStates returns whether the SM has parallel states.
func (p *EventTypeNamesItsQueuePolicy) HasParallelStates() bool {
	return false
}

// NeedsScriptEngine returns whether the SM needs a script engine.
func (p *EventTypeNamesItsQueuePolicy) NeedsScriptEngine() bool {
	return true
}

// NeedsEventScheduler reports whether a host must drive this machine with
// Tick rather than Step alone.
//
// W3C SCXML 6.2: Step runs a macrostep and never drains the delayed-send
// scheduler nor ticks an invoked child, so a host that only ever calls it
// waits forever with nothing said. Same verdict the generate manifest
// publishes as needs_event_scheduler.
func (p *EventTypeNamesItsQueuePolicy) NeedsEventScheduler() bool {
	return false
}

// NeedsDataModelInit returns whether the SM needs datamodel init.
func (p *EventTypeNamesItsQueuePolicy) NeedsDataModelInit() bool {
	return true
}

func (p *EventTypeNamesItsQueuePolicy) HasInvokeSupport() bool { return false }
func (p *EventTypeNamesItsQueuePolicy) HasChildTick() bool { return false }
func (p *EventTypeNamesItsQueuePolicy) HasFinalize() bool { return false }
func (p *EventTypeNamesItsQueuePolicy) HasAutoforward() bool { return false }
func (p *EventTypeNamesItsQueuePolicy) HasActiveStates() bool { return false }
func (p *EventTypeNamesItsQueuePolicy) GetActiveStates() []EventTypeNamesItsQueueState { return nil }
// W3C SCXML 3.4: this machine keeps no active set — its configuration is the
// parent walk from the current state — so there is nothing for a restore to
// hand back here. sce.Engine.EnterAt reaches this only through HasActiveStates,
// which is false above; the method exists because the interface is one contract.
func (p *EventTypeNamesItsQueuePolicy) SetActiveStates(_ []EventTypeNamesItsQueueState) {}
// ExecuteFinalizeForChildEvent is a no-op (no finalize invokes).
func (p *EventTypeNamesItsQueuePolicy) ExecuteFinalizeForChildEvent(_ *sce.EventWithMetadata[EventTypeNamesItsQueueEvent], _ *sce.Engine[EventTypeNamesItsQueueState, EventTypeNamesItsQueueEvent]) {}
// ForwardToAutoforwardChildren is a no-op (no autoforward invokes).
func (p *EventTypeNamesItsQueuePolicy) ForwardToAutoforwardChildren(_ string, _ sce.EventMetadata, _ *sce.Engine[EventTypeNamesItsQueueState, EventTypeNamesItsQueueEvent]) {}

// PopulateEventMetadata stores pending event metadata (W3C SCXML 5.10).
// Note: event name is set separately via setCurrentEvent(), not from metadata.
func (p *EventTypeNamesItsQueuePolicy) PopulateEventMetadata(meta *sce.EventMetadata) {
	p.pendingEventData = meta.Data
	p.pendingEventType = meta.EventType.String()
	p.pendingEventSendid = meta.SendID
	p.pendingEventOrigin = meta.Origin
	p.pendingEventOrigintype = meta.OriginType
	p.pendingEventInvokeid = meta.InvokeID
}

// LiftTypedPayload binds the dequeued event's typed `_event.data` view from
// the data it carries (NL→IR Item C1 Path A).
//
// The typed carrier PopulateEventMetadata reads is filled by one producer, the
// generated Raise<Event> inject seam. Every other producer — <send> with
// <param>, an invoke forwarding either way, autoforward, BasicHTTP, mesh —
// fills EventMetadata.Data, so the schema's fields are read out of that here.
// A refusal is returned for the engine to raise as error.execution, which is
// what W3C SCXML 3.13 gives for a guard that cannot be evaluated.
func (p *EventTypeNamesItsQueuePolicy) LiftTypedPayload(event EventTypeNamesItsQueueEvent, meta *sce.EventMetadata) error {
	// This document lowered no typed guard, so no event carries a typed view.
	return nil
}

// ClearEventMetadata resets pending event metadata (W3C SCXML 5.10).
func (p *EventTypeNamesItsQueuePolicy) ClearEventMetadata() {
	p.pendingEventName = ""
	p.pendingEventData = ""
	p.pendingEventType = ""
	p.pendingEventSendid = ""
	p.pendingEventOrigin = ""
	p.pendingEventOrigintype = ""
	p.pendingEventInvokeid = ""
}



// ExecuteEntryActions enters one state (W3C SCXML 3.8): adds it to the
// configuration, runs its <onentry>, and its <initial> transition's content when
// its initial state is entered by default.
//line event_type_names_its_queue.scxml:29
func (p *EventTypeNamesItsQueuePolicy) ExecuteEntryActions(state EventTypeNamesItsQueueState, engine *sce.Engine[EventTypeNamesItsQueueState, EventTypeNamesItsQueueEvent], isDefaultEntry bool) {
	p.ensureScriptEngine()
	switch state {
	case EventTypeNamesItsQueueStateS0:
		//line event_type_names_its_queue.scxml:39
		// W3C SCXML 3.8: onentry block 0 (break on error stops subsequent actions)
		for actionBlock0 := 0; actionBlock0 < 1; actionBlock0++ {
			_ = actionBlock0

	// W3C SCXML 6.2: send id="__send_0"
	{
		eventDataStr := ""
		_ = eventDataStr
	// W3C SCXML 6.2: External send
	{
		meta := sce.NewEventWithMetadata(EventTypeNamesItsQueueEventExt)
		meta.Metadata = sce.ExternalMetadata("__send_0", "")
		meta.Metadata.Data = eventDataStr
		engine.RaiseExternalWithMeta(meta)
	}
	}


	engine.Raise(sce.NewEventWithMetadata(EventTypeNamesItsQueueEventInt))


	// W3C SCXML 6.2: send id="__send_1"
	// W3C SCXML 6.2: Evaluate <param>/namelist expressions at send time
	{
		p.ensureScriptEngine()
		se := p.ScriptEngine
		// W3C SCXML 6.2 / test178: a name may repeat and every value must be
		// delivered, so this is an ordered list rather than a map. The typed
		// value is kept rather than its text — a receiver reading
		// `_event.data.value === 42` finds the string "42" unequal.
		parts := make([]sce.EventDataParam, 0)
		if paramVal, paramErr := se.EvaluateExpression(p.SessionID, `1`); paramErr == nil {
			parts = append(parts, sce.EventDataParam{Name: "a", Value: paramVal})
		} else {
			engine.Raise(sce.NewPlatformError(EventTypeNamesItsQueueEventErrorExecution, "<send> <param name='a'> expr failed to evaluate"))
		}
		eventDataStr := sce.BuildJSONFromTypedParams(parts)
		_ = eventDataStr
	// W3C SCXML 6.2: Internal send (target="#_internal")
	{
		meta := sce.NewEventWithMetadata(EventTypeNamesItsQueueEventViaInternalSend)
		meta.Metadata.Data = eventDataStr
		engine.Raise(meta)
	}
	}

		}
	default:
		// No entry actions
	}
}

// ExecuteHistoryDefaultContent runs a <history>'s default transition content
// (W3C SCXML 3.10.2), after its parent's onentry (and after the parent's own
// <initial> content) when the history was taken with nothing recorded. The
// engine asks for it by the entry set's defaultHistoryContent answer; a history
// that restored what it recorded runs nothing.
//line event_type_names_its_queue.scxml:29
func (p *EventTypeNamesItsQueuePolicy) ExecuteHistoryDefaultContent(history sce.HistoryID, engine *sce.Engine[EventTypeNamesItsQueueState, EventTypeNamesItsQueueEvent]) {
	// W3C SCXML 3.10.2: no <history> in this document has default content.
}

// ExecuteExitActions exits one state (W3C SCXML 3.9): records its histories,
// runs its <onexit>, cancels its invocations and removes it from the
// configuration — §scxml-D-exitStates's order.
//line event_type_names_its_queue.scxml:29
func (p *EventTypeNamesItsQueuePolicy) ExecuteExitActions(state EventTypeNamesItsQueueState, engine *sce.Engine[EventTypeNamesItsQueueState, EventTypeNamesItsQueueEvent], configurationBeforeExit []EventTypeNamesItsQueueState) {
	p.ensureScriptEngine()
	// §scxml-D-exitStates orders one state's exit as onexit, then
	// cancelInvoke, then configuration.delete(s), so `In(s)` inside s's own
	// handler is true: the <onexit> switch comes first and the removal from
	// the configuration last.
	switch state {
	default:
		// No exit actions
	}
}



// BindCurrentEvent binds the event whose transitions are about to be selected as
// the _event their guards read (W3C SCXML 5.10) — before the first guard runs,
// and not for an eventless selection, which has no event of its own.
//line event_type_names_its_queue.scxml:29
func (p *EventTypeNamesItsQueuePolicy) BindCurrentEvent(event EventTypeNamesItsQueueEvent, engine *sce.Engine[EventTypeNamesItsQueueState, EventTypeNamesItsQueueEvent]) {
	if event != EventTypeNamesItsQueueEventNull {
		// §scxml-B-2-8-1: the rung the payload got, handed to the engine
		// rather than dropped. This is the only frame that has both the
		// reading and the event it belongs to.
		engine.NotePayloadReading(event, p.setCurrentEvent(p.GetEventName(event)))
	}
}

// FirstEnabledTransition is Appendix D selectTransitions, the half only this
// document can answer: the first of state's own transitions, in document order,
// that event enables and whose guard holds. The engine walks the atomic states
// and their ancestors and keeps the ordered set; the null event asks for
// eventless transitions.
//line event_type_names_its_queue.scxml:29
func (p *EventTypeNamesItsQueuePolicy) FirstEnabledTransition(state EventTypeNamesItsQueueState, event EventTypeNamesItsQueueEvent, engine *sce.Engine[EventTypeNamesItsQueueState, EventTypeNamesItsQueueEvent]) (sce.EnabledTransition[EventTypeNamesItsQueueState, sce.HistoryID], bool) {
	switch state {
	case EventTypeNamesItsQueueStateS0:
		if event == EventTypeNamesItsQueueEventInt {
			{
				return sce.EnabledTransition[EventTypeNamesItsQueueState, sce.HistoryID]{
					Source:          state,
					TransitionIndex: 0,
					HasActions:      true,
					IsInternal:      false,
				}, true
			}
		}
		if event == EventTypeNamesItsQueueEventViaInternalSend {
			{
				return sce.EnabledTransition[EventTypeNamesItsQueueState, sce.HistoryID]{
					Source:          state,
					TransitionIndex: 1,
					HasActions:      true,
					IsInternal:      false,
				}, true
			}
		}
		if event == EventTypeNamesItsQueueEventExt {
			{
				return sce.EnabledTransition[EventTypeNamesItsQueueState, sce.HistoryID]{
					Source:          state,
					Targets:         transitionTargetsOfEventTypeNamesItsQueue[state][2],
					TransitionIndex: 2,
					HasActions:      true,
					IsInternal:      false,
				}, true
			}
		}
	}
	return sce.EnabledTransition[EventTypeNamesItsQueueState, sce.HistoryID]{}, false
}

// ExecuteTransitionContent runs one transition's executable content (W3C SCXML
// 3.13), between the microstep's exits and its entries.
//line event_type_names_its_queue.scxml:29
func (p *EventTypeNamesItsQueuePolicy) ExecuteTransitionContent(source EventTypeNamesItsQueueState, transitionIndex int, engine *sce.Engine[EventTypeNamesItsQueueState, EventTypeNamesItsQueueEvent]) {
	p.ensureScriptEngine()
	switch source {
	case EventTypeNamesItsQueueStateS0:
		switch transitionIndex {
		case 0:
			//line event_type_names_its_queue.scxml:48

	if p.evaluateGuard(`_scxml_eq(_event.type, "internal")`, engine) {

	// W3C SCXML 5.3: <assign location="intCode" expr="1">
	if err := p.assignVariable(`intCode`, `1`); err != nil {
		engine.Raise(sce.NewPlatformError(EventTypeNamesItsQueueEventErrorExecution, "<assign> to 'intCode' failed"))
	}

	} else if p.evaluateGuard(`_scxml_eq(_event.type, "external")`, engine) {

	// W3C SCXML 5.3: <assign location="intCode" expr="2">
	if err := p.assignVariable(`intCode`, `2`); err != nil {
		engine.Raise(sce.NewPlatformError(EventTypeNamesItsQueueEventErrorExecution, "<assign> to 'intCode' failed"))
	}

	} else {

	// W3C SCXML 5.3: <assign location="intCode" expr="3">
	if err := p.assignVariable(`intCode`, `3`); err != nil {
		engine.Raise(sce.NewPlatformError(EventTypeNamesItsQueueEventErrorExecution, "<assign> to 'intCode' failed"))
	}

	}
		case 1:
			//line event_type_names_its_queue.scxml:58

	if p.evaluateGuard(`_scxml_eq(_event.type, "internal")`, engine) {

	// W3C SCXML 5.3: <assign location="sendCode" expr="1">
	if err := p.assignVariable(`sendCode`, `1`); err != nil {
		engine.Raise(sce.NewPlatformError(EventTypeNamesItsQueueEventErrorExecution, "<assign> to 'sendCode' failed"))
	}

	} else if p.evaluateGuard(`_scxml_eq(_event.type, "external")`, engine) {

	// W3C SCXML 5.3: <assign location="sendCode" expr="2">
	if err := p.assignVariable(`sendCode`, `2`); err != nil {
		engine.Raise(sce.NewPlatformError(EventTypeNamesItsQueueEventErrorExecution, "<assign> to 'sendCode' failed"))
	}

	} else {

	// W3C SCXML 5.3: <assign location="sendCode" expr="3">
	if err := p.assignVariable(`sendCode`, `3`); err != nil {
		engine.Raise(sce.NewPlatformError(EventTypeNamesItsQueueEventErrorExecution, "<assign> to 'sendCode' failed"))
	}

	}
		case 2:
			//line event_type_names_its_queue.scxml:68

	if p.evaluateGuard(`_scxml_eq(_event.type, "internal")`, engine) {

	// W3C SCXML 5.3: <assign location="extCode" expr="1">
	if err := p.assignVariable(`extCode`, `1`); err != nil {
		engine.Raise(sce.NewPlatformError(EventTypeNamesItsQueueEventErrorExecution, "<assign> to 'extCode' failed"))
	}

	} else if p.evaluateGuard(`_scxml_eq(_event.type, "external")`, engine) {

	// W3C SCXML 5.3: <assign location="extCode" expr="2">
	if err := p.assignVariable(`extCode`, `2`); err != nil {
		engine.Raise(sce.NewPlatformError(EventTypeNamesItsQueueEventErrorExecution, "<assign> to 'extCode' failed"))
	}

	} else {

	// W3C SCXML 5.3: <assign location="extCode" expr="3">
	if err := p.assignVariable(`extCode`, `3`); err != nil {
		engine.Raise(sce.NewPlatformError(EventTypeNamesItsQueueEventErrorExecution, "<assign> to 'extCode' failed"))
	}

	}
		}
	}
}
