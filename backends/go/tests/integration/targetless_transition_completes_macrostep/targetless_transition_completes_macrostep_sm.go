// SCE-GENERATED — DO NOT EDIT
// source-hash: 23347f5c092342ad5655a09f8c78eecec8de3c0705a0affd88f1ecbcd658f869
// template-hash: 63129ea5a60cce4407210a3c2e3ff224327767ebf6618c3f4ed41b0a49b7454d
// generated-at: 0


// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) 2025-2026 [Author of input SCXML file]
//
// Generated by SCE Code Generator (Go backend)
// From: integration_resources/targetless_transition_completes_macrostep/targetless_transition_completes_macrostep.scxml
//
// This generated code is MIT licensed and may be freely used in any project.
// Runtime engine dependency (sce-go-runtime): LGPL-2.1-or-later WITH LicenseRef-SCE-Linking-Exception OR LicenseRef-SCE-Commercial
// Full license: https://github.com/newmassrael/scxml-core-engine/blob/main/LICENSE
// Generated Go state machine for `targetless_transition_completes_macrostep`.
//
// Generated by SCE Code Generator (Go backend).
// Implements sce.StatePolicy with full transition logic,
// entry/exit actions, and event processing.


// SCE-MAP: targetless_transition_completes_macrostep.scxml:51 :: _machine

package targetless_transition_completes_macrostep

import (
	"fmt"
	"path/filepath"
	"strings"
	"time"

	sce "github.com/newmassrael/sce-go-runtime"
)

// Ensure imports are used
var (
	_ = fmt.Sprintf
	_ = filepath.Join
	_ = strings.HasPrefix
	_ = time.Duration(0)
)

// ======================================================================
// State type (W3C SCXML 3.3)
// ======================================================================

type TargetlessTransitionCompletesMacrostepState int

const (
	TargetlessTransitionCompletesMacrostepStateIdle TargetlessTransitionCompletesMacrostepState = 0
	TargetlessTransitionCompletesMacrostepStateRecycled TargetlessTransitionCompletesMacrostepState = 1
	TargetlessTransitionCompletesMacrostepStateSettled TargetlessTransitionCompletesMacrostepState = 2
)

func (s TargetlessTransitionCompletesMacrostepState) String() string {
	switch s {
	case TargetlessTransitionCompletesMacrostepStateIdle:
		return "idle"
	case TargetlessTransitionCompletesMacrostepStateRecycled:
		return "recycled"
	case TargetlessTransitionCompletesMacrostepStateSettled:
		return "settled"
	}
	return "unknown"
}

// ======================================================================
// Event type (W3C SCXML 3.12)
// ======================================================================

type TargetlessTransitionCompletesMacrostepEvent int

const (
	TargetlessTransitionCompletesMacrostepEventArm TargetlessTransitionCompletesMacrostepEvent = 0
	TargetlessTransitionCompletesMacrostepEventErrorExecution TargetlessTransitionCompletesMacrostepEvent = 1
	TargetlessTransitionCompletesMacrostepEventPing TargetlessTransitionCompletesMacrostepEvent = 2
	TargetlessTransitionCompletesMacrostepEventPong TargetlessTransitionCompletesMacrostepEvent = 3
	TargetlessTransitionCompletesMacrostepEventQuiet TargetlessTransitionCompletesMacrostepEvent = 4
	TargetlessTransitionCompletesMacrostepEventRecycle TargetlessTransitionCompletesMacrostepEvent = 5
	// W3C SCXML 3.13: Sentinel for eventless transition dispatch
	TargetlessTransitionCompletesMacrostepEventNull TargetlessTransitionCompletesMacrostepEvent = 6
)

func (e TargetlessTransitionCompletesMacrostepEvent) String() string {
	switch e {
	case TargetlessTransitionCompletesMacrostepEventArm:
		return "arm"
	case TargetlessTransitionCompletesMacrostepEventErrorExecution:
		return "error.execution"
	case TargetlessTransitionCompletesMacrostepEventPing:
		return "ping"
	case TargetlessTransitionCompletesMacrostepEventPong:
		return "pong"
	case TargetlessTransitionCompletesMacrostepEventQuiet:
		return "quiet"
	case TargetlessTransitionCompletesMacrostepEventRecycle:
		return "recycle"
	case TargetlessTransitionCompletesMacrostepEventNull:
		return ""
	}
	return "unknown"
}

// ======================================================================
// Policy struct
// ======================================================================

type TargetlessTransitionCompletesMacrostepPolicy struct {
	// W3C SCXML 3.13: Last transition metadata
	lastTransitionIsInternal  bool
	lastTransitionIsTargetless bool
	lastTransitionSourceState TargetlessTransitionCompletesMacrostepState
	// W3C SCXML 3.13: Transition action tracking
	lastTransitionIndex   int
	hasTransitionActions   bool
	// W3C SCXML 5.10.1: External event flag
	nextEventIsExternal bool
	pendingEventName string
	pendingEventData string
	pendingEventType string
	pendingEventSendid string
	pendingEventOrigin string
	pendingEventOrigintype string
	pendingEventInvokeid string
	// W3C SCXML 5.10: Session ID
	SessionID string
	// W3C SCXML B.1: Script engine handle (per-instance, Path B+ Q1=(d) Go=interface ref)
	ScriptEngine sce.IScriptEngine
	scriptEngineInitialized bool
	// W3C SCXML C.2.3: inbound BasicHTTP endpoint serving this machine,
	// declared by the deployment before Initialize(). The address belongs to
	// whoever runs the listener, so the machine takes it from here rather than
	// guessing one. Empty means no such endpoint is deployed, and no BasicHTTP
	// entry is published in _ioprocessors.
	BasicHTTPAccessURI string
	// W3C SCXML 6.4: Parent communication
	ParentExternalQueue chan sce.ParentEvent
	InvokeID           string
	ChildSessionID     string
	// W3C SCXML 6.4.1: Deferred params from parent invoke (applied after datamodel init)
	pendingParams      map[string]string
}

// NewTargetlessTransitionCompletesMacrostepPolicy creates a new policy with default values.
func NewTargetlessTransitionCompletesMacrostepPolicy() TargetlessTransitionCompletesMacrostepPolicy {
	return TargetlessTransitionCompletesMacrostepPolicy{
		lastTransitionSourceState: TargetlessTransitionCompletesMacrostepStateIdle,
	}
}

// Quiet reports what the `quiet` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `quiet` was assigned a value of another type, or the engine refused.
func (p *TargetlessTransitionCompletesMacrostepPolicy) Quiet() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "quiet")
}

// Armed reports what the `armed` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `armed` was assigned a value of another type, or the engine refused.
func (p *TargetlessTransitionCompletesMacrostepPolicy) Armed() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "armed")
}

// Chained reports what the `chained` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `chained` was assigned a value of another type, or the engine refused.
func (p *TargetlessTransitionCompletesMacrostepPolicy) Chained() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "chained")
}

// Polished reports what the `polished` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `polished` was assigned a value of another type, or the engine refused.
func (p *TargetlessTransitionCompletesMacrostepPolicy) Polished() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "polished")
}

// Answered reports what the `answered` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `answered` was assigned a value of another type, or the engine refused.
func (p *TargetlessTransitionCompletesMacrostepPolicy) Answered() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "answered")
}

// Entries reports what the `entries` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `entries` was assigned a value of another type, or the engine refused.
func (p *TargetlessTransitionCompletesMacrostepPolicy) Entries() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "entries")
}




// ensureScriptEngine lazily initializes the script engine session.
//
// Engine DI Parity RFC: panics if ScriptEngine was not set on the policy
// before Initialize() — mirrors Kotlin's mandatory constructor parameter
// contract. Silent no-op would otherwise turn missed injection into a
// hidden runtime regression (every <assign>/<log>/guard becomes a no-op
// and the SM walks to its default-state final without diagnostic).
func (p *TargetlessTransitionCompletesMacrostepPolicy) ensureScriptEngine() {
	if p.scriptEngineInitialized {
		return
	}
	engine := p.ScriptEngine
	if engine == nil {
		panic("TargetlessTransitionCompletesMacrostepPolicy: ScriptEngine field must be set before Initialize()")
	}
	sessionID := p.SessionID
	if sessionID == "" {
		sessionID = sce.GenerateSessionID()
		p.SessionID = sessionID
	}
	if !engine.HasSession(sessionID) {
		_ = engine.CreateSession(sessionID)
	}
	// W3C SCXML C.1.1 / C.2.3: the _ioprocessors entries come from the same
	// helper every other backend uses, so a machine reads the same entry names
	// and the same addresses whichever one runs it.
	_ = engine.SetupSystemVariables(sessionID, "targetless_transition_completes_macrostep",
		sce.BuildIoProcessors(sessionID, p.BasicHTTPAccessURI))
	p.scriptEngineInitialized = true
}

// InitializeDataModel sets up datamodel via script engine (W3C SCXML 5.3).
func (p *TargetlessTransitionCompletesMacrostepPolicy) InitializeDataModel(eng *sce.Engine[TargetlessTransitionCompletesMacrostepState, TargetlessTransitionCompletesMacrostepEvent]) {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	sessionID := p.SessionID

	// W3C SCXML 5.3: Initialize _sessionid
	_ = engine.SetVariable(sessionID, "_sessionid", sessionID)

	// W3C SCXML 5.3: Initialize _name
	_ = engine.SetVariable(sessionID, "_name", "targetless_transition_completes_macrostep")

	// W3C SCXML 5.2.2: Initialize global datamodel variables
	// W3C SCXML 5.2/5.3: Initialize quiet from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "quiet", result)
		} else {
			eng.Raise(sce.NewPlatformError(TargetlessTransitionCompletesMacrostepEventErrorExecution, "<data id='quiet'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "quiet", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize armed from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "armed", result)
		} else {
			eng.Raise(sce.NewPlatformError(TargetlessTransitionCompletesMacrostepEventErrorExecution, "<data id='armed'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "armed", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize chained from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "chained", result)
		} else {
			eng.Raise(sce.NewPlatformError(TargetlessTransitionCompletesMacrostepEventErrorExecution, "<data id='chained'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "chained", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize polished from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "polished", result)
		} else {
			eng.Raise(sce.NewPlatformError(TargetlessTransitionCompletesMacrostepEventErrorExecution, "<data id='polished'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "polished", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize answered from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "answered", result)
		} else {
			eng.Raise(sce.NewPlatformError(TargetlessTransitionCompletesMacrostepEventErrorExecution, "<data id='answered'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "answered", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize entries from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "entries", result)
		} else {
			eng.Raise(sce.NewPlatformError(TargetlessTransitionCompletesMacrostepEventErrorExecution, "<data id='entries'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "entries", nil)
		}
	}



	// W3C SCXML 6.4.1: Apply deferred params from parent invoke (override defaults, skip undeclared)
	if len(p.pendingParams) > 0 {
		for paramName, paramExpr := range p.pendingParams {
			if engine.HasVariable(sessionID, paramName) {
				if val, err := engine.EvaluateExpression(sessionID, paramExpr); err == nil {
					_ = engine.SetVariable(sessionID, paramName, val)
				} else {
					_ = engine.SetVariable(sessionID, paramName, paramExpr)
				}
			}
		}
		p.pendingParams = nil
	}
}

// evaluateGuard evaluates a guard condition via script engine.
// W3C SCXML 4.3.2: If a condition expression cannot be evaluated, the processor
// MUST treat it as false and MUST raise error.execution.
func (p *TargetlessTransitionCompletesMacrostepPolicy) evaluateGuard(guard string, eng *sce.Engine[TargetlessTransitionCompletesMacrostepState, TargetlessTransitionCompletesMacrostepEvent]) bool {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	result, err := engine.EvaluateExpression(p.SessionID, guard)
	if err != nil {
		eng.Raise(sce.NewPlatformError(TargetlessTransitionCompletesMacrostepEventErrorExecution, "a <transition> cond failed to evaluate"))
		return false
	}
	return sce.ScriptToBool(result)
}

// executeScript executes a script block via script engine.
func (p *TargetlessTransitionCompletesMacrostepPolicy) executeScript(script string) error {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	return engine.ExecuteScript(p.SessionID, script)
}

// setCurrentEvent binds _event system variable (W3C SCXML 5.10).
func (p *TargetlessTransitionCompletesMacrostepPolicy) setCurrentEvent(name string) {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	data := p.pendingEventData
	eventType := p.pendingEventType
	sendID := p.pendingEventSendid
	// W3C SCXML C.1: `_event.origin` is the sender's published
	// `_ioprocessors` location, not its bare session id — and this is the one
	// place that publishes `_event` to the document, so this is where the id
	// becomes a location. The engine keeps the bare id in
	// `EventMetadata.Origin` because its session-keyed lookups (<finalize>
	// dispatch, cancelled-invoke filtering) match on it; converting at the
	// raise would make one value serve two consumers that need different
	// spellings. The conversion itself lives in `sce.PublishedOrigin`, the
	// port of the `IOProcessorHelper::publishedOrigin` the C++ engines share:
	// a second spelling of the rule is how the backends would stop agreeing.
	origin := sce.PublishedOrigin(p.pendingEventOrigin)
	originType := p.pendingEventOrigintype
	invokeID := p.pendingEventInvokeid
	_ = engine.SetCurrentEvent(p.SessionID, sce.SetCurrentEventArgs{
		Name:       name,
		Data:       data,
		Type:       eventType,
		SendID:     sendID,
		Origin:     origin,
		OriginType: originType,
		InvokeID:   invokeID,
	})
}

// assignVariable assigns a value to a datamodel variable via script engine.
func (p *TargetlessTransitionCompletesMacrostepPolicy) assignVariable(location, expr string) error {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	script := location + " = " + expr
	return engine.ExecuteScript(p.SessionID, script)
}

// getVariable reads a datamodel variable from the script engine.
func (p *TargetlessTransitionCompletesMacrostepPolicy) getVariable(name string) interface{} {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	result, err := engine.GetVariable(p.SessionID, name)
	if err != nil {
		return nil
	}
	return result
}

// SetParamInScriptEngine stores a parameter for deferred application after datamodel init (W3C SCXML 6.4.1).
// Params are applied at the end of InitializeDataModel, overriding defaults only for declared vars.
func (p *TargetlessTransitionCompletesMacrostepPolicy) SetParamInScriptEngine(name, expr string) {
	if p.pendingParams == nil {
		p.pendingParams = make(map[string]string)
	}
	p.pendingParams[name] = expr
}


// ExecutePendingInvokes is a no-op (no invokes in this SM).
func (p *TargetlessTransitionCompletesMacrostepPolicy) ExecutePendingInvokes(engine *sce.Engine[TargetlessTransitionCompletesMacrostepState, TargetlessTransitionCompletesMacrostepEvent]) {}

// TickChildren is a no-op (no invokes in this SM).
func (p *TargetlessTransitionCompletesMacrostepPolicy) TickChildren(engine *sce.Engine[TargetlessTransitionCompletesMacrostepState, TargetlessTransitionCompletesMacrostepEvent]) {}

// ======================================================================
// StatePolicy interface implementation
// ======================================================================

// InitialState returns the initial state (W3C SCXML 3.3).
func (p *TargetlessTransitionCompletesMacrostepPolicy) InitialState() TargetlessTransitionCompletesMacrostepState {
	return TargetlessTransitionCompletesMacrostepStateIdle
}

// IsFinalState returns true if state is a <final> state (W3C SCXML 3.7).
func (p *TargetlessTransitionCompletesMacrostepPolicy) IsFinalState(state TargetlessTransitionCompletesMacrostepState) bool {
	switch state {
	}
	return false
}

// GetParent returns the parent of state, or (zero, false) if root (W3C SCXML 3.3).
func (p *TargetlessTransitionCompletesMacrostepPolicy) GetParent(state TargetlessTransitionCompletesMacrostepState) (TargetlessTransitionCompletesMacrostepState, bool) {
	switch state {
	}
	return 0, false
}

// IsCompoundState returns true if state has children (W3C SCXML 3.3).
func (p *TargetlessTransitionCompletesMacrostepPolicy) IsCompoundState(state TargetlessTransitionCompletesMacrostepState) bool {
	switch state {
	}
	return false
}

func (p *TargetlessTransitionCompletesMacrostepPolicy) IsParallelState(_ TargetlessTransitionCompletesMacrostepState) bool { return false }
func (p *TargetlessTransitionCompletesMacrostepPolicy) GetParallelRegions(_ TargetlessTransitionCompletesMacrostepState) []TargetlessTransitionCompletesMacrostepState { return nil }

// IsDescendantOf returns true if desc is a descendant of anc (W3C SCXML 3.12).
func (p *TargetlessTransitionCompletesMacrostepPolicy) IsDescendantOf(desc, anc TargetlessTransitionCompletesMacrostepState) bool {
	current := desc
	for {
		parent, ok := p.GetParent(current)
		if !ok {
			return false
		}
		if parent == anc {
			return true
		}
		current = parent
	}
}

// GetDocumentOrder returns the document order index (W3C SCXML Appendix D).
func (p *TargetlessTransitionCompletesMacrostepPolicy) GetDocumentOrder(state TargetlessTransitionCompletesMacrostepState) int {
	switch state {
	case TargetlessTransitionCompletesMacrostepStateIdle:
		return 0
	case TargetlessTransitionCompletesMacrostepStateRecycled:
		return 2
	case TargetlessTransitionCompletesMacrostepStateSettled:
		return 1
	}
	return -1
}

// GetEventName returns the string name of an event (W3C SCXML 3.12).
func (p *TargetlessTransitionCompletesMacrostepPolicy) GetEventName(event TargetlessTransitionCompletesMacrostepEvent) string {
	return event.String()
}

// GetEventFromName looks up an event by name (W3C SCXML 3.12).
func (p *TargetlessTransitionCompletesMacrostepPolicy) GetEventFromName(name string) (TargetlessTransitionCompletesMacrostepEvent, bool) {
	switch name {
	case "arm":
		return TargetlessTransitionCompletesMacrostepEventArm, true
	case "error.execution":
		return TargetlessTransitionCompletesMacrostepEventErrorExecution, true
	case "ping":
		return TargetlessTransitionCompletesMacrostepEventPing, true
	case "pong":
		return TargetlessTransitionCompletesMacrostepEventPong, true
	case "quiet":
		return TargetlessTransitionCompletesMacrostepEventQuiet, true
	case "recycle":
		return TargetlessTransitionCompletesMacrostepEventRecycle, true
	}
	return TargetlessTransitionCompletesMacrostepEventNull, false
}

// GetStateName returns the string name of a state (W3C SCXML 3.3).
func (p *TargetlessTransitionCompletesMacrostepPolicy) GetStateName(state TargetlessTransitionCompletesMacrostepState) string {
	return state.String()
}

// NullEvent returns the sentinel for eventless transition dispatch (W3C SCXML 3.13).
func (p *TargetlessTransitionCompletesMacrostepPolicy) NullEvent() TargetlessTransitionCompletesMacrostepEvent {
	return TargetlessTransitionCompletesMacrostepEventNull
}

// GetInitialChildren returns initial children of a compound state (W3C SCXML 3.6).
func (p *TargetlessTransitionCompletesMacrostepPolicy) GetInitialChildren(state TargetlessTransitionCompletesMacrostepState) []TargetlessTransitionCompletesMacrostepState {
	switch state {
	}
	return nil
}

// LastTransitionIsInternal returns the internal transition flag (W3C SCXML 3.13).
func (p *TargetlessTransitionCompletesMacrostepPolicy) LastTransitionIsInternal() bool {
	return p.lastTransitionIsInternal
}

// SetLastTransitionIsInternal sets the internal transition flag.
func (p *TargetlessTransitionCompletesMacrostepPolicy) SetLastTransitionIsInternal(value bool) {
	p.lastTransitionIsInternal = value
}

// LastTransitionIsTargetless returns the targetless transition flag (W3C SCXML 3.13).
func (p *TargetlessTransitionCompletesMacrostepPolicy) LastTransitionIsTargetless() bool {
	return p.lastTransitionIsTargetless
}

// SetLastTransitionIsTargetless sets the targetless transition flag.
func (p *TargetlessTransitionCompletesMacrostepPolicy) SetLastTransitionIsTargetless(value bool) {
	p.lastTransitionIsTargetless = value
}

// LastTransitionSourceState returns the source state of the last transition.
func (p *TargetlessTransitionCompletesMacrostepPolicy) LastTransitionSourceState() TargetlessTransitionCompletesMacrostepState {
	return p.lastTransitionSourceState
}

// SetLastTransitionSourceState sets the source state of the last transition.
func (p *TargetlessTransitionCompletesMacrostepPolicy) SetLastTransitionSourceState(state TargetlessTransitionCompletesMacrostepState) {
	p.lastTransitionSourceState = state
}


// SetNextEventIsExternal sets the external event flag (W3C SCXML 5.10.1).
func (p *TargetlessTransitionCompletesMacrostepPolicy) SetNextEventIsExternal(value bool) {
	p.nextEventIsExternal = value
}

// HasParallelStates returns whether the SM has parallel states.
func (p *TargetlessTransitionCompletesMacrostepPolicy) HasParallelStates() bool {
	return false
}

// NeedsScriptEngine returns whether the SM needs a script engine.
func (p *TargetlessTransitionCompletesMacrostepPolicy) NeedsScriptEngine() bool {
	return true
}

// NeedsEventScheduler reports whether a host must drive this machine with
// Tick rather than Step alone.
//
// W3C SCXML 6.2: Step runs a macrostep and never drains the delayed-send
// scheduler nor ticks an invoked child, so a host that only ever calls it
// waits forever with nothing said. Same verdict the generate manifest
// publishes as needs_event_scheduler.
func (p *TargetlessTransitionCompletesMacrostepPolicy) NeedsEventScheduler() bool {
	return false
}

// NeedsDataModelInit returns whether the SM needs datamodel init.
func (p *TargetlessTransitionCompletesMacrostepPolicy) NeedsDataModelInit() bool {
	return true
}

func (p *TargetlessTransitionCompletesMacrostepPolicy) HasInvokeSupport() bool { return false }
func (p *TargetlessTransitionCompletesMacrostepPolicy) HasChildTick() bool { return false }
func (p *TargetlessTransitionCompletesMacrostepPolicy) HasFinalize() bool { return false }
func (p *TargetlessTransitionCompletesMacrostepPolicy) HasAutoforward() bool { return false }
func (p *TargetlessTransitionCompletesMacrostepPolicy) HasActiveStates() bool { return false }
func (p *TargetlessTransitionCompletesMacrostepPolicy) GetActiveStates() []TargetlessTransitionCompletesMacrostepState { return nil }
func (p *TargetlessTransitionCompletesMacrostepPolicy) HasExternalEventFlag() bool { return true }
// GetInitialOrHistoryChild returns the initial child considering history (W3C SCXML 3.11).
func (p *TargetlessTransitionCompletesMacrostepPolicy) GetInitialOrHistoryChild(state TargetlessTransitionCompletesMacrostepState) TargetlessTransitionCompletesMacrostepState {
	children := p.GetInitialChildren(state)
	if len(children) > 0 {
		return children[0]
	}
	return state
}
// ExecuteFinalizeForChildEvent is a no-op (no finalize invokes).
func (p *TargetlessTransitionCompletesMacrostepPolicy) ExecuteFinalizeForChildEvent(_ *sce.EventWithMetadata[TargetlessTransitionCompletesMacrostepEvent], _ *sce.Engine[TargetlessTransitionCompletesMacrostepState, TargetlessTransitionCompletesMacrostepEvent]) {}
// ForwardToAutoforwardChildren is a no-op (no autoforward invokes).
func (p *TargetlessTransitionCompletesMacrostepPolicy) ForwardToAutoforwardChildren(_ string, _ sce.EventMetadata, _ *sce.Engine[TargetlessTransitionCompletesMacrostepState, TargetlessTransitionCompletesMacrostepEvent]) {}

// PopulateEventMetadata stores pending event metadata (W3C SCXML 5.10).
// Note: event name is set separately via setCurrentEvent(), not from metadata.
func (p *TargetlessTransitionCompletesMacrostepPolicy) PopulateEventMetadata(meta *sce.EventMetadata) {
	p.pendingEventData = meta.Data
	p.pendingEventType = meta.EventType.String()
	p.pendingEventSendid = meta.SendID
	p.pendingEventOrigin = meta.Origin
	p.pendingEventOrigintype = meta.OriginType
	p.pendingEventInvokeid = meta.InvokeID
}

// ClearEventMetadata resets pending event metadata (W3C SCXML 5.10).
func (p *TargetlessTransitionCompletesMacrostepPolicy) ClearEventMetadata() {
	p.pendingEventName = ""
	p.pendingEventData = ""
	p.pendingEventType = ""
	p.pendingEventSendid = ""
	p.pendingEventOrigin = ""
	p.pendingEventOrigintype = ""
	p.pendingEventInvokeid = ""
}




// ExecuteEntryActions executes onentry actions for a state (W3C SCXML 3.8).
//line targetless_transition_completes_macrostep.scxml:51
func (p *TargetlessTransitionCompletesMacrostepPolicy) ExecuteEntryActions(state TargetlessTransitionCompletesMacrostepState, engine *sce.Engine[TargetlessTransitionCompletesMacrostepState, TargetlessTransitionCompletesMacrostepEvent], pathChild *TargetlessTransitionCompletesMacrostepState) {
	// Only a `<parallel>` machine descends into defaults here, so a machine
	// without one has nothing to tell an ancestor entry from a target entry.
	_ = pathChild
	p.ensureScriptEngine()
	switch state {
	case TargetlessTransitionCompletesMacrostepStateRecycled:
		//line targetless_transition_completes_macrostep.scxml:147
		// W3C SCXML 3.8: onentry block 0 (break on error stops subsequent actions)
		for actionBlock0 := 0; actionBlock0 < 1; actionBlock0++ {
			_ = actionBlock0

	// W3C SCXML 5.3: <assign location="entries" expr="entries + 1">
	if err := p.assignVariable(`entries`, `_scxml_add(entries, 1)`); err != nil {
		engine.Raise(sce.NewPlatformError(TargetlessTransitionCompletesMacrostepEventErrorExecution, "<assign> to 'entries' failed"))
		break
	}

		}
	default:
		// No entry actions
	}
}

// ExecuteExitActions executes onexit actions for a state (W3C SCXML 3.9).
//line targetless_transition_completes_macrostep.scxml:51
func (p *TargetlessTransitionCompletesMacrostepPolicy) ExecuteExitActions(state TargetlessTransitionCompletesMacrostepState, engine *sce.Engine[TargetlessTransitionCompletesMacrostepState, TargetlessTransitionCompletesMacrostepEvent], preTransitionActive []TargetlessTransitionCompletesMacrostepState) {
	p.ensureScriptEngine()
	switch state {
	default:
		// No exit actions
	}
}

// ProcessTransition evaluates guards and takes a matching transition (W3C SCXML 3.13).
// Returns true if a transition was taken.
//line targetless_transition_completes_macrostep.scxml:51
func (p *TargetlessTransitionCompletesMacrostepPolicy) ProcessTransition(currentState *TargetlessTransitionCompletesMacrostepState, event TargetlessTransitionCompletesMacrostepEvent, engine *sce.Engine[TargetlessTransitionCompletesMacrostepState, TargetlessTransitionCompletesMacrostepEvent]) bool {
	// W3C SCXML 5.10: Bind _event system variable for guard evaluation
	if event != TargetlessTransitionCompletesMacrostepEventNull {
		p.setCurrentEvent(p.GetEventName(event))
	}

	// W3C SCXML 3.12: Try transitions in current state first
	if p.tryTransitionInState(*currentState, event, currentState, engine) {
		return true
	}


	return false
}


// tryTransitionInState checks transitions for a single state.
//line targetless_transition_completes_macrostep.scxml:51
func (p *TargetlessTransitionCompletesMacrostepPolicy) tryTransitionInState(checkState TargetlessTransitionCompletesMacrostepState, event TargetlessTransitionCompletesMacrostepEvent, currentState *TargetlessTransitionCompletesMacrostepState, engine *sce.Engine[TargetlessTransitionCompletesMacrostepState, TargetlessTransitionCompletesMacrostepEvent]) bool {
	switch checkState {
	case TargetlessTransitionCompletesMacrostepStateIdle:
		// Eventless transition 0
		if event == TargetlessTransitionCompletesMacrostepEventNull {
			if p.evaluateGuard(`_scxml_eq(armed, 1)`, engine) {
			*currentState = TargetlessTransitionCompletesMacrostepStateSettled
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = TargetlessTransitionCompletesMacrostepStateIdle
			p.lastTransitionIndex = 0
			p.hasTransitionActions = true
			return true
			}
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == TargetlessTransitionCompletesMacrostepEventQuiet {
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = true
			p.lastTransitionSourceState = TargetlessTransitionCompletesMacrostepStateIdle
			p.lastTransitionIndex = 1
			p.hasTransitionActions = true
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == TargetlessTransitionCompletesMacrostepEventArm {
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = true
			p.lastTransitionSourceState = TargetlessTransitionCompletesMacrostepStateIdle
			p.lastTransitionIndex = 2
			p.hasTransitionActions = true
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == TargetlessTransitionCompletesMacrostepEventPing {
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = true
			p.lastTransitionSourceState = TargetlessTransitionCompletesMacrostepStateIdle
			p.lastTransitionIndex = 3
			p.hasTransitionActions = true
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == TargetlessTransitionCompletesMacrostepEventPong {
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = true
			p.lastTransitionSourceState = TargetlessTransitionCompletesMacrostepStateIdle
			p.lastTransitionIndex = 4
			p.hasTransitionActions = true
			return true
		}
		// W3C SCXML 5.9.3: Direct enum comparison
		if event == TargetlessTransitionCompletesMacrostepEventRecycle {
			*currentState = TargetlessTransitionCompletesMacrostepStateRecycled
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = TargetlessTransitionCompletesMacrostepStateIdle
			p.lastTransitionIndex = 5
			p.hasTransitionActions = false
			return true
		}
	case TargetlessTransitionCompletesMacrostepStateRecycled:
		// Eventless transition 0
		if event == TargetlessTransitionCompletesMacrostepEventNull {
			if p.evaluateGuard(`(entries < 2)`, engine) {
			*currentState = TargetlessTransitionCompletesMacrostepStateRecycled
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = false
			p.lastTransitionSourceState = TargetlessTransitionCompletesMacrostepStateRecycled
			p.lastTransitionIndex = 0
			p.hasTransitionActions = false
			return true
			}
		}
	case TargetlessTransitionCompletesMacrostepStateSettled:
		// Eventless transition 0
		if event == TargetlessTransitionCompletesMacrostepEventNull {
			if p.evaluateGuard(`_scxml_eq(polished, 0)`, engine) {
			p.lastTransitionIsInternal = false
			p.lastTransitionIsTargetless = true
			p.lastTransitionSourceState = TargetlessTransitionCompletesMacrostepStateSettled
			p.lastTransitionIndex = 0
			p.hasTransitionActions = true
			return true
			}
		}
	}
	return false
}

// ExecuteTransitionActions executes actions for the last taken transition (W3C SCXML 3.13).
//line targetless_transition_completes_macrostep.scxml:51
func (p *TargetlessTransitionCompletesMacrostepPolicy) ExecuteTransitionActions(engine *sce.Engine[TargetlessTransitionCompletesMacrostepState, TargetlessTransitionCompletesMacrostepEvent]) {
	p.ensureScriptEngine()
	if !p.hasTransitionActions {
		return
	}
	source := p.lastTransitionSourceState
	idx := p.lastTransitionIndex
	if source == TargetlessTransitionCompletesMacrostepStateIdle && idx == 0 {
		//line targetless_transition_completes_macrostep.scxml:86

	// W3C SCXML 5.3: <assign location="chained" expr="chained + 1">
	if err := p.assignVariable(`chained`, `_scxml_add(chained, 1)`); err != nil {
		engine.Raise(sce.NewPlatformError(TargetlessTransitionCompletesMacrostepEventErrorExecution, "<assign> to 'chained' failed"))
	}

		return
	}
	if source == TargetlessTransitionCompletesMacrostepStateIdle && idx == 1 {
		//line targetless_transition_completes_macrostep.scxml:93

	// W3C SCXML 5.3: <assign location="quiet" expr="quiet + 1">
	if err := p.assignVariable(`quiet`, `_scxml_add(quiet, 1)`); err != nil {
		engine.Raise(sce.NewPlatformError(TargetlessTransitionCompletesMacrostepEventErrorExecution, "<assign> to 'quiet' failed"))
	}

		return
	}
	if source == TargetlessTransitionCompletesMacrostepStateIdle && idx == 2 {
		//line targetless_transition_completes_macrostep.scxml:100

	// W3C SCXML 5.3: <assign location="armed" expr="1">
	if err := p.assignVariable(`armed`, `1`); err != nil {
		engine.Raise(sce.NewPlatformError(TargetlessTransitionCompletesMacrostepEventErrorExecution, "<assign> to 'armed' failed"))
	}

		return
	}
	if source == TargetlessTransitionCompletesMacrostepStateIdle && idx == 3 {
		//line targetless_transition_completes_macrostep.scxml:107

	engine.Raise(sce.NewEventWithMetadata(TargetlessTransitionCompletesMacrostepEventPong))

		return
	}
	if source == TargetlessTransitionCompletesMacrostepStateIdle && idx == 4 {
		//line targetless_transition_completes_macrostep.scxml:111

	// W3C SCXML 5.3: <assign location="answered" expr="answered + 1">
	if err := p.assignVariable(`answered`, `_scxml_add(answered, 1)`); err != nil {
		engine.Raise(sce.NewPlatformError(TargetlessTransitionCompletesMacrostepEventErrorExecution, "<assign> to 'answered' failed"))
	}

		return
	}
	if source == TargetlessTransitionCompletesMacrostepStateSettled && idx == 0 {
		//line targetless_transition_completes_macrostep.scxml:126

	// W3C SCXML 5.3: <assign location="polished" expr="polished + 1">
	if err := p.assignVariable(`polished`, `_scxml_add(polished, 1)`); err != nil {
		engine.Raise(sce.NewPlatformError(TargetlessTransitionCompletesMacrostepEventErrorExecution, "<assign> to 'polished' failed"))
	}

		return
	}
}
