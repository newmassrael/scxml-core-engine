// SCE-GENERATED — DO NOT EDIT
// source-hash: 9cf4fd5f626a0b8e891563a233492fcdd47cb02fca615778881ec79fcd0199e5


// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) 2025-2026 [Author of input SCXML file]
//
// Generated by SCE Code Generator (Go backend)
// From: integration_resources/parallel_regions_take_own_transitions/parallel_regions_take_own_transitions.scxml
//
// This generated code is MIT licensed and may be freely used in any project.
// Runtime engine dependency (sce-go-runtime): LGPL-2.1-or-later WITH LicenseRef-SCE-Linking-Exception OR LicenseRef-SCE-Commercial
// Full license: https://github.com/newmassrael/scxml-core-engine/blob/main/LICENSE
// Generated Go state machine for `parallel_regions_take_own_transitions`.
//
// Generated by SCE Code Generator (Go backend).
// Implements sce.StatePolicy with full transition logic,
// entry/exit actions, and event processing.


// SCE-MAP: parallel_regions_take_own_transitions.scxml:24 :: _machine

package parallel_regions_take_own_transitions

import (
	"fmt"
	"path/filepath"
	"strings"
	"time"

	sce "github.com/newmassrael/sce-go-runtime"
)

// Ensure imports are used
var (
	_ = fmt.Sprintf
	_ = filepath.Join
	_ = strings.HasPrefix
	_ = time.Duration(0)
)

// ======================================================================
// State type (W3C SCXML 3.3)
// ======================================================================

type ParallelRegionsTakeOwnTransitionsState int

const (
	ParallelRegionsTakeOwnTransitionsStateBudget ParallelRegionsTakeOwnTransitionsState = 0
	ParallelRegionsTakeOwnTransitionsStateDrive ParallelRegionsTakeOwnTransitionsState = 1
	ParallelRegionsTakeOwnTransitionsStateJudging ParallelRegionsTakeOwnTransitionsState = 2
	ParallelRegionsTakeOwnTransitionsStateRun ParallelRegionsTakeOwnTransitionsState = 3
	ParallelRegionsTakeOwnTransitionsStateRunning ParallelRegionsTakeOwnTransitionsState = 4
	ParallelRegionsTakeOwnTransitionsStateSettled ParallelRegionsTakeOwnTransitionsState = 5
	ParallelRegionsTakeOwnTransitionsStateWithin ParallelRegionsTakeOwnTransitionsState = 6
	ParallelRegionsTakeOwnTransitionsStateWorking ParallelRegionsTakeOwnTransitionsState = 7
)

func (s ParallelRegionsTakeOwnTransitionsState) String() string {
	switch s {
	case ParallelRegionsTakeOwnTransitionsStateBudget:
		return "budget"
	case ParallelRegionsTakeOwnTransitionsStateDrive:
		return "drive"
	case ParallelRegionsTakeOwnTransitionsStateJudging:
		return "judging"
	case ParallelRegionsTakeOwnTransitionsStateRun:
		return "run"
	case ParallelRegionsTakeOwnTransitionsStateRunning:
		return "running"
	case ParallelRegionsTakeOwnTransitionsStateSettled:
		return "settled"
	case ParallelRegionsTakeOwnTransitionsStateWithin:
		return "within"
	case ParallelRegionsTakeOwnTransitionsStateWorking:
		return "working"
	}
	return "unknown"
}

// ParallelRegionsTakeOwnTransitionsStateFromName is the read half of the pair above — a state
// id back into the state it names (W3C SCXML 3.3).
//
// A host that persists where a machine was has to write it down as TEXT: the
// constants above are a build artefact of one binary, and the process that
// resumes is a different one. String publishes the name and this reads it back,
// which is what lets a journal survive its own record and reach
// sce.Engine.EnterAt.
//
// The second return is false for a name this document does not declare. A name
// guessed at rather than refused is how a restore reaches a configuration
// nobody recorded — and the refusal is what makes a typo in a journal a
// reported failure instead of a machine quietly somewhere else.
//
// Emitted from the same loop over the document's states as String, so the two
// age together.
func ParallelRegionsTakeOwnTransitionsStateFromName(name string) (ParallelRegionsTakeOwnTransitionsState, bool) {
	switch name {
	case "budget":
		return ParallelRegionsTakeOwnTransitionsStateBudget, true
	case "drive":
		return ParallelRegionsTakeOwnTransitionsStateDrive, true
	case "judging":
		return ParallelRegionsTakeOwnTransitionsStateJudging, true
	case "run":
		return ParallelRegionsTakeOwnTransitionsStateRun, true
	case "running":
		return ParallelRegionsTakeOwnTransitionsStateRunning, true
	case "settled":
		return ParallelRegionsTakeOwnTransitionsStateSettled, true
	case "within":
		return ParallelRegionsTakeOwnTransitionsStateWithin, true
	case "working":
		return ParallelRegionsTakeOwnTransitionsStateWorking, true
	}
	var zero ParallelRegionsTakeOwnTransitionsState
	return zero, false
}

// ParallelRegionsTakeOwnTransitionsAllStates is every state this document declares, in the
// order the constants above are issued.
//
// Emitted from the same loop, so a walk over it is a walk over the document
// rather than over a list somebody maintained beside it — a test that spells
// its own list goes on passing when the document grows a state.
var ParallelRegionsTakeOwnTransitionsAllStates = []ParallelRegionsTakeOwnTransitionsState{
	ParallelRegionsTakeOwnTransitionsStateBudget,
	ParallelRegionsTakeOwnTransitionsStateDrive,
	ParallelRegionsTakeOwnTransitionsStateJudging,
	ParallelRegionsTakeOwnTransitionsStateRun,
	ParallelRegionsTakeOwnTransitionsStateRunning,
	ParallelRegionsTakeOwnTransitionsStateSettled,
	ParallelRegionsTakeOwnTransitionsStateWithin,
	ParallelRegionsTakeOwnTransitionsStateWorking,
}

// ParallelRegionsTakeOwnTransitionsTarget is one token of a target list, as the document wrote
// it (W3C SCXML 3.13): a state, or a <history> the engine dereferences.
type ParallelRegionsTakeOwnTransitionsTarget = sce.EntryTarget[ParallelRegionsTakeOwnTransitionsState, sce.HistoryID]

// ======================================================================
// Document structure (W3C SCXML 3.2-3.4, 3.10)
//
// Package-level tables, because the structure is a fact about the document
// and not about a run: the engine's Appendix D procedures read them through
// the policy methods below, and a transition's target list is handed out as
// a slice of them rather than rebuilt on every selection.
// ======================================================================

// childStatesOfParallelRegionsTakeOwnTransitions is §scxml-D-getChildStates per state: its
// <state>, <parallel> and <final> children, in document order.
var childStatesOfParallelRegionsTakeOwnTransitions = [8][]ParallelRegionsTakeOwnTransitionsState{
	ParallelRegionsTakeOwnTransitionsStateBudget: {ParallelRegionsTakeOwnTransitionsStateWithin},
	ParallelRegionsTakeOwnTransitionsStateDrive: {ParallelRegionsTakeOwnTransitionsStateRunning},
	ParallelRegionsTakeOwnTransitionsStateRun: {ParallelRegionsTakeOwnTransitionsStateDrive, ParallelRegionsTakeOwnTransitionsStateBudget},
	ParallelRegionsTakeOwnTransitionsStateRunning: {ParallelRegionsTakeOwnTransitionsStateWorking, ParallelRegionsTakeOwnTransitionsStateJudging},
}

// initialTargetsOfParallelRegionsTakeOwnTransitions is each compound state's initial transition
// target, as written (§scxml-3.3).
var initialTargetsOfParallelRegionsTakeOwnTransitions = [8][]ParallelRegionsTakeOwnTransitionsTarget{
	ParallelRegionsTakeOwnTransitionsStateBudget: {sce.StateTarget[ParallelRegionsTakeOwnTransitionsState, sce.HistoryID](ParallelRegionsTakeOwnTransitionsStateWithin)},
	ParallelRegionsTakeOwnTransitionsStateDrive: {sce.StateTarget[ParallelRegionsTakeOwnTransitionsState, sce.HistoryID](ParallelRegionsTakeOwnTransitionsStateRunning)},
	ParallelRegionsTakeOwnTransitionsStateRunning: {sce.StateTarget[ParallelRegionsTakeOwnTransitionsState, sce.HistoryID](ParallelRegionsTakeOwnTransitionsStateWorking)},
}

// documentInitialTargetsOfParallelRegionsTakeOwnTransitions is the target of the document's own
// initial transition, as written (§scxml-3.2).
var documentInitialTargetsOfParallelRegionsTakeOwnTransitions = []ParallelRegionsTakeOwnTransitionsTarget{sce.StateTarget[ParallelRegionsTakeOwnTransitionsState, sce.HistoryID](ParallelRegionsTakeOwnTransitionsStateRun)}

// transitionTargetsOfParallelRegionsTakeOwnTransitions is each transition's target list, as
// written (§scxml-3.13), by source state and the transition's index among its
// source's own transitions. A targetless transition's entry is empty.
var transitionTargetsOfParallelRegionsTakeOwnTransitions = [8][][]ParallelRegionsTakeOwnTransitionsTarget{
	ParallelRegionsTakeOwnTransitionsStateJudging: {
		0: {sce.StateTarget[ParallelRegionsTakeOwnTransitionsState, sce.HistoryID](ParallelRegionsTakeOwnTransitionsStateSettled)},
	},
	ParallelRegionsTakeOwnTransitionsStateWithin: {
		0: {sce.StateTarget[ParallelRegionsTakeOwnTransitionsState, sce.HistoryID](ParallelRegionsTakeOwnTransitionsStateWithin)},
	},
	ParallelRegionsTakeOwnTransitionsStateWorking: {
		0: {sce.StateTarget[ParallelRegionsTakeOwnTransitionsState, sce.HistoryID](ParallelRegionsTakeOwnTransitionsStateJudging)},
	},
}

// ======================================================================
// Event type (W3C SCXML 3.12)
// ======================================================================

type ParallelRegionsTakeOwnTransitionsEvent int

const (
	ParallelRegionsTakeOwnTransitionsEventCheck ParallelRegionsTakeOwnTransitionsEvent = 0
	ParallelRegionsTakeOwnTransitionsEventE ParallelRegionsTakeOwnTransitionsEvent = 1
	ParallelRegionsTakeOwnTransitionsEventErrorExecution ParallelRegionsTakeOwnTransitionsEvent = 2
	// W3C SCXML 3.13: Sentinel for eventless transition dispatch
	ParallelRegionsTakeOwnTransitionsEventNull ParallelRegionsTakeOwnTransitionsEvent = 3
)

func (e ParallelRegionsTakeOwnTransitionsEvent) String() string {
	switch e {
	case ParallelRegionsTakeOwnTransitionsEventCheck:
		return "check"
	case ParallelRegionsTakeOwnTransitionsEventE:
		return "e"
	case ParallelRegionsTakeOwnTransitionsEventErrorExecution:
		return "error.execution"
	case ParallelRegionsTakeOwnTransitionsEventNull:
		return ""
	}
	return "unknown"
}

// ======================================================================
// Policy struct
// ======================================================================

type ParallelRegionsTakeOwnTransitionsPolicy struct {
	// W3C SCXML 3.4: Active state configuration for parallel states / In() predicate
	activeStates []ParallelRegionsTakeOwnTransitionsState
	// W3C SCXML 5.10.1: External event flag
	nextEventIsExternal bool
	pendingEventName string
	pendingEventData string
	pendingEventType string
	pendingEventSendid string
	pendingEventOrigin string
	pendingEventOrigintype string
	pendingEventInvokeid string
	// W3C SCXML 5.10: Session ID
	SessionID string
	// W3C SCXML B.1: Script engine handle (per-instance, Path B+ Q1=(d) Go=interface ref)
	ScriptEngine sce.IScriptEngine
	scriptEngineInitialized bool
	// W3C SCXML C.2.3: inbound BasicHTTP endpoint serving this machine,
	// declared by the deployment before Initialize(). The address belongs to
	// whoever runs the listener, so the machine takes it from here rather than
	// guessing one. Empty means no such endpoint is deployed, and no BasicHTTP
	// entry is published in _ioprocessors.
	BasicHTTPAccessURI string
	inPredicateStates []string
	// W3C SCXML 6.4: Parent communication
	ParentExternalQueue chan sce.ParentEvent
	InvokeID           string
	ChildSessionID     string
	// §scxml-6.4.3: invoke param VALUES staged by the parent, applied after
	// this machine's datamodel init so they override its `<data>` defaults.
	pendingParams      map[string]interface{}
}

// NewParallelRegionsTakeOwnTransitionsPolicy creates a new policy with default values.
func NewParallelRegionsTakeOwnTransitionsPolicy() ParallelRegionsTakeOwnTransitionsPolicy {
	return ParallelRegionsTakeOwnTransitionsPolicy{
		activeStates: make([]ParallelRegionsTakeOwnTransitionsState, 0),
	}
}

// N reports what the `n` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `n` was assigned a value of another type, or the engine refused.
func (p *ParallelRegionsTakeOwnTransitionsPolicy) N() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "n")
}

// M reports what the `m` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `m` was assigned a value of another type, or the engine refused.
func (p *ParallelRegionsTakeOwnTransitionsPolicy) M() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "m")
}



// IsStateActive checks if a state is active by name (W3C SCXML 5.9.2: In() predicate).
func (p *ParallelRegionsTakeOwnTransitionsPolicy) IsStateActive(stateID string) bool {
	for _, s := range p.activeStates {
		if p.GetStateName(s) == stateID {
			return true
		}
	}
	return false
}


// ensureScriptEngine lazily initializes the script engine session.
//
// Engine DI Parity RFC: panics if ScriptEngine was not set on the policy
// before Initialize() — mirrors Kotlin's mandatory constructor parameter
// contract. Silent no-op would otherwise turn missed injection into a
// hidden runtime regression (every <assign>/<log>/guard becomes a no-op
// and the SM walks to its default-state final without diagnostic).
func (p *ParallelRegionsTakeOwnTransitionsPolicy) ensureScriptEngine() {
	if p.scriptEngineInitialized {
		return
	}
	engine := p.ScriptEngine
	if engine == nil {
		panic("ParallelRegionsTakeOwnTransitionsPolicy: ScriptEngine field must be set before Initialize()")
	}
	sessionID := p.SessionID
	if sessionID == "" {
		sessionID = sce.GenerateSessionID()
		p.SessionID = sessionID
	}
	if !engine.HasSession(sessionID) {
		_ = engine.CreateSession(sessionID)
	}
	// W3C SCXML C.1.1 / C.2.3: the _ioprocessors entries come from the same
	// helper every other backend uses, so a machine reads the same entry names
	// and the same addresses whichever one runs it.
	_ = engine.SetupSystemVariables(sessionID, "parallel_regions_take_own_transitions",
		sce.BuildIoProcessors(sessionID, p.BasicHTTPAccessURI))
	// W3C SCXML 5.9.2: Register In() predicate callback
	engine.SetStateQueryCallback(sessionID, func(stateID string) bool {
		return p.IsStateActive(stateID)
	})
	p.scriptEngineInitialized = true
}


// InitializeDataModel sets up datamodel via script engine (W3C SCXML 5.3).
func (p *ParallelRegionsTakeOwnTransitionsPolicy) InitializeDataModel(eng *sce.Engine[ParallelRegionsTakeOwnTransitionsState, ParallelRegionsTakeOwnTransitionsEvent]) {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	sessionID := p.SessionID

	// W3C SCXML 5.3: Initialize _sessionid
	_ = engine.SetVariable(sessionID, "_sessionid", sessionID)

	// W3C SCXML 5.3: Initialize _name
	_ = engine.SetVariable(sessionID, "_name", "parallel_regions_take_own_transitions")

	// W3C SCXML 5.2.2: Initialize global datamodel variables
	// W3C SCXML 5.2/5.3: Initialize n from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "n", result)
		} else {
			eng.Raise(sce.NewPlatformError(ParallelRegionsTakeOwnTransitionsEventErrorExecution, "<data id='n'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "n", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize m from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "m", result)
		} else {
			eng.Raise(sce.NewPlatformError(ParallelRegionsTakeOwnTransitionsEventErrorExecution, "<data id='m'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "m", nil)
		}
	}



	// §scxml-6.4.3: apply the invoke params the parent staged — overriding the
	// `<data>` default this block just seeded, and only for a name the child
	// declares ("if the names do not match, the Processor MUST NOT add the
	// value ... to the invoked session's data model"). `HasVariable` is that
	// filter, and it is this channel's only copy of the rule.
	if len(p.pendingParams) > 0 {
		for paramName, paramValue := range p.pendingParams {
			if engine.HasVariable(sessionID, paramName) {
				_ = engine.SetVariable(sessionID, paramName, paramValue)
			}
		}
		p.pendingParams = nil
	}
}

// evaluateGuard evaluates a guard condition via script engine.
// W3C SCXML 4.3.2: If a condition expression cannot be evaluated, the processor
// MUST treat it as false and MUST raise error.execution.
func (p *ParallelRegionsTakeOwnTransitionsPolicy) evaluateGuard(guard string, eng *sce.Engine[ParallelRegionsTakeOwnTransitionsState, ParallelRegionsTakeOwnTransitionsEvent]) bool {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	result, err := engine.EvaluateExpression(p.SessionID, guard)
	if err != nil {
		eng.Raise(sce.NewPlatformError(ParallelRegionsTakeOwnTransitionsEventErrorExecution, "a <transition> cond failed to evaluate"))
		return false
	}
	return sce.ScriptToBool(result)
}

// executeScript executes a script block via script engine.
func (p *ParallelRegionsTakeOwnTransitionsPolicy) executeScript(script string) error {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	return engine.ExecuteScript(p.SessionID, script)
}

// setCurrentEvent binds _event system variable (W3C SCXML 5.10) and answers
// which rung of §scxml-B-2-8-1 the payload got, because the binding is the
// only place that knows.
//
// This used to be `_ = engine.SetCurrentEvent(...)`: the ladder decided
// between a DOM, a value and a space-normalized string, and the decision was
// dropped one line later. A payload that announced structure and would not
// parse therefore reached the document as raw characters, every
// _event.data.<field> read empty, and nothing anywhere could say so — see
// sce.PayloadReading.
func (p *ParallelRegionsTakeOwnTransitionsPolicy) setCurrentEvent(name string) sce.PayloadReading {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	data := p.pendingEventData
	eventType := p.pendingEventType
	sendID := p.pendingEventSendid
	// W3C SCXML C.1: `_event.origin` is the sender's published
	// `_ioprocessors` location, not its bare session id — and this is the one
	// place that publishes `_event` to the document, so this is where the id
	// becomes a location. The engine keeps the bare id in
	// `EventMetadata.Origin` because its session-keyed lookups (<finalize>
	// dispatch, cancelled-invoke filtering) match on it; converting at the
	// raise would make one value serve two consumers that need different
	// spellings. The conversion itself lives in `sce.PublishedOrigin`, the
	// port of the `IOProcessorHelper::publishedOrigin` the C++ engines share:
	// a second spelling of the rule is how the backends would stop agreeing.
	origin := sce.PublishedOrigin(p.pendingEventOrigin)
	originType := p.pendingEventOrigintype
	invokeID := p.pendingEventInvokeid
	reading, err := engine.SetCurrentEvent(p.SessionID, sce.SetCurrentEventArgs{
		Name:       name,
		Data:       data,
		Type:       eventType,
		SendID:     sendID,
		Origin:     origin,
		OriginType: originType,
		InvokeID:   invokeID,
	})
	if err != nil {
		// An engine that could not bind _event at all has a larger problem
		// than an unreadable payload, and reporting PayloadUndecodable for it
		// would point a host at the wrong thing.
		return sce.PayloadAbsent
	}
	return reading
}

// assignVariable assigns a value to a datamodel variable via script engine.
func (p *ParallelRegionsTakeOwnTransitionsPolicy) assignVariable(location, expr string) error {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	script := location + " = " + expr
	return engine.ExecuteScript(p.SessionID, script)
}

// getVariable reads a datamodel variable from the script engine.
func (p *ParallelRegionsTakeOwnTransitionsPolicy) getVariable(name string) interface{} {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	result, err := engine.GetVariable(p.SessionID, name)
	if err != nil {
		return nil
	}
	return result
}

// SetParamValueInScriptEngine stores an invoke param's VALUE for deferred
// application after datamodel init (§scxml-6.4.3).
//
// Params are applied at the end of InitializeDataModel, overriding the
// `<data>` default only for names the child declares. The value crosses as a
// value: this took the parent's value rendered as Lua source and
// re-evaluated it here until 2026-08-21, which survived every value a test
// had tried and lost the ones Lua cannot spell — `ToScriptLiteral` renders a
// non-finite number as `+Inf`, which is not a Lua expression, so an
// `<invoke>` `<param expr="1/0"/>` arrived as nothing at all.
func (p *ParallelRegionsTakeOwnTransitionsPolicy) SetParamValueInScriptEngine(name string, value interface{}) {
	if p.pendingParams == nil {
		p.pendingParams = make(map[string]interface{})
	}
	p.pendingParams[name] = value
}


// ExecutePendingInvokes is a no-op (no invokes in this SM).
func (p *ParallelRegionsTakeOwnTransitionsPolicy) ExecutePendingInvokes(engine *sce.Engine[ParallelRegionsTakeOwnTransitionsState, ParallelRegionsTakeOwnTransitionsEvent]) {}

// TickChildren is a no-op (no invokes in this SM).
func (p *ParallelRegionsTakeOwnTransitionsPolicy) TickChildren(engine *sce.Engine[ParallelRegionsTakeOwnTransitionsState, ParallelRegionsTakeOwnTransitionsEvent]) {}

// ======================================================================
// StatePolicy interface implementation
// ======================================================================

// InitialState returns the initial state (W3C SCXML 3.3).
func (p *ParallelRegionsTakeOwnTransitionsPolicy) InitialState() ParallelRegionsTakeOwnTransitionsState {
	return ParallelRegionsTakeOwnTransitionsStateRun
}

// IsFinalState returns true if state is a <final> state (W3C SCXML 3.7).
func (p *ParallelRegionsTakeOwnTransitionsPolicy) IsFinalState(state ParallelRegionsTakeOwnTransitionsState) bool {
	switch state {
	case ParallelRegionsTakeOwnTransitionsStateSettled:
		return true
	}
	return false
}

// GetParent returns the parent of state, or (zero, false) if root (W3C SCXML 3.3).
func (p *ParallelRegionsTakeOwnTransitionsPolicy) GetParent(state ParallelRegionsTakeOwnTransitionsState) (ParallelRegionsTakeOwnTransitionsState, bool) {
	switch state {
	case ParallelRegionsTakeOwnTransitionsStateBudget:
		return ParallelRegionsTakeOwnTransitionsStateRun, true
	case ParallelRegionsTakeOwnTransitionsStateDrive:
		return ParallelRegionsTakeOwnTransitionsStateRun, true
	case ParallelRegionsTakeOwnTransitionsStateJudging:
		return ParallelRegionsTakeOwnTransitionsStateRunning, true
	case ParallelRegionsTakeOwnTransitionsStateRunning:
		return ParallelRegionsTakeOwnTransitionsStateDrive, true
	case ParallelRegionsTakeOwnTransitionsStateWithin:
		return ParallelRegionsTakeOwnTransitionsStateBudget, true
	case ParallelRegionsTakeOwnTransitionsStateWorking:
		return ParallelRegionsTakeOwnTransitionsStateRunning, true
	}
	return 0, false
}

// IsCompoundState returns true if state is a <state> with child states — exactly
// the states that have an initial transition. A <parallel> is not compound
// (W3C SCXML 3.3).
func (p *ParallelRegionsTakeOwnTransitionsPolicy) IsCompoundState(state ParallelRegionsTakeOwnTransitionsState) bool {
	return len(initialTargetsOfParallelRegionsTakeOwnTransitions[state]) > 0
}

// IsParallelState returns true if state is a <parallel> state (W3C SCXML 3.4).
func (p *ParallelRegionsTakeOwnTransitionsPolicy) IsParallelState(state ParallelRegionsTakeOwnTransitionsState) bool {
	switch state {
	case ParallelRegionsTakeOwnTransitionsStateRun:
		return true
	}
	return false
}


// GetChildStates returns state's <state>, <parallel> and <final> children, in
// document order — for a <parallel>, its regions (§scxml-D-getChildStates).
func (p *ParallelRegionsTakeOwnTransitionsPolicy) GetChildStates(state ParallelRegionsTakeOwnTransitionsState) []ParallelRegionsTakeOwnTransitionsState {
	return childStatesOfParallelRegionsTakeOwnTransitions[state]
}

// GetInitialTargets returns a compound state's initial transition target, as
// written; the engine's entry procedures dereference a <history> among them
// (W3C SCXML 3.3).
func (p *ParallelRegionsTakeOwnTransitionsPolicy) GetInitialTargets(state ParallelRegionsTakeOwnTransitionsState) []ParallelRegionsTakeOwnTransitionsTarget {
	return initialTargetsOfParallelRegionsTakeOwnTransitions[state]
}

// GetDocumentInitialTargets returns the target of the document's own initial
// transition, as written (W3C SCXML 3.2).
func (p *ParallelRegionsTakeOwnTransitionsPolicy) GetDocumentInitialTargets() []ParallelRegionsTakeOwnTransitionsTarget {
	return documentInitialTargetsOfParallelRegionsTakeOwnTransitions
}

// W3C SCXML 3.10: this document declares no <history>, so no target list names
// one and the engine never asks the two below; answering would mean inventing
// one.
func (p *ParallelRegionsTakeOwnTransitionsPolicy) GetHistoryParent(history sce.HistoryID) ParallelRegionsTakeOwnTransitionsState {
	panic(fmt.Sprintf("ParallelRegionsTakeOwnTransitionsPolicy declares no <history>; asked for %d", history))
}
func (p *ParallelRegionsTakeOwnTransitionsPolicy) GetHistoryDefaultTargets(history sce.HistoryID) []ParallelRegionsTakeOwnTransitionsTarget {
	panic(fmt.Sprintf("ParallelRegionsTakeOwnTransitionsPolicy declares no <history>; asked for %d", history))
}
func (p *ParallelRegionsTakeOwnTransitionsPolicy) HistoryValue(_ sce.HistoryID) ([]ParallelRegionsTakeOwnTransitionsState, bool) {
	return nil, false
}

// GetDocumentOrder returns the document order index (W3C SCXML Appendix D).
func (p *ParallelRegionsTakeOwnTransitionsPolicy) GetDocumentOrder(state ParallelRegionsTakeOwnTransitionsState) int {
	switch state {
	case ParallelRegionsTakeOwnTransitionsStateBudget:
		return 5
	case ParallelRegionsTakeOwnTransitionsStateDrive:
		return 1
	case ParallelRegionsTakeOwnTransitionsStateJudging:
		return 4
	case ParallelRegionsTakeOwnTransitionsStateRun:
		return 0
	case ParallelRegionsTakeOwnTransitionsStateRunning:
		return 2
	case ParallelRegionsTakeOwnTransitionsStateSettled:
		return 7
	case ParallelRegionsTakeOwnTransitionsStateWithin:
		return 6
	case ParallelRegionsTakeOwnTransitionsStateWorking:
		return 3
	}
	return -1
}

// GetEventName returns the string name of an event (W3C SCXML 3.12).
func (p *ParallelRegionsTakeOwnTransitionsPolicy) GetEventName(event ParallelRegionsTakeOwnTransitionsEvent) string {
	return event.String()
}

// GetEventFromName looks up an event by name (W3C SCXML 3.12).
func (p *ParallelRegionsTakeOwnTransitionsPolicy) GetEventFromName(name string) (ParallelRegionsTakeOwnTransitionsEvent, bool) {
	switch name {
	case "check":
		return ParallelRegionsTakeOwnTransitionsEventCheck, true
	case "e":
		return ParallelRegionsTakeOwnTransitionsEventE, true
	case "error.execution":
		return ParallelRegionsTakeOwnTransitionsEventErrorExecution, true
	}
	return ParallelRegionsTakeOwnTransitionsEventNull, false
}

// GetStateName returns the string name of a state (W3C SCXML 3.3).
func (p *ParallelRegionsTakeOwnTransitionsPolicy) GetStateName(state ParallelRegionsTakeOwnTransitionsState) string {
	return state.String()
}

// GetStateFromName reads a state id back into the state it names (W3C SCXML 3.3).
// The reverse of GetStateName, and what turns a host's recorded configuration
// back into the argument sce.Engine.EnterAt takes.
func (p *ParallelRegionsTakeOwnTransitionsPolicy) GetStateFromName(name string) (ParallelRegionsTakeOwnTransitionsState, bool) {
	return ParallelRegionsTakeOwnTransitionsStateFromName(name)
}

// NullEvent returns the sentinel for eventless transition dispatch (W3C SCXML 3.13).
func (p *ParallelRegionsTakeOwnTransitionsPolicy) NullEvent() ParallelRegionsTakeOwnTransitionsEvent {
	return ParallelRegionsTakeOwnTransitionsEventNull
}

// GetActiveStates returns the active state configuration (W3C SCXML 3.4).
func (p *ParallelRegionsTakeOwnTransitionsPolicy) GetActiveStates() []ParallelRegionsTakeOwnTransitionsState {
	return p.activeStates
}

// SetActiveStates hands this machine an active set whole (W3C SCXML 3.4).
// The write half of GetActiveStates: entry and exit grow the set one state at a
// time as they walk, and sce.Engine.EnterAt hands back a configuration an
// earlier run already settled on. Copied rather than aliased so a caller's slice
// cannot go on changing this machine's configuration behind it.
func (p *ParallelRegionsTakeOwnTransitionsPolicy) SetActiveStates(states []ParallelRegionsTakeOwnTransitionsState) {
	p.activeStates = append(p.activeStates[:0:0], states...)
}

// SetNextEventIsExternal sets the external event flag (W3C SCXML 5.10.1).
func (p *ParallelRegionsTakeOwnTransitionsPolicy) SetNextEventIsExternal(value bool) {
	p.nextEventIsExternal = value
}

// HasParallelStates returns whether the SM has parallel states.
func (p *ParallelRegionsTakeOwnTransitionsPolicy) HasParallelStates() bool {
	return true
}

// NeedsScriptEngine returns whether the SM needs a script engine.
func (p *ParallelRegionsTakeOwnTransitionsPolicy) NeedsScriptEngine() bool {
	return true
}

// NeedsEventScheduler reports whether a host must drive this machine with
// Tick rather than Step alone.
//
// W3C SCXML 6.2: Step runs a macrostep and never drains the delayed-send
// scheduler nor ticks an invoked child, so a host that only ever calls it
// waits forever with nothing said. Same verdict the generate manifest
// publishes as needs_event_scheduler.
func (p *ParallelRegionsTakeOwnTransitionsPolicy) NeedsEventScheduler() bool {
	return false
}

// NeedsDataModelInit returns whether the SM needs datamodel init.
func (p *ParallelRegionsTakeOwnTransitionsPolicy) NeedsDataModelInit() bool {
	return true
}

func (p *ParallelRegionsTakeOwnTransitionsPolicy) HasInvokeSupport() bool { return false }
func (p *ParallelRegionsTakeOwnTransitionsPolicy) HasChildTick() bool { return false }
func (p *ParallelRegionsTakeOwnTransitionsPolicy) HasFinalize() bool { return false }
func (p *ParallelRegionsTakeOwnTransitionsPolicy) HasAutoforward() bool { return false }
func (p *ParallelRegionsTakeOwnTransitionsPolicy) HasActiveStates() bool { return true }
func (p *ParallelRegionsTakeOwnTransitionsPolicy) HasExternalEventFlag() bool { return true }
// ExecuteFinalizeForChildEvent is a no-op (no finalize invokes).
func (p *ParallelRegionsTakeOwnTransitionsPolicy) ExecuteFinalizeForChildEvent(_ *sce.EventWithMetadata[ParallelRegionsTakeOwnTransitionsEvent], _ *sce.Engine[ParallelRegionsTakeOwnTransitionsState, ParallelRegionsTakeOwnTransitionsEvent]) {}
// ForwardToAutoforwardChildren is a no-op (no autoforward invokes).
func (p *ParallelRegionsTakeOwnTransitionsPolicy) ForwardToAutoforwardChildren(_ string, _ sce.EventMetadata, _ *sce.Engine[ParallelRegionsTakeOwnTransitionsState, ParallelRegionsTakeOwnTransitionsEvent]) {}

// PopulateEventMetadata stores pending event metadata (W3C SCXML 5.10).
// Note: event name is set separately via setCurrentEvent(), not from metadata.
func (p *ParallelRegionsTakeOwnTransitionsPolicy) PopulateEventMetadata(meta *sce.EventMetadata) {
	p.pendingEventData = meta.Data
	p.pendingEventType = meta.EventType.String()
	p.pendingEventSendid = meta.SendID
	p.pendingEventOrigin = meta.Origin
	p.pendingEventOrigintype = meta.OriginType
	p.pendingEventInvokeid = meta.InvokeID
}

// LiftTypedPayload binds the dequeued event's typed `_event.data` view from
// the data it carries (NL→IR Item C1 Path A).
//
// The typed carrier PopulateEventMetadata reads is filled by one producer, the
// generated Raise<Event> inject seam. Every other producer — <send> with
// <param>, an invoke forwarding either way, autoforward, BasicHTTP, mesh —
// fills EventMetadata.Data, so the schema's fields are read out of that here.
// A refusal is returned for the engine to raise as error.execution, which is
// what W3C SCXML 3.13 gives for a guard that cannot be evaluated.
func (p *ParallelRegionsTakeOwnTransitionsPolicy) LiftTypedPayload(event ParallelRegionsTakeOwnTransitionsEvent, meta *sce.EventMetadata) error {
	// This document lowered no typed guard, so no event carries a typed view.
	return nil
}

// ClearEventMetadata resets pending event metadata (W3C SCXML 5.10).
func (p *ParallelRegionsTakeOwnTransitionsPolicy) ClearEventMetadata() {
	p.pendingEventName = ""
	p.pendingEventData = ""
	p.pendingEventType = ""
	p.pendingEventSendid = ""
	p.pendingEventOrigin = ""
	p.pendingEventOrigintype = ""
	p.pendingEventInvokeid = ""
}



// ExecuteEntryActions enters one state (W3C SCXML 3.8): adds it to the
// configuration, runs its <onentry>, and its <initial> transition's content when
// its initial state is entered by default.
//line parallel_regions_take_own_transitions.scxml:24
func (p *ParallelRegionsTakeOwnTransitionsPolicy) ExecuteEntryActions(state ParallelRegionsTakeOwnTransitionsState, engine *sce.Engine[ParallelRegionsTakeOwnTransitionsState, ParallelRegionsTakeOwnTransitionsEvent], isDefaultEntry bool) {
	p.ensureScriptEngine()
	// W3C SCXML 3.4/3.12.1: Add state to active configuration for parallel states and In() predicate
	for _, s := range p.activeStates {
		if s == state {
			return // W3C SCXML 3.8: Skip onentry actions for duplicate state entry
		}
	}
	p.activeStates = append(p.activeStates, state)
	switch state {
	default:
		// No entry actions
	}
}

// ExecuteHistoryDefaultContent runs a <history>'s default transition content
// (W3C SCXML 3.10.2), after its parent's onentry (and after the parent's own
// <initial> content) when the history was taken with nothing recorded. The
// engine asks for it by the entry set's defaultHistoryContent answer; a history
// that restored what it recorded runs nothing.
//line parallel_regions_take_own_transitions.scxml:24
func (p *ParallelRegionsTakeOwnTransitionsPolicy) ExecuteHistoryDefaultContent(history sce.HistoryID, engine *sce.Engine[ParallelRegionsTakeOwnTransitionsState, ParallelRegionsTakeOwnTransitionsEvent]) {
	// W3C SCXML 3.10.2: no <history> in this document has default content.
}

// ExecuteExitActions exits one state (W3C SCXML 3.9): records its histories,
// runs its <onexit>, cancels its invocations and removes it from the
// configuration — §scxml-D-exitStates's order.
//line parallel_regions_take_own_transitions.scxml:24
func (p *ParallelRegionsTakeOwnTransitionsPolicy) ExecuteExitActions(state ParallelRegionsTakeOwnTransitionsState, engine *sce.Engine[ParallelRegionsTakeOwnTransitionsState, ParallelRegionsTakeOwnTransitionsEvent], configurationBeforeExit []ParallelRegionsTakeOwnTransitionsState) {
	p.ensureScriptEngine()
	// §scxml-D-exitStates orders one state's exit as onexit, then
	// cancelInvoke, then configuration.delete(s), so `In(s)` inside s's own
	// handler is true: the <onexit> switch comes first and the removal from
	// the configuration last.
	switch state {
	default:
		// No exit actions
	}
	// §scxml-D-exitStates: out of the configuration only after its onexit
	// and invoke cancellation — the configuration.delete(s) step.
	p.activeStates = func() []ParallelRegionsTakeOwnTransitionsState {
		var result []ParallelRegionsTakeOwnTransitionsState
		for _, s := range p.activeStates {
			if s != state {
				result = append(result, s)
			}
		}
		return result
	}()
}



// BindCurrentEvent binds the event whose transitions are about to be selected as
// the _event their guards read (W3C SCXML 5.10) — before the first guard runs,
// and not for an eventless selection, which has no event of its own.
//line parallel_regions_take_own_transitions.scxml:24
func (p *ParallelRegionsTakeOwnTransitionsPolicy) BindCurrentEvent(event ParallelRegionsTakeOwnTransitionsEvent, engine *sce.Engine[ParallelRegionsTakeOwnTransitionsState, ParallelRegionsTakeOwnTransitionsEvent]) {
	if event != ParallelRegionsTakeOwnTransitionsEventNull {
		// §scxml-B-2-8-1: the rung the payload got, handed to the engine
		// rather than dropped. This is the only frame that has both the
		// reading and the event it belongs to.
		engine.NotePayloadReading(event, p.setCurrentEvent(p.GetEventName(event)))
	}
}

// FirstEnabledTransition is Appendix D selectTransitions, the half only this
// document can answer: the first of state's own transitions, in document order,
// that event enables and whose guard holds. The engine walks the atomic states
// and their ancestors and keeps the ordered set; the null event asks for
// eventless transitions.
//line parallel_regions_take_own_transitions.scxml:24
func (p *ParallelRegionsTakeOwnTransitionsPolicy) FirstEnabledTransition(state ParallelRegionsTakeOwnTransitionsState, event ParallelRegionsTakeOwnTransitionsEvent, engine *sce.Engine[ParallelRegionsTakeOwnTransitionsState, ParallelRegionsTakeOwnTransitionsEvent]) (sce.EnabledTransition[ParallelRegionsTakeOwnTransitionsState, sce.HistoryID], bool) {
	switch state {
	case ParallelRegionsTakeOwnTransitionsStateJudging:
		if event == ParallelRegionsTakeOwnTransitionsEventCheck {
			if p.evaluateGuard(`(_scxml_eq(n, 1) and _scxml_eq(m, 1))`, engine) {
				return sce.EnabledTransition[ParallelRegionsTakeOwnTransitionsState, sce.HistoryID]{
					Source:          state,
					Targets:         transitionTargetsOfParallelRegionsTakeOwnTransitions[state][0],
					TransitionIndex: 0,
					HasActions:      false,
					IsInternal:      false,
				}, true
			}
		}
	case ParallelRegionsTakeOwnTransitionsStateWithin:
		if event == ParallelRegionsTakeOwnTransitionsEventE {
			{
				return sce.EnabledTransition[ParallelRegionsTakeOwnTransitionsState, sce.HistoryID]{
					Source:          state,
					Targets:         transitionTargetsOfParallelRegionsTakeOwnTransitions[state][0],
					TransitionIndex: 0,
					HasActions:      true,
					IsInternal:      false,
				}, true
			}
		}
	case ParallelRegionsTakeOwnTransitionsStateWorking:
		if event == ParallelRegionsTakeOwnTransitionsEventE {
			{
				return sce.EnabledTransition[ParallelRegionsTakeOwnTransitionsState, sce.HistoryID]{
					Source:          state,
					Targets:         transitionTargetsOfParallelRegionsTakeOwnTransitions[state][0],
					TransitionIndex: 0,
					HasActions:      true,
					IsInternal:      false,
				}, true
			}
		}
	}
	return sce.EnabledTransition[ParallelRegionsTakeOwnTransitionsState, sce.HistoryID]{}, false
}

// ExecuteTransitionContent runs one transition's executable content (W3C SCXML
// 3.13), between the microstep's exits and its entries.
//line parallel_regions_take_own_transitions.scxml:24
func (p *ParallelRegionsTakeOwnTransitionsPolicy) ExecuteTransitionContent(source ParallelRegionsTakeOwnTransitionsState, transitionIndex int, engine *sce.Engine[ParallelRegionsTakeOwnTransitionsState, ParallelRegionsTakeOwnTransitionsEvent]) {
	p.ensureScriptEngine()
	switch source {
	case ParallelRegionsTakeOwnTransitionsStateWithin:
		switch transitionIndex {
		case 0:
			//line parallel_regions_take_own_transitions.scxml:62

	// W3C SCXML 5.3: <assign location="m" expr="m + 1">
	if err := p.assignVariable(`m`, `_scxml_add(m, 1)`); err != nil {
		engine.Raise(sce.NewPlatformError(ParallelRegionsTakeOwnTransitionsEventErrorExecution, "<assign> to 'm' failed"))
	}

		}
	case ParallelRegionsTakeOwnTransitionsStateWorking:
		switch transitionIndex {
		case 0:
			//line parallel_regions_take_own_transitions.scxml:38

	// W3C SCXML 5.3: <assign location="n" expr="n + 1">
	if err := p.assignVariable(`n`, `_scxml_add(n, 1)`); err != nil {
		engine.Raise(sce.NewPlatformError(ParallelRegionsTakeOwnTransitionsEventErrorExecution, "<assign> to 'n' failed"))
	}

		}
	}
}
