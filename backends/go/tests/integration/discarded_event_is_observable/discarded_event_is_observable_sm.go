// SCE-GENERATED — DO NOT EDIT
// source-hash: df2ef2c591564c7e52022e112ae9c5e384db80574b200165584f410ac8201d24


// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) 2025-2026 [Author of input SCXML file]
//
// Generated by SCE Code Generator (Go backend)
// From: integration_resources/discarded_event_is_observable/discarded_event_is_observable.scxml
//
// This generated code is MIT licensed and may be freely used in any project.
// Runtime engine dependency (sce-go-runtime): LGPL-2.1-or-later WITH LicenseRef-SCE-Linking-Exception OR LicenseRef-SCE-Commercial
// Full license: https://github.com/newmassrael/scxml-core-engine/blob/main/LICENSE
// Generated Go state machine for `discarded_event_is_observable`.
//
// Generated by SCE Code Generator (Go backend).
// Implements sce.StatePolicy with full transition logic,
// entry/exit actions, and event processing.


// SCE-MAP: discarded_event_is_observable.scxml:30 :: _machine

package discarded_event_is_observable

import (
	"fmt"
	"path/filepath"
	"strings"
	"time"

	sce "github.com/newmassrael/sce-go-runtime"
)

// Ensure imports are used
var (
	_ = fmt.Sprintf
	_ = filepath.Join
	_ = strings.HasPrefix
	_ = time.Duration(0)
)

// ======================================================================
// State type (W3C SCXML 3.3)
// ======================================================================

type DiscardedEventIsObservableState int

const (
	DiscardedEventIsObservableStateBusy DiscardedEventIsObservableState = 0
	DiscardedEventIsObservableStateDone DiscardedEventIsObservableState = 1
	DiscardedEventIsObservableStateIdle DiscardedEventIsObservableState = 2
)

func (s DiscardedEventIsObservableState) String() string {
	switch s {
	case DiscardedEventIsObservableStateBusy:
		return "busy"
	case DiscardedEventIsObservableStateDone:
		return "done"
	case DiscardedEventIsObservableStateIdle:
		return "idle"
	}
	return "unknown"
}

// DiscardedEventIsObservableStateFromName is the read half of the pair above — a state
// id back into the state it names (W3C SCXML 3.3).
//
// A host that persists where a machine was has to write it down as TEXT: the
// constants above are a build artefact of one binary, and the process that
// resumes is a different one. String publishes the name and this reads it back,
// which is what lets a journal survive its own record and reach
// sce.Engine.EnterAt.
//
// The second return is false for a name this document does not declare. A name
// guessed at rather than refused is how a restore reaches a configuration
// nobody recorded — and the refusal is what makes a typo in a journal a
// reported failure instead of a machine quietly somewhere else.
//
// Emitted from the same loop over the document's states as String, so the two
// age together.
func DiscardedEventIsObservableStateFromName(name string) (DiscardedEventIsObservableState, bool) {
	switch name {
	case "busy":
		return DiscardedEventIsObservableStateBusy, true
	case "done":
		return DiscardedEventIsObservableStateDone, true
	case "idle":
		return DiscardedEventIsObservableStateIdle, true
	}
	var zero DiscardedEventIsObservableState
	return zero, false
}

// DiscardedEventIsObservableAllStates is every state this document declares, in the
// order the constants above are issued.
//
// Emitted from the same loop, so a walk over it is a walk over the document
// rather than over a list somebody maintained beside it — a test that spells
// its own list goes on passing when the document grows a state.
var DiscardedEventIsObservableAllStates = []DiscardedEventIsObservableState{
	DiscardedEventIsObservableStateBusy,
	DiscardedEventIsObservableStateDone,
	DiscardedEventIsObservableStateIdle,
}

// DiscardedEventIsObservableTarget is one token of a target list, as the document wrote
// it (W3C SCXML 3.13): a state, or a <history> the engine dereferences.
type DiscardedEventIsObservableTarget = sce.EntryTarget[DiscardedEventIsObservableState, sce.HistoryID]

// ======================================================================
// Document structure (W3C SCXML 3.2-3.4, 3.10)
//
// Package-level tables, because the structure is a fact about the document
// and not about a run: the engine's Appendix D procedures read them through
// the policy methods below, and a transition's target list is handed out as
// a slice of them rather than rebuilt on every selection.
// ======================================================================

// childStatesOfDiscardedEventIsObservable is §scxml-D-getChildStates per state: its
// <state>, <parallel> and <final> children, in document order.
var childStatesOfDiscardedEventIsObservable = [3][]DiscardedEventIsObservableState{
}

// initialTargetsOfDiscardedEventIsObservable is each compound state's initial transition
// target, as written (§scxml-3.3).
var initialTargetsOfDiscardedEventIsObservable = [3][]DiscardedEventIsObservableTarget{
}

// documentInitialTargetsOfDiscardedEventIsObservable is the target of the document's own
// initial transition, as written (§scxml-3.2).
var documentInitialTargetsOfDiscardedEventIsObservable = []DiscardedEventIsObservableTarget{sce.StateTarget[DiscardedEventIsObservableState, sce.HistoryID](DiscardedEventIsObservableStateIdle)}

// transitionTargetsOfDiscardedEventIsObservable is each transition's target list, as
// written (§scxml-3.13), by source state and the transition's index among its
// source's own transitions. A targetless transition's entry is empty.
var transitionTargetsOfDiscardedEventIsObservable = [3][][]DiscardedEventIsObservableTarget{
	DiscardedEventIsObservableStateBusy: {
		0: {sce.StateTarget[DiscardedEventIsObservableState, sce.HistoryID](DiscardedEventIsObservableStateDone)},
	},
	DiscardedEventIsObservableStateIdle: {
		0: {sce.StateTarget[DiscardedEventIsObservableState, sce.HistoryID](DiscardedEventIsObservableStateIdle)},
		2: {sce.StateTarget[DiscardedEventIsObservableState, sce.HistoryID](DiscardedEventIsObservableStateBusy)},
	},
}

// ======================================================================
// Event type (W3C SCXML 3.12)
// ======================================================================

type DiscardedEventIsObservableEvent int

const (
	DiscardedEventIsObservableEventErrorExecution DiscardedEventIsObservableEvent = 0
	DiscardedEventIsObservableEventGo DiscardedEventIsObservableEvent = 1
	DiscardedEventIsObservableEventNudge DiscardedEventIsObservableEvent = 2
	DiscardedEventIsObservableEventPoke DiscardedEventIsObservableEvent = 3
	DiscardedEventIsObservableEventSettle DiscardedEventIsObservableEvent = 4
	// W3C SCXML 3.13: Sentinel for eventless transition dispatch
	DiscardedEventIsObservableEventNull DiscardedEventIsObservableEvent = 5
)

func (e DiscardedEventIsObservableEvent) String() string {
	switch e {
	case DiscardedEventIsObservableEventErrorExecution:
		return "error.execution"
	case DiscardedEventIsObservableEventGo:
		return "go"
	case DiscardedEventIsObservableEventNudge:
		return "nudge"
	case DiscardedEventIsObservableEventPoke:
		return "poke"
	case DiscardedEventIsObservableEventSettle:
		return "settle"
	case DiscardedEventIsObservableEventNull:
		return ""
	}
	return "unknown"
}

// ======================================================================
// Policy struct
// ======================================================================

type DiscardedEventIsObservablePolicy struct {
	// W3C SCXML 5.10.1: External event flag
	nextEventIsExternal bool
	pendingEventName string
	pendingEventData string
	pendingEventType string
	pendingEventSendid string
	pendingEventOrigin string
	pendingEventOrigintype string
	pendingEventInvokeid string
	// W3C SCXML 5.10: Session ID
	SessionID string
	// W3C SCXML B.1: Script engine handle (per-instance, Path B+ Q1=(d) Go=interface ref)
	ScriptEngine sce.IScriptEngine
	scriptEngineInitialized bool
	// W3C SCXML C.2.3: inbound BasicHTTP endpoint serving this machine,
	// declared by the deployment before Initialize(). The address belongs to
	// whoever runs the listener, so the machine takes it from here rather than
	// guessing one. Empty means no such endpoint is deployed, and no BasicHTTP
	// entry is published in _ioprocessors.
	BasicHTTPAccessURI string
	// W3C SCXML 6.4: Parent communication
	ParentExternalQueue chan sce.ParentEvent
	InvokeID           string
	ChildSessionID     string
	// §scxml-6.4.3: invoke param VALUES staged by the parent, applied after
	// this machine's datamodel init so they override its `<data>` defaults.
	pendingParams      map[string]interface{}
}

// NewDiscardedEventIsObservablePolicy creates a new policy with default values.
func NewDiscardedEventIsObservablePolicy() DiscardedEventIsObservablePolicy {
	return DiscardedEventIsObservablePolicy{
	}
}

// Pokes reports what the `pokes` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `pokes` was assigned a value of another type, or the engine refused.
func (p *DiscardedEventIsObservablePolicy) Pokes() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "pokes")
}

// Nudges reports what the `nudges` datamodel variable is holding now
// (W3C SCXML 5.3).
//
// The live value, not the authored one: `<assign>` writes into the session, so
// a reader frozen at generation time would answer the document's literal for
// the whole run. The second return value is false when the machine cannot
// answer — no script engine is set, the session is not initialised yet,
// `nudges` was assigned a value of another type, or the engine refused.
func (p *DiscardedEventIsObservablePolicy) Nudges() (int64, bool) {
	return sce.ReadDatamodelInt(p.ScriptEngine, p.SessionID, "nudges")
}




// ensureScriptEngine lazily initializes the script engine session.
//
// Engine DI Parity RFC: panics if ScriptEngine was not set on the policy
// before Initialize() — mirrors Kotlin's mandatory constructor parameter
// contract. Silent no-op would otherwise turn missed injection into a
// hidden runtime regression (every <assign>/<log>/guard becomes a no-op
// and the SM walks to its default-state final without diagnostic).
func (p *DiscardedEventIsObservablePolicy) ensureScriptEngine() {
	if p.scriptEngineInitialized {
		return
	}
	engine := p.ScriptEngine
	if engine == nil {
		panic("DiscardedEventIsObservablePolicy: ScriptEngine field must be set before Initialize()")
	}
	sessionID := p.SessionID
	if sessionID == "" {
		sessionID = sce.GenerateSessionID()
		p.SessionID = sessionID
	}
	if !engine.HasSession(sessionID) {
		_ = engine.CreateSession(sessionID)
	}
	// W3C SCXML C.1.1 / C.2.3: the _ioprocessors entries come from the same
	// helper every other backend uses, so a machine reads the same entry names
	// and the same addresses whichever one runs it.
	_ = engine.SetupSystemVariables(sessionID, "discarded_event_is_observable",
		sce.BuildIoProcessors(sessionID, p.BasicHTTPAccessURI))
	p.scriptEngineInitialized = true
}

// InitializeDataModel sets up datamodel via script engine (W3C SCXML 5.3).
func (p *DiscardedEventIsObservablePolicy) InitializeDataModel(eng *sce.Engine[DiscardedEventIsObservableState, DiscardedEventIsObservableEvent]) {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	sessionID := p.SessionID

	// W3C SCXML 5.3: Initialize _sessionid
	_ = engine.SetVariable(sessionID, "_sessionid", sessionID)

	// W3C SCXML 5.3: Initialize _name
	_ = engine.SetVariable(sessionID, "_name", "discarded_event_is_observable")

	// W3C SCXML 5.2.2: Initialize global datamodel variables
	// W3C SCXML 5.2/5.3: Initialize pokes from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "pokes", result)
		} else {
			eng.Raise(sce.NewPlatformError(DiscardedEventIsObservableEventErrorExecution, "<data id='pokes'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "pokes", nil)
		}
	}
	// W3C SCXML 5.2/5.3: Initialize nudges from expr="0"
	{
		result, err := engine.EvaluateExpression(sessionID, `0`)
		if err == nil {
			_ = engine.SetVariable(sessionID, "nudges", result)
		} else {
			eng.Raise(sce.NewPlatformError(DiscardedEventIsObservableEventErrorExecution, "<data id='nudges'> expr failed to evaluate"))
			_ = engine.SetVariable(sessionID, "nudges", nil)
		}
	}



	// §scxml-6.4.3: apply the invoke params the parent staged — overriding the
	// `<data>` default this block just seeded, and only for a name the child
	// declares ("if the names do not match, the Processor MUST NOT add the
	// value ... to the invoked session's data model"). `HasVariable` is that
	// filter, and it is this channel's only copy of the rule.
	if len(p.pendingParams) > 0 {
		for paramName, paramValue := range p.pendingParams {
			if engine.HasVariable(sessionID, paramName) {
				_ = engine.SetVariable(sessionID, paramName, paramValue)
			}
		}
		p.pendingParams = nil
	}
}

// evaluateGuard evaluates a guard condition via script engine.
// W3C SCXML 4.3.2: If a condition expression cannot be evaluated, the processor
// MUST treat it as false and MUST raise error.execution.
func (p *DiscardedEventIsObservablePolicy) evaluateGuard(guard string, eng *sce.Engine[DiscardedEventIsObservableState, DiscardedEventIsObservableEvent]) bool {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	result, err := engine.EvaluateExpression(p.SessionID, guard)
	if err != nil {
		eng.Raise(sce.NewPlatformError(DiscardedEventIsObservableEventErrorExecution, "a <transition> cond failed to evaluate"))
		return false
	}
	return sce.ScriptToBool(result)
}

// executeScript executes a script block via script engine.
func (p *DiscardedEventIsObservablePolicy) executeScript(script string) error {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	return engine.ExecuteScript(p.SessionID, script)
}

// setCurrentEvent binds _event system variable (W3C SCXML 5.10) and answers
// which rung of §scxml-B-2-8-1 the payload got, because the binding is the
// only place that knows.
//
// This used to be `_ = engine.SetCurrentEvent(...)`: the ladder decided
// between a DOM, a value and a space-normalized string, and the decision was
// dropped one line later. A payload that announced structure and would not
// parse therefore reached the document as raw characters, every
// _event.data.<field> read empty, and nothing anywhere could say so — see
// sce.PayloadReading.
func (p *DiscardedEventIsObservablePolicy) setCurrentEvent(name string) sce.PayloadReading {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	data := p.pendingEventData
	eventType := p.pendingEventType
	sendID := p.pendingEventSendid
	// W3C SCXML C.1: `_event.origin` is the sender's published
	// `_ioprocessors` location, not its bare session id — and this is the one
	// place that publishes `_event` to the document, so this is where the id
	// becomes a location. The engine keeps the bare id in
	// `EventMetadata.Origin` because its session-keyed lookups (<finalize>
	// dispatch, cancelled-invoke filtering) match on it; converting at the
	// raise would make one value serve two consumers that need different
	// spellings. The conversion itself lives in `sce.PublishedOrigin`, the
	// port of the `IOProcessorHelper::publishedOrigin` the C++ engines share:
	// a second spelling of the rule is how the backends would stop agreeing.
	origin := sce.PublishedOrigin(p.pendingEventOrigin)
	originType := p.pendingEventOrigintype
	invokeID := p.pendingEventInvokeid
	reading, err := engine.SetCurrentEvent(p.SessionID, sce.SetCurrentEventArgs{
		Name:       name,
		Data:       data,
		Type:       eventType,
		SendID:     sendID,
		Origin:     origin,
		OriginType: originType,
		InvokeID:   invokeID,
	})
	if err != nil {
		// An engine that could not bind _event at all has a larger problem
		// than an unreadable payload, and reporting PayloadUndecodable for it
		// would point a host at the wrong thing.
		return sce.PayloadAbsent
	}
	return reading
}

// assignVariable assigns a value to a datamodel variable via script engine.
func (p *DiscardedEventIsObservablePolicy) assignVariable(location, expr string) error {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	script := location + " = " + expr
	return engine.ExecuteScript(p.SessionID, script)
}

// getVariable reads a datamodel variable from the script engine.
func (p *DiscardedEventIsObservablePolicy) getVariable(name string) interface{} {
	p.ensureScriptEngine()
	engine := p.ScriptEngine
	result, err := engine.GetVariable(p.SessionID, name)
	if err != nil {
		return nil
	}
	return result
}

// SetParamValueInScriptEngine stores an invoke param's VALUE for deferred
// application after datamodel init (§scxml-6.4.3).
//
// Params are applied at the end of InitializeDataModel, overriding the
// `<data>` default only for names the child declares. The value crosses as a
// value: this took the parent's value rendered as Lua source and
// re-evaluated it here until 2026-08-21, which survived every value a test
// had tried and lost the ones Lua cannot spell — `ToScriptLiteral` renders a
// non-finite number as `+Inf`, which is not a Lua expression, so an
// `<invoke>` `<param expr="1/0"/>` arrived as nothing at all.
func (p *DiscardedEventIsObservablePolicy) SetParamValueInScriptEngine(name string, value interface{}) {
	if p.pendingParams == nil {
		p.pendingParams = make(map[string]interface{})
	}
	p.pendingParams[name] = value
}


// ExecutePendingInvokes is a no-op (no invokes in this SM).
func (p *DiscardedEventIsObservablePolicy) ExecutePendingInvokes(engine *sce.Engine[DiscardedEventIsObservableState, DiscardedEventIsObservableEvent]) {}

// TickChildren is a no-op (no invokes in this SM).
func (p *DiscardedEventIsObservablePolicy) TickChildren(engine *sce.Engine[DiscardedEventIsObservableState, DiscardedEventIsObservableEvent]) {}

// ======================================================================
// StatePolicy interface implementation
// ======================================================================

// InitialState returns the initial state (W3C SCXML 3.3).
func (p *DiscardedEventIsObservablePolicy) InitialState() DiscardedEventIsObservableState {
	return DiscardedEventIsObservableStateIdle
}

// IsFinalState returns true if state is a <final> state (W3C SCXML 3.7).
func (p *DiscardedEventIsObservablePolicy) IsFinalState(state DiscardedEventIsObservableState) bool {
	switch state {
	case DiscardedEventIsObservableStateDone:
		return true
	}
	return false
}

// GetParent returns the parent of state, or (zero, false) if root (W3C SCXML 3.3).
func (p *DiscardedEventIsObservablePolicy) GetParent(state DiscardedEventIsObservableState) (DiscardedEventIsObservableState, bool) {
	switch state {
	}
	return 0, false
}

// IsCompoundState returns true if state is a <state> with child states — exactly
// the states that have an initial transition. A <parallel> is not compound
// (W3C SCXML 3.3).
func (p *DiscardedEventIsObservablePolicy) IsCompoundState(state DiscardedEventIsObservableState) bool {
	return len(initialTargetsOfDiscardedEventIsObservable[state]) > 0
}

func (p *DiscardedEventIsObservablePolicy) IsParallelState(_ DiscardedEventIsObservableState) bool { return false }

// GetChildStates returns state's <state>, <parallel> and <final> children, in
// document order — for a <parallel>, its regions (§scxml-D-getChildStates).
func (p *DiscardedEventIsObservablePolicy) GetChildStates(state DiscardedEventIsObservableState) []DiscardedEventIsObservableState {
	return childStatesOfDiscardedEventIsObservable[state]
}

// GetInitialTargets returns a compound state's initial transition target, as
// written; the engine's entry procedures dereference a <history> among them
// (W3C SCXML 3.3).
func (p *DiscardedEventIsObservablePolicy) GetInitialTargets(state DiscardedEventIsObservableState) []DiscardedEventIsObservableTarget {
	return initialTargetsOfDiscardedEventIsObservable[state]
}

// GetDocumentInitialTargets returns the target of the document's own initial
// transition, as written (W3C SCXML 3.2).
func (p *DiscardedEventIsObservablePolicy) GetDocumentInitialTargets() []DiscardedEventIsObservableTarget {
	return documentInitialTargetsOfDiscardedEventIsObservable
}

// W3C SCXML 3.10: this document declares no <history>, so no target list names
// one and the engine never asks the two below; answering would mean inventing
// one.
func (p *DiscardedEventIsObservablePolicy) GetHistoryParent(history sce.HistoryID) DiscardedEventIsObservableState {
	panic(fmt.Sprintf("DiscardedEventIsObservablePolicy declares no <history>; asked for %d", history))
}
func (p *DiscardedEventIsObservablePolicy) GetHistoryDefaultTargets(history sce.HistoryID) []DiscardedEventIsObservableTarget {
	panic(fmt.Sprintf("DiscardedEventIsObservablePolicy declares no <history>; asked for %d", history))
}
func (p *DiscardedEventIsObservablePolicy) HistoryValue(_ sce.HistoryID) ([]DiscardedEventIsObservableState, bool) {
	return nil, false
}

// GetDocumentOrder returns the document order index (W3C SCXML Appendix D).
func (p *DiscardedEventIsObservablePolicy) GetDocumentOrder(state DiscardedEventIsObservableState) int {
	switch state {
	case DiscardedEventIsObservableStateBusy:
		return 1
	case DiscardedEventIsObservableStateDone:
		return 2
	case DiscardedEventIsObservableStateIdle:
		return 0
	}
	return -1
}

// GetEventName returns the string name of an event (W3C SCXML 3.12).
func (p *DiscardedEventIsObservablePolicy) GetEventName(event DiscardedEventIsObservableEvent) string {
	return event.String()
}

// GetEventFromName looks up an event by name (W3C SCXML 3.12).
func (p *DiscardedEventIsObservablePolicy) GetEventFromName(name string) (DiscardedEventIsObservableEvent, bool) {
	switch name {
	case "error.execution":
		return DiscardedEventIsObservableEventErrorExecution, true
	case "go":
		return DiscardedEventIsObservableEventGo, true
	case "nudge":
		return DiscardedEventIsObservableEventNudge, true
	case "poke":
		return DiscardedEventIsObservableEventPoke, true
	case "settle":
		return DiscardedEventIsObservableEventSettle, true
	}
	return DiscardedEventIsObservableEventNull, false
}

// GetStateName returns the string name of a state (W3C SCXML 3.3).
func (p *DiscardedEventIsObservablePolicy) GetStateName(state DiscardedEventIsObservableState) string {
	return state.String()
}

// GetStateFromName reads a state id back into the state it names (W3C SCXML 3.3).
// The reverse of GetStateName, and what turns a host's recorded configuration
// back into the argument sce.Engine.EnterAt takes.
func (p *DiscardedEventIsObservablePolicy) GetStateFromName(name string) (DiscardedEventIsObservableState, bool) {
	return DiscardedEventIsObservableStateFromName(name)
}

// NullEvent returns the sentinel for eventless transition dispatch (W3C SCXML 3.13).
func (p *DiscardedEventIsObservablePolicy) NullEvent() DiscardedEventIsObservableEvent {
	return DiscardedEventIsObservableEventNull
}


// SetNextEventIsExternal sets the external event flag (W3C SCXML 5.10.1).
func (p *DiscardedEventIsObservablePolicy) SetNextEventIsExternal(value bool) {
	p.nextEventIsExternal = value
}

// HasParallelStates returns whether the SM has parallel states.
func (p *DiscardedEventIsObservablePolicy) HasParallelStates() bool {
	return false
}

// NeedsScriptEngine returns whether the SM needs a script engine.
func (p *DiscardedEventIsObservablePolicy) NeedsScriptEngine() bool {
	return true
}

// NeedsEventScheduler reports whether a host must drive this machine with
// Tick rather than Step alone.
//
// W3C SCXML 6.2: Step runs a macrostep and never drains the delayed-send
// scheduler nor ticks an invoked child, so a host that only ever calls it
// waits forever with nothing said. Same verdict the generate manifest
// publishes as needs_event_scheduler.
func (p *DiscardedEventIsObservablePolicy) NeedsEventScheduler() bool {
	return false
}

// NeedsDataModelInit returns whether the SM needs datamodel init.
func (p *DiscardedEventIsObservablePolicy) NeedsDataModelInit() bool {
	return true
}

func (p *DiscardedEventIsObservablePolicy) HasInvokeSupport() bool { return false }
func (p *DiscardedEventIsObservablePolicy) HasChildTick() bool { return false }
func (p *DiscardedEventIsObservablePolicy) HasFinalize() bool { return false }
func (p *DiscardedEventIsObservablePolicy) HasAutoforward() bool { return false }
func (p *DiscardedEventIsObservablePolicy) HasActiveStates() bool { return false }
func (p *DiscardedEventIsObservablePolicy) GetActiveStates() []DiscardedEventIsObservableState { return nil }
// W3C SCXML 3.4: this machine keeps no active set — its configuration is the
// parent walk from the current state — so there is nothing for a restore to
// hand back here. sce.Engine.EnterAt reaches this only through HasActiveStates,
// which is false above; the method exists because the interface is one contract.
func (p *DiscardedEventIsObservablePolicy) SetActiveStates(_ []DiscardedEventIsObservableState) {}
func (p *DiscardedEventIsObservablePolicy) HasExternalEventFlag() bool { return true }
// ExecuteFinalizeForChildEvent is a no-op (no finalize invokes).
func (p *DiscardedEventIsObservablePolicy) ExecuteFinalizeForChildEvent(_ *sce.EventWithMetadata[DiscardedEventIsObservableEvent], _ *sce.Engine[DiscardedEventIsObservableState, DiscardedEventIsObservableEvent]) {}
// ForwardToAutoforwardChildren is a no-op (no autoforward invokes).
func (p *DiscardedEventIsObservablePolicy) ForwardToAutoforwardChildren(_ string, _ sce.EventMetadata, _ *sce.Engine[DiscardedEventIsObservableState, DiscardedEventIsObservableEvent]) {}

// PopulateEventMetadata stores pending event metadata (W3C SCXML 5.10).
// Note: event name is set separately via setCurrentEvent(), not from metadata.
func (p *DiscardedEventIsObservablePolicy) PopulateEventMetadata(meta *sce.EventMetadata) {
	p.pendingEventData = meta.Data
	p.pendingEventType = meta.EventType.String()
	p.pendingEventSendid = meta.SendID
	p.pendingEventOrigin = meta.Origin
	p.pendingEventOrigintype = meta.OriginType
	p.pendingEventInvokeid = meta.InvokeID
}

// LiftTypedPayload binds the dequeued event's typed `_event.data` view from
// the data it carries (NL→IR Item C1 Path A).
//
// The typed carrier PopulateEventMetadata reads is filled by one producer, the
// generated Raise<Event> inject seam. Every other producer — <send> with
// <param>, an invoke forwarding either way, autoforward, BasicHTTP, mesh —
// fills EventMetadata.Data, so the schema's fields are read out of that here.
// A refusal is returned for the engine to raise as error.execution, which is
// what W3C SCXML 3.13 gives for a guard that cannot be evaluated.
func (p *DiscardedEventIsObservablePolicy) LiftTypedPayload(event DiscardedEventIsObservableEvent, meta *sce.EventMetadata) error {
	// This document lowered no typed guard, so no event carries a typed view.
	return nil
}

// ClearEventMetadata resets pending event metadata (W3C SCXML 5.10).
func (p *DiscardedEventIsObservablePolicy) ClearEventMetadata() {
	p.pendingEventName = ""
	p.pendingEventData = ""
	p.pendingEventType = ""
	p.pendingEventSendid = ""
	p.pendingEventOrigin = ""
	p.pendingEventOrigintype = ""
	p.pendingEventInvokeid = ""
}



// ExecuteEntryActions enters one state (W3C SCXML 3.8): adds it to the
// configuration, runs its <onentry>, and its <initial> transition's content when
// its initial state is entered by default.
//line discarded_event_is_observable.scxml:30
func (p *DiscardedEventIsObservablePolicy) ExecuteEntryActions(state DiscardedEventIsObservableState, engine *sce.Engine[DiscardedEventIsObservableState, DiscardedEventIsObservableEvent], isDefaultEntry bool) {
	p.ensureScriptEngine()
	switch state {
	default:
		// No entry actions
	}
}

// ExecuteHistoryDefaultContent runs a <history>'s default transition content
// (W3C SCXML 3.10.2), after its parent's onentry (and after the parent's own
// <initial> content) when the history was taken with nothing recorded. The
// engine asks for it by the entry set's defaultHistoryContent answer; a history
// that restored what it recorded runs nothing.
//line discarded_event_is_observable.scxml:30
func (p *DiscardedEventIsObservablePolicy) ExecuteHistoryDefaultContent(history sce.HistoryID, engine *sce.Engine[DiscardedEventIsObservableState, DiscardedEventIsObservableEvent]) {
	// W3C SCXML 3.10.2: no <history> in this document has default content.
}

// ExecuteExitActions exits one state (W3C SCXML 3.9): records its histories,
// removes it from the configuration, cancels its invocations and runs its
// <onexit>.
//line discarded_event_is_observable.scxml:30
func (p *DiscardedEventIsObservablePolicy) ExecuteExitActions(state DiscardedEventIsObservableState, engine *sce.Engine[DiscardedEventIsObservableState, DiscardedEventIsObservableEvent], configurationBeforeExit []DiscardedEventIsObservableState) {
	p.ensureScriptEngine()
	switch state {
	default:
		// No exit actions
	}
}



// BindCurrentEvent binds the event whose transitions are about to be selected as
// the _event their guards read (W3C SCXML 5.10) — before the first guard runs,
// and not for an eventless selection, which has no event of its own.
//line discarded_event_is_observable.scxml:30
func (p *DiscardedEventIsObservablePolicy) BindCurrentEvent(event DiscardedEventIsObservableEvent, engine *sce.Engine[DiscardedEventIsObservableState, DiscardedEventIsObservableEvent]) {
	if event != DiscardedEventIsObservableEventNull {
		// §scxml-B-2-8-1: the rung the payload got, handed to the engine
		// rather than dropped. This is the only frame that has both the
		// reading and the event it belongs to.
		engine.NotePayloadReading(event, p.setCurrentEvent(p.GetEventName(event)))
	}
}

// FirstEnabledTransition is Appendix D selectTransitions, the half only this
// document can answer: the first of state's own transitions, in document order,
// that event enables and whose guard holds. The engine walks the atomic states
// and their ancestors and keeps the ordered set; the null event asks for
// eventless transitions.
//line discarded_event_is_observable.scxml:30
func (p *DiscardedEventIsObservablePolicy) FirstEnabledTransition(state DiscardedEventIsObservableState, event DiscardedEventIsObservableEvent, engine *sce.Engine[DiscardedEventIsObservableState, DiscardedEventIsObservableEvent]) (sce.EnabledTransition[DiscardedEventIsObservableState, sce.HistoryID], bool) {
	switch state {
	case DiscardedEventIsObservableStateBusy:
		if event == DiscardedEventIsObservableEventSettle {
			{
				return sce.EnabledTransition[DiscardedEventIsObservableState, sce.HistoryID]{
					Source:          state,
					Targets:         transitionTargetsOfDiscardedEventIsObservable[state][0],
					TransitionIndex: 0,
					HasActions:      false,
					IsInternal:      false,
				}, true
			}
		}
	case DiscardedEventIsObservableStateIdle:
		if event == DiscardedEventIsObservableEventPoke {
			{
				return sce.EnabledTransition[DiscardedEventIsObservableState, sce.HistoryID]{
					Source:          state,
					Targets:         transitionTargetsOfDiscardedEventIsObservable[state][0],
					TransitionIndex: 0,
					HasActions:      true,
					IsInternal:      false,
				}, true
			}
		}
		if event == DiscardedEventIsObservableEventNudge {
			{
				return sce.EnabledTransition[DiscardedEventIsObservableState, sce.HistoryID]{
					Source:          state,
					TransitionIndex: 1,
					HasActions:      true,
					IsInternal:      false,
				}, true
			}
		}
		if event == DiscardedEventIsObservableEventGo {
			{
				return sce.EnabledTransition[DiscardedEventIsObservableState, sce.HistoryID]{
					Source:          state,
					Targets:         transitionTargetsOfDiscardedEventIsObservable[state][2],
					TransitionIndex: 2,
					HasActions:      false,
					IsInternal:      false,
				}, true
			}
		}
	}
	return sce.EnabledTransition[DiscardedEventIsObservableState, sce.HistoryID]{}, false
}

// ExecuteTransitionContent runs one transition's executable content (W3C SCXML
// 3.13), between the microstep's exits and its entries.
//line discarded_event_is_observable.scxml:30
func (p *DiscardedEventIsObservablePolicy) ExecuteTransitionContent(source DiscardedEventIsObservableState, transitionIndex int, engine *sce.Engine[DiscardedEventIsObservableState, DiscardedEventIsObservableEvent]) {
	p.ensureScriptEngine()
	switch source {
	case DiscardedEventIsObservableStateIdle:
		switch transitionIndex {
		case 0:
			//line discarded_event_is_observable.scxml:38

	// W3C SCXML 5.3: <assign location="pokes" expr="pokes + 1">
	if err := p.assignVariable(`pokes`, `_scxml_add(pokes, 1)`); err != nil {
		engine.Raise(sce.NewPlatformError(DiscardedEventIsObservableEventErrorExecution, "<assign> to 'pokes' failed"))
	}

		case 1:
			//line discarded_event_is_observable.scxml:41

	// W3C SCXML 5.3: <assign location="nudges" expr="nudges + 1">
	if err := p.assignVariable(`nudges`, `_scxml_add(nudges, 1)`); err != nil {
		engine.Raise(sce.NewPlatformError(DiscardedEventIsObservableEventErrorExecution, "<assign> to 'nudges' failed"))
	}

		}
	}
}
