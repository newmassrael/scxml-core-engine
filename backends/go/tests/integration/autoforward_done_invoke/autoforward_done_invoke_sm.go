// SCE-GENERATED — DO NOT EDIT
// source-hash: 54fa213afae337fd55d5bdcc6342253ac581ed7cc7a7519be41e894ee31b3f4b


// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) 2025-2026 [Author of input SCXML file]
//
// Generated by SCE Code Generator (Go backend)
// From: integration_resources/autoforward_done_invoke/autoforward_done_invoke.scxml
//
// This generated code is MIT licensed and may be freely used in any project.
// Runtime engine dependency (sce-go-runtime): LGPL-2.1-or-later WITH LicenseRef-SCE-Linking-Exception OR LicenseRef-SCE-Commercial
// Full license: https://github.com/newmassrael/scxml-core-engine/blob/main/LICENSE
// Generated Go state machine for `autoforward_done_invoke`.
//
// Generated by SCE Code Generator (Go backend).
// Implements sce.StatePolicy with full transition logic,
// entry/exit actions, and event processing.


// SCE-MAP: autoforward_done_invoke.scxml:55 :: _machine

package autoforward_done_invoke

import (
	"fmt"
	"path/filepath"
	"strings"
	"time"

	sce "github.com/newmassrael/sce-go-runtime"
)

// Ensure imports are used
var (
	_ = fmt.Sprintf
	_ = filepath.Join
	_ = strings.HasPrefix
	_ = time.Duration(0)
)

// ======================================================================
// State type (W3C SCXML 3.3)
// ======================================================================

type AutoforwardDoneInvokeState int

const (
	AutoforwardDoneInvokeStateFail AutoforwardDoneInvokeState = 0
	AutoforwardDoneInvokeStatePass AutoforwardDoneInvokeState = 1
	AutoforwardDoneInvokeStatePhase AutoforwardDoneInvokeState = 2
)

func (s AutoforwardDoneInvokeState) String() string {
	switch s {
	case AutoforwardDoneInvokeStateFail:
		return "fail"
	case AutoforwardDoneInvokeStatePass:
		return "pass"
	case AutoforwardDoneInvokeStatePhase:
		return "phase"
	}
	return "unknown"
}

// AutoforwardDoneInvokeStateFromName is the read half of the pair above — a state
// id back into the state it names (W3C SCXML 3.3).
//
// A host that persists where a machine was has to write it down as TEXT: the
// constants above are a build artefact of one binary, and the process that
// resumes is a different one. String publishes the name and this reads it back,
// which is what lets a journal survive its own record and reach
// sce.Engine.EnterAt.
//
// The second return is false for a name this document does not declare. A name
// guessed at rather than refused is how a restore reaches a configuration
// nobody recorded — and the refusal is what makes a typo in a journal a
// reported failure instead of a machine quietly somewhere else.
//
// Emitted from the same loop over the document's states as String, so the two
// age together.
func AutoforwardDoneInvokeStateFromName(name string) (AutoforwardDoneInvokeState, bool) {
	switch name {
	case "fail":
		return AutoforwardDoneInvokeStateFail, true
	case "pass":
		return AutoforwardDoneInvokeStatePass, true
	case "phase":
		return AutoforwardDoneInvokeStatePhase, true
	}
	var zero AutoforwardDoneInvokeState
	return zero, false
}

// AutoforwardDoneInvokeAllStates is every state this document declares, in the
// order the constants above are issued.
//
// Emitted from the same loop, so a walk over it is a walk over the document
// rather than over a list somebody maintained beside it — a test that spells
// its own list goes on passing when the document grows a state.
var AutoforwardDoneInvokeAllStates = []AutoforwardDoneInvokeState{
	AutoforwardDoneInvokeStateFail,
	AutoforwardDoneInvokeStatePass,
	AutoforwardDoneInvokeStatePhase,
}

// AutoforwardDoneInvokeTarget is one token of a target list, as the document wrote
// it (W3C SCXML 3.13): a state, or a <history> the engine dereferences.
type AutoforwardDoneInvokeTarget = sce.EntryTarget[AutoforwardDoneInvokeState, sce.HistoryID]

// ======================================================================
// Document structure (W3C SCXML 3.2-3.4, 3.10)
//
// Package-level tables, because the structure is a fact about the document
// and not about a run: the engine's Appendix D procedures read them through
// the policy methods below, and a transition's target list is handed out as
// a slice of them rather than rebuilt on every selection.
// ======================================================================

// childStatesOfAutoforwardDoneInvoke is §scxml-D-getChildStates per state: its
// <state>, <parallel> and <final> children, in document order.
var childStatesOfAutoforwardDoneInvoke = [3][]AutoforwardDoneInvokeState{
}

// initialTargetsOfAutoforwardDoneInvoke is each compound state's initial transition
// target, as written (§scxml-3.3).
var initialTargetsOfAutoforwardDoneInvoke = [3][]AutoforwardDoneInvokeTarget{
}

// documentInitialTargetsOfAutoforwardDoneInvoke is the target of the document's own
// initial transition, as written (§scxml-3.2).
var documentInitialTargetsOfAutoforwardDoneInvoke = []AutoforwardDoneInvokeTarget{sce.StateTarget[AutoforwardDoneInvokeState, sce.HistoryID](AutoforwardDoneInvokeStatePhase)}

// transitionTargetsOfAutoforwardDoneInvoke is each transition's target list, as
// written (§scxml-3.13), by source state and the transition's index among its
// source's own transitions. A targetless transition's entry is empty.
var transitionTargetsOfAutoforwardDoneInvoke = [3][][]AutoforwardDoneInvokeTarget{
	AutoforwardDoneInvokeStatePhase: {
		1: {sce.StateTarget[AutoforwardDoneInvokeState, sce.HistoryID](AutoforwardDoneInvokeStatePass)},
		2: {sce.StateTarget[AutoforwardDoneInvokeState, sce.HistoryID](AutoforwardDoneInvokeStateFail)},
		3: {sce.StateTarget[AutoforwardDoneInvokeState, sce.HistoryID](AutoforwardDoneInvokeStateFail)},
	},
}

// ======================================================================
// Event type (W3C SCXML 3.12)
// ======================================================================

type AutoforwardDoneInvokeEvent int

const (
	AutoforwardDoneInvokeEventCancelInvoke AutoforwardDoneInvokeEvent = 0
	AutoforwardDoneInvokeEventDoneInvoke AutoforwardDoneInvokeEvent = 1
	AutoforwardDoneInvokeEventDoneInvokeInvShort AutoforwardDoneInvokeEvent = 2
	AutoforwardDoneInvokeEventErrorExecution AutoforwardDoneInvokeEvent = 3
	AutoforwardDoneInvokeEventProbe AutoforwardDoneInvokeEvent = 4
	AutoforwardDoneInvokeEventSawPlatform AutoforwardDoneInvokeEvent = 5
	AutoforwardDoneInvokeEventSawProbeOnly AutoforwardDoneInvokeEvent = 6
	// W3C SCXML 3.13: Sentinel for eventless transition dispatch
	AutoforwardDoneInvokeEventNull AutoforwardDoneInvokeEvent = 7
)

func (e AutoforwardDoneInvokeEvent) String() string {
	switch e {
	case AutoforwardDoneInvokeEventCancelInvoke:
		return "cancel.invoke"
	case AutoforwardDoneInvokeEventDoneInvoke:
		return "done.invoke"
	case AutoforwardDoneInvokeEventDoneInvokeInvShort:
		return "done.invoke.inv_short"
	case AutoforwardDoneInvokeEventErrorExecution:
		return "error.execution"
	case AutoforwardDoneInvokeEventProbe:
		return "probe"
	case AutoforwardDoneInvokeEventSawPlatform:
		return "sawPlatform"
	case AutoforwardDoneInvokeEventSawProbeOnly:
		return "sawProbeOnly"
	case AutoforwardDoneInvokeEventNull:
		return ""
	}
	return "unknown"
}

// ======================================================================
// Policy struct
// ======================================================================

type AutoforwardDoneInvokePolicy struct {
	// W3C SCXML 5.10: Session ID
	SessionID string
	pendingInvokes []sce.PendingInvoke[AutoforwardDoneInvokeState]
	activeInvokes  map[string]*sce.ChildSession
	childInvWatch sce.ChildEngine
	pendingDoneInvokeInvWatch bool
	childInvShort sce.ChildEngine
	pendingDoneInvokeInvShort bool
	// W3C SCXML 6.4: Parent communication
	ParentExternalQueue chan sce.ParentEvent
	InvokeID           string
	ChildSessionID     string
	// §scxml-6.4.3: invoke param VALUES staged by the parent, applied after
	// this machine's datamodel init so they override its `<data>` defaults.
	pendingParams      map[string]interface{}
}

// NewAutoforwardDoneInvokePolicy creates a new policy with default values.
func NewAutoforwardDoneInvokePolicy() AutoforwardDoneInvokePolicy {
	return AutoforwardDoneInvokePolicy{
		pendingInvokes: make([]sce.PendingInvoke[AutoforwardDoneInvokeState], 0),
		activeInvokes:  make(map[string]*sce.ChildSession),
	}
}



// No script engine needed for this state machine.
func (p *AutoforwardDoneInvokePolicy) InitializeDataModel(eng *sce.Engine[AutoforwardDoneInvokeState, AutoforwardDoneInvokeEvent]) {}

// SetParamValueInScriptEngine is a no-op for state machines without script engine.
func (p *AutoforwardDoneInvokePolicy) SetParamValueInScriptEngine(name string, value interface{}) {}


// ExecutePendingInvokes starts deferred child state machines (W3C SCXML 6.4).
// Port of Rust do_execute_pending_invokes().
func (p *AutoforwardDoneInvokePolicy) ExecutePendingInvokes(engine *sce.Engine[AutoforwardDoneInvokeState, AutoforwardDoneInvokeEvent]) {
	if len(p.pendingInvokes) == 0 {
		return
	}

	// W3C SCXML 6.4: Copy-and-clear to prevent iterator invalidation
	invokesToExecute := make([]sce.PendingInvoke[AutoforwardDoneInvokeState], len(p.pendingInvokes))
	copy(invokesToExecute, p.pendingInvokes)
	p.pendingInvokes = p.pendingInvokes[:0]

	for _, pending := range invokesToExecute {
		if strings.Contains(pending.InvokeID, ".inv_watch") {

			// W3C SCXML 6.5: Generate child session ID for finalize origin matching
			childSessionID := fmt.Sprintf("%s.%s", p.SessionID, pending.InvokeID)

			// W3C SCXML 6.4: Create child state machine
			childPolicy := NewAutoforwardDoneInvokeSceSynthInvokeInvWatchPolicy()
			parentQueue := make(chan sce.ParentEvent, 100)
			childPolicy.ParentExternalQueue = parentQueue
			childPolicy.InvokeID = pending.InvokeID
			childPolicy.ChildSessionID = childSessionID
			// W3C SCXML C.1: the child adopts the id its parent recorded for
			// it. The parent mints `parentSession.invokeId` and keys
			// `activeInvokes` on it, so an event from this child reaches the
			// parent carrying that id as its origin; if the child also minted
			// an id of its own, the location it publishes in `_ioprocessors`
			// and the origin the parent sees would be two names for one
			// session that can never be compared — which is exactly what C.1
			// requires of them.
			childPolicy.SessionID = childSessionID



			// W3C SCXML 6.4.1: Track active invoke session BEFORE initialize
			p.activeInvokes["inv_watch"] = &sce.ChildSession{
				SessionID:       childSessionID,
				InvokeID:        "inv_watch",
				ParentSessionID: p.SessionID,
				Autoforward:     true,
				FinalizeScript:  "",
			}

			childEngine := sce.NewEngine[AutoforwardDoneInvokeSceSynthInvokeInvWatchState, AutoforwardDoneInvokeSceSynthInvokeInvWatchEvent](&childPolicy)
			// §scxml-6.4: the child's delayed sends are measured against the
			// same clock as ours. A child reading its own would start its
			// origin at construction time, so <send delay="100ms"> on either
			// side of the boundary would mean two different absolute instants —
			// and on a host-owned clock the child would not move at all,
			// because the host advances the engine it holds, not the ones that
			// engine invoked.
			childEngine.SetClock(engine.Clock())
			childEngine.SetCompletionCallback(func() {})
			childEngine.Initialize()

			// W3C SCXML 6.4: Store child and create wrapper
			wrapper := &childEngineWrapperInvWatch{engine: childEngine, policy: &childPolicy}
			p.childInvWatch = wrapper

			// W3C SCXML 6.4: Drain child-to-parent events raised during initialize
			sce.DrainAndRaiseChildEvents(wrapper, p.activeInvokes, "inv_watch", engine)

			// W3C SCXML 6.4: Check if child completed during initialize
			if wrapper.IsInFinalState() {
				p.pendingDoneInvokeInvWatch = true
				// W3C SCXML 5.5 + 6.3.1: Lift the child's stashed donedata onto
				// done.invoke.<id>._event.data. Mirrors the C++ AOT contract
				// and the Rust raise_done_invoke(donedata) 3-arg signature.
				sce.RaiseDoneInvoke("inv_watch", wrapper.DonedataAtFinal(), engine)
			}
			continue
		}
		if strings.Contains(pending.InvokeID, ".inv_short") {

			// W3C SCXML 6.5: Generate child session ID for finalize origin matching
			childSessionID := fmt.Sprintf("%s.%s", p.SessionID, pending.InvokeID)

			// W3C SCXML 6.4: Create child state machine
			childPolicy := NewAutoforwardDoneInvokeSceSynthInvokeInvShortPolicy()
			parentQueue := make(chan sce.ParentEvent, 100)
			childPolicy.ParentExternalQueue = parentQueue
			childPolicy.InvokeID = pending.InvokeID
			childPolicy.ChildSessionID = childSessionID
			// W3C SCXML C.1: the child adopts the id its parent recorded for
			// it. The parent mints `parentSession.invokeId` and keys
			// `activeInvokes` on it, so an event from this child reaches the
			// parent carrying that id as its origin; if the child also minted
			// an id of its own, the location it publishes in `_ioprocessors`
			// and the origin the parent sees would be two names for one
			// session that can never be compared — which is exactly what C.1
			// requires of them.
			childPolicy.SessionID = childSessionID



			// W3C SCXML 6.4.1: Track active invoke session BEFORE initialize
			p.activeInvokes["inv_short"] = &sce.ChildSession{
				SessionID:       childSessionID,
				InvokeID:        "inv_short",
				ParentSessionID: p.SessionID,
				Autoforward:     false,
				FinalizeScript:  "",
			}

			childEngine := sce.NewEngine[AutoforwardDoneInvokeSceSynthInvokeInvShortState, AutoforwardDoneInvokeSceSynthInvokeInvShortEvent](&childPolicy)
			// §scxml-6.4: the child's delayed sends are measured against the
			// same clock as ours. A child reading its own would start its
			// origin at construction time, so <send delay="100ms"> on either
			// side of the boundary would mean two different absolute instants —
			// and on a host-owned clock the child would not move at all,
			// because the host advances the engine it holds, not the ones that
			// engine invoked.
			childEngine.SetClock(engine.Clock())
			childEngine.SetCompletionCallback(func() {})
			childEngine.Initialize()

			// W3C SCXML 6.4: Store child and create wrapper
			wrapper := &childEngineWrapperInvShort{engine: childEngine, policy: &childPolicy}
			p.childInvShort = wrapper

			// W3C SCXML 6.4: Drain child-to-parent events raised during initialize
			sce.DrainAndRaiseChildEvents(wrapper, p.activeInvokes, "inv_short", engine)

			// W3C SCXML 6.4: Check if child completed during initialize
			if wrapper.IsInFinalState() {
				p.pendingDoneInvokeInvShort = true
				// W3C SCXML 5.5 + 6.3.1: Lift the child's stashed donedata onto
				// done.invoke.<id>._event.data. Mirrors the C++ AOT contract
				// and the Rust raise_done_invoke(donedata) 3-arg signature.
				sce.RaiseDoneInvoke("inv_short", wrapper.DonedataAtFinal(), engine)
			}
			continue
		}
	}
}

// TickChildren ticks child state machines (W3C SCXML 6.4).
// Port of Rust do_tick_children().
func (p *AutoforwardDoneInvokePolicy) TickChildren(engine *sce.Engine[AutoforwardDoneInvokeState, AutoforwardDoneInvokeEvent]) {
	if !p.pendingDoneInvokeInvWatch && p.childInvWatch != nil {
		child := p.childInvWatch
		sce.DrainAndRaiseChildEvents(child, p.activeInvokes, "inv_watch", engine)
		child.Tick()
		sce.DrainAndRaiseChildEvents(child, p.activeInvokes, "inv_watch", engine)
		if child.IsInFinalState() && !p.pendingDoneInvokeInvWatch {
			p.pendingDoneInvokeInvWatch = true
			// W3C SCXML 5.5 + 6.3.1: Lift the child's stashed donedata onto
			// done.invoke.<id>._event.data.
			sce.RaiseDoneInvoke("inv_watch", child.DonedataAtFinal(), engine)
		}
	}
	if !p.pendingDoneInvokeInvShort && p.childInvShort != nil {
		child := p.childInvShort
		sce.DrainAndRaiseChildEvents(child, p.activeInvokes, "inv_short", engine)
		child.Tick()
		sce.DrainAndRaiseChildEvents(child, p.activeInvokes, "inv_short", engine)
		if child.IsInFinalState() && !p.pendingDoneInvokeInvShort {
			p.pendingDoneInvokeInvShort = true
			// W3C SCXML 5.5 + 6.3.1: Lift the child's stashed donedata onto
			// done.invoke.<id>._event.data.
			sce.RaiseDoneInvoke("inv_short", child.DonedataAtFinal(), engine)
		}
	}
}

// ForwardToAutoforwardChildren forwards external events to children with autoforward=true (W3C SCXML 6.4.1).
//
// W3C 6.4 requires an exact copy, so the source event's metadata is forwarded
// with the name: the child must observe the same _event.data, _event.origin,
// _event.sendid, _event.origintype and _event.invokeid the parent observed.
//
// The event's name is not consulted. W3C SCXML Appendix D mainEventLoop
// forwards whatever it dequeued from the external queue, and 6.4.2 has
// `done.invoke.<id>` returned onto that queue, so a sibling invoke still
// running must see it. Internal-queue events — `error.*` (3.12.2) and
// `done.state.<id>` (3.7) — stay out of the forwarded set because this
// method is reached only from the external drain, not because of how they
// are spelled.
func (p *AutoforwardDoneInvokePolicy) ForwardToAutoforwardChildren(eventName string, metadata sce.EventMetadata, engine *sce.Engine[AutoforwardDoneInvokeState, AutoforwardDoneInvokeEvent]) {
	if cs, ok := p.activeInvokes["inv_watch"]; ok && cs.Autoforward {
		if p.childInvWatch != nil {
			p.childInvWatch.RaiseExternalByNameWithMeta(eventName, metadata)
		}
	}
}

// DeliverToChildSession delivers an event addressed to a child's published
// location (W3C SCXML C.1).
//
// The parent mints `parentSession.invokeId` for each child and that id is what
// the child's `_ioprocessors` entry names, so a <send> whose target decodes to
// one of them is addressed to that child rather than to this machine. A false
// return means the address names no live child of ours and the event takes the
// normal external path — the routing half of C.1 is what makes the published
// location a usable target rather than a string that merely compares equal.
func (p *AutoforwardDoneInvokePolicy) DeliverToChildSession(childSessionID, eventName, eventData string) bool {
	if childSessionID == "" {
		return false
	}
	if cs, ok := p.activeInvokes["inv_watch"]; ok && cs.SessionID == childSessionID {
		if p.childInvWatch != nil {
			p.childInvWatch.RaiseExternalByName(eventName, eventData)
			return true
		}
	}
	if cs, ok := p.activeInvokes["inv_short"]; ok && cs.SessionID == childSessionID {
		if p.childInvShort != nil {
			p.childInvShort.RaiseExternalByName(eventName, eventData)
			return true
		}
	}
	return false
}


// ── Child engine wrappers (implement sce.ChildEngine interface) ──

type childEngineWrapperInvWatch struct {
	engine *sce.Engine[AutoforwardDoneInvokeSceSynthInvokeInvWatchState, AutoforwardDoneInvokeSceSynthInvokeInvWatchEvent]
	policy *AutoforwardDoneInvokeSceSynthInvokeInvWatchPolicy
}
func (w *childEngineWrapperInvWatch) Initialize() { w.engine.Initialize() }
func (w *childEngineWrapperInvWatch) Tick() { w.engine.Tick() }
func (w *childEngineWrapperInvWatch) IsInFinalState() bool { return w.engine.IsInFinalState() }
func (w *childEngineWrapperInvWatch) RaiseExternalByName(name, data string) { w.engine.RaiseExternalByName(name, data) }
func (w *childEngineWrapperInvWatch) RaiseExternalByNameWithMeta(name string, metadata sce.EventMetadata) { w.engine.RaiseExternalByNameWithMeta(name, metadata) }
func (w *childEngineWrapperInvWatch) SetCompletionCallback(cb func()) { w.engine.SetCompletionCallback(cb) }
func (w *childEngineWrapperInvWatch) GetParentEventQueue() chan sce.ParentEvent { return w.policy.ParentExternalQueue }
func (w *childEngineWrapperInvWatch) DonedataAtFinal() string { return w.engine.DonedataAtFinal() }
type childEngineWrapperInvShort struct {
	engine *sce.Engine[AutoforwardDoneInvokeSceSynthInvokeInvShortState, AutoforwardDoneInvokeSceSynthInvokeInvShortEvent]
	policy *AutoforwardDoneInvokeSceSynthInvokeInvShortPolicy
}
func (w *childEngineWrapperInvShort) Initialize() { w.engine.Initialize() }
func (w *childEngineWrapperInvShort) Tick() { w.engine.Tick() }
func (w *childEngineWrapperInvShort) IsInFinalState() bool { return w.engine.IsInFinalState() }
func (w *childEngineWrapperInvShort) RaiseExternalByName(name, data string) { w.engine.RaiseExternalByName(name, data) }
func (w *childEngineWrapperInvShort) RaiseExternalByNameWithMeta(name string, metadata sce.EventMetadata) { w.engine.RaiseExternalByNameWithMeta(name, metadata) }
func (w *childEngineWrapperInvShort) SetCompletionCallback(cb func()) { w.engine.SetCompletionCallback(cb) }
func (w *childEngineWrapperInvShort) GetParentEventQueue() chan sce.ParentEvent { return w.policy.ParentExternalQueue }
func (w *childEngineWrapperInvShort) DonedataAtFinal() string { return w.engine.DonedataAtFinal() }


// ======================================================================
// StatePolicy interface implementation
// ======================================================================

// InitialState returns the initial state (W3C SCXML 3.3).
func (p *AutoforwardDoneInvokePolicy) InitialState() AutoforwardDoneInvokeState {
	return AutoforwardDoneInvokeStatePhase
}

// IsFinalState returns true if state is a <final> state (W3C SCXML 3.7).
func (p *AutoforwardDoneInvokePolicy) IsFinalState(state AutoforwardDoneInvokeState) bool {
	switch state {
	case AutoforwardDoneInvokeStateFail:
		return true
	case AutoforwardDoneInvokeStatePass:
		return true
	}
	return false
}

// GetParent returns the parent of state, or (zero, false) if root (W3C SCXML 3.3).
func (p *AutoforwardDoneInvokePolicy) GetParent(state AutoforwardDoneInvokeState) (AutoforwardDoneInvokeState, bool) {
	switch state {
	}
	return 0, false
}

// IsCompoundState returns true if state is a <state> with child states — exactly
// the states that have an initial transition. A <parallel> is not compound
// (W3C SCXML 3.3).
func (p *AutoforwardDoneInvokePolicy) IsCompoundState(state AutoforwardDoneInvokeState) bool {
	return len(initialTargetsOfAutoforwardDoneInvoke[state]) > 0
}

func (p *AutoforwardDoneInvokePolicy) IsParallelState(_ AutoforwardDoneInvokeState) bool { return false }

// GetChildStates returns state's <state>, <parallel> and <final> children, in
// document order — for a <parallel>, its regions (§scxml-D-getChildStates).
func (p *AutoforwardDoneInvokePolicy) GetChildStates(state AutoforwardDoneInvokeState) []AutoforwardDoneInvokeState {
	return childStatesOfAutoforwardDoneInvoke[state]
}

// GetInitialTargets returns a compound state's initial transition target, as
// written; the engine's entry procedures dereference a <history> among them
// (W3C SCXML 3.3).
func (p *AutoforwardDoneInvokePolicy) GetInitialTargets(state AutoforwardDoneInvokeState) []AutoforwardDoneInvokeTarget {
	return initialTargetsOfAutoforwardDoneInvoke[state]
}

// GetDocumentInitialTargets returns the target of the document's own initial
// transition, as written (W3C SCXML 3.2).
func (p *AutoforwardDoneInvokePolicy) GetDocumentInitialTargets() []AutoforwardDoneInvokeTarget {
	return documentInitialTargetsOfAutoforwardDoneInvoke
}

// W3C SCXML 3.10: this document declares no <history>, so no target list names
// one and the engine never asks the two below; answering would mean inventing
// one.
func (p *AutoforwardDoneInvokePolicy) GetHistoryParent(history sce.HistoryID) AutoforwardDoneInvokeState {
	panic(fmt.Sprintf("AutoforwardDoneInvokePolicy declares no <history>; asked for %d", history))
}
func (p *AutoforwardDoneInvokePolicy) GetHistoryDefaultTargets(history sce.HistoryID) []AutoforwardDoneInvokeTarget {
	panic(fmt.Sprintf("AutoforwardDoneInvokePolicy declares no <history>; asked for %d", history))
}
func (p *AutoforwardDoneInvokePolicy) HistoryValue(_ sce.HistoryID) ([]AutoforwardDoneInvokeState, bool) {
	return nil, false
}

// GetDocumentOrder returns the document order index (W3C SCXML Appendix D).
func (p *AutoforwardDoneInvokePolicy) GetDocumentOrder(state AutoforwardDoneInvokeState) int {
	switch state {
	case AutoforwardDoneInvokeStateFail:
		return 2
	case AutoforwardDoneInvokeStatePass:
		return 1
	case AutoforwardDoneInvokeStatePhase:
		return 0
	}
	return -1
}

// GetEventName returns the string name of an event (W3C SCXML 3.12).
func (p *AutoforwardDoneInvokePolicy) GetEventName(event AutoforwardDoneInvokeEvent) string {
	return event.String()
}

// GetEventFromName looks up an event by name (W3C SCXML 3.12).
func (p *AutoforwardDoneInvokePolicy) GetEventFromName(name string) (AutoforwardDoneInvokeEvent, bool) {
	switch name {
	case "cancel.invoke":
		return AutoforwardDoneInvokeEventCancelInvoke, true
	case "done.invoke":
		return AutoforwardDoneInvokeEventDoneInvoke, true
	case "done.invoke.inv_short":
		return AutoforwardDoneInvokeEventDoneInvokeInvShort, true
	case "error.execution":
		return AutoforwardDoneInvokeEventErrorExecution, true
	case "probe":
		return AutoforwardDoneInvokeEventProbe, true
	case "sawPlatform":
		return AutoforwardDoneInvokeEventSawPlatform, true
	case "sawProbeOnly":
		return AutoforwardDoneInvokeEventSawProbeOnly, true
	}
	return AutoforwardDoneInvokeEventNull, false
}

// GetStateName returns the string name of a state (W3C SCXML 3.3).
func (p *AutoforwardDoneInvokePolicy) GetStateName(state AutoforwardDoneInvokeState) string {
	return state.String()
}

// GetStateFromName reads a state id back into the state it names (W3C SCXML 3.3).
// The reverse of GetStateName, and what turns a host's recorded configuration
// back into the argument sce.Engine.EnterAt takes.
func (p *AutoforwardDoneInvokePolicy) GetStateFromName(name string) (AutoforwardDoneInvokeState, bool) {
	return AutoforwardDoneInvokeStateFromName(name)
}

// NullEvent returns the sentinel for eventless transition dispatch (W3C SCXML 3.13).
func (p *AutoforwardDoneInvokePolicy) NullEvent() AutoforwardDoneInvokeEvent {
	return AutoforwardDoneInvokeEventNull
}



// HasParallelStates returns whether the SM has parallel states.
func (p *AutoforwardDoneInvokePolicy) HasParallelStates() bool {
	return false
}

// NeedsScriptEngine returns whether the SM needs a script engine.
func (p *AutoforwardDoneInvokePolicy) NeedsScriptEngine() bool {
	return false
}

// NeedsEventScheduler reports whether a host must drive this machine with
// Tick rather than Step alone.
//
// W3C SCXML 6.2: Step runs a macrostep and never drains the delayed-send
// scheduler nor ticks an invoked child, so a host that only ever calls it
// waits forever with nothing said. Same verdict the generate manifest
// publishes as needs_event_scheduler.
func (p *AutoforwardDoneInvokePolicy) NeedsEventScheduler() bool {
	return true
}

// NeedsDataModelInit returns whether the SM needs datamodel init.
func (p *AutoforwardDoneInvokePolicy) NeedsDataModelInit() bool {
	return false
}

// HasInvokeSupport returns true for invoke-capable SMs.
func (p *AutoforwardDoneInvokePolicy) HasInvokeSupport() bool { return true }
// HasChildTick returns true for child-ticking SMs.
func (p *AutoforwardDoneInvokePolicy) HasChildTick() bool { return true }
func (p *AutoforwardDoneInvokePolicy) HasFinalize() bool { return false }
func (p *AutoforwardDoneInvokePolicy) HasAutoforward() bool { return true }
func (p *AutoforwardDoneInvokePolicy) HasActiveStates() bool { return false }
func (p *AutoforwardDoneInvokePolicy) GetActiveStates() []AutoforwardDoneInvokeState { return nil }
// W3C SCXML 3.4: this machine keeps no active set — its configuration is the
// parent walk from the current state — so there is nothing for a restore to
// hand back here. sce.Engine.EnterAt reaches this only through HasActiveStates,
// which is false above; the method exists because the interface is one contract.
func (p *AutoforwardDoneInvokePolicy) SetActiveStates(_ []AutoforwardDoneInvokeState) {}
func (p *AutoforwardDoneInvokePolicy) HasExternalEventFlag() bool { return false }
func (p *AutoforwardDoneInvokePolicy) SetNextEventIsExternal(_ bool) {}
// ExecuteFinalizeForChildEvent is a no-op (no finalize invokes).
func (p *AutoforwardDoneInvokePolicy) ExecuteFinalizeForChildEvent(_ *sce.EventWithMetadata[AutoforwardDoneInvokeEvent], _ *sce.Engine[AutoforwardDoneInvokeState, AutoforwardDoneInvokeEvent]) {}

// PopulateEventMetadata stores pending event metadata (W3C SCXML 5.10).
// Note: event name is set separately via setCurrentEvent(), not from metadata.
func (p *AutoforwardDoneInvokePolicy) PopulateEventMetadata(meta *sce.EventMetadata) {
}

// LiftTypedPayload binds the dequeued event's typed `_event.data` view from
// the data it carries (NL→IR Item C1 Path A).
//
// The typed carrier PopulateEventMetadata reads is filled by one producer, the
// generated Raise<Event> inject seam. Every other producer — <send> with
// <param>, an invoke forwarding either way, autoforward, BasicHTTP, mesh —
// fills EventMetadata.Data, so the schema's fields are read out of that here.
// A refusal is returned for the engine to raise as error.execution, which is
// what W3C SCXML 3.13 gives for a guard that cannot be evaluated.
func (p *AutoforwardDoneInvokePolicy) LiftTypedPayload(event AutoforwardDoneInvokeEvent, meta *sce.EventMetadata) error {
	// This document lowered no typed guard, so no event carries a typed view.
	return nil
}

// ClearEventMetadata resets pending event metadata (W3C SCXML 5.10).
func (p *AutoforwardDoneInvokePolicy) ClearEventMetadata() {
}



// ExecuteEntryActions enters one state (W3C SCXML 3.8): adds it to the
// configuration, runs its <onentry>, and its <initial> transition's content when
// its initial state is entered by default.
//line autoforward_done_invoke.scxml:55
func (p *AutoforwardDoneInvokePolicy) ExecuteEntryActions(state AutoforwardDoneInvokeState, engine *sce.Engine[AutoforwardDoneInvokeState, AutoforwardDoneInvokeEvent], isDefaultEntry bool) {
	switch state {
	case AutoforwardDoneInvokeStatePhase:
		//line autoforward_done_invoke.scxml:58
		// W3C SCXML 6.4: Defer invoke execution until macrostep end
		{
			generatedInvokeID := fmt.Sprintf("%s.%d.inv_watch", "phase", sce.NextInvokeCounter())
			sce.DeferInvoke(&p.pendingInvokes, sce.PendingInvoke[AutoforwardDoneInvokeState]{
				InvokeID: generatedInvokeID,
				State:    AutoforwardDoneInvokeStatePhase,
			})
		}
		{
			generatedInvokeID := fmt.Sprintf("%s.%d.inv_short", "phase", sce.NextInvokeCounter())
			sce.DeferInvoke(&p.pendingInvokes, sce.PendingInvoke[AutoforwardDoneInvokeState]{
				InvokeID: generatedInvokeID,
				State:    AutoforwardDoneInvokeStatePhase,
			})
		}
	default:
		// No entry actions
	}
}

// ExecuteHistoryDefaultContent runs a <history>'s default transition content
// (W3C SCXML 3.10.2), after its parent's onentry (and after the parent's own
// <initial> content) when the history was taken with nothing recorded. The
// engine asks for it by the entry set's defaultHistoryContent answer; a history
// that restored what it recorded runs nothing.
//line autoforward_done_invoke.scxml:55
func (p *AutoforwardDoneInvokePolicy) ExecuteHistoryDefaultContent(history sce.HistoryID, engine *sce.Engine[AutoforwardDoneInvokeState, AutoforwardDoneInvokeEvent]) {
	// W3C SCXML 3.10.2: no <history> in this document has default content.
}

// ExecuteExitActions exits one state (W3C SCXML 3.9): records its histories,
// removes it from the configuration, cancels its invocations and runs its
// <onexit>.
//line autoforward_done_invoke.scxml:55
func (p *AutoforwardDoneInvokePolicy) ExecuteExitActions(state AutoforwardDoneInvokeState, engine *sce.Engine[AutoforwardDoneInvokeState, AutoforwardDoneInvokeEvent], configurationBeforeExit []AutoforwardDoneInvokeState) {
	// W3C SCXML 6.4: Cancel pending invokes and cleanup active children on state exit
	switch state {
	case AutoforwardDoneInvokeStatePhase:
		sce.CancelInvokesForState(&p.pendingInvokes, AutoforwardDoneInvokeStatePhase)
		if p.childInvWatch != nil {
			p.childInvWatch = nil
		}
		delete(p.activeInvokes, "inv_watch")
		p.pendingDoneInvokeInvWatch = false
		if p.childInvShort != nil {
			p.childInvShort = nil
		}
		delete(p.activeInvokes, "inv_short")
		p.pendingDoneInvokeInvShort = false
	default:
	}
	switch state {
	default:
		// No exit actions
	}
}



// BindCurrentEvent binds the event whose transitions are about to be selected as
// the _event their guards read (W3C SCXML 5.10) — before the first guard runs,
// and not for an eventless selection, which has no event of its own.
//line autoforward_done_invoke.scxml:55
func (p *AutoforwardDoneInvokePolicy) BindCurrentEvent(event AutoforwardDoneInvokeEvent, engine *sce.Engine[AutoforwardDoneInvokeState, AutoforwardDoneInvokeEvent]) {
	// This document's guards never read _event, so there is nothing to bind.
}

// FirstEnabledTransition is Appendix D selectTransitions, the half only this
// document can answer: the first of state's own transitions, in document order,
// that event enables and whose guard holds. The engine walks the atomic states
// and their ancestors and keeps the ordered set; the null event asks for
// eventless transitions.
//line autoforward_done_invoke.scxml:55
func (p *AutoforwardDoneInvokePolicy) FirstEnabledTransition(state AutoforwardDoneInvokeState, event AutoforwardDoneInvokeEvent, engine *sce.Engine[AutoforwardDoneInvokeState, AutoforwardDoneInvokeEvent]) (sce.EnabledTransition[AutoforwardDoneInvokeState, sce.HistoryID], bool) {
	switch state {
	case AutoforwardDoneInvokeStatePhase:
		if event == AutoforwardDoneInvokeEventDoneInvokeInvShort {
			{
				return sce.EnabledTransition[AutoforwardDoneInvokeState, sce.HistoryID]{
					Source:          state,
					TransitionIndex: 0,
					HasActions:      true,
					IsInternal:      false,
				}, true
			}
		}
		if event == AutoforwardDoneInvokeEventSawPlatform {
			{
				return sce.EnabledTransition[AutoforwardDoneInvokeState, sce.HistoryID]{
					Source:          state,
					Targets:         transitionTargetsOfAutoforwardDoneInvoke[state][1],
					TransitionIndex: 1,
					HasActions:      false,
					IsInternal:      false,
				}, true
			}
		}
		if event == AutoforwardDoneInvokeEventSawProbeOnly {
			{
				return sce.EnabledTransition[AutoforwardDoneInvokeState, sce.HistoryID]{
					Source:          state,
					Targets:         transitionTargetsOfAutoforwardDoneInvoke[state][2],
					TransitionIndex: 2,
					HasActions:      false,
					IsInternal:      false,
				}, true
			}
		}
		if event == AutoforwardDoneInvokeEventErrorExecution {
			{
				return sce.EnabledTransition[AutoforwardDoneInvokeState, sce.HistoryID]{
					Source:          state,
					Targets:         transitionTargetsOfAutoforwardDoneInvoke[state][3],
					TransitionIndex: 3,
					HasActions:      false,
					IsInternal:      false,
				}, true
			}
		}
	}
	return sce.EnabledTransition[AutoforwardDoneInvokeState, sce.HistoryID]{}, false
}

// ExecuteTransitionContent runs one transition's executable content (W3C SCXML
// 3.13), between the microstep's exits and its entries.
//line autoforward_done_invoke.scxml:55
func (p *AutoforwardDoneInvokePolicy) ExecuteTransitionContent(source AutoforwardDoneInvokeState, transitionIndex int, engine *sce.Engine[AutoforwardDoneInvokeState, AutoforwardDoneInvokeEvent]) {
	switch source {
	case AutoforwardDoneInvokeStatePhase:
		switch transitionIndex {
		case 0:
			//line autoforward_done_invoke.scxml:84

	// W3C SCXML 6.2: send id="__send_0"
	{
		eventDataStr := ""
		_ = eventDataStr
	// W3C SCXML 6.2: External send
	{
		meta := sce.NewEventWithMetadata(AutoforwardDoneInvokeEventProbe)
		meta.Metadata = sce.ExternalMetadata("__send_0", "")
		meta.Metadata.Data = eventDataStr
		engine.RaiseExternalWithMeta(meta)
	}
	}

		}
	}
}
