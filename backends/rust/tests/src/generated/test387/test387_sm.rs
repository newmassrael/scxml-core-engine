// SCE-GENERATED — DO NOT EDIT
// source-hash: b1edd275a200b2f8553040c83495e98b687c11a97259eaf4d60667291dcb916a
// template-hash: 04d657968488f1f11c5b6c78a58b4eab6b99c6cb465480de6bf6cf01d0d597d4
// generated-at: 0

// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) 2025-2026 [Author of input SCXML file]
//
// Generated by SCE Code Generator (Rust backend)
// From: resources/387/test387.scxml
//
// This generated code is MIT licensed and may be freely used in any project.
// Runtime engine dependency (sce-rust-runtime): LGPL-2.1-or-later WITH LicenseRef-SCE-Linking-Exception OR LicenseRef-SCE-Commercial
// Full license: https://github.com/newmassrael/scxml-core-engine/blob/main/LICENSE

//! Generated Rust state machine for `test387`.
//!
//! Generated by SCE Code Generator (Rust backend).
//! Implements `sce_rust_runtime::StatePolicy` with full transition logic,
//! entry/exit actions, and event processing.

// Suppression budget audited 2026-04-15 against the full sce-rust-tests
// W3C suite (202 fixtures). Each allow below corresponds to a lint that
// actually fires on generated code today; see file:line callouts in the
// inline justifications. Adding a new allow here requires reproducing the
// warning on a real fixture and recording it in the same form. Removing an
// allow is safe whenever its justifying pattern stops being emitted.

// Source-faithful identifier names: W3C SCXML 6.4.1 lets users supply
// arbitrary id strings (e.g. `<invoke id="invokedChild">`); preserving the
// camelCase verbatim into Rust field names like `child_invokedChild`
// keeps the generated code traceable back to the SCXML source.
#![allow(non_snake_case)]
// Codegen always emits `use core::time::Duration` and the umbrella
// `use sce_rust_runtime::{Engine, StatePolicy};`; fixtures with no
// `<send delay=...>` and no engine-borrowing actions never reference
// `Duration`/`Engine`, leaving the imports unused.
#![allow(unused_imports)]
// Method parameters such as `_engine: &mut Engine<Self>` and per-state
// fields (`pending_event_invokeid`, `script_engine_initialized`, ...) are
// emitted on every fixture for the StatePolicy trait shape but read only
// by fixtures that actually exercise the corresponding W3C feature.
#![allow(dead_code)]
#![allow(unused_variables)]
// `let mut current_state = ...` in process_transition is mutated only by
// internal-transition fixtures; pure-external-transition fixtures leave it
// untouched after the let binding.
#![allow(unused_mut)]
// `'action_block:` early-exit label wraps every onentry block but is only
// `break`-ed to from fixtures that emit error-on-action sequences.
#![allow(unused_labels)]
// Codegen emits the catch-all `else => {}` arm on every closed-enum match
// for forward compatibility; rustc proves it unreachable once every
// variant is handled.
#![allow(unreachable_patterns)]
#![allow(unreachable_code)]
// W3C SCXML 5.7 donedata pattern: `let mut done_data_ok = true;` then
// per-param `done_data_ok = false;` on failure. Fixtures whose params
// always fail structurally (test298, test343 — empty `location=""`) never
// read the initial `true`.
#![allow(unused_assignments)]
// Generated code is uniform across 200+ fixtures and intentionally avoids
// per-fixture style optimisation passes. The `clippy::style` and
// `clippy::complexity` categories cover suggestions a human author would
// apply by hand (match_like_matches_macro, needless_range_loop,
// type_complexity, single_match, ...) — none of these affect correctness,
// but each one would force the codegen to special-case its output.
// Correctness-relevant clippy categories (`clippy::correctness`,
// `clippy::suspicious`, `clippy::perf`) remain at warn so any genuine bug
// the generator emits still surfaces.
#![allow(clippy::style)]
#![allow(clippy::complexity)]
#![doc = "SCE-MAP: test387.scxml:7 :: _machine"]
// SCE-MAP: test387.scxml:7 :: _machine

use core::time::Duration;
use sce_rust_runtime::{Engine, StatePolicy};

// ======================================================================
// State enum (W3C SCXML 3.3)
// ======================================================================

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Default)]
pub enum Test387State {
    Fail,
    Pass,
    S0,
    S01,
    S011,
    S012,
    S02,
    S021,
    S022,
    S1,
    S11,
    S111,
    S112,
    S12,
    S121,
    S122,
    // W3C SCXML 3.2: `<scxml initial>` state — the machine's `Default`.
    #[default]
    S3,
    S4,
}

// ======================================================================
// Event enum (W3C SCXML 3.12)
// ======================================================================

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum Test387Event {
    EnteringS011,
    EnteringS012,
    EnteringS021,
    EnteringS022,
    EnteringS111,
    EnteringS112,
    EnteringS121,
    EnteringS122,
    ErrorExecution,
    Timeout,
    /// W3C SCXML 3.13: Sentinel for eventless transition dispatch
    Null,
}

// ======================================================================
// Externally-drivable event surface (W3C SCXML 3.12 / 5.10)
// ======================================================================

impl Test387Event {
    /// Events that can arrive from OUTSIDE the machine — external
    /// `<send>` / platform ingress — as opposed to internal `<raise>`
    /// signals and the W3C SCXML 3.13 `Null` sentinel (neither is a
    /// member). This is the machine's structural external trigger
    /// surface: every non-reserved `<transition event>` target minus
    /// every internally-raised event. A consumer that maps event names
    /// back to variants admits only these as externally injectable,
    /// without re-deriving the partition. Structural and consumer-
    /// agnostic; inert when unused. Associated (not a free `const`) so
    /// several machines glob-re-exported into one module never collide
    /// on the name.
    pub const EXTERNALLY_DRIVABLE_EVENTS: &'static [Test387Event] = &[];
}

// ======================================================================
// Policy struct
// ======================================================================

pub struct Test387Policy {
    // W3C SCXML 3.13: Last transition metadata
    last_transition_is_internal: bool,
    last_transition_is_targetless: bool,
    last_transition_source_state: Test387State,
    // W3C SCXML 3.11: History state s0HistDeep (deep)
    //
    // SCE Protocol-Synthesis RFC §synth-5-J-2: the recorded configuration is a
    // [`StateChain`] alias (`Vec<S>` under std, `heapless::Vec<S, MAX_HIERARCHY_DEPTH>`
    // under no_std), mirroring `active_states`. Recorded history is always a
    // subset of the active configuration, so it cannot exceed the depth bound.
    history_s0HistDeep: Option<::sce_rust_runtime::helpers::hierarchy::StateChain<Test387State>>,
    // W3C SCXML 3.11: History state s0HistShallow (shallow)
    //
    // SCE Protocol-Synthesis RFC §synth-5-J-2: the recorded configuration is a
    // [`StateChain`] alias (`Vec<S>` under std, `heapless::Vec<S, MAX_HIERARCHY_DEPTH>`
    // under no_std), mirroring `active_states`. Recorded history is always a
    // subset of the active configuration, so it cannot exceed the depth bound.
    history_s0HistShallow: Option<::sce_rust_runtime::helpers::hierarchy::StateChain<Test387State>>,
    // W3C SCXML 3.11: History state s1HistDeep (deep)
    //
    // SCE Protocol-Synthesis RFC §synth-5-J-2: the recorded configuration is a
    // [`StateChain`] alias (`Vec<S>` under std, `heapless::Vec<S, MAX_HIERARCHY_DEPTH>`
    // under no_std), mirroring `active_states`. Recorded history is always a
    // subset of the active configuration, so it cannot exceed the depth bound.
    history_s1HistDeep: Option<::sce_rust_runtime::helpers::hierarchy::StateChain<Test387State>>,
    // W3C SCXML 3.11: History state s1HistShallow (shallow)
    //
    // SCE Protocol-Synthesis RFC §synth-5-J-2: the recorded configuration is a
    // [`StateChain`] alias (`Vec<S>` under std, `heapless::Vec<S, MAX_HIERARCHY_DEPTH>`
    // under no_std), mirroring `active_states`. Recorded history is always a
    // subset of the active configuration, so it cannot exceed the depth bound.
    history_s1HistShallow: Option<::sce_rust_runtime::helpers::hierarchy::StateChain<Test387State>>,
    // W3C SCXML 5.10: Session ID (script engine + invoke tracking).
    //
    // SCE Protocol-Synthesis RFC §synth-5-J-2: gated to !no_std. Under `--no-std` both the
    // script engine (`codegen/no-std-script-not-supported`) and `<invoke>`
    // (`codegen/no-std-invoke-not-supported`) are codegen-rejected, so no
    // session identity is ever tracked and the alloc-coupled `String` is omitted.
    pub session_id: Option<String>,
    // W3C SCXML 6.4: Parent engine external queue for #_parent send routing
    // Always generated under std — any SM can be invoked as a child. Under
    // `--no-std` (SCE Protocol-Synthesis RFC §synth-5-J-2) the SCXML `<invoke>` element
    // is codegen-rejected, so no parent_external_queue handle is ever
    // wired in, and the Arc<Mutex<...>> (alloc-coupled) is omitted.
    pub parent_external_queue: Option<std::sync::Arc<std::sync::Mutex<Vec<(String, String)>>>>,
    // W3C SCXML 6.4.1: This child's invoke ID (for _event.invokeid in parent).
    // SCE Protocol-Synthesis RFC §synth-5-J-2: gated to !no_std — `<invoke>` is codegen-rejected
    // under no_std, so a machine is never instantiated as a child and this
    // identity is dead. Mirrors the `parent_external_queue` / `invoke` module gate.
    pub invoke_id: String,
    // W3C SCXML 6.5: Child session ID for finalize origin matching.
    pub child_session_id: String,
}

impl Test387Policy {
    pub fn new() -> Self {
        Self {
            last_transition_is_internal: false,
            last_transition_is_targetless: false,
            last_transition_source_state: Test387State::S3,
            history_s0HistDeep: None,
            history_s0HistShallow: None,
            history_s1HistDeep: None,
            history_s1HistShallow: None,
            session_id: None,
            parent_external_queue: None,
            invoke_id: String::new(),
            child_session_id: String::new(),
        }
    }
}

impl Default for Test387Policy {
    fn default() -> Self {
        Self::new()
    }
}

// ======================================================================
// StatePolicy trait implementation
// ======================================================================

impl StatePolicy for Test387Policy {
    type State = Test387State;
    type Event = Test387Event;
    // EventSchema native lowering: `()` = schemaless (dynamic
    // `_event.data` baseline); a `<Machine>Payload` sum is emitted when a
    // transition guard reads a typed `_event.data.<field>` (NL→IR C1 Path A).
    type Payload = ();
    type Hal = sce_rust_runtime::StdHal;

    // W3C SCXML Appendix D internalQueue / externalQueue. Under no_std the
    // depth is the per-document `<scxml sce:capacity>` / deploy value (emitted
    // as the const consumed below); absent a declared capacity the bare form
    // inherits the runtime default `MAX_EVENT_QUEUE_DEPTH` (the single source of
    // the default). Inert under std (`VecDeque`), so this one emission compiles
    // on both runtime profiles.
    type EventQueue = sce_rust_runtime::EventQueueManager<
        sce_rust_runtime::EventWithMetadata<Self::Event, Self::Payload>,
    >;

    // W3C SCXML 6.3: the delayed-send scheduler keeps a per-entry `send_id` only
    // so `<cancel sendid>` can find and drop the matching entry; the timer-fire
    // drain never reads it. A document with no `<cancel>` therefore emits the
    // zero-size `ElidedSendId`, shedding the scheduler ring's per-entry
    // `heapless::String<256>` (~256 B x MAX_SCHEDULED_EVENTS) under no_std --
    // dead weight with no cancel reader. A cancelling document keeps the
    // load-bearing `SceString`. Behaviour-preserving under std either way, so
    // this one emission compiles on both runtime profiles. Mirrors the
    // EventQueue per-machine sizing lever above.
    type ScheduledSendId = sce_rust_runtime::ElidedSendId;

    // W3C SCXML feature flags
    const HAS_PARALLEL_STATES: bool = false;
    const NEEDS_SCRIPT_ENGINE: bool = false;
    const NEEDS_DATA_MODEL_INIT: bool = false;

    // ======================================================================
    // Static metadata methods (W3C SCXML document structure)
    // ======================================================================

    fn initial_state() -> Self::State {
        // The runtime entry state and the type's `Default` derive are one
        // concept, sourced from one computation (`default_state_id`) so
        // they can never diverge: `<scxml initial>`, or the first sorted
        // state when the initial attribute is unset.
        Test387State::S3
    }

    fn is_final_state(state: Self::State) -> bool {
        match state {
            Test387State::Fail => true,
            Test387State::Pass => true,
            _ => false,
        }
    }

    fn get_parent(state: Self::State) -> Option<Self::State> {
        match state {
            Test387State::S01 => Some(Test387State::S0),
            Test387State::S011 => Some(Test387State::S01),
            Test387State::S012 => Some(Test387State::S01),
            Test387State::S02 => Some(Test387State::S0),
            Test387State::S021 => Some(Test387State::S02),
            Test387State::S022 => Some(Test387State::S02),
            Test387State::S11 => Some(Test387State::S1),
            Test387State::S111 => Some(Test387State::S11),
            Test387State::S112 => Some(Test387State::S11),
            Test387State::S12 => Some(Test387State::S1),
            Test387State::S121 => Some(Test387State::S12),
            Test387State::S122 => Some(Test387State::S12),
            _ => None,
        }
    }

    fn is_compound_state(state: Self::State) -> bool {
        match state {
            Test387State::S0 => true,
            Test387State::S01 => true,
            Test387State::S02 => true,
            Test387State::S1 => true,
            Test387State::S11 => true,
            Test387State::S12 => true,
            _ => false,
        }
    }

    fn is_descendant_of(desc: Self::State, anc: Self::State) -> bool {
        let mut current = desc;
        loop {
            match Self::get_parent(current) {
                None => return false,
                Some(parent) => {
                    if parent == anc {
                        return true;
                    }
                    current = parent;
                }
            }
        }
    }

    fn get_document_order(state: Self::State) -> u32 {
        match state {
            Test387State::Fail => 17,
            Test387State::Pass => 16,
            Test387State::S0 => 0,
            Test387State::S01 => 1,
            Test387State::S011 => 2,
            Test387State::S012 => 3,
            Test387State::S02 => 4,
            Test387State::S021 => 5,
            Test387State::S022 => 6,
            Test387State::S1 => 7,
            Test387State::S11 => 8,
            Test387State::S111 => 9,
            Test387State::S112 => 10,
            Test387State::S12 => 11,
            Test387State::S121 => 12,
            Test387State::S122 => 13,
            Test387State::S3 => 14,
            Test387State::S4 => 15,
        }
    }

    fn get_event_name(event: Self::Event) -> &'static str {
        match event {
            Test387Event::EnteringS011 => "enteringS011",
            Test387Event::EnteringS012 => "enteringS012",
            Test387Event::EnteringS021 => "enteringS021",
            Test387Event::EnteringS022 => "enteringS022",
            Test387Event::EnteringS111 => "enteringS111",
            Test387Event::EnteringS112 => "enteringS112",
            Test387Event::EnteringS121 => "enteringS121",
            Test387Event::EnteringS122 => "enteringS122",
            Test387Event::ErrorExecution => "error.execution",
            Test387Event::Timeout => "timeout",
            Test387Event::Null => "",
        }
    }

    fn get_event_from_name(name: &str) -> Option<Self::Event> {
        match name {
            "enteringS011" => Some(Test387Event::EnteringS011),
            "enteringS012" => Some(Test387Event::EnteringS012),
            "enteringS021" => Some(Test387Event::EnteringS021),
            "enteringS022" => Some(Test387Event::EnteringS022),
            "enteringS111" => Some(Test387Event::EnteringS111),
            "enteringS112" => Some(Test387Event::EnteringS112),
            "enteringS121" => Some(Test387Event::EnteringS121),
            "enteringS122" => Some(Test387Event::EnteringS122),
            "error.execution" => Some(Test387Event::ErrorExecution),
            "timeout" => Some(Test387Event::Timeout),
            _ => None,
        }
    }

    fn get_state_name(state: Self::State) -> &'static str {
        match state {
            Test387State::Fail => "fail",
            Test387State::Pass => "pass",
            Test387State::S0 => "s0",
            Test387State::S01 => "s01",
            Test387State::S011 => "s011",
            Test387State::S012 => "s012",
            Test387State::S02 => "s02",
            Test387State::S021 => "s021",
            Test387State::S022 => "s022",
            Test387State::S1 => "s1",
            Test387State::S11 => "s11",
            Test387State::S111 => "s111",
            Test387State::S112 => "s112",
            Test387State::S12 => "s12",
            Test387State::S121 => "s121",
            Test387State::S122 => "s122",
            Test387State::S3 => "s3",
            Test387State::S4 => "s4",
        }
    }

    fn null_event() -> Self::Event {
        Test387Event::Null
    }

    // W3C SCXML 3.6: Get initial children of a compound state
    //
    // SCE Protocol-Synthesis RFC §synth-5-J-2: return type is the runtime crate's
    // [`StateChain`] alias and the body uses `state_chain_from_slice` instead of
    // `vec![...]` so the emitted code compiles under `--no-std` (`vec!` is a
    // std-only macro; heapless has no equivalent).
    fn get_initial_children(
        state: Self::State,
    ) -> ::sce_rust_runtime::helpers::hierarchy::StateChain<Self::State> {
        match state {
            Test387State::S0 => {
                ::sce_rust_runtime::helpers::hierarchy::state_chain_from_slice([Test387State::S01])
            }
            Test387State::S01 => {
                ::sce_rust_runtime::helpers::hierarchy::state_chain_from_slice([Test387State::S011])
            }
            Test387State::S02 => {
                ::sce_rust_runtime::helpers::hierarchy::state_chain_from_slice([Test387State::S021])
            }
            Test387State::S1 => {
                ::sce_rust_runtime::helpers::hierarchy::state_chain_from_slice([Test387State::S11])
            }
            Test387State::S11 => {
                ::sce_rust_runtime::helpers::hierarchy::state_chain_from_slice([Test387State::S111])
            }
            Test387State::S12 => {
                ::sce_rust_runtime::helpers::hierarchy::state_chain_from_slice([Test387State::S121])
            }
            _ => ::sce_rust_runtime::helpers::hierarchy::new_chain(),
        }
    }

    // W3C SCXML 3.11: Get initial or history-restored child
    fn get_initial_or_history_child(&self, state: Self::State) -> Self::State {
        match state {
            Test387State::S0 => Test387State::S01,
            Test387State::S01 => Test387State::S011,
            Test387State::S02 => Test387State::S021,
            Test387State::S1 => Test387State::S11,
            Test387State::S11 => Test387State::S111,
            Test387State::S12 => Test387State::S121,
            _ => state,
        }
    }

    // ======================================================================
    // Mutable field accessors
    // ======================================================================

    fn last_transition_is_internal(&self) -> bool {
        self.last_transition_is_internal
    }

    fn set_last_transition_is_internal(&mut self, value: bool) {
        self.last_transition_is_internal = value;
    }

    fn last_transition_is_targetless(&self) -> bool {
        self.last_transition_is_targetless
    }

    fn set_last_transition_is_targetless(&mut self, value: bool) {
        self.last_transition_is_targetless = value;
    }

    fn last_transition_source_state(&self) -> Self::State {
        self.last_transition_source_state
    }

    fn set_last_transition_source_state(&mut self, state: Self::State) {
        self.last_transition_source_state = state;
    }

    // ======================================================================
    // Instance methods - generated executable content
    // ======================================================================

    // W3C SCXML 3.7: Execute <onentry> actions for a state
    #[doc = "SCE-MAP: test387.scxml:7 :: _machine"]
    // SCE-MAP: test387.scxml:7 :: _machine
    fn execute_entry_actions(
        &mut self,
        state: Self::State,
        engine: &mut sce_rust_runtime::Engine<Self>,
    ) {
        match state {
            Test387State::S011 => {
                // SCE-MAP: test387.scxml:22 :: s011 :: _state_body
                // W3C SCXML 3.8: onentry block 1/1
                // Labeled block allows actions to break out on error (W3C 3.8: error stops block)
                'action_block: {
                    // W3C SCXML 3.8.1: <raise event="enteringS011">
                    engine.raise(sce_rust_runtime::EventWithMetadata::new(
                        Test387Event::EnteringS011,
                    ));
                }
            }
            Test387State::S012 => {
                // SCE-MAP: test387.scxml:27 :: s012 :: _state_body
                // W3C SCXML 3.8: onentry block 1/1
                // Labeled block allows actions to break out on error (W3C 3.8: error stops block)
                'action_block: {
                    // W3C SCXML 3.8.1: <raise event="enteringS012">
                    engine.raise(sce_rust_runtime::EventWithMetadata::new(
                        Test387Event::EnteringS012,
                    ));
                }
            }
            Test387State::S021 => {
                // SCE-MAP: test387.scxml:34 :: s021 :: _state_body
                // W3C SCXML 3.8: onentry block 1/1
                // Labeled block allows actions to break out on error (W3C 3.8: error stops block)
                'action_block: {
                    // W3C SCXML 3.8.1: <raise event="enteringS021">
                    engine.raise(sce_rust_runtime::EventWithMetadata::new(
                        Test387Event::EnteringS021,
                    ));
                }
            }
            Test387State::S022 => {
                // SCE-MAP: test387.scxml:39 :: s022 :: _state_body
                // W3C SCXML 3.8: onentry block 1/1
                // Labeled block allows actions to break out on error (W3C 3.8: error stops block)
                'action_block: {
                    // W3C SCXML 3.8.1: <raise event="enteringS022">
                    engine.raise(sce_rust_runtime::EventWithMetadata::new(
                        Test387Event::EnteringS022,
                    ));
                }
            }
            Test387State::S111 => {
                // SCE-MAP: test387.scxml:60 :: s111 :: _state_body
                // W3C SCXML 3.8: onentry block 1/1
                // Labeled block allows actions to break out on error (W3C 3.8: error stops block)
                'action_block: {
                    // W3C SCXML 3.8.1: <raise event="enteringS111">
                    engine.raise(sce_rust_runtime::EventWithMetadata::new(
                        Test387Event::EnteringS111,
                    ));
                }
            }
            Test387State::S112 => {
                // SCE-MAP: test387.scxml:65 :: s112 :: _state_body
                // W3C SCXML 3.8: onentry block 1/1
                // Labeled block allows actions to break out on error (W3C 3.8: error stops block)
                'action_block: {
                    // W3C SCXML 3.8.1: <raise event="enteringS112">
                    engine.raise(sce_rust_runtime::EventWithMetadata::new(
                        Test387Event::EnteringS112,
                    ));
                }
            }
            Test387State::S121 => {
                // SCE-MAP: test387.scxml:72 :: s121 :: _state_body
                // W3C SCXML 3.8: onentry block 1/1
                // Labeled block allows actions to break out on error (W3C 3.8: error stops block)
                'action_block: {
                    // W3C SCXML 3.8.1: <raise event="enteringS121">
                    engine.raise(sce_rust_runtime::EventWithMetadata::new(
                        Test387Event::EnteringS121,
                    ));
                }
            }
            Test387State::S122 => {
                // SCE-MAP: test387.scxml:77 :: s122 :: _state_body
                // W3C SCXML 3.8: onentry block 1/1
                // Labeled block allows actions to break out on error (W3C 3.8: error stops block)
                'action_block: {
                    // W3C SCXML 3.8.1: <raise event="enteringS122">
                    engine.raise(sce_rust_runtime::EventWithMetadata::new(
                        Test387Event::EnteringS122,
                    ));
                }
            }
            Test387State::S3 => {
                // SCE-MAP: test387.scxml:87 :: s3 :: _state_body
                // W3C SCXML 3.8: onentry block 1/1
                // Labeled block allows actions to break out on error (W3C 3.8: error stops block)
                'action_block: {
                    {
                        let send_id = ::sce_rust_runtime::sce_string_from_str("__send_0");

                        let event_data: &str = "";

                        // W3C SCXML 6.2: Delayed send (1000ms)
                        engine.schedule_event(
                            Test387Event::Timeout,
                            core::time::Duration::from_millis(1000),
                            &send_id,
                            event_data,
                        );

                        let _ = send_id; // suppress unused warning when no send operation
                        let _ = event_data; // suppress unused warning in branches that skip dispatch
                    }
                }
            }
            _ => {}
        }
    }

    // W3C SCXML 3.8: Execute <onexit> actions for a state
    #[doc = "SCE-MAP: test387.scxml:7 :: _machine"]
    // SCE-MAP: test387.scxml:7 :: _machine
    fn execute_exit_actions(
        &mut self,
        state: Self::State,
        engine: &mut sce_rust_runtime::Engine<Self>,
        pre_transition_active: &[Self::State],
    ) {
        // W3C SCXML 3.11: Record history before exiting compound states
        if state == Test387State::S0 {
            // W3C SCXML 3.11: Deep history - record all active descendants.
            // SCE Protocol-Synthesis RFC §synth-5-J-2: [`StateChain`] alias (heapless under
            // no_std); recorded history is a subset of the active configuration
            // so it stays within MAX_HIERARCHY_DEPTH.
            let filtered: ::sce_rust_runtime::helpers::hierarchy::StateChain<Self::State> =
                pre_transition_active
                    .iter()
                    .filter(|&&s| {
                        Self::is_descendant_of(s, Test387State::S0)
                            && !Self::is_compound_state(s)
                            && !Self::is_parallel_state(s)
                    })
                    .copied()
                    .collect();
            self.history_s0HistDeep = Some(filtered);
        }
        if state == Test387State::S0 {
            // W3C SCXML 3.11: Shallow history - record direct children only.
            // SCE Protocol-Synthesis RFC §synth-5-J-2: [`StateChain`] alias (heapless under no_std).
            let filtered: ::sce_rust_runtime::helpers::hierarchy::StateChain<Self::State> =
                pre_transition_active
                    .iter()
                    .filter(|&&s| Self::get_parent(s) == Some(Test387State::S0))
                    .copied()
                    .collect();
            self.history_s0HistShallow = Some(filtered);
        }
        if state == Test387State::S1 {
            // W3C SCXML 3.11: Deep history - record all active descendants.
            // SCE Protocol-Synthesis RFC §synth-5-J-2: [`StateChain`] alias (heapless under
            // no_std); recorded history is a subset of the active configuration
            // so it stays within MAX_HIERARCHY_DEPTH.
            let filtered: ::sce_rust_runtime::helpers::hierarchy::StateChain<Self::State> =
                pre_transition_active
                    .iter()
                    .filter(|&&s| {
                        Self::is_descendant_of(s, Test387State::S1)
                            && !Self::is_compound_state(s)
                            && !Self::is_parallel_state(s)
                    })
                    .copied()
                    .collect();
            self.history_s1HistDeep = Some(filtered);
        }
        if state == Test387State::S1 {
            // W3C SCXML 3.11: Shallow history - record direct children only.
            // SCE Protocol-Synthesis RFC §synth-5-J-2: [`StateChain`] alias (heapless under no_std).
            let filtered: ::sce_rust_runtime::helpers::hierarchy::StateChain<Self::State> =
                pre_transition_active
                    .iter()
                    .filter(|&&s| Self::get_parent(s) == Some(Test387State::S1))
                    .copied()
                    .collect();
            self.history_s1HistShallow = Some(filtered);
        }
    }

    // W3C SCXML 3.13: Evaluate guards and take a matching transition
    #[doc = "SCE-MAP: test387.scxml:7 :: _machine"]
    // SCE-MAP: test387.scxml:7 :: _machine
    fn process_transition(
        &mut self,
        current_state: &mut Self::State,
        event: Self::Event,
        engine: &mut sce_rust_runtime::Engine<Self>,
    ) -> bool {
        let mut transition_taken = false;

        // W3C SCXML 3.12: Hierarchical event processing (innermost to outermost)
        let mut check_state = *current_state;

        loop {
            let found = self.try_transition_in_state(
                check_state,
                event,
                current_state,
                &mut transition_taken,
                engine,
            );

            if found {
                break;
            }

            // W3C SCXML 3.13: Eventless transitions do NOT bubble to parent states
            if event == Self::null_event() {
                break;
            }

            // W3C SCXML 3.12: Move to parent state for hierarchical event bubbling
            match Self::get_parent(check_state) {
                Some(parent) => check_state = parent,
                None => break,
            }
        }

        transition_taken
    }

    // W3C SCXML 3.13: Execute transition actions (called between exit and entry)
    #[doc = "SCE-MAP: test387.scxml:7 :: _machine"]
    // SCE-MAP: test387.scxml:7 :: _machine
    fn execute_transition_actions(&mut self, engine: &mut sce_rust_runtime::Engine<Self>) {
        // W3C SCXML 3.13: No transition actions in this state machine
        let _ = engine;
    }
}

// ======================================================================
// Helper impl block (try_transition_in_state, conflict resolution, etc.)
// ======================================================================

impl Test387Policy {
    // W3C SCXML 3.12: Helper method for hierarchical transition checking
    fn try_transition_in_state(
        &mut self,
        check_state: Test387State,
        event: Test387Event,
        current_state: &mut Test387State,
        transition_taken: &mut bool,
        engine: &mut sce_rust_runtime::Engine<Self>,
    ) -> bool {
        match check_state {
            Test387State::Fail => false,
            Test387State::Pass => false,
            Test387State::S0 => {
                // W3C SCXML 3.12: Event-triggered transitions (document order)
                // W3C SCXML 5.9.3: Direct enum comparison
                if event == Test387Event::EnteringS011 {
                    // W3C SCXML 3.4: Track transition metadata
                    self.last_transition_source_state = check_state;
                    self.last_transition_is_internal = false;
                    self.last_transition_is_targetless = false;

                    *current_state = Test387State::S4;
                    *transition_taken = true;
                    return true;
                }
                // W3C SCXML 5.9.3: Runtime event descriptor matching
                if event != Test387Event::Null
                    && sce_rust_runtime::helpers::event_matching::matches_event_descriptor(
                        Self::get_event_name(event),
                        "*",
                    )
                {
                    // W3C SCXML 3.4: Track transition metadata
                    self.last_transition_source_state = check_state;
                    self.last_transition_is_internal = false;
                    self.last_transition_is_targetless = false;

                    *current_state = Test387State::Fail;
                    *transition_taken = true;
                    return true;
                }
                false
            }
            Test387State::S01 => false,
            Test387State::S011 => false,
            Test387State::S012 => false,
            Test387State::S02 => false,
            Test387State::S021 => false,
            Test387State::S022 => false,
            Test387State::S1 => {
                // W3C SCXML 3.12: Event-triggered transitions (document order)
                // W3C SCXML 5.9.3: Direct enum comparison
                if event == Test387Event::EnteringS122 {
                    // W3C SCXML 3.4: Track transition metadata
                    self.last_transition_source_state = check_state;
                    self.last_transition_is_internal = false;
                    self.last_transition_is_targetless = false;

                    *current_state = Test387State::Pass;
                    *transition_taken = true;
                    return true;
                }
                // W3C SCXML 5.9.3: Runtime event descriptor matching
                if event != Test387Event::Null
                    && sce_rust_runtime::helpers::event_matching::matches_event_descriptor(
                        Self::get_event_name(event),
                        "*",
                    )
                {
                    // W3C SCXML 3.4: Track transition metadata
                    self.last_transition_source_state = check_state;
                    self.last_transition_is_internal = false;
                    self.last_transition_is_targetless = false;

                    *current_state = Test387State::Fail;
                    *transition_taken = true;
                    return true;
                }
                false
            }
            Test387State::S11 => false,
            Test387State::S111 => false,
            Test387State::S112 => false,
            Test387State::S12 => false,
            Test387State::S121 => false,
            Test387State::S122 => false,
            Test387State::S3 => {
                // W3C SCXML 3.13: Eventless transitions
                if event == Test387Event::Null {
                    // Unconditional eventless transition
                    self.last_transition_source_state = check_state;
                    self.last_transition_is_internal = false;
                    self.last_transition_is_targetless = false;
                    *current_state = self.resolve_history_target_s0HistShallow();
                    *transition_taken = true;
                    return true;
                }
                false
            }
            Test387State::S4 => {
                // W3C SCXML 3.13: Eventless transitions
                if event == Test387Event::Null {
                    // Unconditional eventless transition
                    self.last_transition_source_state = check_state;
                    self.last_transition_is_internal = false;
                    self.last_transition_is_targetless = false;
                    *current_state = self.resolve_history_target_s1HistDeep();
                    *transition_taken = true;
                    return true;
                }
                false
            }
            _ => false,
        }
    }

    // W3C SCXML 3.11: Resolve history target for s0HistDeep
    fn resolve_history_target_s0HistDeep(&self) -> Test387State {
        if let Some(ref recorded) = self.history_s0HistDeep {
            if let Some(&first) = recorded.first() {
                return first;
            }
        }
        // No history recorded - use default transition target
        Test387State::S022
    }
    // W3C SCXML 3.11: Resolve history target for s0HistShallow
    fn resolve_history_target_s0HistShallow(&self) -> Test387State {
        if let Some(ref recorded) = self.history_s0HistShallow {
            if let Some(&first) = recorded.first() {
                return first;
            }
        }
        // No history recorded - use default transition target
        Test387State::S011
    }
    // W3C SCXML 3.11: Resolve history target for s1HistDeep
    fn resolve_history_target_s1HistDeep(&self) -> Test387State {
        if let Some(ref recorded) = self.history_s1HistDeep {
            if let Some(&first) = recorded.first() {
                return first;
            }
        }
        // No history recorded - use default transition target
        Test387State::S122
    }
    // W3C SCXML 3.11: Resolve history target for s1HistShallow
    fn resolve_history_target_s1HistShallow(&self) -> Test387State {
        if let Some(ref recorded) = self.history_s1HistShallow {
            if let Some(&first) = recorded.first() {
                return first;
            }
        }
        // No history recorded - use default transition target
        Test387State::S111
    }
}
