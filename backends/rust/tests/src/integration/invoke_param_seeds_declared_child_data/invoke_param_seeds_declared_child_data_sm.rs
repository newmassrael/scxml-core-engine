// SCE-GENERATED — DO NOT EDIT
// source-hash: a9b3d7b7ea8a5bd6001a98d04817a6efb870e7f83add64eb3bb769017877144d

// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) 2025-2026 [Author of input SCXML file]
//
// Generated by SCE Code Generator (Rust backend)
// From: integration_resources/invoke_param_seeds_declared_child_data/invoke_param_seeds_declared_child_data.scxml
//
// This generated code is MIT licensed and may be freely used in any project.
// Runtime engine dependency (sce-rust-runtime): LGPL-2.1-or-later WITH LicenseRef-SCE-Linking-Exception OR LicenseRef-SCE-Commercial
// Full license: https://github.com/newmassrael/scxml-core-engine/blob/main/LICENSE

//! Generated Rust state machine for `invoke_param_seeds_declared_child_data`.
//!
//! Generated by SCE Code Generator (Rust backend).
//! Implements `sce_rust_runtime::StatePolicy` with full transition logic,
//! entry/exit actions, and event processing.

// Suppression budget audited 2026-04-15 against the full sce-rust-tests
// W3C suite (202 fixtures). Each allow below corresponds to a lint that
// actually fires on generated code today; see file:line callouts in the
// inline justifications. Adding a new allow here requires reproducing the
// warning on a real fixture and recording it in the same form. Removing an
// allow is safe whenever its justifying pattern stops being emitted.

// Source-faithful identifier names: W3C SCXML 6.4.1 lets users supply
// arbitrary id strings (e.g. `<invoke id="invokedChild">`); preserving the
// camelCase verbatim into Rust field names like `child_invokedChild`
// keeps the generated code traceable back to the SCXML source.
#![allow(non_snake_case)]
// Codegen always emits `use core::time::Duration` and the umbrella
// `use sce_rust_runtime::{Engine, StatePolicy};`; fixtures with no
// `<send delay=...>` and no engine-borrowing actions never reference
// `Duration`/`Engine`, leaving the imports unused.
#![allow(unused_imports)]
// Method parameters such as `_engine: &mut Engine<Self>` and per-state
// fields (`pending_event_invokeid`, `script_engine_initialized`, ...) are
// emitted on every fixture for the StatePolicy trait shape but read only
// by fixtures that actually exercise the corresponding W3C feature.
#![allow(dead_code)]
#![allow(unused_variables)]
// Bindings emitted `mut` for the documents that write them: `done_data_ok` in
// `<donedata>` evaluation is cleared only by a param that fails (test294,
// test343), `json_parts` is pushed only by a param that evaluates (test298),
// and a send's `err_meta` gets a `send_id` only when the document has a
// datamodel (test194, test199). Measured 2026-09-24: removing this allow
// reds those fixtures under `-D warnings`.
#![allow(unused_mut)]
// `'action_block:` early-exit label wraps every onentry block but is only
// `break`-ed to from fixtures that emit error-on-action sequences.
#![allow(unused_labels)]
// Codegen emits the catch-all `else => {}` arm on every closed-enum match
// for forward compatibility; rustc proves it unreachable once every
// variant is handled.
#![allow(unreachable_patterns)]
#![allow(unreachable_code)]
// W3C SCXML 5.7 donedata pattern: `let mut done_data_ok = true;` then
// per-param `done_data_ok = false;` on failure. Fixtures whose params
// always fail structurally (test298, test343 — empty `location=""`) never
// read the initial `true`.
#![allow(unused_assignments)]
// Generated code is uniform across 200+ fixtures and intentionally avoids
// per-fixture style optimisation passes. The `clippy::style` and
// `clippy::complexity` categories cover suggestions a human author would
// apply by hand (match_like_matches_macro, needless_range_loop,
// type_complexity, single_match, ...) — none of these affect correctness,
// but each one would force the codegen to special-case its output.
// Correctness-relevant clippy categories (`clippy::correctness`,
// `clippy::suspicious`, `clippy::perf`) remain at warn so any genuine bug
// the generator emits still surfaces.
#![allow(clippy::style)]
#![allow(clippy::complexity)]
#![doc = "SCE-MAP: invoke_param_seeds_declared_child_data.scxml:84 :: _machine"]
// SCE-MAP: invoke_param_seeds_declared_child_data.scxml:84 :: _machine

use core::time::Duration;
use sce_rust_runtime::{Engine, StatePolicy};

// ======================================================================
// State enum (W3C SCXML 3.3)
// ======================================================================

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Default)]
pub enum InvokeParamSeedsDeclaredChildDataState {
    FailChildEvaluatedTheExpression,
    FailDeclaredParamLost,
    FailInfiniteParamCollapsed,
    FailInfiniteParamLost,
    FailNamelistValueLost,
    FailParentOnlyExprLost,
    FailShadowSeedLost,
    FailUnmatchedParamEnteredTheChild,
    Infinite,
    NamelistPhase,
    Pass,
    // W3C SCXML 3.2: `<scxml initial>` state — the machine's `Default`.
    #[default]
    Shadowed,
    SoleName,
    Unmatched,
}

// ======================================================================
// Event enum (W3C SCXML 3.12)
// ======================================================================

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum InvokeParamSeedsDeclaredChildDataEvent {
    DoneInvoke,
    ErrorExecution,
    SeedCollapsed,
    SeedLeaked,
    SeedMissing,
    SeedOk,
    SeedShadowed,
    /// W3C SCXML 3.13: Sentinel for eventless transition dispatch
    Null,
}

// ======================================================================
// Externally-drivable event surface (W3C SCXML 3.12 / 5.10)
// ======================================================================

impl InvokeParamSeedsDeclaredChildDataEvent {
    /// Events that can arrive from OUTSIDE the machine — external
    /// `<send>` / platform ingress — as opposed to internal `<raise>`
    /// signals and the W3C SCXML 3.13 `Null` sentinel (neither is a
    /// member). This is the machine's structural external trigger
    /// surface: every non-reserved `<transition event>` target minus
    /// every internally-raised event. A consumer that maps event names
    /// back to variants admits only these as externally injectable,
    /// without re-deriving the partition. Structural and consumer-
    /// agnostic; inert when unused. Associated (not a free `const`) so
    /// several machines glob-re-exported into one module never collide
    /// on the name.
    pub const EXTERNALLY_DRIVABLE_EVENTS: &'static [InvokeParamSeedsDeclaredChildDataEvent] = &[
        InvokeParamSeedsDeclaredChildDataEvent::SeedCollapsed,
        InvokeParamSeedsDeclaredChildDataEvent::SeedLeaked,
        InvokeParamSeedsDeclaredChildDataEvent::SeedMissing,
        InvokeParamSeedsDeclaredChildDataEvent::SeedOk,
        InvokeParamSeedsDeclaredChildDataEvent::SeedShadowed,
    ];
}

// ======================================================================
// Policy struct
// ======================================================================

pub struct InvokeParamSeedsDeclaredChildDataPolicy {
    // W3C SCXML 5.10: Event name for _event.name binding
    //
    // SCE Protocol-Synthesis RFC §synth-5-J-2: typed as the runtime crate's [`SceString`]
    // alias — `String` under std (unchanged ABI), `heapless::String<MAX_EVENT_STRING_LEN>`
    // under no_std — so the emitted field is allocator-free on MCU targets.
    pending_event_name: ::sce_rust_runtime::SceString,
    // W3C SCXML 5.10: Event data for the script `_event.data` baseline (the
    // typed path reads the Payload sum, not this field — so needs_script_engine).
    pending_event_data: ::sce_rust_runtime::SceString,
    // W3C SCXML 5.10.1: Event type for _event.type binding
    pending_event_type: ::sce_rust_runtime::SceString,
    // W3C SCXML 5.10.1: Event sendid for _event.sendid binding
    pending_event_sendid: ::sce_rust_runtime::SceString,
    // W3C SCXML 5.10.1: Event origin for _event.origin binding
    pending_event_origin: ::sce_rust_runtime::SceString,
    // W3C SCXML 5.10.1: Event origintype for _event.origintype binding
    pending_event_origintype: ::sce_rust_runtime::SceString,
    // W3C SCXML 5.10.1: Event invokeid for _event.invokeid binding
    pending_event_invokeid: ::sce_rust_runtime::SceString,
    // W3C SCXML 5.10: Session ID (script engine + invoke tracking).
    //
    // SCE Protocol-Synthesis RFC §synth-5-J-2: gated to !no_std. Under `--no-std` both the
    // script engine (`codegen/no-std-script-not-supported`) and `<invoke>`
    // (`codegen/no-std-invoke-not-supported`) are codegen-rejected, so no
    // session identity is ever tracked and the alloc-coupled `String` is omitted.
    pub session_id: Option<String>,
    // Engine DI Parity RFC (Path B+): per-instance script engine. The
    // constructor parameter is mandatory whenever `model.needs_script_engine`
    // is true, mirroring the Kotlin `StateMachineEngine(scriptEngine)` shape.
    pub script_engine: std::sync::Arc<dyn sce_rust_runtime::IScriptEngine>,
    script_engine_initialized: bool,
    // §scxml-C-2-3: inbound BasicHTTP endpoint serving this machine, declared
    // by the deployment before initialize(). Empty means no such endpoint is
    // deployed, and no BasicHTTP entry is published in `_ioprocessors`.
    basic_http_access_uri: String,
    // W3C SCXML 6.4: Pending invoke queue (deferred until macrostep end).
    // §scxml-6.4.1 unsupported-type invokes ride it too — their execute step
    // raises error.execution instead of starting a session.
    pending_invokes: Vec<sce_rust_runtime::invoke::PendingInvoke<InvokeParamSeedsDeclaredChildDataState>>,
    // W3C SCXML 6.4: Active child sessions (invoke_id -> ChildSession)
    active_invokes: std::collections::HashMap<String, sce_rust_runtime::invoke::ChildSession>,
    // W3C SCXML 6.4: Static invoke child 'inv_shadow' (invoke_param_seeds_declared_child_data__sce_synth_invoke__inv_shadow)
    child_inv_shadow: Option<Box<sce_rust_runtime::Engine<super::invoke_param_seeds_declared_child_data__sce_synth_invoke__inv_shadow_sm::InvokeParamSeedsDeclaredChildDataSceSynthInvokeInvShadowPolicy>>>,
    pending_done_invoke_inv_shadow: bool,
    // W3C SCXML 6.4: Static invoke child 'inv_sole' (invoke_param_seeds_declared_child_data__sce_synth_invoke__inv_sole)
    child_inv_sole: Option<Box<sce_rust_runtime::Engine<super::invoke_param_seeds_declared_child_data__sce_synth_invoke__inv_sole_sm::InvokeParamSeedsDeclaredChildDataSceSynthInvokeInvSolePolicy>>>,
    pending_done_invoke_inv_sole: bool,
    // W3C SCXML 6.4: Static invoke child 'inv_unmatched' (invoke_param_seeds_declared_child_data__sce_synth_invoke__inv_unmatched)
    child_inv_unmatched: Option<Box<sce_rust_runtime::Engine<super::invoke_param_seeds_declared_child_data__sce_synth_invoke__inv_unmatched_sm::InvokeParamSeedsDeclaredChildDataSceSynthInvokeInvUnmatchedPolicy>>>,
    pending_done_invoke_inv_unmatched: bool,
    // W3C SCXML 6.4: Static invoke child 'inv_namelist' (invoke_param_seeds_declared_child_data__sce_synth_invoke__inv_namelist)
    child_inv_namelist: Option<Box<sce_rust_runtime::Engine<super::invoke_param_seeds_declared_child_data__sce_synth_invoke__inv_namelist_sm::InvokeParamSeedsDeclaredChildDataSceSynthInvokeInvNamelistPolicy>>>,
    pending_done_invoke_inv_namelist: bool,
    // W3C SCXML 6.4: Static invoke child 'inv_infinite' (invoke_param_seeds_declared_child_data__sce_synth_invoke__inv_infinite)
    child_inv_infinite: Option<Box<sce_rust_runtime::Engine<super::invoke_param_seeds_declared_child_data__sce_synth_invoke__inv_infinite_sm::InvokeParamSeedsDeclaredChildDataSceSynthInvokeInvInfinitePolicy>>>,
    pending_done_invoke_inv_infinite: bool,
    // W3C SCXML 6.4: Parent engine external queue for #_parent send routing
    // Always generated under std — any SM can be invoked as a child. Under
    // `--no-std` (SCE Protocol-Synthesis RFC §synth-5-J-2) the SCXML `<invoke>` element
    // is codegen-rejected, so no parent_external_queue handle is ever
    // wired in, and the Arc<Mutex<...>> (alloc-coupled) is omitted.
    pub parent_external_queue: Option<std::sync::Arc<std::sync::Mutex<Vec<(String, String)>>>>,
    // W3C SCXML 6.4.1: This child's invoke ID (for _event.invokeid in parent).
    // SCE Protocol-Synthesis RFC §synth-5-J-2: gated to !no_std — `<invoke>` is codegen-rejected
    // under no_std, so a machine is never instantiated as a child and this
    // identity is dead. Mirrors the `parent_external_queue` / `invoke` module gate.
    pub invoke_id: String,
    // W3C SCXML 6.5: Child session ID for finalize origin matching.
    pub child_session_id: String,
}

impl InvokeParamSeedsDeclaredChildDataPolicy {
    /// §scxml-5.3: what the `token` datamodel variable is holding now.
    ///
    /// The live value, not the authored one: `<assign>` writes into the
    /// session, so a reader frozen at generation time would answer the
    /// document's literal for the whole run. `None` means the machine cannot
    /// answer — the session is not initialized yet, `token` was
    /// assigned a value of another type, or the engine refused.
    pub fn token(&self) -> Option<String> {
        ::sce_rust_runtime::helpers::datamodel_read::read_string(
            self.script_engine.as_ref(),
            self.session_id.as_deref(),
            "token",
        )
    }

    /// §scxml-5.3: what the `only_here` datamodel variable is holding now.
    ///
    /// The live value, not the authored one: `<assign>` writes into the
    /// session, so a reader frozen at generation time would answer the
    /// document's literal for the whole run. `None` means the machine cannot
    /// answer — the session is not initialized yet, `only_here` was
    /// assigned a value of another type, or the engine refused.
    pub fn only_here(&self) -> Option<String> {
        ::sce_rust_runtime::helpers::datamodel_read::read_string(
            self.script_engine.as_ref(),
            self.session_id.as_deref(),
            "only_here",
        )
    }

    /// §scxml-C-2-3: declare the inbound BasicHTTP endpoint serving this
    /// machine, published as the processor's 'location' in `_ioprocessors`.
    /// Must be called before `initialize()`, since the entries are populated
    /// once at session setup. Leaving it unset publishes no BasicHTTP entry.
    pub fn set_basic_http_access_uri(&mut self, access_uri: impl Into<String>) {
        self.basic_http_access_uri = access_uri.into();
    }

    pub fn new(script_engine: std::sync::Arc<dyn sce_rust_runtime::IScriptEngine>) -> Self {
        Self {
            script_engine,
            pending_event_name: ::sce_rust_runtime::SceString::new(),
            pending_event_data: ::sce_rust_runtime::SceString::new(),
            pending_event_type: ::sce_rust_runtime::SceString::new(),
            pending_event_sendid: ::sce_rust_runtime::SceString::new(),
            pending_event_origin: ::sce_rust_runtime::SceString::new(),
            pending_event_origintype: ::sce_rust_runtime::SceString::new(),
            pending_event_invokeid: ::sce_rust_runtime::SceString::new(),
            session_id: None,
            script_engine_initialized: false,
            basic_http_access_uri: String::new(),
            pending_invokes: Vec::new(),
            active_invokes: std::collections::HashMap::new(),
            child_inv_shadow: None,
            pending_done_invoke_inv_shadow: false,
            child_inv_sole: None,
            pending_done_invoke_inv_sole: false,
            child_inv_unmatched: None,
            pending_done_invoke_inv_unmatched: false,
            child_inv_namelist: None,
            pending_done_invoke_inv_namelist: false,
            child_inv_infinite: None,
            pending_done_invoke_inv_infinite: false,
            parent_external_queue: None,
            invoke_id: String::new(),
            child_session_id: String::new(),
        }
    }

    // W3C SCXML 5.10: Ensure session ID is initialized
    // Uses atomic counter (1:1 with C++ UniqueIdGenerator::generateSessionId)
    fn ensure_session_id(&mut self) {
        if self.session_id.is_none() {
            static SESSION_COUNTER: core::sync::atomic::AtomicU64 =
                core::sync::atomic::AtomicU64::new(0);
            let id = SESSION_COUNTER.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
            self.session_id = Some(format!("session_{}", id));
        }
    }

    // W3C SCXML 5.2: Lazy script engine initialization
    fn ensure_script_engine(&mut self) {
        if self.script_engine_initialized {
            return;
        }
        self.ensure_session_id();
        let sid = self.session_id.as_ref().unwrap().clone();
        let se = self.script_engine.clone();
        let se: &dyn sce_rust_runtime::IScriptEngine = &*se;
        se.create_session(&sid);

        // §scxml-C-1-1 / §scxml-C-2-3: the `_ioprocessors` entries come from the
        // same helper every other backend uses, so a machine reads the same
        // entry names and the same addresses whichever one runs it.
        let io_processors =
            sce_rust_runtime::helpers::io_processors::build(&sid, &self.basic_http_access_uri);
        if let Err(e) = se.setup_system_variables(
            &sid,
            "invoke_param_seeds_declared_child_data",
            &io_processors,
        ) {
            ::sce_rust_runtime::sce_log_error!("Failed to setup system variables: {}", e);
        }

        // W3C SCXML 5.2.2: Initialize global datamodel variables (no error events)
        // W3C SCXML 5.2/5.3: Initialize 'token' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se,
            &sid,
            "token",
            "\"parent\"",
        ) {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
        }

        // W3C SCXML 5.2/5.3: Initialize 'only_here' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se,
            &sid,
            "only_here",
            "\"sole\"",
        ) {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
        }

        self.script_engine_initialized = true;
    }

    // W3C SCXML 5.2/5.3: Initialize datamodel and raise error.execution on failure
    // Called from StatePolicy::initialize_data_model() trait override
    fn do_initialize_data_model(&mut self, engine: &mut Engine<Self>) {
        if self.script_engine_initialized {
            return;
        }
        self.ensure_session_id();
        let sid = self.session_id.as_ref().unwrap().clone();
        let se = self.script_engine.clone();
        let se: &dyn sce_rust_runtime::IScriptEngine = &*se;
        se.create_session(&sid);

        // §scxml-C-1-1 / §scxml-C-2-3: the `_ioprocessors` entries come from the
        // same helper every other backend uses, so a machine reads the same
        // entry names and the same addresses whichever one runs it.
        let io_processors =
            sce_rust_runtime::helpers::io_processors::build(&sid, &self.basic_http_access_uri);
        if let Err(e) = se.setup_system_variables(
            &sid,
            "invoke_param_seeds_declared_child_data",
            &io_processors,
        ) {
            ::sce_rust_runtime::sce_log_error!("Failed to setup system variables: {}", e);
        }

        // W3C SCXML 5.2.2: Initialize global datamodel variables (with error events)
        // W3C SCXML 5.2/5.3: Initialize 'token' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se,
            &sid,
            "token",
            "\"parent\"",
        ) {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
            engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(
                InvokeParamSeedsDeclaredChildDataEvent::ErrorExecution,
                "<data id='token'> expr failed to evaluate",
            ));
        }

        // W3C SCXML 5.2/5.3: Initialize 'only_here' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se,
            &sid,
            "only_here",
            "\"sole\"",
        ) {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
            engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(
                InvokeParamSeedsDeclaredChildDataEvent::ErrorExecution,
                "<data id='only_here'> expr failed to evaluate",
            ));
        }

        self.script_engine_initialized = true;
    }

    // W3C SCXML 5.9: Safe guard evaluation with error handling
    fn safe_evaluate_guard(&mut self, cond: &str, engine: &mut Engine<Self>) -> bool {
        self.ensure_script_engine();
        let sid = self.session_id.as_ref().unwrap().clone();
        let se = self.script_engine.clone();
        let se: &dyn sce_rust_runtime::IScriptEngine = &*se;
        match se.evaluate_expression(&sid, cond) {
            Ok(val) => val.to_bool(),
            Err(e) => {
                ::sce_rust_runtime::sce_log_error!("Guard evaluation failed for '{}': {}", cond, e);
                engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(
                    InvokeParamSeedsDeclaredChildDataEvent::ErrorExecution,
                    "a <transition> cond failed to evaluate",
                ));
                false
            }
        }
    }

    // W3C SCXML 5.10: Set _event system variable for current event
    //
    // W3C SCXML C.1: `_event.origin` is the sender's published
    // `_ioprocessors` location, not its bare session id — and this is the one
    // place that publishes `_event` to the document, so this is where the id
    // becomes a location. The engine keeps the bare id in
    // `EventMetadata::origin` because its session-keyed lookups match on it;
    // converting at the raise would make one value serve two consumers that
    // need different spellings. The conversion itself lives in
    // `helpers::io_processors::published_origin`, the port of the
    // `IOProcessorHelper::publishedOrigin` the C++ engines share: a second
    // spelling of the rule is how the backends would stop agreeing.
    // Returns which rung of §scxml-B-2-8-1 the payload got, because the
    // binding is the only place that knows. This used to be `let _ = ...`:
    // the ladder decided between a DOM, a value and a space-normalized string,
    // and the decision was dropped one line later. A payload that announced
    // structure and would not parse therefore reached the document as raw
    // characters, every `_event.data.<field>` read empty, and nothing anywhere
    // could say so — see `sce_rust_runtime::PayloadReading`.
    //
    // A session that has not been created yet binds nothing, which is
    // `Absent` rather than a failure: there is no payload in play.
    fn set_current_event_in_script_engine(
        &self,
        event_name: &str,
        event_data: &str,
        event_type: &str,
        send_id: &str,
        origin: &str,
        origin_type: &str,
        invoke_id: &str,
    ) -> sce_rust_runtime::PayloadReading {
        if let Some(ref sid) = self.session_id {
            let se = self.script_engine.clone();
            let se: &dyn sce_rust_runtime::IScriptEngine = &*se;
            let published = sce_rust_runtime::helpers::io_processors::published_origin(origin);
            se.set_current_event(
                sid,
                sce_rust_runtime::SetCurrentEventArgs {
                    event_name,
                    event_data,
                    event_type,
                    send_id,
                    origin: &published,
                    origin_type,
                    invoke_id,
                },
            )
            // An engine that could not bind `_event` at all has a larger
            // problem than an unreadable payload, and reporting `Undecodable`
            // for it would point a host at the wrong thing.
            .unwrap_or(sce_rust_runtime::PayloadReading::Absent)
        } else {
            sce_rust_runtime::PayloadReading::Absent
        }
    }

    // §scxml-6.4.3: seed a `<data>` this (child) session declares with the
    // value the INVOKING session produced for an `<invoke>` `<param>` or
    // namelist location.
    //
    // The value crosses as a value. This used to take the parent's value
    // rendered as an engine literal and re-evaluate it here, which survived
    // every value a test had tried and lost the ones no Lua grammar can
    // spell: `to_script_literal` renders a non-finite number as `inf`, and
    // `inf` is an undeclared identifier to the engine that reads it back, so
    // an `<invoke>` `<param expr="1/0"/>` arrived as nothing at all. The
    // Interpreter, C++ AOT, C11, Python and Kotlin all pass the value; this
    // is the channel that did not.
    pub fn set_param_value_in_script_engine(
        &mut self,
        name: &str,
        value: sce_rust_runtime::ScriptValue,
    ) {
        self.ensure_script_engine();
        let sid = self.session_id.as_ref().unwrap().clone();
        let se = self.script_engine.clone();
        let se: &dyn sce_rust_runtime::IScriptEngine = &*se;
        let _ = se.set_variable(&sid, name, value);
    }

    // W3C SCXML 6.4: Execute pending invokes at macrostep end
    // 1:1 port of C++ executePendingInvokes() in entry_exit_actions.jinja2
    fn do_execute_pending_invokes(&mut self, engine: &mut sce_rust_runtime::Engine<Self>) {
        if self.pending_invokes.is_empty() {
            return;
        }

        // W3C SCXML 6.4: Copy-and-clear to prevent iterator invalidation.
        // `mem::take` rather than `drain(..).collect()`: the two are the same
        // move, but the drain allocates a second vector to put the elements
        // back into. clippy 1.98's `drain_collect` says so, and the gate syncs
        // the toolchain to CI's stable — so a lint that arrives with a new
        // release turns every generated machine red at once (50 of them,
        // 2026-08-21), which is why this belongs in the template.
        let invokes_to_execute = std::mem::take(&mut self.pending_invokes);

        for pending in &invokes_to_execute {
            if pending.document_id == "inv_infinite" {
                // W3C SCXML 6.5: Generate child session ID for finalize origin matching
                let child_session_id = format!(
                    "{}.{}",
                    self.session_id.as_deref().unwrap_or(""),
                    pending.invoke_id
                );

                // W3C SCXML 6.4: Create child state machine
                // Engine DI Parity RFC: forward parent's script engine to child when the
                // child Policy was generated with `model.needs_script_engine = true`.
                let mut child_policy = super::invoke_param_seeds_declared_child_data__sce_synth_invoke__inv_infinite_sm::InvokeParamSeedsDeclaredChildDataSceSynthInvokeInvInfinitePolicy::new(self.script_engine.clone());
                // W3C SCXML 6.4: Pass parent session info to child for #_parent routing
                child_policy.parent_external_queue = Some(engine.get_external_queue_handle());
                child_policy.invoke_id = pending.invoke_id.clone();
                child_policy.child_session_id = child_session_id.clone();

                // W3C SCXML C.1: the child adopts the id its parent recorded
                // for it. The parent mints `parentSession.invokeId` and keys
                // `active_invokes` on it, so an event from this child reaches
                // the parent carrying that id as its origin; if the child also
                // minted an id of its own, the location it publishes in
                // `_ioprocessors` and the origin the parent sees would be two
                // names for one session that can never be compared — which is
                // exactly what C.1 requires of them.
                if child_policy.session_id.is_none() {
                    child_policy.session_id = Some(child_session_id.clone());
                }

                // W3C SCXML 6.4.1: Pass params to child datamodel before initialization
                {
                    self.ensure_script_engine();
                    let sid = self.session_id.as_ref().unwrap().clone();
                    let se = self.script_engine.clone();
                    let se: &dyn sce_rust_runtime::IScriptEngine = &*se;
                    match se.evaluate_expression(&sid, "(1 / 0)") {
                        Ok(val) => {
                            // §scxml-6.4.3: the VALUE of the param element becomes
                            // the child `<data>`'s initial value.
                            child_policy.set_param_value_in_script_engine("seen", val);
                        }
                        Err(e) => {
                            // §scxml-5.7.1: BOTH halves — `error.execution` on the
                            // internal queue AND the name and value ignored. Only
                            // the silent half was here: an `if let Ok` dropped the
                            // failure, so a document that miscomputed one `<param>`
                            // of an `<invoke>` got a child with a `<data>` nothing
                            // explained and no event to act on.
                            //
                            // The clause delegates only the SUCCESSFUL name and
                            // value to the context — "Otherwise the use of the name
                            // and value depends on the context in which the <param>
                            // element occurs. See 5.5 <donedata>, 6.2 <send> and 6.4
                            // <invoke>" — so §scxml-6.4.2's "terminate the
                            // processing of the element" is not what a failing
                            // `<param>` costs: the child still starts, one pair
                            // short. W3C test343 settles the same clause from the
                            // `<donedata>` side.
                            ::sce_rust_runtime::sce_log_error!(
                                "invoke param 'seen' eval failed: {}",
                                e
                            );
                            engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(
                                InvokeParamSeedsDeclaredChildDataEvent::ErrorExecution,
                                "<invoke> <param name='seen'> expr failed to evaluate",
                            ));
                        }
                    }
                }

                // W3C SCXML 6.4.1: Track active invoke session BEFORE initialize
                self.active_invokes.insert(
                    "inv_infinite".to_string(),
                    sce_rust_runtime::invoke::ChildSession {
                        session_id: child_session_id,
                        // W3C SCXML 6.4: Element invoke_id for _event.invokeid (test 228)
                        invoke_id: "inv_infinite".to_string(),
                        parent_session_id: self.session_id.clone().unwrap_or_default(),
                        autoforward: false,
                        finalize_script: "".to_string(),
                    },
                );

                let mut child_engine = sce_rust_runtime::Engine::new(child_policy);
                // §scxml-6.4: the child's delayed sends are measured against
                // the same clock as ours. A child reading its own would start
                // its origin at construction time, so `<send delay="100ms">`
                // on either side of the boundary would mean two different
                // absolute instants — and on a host-owned clock the child
                // would not move at all, because the host advances the engine
                // it holds, not the ones that engine invoked.
                child_engine.set_clock(engine.clock());
                // W3C SCXML 6.4: Set completion callback so child runs final state onexit (test 236)
                child_engine.set_completion_callback(|| {});
                child_engine.initialize();

                // W3C SCXML 6.4: Store child BEFORE draining events so autoforward can find it
                self.child_inv_infinite = Some(Box::new(child_engine));

                // W3C SCXML 6.4: Drain child-to-parent events raised during initialize
                sce_rust_runtime::helpers::invoke_processing::drain_and_raise_child_events(
                    &self
                        .child_inv_infinite
                        .as_ref()
                        .unwrap()
                        .policy()
                        .parent_external_queue,
                    &self.active_invokes,
                    "inv_infinite",
                    engine,
                );

                // W3C SCXML 6.4: Check if child completed during initialize (test 236)
                if self
                    .child_inv_infinite
                    .as_ref()
                    .map_or(false, |c| c.is_in_final_state())
                {
                    self.pending_done_invoke_inv_infinite = true;
                    // W3C SCXML 5.5 + 6.3.1: Lift the child's stashed donedata onto
                    // done.invoke.<id>._event.data. Mirrors the C++ AOT contract in
                    // tools/codegen/templates/invoke_methods.jinja2 (child->donedataAtFinal()
                    // + EventMetadataHelper::createDoneInvokeEvent).
                    let donedata = self
                        .child_inv_infinite
                        .as_ref()
                        .map(|c| c.donedata_at_final().to_string())
                        .unwrap_or_default();
                    sce_rust_runtime::helpers::invoke_processing::raise_done_invoke(
                        "inv_infinite",
                        donedata,
                        engine,
                    );
                }
                continue;
            }
            if pending.document_id == "inv_namelist" {
                // W3C SCXML 6.4.1: Validate namelist variables in PARENT before creating child
                // 1:1 port of C++ NamelistHelper::evaluateNamelist — validate in parent scope
                {
                    self.ensure_script_engine();
                    let sid = self.session_id.as_ref().unwrap().clone();
                    let se = self.script_engine.clone();
                    let se: &dyn sce_rust_runtime::IScriptEngine = &*se;
                    let mut namelist_valid = true;
                    if !se.has_variable(&sid, "token") {
                        ::sce_rust_runtime::sce_log_error!(
                            "Namelist validation failed: 'token' not declared in parent"
                        );
                        engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(
                            InvokeParamSeedsDeclaredChildDataEvent::ErrorExecution,
                            "<invoke> namelist names 'token', which the parent does not declare",
                        ));
                        namelist_valid = false;
                    }
                    if !namelist_valid {
                        continue; // W3C SCXML 6.4.1: Skip invoke on namelist error
                    }
                }

                // W3C SCXML 6.5: Generate child session ID for finalize origin matching
                let child_session_id = format!(
                    "{}.{}",
                    self.session_id.as_deref().unwrap_or(""),
                    pending.invoke_id
                );

                // W3C SCXML 6.4: Create child state machine
                // Engine DI Parity RFC: forward parent's script engine to child when the
                // child Policy was generated with `model.needs_script_engine = true`.
                let mut child_policy = super::invoke_param_seeds_declared_child_data__sce_synth_invoke__inv_namelist_sm::InvokeParamSeedsDeclaredChildDataSceSynthInvokeInvNamelistPolicy::new(self.script_engine.clone());
                // W3C SCXML 6.4: Pass parent session info to child for #_parent routing
                child_policy.parent_external_queue = Some(engine.get_external_queue_handle());
                child_policy.invoke_id = pending.invoke_id.clone();
                child_policy.child_session_id = child_session_id.clone();

                // W3C SCXML C.1: the child adopts the id its parent recorded
                // for it. The parent mints `parentSession.invokeId` and keys
                // `active_invokes` on it, so an event from this child reaches
                // the parent carrying that id as its origin; if the child also
                // minted an id of its own, the location it publishes in
                // `_ioprocessors` and the origin the parent sees would be two
                // names for one session that can never be compared — which is
                // exactly what C.1 requires of them.
                if child_policy.session_id.is_none() {
                    child_policy.session_id = Some(child_session_id.clone());
                }

                // W3C SCXML 6.4.1: Pass validated namelist values to child datamodel
                // W3C SCXML 6.3.2: Only set if variable is declared in child's datamodel
                {
                    let sid = self.session_id.as_ref().unwrap().clone();
                    let se = self.script_engine.clone();
                    let se: &dyn sce_rust_runtime::IScriptEngine = &*se;
                    let child_vars: &[&str] = &["token"];
                    if child_vars.contains(&"token") {
                        if let Ok(val) = se.evaluate_expression(&sid, "token") {
                            // §scxml-6.4.1: "the value stored at the location
                            // is the value" — so the value crosses, not a
                            // rendering of it.
                            child_policy.set_param_value_in_script_engine("token", val);
                        }
                    }
                }

                // W3C SCXML 6.4.1: Track active invoke session BEFORE initialize
                self.active_invokes.insert(
                    "inv_namelist".to_string(),
                    sce_rust_runtime::invoke::ChildSession {
                        session_id: child_session_id,
                        // W3C SCXML 6.4: Element invoke_id for _event.invokeid (test 228)
                        invoke_id: "inv_namelist".to_string(),
                        parent_session_id: self.session_id.clone().unwrap_or_default(),
                        autoforward: false,
                        finalize_script: "".to_string(),
                    },
                );

                let mut child_engine = sce_rust_runtime::Engine::new(child_policy);
                // §scxml-6.4: the child's delayed sends are measured against
                // the same clock as ours. A child reading its own would start
                // its origin at construction time, so `<send delay="100ms">`
                // on either side of the boundary would mean two different
                // absolute instants — and on a host-owned clock the child
                // would not move at all, because the host advances the engine
                // it holds, not the ones that engine invoked.
                child_engine.set_clock(engine.clock());
                // W3C SCXML 6.4: Set completion callback so child runs final state onexit (test 236)
                child_engine.set_completion_callback(|| {});
                child_engine.initialize();

                // W3C SCXML 6.4: Store child BEFORE draining events so autoforward can find it
                self.child_inv_namelist = Some(Box::new(child_engine));

                // W3C SCXML 6.4: Drain child-to-parent events raised during initialize
                sce_rust_runtime::helpers::invoke_processing::drain_and_raise_child_events(
                    &self
                        .child_inv_namelist
                        .as_ref()
                        .unwrap()
                        .policy()
                        .parent_external_queue,
                    &self.active_invokes,
                    "inv_namelist",
                    engine,
                );

                // W3C SCXML 6.4: Check if child completed during initialize (test 236)
                if self
                    .child_inv_namelist
                    .as_ref()
                    .map_or(false, |c| c.is_in_final_state())
                {
                    self.pending_done_invoke_inv_namelist = true;
                    // W3C SCXML 5.5 + 6.3.1: Lift the child's stashed donedata onto
                    // done.invoke.<id>._event.data. Mirrors the C++ AOT contract in
                    // tools/codegen/templates/invoke_methods.jinja2 (child->donedataAtFinal()
                    // + EventMetadataHelper::createDoneInvokeEvent).
                    let donedata = self
                        .child_inv_namelist
                        .as_ref()
                        .map(|c| c.donedata_at_final().to_string())
                        .unwrap_or_default();
                    sce_rust_runtime::helpers::invoke_processing::raise_done_invoke(
                        "inv_namelist",
                        donedata,
                        engine,
                    );
                }
                continue;
            }
            if pending.document_id == "inv_shadow" {
                // W3C SCXML 6.5: Generate child session ID for finalize origin matching
                let child_session_id = format!(
                    "{}.{}",
                    self.session_id.as_deref().unwrap_or(""),
                    pending.invoke_id
                );

                // W3C SCXML 6.4: Create child state machine
                // Engine DI Parity RFC: forward parent's script engine to child when the
                // child Policy was generated with `model.needs_script_engine = true`.
                let mut child_policy = super::invoke_param_seeds_declared_child_data__sce_synth_invoke__inv_shadow_sm::InvokeParamSeedsDeclaredChildDataSceSynthInvokeInvShadowPolicy::new(self.script_engine.clone());
                // W3C SCXML 6.4: Pass parent session info to child for #_parent routing
                child_policy.parent_external_queue = Some(engine.get_external_queue_handle());
                child_policy.invoke_id = pending.invoke_id.clone();
                child_policy.child_session_id = child_session_id.clone();

                // W3C SCXML C.1: the child adopts the id its parent recorded
                // for it. The parent mints `parentSession.invokeId` and keys
                // `active_invokes` on it, so an event from this child reaches
                // the parent carrying that id as its origin; if the child also
                // minted an id of its own, the location it publishes in
                // `_ioprocessors` and the origin the parent sees would be two
                // names for one session that can never be compared — which is
                // exactly what C.1 requires of them.
                if child_policy.session_id.is_none() {
                    child_policy.session_id = Some(child_session_id.clone());
                }

                // W3C SCXML 6.4.1: Pass params to child datamodel before initialization
                {
                    self.ensure_script_engine();
                    let sid = self.session_id.as_ref().unwrap().clone();
                    let se = self.script_engine.clone();
                    let se: &dyn sce_rust_runtime::IScriptEngine = &*se;
                    match se.evaluate_expression(&sid, "token") {
                        Ok(val) => {
                            // §scxml-6.4.3: the VALUE of the param element becomes
                            // the child `<data>`'s initial value.
                            child_policy.set_param_value_in_script_engine("seen", val);
                        }
                        Err(e) => {
                            // §scxml-5.7.1: BOTH halves — `error.execution` on the
                            // internal queue AND the name and value ignored. Only
                            // the silent half was here: an `if let Ok` dropped the
                            // failure, so a document that miscomputed one `<param>`
                            // of an `<invoke>` got a child with a `<data>` nothing
                            // explained and no event to act on.
                            //
                            // The clause delegates only the SUCCESSFUL name and
                            // value to the context — "Otherwise the use of the name
                            // and value depends on the context in which the <param>
                            // element occurs. See 5.5 <donedata>, 6.2 <send> and 6.4
                            // <invoke>" — so §scxml-6.4.2's "terminate the
                            // processing of the element" is not what a failing
                            // `<param>` costs: the child still starts, one pair
                            // short. W3C test343 settles the same clause from the
                            // `<donedata>` side.
                            ::sce_rust_runtime::sce_log_error!(
                                "invoke param 'seen' eval failed: {}",
                                e
                            );
                            engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(
                                InvokeParamSeedsDeclaredChildDataEvent::ErrorExecution,
                                "<invoke> <param name='seen'> expr failed to evaluate",
                            ));
                        }
                    }
                }

                // W3C SCXML 6.4.1: Track active invoke session BEFORE initialize
                self.active_invokes.insert(
                    "inv_shadow".to_string(),
                    sce_rust_runtime::invoke::ChildSession {
                        session_id: child_session_id,
                        // W3C SCXML 6.4: Element invoke_id for _event.invokeid (test 228)
                        invoke_id: "inv_shadow".to_string(),
                        parent_session_id: self.session_id.clone().unwrap_or_default(),
                        autoforward: false,
                        finalize_script: "".to_string(),
                    },
                );

                let mut child_engine = sce_rust_runtime::Engine::new(child_policy);
                // §scxml-6.4: the child's delayed sends are measured against
                // the same clock as ours. A child reading its own would start
                // its origin at construction time, so `<send delay="100ms">`
                // on either side of the boundary would mean two different
                // absolute instants — and on a host-owned clock the child
                // would not move at all, because the host advances the engine
                // it holds, not the ones that engine invoked.
                child_engine.set_clock(engine.clock());
                // W3C SCXML 6.4: Set completion callback so child runs final state onexit (test 236)
                child_engine.set_completion_callback(|| {});
                child_engine.initialize();

                // W3C SCXML 6.4: Store child BEFORE draining events so autoforward can find it
                self.child_inv_shadow = Some(Box::new(child_engine));

                // W3C SCXML 6.4: Drain child-to-parent events raised during initialize
                sce_rust_runtime::helpers::invoke_processing::drain_and_raise_child_events(
                    &self
                        .child_inv_shadow
                        .as_ref()
                        .unwrap()
                        .policy()
                        .parent_external_queue,
                    &self.active_invokes,
                    "inv_shadow",
                    engine,
                );

                // W3C SCXML 6.4: Check if child completed during initialize (test 236)
                if self
                    .child_inv_shadow
                    .as_ref()
                    .map_or(false, |c| c.is_in_final_state())
                {
                    self.pending_done_invoke_inv_shadow = true;
                    // W3C SCXML 5.5 + 6.3.1: Lift the child's stashed donedata onto
                    // done.invoke.<id>._event.data. Mirrors the C++ AOT contract in
                    // tools/codegen/templates/invoke_methods.jinja2 (child->donedataAtFinal()
                    // + EventMetadataHelper::createDoneInvokeEvent).
                    let donedata = self
                        .child_inv_shadow
                        .as_ref()
                        .map(|c| c.donedata_at_final().to_string())
                        .unwrap_or_default();
                    sce_rust_runtime::helpers::invoke_processing::raise_done_invoke(
                        "inv_shadow",
                        donedata,
                        engine,
                    );
                }
                continue;
            }
            if pending.document_id == "inv_sole" {
                // W3C SCXML 6.5: Generate child session ID for finalize origin matching
                let child_session_id = format!(
                    "{}.{}",
                    self.session_id.as_deref().unwrap_or(""),
                    pending.invoke_id
                );

                // W3C SCXML 6.4: Create child state machine
                // Engine DI Parity RFC: forward parent's script engine to child when the
                // child Policy was generated with `model.needs_script_engine = true`.
                let mut child_policy = super::invoke_param_seeds_declared_child_data__sce_synth_invoke__inv_sole_sm::InvokeParamSeedsDeclaredChildDataSceSynthInvokeInvSolePolicy::new(self.script_engine.clone());
                // W3C SCXML 6.4: Pass parent session info to child for #_parent routing
                child_policy.parent_external_queue = Some(engine.get_external_queue_handle());
                child_policy.invoke_id = pending.invoke_id.clone();
                child_policy.child_session_id = child_session_id.clone();

                // W3C SCXML C.1: the child adopts the id its parent recorded
                // for it. The parent mints `parentSession.invokeId` and keys
                // `active_invokes` on it, so an event from this child reaches
                // the parent carrying that id as its origin; if the child also
                // minted an id of its own, the location it publishes in
                // `_ioprocessors` and the origin the parent sees would be two
                // names for one session that can never be compared — which is
                // exactly what C.1 requires of them.
                if child_policy.session_id.is_none() {
                    child_policy.session_id = Some(child_session_id.clone());
                }

                // W3C SCXML 6.4.1: Pass params to child datamodel before initialization
                {
                    self.ensure_script_engine();
                    let sid = self.session_id.as_ref().unwrap().clone();
                    let se = self.script_engine.clone();
                    let se: &dyn sce_rust_runtime::IScriptEngine = &*se;
                    match se.evaluate_expression(&sid, "only_here") {
                        Ok(val) => {
                            // §scxml-6.4.3: the VALUE of the param element becomes
                            // the child `<data>`'s initial value.
                            child_policy.set_param_value_in_script_engine("seen", val);
                        }
                        Err(e) => {
                            // §scxml-5.7.1: BOTH halves — `error.execution` on the
                            // internal queue AND the name and value ignored. Only
                            // the silent half was here: an `if let Ok` dropped the
                            // failure, so a document that miscomputed one `<param>`
                            // of an `<invoke>` got a child with a `<data>` nothing
                            // explained and no event to act on.
                            //
                            // The clause delegates only the SUCCESSFUL name and
                            // value to the context — "Otherwise the use of the name
                            // and value depends on the context in which the <param>
                            // element occurs. See 5.5 <donedata>, 6.2 <send> and 6.4
                            // <invoke>" — so §scxml-6.4.2's "terminate the
                            // processing of the element" is not what a failing
                            // `<param>` costs: the child still starts, one pair
                            // short. W3C test343 settles the same clause from the
                            // `<donedata>` side.
                            ::sce_rust_runtime::sce_log_error!(
                                "invoke param 'seen' eval failed: {}",
                                e
                            );
                            engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(
                                InvokeParamSeedsDeclaredChildDataEvent::ErrorExecution,
                                "<invoke> <param name='seen'> expr failed to evaluate",
                            ));
                        }
                    }
                }

                // W3C SCXML 6.4.1: Track active invoke session BEFORE initialize
                self.active_invokes.insert(
                    "inv_sole".to_string(),
                    sce_rust_runtime::invoke::ChildSession {
                        session_id: child_session_id,
                        // W3C SCXML 6.4: Element invoke_id for _event.invokeid (test 228)
                        invoke_id: "inv_sole".to_string(),
                        parent_session_id: self.session_id.clone().unwrap_or_default(),
                        autoforward: false,
                        finalize_script: "".to_string(),
                    },
                );

                let mut child_engine = sce_rust_runtime::Engine::new(child_policy);
                // §scxml-6.4: the child's delayed sends are measured against
                // the same clock as ours. A child reading its own would start
                // its origin at construction time, so `<send delay="100ms">`
                // on either side of the boundary would mean two different
                // absolute instants — and on a host-owned clock the child
                // would not move at all, because the host advances the engine
                // it holds, not the ones that engine invoked.
                child_engine.set_clock(engine.clock());
                // W3C SCXML 6.4: Set completion callback so child runs final state onexit (test 236)
                child_engine.set_completion_callback(|| {});
                child_engine.initialize();

                // W3C SCXML 6.4: Store child BEFORE draining events so autoforward can find it
                self.child_inv_sole = Some(Box::new(child_engine));

                // W3C SCXML 6.4: Drain child-to-parent events raised during initialize
                sce_rust_runtime::helpers::invoke_processing::drain_and_raise_child_events(
                    &self
                        .child_inv_sole
                        .as_ref()
                        .unwrap()
                        .policy()
                        .parent_external_queue,
                    &self.active_invokes,
                    "inv_sole",
                    engine,
                );

                // W3C SCXML 6.4: Check if child completed during initialize (test 236)
                if self
                    .child_inv_sole
                    .as_ref()
                    .map_or(false, |c| c.is_in_final_state())
                {
                    self.pending_done_invoke_inv_sole = true;
                    // W3C SCXML 5.5 + 6.3.1: Lift the child's stashed donedata onto
                    // done.invoke.<id>._event.data. Mirrors the C++ AOT contract in
                    // tools/codegen/templates/invoke_methods.jinja2 (child->donedataAtFinal()
                    // + EventMetadataHelper::createDoneInvokeEvent).
                    let donedata = self
                        .child_inv_sole
                        .as_ref()
                        .map(|c| c.donedata_at_final().to_string())
                        .unwrap_or_default();
                    sce_rust_runtime::helpers::invoke_processing::raise_done_invoke(
                        "inv_sole", donedata, engine,
                    );
                }
                continue;
            }
            if pending.document_id == "inv_unmatched" {
                // W3C SCXML 6.5: Generate child session ID for finalize origin matching
                let child_session_id = format!(
                    "{}.{}",
                    self.session_id.as_deref().unwrap_or(""),
                    pending.invoke_id
                );

                // W3C SCXML 6.4: Create child state machine
                // Engine DI Parity RFC: forward parent's script engine to child when the
                // child Policy was generated with `model.needs_script_engine = true`.
                let mut child_policy = super::invoke_param_seeds_declared_child_data__sce_synth_invoke__inv_unmatched_sm::InvokeParamSeedsDeclaredChildDataSceSynthInvokeInvUnmatchedPolicy::new(self.script_engine.clone());
                // W3C SCXML 6.4: Pass parent session info to child for #_parent routing
                child_policy.parent_external_queue = Some(engine.get_external_queue_handle());
                child_policy.invoke_id = pending.invoke_id.clone();
                child_policy.child_session_id = child_session_id.clone();

                // W3C SCXML C.1: the child adopts the id its parent recorded
                // for it. The parent mints `parentSession.invokeId` and keys
                // `active_invokes` on it, so an event from this child reaches
                // the parent carrying that id as its origin; if the child also
                // minted an id of its own, the location it publishes in
                // `_ioprocessors` and the origin the parent sees would be two
                // names for one session that can never be compared — which is
                // exactly what C.1 requires of them.
                if child_policy.session_id.is_none() {
                    child_policy.session_id = Some(child_session_id.clone());
                }

                // W3C SCXML 6.4.1: Pass params to child datamodel before initialization
                {
                    self.ensure_script_engine();
                    let sid = self.session_id.as_ref().unwrap().clone();
                    let se = self.script_engine.clone();
                    let se: &dyn sce_rust_runtime::IScriptEngine = &*se;
                    match se.evaluate_expression(&sid, "\"carried\"") {
                        Ok(val) => {
                            // §scxml-6.4.3: the VALUE of the param element becomes
                            // the child `<data>`'s initial value.
                            child_policy.set_param_value_in_script_engine("declared", val);
                        }
                        Err(e) => {
                            // §scxml-5.7.1: BOTH halves — `error.execution` on the
                            // internal queue AND the name and value ignored. Only
                            // the silent half was here: an `if let Ok` dropped the
                            // failure, so a document that miscomputed one `<param>`
                            // of an `<invoke>` got a child with a `<data>` nothing
                            // explained and no event to act on.
                            //
                            // The clause delegates only the SUCCESSFUL name and
                            // value to the context — "Otherwise the use of the name
                            // and value depends on the context in which the <param>
                            // element occurs. See 5.5 <donedata>, 6.2 <send> and 6.4
                            // <invoke>" — so §scxml-6.4.2's "terminate the
                            // processing of the element" is not what a failing
                            // `<param>` costs: the child still starts, one pair
                            // short. W3C test343 settles the same clause from the
                            // `<donedata>` side.
                            ::sce_rust_runtime::sce_log_error!(
                                "invoke param 'declared' eval failed: {}",
                                e
                            );
                            engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(
                                InvokeParamSeedsDeclaredChildDataEvent::ErrorExecution,
                                "<invoke> <param name='declared'> expr failed to evaluate",
                            ));
                        }
                    }
                    // §scxml-6.4.3: `nowhere` matches no top-level `<data>` of
                    // the child, so the Processor MUST NOT add it to the invoked
                    // session's data model. Decided here because both the param
                    // names and the child's declarations are static.
                }

                // W3C SCXML 6.4.1: Track active invoke session BEFORE initialize
                self.active_invokes.insert(
                    "inv_unmatched".to_string(),
                    sce_rust_runtime::invoke::ChildSession {
                        session_id: child_session_id,
                        // W3C SCXML 6.4: Element invoke_id for _event.invokeid (test 228)
                        invoke_id: "inv_unmatched".to_string(),
                        parent_session_id: self.session_id.clone().unwrap_or_default(),
                        autoforward: false,
                        finalize_script: "".to_string(),
                    },
                );

                let mut child_engine = sce_rust_runtime::Engine::new(child_policy);
                // §scxml-6.4: the child's delayed sends are measured against
                // the same clock as ours. A child reading its own would start
                // its origin at construction time, so `<send delay="100ms">`
                // on either side of the boundary would mean two different
                // absolute instants — and on a host-owned clock the child
                // would not move at all, because the host advances the engine
                // it holds, not the ones that engine invoked.
                child_engine.set_clock(engine.clock());
                // W3C SCXML 6.4: Set completion callback so child runs final state onexit (test 236)
                child_engine.set_completion_callback(|| {});
                child_engine.initialize();

                // W3C SCXML 6.4: Store child BEFORE draining events so autoforward can find it
                self.child_inv_unmatched = Some(Box::new(child_engine));

                // W3C SCXML 6.4: Drain child-to-parent events raised during initialize
                sce_rust_runtime::helpers::invoke_processing::drain_and_raise_child_events(
                    &self
                        .child_inv_unmatched
                        .as_ref()
                        .unwrap()
                        .policy()
                        .parent_external_queue,
                    &self.active_invokes,
                    "inv_unmatched",
                    engine,
                );

                // W3C SCXML 6.4: Check if child completed during initialize (test 236)
                if self
                    .child_inv_unmatched
                    .as_ref()
                    .map_or(false, |c| c.is_in_final_state())
                {
                    self.pending_done_invoke_inv_unmatched = true;
                    // W3C SCXML 5.5 + 6.3.1: Lift the child's stashed donedata onto
                    // done.invoke.<id>._event.data. Mirrors the C++ AOT contract in
                    // tools/codegen/templates/invoke_methods.jinja2 (child->donedataAtFinal()
                    // + EventMetadataHelper::createDoneInvokeEvent).
                    let donedata = self
                        .child_inv_unmatched
                        .as_ref()
                        .map(|c| c.donedata_at_final().to_string())
                        .unwrap_or_default();
                    sce_rust_runtime::helpers::invoke_processing::raise_done_invoke(
                        "inv_unmatched",
                        donedata,
                        engine,
                    );
                }
                continue;
            }
        }
    }

    // W3C SCXML 6.4: Tick child state machines (propagate scheduler ticks)
    fn do_tick_children(&mut self, engine: &mut sce_rust_runtime::Engine<Self>) {
        if !self.pending_done_invoke_inv_shadow {
            // W3C SCXML 6.4: Take child out temporarily to avoid split-borrow
            if let Some(mut child) = self.child_inv_shadow.take() {
                // W3C SCXML 6.4: Drain parent events sent by child via #_parent BEFORE tick
                sce_rust_runtime::helpers::invoke_processing::drain_and_raise_child_events(
                    &child.policy().parent_external_queue,
                    &self.active_invokes,
                    "inv_shadow",
                    engine,
                );

                child.tick();

                // W3C SCXML 6.4: Drain parent events sent by child AFTER tick
                sce_rust_runtime::helpers::invoke_processing::drain_and_raise_child_events(
                    &child.policy().parent_external_queue,
                    &self.active_invokes,
                    "inv_shadow",
                    engine,
                );

                // W3C SCXML 6.4: Check if child reached final state after tick
                if child.is_in_final_state() && !self.pending_done_invoke_inv_shadow {
                    self.pending_done_invoke_inv_shadow = true;
                    // W3C SCXML 5.5 + 6.3.1: Lift the child's stashed donedata onto
                    // done.invoke.<id>._event.data.
                    let donedata = child.donedata_at_final().to_string();
                    sce_rust_runtime::helpers::invoke_processing::raise_done_invoke(
                        "inv_shadow",
                        donedata,
                        engine,
                    );
                }

                // Put child back
                self.child_inv_shadow = Some(child);
            }
        }
        if !self.pending_done_invoke_inv_sole {
            // W3C SCXML 6.4: Take child out temporarily to avoid split-borrow
            if let Some(mut child) = self.child_inv_sole.take() {
                // W3C SCXML 6.4: Drain parent events sent by child via #_parent BEFORE tick
                sce_rust_runtime::helpers::invoke_processing::drain_and_raise_child_events(
                    &child.policy().parent_external_queue,
                    &self.active_invokes,
                    "inv_sole",
                    engine,
                );

                child.tick();

                // W3C SCXML 6.4: Drain parent events sent by child AFTER tick
                sce_rust_runtime::helpers::invoke_processing::drain_and_raise_child_events(
                    &child.policy().parent_external_queue,
                    &self.active_invokes,
                    "inv_sole",
                    engine,
                );

                // W3C SCXML 6.4: Check if child reached final state after tick
                if child.is_in_final_state() && !self.pending_done_invoke_inv_sole {
                    self.pending_done_invoke_inv_sole = true;
                    // W3C SCXML 5.5 + 6.3.1: Lift the child's stashed donedata onto
                    // done.invoke.<id>._event.data.
                    let donedata = child.donedata_at_final().to_string();
                    sce_rust_runtime::helpers::invoke_processing::raise_done_invoke(
                        "inv_sole", donedata, engine,
                    );
                }

                // Put child back
                self.child_inv_sole = Some(child);
            }
        }
        if !self.pending_done_invoke_inv_unmatched {
            // W3C SCXML 6.4: Take child out temporarily to avoid split-borrow
            if let Some(mut child) = self.child_inv_unmatched.take() {
                // W3C SCXML 6.4: Drain parent events sent by child via #_parent BEFORE tick
                sce_rust_runtime::helpers::invoke_processing::drain_and_raise_child_events(
                    &child.policy().parent_external_queue,
                    &self.active_invokes,
                    "inv_unmatched",
                    engine,
                );

                child.tick();

                // W3C SCXML 6.4: Drain parent events sent by child AFTER tick
                sce_rust_runtime::helpers::invoke_processing::drain_and_raise_child_events(
                    &child.policy().parent_external_queue,
                    &self.active_invokes,
                    "inv_unmatched",
                    engine,
                );

                // W3C SCXML 6.4: Check if child reached final state after tick
                if child.is_in_final_state() && !self.pending_done_invoke_inv_unmatched {
                    self.pending_done_invoke_inv_unmatched = true;
                    // W3C SCXML 5.5 + 6.3.1: Lift the child's stashed donedata onto
                    // done.invoke.<id>._event.data.
                    let donedata = child.donedata_at_final().to_string();
                    sce_rust_runtime::helpers::invoke_processing::raise_done_invoke(
                        "inv_unmatched",
                        donedata,
                        engine,
                    );
                }

                // Put child back
                self.child_inv_unmatched = Some(child);
            }
        }
        if !self.pending_done_invoke_inv_namelist {
            // W3C SCXML 6.4: Take child out temporarily to avoid split-borrow
            if let Some(mut child) = self.child_inv_namelist.take() {
                // W3C SCXML 6.4: Drain parent events sent by child via #_parent BEFORE tick
                sce_rust_runtime::helpers::invoke_processing::drain_and_raise_child_events(
                    &child.policy().parent_external_queue,
                    &self.active_invokes,
                    "inv_namelist",
                    engine,
                );

                child.tick();

                // W3C SCXML 6.4: Drain parent events sent by child AFTER tick
                sce_rust_runtime::helpers::invoke_processing::drain_and_raise_child_events(
                    &child.policy().parent_external_queue,
                    &self.active_invokes,
                    "inv_namelist",
                    engine,
                );

                // W3C SCXML 6.4: Check if child reached final state after tick
                if child.is_in_final_state() && !self.pending_done_invoke_inv_namelist {
                    self.pending_done_invoke_inv_namelist = true;
                    // W3C SCXML 5.5 + 6.3.1: Lift the child's stashed donedata onto
                    // done.invoke.<id>._event.data.
                    let donedata = child.donedata_at_final().to_string();
                    sce_rust_runtime::helpers::invoke_processing::raise_done_invoke(
                        "inv_namelist",
                        donedata,
                        engine,
                    );
                }

                // Put child back
                self.child_inv_namelist = Some(child);
            }
        }
        if !self.pending_done_invoke_inv_infinite {
            // W3C SCXML 6.4: Take child out temporarily to avoid split-borrow
            if let Some(mut child) = self.child_inv_infinite.take() {
                // W3C SCXML 6.4: Drain parent events sent by child via #_parent BEFORE tick
                sce_rust_runtime::helpers::invoke_processing::drain_and_raise_child_events(
                    &child.policy().parent_external_queue,
                    &self.active_invokes,
                    "inv_infinite",
                    engine,
                );

                child.tick();

                // W3C SCXML 6.4: Drain parent events sent by child AFTER tick
                sce_rust_runtime::helpers::invoke_processing::drain_and_raise_child_events(
                    &child.policy().parent_external_queue,
                    &self.active_invokes,
                    "inv_infinite",
                    engine,
                );

                // W3C SCXML 6.4: Check if child reached final state after tick
                if child.is_in_final_state() && !self.pending_done_invoke_inv_infinite {
                    self.pending_done_invoke_inv_infinite = true;
                    // W3C SCXML 5.5 + 6.3.1: Lift the child's stashed donedata onto
                    // done.invoke.<id>._event.data.
                    let donedata = child.donedata_at_final().to_string();
                    sce_rust_runtime::helpers::invoke_processing::raise_done_invoke(
                        "inv_infinite",
                        donedata,
                        engine,
                    );
                }

                // Put child back
                self.child_inv_infinite = Some(child);
            }
        }
    }

    // W3C SCXML C.1: deliver an event addressed to a child's published
    // location.
    //
    // The parent mints `parentSession.invokeId` for each child and that id is
    // what the child's `_ioprocessors` entry names, so a `<send>` whose target
    // decodes to one of them is addressed to that child rather than to this
    // machine. A false return means the address names no live child of ours
    // and the event takes the normal external path — the routing half of C.1
    // is what makes the published location a usable target rather than a
    // string that merely compares equal.
    fn deliver_to_child_session(
        &mut self,
        child_session_id: &str,
        event_name: &str,
        event_data: &str,
    ) -> bool {
        if child_session_id.is_empty() {
            return false;
        }
        {
            let addressed = self
                .active_invokes
                .get("inv_shadow")
                .map_or(false, |cs| cs.session_id == child_session_id);
            if addressed {
                if let Some(ref mut child) = self.child_inv_shadow {
                    child.raise_external_by_name(event_name, event_data);
                    return true;
                }
            }
        }
        {
            let addressed = self
                .active_invokes
                .get("inv_sole")
                .map_or(false, |cs| cs.session_id == child_session_id);
            if addressed {
                if let Some(ref mut child) = self.child_inv_sole {
                    child.raise_external_by_name(event_name, event_data);
                    return true;
                }
            }
        }
        {
            let addressed = self
                .active_invokes
                .get("inv_unmatched")
                .map_or(false, |cs| cs.session_id == child_session_id);
            if addressed {
                if let Some(ref mut child) = self.child_inv_unmatched {
                    child.raise_external_by_name(event_name, event_data);
                    return true;
                }
            }
        }
        {
            let addressed = self
                .active_invokes
                .get("inv_namelist")
                .map_or(false, |cs| cs.session_id == child_session_id);
            if addressed {
                if let Some(ref mut child) = self.child_inv_namelist {
                    child.raise_external_by_name(event_name, event_data);
                    return true;
                }
            }
        }
        {
            let addressed = self
                .active_invokes
                .get("inv_infinite")
                .map_or(false, |cs| cs.session_id == child_session_id);
            if addressed {
                if let Some(ref mut child) = self.child_inv_infinite {
                    child.raise_external_by_name(event_name, event_data);
                    return true;
                }
            }
        }
        false
    }
}

// ======================================================================
// StatePolicy trait implementation
// ======================================================================

impl StatePolicy for InvokeParamSeedsDeclaredChildDataPolicy {
    type State = InvokeParamSeedsDeclaredChildDataState;
    type Event = InvokeParamSeedsDeclaredChildDataEvent;
    // W3C SCXML 3.10: this document declares no <history>, so no target list
    // can name one.
    type History = sce_rust_runtime::NoHistory;
    // EventSchema native lowering: `()` = schemaless (dynamic
    // `_event.data` baseline); a `<Machine>Payload` sum is emitted when a
    // transition guard reads a typed `_event.data.<field>` (NL→IR C1 Path A).
    type Payload = ();
    type Hal = sce_rust_runtime::StdHal;

    // W3C SCXML Appendix D internalQueue / externalQueue. Under no_std the
    // depth is the per-document `<scxml sce:capacity>` / deploy value (emitted
    // as the const consumed below); absent a declared capacity the bare form
    // inherits the runtime default `MAX_EVENT_QUEUE_DEPTH` (the single source of
    // the default). Inert under std (`VecDeque`), so this one emission compiles
    // on both runtime profiles.
    type EventQueue = sce_rust_runtime::EventQueueManager<
        sce_rust_runtime::EventWithMetadata<Self::Event, Self::Payload>,
    >;

    // W3C SCXML 6.3: the delayed-send scheduler keeps a per-entry `send_id` only
    // so `<cancel sendid>` can find and drop the matching entry; the timer-fire
    // drain never reads it. A document with no `<cancel>` therefore emits the
    // zero-size `ElidedSendId`, shedding the scheduler ring's per-entry
    // `heapless::String<256>` (~256 B x MAX_SCHEDULED_EVENTS) under no_std --
    // dead weight with no cancel reader. A cancelling document keeps the
    // load-bearing `SceString`. Behaviour-preserving under std either way, so
    // this one emission compiles on both runtime profiles. Mirrors the
    // EventQueue per-machine sizing lever above.
    type ScheduledSendId = sce_rust_runtime::ElidedSendId;

    // W3C SCXML feature flags
    const HAS_PARALLEL_STATES: bool = false;
    const NEEDS_SCRIPT_ENGINE: bool = true;
    const NEEDS_DATA_MODEL_INIT: bool = true;
    // The same fact the generate manifest publishes as `needs_event_scheduler`.
    // A `build.rs` consumer never sees that manifest — `compile_scxml` returns
    // `()` — so without this constant a host had no route to the knowledge that
    // its driving loop must call `tick()` rather than `step()`.
    const NEEDS_EVENT_SCHEDULER: bool = true;
    // §scxml-6.4: gates the engine's macrostep-end call into
    // `execute_pending_invokes`. §scxml-6.4.1 unsupported-type invokes need
    // it too — that call site is where their error.execution is raised.
    const HAS_INVOKE_SUPPORT: bool = true;
    // Only session-bearing invokes have a child engine to tick.
    const HAS_CHILD_TICK: bool = true;

    // ======================================================================
    // Static metadata methods (W3C SCXML document structure)
    // ======================================================================

    fn initial_state() -> Self::State {
        // The runtime entry state and the type's `Default` derive are one
        // concept, sourced from one computation (`default_state_id`) so
        // they can never diverge: `<scxml initial>`, or the first sorted
        // state when the initial attribute is unset.
        InvokeParamSeedsDeclaredChildDataState::Shadowed
    }

    fn is_final_state(state: Self::State) -> bool {
        match state {
            InvokeParamSeedsDeclaredChildDataState::FailChildEvaluatedTheExpression => true,
            InvokeParamSeedsDeclaredChildDataState::FailDeclaredParamLost => true,
            InvokeParamSeedsDeclaredChildDataState::FailInfiniteParamCollapsed => true,
            InvokeParamSeedsDeclaredChildDataState::FailInfiniteParamLost => true,
            InvokeParamSeedsDeclaredChildDataState::FailNamelistValueLost => true,
            InvokeParamSeedsDeclaredChildDataState::FailParentOnlyExprLost => true,
            InvokeParamSeedsDeclaredChildDataState::FailShadowSeedLost => true,
            InvokeParamSeedsDeclaredChildDataState::FailUnmatchedParamEnteredTheChild => true,
            InvokeParamSeedsDeclaredChildDataState::Pass => true,
            _ => false,
        }
    }

    fn get_parent(state: Self::State) -> Option<Self::State> {
        match state {
            _ => None,
        }
    }

    // W3C SCXML 3.3: a <state> with child states — exactly the states that have
    // an initial transition. A <parallel> is not compound.
    fn is_compound_state(state: Self::State) -> bool {
        match state {
            _ => false,
        }
    }

    // §scxml-D-getChildStates: a state's <state>, <parallel> and <final>
    // children, in document order — for a <parallel>, its regions.
    fn get_child_states(state: Self::State) -> &'static [Self::State] {
        match state {
            _ => &[],
        }
    }

    // §scxml-3.3: a compound state's initial transition target, as written —
    // the engine's entry procedures dereference a <history> among them.
    fn get_initial_targets(
        state: Self::State,
    ) -> &'static [::sce_rust_runtime::EntryTarget<Self::State, Self::History>] {
        match state {
            _ => &[],
        }
    }

    // §scxml-3.2: the target of the document's own initial transition, as written.
    fn get_document_initial_targets(
    ) -> &'static [::sce_rust_runtime::EntryTarget<Self::State, Self::History>] {
        &[::sce_rust_runtime::EntryTarget::State(
            InvokeParamSeedsDeclaredChildDataState::Shadowed,
        )]
    }

    // §scxml-3.10: the state a <history> is declared in.
    fn get_history_parent(history: Self::History) -> Self::State {
        match history {}
    }

    // §scxml-3.10.2: a <history>'s default transition target, as written.
    fn get_history_default_targets(
        history: Self::History,
    ) -> &'static [::sce_rust_runtime::EntryTarget<Self::State, Self::History>] {
        match history {}
    }

    fn get_document_order(state: Self::State) -> u32 {
        match state {
            InvokeParamSeedsDeclaredChildDataState::FailChildEvaluatedTheExpression => 6,
            InvokeParamSeedsDeclaredChildDataState::FailDeclaredParamLost => 10,
            InvokeParamSeedsDeclaredChildDataState::FailInfiniteParamCollapsed => 13,
            InvokeParamSeedsDeclaredChildDataState::FailInfiniteParamLost => 12,
            InvokeParamSeedsDeclaredChildDataState::FailNamelistValueLost => 11,
            InvokeParamSeedsDeclaredChildDataState::FailParentOnlyExprLost => 8,
            InvokeParamSeedsDeclaredChildDataState::FailShadowSeedLost => 7,
            InvokeParamSeedsDeclaredChildDataState::FailUnmatchedParamEnteredTheChild => 9,
            InvokeParamSeedsDeclaredChildDataState::Infinite => 4,
            InvokeParamSeedsDeclaredChildDataState::NamelistPhase => 3,
            InvokeParamSeedsDeclaredChildDataState::Pass => 5,
            InvokeParamSeedsDeclaredChildDataState::Shadowed => 0,
            InvokeParamSeedsDeclaredChildDataState::SoleName => 1,
            InvokeParamSeedsDeclaredChildDataState::Unmatched => 2,
        }
    }

    fn get_event_name(event: Self::Event) -> &'static str {
        match event {
            InvokeParamSeedsDeclaredChildDataEvent::DoneInvoke => "done.invoke",
            InvokeParamSeedsDeclaredChildDataEvent::ErrorExecution => "error.execution",
            InvokeParamSeedsDeclaredChildDataEvent::SeedCollapsed => "seed.collapsed",
            InvokeParamSeedsDeclaredChildDataEvent::SeedLeaked => "seed.leaked",
            InvokeParamSeedsDeclaredChildDataEvent::SeedMissing => "seed.missing",
            InvokeParamSeedsDeclaredChildDataEvent::SeedOk => "seed.ok",
            InvokeParamSeedsDeclaredChildDataEvent::SeedShadowed => "seed.shadowed",
            InvokeParamSeedsDeclaredChildDataEvent::Null => "",
        }
    }

    fn get_event_from_name(name: &str) -> Option<Self::Event> {
        match name {
            "done.invoke" => Some(InvokeParamSeedsDeclaredChildDataEvent::DoneInvoke),
            "error.execution" => Some(InvokeParamSeedsDeclaredChildDataEvent::ErrorExecution),
            "seed.collapsed" => Some(InvokeParamSeedsDeclaredChildDataEvent::SeedCollapsed),
            "seed.leaked" => Some(InvokeParamSeedsDeclaredChildDataEvent::SeedLeaked),
            "seed.missing" => Some(InvokeParamSeedsDeclaredChildDataEvent::SeedMissing),
            "seed.ok" => Some(InvokeParamSeedsDeclaredChildDataEvent::SeedOk),
            "seed.shadowed" => Some(InvokeParamSeedsDeclaredChildDataEvent::SeedShadowed),
            _ => None,
        }
    }

    fn get_state_name(state: Self::State) -> &'static str {
        match state {
            InvokeParamSeedsDeclaredChildDataState::FailChildEvaluatedTheExpression => {
                "failChildEvaluatedTheExpression"
            }
            InvokeParamSeedsDeclaredChildDataState::FailDeclaredParamLost => {
                "failDeclaredParamLost"
            }
            InvokeParamSeedsDeclaredChildDataState::FailInfiniteParamCollapsed => {
                "failInfiniteParamCollapsed"
            }
            InvokeParamSeedsDeclaredChildDataState::FailInfiniteParamLost => {
                "failInfiniteParamLost"
            }
            InvokeParamSeedsDeclaredChildDataState::FailNamelistValueLost => {
                "failNamelistValueLost"
            }
            InvokeParamSeedsDeclaredChildDataState::FailParentOnlyExprLost => {
                "failParentOnlyExprLost"
            }
            InvokeParamSeedsDeclaredChildDataState::FailShadowSeedLost => "failShadowSeedLost",
            InvokeParamSeedsDeclaredChildDataState::FailUnmatchedParamEnteredTheChild => {
                "failUnmatchedParamEnteredTheChild"
            }
            InvokeParamSeedsDeclaredChildDataState::Infinite => "infinite",
            InvokeParamSeedsDeclaredChildDataState::NamelistPhase => "namelistPhase",
            InvokeParamSeedsDeclaredChildDataState::Pass => "pass",
            InvokeParamSeedsDeclaredChildDataState::Shadowed => "shadowed",
            InvokeParamSeedsDeclaredChildDataState::SoleName => "soleName",
            InvokeParamSeedsDeclaredChildDataState::Unmatched => "unmatched",
        }
    }

    // The inverse of the table above, emitted from the same loop over the
    // document's states so the two age together. It is what lets a host turn a
    // recorded configuration back into the `StateChain` `enter_at` takes.
    fn get_state_from_name(name: &str) -> Option<Self::State> {
        match name {
            "failChildEvaluatedTheExpression" => {
                Some(InvokeParamSeedsDeclaredChildDataState::FailChildEvaluatedTheExpression)
            }
            "failDeclaredParamLost" => {
                Some(InvokeParamSeedsDeclaredChildDataState::FailDeclaredParamLost)
            }
            "failInfiniteParamCollapsed" => {
                Some(InvokeParamSeedsDeclaredChildDataState::FailInfiniteParamCollapsed)
            }
            "failInfiniteParamLost" => {
                Some(InvokeParamSeedsDeclaredChildDataState::FailInfiniteParamLost)
            }
            "failNamelistValueLost" => {
                Some(InvokeParamSeedsDeclaredChildDataState::FailNamelistValueLost)
            }
            "failParentOnlyExprLost" => {
                Some(InvokeParamSeedsDeclaredChildDataState::FailParentOnlyExprLost)
            }
            "failShadowSeedLost" => {
                Some(InvokeParamSeedsDeclaredChildDataState::FailShadowSeedLost)
            }
            "failUnmatchedParamEnteredTheChild" => {
                Some(InvokeParamSeedsDeclaredChildDataState::FailUnmatchedParamEnteredTheChild)
            }
            "infinite" => Some(InvokeParamSeedsDeclaredChildDataState::Infinite),
            "namelistPhase" => Some(InvokeParamSeedsDeclaredChildDataState::NamelistPhase),
            "pass" => Some(InvokeParamSeedsDeclaredChildDataState::Pass),
            "shadowed" => Some(InvokeParamSeedsDeclaredChildDataState::Shadowed),
            "soleName" => Some(InvokeParamSeedsDeclaredChildDataState::SoleName),
            "unmatched" => Some(InvokeParamSeedsDeclaredChildDataState::Unmatched),
            _ => None,
        }
    }

    fn null_event() -> Self::Event {
        InvokeParamSeedsDeclaredChildDataEvent::Null
    }

    // ======================================================================
    // Run-time state the entry procedures read
    // ======================================================================

    // §scxml-3.10: what a <history> recorded when its parent was last exited;
    // None before that ever happened.
    fn history_value(&self, history: Self::History) -> Option<&[Self::State]> {
        match history {}
    }

    // W3C SCXML 5.10: Populate pending event metadata from EventWithMetadata
    // Ports C++ EventMetadataHelper::populatePolicyFromMetadata
    fn populate_event_metadata(&mut self, metadata: &sce_rust_runtime::EventMetadata) {
        self.pending_event_data = metadata.data.clone();
        self.pending_event_type =
            ::sce_rust_runtime::sce_string_from_str(metadata.event_type.as_str());
        self.pending_event_sendid = metadata.send_id.clone();
        self.pending_event_origin = metadata.origin.clone();
        self.pending_event_origintype = metadata.origin_type.clone();
        self.pending_event_invokeid = metadata.invoke_id.clone();
    }

    // W3C SCXML 5.10: Clear pending event metadata after transition processing
    // Ports C++ EventMetadataHelper::clearPolicyMetadata
    fn clear_event_metadata(&mut self) {
        self.pending_event_name.clear();
        self.pending_event_data.clear();
        self.pending_event_type.clear();
        self.pending_event_sendid.clear();
        self.pending_event_origin.clear();
        self.pending_event_origintype.clear();
        self.pending_event_invokeid.clear();
    }

    // ======================================================================
    // Instance methods - generated executable content
    // ======================================================================

    // W3C SCXML 3.7: Execute <onentry> actions for a state
    #[doc = "SCE-MAP: invoke_param_seeds_declared_child_data.scxml:84 :: _machine"]
    // SCE-MAP: invoke_param_seeds_declared_child_data.scxml:84 :: _machine
    fn execute_entry_actions(
        &mut self,
        state: Self::State,
        engine: &mut sce_rust_runtime::Engine<Self>,
        is_default_entry: bool,
    ) {
        match state {
            InvokeParamSeedsDeclaredChildDataState::Infinite => {
                // SCE-MAP: invoke_param_seeds_declared_child_data.scxml:235 :: infinite :: _state_body
                // W3C SCXML 6.4: Defer invoke execution until macrostep end
                {
                    let generated_invoke_id =
                        format!("{}.{}.inv_infinite", "infinite", self as *const _ as usize);
                    let id_stored = true;
                    if id_stored {
                        sce_rust_runtime::invoke::defer_invoke(
                            &mut self.pending_invokes,
                            sce_rust_runtime::invoke::PendingInvoke {
                                invoke_id: generated_invoke_id,
                                state: InvokeParamSeedsDeclaredChildDataState::Infinite,
                                document_id: "inv_infinite",
                            },
                        );
                    }
                }
            }
            InvokeParamSeedsDeclaredChildDataState::NamelistPhase => {
                // SCE-MAP: invoke_param_seeds_declared_child_data.scxml:193 :: namelistPhase :: _state_body
                // W3C SCXML 6.4: Defer invoke execution until macrostep end
                {
                    let generated_invoke_id = format!(
                        "{}.{}.inv_namelist",
                        "namelistPhase", self as *const _ as usize
                    );
                    let id_stored = true;
                    if id_stored {
                        sce_rust_runtime::invoke::defer_invoke(
                            &mut self.pending_invokes,
                            sce_rust_runtime::invoke::PendingInvoke {
                                invoke_id: generated_invoke_id,
                                state: InvokeParamSeedsDeclaredChildDataState::NamelistPhase,
                                document_id: "inv_namelist",
                            },
                        );
                    }
                }
            }
            InvokeParamSeedsDeclaredChildDataState::Shadowed => {
                // SCE-MAP: invoke_param_seeds_declared_child_data.scxml:92 :: shadowed :: _state_body
                // W3C SCXML 6.4: Defer invoke execution until macrostep end
                {
                    let generated_invoke_id =
                        format!("{}.{}.inv_shadow", "shadowed", self as *const _ as usize);
                    let id_stored = true;
                    if id_stored {
                        sce_rust_runtime::invoke::defer_invoke(
                            &mut self.pending_invokes,
                            sce_rust_runtime::invoke::PendingInvoke {
                                invoke_id: generated_invoke_id,
                                state: InvokeParamSeedsDeclaredChildDataState::Shadowed,
                                document_id: "inv_shadow",
                            },
                        );
                    }
                }
            }
            InvokeParamSeedsDeclaredChildDataState::SoleName => {
                // SCE-MAP: invoke_param_seeds_declared_child_data.scxml:123 :: soleName :: _state_body
                // W3C SCXML 6.4: Defer invoke execution until macrostep end
                {
                    let generated_invoke_id =
                        format!("{}.{}.inv_sole", "soleName", self as *const _ as usize);
                    let id_stored = true;
                    if id_stored {
                        sce_rust_runtime::invoke::defer_invoke(
                            &mut self.pending_invokes,
                            sce_rust_runtime::invoke::PendingInvoke {
                                invoke_id: generated_invoke_id,
                                state: InvokeParamSeedsDeclaredChildDataState::SoleName,
                                document_id: "inv_sole",
                            },
                        );
                    }
                }
            }
            InvokeParamSeedsDeclaredChildDataState::Unmatched => {
                // SCE-MAP: invoke_param_seeds_declared_child_data.scxml:149 :: unmatched :: _state_body
                // W3C SCXML 6.4: Defer invoke execution until macrostep end
                {
                    let generated_invoke_id = format!(
                        "{}.{}.inv_unmatched",
                        "unmatched", self as *const _ as usize
                    );
                    let id_stored = true;
                    if id_stored {
                        sce_rust_runtime::invoke::defer_invoke(
                            &mut self.pending_invokes,
                            sce_rust_runtime::invoke::PendingInvoke {
                                invoke_id: generated_invoke_id,
                                state: InvokeParamSeedsDeclaredChildDataState::Unmatched,
                                document_id: "inv_unmatched",
                            },
                        );
                    }
                }
            }
            _ => {}
        }
    }

    // §scxml-3.10.2: a <history>'s default transition content, run after its
    // parent's onentry (and after the parent's own <initial> content) when the
    // history was taken with nothing recorded. The engine asks for it by the
    // entry set's defaultHistoryContent answer; a history that restored what it
    // recorded runs nothing.

    // W3C SCXML 3.8: Execute <onexit> actions for a state
    #[doc = "SCE-MAP: invoke_param_seeds_declared_child_data.scxml:84 :: _machine"]
    // SCE-MAP: invoke_param_seeds_declared_child_data.scxml:84 :: _machine
    fn execute_exit_actions(
        &mut self,
        state: Self::State,
        engine: &mut sce_rust_runtime::Engine<Self>,
        configuration_before_exit: &[Self::State],
    ) {
        // §scxml-D-exitStates orders one state's exit as onexit, then
        // cancelInvoke, then configuration.delete(s), so `In(s)` inside s's
        // own handler is true: the deactivation is the LAST step here.
        match state {
            _ => {}
        }
        // W3C SCXML 6.4: Cancel pending invokes and cleanup active children on
        // state exit — after the onexit above, per §scxml-D-exitStates.
        match state {
            InvokeParamSeedsDeclaredChildDataState::Infinite => {
                // W3C SCXML 6.4: Cancel pending invokes for exited state.
                // Covers §scxml-6.4.1 entries: an invoke whose state exits
                // before the macrostep ends never runs, so it raises nothing.
                sce_rust_runtime::invoke::cancel_invokes_for_state(
                    &mut self.pending_invokes,
                    InvokeParamSeedsDeclaredChildDataState::Infinite,
                );
                // W3C SCXML 6.4: Cleanup running static child 'inv_infinite'
                if self.child_inv_infinite.is_some() {
                    // §scxml-6.4: cancelling raises nothing in this session —
                    // the spec defines no `cancel.invoke` event, and a document
                    // that named one used to receive it here.
                    //
                    // §scxml-D-exitInterpreter: a cancelled session is exited —
                    // its states' `<onexit>` run — before it is dropped. What
                    // that exit sends to `#_parent` lands in the child's own
                    // queue, which is dropped with it undrained (§scxml-6.4
                    // cancel-drop, test252).
                    if let Some(mut child) = self.child_inv_infinite.take() {
                        child.stop();
                    }
                }
                self.active_invokes.remove("inv_infinite");
                self.pending_done_invoke_inv_infinite = false;
            }
            InvokeParamSeedsDeclaredChildDataState::NamelistPhase => {
                // W3C SCXML 6.4: Cancel pending invokes for exited state.
                // Covers §scxml-6.4.1 entries: an invoke whose state exits
                // before the macrostep ends never runs, so it raises nothing.
                sce_rust_runtime::invoke::cancel_invokes_for_state(
                    &mut self.pending_invokes,
                    InvokeParamSeedsDeclaredChildDataState::NamelistPhase,
                );
                // W3C SCXML 6.4: Cleanup running static child 'inv_namelist'
                if self.child_inv_namelist.is_some() {
                    // §scxml-6.4: cancelling raises nothing in this session —
                    // the spec defines no `cancel.invoke` event, and a document
                    // that named one used to receive it here.
                    //
                    // §scxml-D-exitInterpreter: a cancelled session is exited —
                    // its states' `<onexit>` run — before it is dropped. What
                    // that exit sends to `#_parent` lands in the child's own
                    // queue, which is dropped with it undrained (§scxml-6.4
                    // cancel-drop, test252).
                    if let Some(mut child) = self.child_inv_namelist.take() {
                        child.stop();
                    }
                }
                self.active_invokes.remove("inv_namelist");
                self.pending_done_invoke_inv_namelist = false;
            }
            InvokeParamSeedsDeclaredChildDataState::Shadowed => {
                // W3C SCXML 6.4: Cancel pending invokes for exited state.
                // Covers §scxml-6.4.1 entries: an invoke whose state exits
                // before the macrostep ends never runs, so it raises nothing.
                sce_rust_runtime::invoke::cancel_invokes_for_state(
                    &mut self.pending_invokes,
                    InvokeParamSeedsDeclaredChildDataState::Shadowed,
                );
                // W3C SCXML 6.4: Cleanup running static child 'inv_shadow'
                if self.child_inv_shadow.is_some() {
                    // §scxml-6.4: cancelling raises nothing in this session —
                    // the spec defines no `cancel.invoke` event, and a document
                    // that named one used to receive it here.
                    //
                    // §scxml-D-exitInterpreter: a cancelled session is exited —
                    // its states' `<onexit>` run — before it is dropped. What
                    // that exit sends to `#_parent` lands in the child's own
                    // queue, which is dropped with it undrained (§scxml-6.4
                    // cancel-drop, test252).
                    if let Some(mut child) = self.child_inv_shadow.take() {
                        child.stop();
                    }
                }
                self.active_invokes.remove("inv_shadow");
                self.pending_done_invoke_inv_shadow = false;
            }
            InvokeParamSeedsDeclaredChildDataState::SoleName => {
                // W3C SCXML 6.4: Cancel pending invokes for exited state.
                // Covers §scxml-6.4.1 entries: an invoke whose state exits
                // before the macrostep ends never runs, so it raises nothing.
                sce_rust_runtime::invoke::cancel_invokes_for_state(
                    &mut self.pending_invokes,
                    InvokeParamSeedsDeclaredChildDataState::SoleName,
                );
                // W3C SCXML 6.4: Cleanup running static child 'inv_sole'
                if self.child_inv_sole.is_some() {
                    // §scxml-6.4: cancelling raises nothing in this session —
                    // the spec defines no `cancel.invoke` event, and a document
                    // that named one used to receive it here.
                    //
                    // §scxml-D-exitInterpreter: a cancelled session is exited —
                    // its states' `<onexit>` run — before it is dropped. What
                    // that exit sends to `#_parent` lands in the child's own
                    // queue, which is dropped with it undrained (§scxml-6.4
                    // cancel-drop, test252).
                    if let Some(mut child) = self.child_inv_sole.take() {
                        child.stop();
                    }
                }
                self.active_invokes.remove("inv_sole");
                self.pending_done_invoke_inv_sole = false;
            }
            InvokeParamSeedsDeclaredChildDataState::Unmatched => {
                // W3C SCXML 6.4: Cancel pending invokes for exited state.
                // Covers §scxml-6.4.1 entries: an invoke whose state exits
                // before the macrostep ends never runs, so it raises nothing.
                sce_rust_runtime::invoke::cancel_invokes_for_state(
                    &mut self.pending_invokes,
                    InvokeParamSeedsDeclaredChildDataState::Unmatched,
                );
                // W3C SCXML 6.4: Cleanup running static child 'inv_unmatched'
                if self.child_inv_unmatched.is_some() {
                    // §scxml-6.4: cancelling raises nothing in this session —
                    // the spec defines no `cancel.invoke` event, and a document
                    // that named one used to receive it here.
                    //
                    // §scxml-D-exitInterpreter: a cancelled session is exited —
                    // its states' `<onexit>` run — before it is dropped. What
                    // that exit sends to `#_parent` lands in the child's own
                    // queue, which is dropped with it undrained (§scxml-6.4
                    // cancel-drop, test252).
                    if let Some(mut child) = self.child_inv_unmatched.take() {
                        child.stop();
                    }
                }
                self.active_invokes.remove("inv_unmatched");
                self.pending_done_invoke_inv_unmatched = false;
            }
            _ => {}
        }
    }

    // §scxml-5.10: the event whose transitions are about to be selected is the
    // `_event` their guards read — bound before the first guard runs, and not
    // for an eventless selection, which has no event of its own.
    #[doc = "SCE-MAP: invoke_param_seeds_declared_child_data.scxml:84 :: _machine"]
    // SCE-MAP: invoke_param_seeds_declared_child_data.scxml:84 :: _machine
    fn bind_current_event(
        &mut self,
        event: Self::Event,
        engine: &mut sce_rust_runtime::Engine<Self>,
    ) {
        // W3C SCXML 5.10: Ensure script engine and set _event for guard evaluation
        self.ensure_script_engine();
        if event != Self::null_event() {
            let event_name = Self::get_event_name(event);
            self.pending_event_name = event_name.to_string();
            // §scxml-5.10.1: typed by the queue the engine took the event from.
            let event_type = sce_rust_runtime::EventType::classify(
                event_name,
                engine.is_current_event_external(),
            )
            .as_str();
            self.pending_event_type = event_type.to_string();
            // W3C SCXML 5.10: Set _event with all available metadata fields
            let ev_data: &str = &self.pending_event_data;
            let ev_sendid: &str = &self.pending_event_sendid;
            let ev_origin: &str = &self.pending_event_origin;
            let ev_origintype: &str = &self.pending_event_origintype;
            let ev_invokeid: &str = &self.pending_event_invokeid;
            // §scxml-B-2-8-1: the rung the payload got, handed to the engine
            // rather than dropped. This is the only frame that has both the
            // reading and the event it belongs to.
            let payload_reading = self.set_current_event_in_script_engine(
                event_name,
                ev_data,
                event_type,
                ev_sendid,
                ev_origin,
                ev_origintype,
                ev_invokeid,
            );
            engine.note_payload_reading(event, payload_reading);
        }
    }

    // Appendix D selectTransitions, the half only this document can answer:
    // the first of `state`'s own transitions, in document order, that `event`
    // enables. The engine walks the atomic states and their ancestors and
    // keeps the ordered set. `Event::Null` asks for eventless transitions.
    #[doc = "SCE-MAP: invoke_param_seeds_declared_child_data.scxml:84 :: _machine"]
    // SCE-MAP: invoke_param_seeds_declared_child_data.scxml:84 :: _machine
    fn first_enabled_transition(
        &mut self,
        state: Self::State,
        event: Self::Event,
        engine: &mut sce_rust_runtime::Engine<Self>,
    ) -> Option<::sce_rust_runtime::EnabledTransition<Self::State, Self::History>> {
        match state {
            InvokeParamSeedsDeclaredChildDataState::Infinite => {
                if event == InvokeParamSeedsDeclaredChildDataEvent::SeedOk {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                InvokeParamSeedsDeclaredChildDataState::Pass,
                            )],
                            transition_index: 0,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                if event == InvokeParamSeedsDeclaredChildDataEvent::SeedMissing {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                InvokeParamSeedsDeclaredChildDataState::FailInfiniteParamLost,
                            )],
                            transition_index: 1,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                if event == InvokeParamSeedsDeclaredChildDataEvent::SeedCollapsed {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                InvokeParamSeedsDeclaredChildDataState::FailInfiniteParamCollapsed,
                            )],
                            transition_index: 2,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                if event == InvokeParamSeedsDeclaredChildDataEvent::ErrorExecution {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                InvokeParamSeedsDeclaredChildDataState::FailInfiniteParamLost,
                            )],
                            transition_index: 3,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                None
            }
            InvokeParamSeedsDeclaredChildDataState::NamelistPhase => {
                if event == InvokeParamSeedsDeclaredChildDataEvent::SeedOk {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                InvokeParamSeedsDeclaredChildDataState::Infinite,
                            )],
                            transition_index: 0,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                if event == InvokeParamSeedsDeclaredChildDataEvent::SeedMissing {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                InvokeParamSeedsDeclaredChildDataState::FailNamelistValueLost,
                            )],
                            transition_index: 1,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                if event == InvokeParamSeedsDeclaredChildDataEvent::ErrorExecution {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                InvokeParamSeedsDeclaredChildDataState::FailNamelistValueLost,
                            )],
                            transition_index: 2,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                None
            }
            InvokeParamSeedsDeclaredChildDataState::Shadowed => {
                if event == InvokeParamSeedsDeclaredChildDataEvent::SeedOk {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                InvokeParamSeedsDeclaredChildDataState::SoleName,
                            )],
                            transition_index: 0,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                if event == InvokeParamSeedsDeclaredChildDataEvent::SeedShadowed {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(InvokeParamSeedsDeclaredChildDataState::FailChildEvaluatedTheExpression)],
                            transition_index: 1,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                if event == InvokeParamSeedsDeclaredChildDataEvent::SeedMissing {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                InvokeParamSeedsDeclaredChildDataState::FailShadowSeedLost,
                            )],
                            transition_index: 2,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                if event == InvokeParamSeedsDeclaredChildDataEvent::ErrorExecution {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                InvokeParamSeedsDeclaredChildDataState::FailShadowSeedLost,
                            )],
                            transition_index: 3,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                None
            }
            InvokeParamSeedsDeclaredChildDataState::SoleName => {
                if event == InvokeParamSeedsDeclaredChildDataEvent::SeedOk {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                InvokeParamSeedsDeclaredChildDataState::Unmatched,
                            )],
                            transition_index: 0,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                if event == InvokeParamSeedsDeclaredChildDataEvent::SeedMissing {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                InvokeParamSeedsDeclaredChildDataState::FailParentOnlyExprLost,
                            )],
                            transition_index: 1,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                if event == InvokeParamSeedsDeclaredChildDataEvent::ErrorExecution {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                InvokeParamSeedsDeclaredChildDataState::FailParentOnlyExprLost,
                            )],
                            transition_index: 2,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                None
            }
            InvokeParamSeedsDeclaredChildDataState::Unmatched => {
                if event == InvokeParamSeedsDeclaredChildDataEvent::SeedOk {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                InvokeParamSeedsDeclaredChildDataState::NamelistPhase,
                            )],
                            transition_index: 0,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                if event == InvokeParamSeedsDeclaredChildDataEvent::SeedLeaked {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(InvokeParamSeedsDeclaredChildDataState::FailUnmatchedParamEnteredTheChild)],
                            transition_index: 1,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                if event == InvokeParamSeedsDeclaredChildDataEvent::SeedMissing {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                InvokeParamSeedsDeclaredChildDataState::FailDeclaredParamLost,
                            )],
                            transition_index: 2,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                if event == InvokeParamSeedsDeclaredChildDataEvent::ErrorExecution {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                InvokeParamSeedsDeclaredChildDataState::FailDeclaredParamLost,
                            )],
                            transition_index: 3,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                None
            }
            _ => None,
        }
    }

    // W3C SCXML 3.13: a transition's executable content, run by the engine
    // between the microstep's exits and its entries.
    #[doc = "SCE-MAP: invoke_param_seeds_declared_child_data.scxml:84 :: _machine"]
    // SCE-MAP: invoke_param_seeds_declared_child_data.scxml:84 :: _machine
    fn execute_transition_content(
        &mut self,
        source: Self::State,
        transition_index: usize,
        engine: &mut sce_rust_runtime::Engine<Self>,
    ) {
        // W3C SCXML 3.13: no transition in this document has content.
        let _ = (source, transition_index, engine);
    }
    // W3C SCXML 5.2/5.3: Datamodel initialization with error.execution support
    // Delegates to inherent impl method (matches C++ initializeDataModel pattern)
    fn initialize_data_model(&mut self, engine: &mut Engine<Self>) {
        self.do_initialize_data_model(engine);
    }

    // W3C SCXML 6.4: Execute pending invokes at macrostep end
    fn execute_pending_invokes(&mut self, engine: &mut Engine<Self>) {
        self.do_execute_pending_invokes(engine);
    }

    // W3C SCXML 6.4: Tick child state machines
    fn tick_children(&mut self, engine: &mut Engine<Self>) {
        self.do_tick_children(engine);
    }
}
