// SCE-GENERATED — DO NOT EDIT
// source-hash: 321b42acfe8cb266c51aff87d805eb471548c8d5250d5f0a5214385ef864d6e9

// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) 2025-2026 [Author of input SCXML file]
//
// Generated by SCE Code Generator (Rust backend)
// From: examples/ai_loop/ai_loop.scxml
//
// This generated code is MIT licensed and may be freely used in any project.
// Runtime engine dependency (sce-rust-runtime): LGPL-2.1-or-later WITH LicenseRef-SCE-Linking-Exception OR LicenseRef-SCE-Commercial
// Full license: https://github.com/newmassrael/scxml-core-engine/blob/main/LICENSE

//! Generated Rust state machine for `ai_loop`.
//!
//! Generated by SCE Code Generator (Rust backend).
//! Implements `sce_rust_runtime::StatePolicy` with full transition logic,
//! entry/exit actions, and event processing.

// Suppression budget audited 2026-04-15 against the full sce-rust-tests
// W3C suite (202 fixtures). Each allow below corresponds to a lint that
// actually fires on generated code today; see file:line callouts in the
// inline justifications. Adding a new allow here requires reproducing the
// warning on a real fixture and recording it in the same form. Removing an
// allow is safe whenever its justifying pattern stops being emitted.

// Source-faithful identifier names: W3C SCXML 6.4.1 lets users supply
// arbitrary id strings (e.g. `<invoke id="invokedChild">`); preserving the
// camelCase verbatim into Rust field names like `child_invokedChild`
// keeps the generated code traceable back to the SCXML source.
#![allow(non_snake_case)]
// Codegen always emits `use core::time::Duration` and the umbrella
// `use sce_rust_runtime::{Engine, StatePolicy};`; fixtures with no
// `<send delay=...>` and no engine-borrowing actions never reference
// `Duration`/`Engine`, leaving the imports unused.
#![allow(unused_imports)]
// Method parameters such as `_engine: &mut Engine<Self>` and per-state
// fields (`pending_event_invokeid`, `script_engine_initialized`, ...) are
// emitted on every fixture for the StatePolicy trait shape but read only
// by fixtures that actually exercise the corresponding W3C feature.
#![allow(dead_code)]
#![allow(unused_variables)]
// Bindings emitted `mut` for the documents that write them: `done_data_ok` in
// `<donedata>` evaluation is cleared only by a param that fails (test294,
// test343), `json_parts` is pushed only by a param that evaluates (test298),
// and a send's `err_meta` gets a `send_id` only when the document has a
// datamodel (test194, test199). Measured 2026-09-24: removing this allow
// reds those fixtures under `-D warnings`.
#![allow(unused_mut)]
// `'action_block:` early-exit label wraps every onentry block but is only
// `break`-ed to from fixtures that emit error-on-action sequences.
#![allow(unused_labels)]
// Codegen emits the catch-all `else => {}` arm on every closed-enum match
// for forward compatibility; rustc proves it unreachable once every
// variant is handled.
#![allow(unreachable_patterns)]
#![allow(unreachable_code)]
// W3C SCXML 5.7 donedata pattern: `let mut done_data_ok = true;` then
// per-param `done_data_ok = false;` on failure. Fixtures whose params
// always fail structurally (test298, test343 — empty `location=""`) never
// read the initial `true`.
#![allow(unused_assignments)]
// Generated code is uniform across 200+ fixtures and intentionally avoids
// per-fixture style optimisation passes. The `clippy::style` and
// `clippy::complexity` categories cover suggestions a human author would
// apply by hand (match_like_matches_macro, needless_range_loop,
// type_complexity, single_match, ...) — none of these affect correctness,
// but each one would force the codegen to special-case its output.
// Correctness-relevant clippy categories (`clippy::correctness`,
// `clippy::suspicious`, `clippy::perf`) remain at warn so any genuine bug
// the generator emits still surfaces.
#![allow(clippy::style)]
#![allow(clippy::complexity)]
#![doc = "SCE-MAP: ai_loop.scxml:155 :: _machine"]
// SCE-MAP: ai_loop.scxml:155 :: _machine

use core::time::Duration;
use sce_rust_runtime::{Engine, StatePolicy};

// ======================================================================
// State enum (W3C SCXML 3.3)
// ======================================================================

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Default)]
pub enum AiLoopState {
    Abandoned,
    Alive,
    Blocked,
    Budget,
    Cancelled,
    Closing,
    Converged,
    Drive,
    Exhausted,
    Failed,
    Judging,
    Paused,
    Priming,
    Rebuilding,
    Reflecting,
    Reported,
    Restarting,
    // W3C SCXML 3.2: `<scxml initial>` state — the machine's `Default`.
    #[default]
    Run,
    Running,
    Screening,
    Spent,
    Stuck,
    Watch,
    Within,
    Working,
}

// ======================================================================
// History enum (W3C SCXML 3.10)
// ======================================================================

/// The document's `<history>` pseudo-states. A history is not a state — it is
/// never in a configuration — so it is named apart from `AiLoopState`,
/// and a target list names one as `EntryTarget::History`: the engine
/// dereferences it to what it recorded, or to its default.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum AiLoopHistory {
    Where,
}

// ======================================================================
// Event enum (W3C SCXML 3.12)
// ======================================================================

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum AiLoopEvent {
    Cancel,
    DoneStateDrive,
    DoneStateRun,
    DoneStateRunning,
    ErrorExecution,
    Fail,
    Hold,
    Judge,
    JudgeBegin,
    NotifyHuman,
    PromptEnd,
    PromptSent,
    PromptStart,
    PromptTurn,
    ReflectApplied,
    ReflectBegin,
    ReflectNone,
    Resume,
    RunBlocked,
    RunConverged,
    RunExhausted,
    ScreenBegin,
    ScreenMatched,
    ScreenNone,
    SessionLost,
    SessionReady,
    SessionReplace,
    TurnBlocked,
    TurnDone,
    TurnInterrupted,
    Unattended,
    /// W3C SCXML 3.13: Sentinel for eventless transition dispatch
    Null,
}

// ======================================================================
// Externally-drivable event surface (W3C SCXML 3.12 / 5.10)
// ======================================================================

impl AiLoopEvent {
    /// Events that can arrive from OUTSIDE the machine — external
    /// `<send>` / platform ingress — as opposed to internal `<raise>`
    /// signals and the W3C SCXML 3.13 `Null` sentinel (neither is a
    /// member). This is the machine's structural external trigger
    /// surface: every non-reserved `<transition event>` target minus
    /// every internally-raised event. A consumer that maps event names
    /// back to variants admits only these as externally injectable,
    /// without re-deriving the partition. Structural and consumer-
    /// agnostic; inert when unused. Associated (not a free `const`) so
    /// several machines glob-re-exported into one module never collide
    /// on the name.
    pub const EXTERNALLY_DRIVABLE_EVENTS: &'static [AiLoopEvent] = &[
        AiLoopEvent::Cancel,
        AiLoopEvent::Fail,
        AiLoopEvent::Hold,
        AiLoopEvent::Judge,
        AiLoopEvent::PromptSent,
        AiLoopEvent::ReflectApplied,
        AiLoopEvent::ReflectNone,
        AiLoopEvent::Resume,
        AiLoopEvent::ScreenMatched,
        AiLoopEvent::ScreenNone,
        AiLoopEvent::SessionLost,
        AiLoopEvent::SessionReady,
        AiLoopEvent::TurnBlocked,
        AiLoopEvent::TurnDone,
        AiLoopEvent::TurnInterrupted,
        AiLoopEvent::Unattended,
    ];
}

// ======================================================================
// Policy struct
// ======================================================================

pub struct AiLoopPolicy {
    // W3C SCXML 3.4: Active state configuration for parallel states / In() predicate
    //
    // SCE Protocol-Synthesis RFC §synth-5-J-2: type is the runtime crate's
    // [`StateChain`] alias — `Vec<S>` under std, `heapless::Vec<S, MAX_HIERARCHY_DEPTH>`
    // under no_std. The std alias preserves the existing ABI; the no_std alias keeps
    // the generated code allocator-free.
    active_states: ::sce_rust_runtime::helpers::hierarchy::StateChain<AiLoopState>,
    // W3C SCXML 5.10.1: External event flag for _event.type classification
    next_event_is_external: bool,
    // W3C SCXML 5.10: Event name for _event.name binding
    //
    // SCE Protocol-Synthesis RFC §synth-5-J-2: typed as the runtime crate's [`SceString`]
    // alias — `String` under std (unchanged ABI), `heapless::String<MAX_EVENT_STRING_LEN>`
    // under no_std — so the emitted field is allocator-free on MCU targets.
    pending_event_name: ::sce_rust_runtime::SceString,
    // W3C SCXML 5.10: Event data for the script `_event.data` baseline (the
    // typed path reads the Payload sum, not this field — so needs_script_engine).
    pending_event_data: ::sce_rust_runtime::SceString,
    // W3C SCXML 5.10.1: Event type for _event.type binding
    pending_event_type: ::sce_rust_runtime::SceString,
    // W3C SCXML 5.10.1: Event sendid for _event.sendid binding
    pending_event_sendid: ::sce_rust_runtime::SceString,
    // W3C SCXML 5.10.1: Event origin for _event.origin binding
    pending_event_origin: ::sce_rust_runtime::SceString,
    // W3C SCXML 5.10.1: Event origintype for _event.origintype binding
    pending_event_origintype: ::sce_rust_runtime::SceString,
    // W3C SCXML 5.10.1: Event invokeid for _event.invokeid binding
    pending_event_invokeid: ::sce_rust_runtime::SceString,
    // W3C SCXML 3.11: History state where (shallow)
    //
    // SCE Protocol-Synthesis RFC §synth-5-J-2: the recorded configuration is a
    // [`StateChain`] alias (`Vec<S>` under std, `heapless::Vec<S, MAX_HIERARCHY_DEPTH>`
    // under no_std), mirroring `active_states`. Recorded history is always a
    // subset of the active configuration, so it cannot exceed the depth bound.
    history_where: Option<::sce_rust_runtime::helpers::hierarchy::StateChain<AiLoopState>>,
    // W3C SCXML 5.10: Session ID (script engine + invoke tracking).
    //
    // SCE Protocol-Synthesis RFC §synth-5-J-2: gated to !no_std. Under `--no-std` both the
    // script engine (`codegen/no-std-script-not-supported`) and `<invoke>`
    // (`codegen/no-std-invoke-not-supported`) are codegen-rejected, so no
    // session identity is ever tracked and the alloc-coupled `String` is omitted.
    pub session_id: Option<String>,
    // Engine DI Parity RFC (Path B+): per-instance script engine. The
    // constructor parameter is mandatory whenever `model.needs_script_engine`
    // is true, mirroring the Kotlin `StateMachineEngine(scriptEngine)` shape.
    pub script_engine: std::sync::Arc<dyn sce_rust_runtime::IScriptEngine>,
    script_engine_initialized: bool,
    // §scxml-C-2-3: inbound BasicHTTP endpoint serving this machine, declared
    // by the deployment before initialize(). Empty means no such endpoint is
    // deployed, and no BasicHTTP entry is published in `_ioprocessors`.
    basic_http_access_uri: String,
    // W3C SCXML 5.9.2: the configuration as the script engine's In() reads it —
    // a copy of `active_states` by name, republished on every change to it.
    in_predicate_states: Option<std::sync::Arc<std::sync::Mutex<Vec<String>>>>,
    // W3C SCXML 6.4: Parent engine external queue for #_parent send routing
    // Always generated under std — any SM can be invoked as a child. Under
    // `--no-std` (SCE Protocol-Synthesis RFC §synth-5-J-2) the SCXML `<invoke>` element
    // is codegen-rejected, so no parent_external_queue handle is ever
    // wired in, and the Arc<Mutex<...>> (alloc-coupled) is omitted.
    pub parent_external_queue: Option<std::sync::Arc<std::sync::Mutex<Vec<(String, String)>>>>,
    // W3C SCXML 6.4.1: This child's invoke ID (for _event.invokeid in parent).
    // SCE Protocol-Synthesis RFC §synth-5-J-2: gated to !no_std — `<invoke>` is codegen-rejected
    // under no_std, so a machine is never instantiated as a child and this
    // identity is dead. Mirrors the `parent_external_queue` / `invoke` module gate.
    pub invoke_id: String,
    // W3C SCXML 6.5: Child session ID for finalize origin matching.
    pub child_session_id: String,
}

impl AiLoopPolicy {
    /// §scxml-5.3: what the `north_star` datamodel variable is holding now.
    ///
    /// The live value, not the authored one: `<assign>` writes into the
    /// session, so a reader frozen at generation time would answer the
    /// document's literal for the whole run. `None` means the machine cannot
    /// answer — the session is not initialized yet, `north_star` was
    /// assigned a value of another type, or the engine refused.
    pub fn north_star(&self) -> Option<String> {
        ::sce_rust_runtime::helpers::datamodel_read::read_string(
            self.script_engine.as_ref(),
            self.session_id.as_deref(),
            "north_star",
        )
    }

    /// §scxml-5.3: what the `milestone` datamodel variable is holding now.
    ///
    /// The live value, not the authored one: `<assign>` writes into the
    /// session, so a reader frozen at generation time would answer the
    /// document's literal for the whole run. `None` means the machine cannot
    /// answer — the session is not initialized yet, `milestone` was
    /// assigned a value of another type, or the engine refused.
    pub fn milestone(&self) -> Option<String> {
        ::sce_rust_runtime::helpers::datamodel_read::read_string(
            self.script_engine.as_ref(),
            self.session_id.as_deref(),
            "milestone",
        )
    }

    /// §scxml-5.3: what the `reference` datamodel variable is holding now.
    ///
    /// The live value, not the authored one: `<assign>` writes into the
    /// session, so a reader frozen at generation time would answer the
    /// document's literal for the whole run. `None` means the machine cannot
    /// answer — the session is not initialized yet, `reference` was
    /// assigned a value of another type, or the engine refused.
    pub fn reference(&self) -> Option<String> {
        ::sce_rust_runtime::helpers::datamodel_read::read_string(
            self.script_engine.as_ref(),
            self.session_id.as_deref(),
            "reference",
        )
    }

    /// §scxml-5.3: what the `start_prompt` datamodel variable is holding now.
    ///
    /// The live value, not the authored one: `<assign>` writes into the
    /// session, so a reader frozen at generation time would answer the
    /// document's literal for the whole run. `None` means the machine cannot
    /// answer — the session is not initialized yet, `start_prompt` was
    /// assigned a value of another type, or the engine refused.
    pub fn start_prompt(&self) -> Option<String> {
        ::sce_rust_runtime::helpers::datamodel_read::read_string(
            self.script_engine.as_ref(),
            self.session_id.as_deref(),
            "start_prompt",
        )
    }

    /// §scxml-5.3: what the `turn_prompt` datamodel variable is holding now.
    ///
    /// The live value, not the authored one: `<assign>` writes into the
    /// session, so a reader frozen at generation time would answer the
    /// document's literal for the whole run. `None` means the machine cannot
    /// answer — the session is not initialized yet, `turn_prompt` was
    /// assigned a value of another type, or the engine refused.
    pub fn turn_prompt(&self) -> Option<String> {
        ::sce_rust_runtime::helpers::datamodel_read::read_string(
            self.script_engine.as_ref(),
            self.session_id.as_deref(),
            "turn_prompt",
        )
    }

    /// §scxml-5.3: what the `end_prompt` datamodel variable is holding now.
    ///
    /// The live value, not the authored one: `<assign>` writes into the
    /// session, so a reader frozen at generation time would answer the
    /// document's literal for the whole run. `None` means the machine cannot
    /// answer — the session is not initialized yet, `end_prompt` was
    /// assigned a value of another type, or the engine refused.
    pub fn end_prompt(&self) -> Option<String> {
        ::sce_rust_runtime::helpers::datamodel_read::read_string(
            self.script_engine.as_ref(),
            self.session_id.as_deref(),
            "end_prompt",
        )
    }

    /// §scxml-5.3: what the `done_marker` datamodel variable is holding now.
    ///
    /// The live value, not the authored one: `<assign>` writes into the
    /// session, so a reader frozen at generation time would answer the
    /// document's literal for the whole run. `None` means the machine cannot
    /// answer — the session is not initialized yet, `done_marker` was
    /// assigned a value of another type, or the engine refused.
    pub fn done_marker(&self) -> Option<String> {
        ::sce_rust_runtime::helpers::datamodel_read::read_string(
            self.script_engine.as_ref(),
            self.session_id.as_deref(),
            "done_marker",
        )
    }

    /// §scxml-5.3: what the `screen_rules` datamodel variable is holding now.
    ///
    /// The live value, not the authored one: `<assign>` writes into the
    /// session, so a reader frozen at generation time would answer the
    /// document's literal for the whole run. `None` means the machine cannot
    /// answer — the session is not initialized yet, `screen_rules` was
    /// assigned a value of another type, or the engine refused.
    ///
    /// The value as JSON text, serialized by the engine's own
    /// `JSON.stringify` (§scxml-B-2) so the key order is the document's.
    pub fn screen_rules(&self) -> Option<String> {
        ::sce_rust_runtime::helpers::datamodel_read::read_json(
            self.script_engine.as_ref(),
            self.session_id.as_deref(),
            "screen_rules",
        )
    }

    /// §scxml-5.3: what the `screen_permissions` datamodel variable is holding now.
    ///
    /// The live value, not the authored one: `<assign>` writes into the
    /// session, so a reader frozen at generation time would answer the
    /// document's literal for the whole run. `None` means the machine cannot
    /// answer — the session is not initialized yet, `screen_permissions` was
    /// assigned a value of another type, or the engine refused.
    pub fn screen_permissions(&self) -> Option<bool> {
        ::sce_rust_runtime::helpers::datamodel_read::read_bool(
            self.script_engine.as_ref(),
            self.session_id.as_deref(),
            "screen_permissions",
        )
    }

    /// §scxml-5.3: what the `max_turns` datamodel variable is holding now.
    ///
    /// The live value, not the authored one: `<assign>` writes into the
    /// session, so a reader frozen at generation time would answer the
    /// document's literal for the whole run. `None` means the machine cannot
    /// answer — the session is not initialized yet, `max_turns` was
    /// assigned a value of another type, or the engine refused.
    pub fn max_turns(&self) -> Option<i64> {
        ::sce_rust_runtime::helpers::datamodel_read::read_int(
            self.script_engine.as_ref(),
            self.session_id.as_deref(),
            "max_turns",
        )
    }

    /// §scxml-5.3: what the `reflect_every` datamodel variable is holding now.
    ///
    /// The live value, not the authored one: `<assign>` writes into the
    /// session, so a reader frozen at generation time would answer the
    /// document's literal for the whole run. `None` means the machine cannot
    /// answer — the session is not initialized yet, `reflect_every` was
    /// assigned a value of another type, or the engine refused.
    pub fn reflect_every(&self) -> Option<i64> {
        ::sce_rust_runtime::helpers::datamodel_read::read_int(
            self.script_engine.as_ref(),
            self.session_id.as_deref(),
            "reflect_every",
        )
    }

    /// §scxml-5.3: what the `max_restarts` datamodel variable is holding now.
    ///
    /// The live value, not the authored one: `<assign>` writes into the
    /// session, so a reader frozen at generation time would answer the
    /// document's literal for the whole run. `None` means the machine cannot
    /// answer — the session is not initialized yet, `max_restarts` was
    /// assigned a value of another type, or the engine refused.
    pub fn max_restarts(&self) -> Option<i64> {
        ::sce_rust_runtime::helpers::datamodel_read::read_int(
            self.script_engine.as_ref(),
            self.session_id.as_deref(),
            "max_restarts",
        )
    }

    /// §scxml-5.3: what the `turns` datamodel variable is holding now.
    ///
    /// The live value, not the authored one: `<assign>` writes into the
    /// session, so a reader frozen at generation time would answer the
    /// document's literal for the whole run. `None` means the machine cannot
    /// answer — the session is not initialized yet, `turns` was
    /// assigned a value of another type, or the engine refused.
    pub fn turns(&self) -> Option<i64> {
        ::sce_rust_runtime::helpers::datamodel_read::read_int(
            self.script_engine.as_ref(),
            self.session_id.as_deref(),
            "turns",
        )
    }

    /// §scxml-5.3: what the `turns_since_reflect` datamodel variable is holding now.
    ///
    /// The live value, not the authored one: `<assign>` writes into the
    /// session, so a reader frozen at generation time would answer the
    /// document's literal for the whole run. `None` means the machine cannot
    /// answer — the session is not initialized yet, `turns_since_reflect` was
    /// assigned a value of another type, or the engine refused.
    pub fn turns_since_reflect(&self) -> Option<i64> {
        ::sce_rust_runtime::helpers::datamodel_read::read_int(
            self.script_engine.as_ref(),
            self.session_id.as_deref(),
            "turns_since_reflect",
        )
    }

    /// §scxml-5.3: what the `screened` datamodel variable is holding now.
    ///
    /// The live value, not the authored one: `<assign>` writes into the
    /// session, so a reader frozen at generation time would answer the
    /// document's literal for the whole run. `None` means the machine cannot
    /// answer — the session is not initialized yet, `screened` was
    /// assigned a value of another type, or the engine refused.
    pub fn screened(&self) -> Option<i64> {
        ::sce_rust_runtime::helpers::datamodel_read::read_int(
            self.script_engine.as_ref(),
            self.session_id.as_deref(),
            "screened",
        )
    }

    /// §scxml-5.3: what the `restarts` datamodel variable is holding now.
    ///
    /// The live value, not the authored one: `<assign>` writes into the
    /// session, so a reader frozen at generation time would answer the
    /// document's literal for the whole run. `None` means the machine cannot
    /// answer — the session is not initialized yet, `restarts` was
    /// assigned a value of another type, or the engine refused.
    pub fn restarts(&self) -> Option<i64> {
        ::sce_rust_runtime::helpers::datamodel_read::read_int(
            self.script_engine.as_ref(),
            self.session_id.as_deref(),
            "restarts",
        )
    }

    /// §scxml-C-2-3: declare the inbound BasicHTTP endpoint serving this
    /// machine, published as the processor's 'location' in `_ioprocessors`.
    /// Must be called before `initialize()`, since the entries are populated
    /// once at session setup. Leaving it unset publishes no BasicHTTP entry.
    pub fn set_basic_http_access_uri(&mut self, access_uri: impl Into<String>) {
        self.basic_http_access_uri = access_uri.into();
    }

    pub fn new(script_engine: std::sync::Arc<dyn sce_rust_runtime::IScriptEngine>) -> Self {
        Self {
            script_engine,
            active_states: ::sce_rust_runtime::helpers::hierarchy::new_chain(),
            next_event_is_external: false,
            pending_event_name: ::sce_rust_runtime::SceString::new(),
            pending_event_data: ::sce_rust_runtime::SceString::new(),
            pending_event_type: ::sce_rust_runtime::SceString::new(),
            pending_event_sendid: ::sce_rust_runtime::SceString::new(),
            pending_event_origin: ::sce_rust_runtime::SceString::new(),
            pending_event_origintype: ::sce_rust_runtime::SceString::new(),
            pending_event_invokeid: ::sce_rust_runtime::SceString::new(),
            history_where: None,
            session_id: None,
            script_engine_initialized: false,
            basic_http_access_uri: String::new(),
            in_predicate_states: None,
            parent_external_queue: None,
            invoke_id: String::new(),
            child_session_id: String::new(),
        }
    }

    // W3C SCXML 5.9.2: Check if state is active (for In() predicate)
    fn is_state_active(&self, state_id: &str) -> bool {
        self.active_states
            .iter()
            .any(|&s| Self::get_state_name(s) == state_id)
    }

    // §scxml-3.4: add `state` to the configuration this machine keeps; false
    // when it was already there.
    fn activate(&mut self, state: AiLoopState) -> bool {
        if self.active_states.contains(&state) {
            return false;
        }
        ::sce_rust_runtime::helpers::hierarchy::push_chain(&mut self.active_states, state);
        self.publish_in_predicate_states();
        true
    }

    // §scxml-3.4: remove `state` from the configuration this machine keeps.
    fn deactivate(&mut self, state: AiLoopState) {
        self.active_states.retain(|&s| s != state);
        self.publish_in_predicate_states();
    }

    // W3C SCXML 5.9.2: the script engine's In() reads a copy of the
    // configuration, so the copy is refreshed on every change to it rather than
    // once per selection. In() is asked inside executable content as well as in
    // guards — `<if cond="In('s')">` in an <onentry> runs halfway through a
    // microstep — and must see the configuration as it stands at that point.
    fn publish_in_predicate_states(&self) {
        if let Some(ref shared) = self.in_predicate_states {
            let mut states = shared.lock().unwrap();
            states.clear();
            states.extend(
                self.active_states
                    .iter()
                    .map(|&s| Self::get_state_name(s).to_string()),
            );
        }
    }

    // W3C SCXML 5.10: Ensure session ID is initialized
    // Uses atomic counter (1:1 with C++ UniqueIdGenerator::generateSessionId)
    fn ensure_session_id(&mut self) {
        if self.session_id.is_none() {
            static SESSION_COUNTER: core::sync::atomic::AtomicU64 =
                core::sync::atomic::AtomicU64::new(0);
            let id = SESSION_COUNTER.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
            self.session_id = Some(format!("session_{}", id));
        }
    }

    // W3C SCXML 5.2: Lazy script engine initialization
    fn ensure_script_engine(&mut self) {
        if self.script_engine_initialized {
            return;
        }
        self.ensure_session_id();
        let sid = self.session_id.as_ref().unwrap().clone();
        let se = self.script_engine.clone();
        let se: &dyn sce_rust_runtime::IScriptEngine = &*se;
        se.create_session(&sid);

        // §scxml-C-1-1 / §scxml-C-2-3: the `_ioprocessors` entries come from the
        // same helper every other backend uses, so a machine reads the same
        // entry names and the same addresses whichever one runs it.
        let io_processors =
            sce_rust_runtime::helpers::io_processors::build(&sid, &self.basic_http_access_uri);
        if let Err(e) = se.setup_system_variables(&sid, "ai_loop", &io_processors) {
            ::sce_rust_runtime::sce_log_error!("Failed to setup system variables: {}", e);
        }

        // W3C SCXML 5.2.2: Initialize global datamodel variables (no error events)
        // W3C SCXML 5.2/5.3: Initialize 'north_star' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se,
            &sid,
            "north_star",
            "\"(edit me) the outcome this loop exists to reach\"",
        ) {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
        }

        // W3C SCXML 5.2/5.3: Initialize 'milestone' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se,
            &sid,
            "milestone",
            "\"(edit me) the next checkpoint on the way there\"",
        ) {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
        }

        // W3C SCXML 5.2/5.3: Initialize 'reference' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se,
            &sid,
            "reference",
            "\"(edit me) paths, URLs or repos to consult\"",
        ) {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
        }

        // W3C SCXML 5.2/5.3: Initialize 'start_prompt' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se, &sid, "start_prompt", "(_scxml_tostring((_scxml_tostring(_scxml_add((_scxml_tostring((_scxml_tostring(_scxml_add((_scxml_tostring((_scxml_tostring((\"North star: \" .. _scxml_tostring(north_star))) .. \"\\n\")) .. \"Milestone: \"), milestone)) .. \"\\n\")) .. \"Reference: \"), reference)) .. \"\\n\")) .. \"Report what you did and what is left.\")") {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
        }

        // W3C SCXML 5.2/5.3: Initialize 'turn_prompt' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se, &sid, "turn_prompt", "(_scxml_tostring((_scxml_tostring((\"Continue toward: \" .. _scxml_tostring(milestone))) .. \"\\n\")) .. \"Do the next smallest thing that is verifiable, then report.\")") {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
        }

        // W3C SCXML 5.2/5.3: Initialize 'end_prompt' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se,
            &sid,
            "end_prompt",
            "\"Summarise what changed, what was verified, and what is left open.\"",
        ) {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
        }

        // W3C SCXML 5.2/5.3: Initialize 'done_marker' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se,
            &sid,
            "done_marker",
            "\"MILESTONE REACHED\"",
        ) {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
        }

        // W3C SCXML 5.2/5.3: Initialize 'screen_rules' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se, &sid, "screen_rules", "{{[\"when\"] = \"design-decision\", [\"keys\"] = \"Escape\", [\"text\"] = \"Ignore cost. Rethink for the most durable answer, then proceed.\"}, {[\"when\"] = \"design-proposal\", [\"keys\"] = \"Escape\", [\"text\"] = \"Ignore cost. Rethink for the most durable answer, then proceed.\"}, {[\"when\"] = \"multiple-choice\", [\"keys\"] = \"Escape\", [\"text\"] = \"Ignore cost. Rethink for the most durable answer, then proceed.\"}}") {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
        }

        // W3C SCXML 5.2/5.3: Initialize 'screen_permissions' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se,
            &sid,
            "screen_permissions",
            "false",
        ) {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
        }

        // W3C SCXML 5.2/5.3: Initialize 'max_turns' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se,
            &sid,
            "max_turns",
            "40",
        ) {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
        }

        // W3C SCXML 5.2/5.3: Initialize 'reflect_every' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se,
            &sid,
            "reflect_every",
            "8",
        ) {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
        }

        // W3C SCXML 5.2/5.3: Initialize 'max_restarts' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se,
            &sid,
            "max_restarts",
            "6",
        ) {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
        }

        // W3C SCXML 5.2/5.3: Initialize 'turns' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se, &sid, "turns", "0",
        ) {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
        }

        // W3C SCXML 5.2/5.3: Initialize 'turns_since_reflect' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se,
            &sid,
            "turns_since_reflect",
            "0",
        ) {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
        }

        // W3C SCXML 5.2/5.3: Initialize 'screened' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se, &sid, "screened", "0",
        ) {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
        }

        // W3C SCXML 5.2/5.3: Initialize 'restarts' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se, &sid, "restarts", "0",
        ) {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
        }

        // W3C SCXML 5.9.2: Register In() state query callback
        {
            let shared_states = std::sync::Arc::new(std::sync::Mutex::new(Vec::<String>::new()));
            self.in_predicate_states = Some(shared_states.clone());
            // Seeded with the configuration as it stands now; every later change
            // to it republishes (`activate` / `deactivate` / `set_active_states`).
            self.publish_in_predicate_states();
            se.set_state_query_callback(
                &sid,
                Some(Box::new(move |state_id: &str| -> bool {
                    if let Ok(states) = shared_states.lock() {
                        states.iter().any(|s| s == state_id)
                    } else {
                        false
                    }
                })),
            );
        }

        self.script_engine_initialized = true;
    }

    // W3C SCXML 5.2/5.3: Initialize datamodel and raise error.execution on failure
    // Called from StatePolicy::initialize_data_model() trait override
    fn do_initialize_data_model(&mut self, engine: &mut Engine<Self>) {
        if self.script_engine_initialized {
            return;
        }
        self.ensure_session_id();
        let sid = self.session_id.as_ref().unwrap().clone();
        let se = self.script_engine.clone();
        let se: &dyn sce_rust_runtime::IScriptEngine = &*se;
        se.create_session(&sid);

        // §scxml-C-1-1 / §scxml-C-2-3: the `_ioprocessors` entries come from the
        // same helper every other backend uses, so a machine reads the same
        // entry names and the same addresses whichever one runs it.
        let io_processors =
            sce_rust_runtime::helpers::io_processors::build(&sid, &self.basic_http_access_uri);
        if let Err(e) = se.setup_system_variables(&sid, "ai_loop", &io_processors) {
            ::sce_rust_runtime::sce_log_error!("Failed to setup system variables: {}", e);
        }

        // W3C SCXML 5.2.2: Initialize global datamodel variables (with error events)
        // W3C SCXML 5.2/5.3: Initialize 'north_star' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se,
            &sid,
            "north_star",
            "\"(edit me) the outcome this loop exists to reach\"",
        ) {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
            engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(
                AiLoopEvent::ErrorExecution,
                "<data id='north_star'> expr failed to evaluate",
            ));
        }

        // W3C SCXML 5.2/5.3: Initialize 'milestone' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se,
            &sid,
            "milestone",
            "\"(edit me) the next checkpoint on the way there\"",
        ) {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
            engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(
                AiLoopEvent::ErrorExecution,
                "<data id='milestone'> expr failed to evaluate",
            ));
        }

        // W3C SCXML 5.2/5.3: Initialize 'reference' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se,
            &sid,
            "reference",
            "\"(edit me) paths, URLs or repos to consult\"",
        ) {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
            engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(
                AiLoopEvent::ErrorExecution,
                "<data id='reference'> expr failed to evaluate",
            ));
        }

        // W3C SCXML 5.2/5.3: Initialize 'start_prompt' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se, &sid, "start_prompt", "(_scxml_tostring((_scxml_tostring(_scxml_add((_scxml_tostring((_scxml_tostring(_scxml_add((_scxml_tostring((_scxml_tostring((\"North star: \" .. _scxml_tostring(north_star))) .. \"\\n\")) .. \"Milestone: \"), milestone)) .. \"\\n\")) .. \"Reference: \"), reference)) .. \"\\n\")) .. \"Report what you did and what is left.\")") {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
            engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(AiLoopEvent::ErrorExecution, "<data id='start_prompt'> expr failed to evaluate"));
        }

        // W3C SCXML 5.2/5.3: Initialize 'turn_prompt' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se, &sid, "turn_prompt", "(_scxml_tostring((_scxml_tostring((\"Continue toward: \" .. _scxml_tostring(milestone))) .. \"\\n\")) .. \"Do the next smallest thing that is verifiable, then report.\")") {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
            engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(AiLoopEvent::ErrorExecution, "<data id='turn_prompt'> expr failed to evaluate"));
        }

        // W3C SCXML 5.2/5.3: Initialize 'end_prompt' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se,
            &sid,
            "end_prompt",
            "\"Summarise what changed, what was verified, and what is left open.\"",
        ) {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
            engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(
                AiLoopEvent::ErrorExecution,
                "<data id='end_prompt'> expr failed to evaluate",
            ));
        }

        // W3C SCXML 5.2/5.3: Initialize 'done_marker' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se,
            &sid,
            "done_marker",
            "\"MILESTONE REACHED\"",
        ) {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
            engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(
                AiLoopEvent::ErrorExecution,
                "<data id='done_marker'> expr failed to evaluate",
            ));
        }

        // W3C SCXML 5.2/5.3: Initialize 'screen_rules' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se, &sid, "screen_rules", "{{[\"when\"] = \"design-decision\", [\"keys\"] = \"Escape\", [\"text\"] = \"Ignore cost. Rethink for the most durable answer, then proceed.\"}, {[\"when\"] = \"design-proposal\", [\"keys\"] = \"Escape\", [\"text\"] = \"Ignore cost. Rethink for the most durable answer, then proceed.\"}, {[\"when\"] = \"multiple-choice\", [\"keys\"] = \"Escape\", [\"text\"] = \"Ignore cost. Rethink for the most durable answer, then proceed.\"}}") {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
            engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(AiLoopEvent::ErrorExecution, "<data id='screen_rules'> expr failed to evaluate"));
        }

        // W3C SCXML 5.2/5.3: Initialize 'screen_permissions' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se,
            &sid,
            "screen_permissions",
            "false",
        ) {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
            engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(
                AiLoopEvent::ErrorExecution,
                "<data id='screen_permissions'> expr failed to evaluate",
            ));
        }

        // W3C SCXML 5.2/5.3: Initialize 'max_turns' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se,
            &sid,
            "max_turns",
            "40",
        ) {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
            engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(
                AiLoopEvent::ErrorExecution,
                "<data id='max_turns'> expr failed to evaluate",
            ));
        }

        // W3C SCXML 5.2/5.3: Initialize 'reflect_every' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se,
            &sid,
            "reflect_every",
            "8",
        ) {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
            engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(
                AiLoopEvent::ErrorExecution,
                "<data id='reflect_every'> expr failed to evaluate",
            ));
        }

        // W3C SCXML 5.2/5.3: Initialize 'max_restarts' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se,
            &sid,
            "max_restarts",
            "6",
        ) {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
            engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(
                AiLoopEvent::ErrorExecution,
                "<data id='max_restarts'> expr failed to evaluate",
            ));
        }

        // W3C SCXML 5.2/5.3: Initialize 'turns' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se, &sid, "turns", "0",
        ) {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
            engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(
                AiLoopEvent::ErrorExecution,
                "<data id='turns'> expr failed to evaluate",
            ));
        }

        // W3C SCXML 5.2/5.3: Initialize 'turns_since_reflect' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se,
            &sid,
            "turns_since_reflect",
            "0",
        ) {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
            engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(
                AiLoopEvent::ErrorExecution,
                "<data id='turns_since_reflect'> expr failed to evaluate",
            ));
        }

        // W3C SCXML 5.2/5.3: Initialize 'screened' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se, &sid, "screened", "0",
        ) {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
            engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(
                AiLoopEvent::ErrorExecution,
                "<data id='screened'> expr failed to evaluate",
            ));
        }

        // W3C SCXML 5.2/5.3: Initialize 'restarts' from expr (global)
        if let Err(e) = sce_rust_runtime::helpers::datamodel_init::initialize_variable_from_expr(
            se, &sid, "restarts", "0",
        ) {
            ::sce_rust_runtime::sce_log_error!("global: {}", e);
            engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(
                AiLoopEvent::ErrorExecution,
                "<data id='restarts'> expr failed to evaluate",
            ));
        }

        // W3C SCXML 5.9.2: Register In() state query callback
        {
            let shared_states = std::sync::Arc::new(std::sync::Mutex::new(Vec::<String>::new()));
            self.in_predicate_states = Some(shared_states.clone());
            // Seeded with the configuration as it stands now; every later change
            // to it republishes (`activate` / `deactivate` / `set_active_states`).
            self.publish_in_predicate_states();
            se.set_state_query_callback(
                &sid,
                Some(Box::new(move |state_id: &str| -> bool {
                    if let Ok(states) = shared_states.lock() {
                        states.iter().any(|s| s == state_id)
                    } else {
                        false
                    }
                })),
            );
        }

        self.script_engine_initialized = true;
    }

    // W3C SCXML 5.9: Safe guard evaluation with error handling
    fn safe_evaluate_guard(&mut self, cond: &str, engine: &mut Engine<Self>) -> bool {
        self.ensure_script_engine();
        let sid = self.session_id.as_ref().unwrap().clone();
        let se = self.script_engine.clone();
        let se: &dyn sce_rust_runtime::IScriptEngine = &*se;
        match se.evaluate_expression(&sid, cond) {
            Ok(val) => val.to_bool(),
            Err(e) => {
                ::sce_rust_runtime::sce_log_error!("Guard evaluation failed for '{}': {}", cond, e);
                engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(
                    AiLoopEvent::ErrorExecution,
                    "a <transition> cond failed to evaluate",
                ));
                false
            }
        }
    }

    // W3C SCXML 5.10: Set _event system variable for current event
    //
    // W3C SCXML C.1: `_event.origin` is the sender's published
    // `_ioprocessors` location, not its bare session id — and this is the one
    // place that publishes `_event` to the document, so this is where the id
    // becomes a location. The engine keeps the bare id in
    // `EventMetadata::origin` because its session-keyed lookups match on it;
    // converting at the raise would make one value serve two consumers that
    // need different spellings. The conversion itself lives in
    // `helpers::io_processors::published_origin`, the port of the
    // `IOProcessorHelper::publishedOrigin` the C++ engines share: a second
    // spelling of the rule is how the backends would stop agreeing.
    // Returns which rung of §scxml-B-2-8-1 the payload got, because the
    // binding is the only place that knows. This used to be `let _ = ...`:
    // the ladder decided between a DOM, a value and a space-normalized string,
    // and the decision was dropped one line later. A payload that announced
    // structure and would not parse therefore reached the document as raw
    // characters, every `_event.data.<field>` read empty, and nothing anywhere
    // could say so — see `sce_rust_runtime::PayloadReading`.
    //
    // A session that has not been created yet binds nothing, which is
    // `Absent` rather than a failure: there is no payload in play.
    fn set_current_event_in_script_engine(
        &self,
        event_name: &str,
        event_data: &str,
        event_type: &str,
        send_id: &str,
        origin: &str,
        origin_type: &str,
        invoke_id: &str,
    ) -> sce_rust_runtime::PayloadReading {
        if let Some(ref sid) = self.session_id {
            let se = self.script_engine.clone();
            let se: &dyn sce_rust_runtime::IScriptEngine = &*se;
            let published = sce_rust_runtime::helpers::io_processors::published_origin(origin);
            se.set_current_event(
                sid,
                sce_rust_runtime::SetCurrentEventArgs {
                    event_name,
                    event_data,
                    event_type,
                    send_id,
                    origin: &published,
                    origin_type,
                    invoke_id,
                },
            )
            // An engine that could not bind `_event` at all has a larger
            // problem than an unreadable payload, and reporting `Undecodable`
            // for it would point a host at the wrong thing.
            .unwrap_or(sce_rust_runtime::PayloadReading::Absent)
        } else {
            sce_rust_runtime::PayloadReading::Absent
        }
    }

    // W3C SCXML 5.9.2: Check if a state name matches a state variant
    fn state_name_matches(state: AiLoopState, name: &str) -> bool {
        Self::get_state_name(state) == name
    }

    // §scxml-6.4.3: seed a `<data>` this (child) session declares with the
    // value the INVOKING session produced for an `<invoke>` `<param>` or
    // namelist location.
    //
    // The value crosses as a value. This used to take the parent's value
    // rendered as an engine literal and re-evaluate it here, which survived
    // every value a test had tried and lost the ones no Lua grammar can
    // spell: `to_script_literal` renders a non-finite number as `inf`, and
    // `inf` is an undeclared identifier to the engine that reads it back, so
    // an `<invoke>` `<param expr="1/0"/>` arrived as nothing at all. The
    // Interpreter, C++ AOT, C11, Python and Kotlin all pass the value; this
    // is the channel that did not.
    pub fn set_param_value_in_script_engine(
        &mut self,
        name: &str,
        value: sce_rust_runtime::ScriptValue,
    ) {
        self.ensure_script_engine();
        let sid = self.session_id.as_ref().unwrap().clone();
        let se = self.script_engine.clone();
        let se: &dyn sce_rust_runtime::IScriptEngine = &*se;
        let _ = se.set_variable(&sid, name, value);
    }
}

// ======================================================================
// StatePolicy trait implementation
// ======================================================================

impl StatePolicy for AiLoopPolicy {
    type State = AiLoopState;
    type Event = AiLoopEvent;
    type History = AiLoopHistory;
    // EventSchema native lowering: `()` = schemaless (dynamic
    // `_event.data` baseline); a `<Machine>Payload` sum is emitted when a
    // transition guard reads a typed `_event.data.<field>` (NL→IR C1 Path A).
    type Payload = ();
    type Hal = sce_rust_runtime::StdHal;

    // W3C SCXML Appendix D internalQueue / externalQueue. Under no_std the
    // depth is the per-document `<scxml sce:capacity>` / deploy value (emitted
    // as the const consumed below); absent a declared capacity the bare form
    // inherits the runtime default `MAX_EVENT_QUEUE_DEPTH` (the single source of
    // the default). Inert under std (`VecDeque`), so this one emission compiles
    // on both runtime profiles.
    type EventQueue = sce_rust_runtime::EventQueueManager<
        sce_rust_runtime::EventWithMetadata<Self::Event, Self::Payload>,
    >;

    // W3C SCXML 6.3: the delayed-send scheduler keeps a per-entry `send_id` only
    // so `<cancel sendid>` can find and drop the matching entry; the timer-fire
    // drain never reads it. A document with no `<cancel>` therefore emits the
    // zero-size `ElidedSendId`, shedding the scheduler ring's per-entry
    // `heapless::String<256>` (~256 B x MAX_SCHEDULED_EVENTS) under no_std --
    // dead weight with no cancel reader. A cancelling document keeps the
    // load-bearing `SceString`. Behaviour-preserving under std either way, so
    // this one emission compiles on both runtime profiles. Mirrors the
    // EventQueue per-machine sizing lever above.
    type ScheduledSendId = sce_rust_runtime::ElidedSendId;

    // W3C SCXML feature flags
    const HAS_PARALLEL_STATES: bool = true;
    const NEEDS_SCRIPT_ENGINE: bool = true;
    const NEEDS_DATA_MODEL_INIT: bool = true;
    // The same fact the generate manifest publishes as `needs_event_scheduler`.
    // A `build.rs` consumer never sees that manifest — `compile_scxml` returns
    // `()` — so without this constant a host had no route to the knowledge that
    // its driving loop must call `tick()` rather than `step()`.
    const NEEDS_EVENT_SCHEDULER: bool = false;
    const HAS_ACTIVE_STATES: bool = true;
    const HAS_EXTERNAL_EVENT_FLAG: bool = true;

    // ======================================================================
    // Static metadata methods (W3C SCXML document structure)
    // ======================================================================

    fn initial_state() -> Self::State {
        // The runtime entry state and the type's `Default` derive are one
        // concept, sourced from one computation (`default_state_id`) so
        // they can never diverge: `<scxml initial>`, or the first sorted
        // state when the initial attribute is unset.
        AiLoopState::Run
    }

    fn is_final_state(state: Self::State) -> bool {
        match state {
            AiLoopState::Abandoned => true,
            AiLoopState::Blocked => true,
            AiLoopState::Cancelled => true,
            AiLoopState::Converged => true,
            AiLoopState::Exhausted => true,
            AiLoopState::Failed => true,
            AiLoopState::Reported => true,
            AiLoopState::Stuck => true,
            _ => false,
        }
    }

    fn get_parent(state: Self::State) -> Option<Self::State> {
        match state {
            AiLoopState::Abandoned => Some(AiLoopState::Drive),
            AiLoopState::Alive => Some(AiLoopState::Watch),
            AiLoopState::Budget => Some(AiLoopState::Run),
            AiLoopState::Closing => Some(AiLoopState::Running),
            AiLoopState::Drive => Some(AiLoopState::Run),
            AiLoopState::Judging => Some(AiLoopState::Running),
            AiLoopState::Paused => Some(AiLoopState::Drive),
            AiLoopState::Priming => Some(AiLoopState::Running),
            AiLoopState::Rebuilding => Some(AiLoopState::Watch),
            AiLoopState::Reflecting => Some(AiLoopState::Running),
            AiLoopState::Reported => Some(AiLoopState::Running),
            AiLoopState::Restarting => Some(AiLoopState::Running),
            AiLoopState::Running => Some(AiLoopState::Drive),
            AiLoopState::Screening => Some(AiLoopState::Running),
            AiLoopState::Spent => Some(AiLoopState::Budget),
            AiLoopState::Stuck => Some(AiLoopState::Running),
            AiLoopState::Watch => Some(AiLoopState::Run),
            AiLoopState::Within => Some(AiLoopState::Budget),
            AiLoopState::Working => Some(AiLoopState::Running),
            _ => None,
        }
    }

    // W3C SCXML 3.3: a <state> with child states — exactly the states that have
    // an initial transition. A <parallel> is not compound.
    fn is_compound_state(state: Self::State) -> bool {
        match state {
            AiLoopState::Budget => true,
            AiLoopState::Drive => true,
            AiLoopState::Running => true,
            AiLoopState::Watch => true,
            _ => false,
        }
    }

    fn is_parallel_state(state: Self::State) -> bool {
        match state {
            AiLoopState::Run => true,
            _ => false,
        }
    }

    // §scxml-D-getChildStates: a state's <state>, <parallel> and <final>
    // children, in document order — for a <parallel>, its regions.
    fn get_child_states(state: Self::State) -> &'static [Self::State] {
        match state {
            AiLoopState::Budget => &[AiLoopState::Within, AiLoopState::Spent],
            AiLoopState::Drive => &[
                AiLoopState::Running,
                AiLoopState::Paused,
                AiLoopState::Abandoned,
            ],
            AiLoopState::Run => &[AiLoopState::Drive, AiLoopState::Watch, AiLoopState::Budget],
            AiLoopState::Running => &[
                AiLoopState::Priming,
                AiLoopState::Working,
                AiLoopState::Screening,
                AiLoopState::Judging,
                AiLoopState::Reflecting,
                AiLoopState::Restarting,
                AiLoopState::Closing,
                AiLoopState::Reported,
                AiLoopState::Stuck,
            ],
            AiLoopState::Watch => &[AiLoopState::Alive, AiLoopState::Rebuilding],
            _ => &[],
        }
    }

    // §scxml-3.3: a compound state's initial transition target, as written —
    // the engine's entry procedures dereference a <history> among them.
    fn get_initial_targets(
        state: Self::State,
    ) -> &'static [::sce_rust_runtime::EntryTarget<Self::State, Self::History>] {
        match state {
            AiLoopState::Budget => &[::sce_rust_runtime::EntryTarget::State(AiLoopState::Within)],
            AiLoopState::Drive => &[::sce_rust_runtime::EntryTarget::State(AiLoopState::Running)],
            AiLoopState::Running => &[::sce_rust_runtime::EntryTarget::State(AiLoopState::Priming)],
            AiLoopState::Watch => &[::sce_rust_runtime::EntryTarget::State(AiLoopState::Alive)],
            _ => &[],
        }
    }

    // §scxml-3.2: the target of the document's own initial transition, as written.
    fn get_document_initial_targets(
    ) -> &'static [::sce_rust_runtime::EntryTarget<Self::State, Self::History>] {
        &[::sce_rust_runtime::EntryTarget::State(AiLoopState::Run)]
    }

    // §scxml-3.10: the state a <history> is declared in.
    fn get_history_parent(history: Self::History) -> Self::State {
        match history {
            AiLoopHistory::Where => AiLoopState::Running,
        }
    }

    // §scxml-3.10.2: a <history>'s default transition target, as written.
    fn get_history_default_targets(
        history: Self::History,
    ) -> &'static [::sce_rust_runtime::EntryTarget<Self::State, Self::History>] {
        match history {
            AiLoopHistory::Where => &[::sce_rust_runtime::EntryTarget::State(AiLoopState::Working)],
        }
    }

    fn get_document_order(state: Self::State) -> u32 {
        match state {
            AiLoopState::Abandoned => 13,
            AiLoopState::Alive => 15,
            AiLoopState::Blocked => 24,
            AiLoopState::Budget => 17,
            AiLoopState::Cancelled => 23,
            AiLoopState::Closing => 9,
            AiLoopState::Converged => 20,
            AiLoopState::Drive => 1,
            AiLoopState::Exhausted => 21,
            AiLoopState::Failed => 22,
            AiLoopState::Judging => 6,
            AiLoopState::Paused => 12,
            AiLoopState::Priming => 3,
            AiLoopState::Rebuilding => 16,
            AiLoopState::Reflecting => 7,
            AiLoopState::Reported => 10,
            AiLoopState::Restarting => 8,
            AiLoopState::Run => 0,
            AiLoopState::Running => 2,
            AiLoopState::Screening => 5,
            AiLoopState::Spent => 19,
            AiLoopState::Stuck => 11,
            AiLoopState::Watch => 14,
            AiLoopState::Within => 18,
            AiLoopState::Working => 4,
        }
    }

    fn get_event_name(event: Self::Event) -> &'static str {
        match event {
            AiLoopEvent::Cancel => "cancel",
            AiLoopEvent::DoneStateDrive => "done.state.drive",
            AiLoopEvent::DoneStateRun => "done.state.run",
            AiLoopEvent::DoneStateRunning => "done.state.running",
            AiLoopEvent::ErrorExecution => "error.execution",
            AiLoopEvent::Fail => "fail",
            AiLoopEvent::Hold => "hold",
            AiLoopEvent::Judge => "judge",
            AiLoopEvent::JudgeBegin => "judge.begin",
            AiLoopEvent::NotifyHuman => "notify.human",
            AiLoopEvent::PromptEnd => "prompt.end",
            AiLoopEvent::PromptSent => "prompt.sent",
            AiLoopEvent::PromptStart => "prompt.start",
            AiLoopEvent::PromptTurn => "prompt.turn",
            AiLoopEvent::ReflectApplied => "reflect.applied",
            AiLoopEvent::ReflectBegin => "reflect.begin",
            AiLoopEvent::ReflectNone => "reflect.none",
            AiLoopEvent::Resume => "resume",
            AiLoopEvent::RunBlocked => "run.blocked",
            AiLoopEvent::RunConverged => "run.converged",
            AiLoopEvent::RunExhausted => "run.exhausted",
            AiLoopEvent::ScreenBegin => "screen.begin",
            AiLoopEvent::ScreenMatched => "screen.matched",
            AiLoopEvent::ScreenNone => "screen.none",
            AiLoopEvent::SessionLost => "session.lost",
            AiLoopEvent::SessionReady => "session.ready",
            AiLoopEvent::SessionReplace => "session.replace",
            AiLoopEvent::TurnBlocked => "turn.blocked",
            AiLoopEvent::TurnDone => "turn.done",
            AiLoopEvent::TurnInterrupted => "turn.interrupted",
            AiLoopEvent::Unattended => "unattended",
            AiLoopEvent::Null => "",
        }
    }

    fn get_event_from_name(name: &str) -> Option<Self::Event> {
        match name {
            "cancel" => Some(AiLoopEvent::Cancel),
            "done.state.drive" => Some(AiLoopEvent::DoneStateDrive),
            "done.state.run" => Some(AiLoopEvent::DoneStateRun),
            "done.state.running" => Some(AiLoopEvent::DoneStateRunning),
            "error.execution" => Some(AiLoopEvent::ErrorExecution),
            "fail" => Some(AiLoopEvent::Fail),
            "hold" => Some(AiLoopEvent::Hold),
            "judge" => Some(AiLoopEvent::Judge),
            "judge.begin" => Some(AiLoopEvent::JudgeBegin),
            "notify.human" => Some(AiLoopEvent::NotifyHuman),
            "prompt.end" => Some(AiLoopEvent::PromptEnd),
            "prompt.sent" => Some(AiLoopEvent::PromptSent),
            "prompt.start" => Some(AiLoopEvent::PromptStart),
            "prompt.turn" => Some(AiLoopEvent::PromptTurn),
            "reflect.applied" => Some(AiLoopEvent::ReflectApplied),
            "reflect.begin" => Some(AiLoopEvent::ReflectBegin),
            "reflect.none" => Some(AiLoopEvent::ReflectNone),
            "resume" => Some(AiLoopEvent::Resume),
            "run.blocked" => Some(AiLoopEvent::RunBlocked),
            "run.converged" => Some(AiLoopEvent::RunConverged),
            "run.exhausted" => Some(AiLoopEvent::RunExhausted),
            "screen.begin" => Some(AiLoopEvent::ScreenBegin),
            "screen.matched" => Some(AiLoopEvent::ScreenMatched),
            "screen.none" => Some(AiLoopEvent::ScreenNone),
            "session.lost" => Some(AiLoopEvent::SessionLost),
            "session.ready" => Some(AiLoopEvent::SessionReady),
            "session.replace" => Some(AiLoopEvent::SessionReplace),
            "turn.blocked" => Some(AiLoopEvent::TurnBlocked),
            "turn.done" => Some(AiLoopEvent::TurnDone),
            "turn.interrupted" => Some(AiLoopEvent::TurnInterrupted),
            "unattended" => Some(AiLoopEvent::Unattended),
            _ => None,
        }
    }

    fn get_state_name(state: Self::State) -> &'static str {
        match state {
            AiLoopState::Abandoned => "abandoned",
            AiLoopState::Alive => "alive",
            AiLoopState::Blocked => "blocked",
            AiLoopState::Budget => "budget",
            AiLoopState::Cancelled => "cancelled",
            AiLoopState::Closing => "closing",
            AiLoopState::Converged => "converged",
            AiLoopState::Drive => "drive",
            AiLoopState::Exhausted => "exhausted",
            AiLoopState::Failed => "failed",
            AiLoopState::Judging => "judging",
            AiLoopState::Paused => "paused",
            AiLoopState::Priming => "priming",
            AiLoopState::Rebuilding => "rebuilding",
            AiLoopState::Reflecting => "reflecting",
            AiLoopState::Reported => "reported",
            AiLoopState::Restarting => "restarting",
            AiLoopState::Run => "run",
            AiLoopState::Running => "running",
            AiLoopState::Screening => "screening",
            AiLoopState::Spent => "spent",
            AiLoopState::Stuck => "stuck",
            AiLoopState::Watch => "watch",
            AiLoopState::Within => "within",
            AiLoopState::Working => "working",
        }
    }

    // The inverse of the table above, emitted from the same loop over the
    // document's states so the two age together. It is what lets a host turn a
    // recorded configuration back into the `StateChain` `enter_at` takes.
    fn get_state_from_name(name: &str) -> Option<Self::State> {
        match name {
            "abandoned" => Some(AiLoopState::Abandoned),
            "alive" => Some(AiLoopState::Alive),
            "blocked" => Some(AiLoopState::Blocked),
            "budget" => Some(AiLoopState::Budget),
            "cancelled" => Some(AiLoopState::Cancelled),
            "closing" => Some(AiLoopState::Closing),
            "converged" => Some(AiLoopState::Converged),
            "drive" => Some(AiLoopState::Drive),
            "exhausted" => Some(AiLoopState::Exhausted),
            "failed" => Some(AiLoopState::Failed),
            "judging" => Some(AiLoopState::Judging),
            "paused" => Some(AiLoopState::Paused),
            "priming" => Some(AiLoopState::Priming),
            "rebuilding" => Some(AiLoopState::Rebuilding),
            "reflecting" => Some(AiLoopState::Reflecting),
            "reported" => Some(AiLoopState::Reported),
            "restarting" => Some(AiLoopState::Restarting),
            "run" => Some(AiLoopState::Run),
            "running" => Some(AiLoopState::Running),
            "screening" => Some(AiLoopState::Screening),
            "spent" => Some(AiLoopState::Spent),
            "stuck" => Some(AiLoopState::Stuck),
            "watch" => Some(AiLoopState::Watch),
            "within" => Some(AiLoopState::Within),
            "working" => Some(AiLoopState::Working),
            _ => None,
        }
    }

    fn null_event() -> Self::Event {
        AiLoopEvent::Null
    }

    // ======================================================================
    // Run-time state the entry procedures read
    // ======================================================================

    // §scxml-3.10: what a <history> recorded when its parent was last exited;
    // None before that ever happened.
    fn history_value(&self, history: Self::History) -> Option<&[Self::State]> {
        match history {
            AiLoopHistory::Where => self.history_where.as_deref(),
        }
    }

    // SCE Protocol-Synthesis RFC §synth-5-J-2: trait default in `sce-rust-runtime::policy`
    // returns `StateChain<Self::State>`; this override matches that signature
    // so the std alias-to-`Vec<S>` and no_std alias-to-`heapless::Vec<S, …>`
    // both resolve correctly.
    fn get_active_states(&self) -> ::sce_rust_runtime::helpers::hierarchy::StateChain<Self::State> {
        self.active_states.clone()
    }

    // §scxml-3.4: the write half of the pair above, for `Engine::enter_at`.
    // Emitted under the same condition as the read half because the same fact
    // decides both — this machine keeps its own active set, so this machine is
    // the one that can be handed one back. The trait default is a tripwire, so
    // emitting the reader without this one would turn a restore into a panic.
    fn set_active_states(
        &mut self,
        states: ::sce_rust_runtime::helpers::hierarchy::StateChain<Self::State>,
    ) {
        self.active_states = states;
        self.publish_in_predicate_states();
    }

    fn set_next_event_is_external(&mut self, value: bool) {
        self.next_event_is_external = value;
    }

    // W3C SCXML 5.10: Populate pending event metadata from EventWithMetadata
    // Ports C++ EventMetadataHelper::populatePolicyFromMetadata
    fn populate_event_metadata(&mut self, metadata: &sce_rust_runtime::EventMetadata) {
        self.pending_event_data = metadata.data.clone();
        self.pending_event_type =
            ::sce_rust_runtime::sce_string_from_str(metadata.event_type.as_str());
        self.pending_event_sendid = metadata.send_id.clone();
        self.pending_event_origin = metadata.origin.clone();
        self.pending_event_origintype = metadata.origin_type.clone();
        self.pending_event_invokeid = metadata.invoke_id.clone();
    }

    // W3C SCXML 5.10: Clear pending event metadata after transition processing
    // Ports C++ EventMetadataHelper::clearPolicyMetadata
    fn clear_event_metadata(&mut self) {
        self.pending_event_name.clear();
        self.pending_event_data.clear();
        self.pending_event_type.clear();
        self.pending_event_sendid.clear();
        self.pending_event_origin.clear();
        self.pending_event_origintype.clear();
        self.pending_event_invokeid.clear();
    }

    // ======================================================================
    // Instance methods - generated executable content
    // ======================================================================

    // W3C SCXML 3.7: Execute <onentry> actions for a state
    #[doc = "SCE-MAP: ai_loop.scxml:155 :: _machine"]
    // SCE-MAP: ai_loop.scxml:155 :: _machine
    fn execute_entry_actions(
        &mut self,
        state: Self::State,
        engine: &mut sce_rust_runtime::Engine<Self>,
        is_default_entry: bool,
    ) {
        // W3C SCXML 3.4/3.12.1: Add state to active configuration for parallel states and In() predicate
        if !self.activate(state) {
            return; // W3C SCXML 3.8: Skip onentry actions for duplicate state entry
        }
        match state {
            AiLoopState::Abandoned => {
                // SCE-MAP: ai_loop.scxml:493 :: abandoned :: _state_body
                // W3C SCXML 3.8: onentry block 1/1
                // Labeled block allows actions to break out on error (W3C 3.8: error stops block)
                'action_block: {
                    // W3C SCXML 3.8.1: <raise event="run.blocked">
                    engine.raise(sce_rust_runtime::EventWithMetadata::new(
                        AiLoopEvent::RunBlocked,
                    ));
                }
                // W3C SCXML 3.7: Generate done.state.drive event
                engine.raise(sce_rust_runtime::EventWithMetadata::new(
                    AiLoopEvent::DoneStateDrive,
                ));
                // W3C SCXML 3.4 / §scxml-D-enterStates: a region of
                // run has reached a final state, and the
                // <parallel> completes when every one of its child states is in
                // one — Appendix D's isInFinalState, which counts a region that
                // is itself a <parallel> only once all of ITS regions are.
                {
                    let parallel_state = AiLoopState::Run;
                    if ::sce_rust_runtime::helpers::microstep::is_in_final_state(
                        &::sce_rust_runtime::PolicyDocument(&*self),
                        parallel_state,
                        &self.active_states,
                    ) {
                        engine.raise(sce_rust_runtime::EventWithMetadata::new(
                            AiLoopEvent::DoneStateRun,
                        ));
                    }
                }
            }
            AiLoopState::Closing => {
                // SCE-MAP: ai_loop.scxml:405 :: closing :: _state_body
                // W3C SCXML 3.8: onentry block 1/1
                // Labeled block allows actions to break out on error (W3C 3.8: error stops block)
                'action_block: {
                    {
                        let send_id = ::sce_rust_runtime::sce_string_from_str("__send_7");

                        // W3C SCXML 6.2 / test178: a name may repeat and every value must be
                        // delivered, so each name carries a vector. The typed value is kept
                        // rather than its text — a receiver reading `_event.data.value === 42`
                        // finds the string "42" unequal.
                        //
                        // Declared out here rather than inside the payload block because
                        // §scxml-6.2.3 evaluates a `<send>`'s arguments ONCE, and the transports
                        // below are renderings of that one evaluation: the BasicHTTP and
                        // host-served arms read this map instead of asking the data model again.
                        // While it was block-scoped they had to, and what they re-read was
                        // `<param>` alone — so `namelist="Var1"` reached `_event.data` and then
                        // posted zero form parameters, against §scxml-C-2.
                        let mut _send_wire_params: ::std::collections::BTreeMap<
                            String,
                            Vec<::sce_rust_runtime::ScriptValue>,
                        > = ::std::collections::BTreeMap::new();
                        // W3C SCXML 6.2: Evaluate <param>/namelist expressions at send time
                        let event_data_string: String = {
                            self.ensure_script_engine();
                            let sid = self.session_id.as_ref().unwrap().clone();
                            let se = self.script_engine.clone();
                            let se: &dyn sce_rust_runtime::IScriptEngine = &*se;
                            let wire_params = &mut _send_wire_params;
                            match se.evaluate_expression(&sid, "end_prompt") {
                                Ok(val) => {
                                    wire_params.entry("text".to_string()).or_default().push(val);
                                }
                                Err(e) => {
                                    ::sce_rust_runtime::sce_log_error!(
                                        "send param 'text' eval failed: {}",
                                        e
                                    );
                                    engine.raise(
                                        sce_rust_runtime::EventWithMetadata::platform_error(
                                            AiLoopEvent::ErrorExecution,
                                            "<send> <param name='text'> expr failed to evaluate",
                                        ),
                                    );
                                }
                            }
                            ::sce_rust_runtime::helpers::event_data::build_json_from_typed_params(
                                wire_params,
                            )
                        };
                        let event_data: &str = &event_data_string;

                        // W3C SCXML 6.2.5: "x-sce-host" is served by the host,
                        // which declared it to this build. Dispatch rather than refuse.
                        {
                            let host_params = ::sce_rust_runtime::helpers::event_data::typed_params_to_wire_strings(&_send_wire_params);
                            let __sce_request = sce_rust_runtime::HostSendRequest {
                                processor_type: "x-sce-host".to_string(),
                                event_name: "prompt.end".to_string(),
                                target: "".to_string(),
                                content: "".to_string(),
                                params: host_params,
                                send_id: send_id.to_string(),
                            };
                            let __sce_served = engine.perform_host_send(__sce_request);
                            // W3C SCXML 6.2: a declared type with no handler registered is,
                            // from the document's side, a processor the platform does not
                            // support — the act it asked for was performed by nobody. Same
                            // event, so a wiring mistake cannot read as success.
                            if __sce_served.is_none() && !engine.has_event_processor("x-sce-host") {
                                let mut err_meta = sce_rust_runtime::EventWithMetadata::platform_error(AiLoopEvent::ErrorExecution, "<send type='x-sce-host'> names a processor the host declared but never registered");
                                err_meta.metadata.send_id = send_id.clone();
                                engine.raise(err_meta);
                            }
                        }

                        let _ = send_id; // suppress unused warning when no send operation
                        let _ = event_data; // suppress unused warning in branches that skip dispatch
                    }
                }
            }
            AiLoopState::Judging => {
                // SCE-MAP: ai_loop.scxml:344 :: judging :: _state_body
                // W3C SCXML 3.8: onentry block 1/1
                // Labeled block allows actions to break out on error (W3C 3.8: error stops block)
                'action_block: {
                    {
                        let send_id = ::sce_rust_runtime::sce_string_from_str("__send_3");

                        // W3C SCXML 6.2 / test178: a name may repeat and every value must be
                        // delivered, so each name carries a vector. The typed value is kept
                        // rather than its text — a receiver reading `_event.data.value === 42`
                        // finds the string "42" unequal.
                        //
                        // Declared out here rather than inside the payload block because
                        // §scxml-6.2.3 evaluates a `<send>`'s arguments ONCE, and the transports
                        // below are renderings of that one evaluation: the BasicHTTP and
                        // host-served arms read this map instead of asking the data model again.
                        // While it was block-scoped they had to, and what they re-read was
                        // `<param>` alone — so `namelist="Var1"` reached `_event.data` and then
                        // posted zero form parameters, against §scxml-C-2.
                        let mut _send_wire_params: ::std::collections::BTreeMap<
                            String,
                            Vec<::sce_rust_runtime::ScriptValue>,
                        > = ::std::collections::BTreeMap::new();
                        // W3C SCXML 6.2: Evaluate <param>/namelist expressions at send time
                        let event_data_string: String = {
                            self.ensure_script_engine();
                            let sid = self.session_id.as_ref().unwrap().clone();
                            let se = self.script_engine.clone();
                            let se: &dyn sce_rust_runtime::IScriptEngine = &*se;
                            let wire_params = &mut _send_wire_params;
                            match se.evaluate_expression(&sid, "done_marker") {
                                Ok(val) => {
                                    wire_params
                                        .entry("marker".to_string())
                                        .or_default()
                                        .push(val);
                                }
                                Err(e) => {
                                    ::sce_rust_runtime::sce_log_error!(
                                        "send param 'marker' eval failed: {}",
                                        e
                                    );
                                    engine.raise(
                                        sce_rust_runtime::EventWithMetadata::platform_error(
                                            AiLoopEvent::ErrorExecution,
                                            "<send> <param name='marker'> expr failed to evaluate",
                                        ),
                                    );
                                }
                            }
                            ::sce_rust_runtime::helpers::event_data::build_json_from_typed_params(
                                wire_params,
                            )
                        };
                        let event_data: &str = &event_data_string;

                        // W3C SCXML 6.2.5: "x-sce-host" is served by the host,
                        // which declared it to this build. Dispatch rather than refuse.
                        {
                            let host_params = ::sce_rust_runtime::helpers::event_data::typed_params_to_wire_strings(&_send_wire_params);
                            let __sce_request = sce_rust_runtime::HostSendRequest {
                                processor_type: "x-sce-host".to_string(),
                                event_name: "judge.begin".to_string(),
                                target: "".to_string(),
                                content: "".to_string(),
                                params: host_params,
                                send_id: send_id.to_string(),
                            };
                            let __sce_served = engine.perform_host_send(__sce_request);
                            // W3C SCXML 6.2: a declared type with no handler registered is,
                            // from the document's side, a processor the platform does not
                            // support — the act it asked for was performed by nobody. Same
                            // event, so a wiring mistake cannot read as success.
                            if __sce_served.is_none() && !engine.has_event_processor("x-sce-host") {
                                let mut err_meta = sce_rust_runtime::EventWithMetadata::platform_error(AiLoopEvent::ErrorExecution, "<send type='x-sce-host'> names a processor the host declared but never registered");
                                err_meta.metadata.send_id = send_id.clone();
                                engine.raise(err_meta);
                            }
                        }

                        let _ = send_id; // suppress unused warning when no send operation
                        let _ = event_data; // suppress unused warning in branches that skip dispatch
                    }
                }
            }
            AiLoopState::Paused => {
                // SCE-MAP: ai_loop.scxml:451 :: paused :: _state_body
                // W3C SCXML 3.8: onentry block 1/1
                // Labeled block allows actions to break out on error (W3C 3.8: error stops block)
                'action_block: {
                    {
                        let send_id = ::sce_rust_runtime::sce_string_from_str("__send_8");

                        let event_data: &str = "";

                        // W3C SCXML 6.2.5: "x-sce-host" is served by the host,
                        // which declared it to this build. Dispatch rather than refuse.
                        {
                            let host_params =
                                std::collections::HashMap::<String, Vec<String>>::new();
                            let __sce_request = sce_rust_runtime::HostSendRequest {
                                processor_type: "x-sce-host".to_string(),
                                event_name: "notify.human".to_string(),
                                target: "".to_string(),
                                content: "".to_string(),
                                params: host_params,
                                send_id: send_id.to_string(),
                            };
                            let __sce_served = engine.perform_host_send(__sce_request);
                            // W3C SCXML 6.2: a declared type with no handler registered is,
                            // from the document's side, a processor the platform does not
                            // support — the act it asked for was performed by nobody. Same
                            // event, so a wiring mistake cannot read as success.
                            if __sce_served.is_none() && !engine.has_event_processor("x-sce-host") {
                                let mut err_meta = sce_rust_runtime::EventWithMetadata::platform_error(AiLoopEvent::ErrorExecution, "<send type='x-sce-host'> names a processor the host declared but never registered");
                                err_meta.metadata.send_id = send_id.clone();
                                engine.raise(err_meta);
                            }
                        }

                        let _ = send_id; // suppress unused warning when no send operation
                        let _ = event_data; // suppress unused warning in branches that skip dispatch
                    }
                }
            }
            AiLoopState::Priming => {
                // SCE-MAP: ai_loop.scxml:291 :: priming :: _state_body
                // W3C SCXML 3.8: onentry block 1/1
                // Labeled block allows actions to break out on error (W3C 3.8: error stops block)
                'action_block: {
                    {
                        let send_id = ::sce_rust_runtime::sce_string_from_str("__send_0");

                        // W3C SCXML 6.2 / test178: a name may repeat and every value must be
                        // delivered, so each name carries a vector. The typed value is kept
                        // rather than its text — a receiver reading `_event.data.value === 42`
                        // finds the string "42" unequal.
                        //
                        // Declared out here rather than inside the payload block because
                        // §scxml-6.2.3 evaluates a `<send>`'s arguments ONCE, and the transports
                        // below are renderings of that one evaluation: the BasicHTTP and
                        // host-served arms read this map instead of asking the data model again.
                        // While it was block-scoped they had to, and what they re-read was
                        // `<param>` alone — so `namelist="Var1"` reached `_event.data` and then
                        // posted zero form parameters, against §scxml-C-2.
                        let mut _send_wire_params: ::std::collections::BTreeMap<
                            String,
                            Vec<::sce_rust_runtime::ScriptValue>,
                        > = ::std::collections::BTreeMap::new();
                        // W3C SCXML 6.2: Evaluate <param>/namelist expressions at send time
                        let event_data_string: String = {
                            self.ensure_script_engine();
                            let sid = self.session_id.as_ref().unwrap().clone();
                            let se = self.script_engine.clone();
                            let se: &dyn sce_rust_runtime::IScriptEngine = &*se;
                            let wire_params = &mut _send_wire_params;
                            match se.evaluate_expression(&sid, "start_prompt") {
                                Ok(val) => {
                                    wire_params.entry("text".to_string()).or_default().push(val);
                                }
                                Err(e) => {
                                    ::sce_rust_runtime::sce_log_error!(
                                        "send param 'text' eval failed: {}",
                                        e
                                    );
                                    engine.raise(
                                        sce_rust_runtime::EventWithMetadata::platform_error(
                                            AiLoopEvent::ErrorExecution,
                                            "<send> <param name='text'> expr failed to evaluate",
                                        ),
                                    );
                                }
                            }
                            ::sce_rust_runtime::helpers::event_data::build_json_from_typed_params(
                                wire_params,
                            )
                        };
                        let event_data: &str = &event_data_string;

                        // W3C SCXML 6.2.5: "x-sce-host" is served by the host,
                        // which declared it to this build. Dispatch rather than refuse.
                        {
                            let host_params = ::sce_rust_runtime::helpers::event_data::typed_params_to_wire_strings(&_send_wire_params);
                            let __sce_request = sce_rust_runtime::HostSendRequest {
                                processor_type: "x-sce-host".to_string(),
                                event_name: "prompt.start".to_string(),
                                target: "".to_string(),
                                content: "".to_string(),
                                params: host_params,
                                send_id: send_id.to_string(),
                            };
                            let __sce_served = engine.perform_host_send(__sce_request);
                            // W3C SCXML 6.2: a declared type with no handler registered is,
                            // from the document's side, a processor the platform does not
                            // support — the act it asked for was performed by nobody. Same
                            // event, so a wiring mistake cannot read as success.
                            if __sce_served.is_none() && !engine.has_event_processor("x-sce-host") {
                                let mut err_meta = sce_rust_runtime::EventWithMetadata::platform_error(AiLoopEvent::ErrorExecution, "<send type='x-sce-host'> names a processor the host declared but never registered");
                                err_meta.metadata.send_id = send_id.clone();
                                engine.raise(err_meta);
                            }
                        }

                        let _ = send_id; // suppress unused warning when no send operation
                        let _ = event_data; // suppress unused warning in branches that skip dispatch
                    }
                }
            }
            AiLoopState::Reflecting => {
                // SCE-MAP: ai_loop.scxml:374 :: reflecting :: _state_body
                // W3C SCXML 3.8: onentry block 1/1
                // Labeled block allows actions to break out on error (W3C 3.8: error stops block)
                'action_block: {
                    {
                        // W3C SCXML 5.3: <assign location="turns_since_reflect">
                        self.ensure_script_engine();
                        let sid = self.session_id.as_ref().unwrap().clone();
                        let se = self.script_engine.clone();
                        let se: &dyn sce_rust_runtime::IScriptEngine = &*se;
                        let expr = "0";
                        // W3C SCXML 5.3: Assign via execute_script preserves Lua reference identity for
                        // table values (e.g. `Var2 = _event` — test 329 requires `Var2 == _event`). Going
                        // through evaluate_expression + set_variable would round-trip through ScriptValue
                        // and create a fresh table, breaking reference equality.
                        let assign_script = format!("{} = {}", "turns_since_reflect", expr);
                        if let Err(e) = se.execute_script(&sid, &assign_script) {
                            ::sce_rust_runtime::sce_log_error!(
                                "Assign failed for 'turns_since_reflect': {}",
                                e
                            );
                            engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(
                                AiLoopEvent::ErrorExecution,
                                "<assign> to 'turns_since_reflect' failed",
                            ));
                            // W3C SCXML 3.8/3.9: Error stops subsequent actions in this onentry/onexit block
                            break 'action_block;
                        }
                    }

                    {
                        let send_id = ::sce_rust_runtime::sce_string_from_str("__send_5");

                        let event_data: &str = "";

                        // W3C SCXML 6.2.5: "x-sce-host" is served by the host,
                        // which declared it to this build. Dispatch rather than refuse.
                        {
                            let host_params =
                                std::collections::HashMap::<String, Vec<String>>::new();
                            let __sce_request = sce_rust_runtime::HostSendRequest {
                                processor_type: "x-sce-host".to_string(),
                                event_name: "reflect.begin".to_string(),
                                target: "".to_string(),
                                content: "".to_string(),
                                params: host_params,
                                send_id: send_id.to_string(),
                            };
                            let __sce_served = engine.perform_host_send(__sce_request);
                            // W3C SCXML 6.2: a declared type with no handler registered is,
                            // from the document's side, a processor the platform does not
                            // support — the act it asked for was performed by nobody. Same
                            // event, so a wiring mistake cannot read as success.
                            if __sce_served.is_none() && !engine.has_event_processor("x-sce-host") {
                                let mut err_meta = sce_rust_runtime::EventWithMetadata::platform_error(AiLoopEvent::ErrorExecution, "<send type='x-sce-host'> names a processor the host declared but never registered");
                                err_meta.metadata.send_id = send_id.clone();
                                engine.raise(err_meta);
                            }
                        }

                        let _ = send_id; // suppress unused warning when no send operation
                        let _ = event_data; // suppress unused warning in branches that skip dispatch
                    }
                }
            }
            AiLoopState::Reported => {
                // SCE-MAP: ai_loop.scxml:426 :: reported :: _state_body
                // W3C SCXML 3.8: onentry block 1/1
                // Labeled block allows actions to break out on error (W3C 3.8: error stops block)
                'action_block: {
                    // W3C SCXML 3.8.1: <raise event="run.converged">
                    engine.raise(sce_rust_runtime::EventWithMetadata::new(
                        AiLoopEvent::RunConverged,
                    ));
                }
                // W3C SCXML 3.7: Generate done.state.running event
                engine.raise(sce_rust_runtime::EventWithMetadata::new(
                    AiLoopEvent::DoneStateRunning,
                ));
            }
            AiLoopState::Restarting => {
                // SCE-MAP: ai_loop.scxml:394 :: restarting :: _state_body
                // W3C SCXML 3.8: onentry block 1/1
                // Labeled block allows actions to break out on error (W3C 3.8: error stops block)
                'action_block: {
                    {
                        // W3C SCXML 5.3: <assign location="restarts">
                        self.ensure_script_engine();
                        let sid = self.session_id.as_ref().unwrap().clone();
                        let se = self.script_engine.clone();
                        let se: &dyn sce_rust_runtime::IScriptEngine = &*se;
                        let expr = "_scxml_add(restarts, 1)";
                        // W3C SCXML 5.3: Assign via execute_script preserves Lua reference identity for
                        // table values (e.g. `Var2 = _event` — test 329 requires `Var2 == _event`). Going
                        // through evaluate_expression + set_variable would round-trip through ScriptValue
                        // and create a fresh table, breaking reference equality.
                        let assign_script = format!("{} = {}", "restarts", expr);
                        if let Err(e) = se.execute_script(&sid, &assign_script) {
                            ::sce_rust_runtime::sce_log_error!(
                                "Assign failed for 'restarts': {}",
                                e
                            );
                            engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(
                                AiLoopEvent::ErrorExecution,
                                "<assign> to 'restarts' failed",
                            ));
                            // W3C SCXML 3.8/3.9: Error stops subsequent actions in this onentry/onexit block
                            break 'action_block;
                        }
                    }

                    {
                        let send_id = ::sce_rust_runtime::sce_string_from_str("__send_6");

                        let event_data: &str = "";

                        // W3C SCXML 6.2.5: "x-sce-host" is served by the host,
                        // which declared it to this build. Dispatch rather than refuse.
                        {
                            let host_params =
                                std::collections::HashMap::<String, Vec<String>>::new();
                            let __sce_request = sce_rust_runtime::HostSendRequest {
                                processor_type: "x-sce-host".to_string(),
                                event_name: "session.replace".to_string(),
                                target: "".to_string(),
                                content: "".to_string(),
                                params: host_params,
                                send_id: send_id.to_string(),
                            };
                            let __sce_served = engine.perform_host_send(__sce_request);
                            // W3C SCXML 6.2: a declared type with no handler registered is,
                            // from the document's side, a processor the platform does not
                            // support — the act it asked for was performed by nobody. Same
                            // event, so a wiring mistake cannot read as success.
                            if __sce_served.is_none() && !engine.has_event_processor("x-sce-host") {
                                let mut err_meta = sce_rust_runtime::EventWithMetadata::platform_error(AiLoopEvent::ErrorExecution, "<send type='x-sce-host'> names a processor the host declared but never registered");
                                err_meta.metadata.send_id = send_id.clone();
                                engine.raise(err_meta);
                            }
                        }

                        let _ = send_id; // suppress unused warning when no send operation
                        let _ = event_data; // suppress unused warning in branches that skip dispatch
                    }
                }
            }
            AiLoopState::Screening => {
                // SCE-MAP: ai_loop.scxml:327 :: screening :: _state_body
                // W3C SCXML 3.8: onentry block 1/1
                // Labeled block allows actions to break out on error (W3C 3.8: error stops block)
                'action_block: {
                    {
                        // W3C SCXML 5.3: <assign location="screened">
                        self.ensure_script_engine();
                        let sid = self.session_id.as_ref().unwrap().clone();
                        let se = self.script_engine.clone();
                        let se: &dyn sce_rust_runtime::IScriptEngine = &*se;
                        let expr = "_scxml_add(screened, 1)";
                        // W3C SCXML 5.3: Assign via execute_script preserves Lua reference identity for
                        // table values (e.g. `Var2 = _event` — test 329 requires `Var2 == _event`). Going
                        // through evaluate_expression + set_variable would round-trip through ScriptValue
                        // and create a fresh table, breaking reference equality.
                        let assign_script = format!("{} = {}", "screened", expr);
                        if let Err(e) = se.execute_script(&sid, &assign_script) {
                            ::sce_rust_runtime::sce_log_error!(
                                "Assign failed for 'screened': {}",
                                e
                            );
                            engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(
                                AiLoopEvent::ErrorExecution,
                                "<assign> to 'screened' failed",
                            ));
                            // W3C SCXML 3.8/3.9: Error stops subsequent actions in this onentry/onexit block
                            break 'action_block;
                        }
                    }

                    {
                        let send_id = ::sce_rust_runtime::sce_string_from_str("__send_1");

                        let event_data: &str = "";

                        // W3C SCXML 6.2.5: "x-sce-host" is served by the host,
                        // which declared it to this build. Dispatch rather than refuse.
                        {
                            let host_params =
                                std::collections::HashMap::<String, Vec<String>>::new();
                            let __sce_request = sce_rust_runtime::HostSendRequest {
                                processor_type: "x-sce-host".to_string(),
                                event_name: "screen.begin".to_string(),
                                target: "".to_string(),
                                content: "".to_string(),
                                params: host_params,
                                send_id: send_id.to_string(),
                            };
                            let __sce_served = engine.perform_host_send(__sce_request);
                            // W3C SCXML 6.2: a declared type with no handler registered is,
                            // from the document's side, a processor the platform does not
                            // support — the act it asked for was performed by nobody. Same
                            // event, so a wiring mistake cannot read as success.
                            if __sce_served.is_none() && !engine.has_event_processor("x-sce-host") {
                                let mut err_meta = sce_rust_runtime::EventWithMetadata::platform_error(AiLoopEvent::ErrorExecution, "<send type='x-sce-host'> names a processor the host declared but never registered");
                                err_meta.metadata.send_id = send_id.clone();
                                engine.raise(err_meta);
                            }
                        }

                        let _ = send_id; // suppress unused warning when no send operation
                        let _ = event_data; // suppress unused warning in branches that skip dispatch
                    }
                }
            }
            AiLoopState::Spent => {
                // SCE-MAP: ai_loop.scxml:529 :: spent :: _state_body
                // W3C SCXML 3.8: onentry block 1/1
                // Labeled block allows actions to break out on error (W3C 3.8: error stops block)
                'action_block: {
                    // W3C SCXML 3.8.1: <raise event="run.exhausted">
                    engine.raise(sce_rust_runtime::EventWithMetadata::new(
                        AiLoopEvent::RunExhausted,
                    ));
                }
            }
            AiLoopState::Stuck => {
                // SCE-MAP: ai_loop.scxml:434 :: stuck :: _state_body
                // W3C SCXML 3.8: onentry block 1/1
                // Labeled block allows actions to break out on error (W3C 3.8: error stops block)
                'action_block: {
                    // W3C SCXML 3.8.1: <raise event="run.exhausted">
                    engine.raise(sce_rust_runtime::EventWithMetadata::new(
                        AiLoopEvent::RunExhausted,
                    ));
                }
                // W3C SCXML 3.7: Generate done.state.running event
                engine.raise(sce_rust_runtime::EventWithMetadata::new(
                    AiLoopEvent::DoneStateRunning,
                ));
            }
            _ => {}
        }
    }

    // §scxml-3.10.2: a <history>'s default transition content, run after its
    // parent's onentry (and after the parent's own <initial> content) when the
    // history was taken with nothing recorded. The engine asks for it by the
    // entry set's defaultHistoryContent answer; a history that restored what it
    // recorded runs nothing.

    // W3C SCXML 3.8: Execute <onexit> actions for a state
    #[doc = "SCE-MAP: ai_loop.scxml:155 :: _machine"]
    // SCE-MAP: ai_loop.scxml:155 :: _machine
    fn execute_exit_actions(
        &mut self,
        state: Self::State,
        engine: &mut sce_rust_runtime::Engine<Self>,
        configuration_before_exit: &[Self::State],
    ) {
        // §scxml-D-exitStates orders one state's exit as onexit, then
        // cancelInvoke, then configuration.delete(s), so `In(s)` inside s's
        // own handler is true: the deactivation is the LAST step here.
        // W3C SCXML 3.10: Record history as this state exits, from the
        // configuration the microstep started from — the engine hands every
        // exit the configuration as it stood before the first one, so every
        // history in one microstep reads the same configuration.
        if state == AiLoopState::Running {
            // W3C SCXML 3.11: shallow history — SCE Protocol-Synthesis
            // RFC §synth-5-J-2: the record is a [`StateChain`] (heapless under
            // no_std), and a subset of the configuration, so it fits the depth bound.
            self.history_where = Some(::sce_rust_runtime::helpers::microstep::recorded_history(
                &::sce_rust_runtime::PolicyDocument(&*self),
                AiLoopState::Running,
                false,
                configuration_before_exit,
            ));
        }
        // §scxml-D-exitStates: out of the configuration only after its onexit
        // and invoke cancellation — the configuration.delete(s) step.
        self.deactivate(state);
    }

    // §scxml-5.10: the event whose transitions are about to be selected is the
    // `_event` their guards read — bound before the first guard runs, and not
    // for an eventless selection, which has no event of its own.
    #[doc = "SCE-MAP: ai_loop.scxml:155 :: _machine"]
    // SCE-MAP: ai_loop.scxml:155 :: _machine
    fn bind_current_event(
        &mut self,
        event: Self::Event,
        engine: &mut sce_rust_runtime::Engine<Self>,
    ) {
        // W3C SCXML 5.10: Ensure script engine and set _event for guard evaluation
        self.ensure_script_engine();
        if event != Self::null_event() {
            let event_name = Self::get_event_name(event);
            self.pending_event_name = event_name.to_string();
            // W3C SCXML 5.10.1: Classify event type (ports C++ EventTypeHelper::classifyEventType)
            let event_type = if event_name.starts_with("error.") || event_name.starts_with("done.")
            {
                "platform"
            } else if self.next_event_is_external {
                self.next_event_is_external = false;
                "external"
            } else {
                "internal"
            };
            self.pending_event_type = event_type.to_string();
            // W3C SCXML 5.10: Set _event with all available metadata fields
            let ev_data: &str = &self.pending_event_data;
            let ev_sendid: &str = &self.pending_event_sendid;
            let ev_origin: &str = &self.pending_event_origin;
            let ev_origintype: &str = &self.pending_event_origintype;
            let ev_invokeid: &str = &self.pending_event_invokeid;
            // §scxml-B-2-8-1: the rung the payload got, handed to the engine
            // rather than dropped. This is the only frame that has both the
            // reading and the event it belongs to.
            let payload_reading = self.set_current_event_in_script_engine(
                event_name,
                ev_data,
                event_type,
                ev_sendid,
                ev_origin,
                ev_origintype,
                ev_invokeid,
            );
            engine.note_payload_reading(event, payload_reading);
        }
    }

    // Appendix D selectTransitions, the half only this document can answer:
    // the first of `state`'s own transitions, in document order, that `event`
    // enables. The engine walks the atomic states and their ancestors and
    // keeps the ordered set. `Event::Null` asks for eventless transitions.
    #[doc = "SCE-MAP: ai_loop.scxml:155 :: _machine"]
    // SCE-MAP: ai_loop.scxml:155 :: _machine
    fn first_enabled_transition(
        &mut self,
        state: Self::State,
        event: Self::Event,
        engine: &mut sce_rust_runtime::Engine<Self>,
    ) -> Option<::sce_rust_runtime::EnabledTransition<Self::State, Self::History>> {
        match state {
            AiLoopState::Alive => {
                if event == AiLoopEvent::SessionLost {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                AiLoopState::Rebuilding,
                            )],
                            transition_index: 0,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                None
            }
            AiLoopState::Closing => {
                if event == AiLoopEvent::TurnDone {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                AiLoopState::Reported,
                            )],
                            transition_index: 0,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                if event == AiLoopEvent::TurnBlocked {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                AiLoopState::Screening,
                            )],
                            transition_index: 1,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                None
            }
            AiLoopState::Drive => {
                if event == AiLoopEvent::Hold {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(AiLoopState::Paused)],
                            transition_index: 0,
                            has_actions: false,
                            is_internal: true,
                        });
                    }
                }
                if event == AiLoopEvent::TurnInterrupted {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(AiLoopState::Paused)],
                            transition_index: 1,
                            has_actions: false,
                            is_internal: true,
                        });
                    }
                }
                if event == AiLoopEvent::SessionLost {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                AiLoopState::Restarting,
                            )],
                            transition_index: 2,
                            has_actions: false,
                            is_internal: true,
                        });
                    }
                }
                None
            }
            AiLoopState::Judging => {
                if event == AiLoopEvent::Judge || event == AiLoopEvent::JudgeBegin {
                    if self.safe_evaluate_guard("_scxml_truthy(_event.data.done)", engine) {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                AiLoopState::Closing,
                            )],
                            transition_index: 0,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                if event == AiLoopEvent::Judge || event == AiLoopEvent::JudgeBegin {
                    if self.safe_evaluate_guard("(turns_since_reflect >= reflect_every)", engine) {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                AiLoopState::Reflecting,
                            )],
                            transition_index: 1,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                if event == AiLoopEvent::Judge || event == AiLoopEvent::JudgeBegin {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                AiLoopState::Working,
                            )],
                            transition_index: 2,
                            has_actions: true,
                            is_internal: false,
                        });
                    }
                }
                None
            }
            AiLoopState::Paused => {
                if event == AiLoopEvent::TurnDone {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                AiLoopState::Judging,
                            )],
                            transition_index: 0,
                            has_actions: true,
                            is_internal: false,
                        });
                    }
                }
                if event == AiLoopEvent::TurnInterrupted {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(AiLoopState::Paused)],
                            transition_index: 1,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                if event == AiLoopEvent::Resume {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::History(
                                AiLoopHistory::Where,
                            )],
                            transition_index: 2,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                if event == AiLoopEvent::Unattended {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                AiLoopState::Abandoned,
                            )],
                            transition_index: 3,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                None
            }
            AiLoopState::Priming => {
                if event == AiLoopEvent::PromptSent {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                AiLoopState::Working,
                            )],
                            transition_index: 0,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                None
            }
            AiLoopState::Rebuilding => {
                if event == AiLoopEvent::SessionReady {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(AiLoopState::Alive)],
                            transition_index: 0,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                None
            }
            AiLoopState::Reflecting => {
                if event == AiLoopEvent::ReflectApplied {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                AiLoopState::Restarting,
                            )],
                            transition_index: 0,
                            has_actions: true,
                            is_internal: false,
                        });
                    }
                }
                if event == AiLoopEvent::ReflectNone {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                AiLoopState::Working,
                            )],
                            transition_index: 1,
                            has_actions: true,
                            is_internal: false,
                        });
                    }
                }
                None
            }
            AiLoopState::Restarting => {
                if event == AiLoopEvent::SessionReady {
                    if self.safe_evaluate_guard("(restarts > max_restarts)", engine) {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(AiLoopState::Stuck)],
                            transition_index: 0,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                if event == AiLoopEvent::SessionReady {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                AiLoopState::Priming,
                            )],
                            transition_index: 1,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                None
            }
            AiLoopState::Run => {
                if event == AiLoopEvent::RunConverged {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                AiLoopState::Converged,
                            )],
                            transition_index: 0,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                if event == AiLoopEvent::RunExhausted {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                AiLoopState::Exhausted,
                            )],
                            transition_index: 1,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                if event == AiLoopEvent::RunBlocked {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                AiLoopState::Blocked,
                            )],
                            transition_index: 2,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                if event == AiLoopEvent::Fail {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(AiLoopState::Failed)],
                            transition_index: 3,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                if event == AiLoopEvent::Cancel {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                AiLoopState::Cancelled,
                            )],
                            transition_index: 4,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                None
            }
            AiLoopState::Screening => {
                if event == AiLoopEvent::ScreenMatched {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                AiLoopState::Working,
                            )],
                            transition_index: 0,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                if event == AiLoopEvent::ScreenNone {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(AiLoopState::Paused)],
                            transition_index: 1,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                None
            }
            AiLoopState::Within => {
                if event == AiLoopEvent::TurnDone {
                    if self.safe_evaluate_guard("(_scxml_add(turns, 1) >= max_turns)", engine) {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(AiLoopState::Spent)],
                            transition_index: 0,
                            has_actions: true,
                            is_internal: false,
                        });
                    }
                }
                if event == AiLoopEvent::TurnDone {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(AiLoopState::Within)],
                            transition_index: 1,
                            has_actions: true,
                            is_internal: false,
                        });
                    }
                }
                None
            }
            AiLoopState::Working => {
                if event == AiLoopEvent::TurnDone {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                AiLoopState::Judging,
                            )],
                            transition_index: 0,
                            has_actions: true,
                            is_internal: false,
                        });
                    }
                }
                if event == AiLoopEvent::TurnBlocked {
                    {
                        return Some(::sce_rust_runtime::EnabledTransition {
                            source: state,
                            targets: &[::sce_rust_runtime::EntryTarget::State(
                                AiLoopState::Screening,
                            )],
                            transition_index: 1,
                            has_actions: false,
                            is_internal: false,
                        });
                    }
                }
                None
            }
            _ => None,
        }
    }

    // W3C SCXML 3.13: a transition's executable content, run by the engine
    // between the microstep's exits and its entries.
    #[doc = "SCE-MAP: ai_loop.scxml:155 :: _machine"]
    // SCE-MAP: ai_loop.scxml:155 :: _machine
    fn execute_transition_content(
        &mut self,
        source: Self::State,
        transition_index: usize,
        engine: &mut sce_rust_runtime::Engine<Self>,
    ) {
        match source {
            AiLoopState::Judging => {
                match transition_index {
                    2 => {
                        // SCE-MAP: ai_loop.scxml:362 :: judging :: _transition_2
                        // W3C SCXML 3.13: Transition 2 actions

                        {
                            let send_id = ::sce_rust_runtime::sce_string_from_str("__send_2");

                            // W3C SCXML 6.2 / test178: a name may repeat and every value must be
                            // delivered, so each name carries a vector. The typed value is kept
                            // rather than its text — a receiver reading `_event.data.value === 42`
                            // finds the string "42" unequal.
                            //
                            // Declared out here rather than inside the payload block because
                            // §scxml-6.2.3 evaluates a `<send>`'s arguments ONCE, and the transports
                            // below are renderings of that one evaluation: the BasicHTTP and
                            // host-served arms read this map instead of asking the data model again.
                            // While it was block-scoped they had to, and what they re-read was
                            // `<param>` alone — so `namelist="Var1"` reached `_event.data` and then
                            // posted zero form parameters, against §scxml-C-2.
                            let mut _send_wire_params: ::std::collections::BTreeMap<
                                String,
                                Vec<::sce_rust_runtime::ScriptValue>,
                            > = ::std::collections::BTreeMap::new();
                            // W3C SCXML 6.2: Evaluate <param>/namelist expressions at send time
                            let event_data_string: String = {
                                self.ensure_script_engine();
                                let sid = self.session_id.as_ref().unwrap().clone();
                                let se = self.script_engine.clone();
                                let se: &dyn sce_rust_runtime::IScriptEngine = &*se;
                                let wire_params = &mut _send_wire_params;
                                match se.evaluate_expression(&sid, "turn_prompt") {
                                    Ok(val) => {
                                        wire_params
                                            .entry("text".to_string())
                                            .or_default()
                                            .push(val);
                                    }
                                    Err(e) => {
                                        ::sce_rust_runtime::sce_log_error!(
                                            "send param 'text' eval failed: {}",
                                            e
                                        );
                                        engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(AiLoopEvent::ErrorExecution, "<send> <param name='text'> expr failed to evaluate"));
                                    }
                                }
                                ::sce_rust_runtime::helpers::event_data::build_json_from_typed_params(wire_params)
                            };
                            let event_data: &str = &event_data_string;

                            // W3C SCXML 6.2.5: "x-sce-host" is served by the host,
                            // which declared it to this build. Dispatch rather than refuse.
                            {
                                let host_params = ::sce_rust_runtime::helpers::event_data::typed_params_to_wire_strings(&_send_wire_params);
                                let __sce_request = sce_rust_runtime::HostSendRequest {
                                    processor_type: "x-sce-host".to_string(),
                                    event_name: "prompt.turn".to_string(),
                                    target: "".to_string(),
                                    content: "".to_string(),
                                    params: host_params,
                                    send_id: send_id.to_string(),
                                };
                                let __sce_served = engine.perform_host_send(__sce_request);
                                // W3C SCXML 6.2: a declared type with no handler registered is,
                                // from the document's side, a processor the platform does not
                                // support — the act it asked for was performed by nobody. Same
                                // event, so a wiring mistake cannot read as success.
                                if __sce_served.is_none()
                                    && !engine.has_event_processor("x-sce-host")
                                {
                                    let mut err_meta = sce_rust_runtime::EventWithMetadata::platform_error(AiLoopEvent::ErrorExecution, "<send type='x-sce-host'> names a processor the host declared but never registered");
                                    err_meta.metadata.send_id = send_id.clone();
                                    engine.raise(err_meta);
                                }
                            }

                            let _ = send_id; // suppress unused warning when no send operation
                            let _ = event_data; // suppress unused warning in branches that skip dispatch
                        }
                    }
                    _ => {}
                }
            }
            AiLoopState::Paused => {
                match transition_index {
                    0 => {
                        // SCE-MAP: ai_loop.scxml:468 :: paused :: _transition_0
                        // W3C SCXML 3.13: Transition 0 actions

                        {
                            // W3C SCXML 5.3: <assign location="turns_since_reflect">
                            self.ensure_script_engine();
                            let sid = self.session_id.as_ref().unwrap().clone();
                            let se = self.script_engine.clone();
                            let se: &dyn sce_rust_runtime::IScriptEngine = &*se;
                            let expr = "_scxml_add(turns_since_reflect, 1)";
                            // W3C SCXML 5.3: Assign via execute_script preserves Lua reference identity for
                            // table values (e.g. `Var2 = _event` — test 329 requires `Var2 == _event`). Going
                            // through evaluate_expression + set_variable would round-trip through ScriptValue
                            // and create a fresh table, breaking reference equality.
                            let assign_script = format!("{} = {}", "turns_since_reflect", expr);
                            if let Err(e) = se.execute_script(&sid, &assign_script) {
                                ::sce_rust_runtime::sce_log_error!(
                                    "Assign failed for 'turns_since_reflect': {}",
                                    e
                                );
                                engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(
                                    AiLoopEvent::ErrorExecution,
                                    "<assign> to 'turns_since_reflect' failed",
                                ));
                            }
                        }
                    }
                    _ => {}
                }
            }
            AiLoopState::Reflecting => {
                match transition_index {
                    0 => {
                        // SCE-MAP: ai_loop.scxml:379 :: reflecting :: _transition_0
                        // W3C SCXML 3.13: Transition 0 actions

                        {
                            // W3C SCXML 5.3: <assign location="start_prompt">
                            self.ensure_script_engine();
                            let sid = self.session_id.as_ref().unwrap().clone();
                            let se = self.script_engine.clone();
                            let se: &dyn sce_rust_runtime::IScriptEngine = &*se;
                            let expr = "_event.data.start_prompt";
                            // W3C SCXML 5.3: Assign via execute_script preserves Lua reference identity for
                            // table values (e.g. `Var2 = _event` — test 329 requires `Var2 == _event`). Going
                            // through evaluate_expression + set_variable would round-trip through ScriptValue
                            // and create a fresh table, breaking reference equality.
                            let assign_script = format!("{} = {}", "start_prompt", expr);
                            if let Err(e) = se.execute_script(&sid, &assign_script) {
                                ::sce_rust_runtime::sce_log_error!(
                                    "Assign failed for 'start_prompt': {}",
                                    e
                                );
                                engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(
                                    AiLoopEvent::ErrorExecution,
                                    "<assign> to 'start_prompt' failed",
                                ));
                            }
                        }

                        {
                            // W3C SCXML 5.3: <assign location="turn_prompt">
                            self.ensure_script_engine();
                            let sid = self.session_id.as_ref().unwrap().clone();
                            let se = self.script_engine.clone();
                            let se: &dyn sce_rust_runtime::IScriptEngine = &*se;
                            let expr = "_event.data.turn_prompt";
                            // W3C SCXML 5.3: Assign via execute_script preserves Lua reference identity for
                            // table values (e.g. `Var2 = _event` — test 329 requires `Var2 == _event`). Going
                            // through evaluate_expression + set_variable would round-trip through ScriptValue
                            // and create a fresh table, breaking reference equality.
                            let assign_script = format!("{} = {}", "turn_prompt", expr);
                            if let Err(e) = se.execute_script(&sid, &assign_script) {
                                ::sce_rust_runtime::sce_log_error!(
                                    "Assign failed for 'turn_prompt': {}",
                                    e
                                );
                                engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(
                                    AiLoopEvent::ErrorExecution,
                                    "<assign> to 'turn_prompt' failed",
                                ));
                            }
                        }

                        {
                            // W3C SCXML 5.3: <assign location="milestone">
                            self.ensure_script_engine();
                            let sid = self.session_id.as_ref().unwrap().clone();
                            let se = self.script_engine.clone();
                            let se: &dyn sce_rust_runtime::IScriptEngine = &*se;
                            let expr = "_event.data.milestone";
                            // W3C SCXML 5.3: Assign via execute_script preserves Lua reference identity for
                            // table values (e.g. `Var2 = _event` — test 329 requires `Var2 == _event`). Going
                            // through evaluate_expression + set_variable would round-trip through ScriptValue
                            // and create a fresh table, breaking reference equality.
                            let assign_script = format!("{} = {}", "milestone", expr);
                            if let Err(e) = se.execute_script(&sid, &assign_script) {
                                ::sce_rust_runtime::sce_log_error!(
                                    "Assign failed for 'milestone': {}",
                                    e
                                );
                                engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(
                                    AiLoopEvent::ErrorExecution,
                                    "<assign> to 'milestone' failed",
                                ));
                            }
                        }
                    }
                    1 => {
                        // SCE-MAP: ai_loop.scxml:385 :: reflecting :: _transition_1
                        // W3C SCXML 3.13: Transition 1 actions

                        {
                            let send_id = ::sce_rust_runtime::sce_string_from_str("__send_4");

                            // W3C SCXML 6.2 / test178: a name may repeat and every value must be
                            // delivered, so each name carries a vector. The typed value is kept
                            // rather than its text — a receiver reading `_event.data.value === 42`
                            // finds the string "42" unequal.
                            //
                            // Declared out here rather than inside the payload block because
                            // §scxml-6.2.3 evaluates a `<send>`'s arguments ONCE, and the transports
                            // below are renderings of that one evaluation: the BasicHTTP and
                            // host-served arms read this map instead of asking the data model again.
                            // While it was block-scoped they had to, and what they re-read was
                            // `<param>` alone — so `namelist="Var1"` reached `_event.data` and then
                            // posted zero form parameters, against §scxml-C-2.
                            let mut _send_wire_params: ::std::collections::BTreeMap<
                                String,
                                Vec<::sce_rust_runtime::ScriptValue>,
                            > = ::std::collections::BTreeMap::new();
                            // W3C SCXML 6.2: Evaluate <param>/namelist expressions at send time
                            let event_data_string: String = {
                                self.ensure_script_engine();
                                let sid = self.session_id.as_ref().unwrap().clone();
                                let se = self.script_engine.clone();
                                let se: &dyn sce_rust_runtime::IScriptEngine = &*se;
                                let wire_params = &mut _send_wire_params;
                                match se.evaluate_expression(&sid, "turn_prompt") {
                                    Ok(val) => {
                                        wire_params
                                            .entry("text".to_string())
                                            .or_default()
                                            .push(val);
                                    }
                                    Err(e) => {
                                        ::sce_rust_runtime::sce_log_error!(
                                            "send param 'text' eval failed: {}",
                                            e
                                        );
                                        engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(AiLoopEvent::ErrorExecution, "<send> <param name='text'> expr failed to evaluate"));
                                    }
                                }
                                ::sce_rust_runtime::helpers::event_data::build_json_from_typed_params(wire_params)
                            };
                            let event_data: &str = &event_data_string;

                            // W3C SCXML 6.2.5: "x-sce-host" is served by the host,
                            // which declared it to this build. Dispatch rather than refuse.
                            {
                                let host_params = ::sce_rust_runtime::helpers::event_data::typed_params_to_wire_strings(&_send_wire_params);
                                let __sce_request = sce_rust_runtime::HostSendRequest {
                                    processor_type: "x-sce-host".to_string(),
                                    event_name: "prompt.turn".to_string(),
                                    target: "".to_string(),
                                    content: "".to_string(),
                                    params: host_params,
                                    send_id: send_id.to_string(),
                                };
                                let __sce_served = engine.perform_host_send(__sce_request);
                                // W3C SCXML 6.2: a declared type with no handler registered is,
                                // from the document's side, a processor the platform does not
                                // support — the act it asked for was performed by nobody. Same
                                // event, so a wiring mistake cannot read as success.
                                if __sce_served.is_none()
                                    && !engine.has_event_processor("x-sce-host")
                                {
                                    let mut err_meta = sce_rust_runtime::EventWithMetadata::platform_error(AiLoopEvent::ErrorExecution, "<send type='x-sce-host'> names a processor the host declared but never registered");
                                    err_meta.metadata.send_id = send_id.clone();
                                    engine.raise(err_meta);
                                }
                            }

                            let _ = send_id; // suppress unused warning when no send operation
                            let _ = event_data; // suppress unused warning in branches that skip dispatch
                        }
                    }
                    _ => {}
                }
            }
            AiLoopState::Within => {
                match transition_index {
                    0 => {
                        // SCE-MAP: ai_loop.scxml:522 :: within :: _transition_0
                        // W3C SCXML 3.13: Transition 0 actions

                        {
                            // W3C SCXML 5.3: <assign location="turns">
                            self.ensure_script_engine();
                            let sid = self.session_id.as_ref().unwrap().clone();
                            let se = self.script_engine.clone();
                            let se: &dyn sce_rust_runtime::IScriptEngine = &*se;
                            let expr = "_scxml_add(turns, 1)";
                            // W3C SCXML 5.3: Assign via execute_script preserves Lua reference identity for
                            // table values (e.g. `Var2 = _event` — test 329 requires `Var2 == _event`). Going
                            // through evaluate_expression + set_variable would round-trip through ScriptValue
                            // and create a fresh table, breaking reference equality.
                            let assign_script = format!("{} = {}", "turns", expr);
                            if let Err(e) = se.execute_script(&sid, &assign_script) {
                                ::sce_rust_runtime::sce_log_error!(
                                    "Assign failed for 'turns': {}",
                                    e
                                );
                                engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(
                                    AiLoopEvent::ErrorExecution,
                                    "<assign> to 'turns' failed",
                                ));
                            }
                        }
                    }
                    1 => {
                        // SCE-MAP: ai_loop.scxml:525 :: within :: _transition_1
                        // W3C SCXML 3.13: Transition 1 actions

                        {
                            // W3C SCXML 5.3: <assign location="turns">
                            self.ensure_script_engine();
                            let sid = self.session_id.as_ref().unwrap().clone();
                            let se = self.script_engine.clone();
                            let se: &dyn sce_rust_runtime::IScriptEngine = &*se;
                            let expr = "_scxml_add(turns, 1)";
                            // W3C SCXML 5.3: Assign via execute_script preserves Lua reference identity for
                            // table values (e.g. `Var2 = _event` — test 329 requires `Var2 == _event`). Going
                            // through evaluate_expression + set_variable would round-trip through ScriptValue
                            // and create a fresh table, breaking reference equality.
                            let assign_script = format!("{} = {}", "turns", expr);
                            if let Err(e) = se.execute_script(&sid, &assign_script) {
                                ::sce_rust_runtime::sce_log_error!(
                                    "Assign failed for 'turns': {}",
                                    e
                                );
                                engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(
                                    AiLoopEvent::ErrorExecution,
                                    "<assign> to 'turns' failed",
                                ));
                            }
                        }
                    }
                    _ => {}
                }
            }
            AiLoopState::Working => {
                match transition_index {
                    0 => {
                        // SCE-MAP: ai_loop.scxml:311 :: working :: _transition_0
                        // W3C SCXML 3.13: Transition 0 actions

                        {
                            // W3C SCXML 5.3: <assign location="turns_since_reflect">
                            self.ensure_script_engine();
                            let sid = self.session_id.as_ref().unwrap().clone();
                            let se = self.script_engine.clone();
                            let se: &dyn sce_rust_runtime::IScriptEngine = &*se;
                            let expr = "_scxml_add(turns_since_reflect, 1)";
                            // W3C SCXML 5.3: Assign via execute_script preserves Lua reference identity for
                            // table values (e.g. `Var2 = _event` — test 329 requires `Var2 == _event`). Going
                            // through evaluate_expression + set_variable would round-trip through ScriptValue
                            // and create a fresh table, breaking reference equality.
                            let assign_script = format!("{} = {}", "turns_since_reflect", expr);
                            if let Err(e) = se.execute_script(&sid, &assign_script) {
                                ::sce_rust_runtime::sce_log_error!(
                                    "Assign failed for 'turns_since_reflect': {}",
                                    e
                                );
                                engine.raise(sce_rust_runtime::EventWithMetadata::platform_error(
                                    AiLoopEvent::ErrorExecution,
                                    "<assign> to 'turns_since_reflect' failed",
                                ));
                            }
                        }
                    }
                    _ => {}
                }
            }
            _ => {}
        }
    }
    // W3C SCXML 5.2/5.3: Datamodel initialization with error.execution support
    // Delegates to inherent impl method (matches C++ initializeDataModel pattern)
    fn initialize_data_model(&mut self, engine: &mut Engine<Self>) {
        self.do_initialize_data_model(engine);
    }
}
